//@strict-types
//@skip-check bsl-variable-name-invalid
//@skip-check method-too-many-params

////////////////////////////////////////////////////////////////////////////////
// Copyright 2024 ПАО СКБ Контур
// Лицензировано согласно Apache License, Версия 2.0 (далее - "Лицензия");
// вы не можете использовать этот файл иначе, чем в соответствии с Лицензией.
// Вы можете получить копию Лицензии по следующему адресу:
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Если это не требуется действующим законодательством или не согласовано в
// письменной форме, программное обеспечение, распространяемое на основании
// Лицензии, предоставляется "КАК ЕСТЬ", БЕЗ КАКИХ-ЛИБО ГАРАНТИЙ ИЛИ УСЛОВИЙ,
// явных или подразумеваемых. См. Лицензию для получения более подробной
// информации о правах и ограничениях, установленных Лицензией.
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Программный интерфейс

// Расчитать хеш MD5.
// 
// Параметры:
//  ДанныеДляХеширования - ДвоичныеДанные - Двоичные данные для хеширования
//                       - Строка - Строка для хеширования
// 
// Возвращаемое значение:
//  Строка - хеш MD5 в нижнем регистре
//  
Функция MD5Лок(Знач ДанныеДляХеширования) Экспорт
	
	СтруктураПеременных = ИнициализацияСтруктурыПеременных();

	Если ТипЗнч(ДанныеДляХеширования) = Тип("Строка") Тогда
		
		//@skip-check statement-type-change
		ДанныеДляХеширования = КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьДвоичныеДанныеИзСтрокиЛок(
			ДанныеДляХеширования);
		
	КонецЕсли;

	СтруктураЧтениеДанных = КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ЧтениеДанных(ДанныеДляХеширования);

	Расчет(СтруктураПеременных, СтруктураЧтениеДанных);
	СтруктураПеременных.A = 0;
	СтруктураПеременных.B = 0;
	СтруктураПеременных.C = 0;
	СтруктураПеременных.D = 0;

	Для i = 0 По 31 Цикл
		
		Степ = СтруктураПеременных.Степень[i];
		
		Если СтруктураПеременных.A_[i] Тогда
			
			СтруктураПеременных.A = СтруктураПеременных.A + Степ;
			
		КонецЕсли;

		Если СтруктураПеременных.B_[i] Тогда
			
			СтруктураПеременных.B = СтруктураПеременных.B + Степ;
			
		КонецЕсли;

		Если СтруктураПеременных.C_[i] Тогда
			
			СтруктураПеременных.C = СтруктураПеременных.C + Степ;
			
		КонецЕсли;

		Если СтруктураПеременных.D_[i] Тогда
			
			СтруктураПеременных.D = СтруктураПеременных.D + Степ;
			
		КонецЕсли;
		
	КонецЦикла;

	Возврат НРег(КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.Dec2Hex(СтруктураПеременных.A, 8, Истина)
			+ КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.Dec2Hex(СтруктураПеременных.B, 8, Истина)
			+ КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.Dec2Hex(СтруктураПеременных.C, 8, Истина)
			+ КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.Dec2Hex(СтруктураПеременных.D, 8, Истина));
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Служебные процедуры и функции

Процедура FF(a, b, c, d, X, s, t, СтруктураПеременных)
	
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._AND(b, c, СтруктураПеременных.EAX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._NOT(b, СтруктураПеременных.EBX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._AND(СтруктураПеременных.EBX, d, СтруктураПеременных.ECX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._OR(СтруктураПеременных.EAX, СтруктураПеременных.ECX,
		СтруктураПеременных.EBX);

	X1 = X + t;
	
	Если X1 >= СтруктураПеременных.Stepen32 Тогда
		
		X1 = X1 - СтруктураПеременных.Stepen32;
		
	КонецЕсли;
	
	Для инд = 0 По 31 Цикл
		
		инд1 = 31 - инд;
		Степ = СтруктураПеременных.Степень[инд1];
		
		Если X1 >= Степ Тогда
			
			X1 = X1 - Степ;
			СтруктураПеременных.EDX[инд1] = Истина;
			
		Иначе
			
			СтруктураПеременных.EDX[инд1] = Ложь;
			
		КонецЕсли;
		
	КонецЦикла;
	
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.EBX, a, СтруктураПеременных.ECX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.ECX, СтруктураПеременных.EDX,
		СтруктураПеременных.EAX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ROL(СтруктураПеременных.EAX, s, СтруктураПеременных.ECX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.ECX, b, a);
	
КонецПроцедуры

Процедура GG(a, b, c, d, X, s, t, СтруктураПеременных)
	
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._AND(b, d, СтруктураПеременных.EAX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._NOT(d, СтруктураПеременных.EDX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._AND(СтруктураПеременных.EDX, c, СтруктураПеременных.ECX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._OR(СтруктураПеременных.EAX, СтруктураПеременных.ECX,
		СтруктураПеременных.EBX);

	X1 = X + t;
	
	Если X1 >= СтруктураПеременных.Stepen32 Тогда
		
		X1 = X1 - СтруктураПеременных.Stepen32;
		
	КонецЕсли;

	Для инд = 0 По 31 Цикл
		
		инд1 = 31 - инд;
		Степ = СтруктураПеременных.Степень[инд1];
		
		Если X1 >= Степ Тогда
			
			X1 = X1 - Степ;
			СтруктураПеременных.EDX[инд1] = Истина;
			
		Иначе
			
			СтруктураПеременных.EDX[инд1] = Ложь;
			
		КонецЕсли;
		
	КонецЦикла;
	
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.EBX, a, СтруктураПеременных.ECX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.ECX, СтруктураПеременных.EDX,
		СтруктураПеременных.EAX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ROL(СтруктураПеременных.EAX, s, СтруктураПеременных.ECX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.ECX, b, a);
	
КонецПроцедуры

Процедура HH(a, b, c, d, X, s, t, СтруктураПеременных)
	
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._XOR(b, c, СтруктураПеременных.EAX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._XOR(СтруктураПеременных.EAX, d, СтруктураПеременных.EBX);

	X1 = X + t;
	
	Если X1 >= СтруктураПеременных.Stepen32 Тогда
		
		X1 = X1 - СтруктураПеременных.Stepen32;
		
	КонецЕсли;

	Для Инд = 0 По 31 Цикл
		
		инд1 = 31 - Инд;
		Степ = СтруктураПеременных.Степень[инд1];
		
		Если X1 >= Степ Тогда
			
			X1 = X1 - Степ;
			СтруктураПеременных.EDX[инд1] = Истина;
			
		Иначе
			
			СтруктураПеременных.EDX[инд1] = Ложь;
			
		КонецЕсли;
		
	КонецЦикла;

	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.EBX, a, СтруктураПеременных.ECX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.ECX, СтруктураПеременных.EDX,
		СтруктураПеременных.EAX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ROL(СтруктураПеременных.EAX, s, СтруктураПеременных.ECX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.ECX, b, a);

КонецПроцедуры

Процедура II(a, b, c, d, X, s, t, СтруктураПеременных)
	
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._NOT(d, СтруктураПеременных.EAX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._OR(СтруктураПеременных.EAX, b, СтруктураПеременных.ECX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._XOR(СтруктураПеременных.ECX, c, СтруктураПеременных.EBX);

	X1 = X + t;
	
	Если X1 >= СтруктураПеременных.Stepen32 Тогда
		
		X1 = X1 - СтруктураПеременных.Stepen32;
		
	КонецЕсли;

	Для Инд = 0 По 31 Цикл
		
		инд1 = 31 - Инд;
		Степ = СтруктураПеременных.Степень[инд1];
		
		Если X1 >= Степ Тогда
			
			X1 = X1 - Степ;
			СтруктураПеременных.EDX[инд1] = Истина;
			
		Иначе
			
			СтруктураПеременных.EDX[инд1] = Ложь;
			
		КонецЕсли;
		
	КонецЦикла;

	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.EBX, a, СтруктураПеременных.ECX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.ECX, СтруктураПеременных.EDX, СтруктураПеременных.EAX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ROL(СтруктураПеременных.EAX, s, СтруктураПеременных.ECX);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.ECX, b, a);
	
КонецПроцедуры

Процедура MD5Hash(СтруктураПеременных)
	
	A1 = КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьПустойМассивБулево(32);
	B1 = КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьПустойМассивБулево(32);
	C1 = КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьПустойМассивБулево(32);
	D1 = КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьПустойМассивБулево(32);

	Для i = 0 По 31 Цикл
		
		A1[i] = СтруктураПеременных.A_[i];
		B1[i] = СтруктураПеременных.B_[i];
		C1[i] = СтруктураПеременных.C_[i];
		D1[i] = СтруктураПеременных.D_[i];
		
	КонецЦикла;
 
	// --------------------------------------------------------------------------
	// Round 1.
	// --------------------------------------------------------------------------
	FF(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[0], СтруктураПеременных.S11, 3614090360, СтруктураПеременных); // Step 1
	FF(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[1], СтруктураПеременных.S12, 3905402710, СтруктураПеременных); // Step 2
	FF(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[2], СтруктураПеременных.S13, 606105819, СтруктураПеременных); // Step 3
	FF(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[3], СтруктураПеременных.S14, 3250441966, СтруктураПеременных); // Step 4
	FF(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[4], СтруктураПеременных.S11, 4118548399, СтруктураПеременных); // Step 5
	FF(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[5], СтруктураПеременных.S12, 1200080426, СтруктураПеременных); // Step 6
	FF(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[6], СтруктураПеременных.S13, 2821735955, СтруктураПеременных); // Step 7
	FF(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[7], СтруктураПеременных.S14, 4249261313, СтруктураПеременных); // Step 8
	FF(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[8], СтруктураПеременных.S11, 1770035416, СтруктураПеременных); // Step 9
	FF(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[9], СтруктураПеременных.S12, 2336552879, СтруктураПеременных); // Step 10
	FF(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[10], СтруктураПеременных.S13, 4294925233, СтруктураПеременных); // Step 11
	FF(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[11], СтруктураПеременных.S14, 2304563134, СтруктураПеременных); // Step 12
	FF(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[12], СтруктураПеременных.S11, 1804603682, СтруктураПеременных); // Step 13
	FF(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[13], СтруктураПеременных.S12, 4254626195, СтруктураПеременных); // Step 14
	FF(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[14], СтруктураПеременных.S13, 2792965006, СтруктураПеременных); // Step 15
	FF(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[15], СтруктураПеременных.S14, 1236535329, СтруктураПеременных); // Step 16
// --------------------------------------------------------------------------
// Round 2.
// --------------------------------------------------------------------------
	GG(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[1], СтруктураПеременных.S21, 4129170786, СтруктураПеременных); // Step 17
	GG(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[6], СтруктураПеременных.S22, 3225465664, СтруктураПеременных); // Step 18
	GG(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[11], СтруктураПеременных.S23, 643717713, СтруктураПеременных); // Step 19
	GG(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[0], СтруктураПеременных.S24, 3921069994, СтруктураПеременных); // Step 20
	GG(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[5], СтруктураПеременных.S21, 3593408605, СтруктураПеременных); // Step 21
	GG(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[10], СтруктураПеременных.S22, 38016083, СтруктураПеременных); // Step 22 
	GG(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[15], СтруктураПеременных.S23, 3634488961, СтруктураПеременных); // Step 23
	GG(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[4], СтруктураПеременных.S24, 3889429448, СтруктураПеременных); // Step 24
	GG(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[9], СтруктураПеременных.S21, 568446438, СтруктураПеременных); // Step 25
	GG(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[14], СтруктураПеременных.S22, 3275163606, СтруктураПеременных); // Step 26
	GG(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[3], СтруктураПеременных.S23, 4107603335, СтруктураПеременных); // Step 27
	GG(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[8], СтруктураПеременных.S24, 1163531501, СтруктураПеременных); // Step 28
	GG(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[13], СтруктураПеременных.S21, 2850285829, СтруктураПеременных); // Step 29
	GG(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[2], СтруктураПеременных.S22, 4243563512, СтруктураПеременных); // Step 30
	GG(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[7], СтруктураПеременных.S23, 1735328473, СтруктураПеременных); // Step 31
	GG(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[12], СтруктураПеременных.S24, 2368359562, СтруктураПеременных); // Step 32
// --------------------------------------------------------------------------
// Round 3.
// --------------------------------------------------------------------------
	HH(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[5], СтруктураПеременных.S31, 4294588738, СтруктураПеременных); // Step 33
	HH(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[8], СтруктураПеременных.S32, 2272392833, СтруктураПеременных); // Step 34
	HH(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[11], СтруктураПеременных.S33, 1839030562, СтруктураПеременных); // Step 35
	HH(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[14], СтруктураПеременных.S34, 4259657740, СтруктураПеременных); // Step 36
	HH(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[1], СтруктураПеременных.S31, 2763975236, СтруктураПеременных); // Step 37
	HH(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[4], СтруктураПеременных.S32, 1272893353, СтруктураПеременных); // Step 38
	HH(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[7], СтруктураПеременных.S33, 4139469664, СтруктураПеременных); // Step 39
	HH(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[10], СтруктураПеременных.S34, 3200236656, СтруктураПеременных); // Step 40
	HH(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[13], СтруктураПеременных.S31, 681279174, СтруктураПеременных); // Step 41
	HH(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[0], СтруктураПеременных.S32, 3936430074, СтруктураПеременных); // Step 42
	HH(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[3], СтруктураПеременных.S33, 3572445317, СтруктураПеременных); // Step 43
	HH(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[6], СтруктураПеременных.S34, 76029189, СтруктураПеременных); // Step 44
	HH(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[9], СтруктураПеременных.S31, 3654602809, СтруктураПеременных); // Step 45
	HH(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[12], СтруктураПеременных.S32, 3873151461, СтруктураПеременных); // Step 46
	HH(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[15], СтруктураПеременных.S33, 530742520, СтруктураПеременных); // Step 47
	HH(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[2], СтруктураПеременных.S34, 3299628645, СтруктураПеременных); // Step 48
// --------------------------------------------------------------------------
// Round 4.
// --------------------------------------------------------------------------
	II(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[0], СтруктураПеременных.S41, 4096336452, СтруктураПеременных); // Step 49
	II(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[7], СтруктураПеременных.S42, 1126891415, СтруктураПеременных); // Step 50
	II(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[14], СтруктураПеременных.S43, 2878612391, СтруктураПеременных); // Step 51
	II(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[5], СтруктураПеременных.S44, 4237533241, СтруктураПеременных); // Step 52
	II(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[12], СтруктураПеременных.S41, 1700485571, СтруктураПеременных); // Step 53
	II(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[3], СтруктураПеременных.S42, 2399980690, СтруктураПеременных); // Step 54
	II(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[10], СтруктураПеременных.S43, 4293915773, СтруктураПеременных); // Step 55
	II(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[1], СтруктураПеременных.S44, 2240044497, СтруктураПеременных); // Step 56
	II(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[8], СтруктураПеременных.S41, 1873313359, СтруктураПеременных); // Step 57
	II(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[15], СтруктураПеременных.S42, 4264355552, СтруктураПеременных); // Step 58
	II(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[6], СтруктураПеременных.S43, 2734768916, СтруктураПеременных); // Step 59
	II(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[13], СтруктураПеременных.S44, 1309151649, СтруктураПеременных); // Step 60
	II(СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_,
		СтруктураПеременных.X[4], СтруктураПеременных.S41, 4149444226, СтруктураПеременных); // Step 61
	II(СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_, СтруктураПеременных.c_,
		СтруктураПеременных.X[11], СтруктураПеременных.S42, 3174756917, СтруктураПеременных); // Step 62
	II(СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_, СтруктураПеременных.b_,
		СтруктураПеременных.X[2], СтруктураПеременных.S43, 718787259, СтруктураПеременных); // Step 63
	II(СтруктураПеременных.b_, СтруктураПеременных.c_, СтруктураПеременных.d_, СтруктураПеременных.a_,
		СтруктураПеременных.X[9], СтруктураПеременных.S44, 3951481745, СтруктураПеременных); // Step 64
// --------------------------------------------------------------------------

	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.A_, A1, СтруктураПеременных.A_);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.B_, B1, СтруктураПеременных.B_);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.C_, C1, СтруктураПеременных.C_);
	КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный._ADD(СтруктураПеременных.D_, D1, СтруктураПеременных.D_);
	
КонецПроцедуры

Процедура Расчет(СтруктураПеременных, СтруктураЧтениеДанных)
	
	Мас = ПолучитьПустойМассивЧисло(64);
	Длина = КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.РазмерПотока(
		КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ИсходныйПотокЧтениеДанных(СтруктураЧтениеДанных));

	// Определение Хэш кодов 64 байтных блоков
	ДлСтр = Длина;
	
	Пока ДлСтр >= 64 Цикл
		
		Для i = 0 По 15 Цикл
			
			СтруктураПеременных.X[i] = КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПрочитатьЦелое32ЧтениеДанных(
				СтруктураЧтениеДанных);
			
		КонецЦикла;

		MD5Hash(СтруктураПеременных);

		ДлСтр = ДлСтр - 64;
		
	КонецЦикла;
	
	// Финал - определение Хэш кода последнего блока
	
	Если Длина <> -1 Тогда  // Если Длина = -1 то значит это не последний блок
	
		ДлинаБит = Длина * 8;
		
		Для i = 0 По ДлСтр - 1 Цикл
			
			Мас[i] = КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПрочитатьБайтЧтениеДанных(СтруктураЧтениеДанных);
			
		КонецЦикла;
		
		Мас[ДлСтр] = 128;
		Длина = ДлСтр + 1;
		
		// В последнем блоке нет свободных 8 байт для длины
		// Дополняем блок до 64 байт
		
		Если Длина > 56 Тогда
			
			Для i = Длина По 63 Цикл
				
				Мас[i] = 0;
				
			КонецЦикла;
			
			i1 = 0;
			
			Для i = 0 По 15 Цикл
				
				СтруктураПеременных.X[i] = Мас[i1] + Мас[i1 + 1] * 256 + Мас[i1 + 2] * 65536 + Мас[i1 + 3] * 16777216;
				i1 = i1 + 4;
				
			КонецЦикла;
			
			MD5Hash(СтруктураПеременных);
			Длина = 0;
			
		КонецЕсли;
		
		// Дополняем блок до 56 байт и заносим длину в битах
		
		Для i = Длина По 55 Цикл
			
			Мас[i] = 0;
			
		КонецЦикла;
		
		i1 = 0;
		
		Для i = 0 По 13 Цикл
			
			СтруктураПеременных.X[i] = Мас[i1] + Мас[i1 + 1] * 256 + Мас[i1 + 2] * 65536 + Мас[i1 + 3] * 16777216;
			i1 = i1 + 4;
			
		КонецЦикла;
		
		СтруктураПеременных.X[14] = ДлинаБит % СтруктураПеременных.Stepen32;
		СтруктураПеременных.X[15] = Цел(ДлинаБит / СтруктураПеременных.Stepen32);

		MD5Hash(СтруктураПеременных);
		
	КонецЕсли;

КонецПроцедуры

Функция ПолучитьПустойМассивЧисло(Р)
	
	Массив = Новый Массив; // Массив из Число
	
	Для Сч = 0 По Р - 1 Цикл
		
		Массив.Добавить(0);
		
	КонецЦикла;

	Возврат Массив;
	
КонецФункции

// Инициализация структуры переменных.
// 
// Возвращаемое значение:
//  Структура - Инициализация структуры переменных:
// * A_ - Массив из Булево - 
// * B_ - Массив из Булево - 
// * C_ - Массив из Булево - 
// * D_ - Массив из Булево - 
// * A - Число - 
// * B - Число - 
// * C - Число - 
// * D - Число - 
// * EAX - Массив из Булево - 
// * EBX - Массив из Булево - 
// * ECX - Массив из Булево - 
// * EDX - Массив из Булево - 
// * M - Массив из Число - 
// * Степень - Массив из Число - 
// * X - Массив из Число - 
// * S11 - Число - 
// * S12 - Число - 
// * S13 - Число - 
// * S14 - Число - 
// * S21 - Число - 
// * S22 - Число - 
// * S23 - Число - 
// * S24 - Число - 
// * S31 - Число - 
// * S32 - Число - 
// * S33 - Число - 
// * S34 - Число - 
// * S41 - Число - 
// * S42 - Число - 
// * S43 - Число - 
// * S44 - Число - 
// * Stepen31 - Число - 
// * Stepen32 - Число - 
// 
Функция ИнициализацияСтруктурыПеременных()

	СтруктураПеременных = Новый Структура;
	СтруктураПеременных.Вставить("A_", КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьПустойМассивБулево(32));
	СтруктураПеременных.Вставить("B_", КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьПустойМассивБулево(32));
	СтруктураПеременных.Вставить("C_", КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьПустойМассивБулево(32));
	СтруктураПеременных.Вставить("D_", КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьПустойМассивБулево(32));

	СтруктураПеременных.Вставить("A", 1732584193);
	СтруктураПеременных.Вставить("B", 4023233417);
	СтруктураПеременных.Вставить("C", 2562383102);
	СтруктураПеременных.Вставить("D", 271733878);

	СтруктураПеременных.Вставить("EAX", КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьПустойМассивБулево(32));
	СтруктураПеременных.Вставить("EBX", КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьПустойМассивБулево(32));
	СтруктураПеременных.Вставить("ECX", КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьПустойМассивБулево(32));
	СтруктураПеременных.Вставить("EDX", КонтурДиадокКоннекторHTTPДвоичныеДанные82Служебный.ПолучитьПустойМассивБулево(32));

	СтруктураПеременных.Вставить("M", ПолучитьПустойМассивЧисло(64));

	СтруктураПеременных.Вставить("Степень", ПолучитьПустойМассивЧисло(32));

	СтруктураПеременных.Вставить("X", ПолучитьПустойМассивЧисло(16));

	СтруктураПеременных.Вставить("S11", 7);
	СтруктураПеременных.Вставить("S12", 12);
	СтруктураПеременных.Вставить("S13", 17);
	СтруктураПеременных.Вставить("S14", 22);

	СтруктураПеременных.Вставить("S21", 5);
	СтруктураПеременных.Вставить("S22", 9);
	СтруктураПеременных.Вставить("S23", 14);
	СтруктураПеременных.Вставить("S24", 20);

	СтруктураПеременных.Вставить("S31", 4);
	СтруктураПеременных.Вставить("S32", 11);
	СтруктураПеременных.Вставить("S33", 16);
	СтруктураПеременных.Вставить("S34", 23);

	СтруктураПеременных.Вставить("S41", 6);
	СтруктураПеременных.Вставить("S42", 10);
	СтруктураПеременных.Вставить("S43", 15);
	СтруктураПеременных.Вставить("S44", 21);
	СтруктураПеременных.Вставить("Stepen31", 0);
	СтруктураПеременных.Вставить("Stepen32", 0);
	СтруктураПеременных.Степень[0] = 1;
	
	Для инд = 1 По 31 Цикл
		
		СтруктураПеременных.Степень[инд] = СтруктураПеременных.Степень[инд - 1] * 2;
		
	КонецЦикла;
	
	СтруктураПеременных.Stepen31 = СтруктураПеременных.Степень[31];
	СтруктураПеременных.Stepen32 = СтруктураПеременных.Stepen31 * 2;

	Для i = 0 По 31 Цикл
		
		i1 = 31 - i;
		Степ = СтруктураПеременных.Степень[i1];
		
		Если СтруктураПеременных.A >= Степ Тогда
			
			СтруктураПеременных.A = СтруктураПеременных.A - Степ;
			СтруктураПеременных.A_[i1] = Истина;
			
		Иначе
			
			СтруктураПеременных.A_[i1] = Ложь;
			
		КонецЕсли;

		Если СтруктураПеременных.B >= Степ Тогда
			
			СтруктураПеременных.B = СтруктураПеременных.B - Степ;
			СтруктураПеременных.B_[i1] = Истина;
			
		Иначе
			
			СтруктураПеременных.B_[i1] = Ложь;
			
		КонецЕсли;

		Если СтруктураПеременных.C >= Степ Тогда
			
			СтруктураПеременных.C = СтруктураПеременных.C - Степ;
			СтруктураПеременных.C_[i1] = Истина;
			
		Иначе
			
			СтруктураПеременных.C_[i1] = Ложь;
			
		КонецЕсли;
		
		Если СтруктураПеременных.D >= Степ Тогда
			
			СтруктураПеременных.D = СтруктураПеременных.D - Степ;
			СтруктураПеременных.D_[i1] = Истина;
			
		Иначе
			
			СтруктураПеременных.D_[i1] = Ложь;
			
		КонецЕсли;
		
	КонецЦикла;

	Возврат СтруктураПеременных;
	
КонецФункции