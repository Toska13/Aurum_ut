
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Элементы.ДекорацияПример.Заголовок="Пример:
		|Результат=ш_ПроцедурыОбработкиДанныхПереопределяемый.ПолучитьСтруктуруВыгрузкиКонтрагента(Объект.Ссылка);
		|Сообщение=ш_РаботаСшинойДанныхСервер.ЗаписатьОбъектВJSON(Результат);
		|
		|Результат алгоритма - должно быть заполнена переменная Сообщение строковым значением
		|В переменной Объект - объект по которому идет событие
		|Если в переменной Сообщений пусто, то пакет не записывается для отправки";
	
КонецПроцедуры
