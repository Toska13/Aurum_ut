
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Массив=Новый Массив;
	Для каждого стрМатериал Из ИспользованныеМатериалы Цикл
		Массив.Добавить(СокрЛП(стрМатериал.Материал));
	КонецЦикла;
	ИспользованныеМатериалыКратко=СтрСоединить(Массив,", ");
	
КонецПроцедуры
