// BSLLS:MissingVariablesDescription-off
// BSLLS:UnusedLocalVariable-off
// BSLLS:ExportVariables-off
Перем Ядро Экспорт;

// ФиксированнаяСтруктура - программное перечисление:
//    * "ОбъектыДиадокаРасширение" - СХ-1 (расширение)
//    * "ОбъектыДиадокаПодсистема" - СХ-1 (подсистема)
//    * "ОбъектыБСП83" - СХ-1 (управляемые формы)
//    * "ОбъектыБСП82" - СХ-2 (обычные формы)
Перем ТипыХранения Экспорт;

Перем СтдСтрЛок;
Перем ЭтоВнешняяОбработкаКэш;
// BSLLS:ExportVariables-on
// BSLLS:UnusedLocalVariable-on
// BSLLS:MissingVariablesDescription-on

//**************************************
//{		СВЕДЕНИЯ О ВНЕШНЕЙ ОБРАБОТКЕ

// Сведения о внешней обработке совместимые со стандартной подсистемой ДополнительныеОтчетыИОбработки
//
// Возвращаемое значение:
//  Структура - содержит свойства:
//   * Версия          - Строка - версия обработки;
//   * Наименование    - Строка - представление для администратора (наименование элемента справочника),
//                                берется синоним объекта метаданных внешней обработки;
//   * Информация      - Строка - краткая информация о внешней обработке,
//                                берется комментарий объекта метаданных внешней обработки;
//   * БезопасныйРежим - Булево - признак подключения внешней обработки в безопасном режиме:
//                                 Игнорируется привилегированный режим
//                                 Запрещены внешние по отношению к платформе «1С:Предприятия» действия:
//                                  COM;
//                                  загрузка внешних компонент;
//                                  запуск внешних приложений и команд операционной системы;
//                                  доступ к файловой системе, кроме временных файлов;
//                                  доступ к Интернету.
//   * Вид             - Строка - содержит значение "ДополнительнаяОбработка";
//   * Команды         - ТаблицаЗначений - настройки команд, поставляемых внешней обработкой:
//      ** Идентификатор        - Строка- внутреннее имя команды;
//      ** Представление        - Строка- пользовательское представление команды;
//      ** Использование        - Строка- тип команды:
//                                         "ВызовКлиентскогоМетода",
//                                         "ВызовСерверногоМетода";
//      ** Модификатор          - Строка- дополнительная классификация команды;
//      ** ПоказыватьОповещение - Булево- если Истина, то при запуске команды выводится оповещение "Команда выполняется...";
//
Функция СведенияОВнешнейОбработке() Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("БезопасныйРежим", Истина);
	Результат.Вставить("Наименование"	, Метаданные().Синоним);
	Результат.Вставить("Информация"		, Метаданные().Комментарий);
	Результат.Вставить("Версия"			, ПолнаяВерсияОбработки());
	Результат.Вставить("Команды"		, Новый ТаблицаЗначений);
	Результат.Вставить("Вид"			, "ДополнительнаяОбработка");
	
	Возврат Результат;
	
КонецФункции

// Версия обработки в стандартном формате
//
// Возвращаемое значение:
//  Строка - версия в формате РР.ВВ.ФФ.СС, где:
//   * РР - номер редакции    - в рамках одной редакции гарантируется обратная совместимость;
//   * ВВ - номер версии      - новая функциональность (или несколько);
//   * ФФ - номер исправления - исправление ошибки;
//   * СС - номер сборки      - сквозная нумерация обновляется автоматически;
Функция ВерсияОбработки() Экспорт
	
	Результат = "1.0.0";
	
	Возврат Результат;
	
КонецФункции

Функция ПолнаяВерсияОбработки()
	
	Версия = ВерсияОбработки();
	
	Результат = Версия + "." + НомерСборки();
	
	Возврат Результат;
	
КонецФункции

// Сквозной номер сборки, обновляется автоматически
//
Функция НомерСборки()
	
	Возврат "34";
	
КонецФункции

//}		СВЕДЕНИЯ О ВНЕШНЕЙ ОБРАБОТКЕ


///////////////////////////////////////////////////////////////////////////////
//{		ИНИЦИАЛИЗАЦИЯ МОДУЛЯ

Функция ОшибкиДоступаКПодсистемеХранения() Экспорт
	
	Результат = Новый СписокЗначений;
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = ОшибкиДоступаКПодсистемеХранения_Диадок();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Проверяет структуру конфигурации на наличие метаданных,
// необходимых для функционирования модуля на объектах Диадока.
//
// Возвращаемое значение: Булево
//
Функция УстановленаПодсистемаДиадока() Экспорт
	
	Результат = Истина;
	
	НеобходимыеМетаданные = Новый Массив;
	
	НеобходимыеМетаданные.Добавить("Справочники.Диадок_ДополнительныеСправочники");
	НеобходимыеМетаданные.Добавить("Справочники.Диадок_ЮрФизЛица");
	НеобходимыеМетаданные.Добавить("Справочники.Диадок_Документы");
	НеобходимыеМетаданные.Добавить("Справочники.Диадок_ПакетыДокументов");
	НеобходимыеМетаданные.Добавить("Справочники.Диадок_ХранениеДанных");
	НеобходимыеМетаданные.Добавить("Справочники.Диадок_Файлы");
	НеобходимыеМетаданные.Добавить("РегистрыСведений.Диадок_НастройкиКонтрагентов");
	НеобходимыеМетаданные.Добавить("РегистрыСведений.Диадок_СтатусыДокументов");
	
	Для Каждого Элемент Из НеобходимыеМетаданные Цикл
		
		Результат = Результат И ПроверитьНаличиеОбъектаМетаданных(Элемент);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Проверяет структуру конфигурации на наличие метаданных,
// необходимых для функционирования модуля на стандартных объектах.
//
// Возвращаемое значение: Булево
//
Функция УстановленРегистрДопСведений() Экспорт
	
	ИмяРегистраСведений = Свойства_ИмяРегистраСведений();
	
	Результат = Ложь;

	Если ЗначениеЗаполнено(ИмяРегистраСведений) Тогда
					
		Результат = Истина;
		
		ЕстьСправочник = ПроверитьНаличиеОбъектаМетаданных("Справочники.Валюты");
		Если Не ЕстьСправочник Тогда
			ЕстьСправочник = ПроверитьНаличиеОбъектаМетаданных("Справочники.ПрофилиГруппДоступа");
		КонецЕсли;
		
		НеобходимыеМетаданные = Новый Массив;
		
		//*НеобходимыеМетаданные.Добавить("Справочники.Валюты");
		НеобходимыеМетаданные.Добавить("РегистрыСведений." + ИмяРегистраСведений);
		НеобходимыеМетаданные.Добавить("РегистрыСведений." + ИмяРегистраСведений + ".Измерения.Объект");
		
		ЕстьРегистр = Истина;
		
		Для Каждого Элемент Из НеобходимыеМетаданные Цикл
			
			ЕстьРегистр = ЕстьРегистр И ПроверитьНаличиеОбъектаМетаданных(Элемент);
			//*Результат = Результат И ПроверитьНаличиеОбъектаМетаданных(Элемент);
			
		КонецЦикла;
		
		Результат = ЕстьСправочник И ЕстьРегистр;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ОшибкиДоступаКПодсистемеХранения_Диадок()
	
	Результат = Новый СписокЗначений;
	
	ПроверитьДоступностьОбъектаМетаданных(Результат, "Справочники.Диадок_ДополнительныеСправочники");
	ПроверитьДоступностьОбъектаМетаданных(Результат, "Справочники.Диадок_ЮрФизЛица");
	ПроверитьДоступностьОбъектаМетаданных(Результат, "РегистрыСведений.Диадок_НастройкиКонтрагентов");
	
	Возврат Результат;
	
КонецФункции

// Возвращает техническую информацию о системе хранения
//
// Возвращаемое значение:
//   ФиксированнаяСтруктура - содержит поля:
//      * Тип - Строка - идентификатор варианта СХ (см. перечисление ТипыХранения)
//      * Версия - Строка - номер версии СХ (для СХ-2 - версия БСП)
//
// Пример:
//   ОписаниеСХ = МодульХранениеДанных().ТехническаяИнформация();
//
Функция ТехническаяИнформация() Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Тип", ТекущийВариантХранения());
	
	Возврат Результат;
	
КонецФункции

Функция ТекущийВариантХранения()
	
	ТипыХранения = Перечисление_ТипыХранения();
	
	Если ИспользуетсяРасширениеПодсистемыХранения() Тогда
		Результат = ТипыХранения.ОбъектыДиадокаРасширение;
	ИначеЕсли ИспользоватьПодсистемуДиадока() Тогда
		Результат = ТипыХранения.ОбъектыДиадокаПодсистема;
	ИначеЕсли ИспользоватьСистемуХраненияОбъектыБСП82() Тогда
		Результат = ТипыХранения.ОбъектыБСП82;
	ИначеЕсли ИспользоватьСистемуХраненияОбъектыБСП83() Тогда
		Результат = ТипыХранения.ОбъектыБСП83;
	Иначе
		Результат = Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ИспользоватьСистемуХраненияОбъектыБСП82()
	
	Результат = (
		ИспользоватьТиповуюПодсистему1С()
		И Свойства_ИмяРегистраСведений() = ИмяРегистраСведенийЗначенияСвойствОбъекта()
	);
	
	Возврат Результат;
	
КонецФункции

Функция ИспользоватьСистемуХраненияОбъектыБСП83()
	
	Результат = (
		ИспользоватьТиповуюПодсистему1С()
		И Свойства_ИмяРегистраСведений() = ИмяРегистраСведенийДополнительныеСведения()
	);
	
	Возврат Результат;
	
КонецФункции

Функция ИмяРегистраСведенийЗначенияСвойствОбъекта()
	Возврат "ЗначенияСвойствОбъектов";
КонецФункции

Функция ИмяРегистраСведенийДополнительныеСведения()
	Возврат "ДополнительныеСведения";
КонецФункции

Функция Перечисление_ТипыХранения()
	
	Результат = Новый Структура;
	Результат.Вставить("ОбъектыДиадокаРасширение", "ОбъектыДиадокаРасширение");
	Результат.Вставить("ОбъектыДиадокаПодсистема", "ОбъектыДиадокаПодсистема");
	Результат.Вставить("ОбъектыБСП83", "ОбъектыБСП83");
	Результат.Вставить("ОбъектыБСП82", "ОбъектыБСП82");
	
	Возврат Результат;
	
КонецФункции

///////////////////////////////////////////////////////////////////////////////
//{		НАСТРОЙКИ

// Заполняет общие настройки
//
// Возвращаемое значение: Структура
//
Функция Настройки_ОбщиеНастройки() Экспорт
	
	Результат = Кэш_Прочитать("ОбщиеНастройки");
	
	Если Результат <> Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Настройки_ОбщиеНастройки_Диадок();
	Иначе
		Результат = Настройки_ОбщиеНастройки_1С();
	КонецЕсли;
	
	// дефолтные настройки, не заданные в предыдущем релизе модуля
	Если ТипЗнч(Результат.ХранитьДокументыНаДиске) <> Тип("Булево") Тогда
		Результат.ХранитьДокументыНаДиске = Ложь;
	КонецЕсли;
	
	Если Результат.СрокОчисткиДокументов = Неопределено Тогда
		Результат.СрокОчисткиДокументов = 0;
	КонецЕсли;
	
	Если ТипЗнч(Результат.ОтключитьПревьюНеформализованного) <> Тип("Булево") Тогда
		Результат.ОтключитьПревьюНеформализованного = Ложь;
	КонецЕсли;
	
	Если ТипЗнч(Результат.ОтключитьКонтрольДублированияОтправкиПакетов) <> Тип("Булево") Тогда
		Результат.ОтключитьКонтрольДублированияОтправкиПакетов = Ложь;
	КонецЕсли;
	
	Если Не ПриложениеСтаршеВерсии("8.3.14") Тогда
		Результат.ОтключитьПревьюНеформализованного = Истина;
	КонецЕсли;
	
	Если ТипЗнч(Результат.УдалятьПриПереотправкеПредыдущийДокумент) <> Тип("Булево") Тогда
		Результат.УдалятьПриПереотправкеПредыдущийДокумент = Ложь;
	КонецЕсли;
	
	Если Результат.ИдентификаторБазы1С = "" Тогда
		
		Результат.ИдентификаторБазы1С = Новый УникальныйИдентификатор;
		Результат.ЭтоПервыйЗапускМодуля = Истина;
		
		Если Результат.СохраненнаяВерсияМодуля <> "0.0.0" Тогда
			Настройки_ЗаписатьОбщиеНастройки(Результат);
		КонецЕсли;
		
	КонецЕсли;
	
	Настройки_Конвертация970_ЗаполнитьЗначениеПоУмолчанию(Результат);
	
	Результат = ОбщиеНастройки_ОбработанныеЗначенияПослеЧтения(Результат);
	
	Настройки_ОбщиеНастройки_УстановитьКэш(Результат);
	
	Возврат Результат;
	
КонецФункции

Функция Настройки_ЗаписатьОбщиеНастройки(СтруктураНастроек) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Настройки_ЗаписатьОбщиеНастройки_Диадок(СтруктураНастроек);
	Иначе
		Результат = Настройки_ЗаписатьОбщиеНастройки_1С(СтруктураНастроек);
		СтруктураНастроек.ПодключаемыйМодульСсылка = ПодключаемыйМодульСсылка(СтруктураНастроек);
	КонецЕсли;
	
	СтруктураНастроек = ОбщиеНастройки_ОбработанныеЗначенияПослеЧтения(СтруктураНастроек);
	
	Настройки_ОбщиеНастройки_УстановитьКэш(СтруктураНастроек);
	
	Возврат Результат;
	
КонецФункции 

Функция Настройки_Конвертация970_РежимыСовместимостиФорматов820и970()

	Результат = Новый Структура;
	Результат.Вставить("НеИспользуется", "НеИспользуется");
	Результат.Вставить("Авто", "Авто");
	
	Возврат Результат;
	
КонецФункции

Процедура Настройки_Конвертация970_ЗаполнитьЗначениеПоУмолчанию(ОбщиеНастройки)
	
	Если ОбщиеНастройки.РежимСовместимостиФорматов820и970 = Неопределено Тогда
		РежимСовместимости = Настройки_Конвертация970_РежимыСовместимостиФорматов820и970().НеИспользуется;
		ОбщиеНастройки.РежимСовместимостиФорматов820и970 = РежимСовместимости;
	КонецЕсли;

КонецПроцедуры

Функция Настройки_Конвертация970_УстановленаНастройкаРежимаСовместимостиФорматовАвто() Экспорт
	
	ОбщиеНастройки = Настройки_ОбщиеНастройки();
	
	ТекущийРежимСовместимости = ОбщиеНастройки.РежимСовместимостиФорматов820и970;
	РежимыСовместимости = Настройки_Конвертация970_РежимыСовместимостиФорматов820и970();
		
	Результат = (ТекущийРежимСовместимости = РежимыСовместимости.Авто);
	
	Возврат Результат;
	
КонецФункции


// Устанавливает кэш общих настроек
// Вызывается также из ядра при выполнении фонового задания
//
// Параметры:
//  ОбщиеНастройки - Структура -  см. функцию Настройки_ОбщиеНастройки
//
Процедура Настройки_ОбщиеНастройки_УстановитьКэш(ОбщиеНастройки) Экспорт
	
	Кэш_Поместить("ОбщиеНастройки", ОбщиеНастройки);
	
КонецПроцедуры

Функция ОбщиеНастройки_ОбработанныеЗначенияПослеЧтения(ЗначенияНастроек)
	
	Результат = ЗначенияНастроек;
	
	Если ТипЗнч(Результат.СохранениеДокументовИмяКаталогаПакета) = Тип("Строка") Тогда
		
		ПриоритетыИменКаталогаПакета = РазделитьСтрокуЛок(Результат.СохранениеДокументовИмяКаталогаПакета, ";");
		
		Результат.СохранениеДокументовИмяКаталогаПакета = Новый СписокЗначений;
		
		Для Каждого Имя Из ПриоритетыИменКаталогаПакета Цикл
			Если ЗначениеЗаполнено(Имя) Тогда
				Результат.СохранениеДокументовИмяКаталогаПакета.Добавить(Имя);
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	ЗаполнитьПрокси(Результат);
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьПрокси(Результат)
	
	ПортПроксиСервера = Неопределено;
	Результат.Свойство("ПортПроксиСервера", ПортПроксиСервера);
	Результат.Вставить("ПортПроксиСервера",	?(ЗначениеЗаполнено(ПортПроксиСервера), Число(ПортПроксиСервера), 0));
	
КонецПроцедуры

Функция ПодключаемыйМодульСсылка(СтруктураНастроек)
	
	Результат = Неопределено;
	
	Если СтруктураНастроек.Свойство("ПодключаемыйМодульИмяСправочника")
		И СтруктураНастроек.Свойство("ПодключаемыйМодульСсылка")
		И ЗначениеЗаполнено(СтруктураНастроек.ПодключаемыйМодульИмяСправочника)
		И ЗначениеЗаполнено(СтруктураНастроек.ПодключаемыйМодульСсылка) Тогда
		
		Попытка
			ГУИД = Новый УникальныйИдентификатор(СтруктураНастроек.ПодключаемыйМодульСсылка);
			Результат = Справочники[СтруктураНастроек.ПодключаемыйМодульИмяСправочника].ПолучитьСсылку(ГУИД);
		Исключение
			Сообщить("Не удалось прочитать настройки подключаемого модуля");	
		КонецПопытки;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции	


Функция Настройки_ЗначениеСвойства(ИмяСвойства, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Настройки_ЗначениеСвойства_Диадок(ИмяСвойства, ДанныеОрганизации, ДанныеКонтрагента);
	Иначе
		Результат = Настройки_ЗначениеСвойства_1С(ИмяСвойства, ДанныеОрганизации, ДанныеКонтрагента);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает сохраненные в информационной базе настройки видо пакетов документов на отправку в разрезе категорий для организации или контрагента
// (если для контрагента были включена индивидуальная настройка).
//
// Параметры:
//  КатегорииПакетов	 - Массив	 - имена категорий пакетов (например, ОсновнойПакет, КорректировочныйПакет, СчетФактураНаАванс и т.п.)
//  ДанныеОрганизации	 - Структура	- заполненный контракт данных организации (см. Контракт_ОрганизацияЭДО)
//  ДанныеКонтрагента	 - Структура	- заполненный контракт данных контрагента (см. Контракт_КонтрагентЭДО). Необязательный параметр. 
// 
// Возвращаемое значение:
//  Соответствие - Ключ - имя категории, значение - вид пакета (строка (СХ2) или ссылка (СХ1))
//
Функция Настройки_ВидыПакетовНаОтправку(КатегорииПакетов, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Настройки_ВидыПакетовНаОтправку_Диадок(КатегорииПакетов, ДанныеОрганизации, ДанныеКонтрагента);
	Иначе
		Результат = Свойства_ЗначенияСвойствОбъекта_1С(КатегорииПакетов, ДанныеОрганизации, ДанныеКонтрагента);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает коллекцию установленных форматов документов для видов документов для организации или контрагента
// (если для контрагента были включена индивидуальная настройка).
//
// Параметры:
//  ИдентификаторыВидовДокументов	- Массив 	- массив строк идентификаторов видов документов
//												(пример ид. видов документов: ID_УПД, ID_УКД, ID_ТОРГ12, ID_СчетФактура и др).
//  ДанныеОрганизации	 			- Структура	- заполненный контракт данных организации (см. Контракт_ОрганизацияЭДО)
//  ДанныеКонтрагента	 			- Структура	- заполненный контракт данных контрагента (см. Контракт_КонтрагентЭДО). Необязательный параметр. 
// 
// Возвращаемое значение:
//  Соответствие - ключ - ид. вида документа (строка), значение - формат, установленный для данного вида документа (строка)
//
Функция Настройки_ФорматыВидовПакетовНаОтправку(ИдентификаторыВидовДокументов, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт

	Результат = Новый Соответствие;

	ИменаСвойств = Новый Массив;
	Для Каждого ИдВидаДокумента Из ИдентификаторыВидовДокументов Цикл
		ИмяСвойства = "Формат_" + ИдВидаДокумента;
		ИменаСвойств.Добавить(ИмяСвойства);
	КонецЦикла;

	ЗначенияСвойств = Настройки_ЗначенияСвойствОбъекта(ИменаСвойств, ДанныеОрганизации, ДанныеКонтрагента);
	
	Для Каждого ЭлементКоллекции Из ЗначенияСвойств Цикл
		ИдВидаДокумента = Сред(ЭлементКоллекции.Ключ, 8);
		Результат.Вставить(ИдВидаДокумента, ЭлементКоллекции.Значение);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Возвращает коллекцию значений свойств в разрезе их (свойств) имен для организации или контрагента
// (если для контрагента были включена индивидуальная настройка).
//
// Параметры:
//  ИменаСвойств		 - Массив	 - имена свойств строкой
//  ДанныеОрганизации	 - Структура	- заполненный контракт данных организации (см. Контракт_ОрганизацияЭДО)
//  ДанныеКонтрагента	 - Структура	- заполненный контракт данных контрагента (см. Контракт_КонтрагентЭДО). Необязательный параметр. 
// 
// Возвращаемое значение:
//  Соответствие - ключ - имя свойства (строка), значение - значение свойства (произвольный тип)
//
Функция Настройки_ЗначенияСвойствОбъекта(ИменаСвойств, ДанныеОрганизации, ДанныеКонтрагента)
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Настройки_ЗначенияСвойствОбъекта_Диадок(ИменаСвойств, ДанныеОрганизации, ДанныеКонтрагента);
	Иначе
		Результат = Свойства_ЗначенияСвойствОбъекта_1С(ИменаСвойств, ДанныеОрганизации, ДанныеКонтрагента);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// см. Настройки_ВидыПакетовНаОтправку
//
Функция Настройки_ВидыПакетовНаОтправку_Диадок(ИменаСвойств, ДанныеОрганизации, ДанныеКонтрагента)

	Результат = Новый Соответствие;
	
	ВидДопСправочника = "ВидыПакетов";
	
	ТекстЗапроса = ТекстЗапроса_НастройкиКонтрагентаСПриведениемКТипуЗначений_Диадок(ВидДопСправочника);
	
	Запрос = Новый Запрос(ТекстЗапроса);
	
	Настройки_ЗаполнитьОбщиеПараметрыЗапроса_Диадок(Запрос, ДанныеОрганизации, ДанныеКонтрагента);
	Запрос.УстановитьПараметр("ИменаСвойств", ИменаСвойств);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Результат.Вставить(Выборка.ИмяСвойства, Выборка.Значение);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// см. Настройки_ЗначенияСвойствОбъекта 
//
Функция Свойства_ЗначенияСвойствОбъекта_1С(ИменаСвойств, ДанныеОрганизации, ДанныеКонтрагента)
	
	Результат = Новый Соответствие;

	ТекстЗапроса = ТекстЗапроса_ЗначенияСвойствОбъекта_1С();
	Запрос = Новый Запрос(ТекстЗапроса);
	
	КоллекцияСвойств = Новый Массив;
	Для Каждого ИмяСвойства Из ИменаСвойств Цикл
		Свойство = Свойства_СсылкаНаСвойство(ИмяСвойства);
		КоллекцияСвойств.Добавить(Свойство);
	КонецЦикла;
	
	Если ДанныеКонтрагента <> Неопределено Тогда
		ОбъектНастроек = ДанныеКонтрагента.СвязанныйСправочник1;
	Иначе
		ОбъектНастроек = ДанныеОрганизации.СвязанныйСправочник1;
	КонецЕсли;
	
	Запрос.УстановитьПараметр("Свойства", КоллекцияСвойств);
	Запрос.УстановитьПараметр("Объект"	, ОбъектНастроек);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	ПрефиксИмен = ПрефиксНастройки_1С(Истина);
	
	Пока Выборка.Следующий() Цикл
		ИмяСвойстваБезПрефикса = СтрЗаменить(Выборка.ИмяСвойства, ПрефиксИмен, "");
		Результат.Вставить(ИмяСвойстваБезПрефикса, Выборка.Значение);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// см. Настройки_ЗначенияСвойствОбъекта 
//
Функция Настройки_ЗначенияСвойствОбъекта_Диадок(ИменаСвойств, ДанныеОрганизации, ДанныеКонтрагента)
	
	Результат = Новый Соответствие;
	
	ТекстЗапроса = ТекстЗапроса_НастройкиКонтрагента_Диадок();
	
	Запрос = Новый Запрос(ТекстЗапроса);
	
	Настройки_ЗаполнитьОбщиеПараметрыЗапроса_Диадок(Запрос, ДанныеОрганизации, ДанныеКонтрагента);
	Запрос.УстановитьПараметр("ИменаСвойств", ИменаСвойств);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Результат.Вставить(Выборка.ИмяСвойства, Выборка.Значение);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Вспомогательный метод, заполняет параметры запроса ОрганизацияЭДО и КонтрагентЭДО
//
Процедура Настройки_ЗаполнитьОбщиеПараметрыЗапроса_Диадок(Запрос, ДанныеОрганизации, ДанныеКонтрагента)
	
	ОрганизацияЭДО = ДанныеОрганизации.Ссылка;
	
	Если ДанныеКонтрагента = Неопределено Тогда
		КонтрагентЭДО = Контрагенты_ПустаяСсылка_Диадок();
	Иначе
		КонтрагентЭДО = ДанныеКонтрагента.Ссылка;
	КонецЕсли;
	
	Запрос.УстановитьПараметр("ОрганизацияЭДО"	, ОрганизацияЭДО);
	Запрос.УстановитьПараметр("КонтрагентЭДО"	, КонтрагентЭДО);
	
КонецПроцедуры

Процедура Настройки_ЗаписатьЗначенияСвойств(КоллекцияСвойств, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Настройки_ЗаписатьЗначенияСвойств_Диадок(КоллекцияСвойств, ДанныеОрганизации, ДанныеКонтрагента);
	Иначе
		Настройки_ЗаписатьЗначенияСвойств_1С(КоллекцияСвойств, ДанныеОрганизации, ДанныеКонтрагента);
	КонецЕсли;
	
КонецПроцедуры

Процедура Настройки_ЗаписатьКатегорииПакетов(КатегорииПакетов, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		
		Для Каждого Элемент Из КатегорииПакетов Цикл
			
			Если ЗначениеЗаполнено(Элемент.Значение)
				И ТипЗнч(Элемент.Значение) = Тип("Строка") Тогда
				
				ЗначениеДляЗаписи = ДопСправочники_СсылкаПоИдентификатору("ВидыПакетов", Элемент.Значение);
				КатегорииПакетов.Вставить(Элемент.Ключ, ЗначениеДляЗаписи);
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	                                                                              
	Настройки_ЗаписатьЗначенияСвойств(КатегорииПакетов, ДанныеОрганизации, ДанныеКонтрагента);
		
КонецПроцедуры

Процедура Настройки_УдалитьЗначениеСвойства(ИмяСвойства, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		
		СтруктураДанных = Новый Структура;
		СтруктураДанных.Вставить("ОрганизацияЭДО",	ДанныеОрганизации.Ссылка);
		СтруктураДанных.Вставить("ИмяСвойства",		ИмяСвойства);
		
		Если ДанныеКонтрагента = Неопределено Тогда
			СтруктураДанных.Вставить("КонтрагентЭДО", Контрагенты_ПустаяСсылка_Диадок());
		Иначе
			СтруктураДанных.Вставить("КонтрагентЭДО", ДанныеКонтрагента.Ссылка);
		КонецЕсли;
		
		Справочники_УдалитьЗначениеНастроекКонтрагентов_Диадок(СтруктураДанных);
		
	Иначе
		
		ХранителиСвойств = СвязанныеСправочники(ДанныеОрганизации, ДанныеКонтрагента);
						
		Для Каждого ЭлементКоллекции Из ХранителиСвойств Цикл
		
			ДопРеквизиты_УдалитьЗначение_1С(ЭлементКоллекции.Ссылка, ИмяСвойства);
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

// Возвращает значение сохраненной настройки модуля Diadoc.StandartUF "ТочностьЦеныСФ_Диадок"
// 
// Возвращаемое значение:
//  Число, Неопределено - значение настройки точности цены сф
//
Функция НастройкаМодуляСтандарт_ТочностьЦены() Экспорт 
	
	ИмяНастройки = НастройкаМодуляСтандарт_ИмяНастройкиТочностьЦены();
	Результат = ХранилищеОбщихНастроек.Загрузить("НастройкаОпций_Диадок", ИмяНастройки);
	Возврат Результат;
	
КонецФункции

Функция НастройкаМодуляСтандарт_ИмяНастройкиТочностьЦены()
	
	Результат = "ТочностьЦеныСФ_Диадок";
	Возврат Результат;
	
КонецФункции

// Получает из БД значение настройки "ТипОснования" для вида документа исходя из используемой подсистемы хранения
//
// Параметры:
//	IDВидаДокумента		- Строка					- идентификатор вида документа: "ID_АктФормализованный", "ID_ТОРГ12" и т.д.
//  ДанныеОрганизации	- Структура					- данные организации, в разрезе которой хранится настройка
//	ДанныеКонтрагента	- Структура, Неопределено	- данные контрагента, в разрезе которого хранится настройка
//
// Возвращаемое значение:
//	Строка - значение настройки
Функция НастройкиДокументов_ОснованияВидовДокументов_Прочитать(IDВидаДокумента, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт
	
	ИмяСвойства = КлючСвойстваТипОснования(IDВидаДокумента);
	
	Результат = Настройки_ЗначениеСвойства(ИмяСвойства, ДанныеОрганизации, ДанныеКонтрагента);
			
	Возврат Результат;
	
КонецФункции

// Сохраняет в БД значение настройки "ТипОснования" для вида документа исходя из используемой подсистемы хранения
//
// Параметры:
//	ОснованияВидовДокументов	- Соответствие				- коллекция значений настройки по видам документа
//  ДанныеОрганизации			- Структура					- данные организации, в разрезе которой хранится настройка
//	ДанныеКонтрагента			- Структура, Неопределено	- данные контрагента, в разрезе которого хранится настройка
//
Процедура НастройкиДокументов_ОснованияВидовДокументов_Записать(ОснованияВидовДокументов, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт
	
	КоллекцияСвойств = Новый Соответствие;
	
	Для Каждого КлючЗначение Из ОснованияВидовДокументов Цикл
		ИмяСвойства = КлючСвойстваТипОснования(КлючЗначение.Ключ);
		КоллекцияСвойств.Вставить(ИмяСвойства, КлючЗначение.Значение);
	КонецЦикла;
	
	Настройки_ЗаписатьЗначенияСвойств(КоллекцияСвойств, ДанныеОрганизации, ДанныеКонтрагента);
			
КонецПроцедуры

// Возвращает таблицу со значениями оснований для вида электронного документа, записанными в БД
//
// Параметры:
//	ТаблицаВидовДокументов - ТаблицаЗначений - См. Новый_ТаблицаСвойстваТипОснованияПоВидуДокумента()
//
// ВозвращаемоеЗначение:
//	ТаблицаЗначений с колонками: 
//		* ВидДокументаID
//		* ЗначениеНастройки
Функция НастройкиДокументов_ОснованияВидовДокументов_ПрочитатьНаборНастроек(ТаблицаВидовДокументов) Экспорт
			
	Если ИспользоватьПодсистемуДиадока() Тогда
						
		ТекстЗапроса = 
		"ВЫБРАТЬ
		|	ТаблицаВидовДокументов.ВидДокументаID КАК ВидДокументаID,
		|	ТаблицаВидовДокументов.СвойствоТипОснования КАК СвойствоТипОснования
		|ПОМЕСТИТЬ ВТ_ТаблицаВидовДокументов
		|ИЗ
		|	&ТаблицаВидовДокументов КАК ТаблицаВидовДокументов
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	ВТ_ТаблицаВидовДокументов.ВидДокументаID КАК ВидДокументаID,
		|	Диадок_НастройкиКонтрагентов.Значение КАК ЗначениеНастройки
		|ИЗ
		|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ТаблицаВидовДокументов КАК ВТ_ТаблицаВидовДокументов
		|		ПО Диадок_НастройкиКонтрагентов.ИмяСвойства = ВТ_ТаблицаВидовДокументов.СвойствоТипОснования
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_ТаблицаВидовДокументов";
						
	Иначе
				
		ТекстЗапроса = 
		"ВЫБРАТЬ
		|	ТаблицаВидовДокументов.ВидДокументаID КАК ВидДокументаID,
		|	ТаблицаВидовДокументов.СвойствоТипОснования КАК СвойствоТипОснования
		|ПОМЕСТИТЬ ВТ_ТаблицаВидовДокументов
		|ИЗ
		|	&ТаблицаВидовДокументов КАК ТаблицаВидовДокументов
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	ВТ_ТаблицаВидовДокументов.ВидДокументаID КАК ВидДокументаID,
		|	ДополнительныеСведения.Значение КАК ЗначениеНастройки
		|ИЗ
		|	РегистрСведений.ДополнительныеСведения КАК ДополнительныеСведения
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ТаблицаВидовДокументов КАК ВТ_ТаблицаВидовДокументов
		|		ПО ДополнительныеСведения.Свойство = ВТ_ТаблицаВидовДокументов.СвойствоТипОснования
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_ТаблицаВидовДокументов";
		
		ТекстЗапроса_УстановитьИмяРегистраСведений(ТекстЗапроса);
						
	КонецЕсли;
			
	Запрос = Новый Запрос;
	
	Запрос.Текст = ТекстЗапроса;
	
	Запрос.УстановитьПараметр("ТаблицаВидовДокументов", ТаблицаВидовДокументов);
		
	Результат = Запрос.Выполнить().Выгрузить();
	
	Возврат Результат;
	
КонецФункции

// Удаляет из БД значение настройки "ТипОснования" для вида документа исходя из используемой подсистемы хранения 
//
// Параметры:
//	ВидДокумента 		- Строка					- идентификатор вида документа: "ID_АктФормализованный", "ID_ТОРГ12" и т.д.
//	ДанныеОрганизации 	- Структура					- данные организации, в разрезе которой хранится настройка
//	ДанныеКонтрагента 	- Структура, Неопределено	- данные контрагента, в разрезе которого хранится настройка
Процедура НастройкиДокументов_ОснованияВидовДокументов_Удалить(ВидДокумента, ДанныеОрганизации, ДанныеКонтрагента) Экспорт
	
	ИмяСвойства = КлючСвойстваТипОснования(ВидДокумента);
	
	Настройки_УдалитьЗначениеСвойства(ИмяСвойства, ДанныеОрганизации, ДанныеКонтрагента);
	
КонецПроцедуры

// Функция - Таблица свойства тип основания по виду документа
//
// Параметры:
//  ВидыДокументов	 - Массив - коллекция идентификаторов видов документов/пакетов 
// 
// Возвращаемое значение:
//  ТаблицаЗначений - см. Контракт Новый_ТаблицаСвойстваТипОснованияПоВидуДокумента  
//
Функция ТаблицаСвойстваТипОснованияПоВидуДокумента(ВидыДокументов) Экспорт
	
	Результат = Новый_ТаблицаСвойстваТипОснованияПоВидуДокумента();
	
	Для Каждого ВидДокумента Из ВидыДокументов Цикл
		
		НовыйЭлементТЗ = Результат.Добавить();
		НовыйЭлементТЗ.ВидДокументаID 		= ВидДокумента;
		НовыйЭлементТЗ.СвойствоТипОснования = Свойства_ТипОснованияПоВидуДокумента(ВидДокумента);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция Новый_ТаблицаСвойстваТипОснованияПоВидуДокумента()
	
	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("ВидДокументаID"	, Новый ОписаниеТипов("Строка", , Новый КвалификаторыСтроки(50)));
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат.Колонки.Добавить("СвойствоТипОснования", Новый ОписаниеТипов("Строка", , Новый КвалификаторыСтроки(50)));
	Иначе
		ИмяТипаСвойств = "ПланВидовХарактеристикСсылка." + Свойства_ИмяПланаВидовХарактеристик();
		Результат.Колонки.Добавить("СвойствоТипОснования", Новый ОписаниеТипов(ИмяТипаСвойств));
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает признак использования в модуле функциональности добавляения счетов в пакет
// 
// Возвращаемое значение:
//  Булево - Истина, если хотя бы для одной организации или контрагента значение настройки Истина
//
Функция Настройки_ЕстьНастройкаДобавлятьСчетВПакет() Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Настройки_ЕстьНастройкаДобавлятьСчетВПакет_Диадок();	
	Иначе
		Результат = Настройки_ЕстьНастройкаДобавлятьСчетВПакет_1С();
	КонецЕсли;

	Возврат Результат;
	
КонецФункции

// см. Настройки_ЕстьНастройкаДобавлятьСчетВПакет
//
Функция Настройки_ЕстьНастройкаДобавлятьСчетВПакет_Диадок() Экспорт
	
	ИмяСвойства = Перечисление_ИмяСвойствНастройки_ДобавлятьСчетВПакет();
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Значение
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов
	|ГДЕ
	|	ИмяСвойства = &ИмяСвойства
	|	И Значение = ИСТИНА"
	);                            
	Запрос.УстановитьПараметр("ИмяСвойства", ИмяСвойства);
	
	РезультатЗапроса = Запрос.Выполнить();
	Результат = Не РезультатЗапроса.Пустой();
	
	Возврат Результат;
	
КонецФункции

// см. Настройки_ЕстьНастройкаДобавлятьСчетВПакет
//
Функция Настройки_ЕстьНастройкаДобавлятьСчетВПакет_1С() Экспорт

	ИмяСвойства = Перечисление_ИмяСвойствНастройки_ДобавлятьСчетВПакет();
	
	СсылкаНаСвойство = Свойства_СсылкаНаСвойство(ИмяСвойства, Истина);
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Значение
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов
	|ГДЕ
	|	Свойство = &СсылкаНаСвойство
	|	И Значение = ИСТИНА"
	);                            
	Запрос.УстановитьПараметр("СсылкаНаСвойство", СсылкаНаСвойство);
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Запрос.Текст);
	
	РезультатЗапроса = Запрос.Выполнить();
	Результат = Не РезультатЗапроса.Пустой();
	
	Возврат Результат;
	
КонецФункции

// Сохраняет значение настройки Добавлять счет в пакет для организации или контрагента
//
// Параметры:
//  ЗначениеНастройки	 - Булево - сохранаяемое значение настройки
//  ДанныеОрганизации	 - Структура - заполненный данные организации (см. Организации_ЗаполненныеРеквизиты)
//  ДанныеКонтрагента	 - Структура, Неопределено - заполненный данные контрагента (см. Контрагенты_ЗаполненныеРеквизиты).
//													Неопределено, если сохраняем настройки для организации
//
Процедура НастройкиДокументов_СохранитьЗначениеДобавлятьСчетВПакет(ЗначениеНастройки, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт
	
	ИмяСвойства = Перечисление_ИмяСвойствНастройки_ДобавлятьСчетВПакет();
	
	ЗначенияСвойств = Новый Соответствие;
	ЗначенияСвойств.Вставить(ИмяСвойства, ЗначениеНастройки);
	
	Настройки_ЗаписатьЗначенияСвойств(ЗначенияСвойств, ДанныеОрганизации, ДанныеКонтрагента);
	
КонецПроцедуры

// Возвращает значение настройки "Добавлять счет в пакет" для контрагента или организации
// Если ДанныеКонтрагента <> Неопределено, тогда проверяем настройку 2 раза.
// Первый раз индивидуальную настройку контрагента. Если значение ее Ложь, то смотрим уже
// настройку для организации.
//
// Параметры:
//  ДанныеОрганизации	 - Структура 				- см. Организация_ЗаполненныеРеквизиты
//  ДанныеКонтрагента	 - Структура, Неопределено	- см. Контрагенты_ЗаполненныеРеквизиты
// 
// Возвращаемое значение:
//  Булево - Истина - настройка включена, Ложь - нет
//
Функция НастройкиДокументов_ЗначениеНастройкиДобавлятьСчетВПакет(ДанныеОрганизации,	ДанныеКонтрагента = Неопределено) Экспорт
	
	ИмяСвойства =  Перечисление_ИмяСвойствНастройки_ДобавлятьСчетВПакет();
		
	Результат = Настройки_ЗначениеСвойства(ИмяСвойства, ДанныеОрганизации, ДанныеКонтрагента);

	Возврат Результат;
	
КонецФункции

// Сохраняет значение настройки "Передавать МЧД файлом" для организации или контрагента
//
// Параметры:
//  ЗначениеНастройки	 - Булево - сохранаяемое значение настройки
//  ДанныеОрганизации	 - Структура - заполненный данные организации (см. Организации_ЗаполненныеРеквизиты)
//  ДанныеКонтрагента	 - Структура, Неопределено - заполненный данные контрагента (см. Контрагенты_ЗаполненныеРеквизиты).
//													Неопределено, если сохраняем настройки для организации
//
Процедура НастройкиДокументов_СохранитьЗначениеПередаватьМЧДФайлом(ЗначениеНастройки, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт
	
	ИмяСвойства = Перечисление_ИмяСвойствНастройки_ПередаватьМЧДФайлом();
	
	ЗначенияСвойств = Новый Соответствие;
	ЗначенияСвойств.Вставить(ИмяСвойства, ЗначениеНастройки);
	
	Настройки_ЗаписатьЗначенияСвойств(ЗначенияСвойств, ДанныеОрганизации, ДанныеКонтрагента);
	
КонецПроцедуры

// Возвращает значение настройки "Передавать МЧД файлом" для контрагента или организации
//
// Параметры:
//  ДанныеОрганизации	 - Структура 				- см. Организация_ЗаполненныеРеквизиты
//  ДанныеКонтрагента	 - Структура, Неопределено	- см. Контрагенты_ЗаполненныеРеквизиты
// 
// Возвращаемое значение:
//  Булево - Истина - настройка включена, Ложь - нет
//
Функция НастройкиДокументов_ЗначениеНастройкиПередаватьМЧДФайлом(ДанныеОрганизации,	ДанныеКонтрагента = Неопределено) Экспорт
	
	ИмяСвойства =  Перечисление_ИмяСвойствНастройки_ПередаватьМЧДФайлом();
		
	Результат = Настройки_ЗначениеСвойства(ИмяСвойства, ДанныеОрганизации, ДанныеКонтрагента);

	Возврат Результат;
	
КонецФункции

Процедура ОбщиеНастройки_УдалитьЗапись(ИмяНастройки) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		ОбщиеНастройки_УдалитьЗапись_Диадок(ИмяНастройки);	
	Иначе
		ОбщиеНастройки_УдалитьЗапись_1С(ИмяНастройки)	
	КонецЕсли;
	
	Настройки_ОбщиеНастройки_УстановитьКэш(Неопределено);
	
КонецПроцедуры

Процедура ОбщиеНастройки_УдалитьЗапись_Диадок(ИмяНастройки)
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	ДопСправочник.Ссылка
	|ИЗ
	|	Справочник.Диадок_ДополнительныеСправочники КАК ДопСправочник
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ДополнительныеСправочники.ДополнительныеРеквизиты КАК ДопРеквизиты
	|		ПО ДопСправочник.Ссылка = ДопРеквизиты.Ссылка
	|ГДЕ
	|	ДопСправочник.Наименование = ""ОбщиеНастройки""
	|	И ДопРеквизиты.ИмяРеквизита = &ИмяРеквизита"
	);
	Запрос.УстановитьПараметр("ИмяРеквизита", ИмяНастройки);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		
		ОбщиеНастройкиОбъект = Выборка.Ссылка.ПолучитьОбъект();
	
		ДопРеквизит = ОбщиеНастройкиОбъект.ДополнительныеРеквизиты.Найти(ИмяНастройки, "ИмяРеквизита");
		ОбщиеНастройкиОбъект.ДополнительныеРеквизиты.Удалить(ДопРеквизит);
		
		ОбщиеНастройкиОбъект.ОбменДанными.Загрузка = Истина;
		ОбщиеНастройкиОбъект.Записать();
		
	КонецЕсли;
		
КонецПроцедуры

Процедура ОбщиеНастройки_УдалитьЗапись_1С(ИмяНастройки)
	
	ИмяСПрефиксом = Истина;
	
	Свойство = Свойства_НайтиСвойство(ИмяНастройки, ИмяСПрефиксом);
	Если Не ЗначениеЗаполнено(Свойство) Тогда
		Возврат;
	КонецЕсли;
	
	ОбъектОбщихНастроек = ОбъектОбщихНастроек_1С();
	Если ЗначениеЗаполнено(ОбъектОбщихНастроек) Тогда
		ДопРеквизиты_УдалитьЗначение_1С(ОбъектОбщихНастроек, Свойство);
	КонецЕсли;
	
КонецПроцедуры

Процедура Настройки_ЗаписатьАвтоматическаяОтправкаПриглашенийПоОрганизациям(СписокЗначений) Экспорт

	Свойство = Перечисление_ИменаСвойствНастроек_1С().АвтоматическаяОтправкаПриглашений;
	Для Каждого Элемент Из СписокЗначений Цикл
		ДопРеквизиты_УстановитьЗначение_1С(Элемент.Значение, Свойство, Элемент.Пометка);
	КонецЦикла;
	
КонецПроцедуры

Функция Настройки_АвтоматическаяОтправкаПриглашенийПоОрганизациям(Ссылка) Экспорт

	Свойство = Перечисление_ИменаСвойствНастроек_1С().АвтоматическаяОтправкаПриглашений;
	Возврат ДопРеквизиты_Значение_1С(Ссылка, Свойство) = Истина;
	
КонецФункции

// Тексты запросов

Функция ТекстЗапроса_НастройкиКонтрагента_Диадок()
	
	Результат = 
	"ВЫБРАТЬ
	|	НастройкиКонтрагентов.ИмяСвойства 	КАК ИмяСвойства,
	|	НастройкиКонтрагентов.Значение  	КАК Значение
	|	//ДОП_ПОЛЯ
	|//ИМЯ_ВРЕМЕННОЙ_ТАБЛИЦЫ
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК НастройкиКонтрагентов
	|ГДЕ
	|	НастройкиКонтрагентов.Организация = &ОрганизацияЭДО
	|	И НастройкиКонтрагентов.Контрагент = &КонтрагентЭДО
	|	И НастройкиКонтрагентов.ИмяСвойства В (&ИменаСвойств)
	|	//ДОП_ОТБОРЫ";
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_НастройкиКонтрагентаСПриведениемКТипуЗначений_Диадок(ВидДопСправочника)
	
	Результат = ТекстЗапроса_НастройкиКонтрагента_Диадок();
	
	Результат = СтрЗаменить(Результат, "//ИМЯ_ВРЕМЕННОЙ_ТАБЛИЦЫ", "ПОМЕСТИТЬ ВремЗначенияНастроек");

	Результат = Результат + ТекстЗапроса_РазделительЗапросов() + 
	"ВЫБРАТЬ
	|	ЗначенияНастроек.ИмяСвойства 	КАК ИмяСвойства,
	|	ЗначенияНастроек.Значение  		КАК Значение
	|ПОМЕСТИТЬ ВремСтроковыеЗначенияНастроек
	|ИЗ
	|	ВремЗначенияНастроек КАК ЗначенияНастроек
	|ГДЕ
	|	ТИПЗНАЧЕНИЯ(ЗначенияНастроек.Значение) = ТИП(СТРОКА)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ДопРеквизиты.Ссылка					КАК Ссылка,
	|	СтроковыеЗначенияНастроек.Значение 	КАК Значение
	|ПОМЕСТИТЬ ВремСсылкиНаЗначенияНастроек
	|ИЗ
	|	Справочник.Диадок_ДополнительныеСправочники.ДополнительныеРеквизиты КАК ДопРеквизиты
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ДополнительныеСправочники КАК ДопСправочники
	|		ПО ДопРеквизиты.Ссылка = ДопСправочники.Ссылка
	|			И (ДопСправочники.ИмяСправочника = """ + ВидДопСправочника + """)
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВремСтроковыеЗначенияНастроек КАК СтроковыеЗначенияНастроек
	|		ПО ДопРеквизиты.Значение = СтроковыеЗначенияНастроек.Значение
	|ГДЕ  
	|	НЕ ДопСправочники.Ссылка ЕСТЬ NULL
	|	И ДопРеквизиты.ИмяРеквизита = ""ID""
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	НастройкиКонтрагентов.ИмяСвойства КАК ИмяСвойства,
	|	ВЫБОР
	|		КОГДА СсылкиНаЗначенияНастроек.Ссылка ЕСТЬ NULL
	|			ТОГДА НастройкиКонтрагентов.Значение
	|		ИНАЧЕ СсылкиНаЗначенияНастроек.Ссылка
	|	КОНЕЦ КАК Значение
	|ИЗ
	|	ВремЗначенияНастроек КАК НастройкиКонтрагентов
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВремСсылкиНаЗначенияНастроек КАК СсылкиНаЗначенияНастроек
	|		ПО НастройкиКонтрагентов.Значение = СсылкиНаЗначенияНастроек.Значение";
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ЗначенияСвойствОбъекта_1С()
	
	Результат =	
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЗначенияСвойств.Свойство				КАК Свойство,
	|	ЗначенияСвойств.Свойство.Наименование 	КАК ИмяСвойства,
	|	ЗначенияСвойств.Значение 				КАК Значение
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойств
	|ГДЕ
	|	ЗначенияСвойств.Свойство В (&Свойства)
	|	И ЗначенияСвойств.Объект = &Объект";
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Результат);
	
	Возврат Результат;
	
КонецФункции

//*************************************************
// НАСТРОЙКИ: ПОДСИСТЕМА ДИАДОКА

// Структура значений строк элемента "ОбщиеНастройки" в справочнике "Диадок_ДополнительныеСправочники"
Функция Настройки_ОбщиеНастройки_Диадок()
	
	Результат = Контракт_ОбщиеНастройки();
	
	ОбщиеНастройкиСсылка = ДопСправочники_НайтиПоНаименованию_Диадок("ОбщиеНастройки", "ОбщиеНастройки");
	
	Если ОбщиеНастройкиСсылка <> Неопределено Тогда
		
		Для Каждого ТекНастройка Из Результат Цикл
			
			ЗначениеНастройки = ДопРеквизиты_Значение(ОбщиеНастройкиСсылка, ТекНастройка.Ключ);
			
			Если ЗначениеНастройки <> Неопределено Тогда
				Результат[ТекНастройка.Ключ] = ЗначениеНастройки;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Настройки_ЗаписатьОбщиеНастройки_Диадок(СтруктураНастроек)
	
	Результат = Новый Массив;
	
	ОбщиеНастройкиСсылка = ДопСправочники_НайтиПоНаименованию_Диадок("ОбщиеНастройки", "ОбщиеНастройки");
	
	Если ОбщиеНастройкиСсылка = Неопределено Тогда
		
		ОбщиеНастройкиОбъект = Справочники.Диадок_ДополнительныеСправочники.СоздатьЭлемент();
		ОбщиеНастройкиОбъект.Наименование	= "ОбщиеНастройки";
		ОбщиеНастройкиОбъект.ИмяСправочника	= "ОбщиеНастройки";
		ОбщиеНастройкиОбъект.УстановитьНовыйКод();
		
	Иначе
		ОбщиеНастройкиОбъект = ОбщиеНастройкиСсылка.ПолучитьОбъект();
	КонецЕсли;
	
	Для Каждого ТекНастройка Из СтруктураНастроек Цикл
		
		Если ТекНастройка.Ключ = "СлужебнаяУчетнаяЗаписьПароль" Тогда
			СохраняемоеЗначение = Новый ХранилищеЗначения(ТекНастройка.Значение);
		Иначе
			СохраняемоеЗначение = ТекНастройка.Значение;
		КонецЕсли;
		
		ДопРеквизиты_УстановитьЗначение(ОбщиеНастройкиОбъект, ТекНастройка.Ключ, СохраняемоеЗначение);
		
	КонецЦикла;
	
	ОбщиеНастройкиОбъект.Записать();
	
	Возврат Результат;
	
КонецФункции

Функция Настройки_ЗначениеСвойства_Диадок(ИмяСвойства, ДанныеОрганизации, ДанныеКонтрагента = Неопределено)
	
	СтруктураДанных = Новый Структура;
	СтруктураДанных.Вставить("ОрганизацияЭДО",	ДанныеОрганизации.Ссылка);
	СтруктураДанных.Вставить("ИмяСвойства",		ИмяСвойства);
	
	Если ДанныеКонтрагента = Неопределено Тогда
		СтруктураДанных.Вставить("КонтрагентЭДО", Контрагенты_ПустаяСсылка_Диадок());
	Иначе
		СтруктураДанных.Вставить("КонтрагентЭДО", ДанныеКонтрагента.Ссылка);
	КонецЕсли;
	
	Результат = Справочники_ЗначениеЗаписиНастроекКонтрагентов_Диадок(СтруктураДанных);
	
	Возврат Результат;
	
КонецФункции

Процедура Настройки_ЗаписатьЗначенияСвойств_Диадок(КоллекцияСвойств, ДанныеОрганизации, ДанныеКонтрагента = Неопределено)
	
	Для Каждого Элемент Из КоллекцияСвойств Цикл
		
		СтруктураДанных = Новый Структура;
		
		СтруктураДанных.Вставить("ОрганизацияЭДО",	ДанныеОрганизации.Ссылка);
		СтруктураДанных.Вставить("ИмяСвойства",		Элемент.Ключ);
		СтруктураДанных.Вставить("Значение",		Элемент.Значение);
		
		Если ДанныеКонтрагента = Неопределено Тогда
			СтруктураДанных.Вставить("КонтрагентЭДО", Контрагенты_ПустаяСсылка_Диадок());
		Иначе
			СтруктураДанных.Вставить("КонтрагентЭДО", ДанныеКонтрагента.Ссылка);
		КонецЕсли;
		
		Справочники_ЗаписатьЗначениеНастроекКонтрагентов_Диадок(СтруктураДанных);
	
	КонецЦикла;
	
КонецПроцедуры


//*************************************************
// НАСТРОЙКИ: 1С

Функция Настройки_ОбщиеНастройки_1С()
	
	Результат = Контракт_ОбщиеНастройки();
	
	СтруктураНастроек = Свойства_ОбщиеНастройки();
	ЗаполнитьЗначенияСвойств(Результат, СтруктураНастроек);
	
	Результат.ПодключаемыйМодульСсылка = ПодключаемыйМодульСсылка(СтруктураНастроек);
	
	// TODO - сократить названия настроек с учетом ограничения: длина строки в регистре 50 - длина префикса "DDPro_"
	Если СтруктураНастроек.Свойство("СохранениеДокументовИмяКаталогаПакетаПоУмолч") Тогда
		ИмяКаталогаПакетаПоУмолчанию = Настройки_ЗначениеНастройки_1С(СтруктураНастроек, "СохранениеДокументовИмяКаталогаПакетаПоУмолч");
		Результат.СохранениеДокументовИмяКаталогаПакетаПоУмолчанию = ИмяКаталогаПакетаПоУмолчанию;
	КонецЕсли;
	
	Если СтруктураНастроек.Свойство("СохранениеДокументовИмяКаталогаПакета") Тогда
		ИмяКаталогаПакета = Настройки_ЗначениеНастройки_1С(СтруктураНастроек, "СохранениеДокументовИмяКаталогаПакета");
		Результат.СохранениеДокументовИмяКаталогаПакета = ИмяКаталогаПакета;
	КонецЕсли;
		
	Если СтруктураНастроек.Свойство("ЗагружатьДокументыПоСопоставленнымКонтрагент") Тогда
		Результат.ЗагружатьДокументыПоСопоставленнымКонтрагентам = СтруктураНастроек.ЗагружатьДокументыПоСопоставленнымКонтрагент;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Настройки_ЗаписатьОбщиеНастройки_1С(СтруктураНастроек)
	
	Результат = Новый Массив;
	
	НачатьТранзакцию();
	
	Попытка
		
		Если ЗначениеЗаполнено(СтруктураНастроек.ПодключаемыйМодульСсылка)
			И Метаданные.Справочники.Содержит(СтруктураНастроек.ПодключаемыйМодульСсылка.Метаданные()) Тогда
			
			СтруктураНастроек.Вставить("ПодключаемыйМодульИмяСправочника",	СтруктураНастроек.ПодключаемыйМодульСсылка.Метаданные().Имя);
			СтруктураНастроек.Вставить("ПодключаемыйМодульСсылка",			Строка(СтруктураНастроек.ПодключаемыйМодульСсылка.УникальныйИдентификатор()));
			
		КонецЕсли;
		
 		Результат = Настройки_МассивОшибок_1С(СтруктураНастроек);
		
		Если Результат.Количество() = 0 Тогда
			
			Для Каждого ТекНастройка Из СтруктураНастроек Цикл
				
				// TODO - сократить названия настроек с учетом ограничения: длина строки в регистре 50 - длина префикса "DDPro_"
				Если ТекНастройка.Ключ = "СохранениеДокументовИмяКаталогаПакетаПоУмолчанию" Тогда
					
					Настройки_СохранитьЗначениеНастройки_1С(ТекНастройка.Значение, "СохранениеДокументовИмяКаталогаПакетаПоУмолч");					
					
				ИначеЕсли ТекНастройка.Ключ = "ЗагружатьДокументыПоСопоставленнымКонтрагентам" Тогда
					
					ДопРеквизиты_УстановитьЗначение(, "ЗагружатьДокументыПоСопоставленнымКонтрагент", ТекНастройка.Значение);
					
				ИначеЕсли ТекНастройка.Ключ = "СохранениеДокументовИмяКаталогаПакета" Тогда
					
					Настройки_СохранитьЗначениеНастройки_1С(ТекНастройка.Значение, "СохранениеДокументовИмяКаталогаПакета");
									
				Иначе
					
					ДопРеквизиты_УстановитьЗначение(, ТекНастройка.Ключ, ТекНастройка.Значение);
					
				КонецЕсли;	
				
			КонецЦикла;
			
		КонецЕсли;
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		Если ТранзакцияАктивна() Тогда
			ОтменитьТранзакцию();
		КонецЕсли;
		
		_Ошибка = ОписаниеОшибки();
		
		ВызватьИсключение;
		
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции

Функция Настройки_МассивОшибок_1С(СтруктураНастроек)
	
	Результат = Новый Массив;
	
	Если ТекущийРежимЗапускаПриложения() = "Обычное приложение" Тогда
		
		НастройкиДляПроверки = 
		"ПодключаемыйМодульИмяОбработки
		|ПодключаемыйМодульПуть
		|СохранениеДокументовПуть
		|КорневойКаталогДокументов
		|ПутьКМодулюВСправочнике1С";
		
		МассивНастроек = РазделитьСтрокуЛок(НастройкиДляПроверки, Символы.ПС);
		
		Для Каждого ИмяНастройки Из МассивНастроек Цикл
			
			ЗначениеНастройки = СтруктураНастроек[ИмяНастройки];
			
			Если СтрДлина(ЗначениеНастройки) > 50 Тогда
				ТекстОшибки = Настройки_ЗаполнитьОшибкуПроверки_1С(ИмяНастройки);
				Результат.Добавить(ТекстОшибки);
			КонецЕсли;
			
		КонецЦикла;				
		
	КонецЕсли;	
	
	Возврат Результат;
	
КонецФункции

Функция Настройки_ЗаполнитьОшибкуПроверки_1С(ИмяНастройки)
	
	Результат = "";
	
	ОписаниеОшибки	= НСтр("ru = 'Длина пути в настройке %1 должна быть меньше 50 символов'") + Символы.ПС;
	
	Результат = СтрШаблонЛок(ОписаниеОшибки, ИмяНастройки);
						
	Возврат Результат;
	
КонецФункции

Функция Настройки_ЗначениеНастройки_1С(СтруктураНастроек, ИмяНастройки)
	
	Если ТекущийРежимЗапускаПриложения() = "Обычное приложение" Тогда
		Результат = ХранилищеОбщихНастроек.Загрузить("НастройкиКонтурЭДО", ИмяНастройки);
	Иначе
		Результат = СтруктураНастроек[ИмяНастройки];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура Настройки_СохранитьЗначениеНастройки_1С(ЗначениеНастройки, ИмяНастройки)
	
	Если ТекущийРежимЗапускаПриложения() = "Обычное приложение" Тогда
		ХранилищеОбщихНастроек.Сохранить("НастройкиКонтурЭДО", ИмяНастройки, ЗначениеНастройки);
	Иначе
		ДопРеквизиты_УстановитьЗначение(, ИмяНастройки, ЗначениеНастройки);
	КонецЕсли;
	
КонецПроцедуры

Функция Настройки_ЗначениеСвойства_1С(ИмяСвойства, ДанныеОрганизации, ДанныеКонтрагента = Неопределено)
	
	Если ДанныеКонтрагента <> Неопределено Тогда
		ЭлементХраненияСвойств = ДанныеКонтрагента.СвязанныйСправочник1;
	Иначе
		ЭлементХраненияСвойств = ДанныеОрганизации.СвязанныйСправочник1;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ЭлементХраненияСвойств) Тогда
		Результат = ДопРеквизиты_Значение_1С(ЭлементХраненияСвойств, ИмяСвойства);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура Настройки_ЗаписатьЗначенияСвойств_1С(КоллекцияСвойств, ДанныеОрганизации, ДанныеКонтрагента = Неопределено)
	
	ХранителиСвойств = СвязанныеСправочники(ДанныеОрганизации, ДанныеКонтрагента);
	
	Для Каждого ЭлементКоллекции Из ХранителиСвойств Цикл
				
		Для Каждого Элемент Из КоллекцияСвойств Цикл
			ДопРеквизиты_УстановитьЗначение_1С(ЭлементКоллекции.Ссылка, Элемент.Ключ, Элемент.Значение);
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

//}		НАСТРОЙКИ


///////////////////////////////////////////////////////////////////////////////
//{		НАСТРОЙКИ ПЕЧАТНЫХ ФОРМ

// Записывает в базу настройки печатных форм для вида документа в разрезе организации и/или контрагента,
// удаляя при этом ранее записанные данные.
//
// Для подсистемы хранения 1С настройки могут храниться только в одном разрезе: или организации, или контрагента.
// Подсистема хранения Диадок может хранить настройки одновременно в разрезе организации и контрагента.
//
// Настройки хранятся в виде уникальных идентификаторов печатных форм
//
// Параметры:
//  ВидДокумента			 - 	Строка - идентификатор вида документа: "ID_АктФормализованный", "ID_ТОРГ12" и т.д.
//  НастройкиПечатныхФорм	 - 	Структура - см. функцию Ядро.Новый_НастройкиПечатныхФорм()
//  ДанныеОрганизации		 - 	Структура - см. функцию Ядро.Контракт_ОрганизацияЭДО()
//  ДанныеКонтрагента		 - 	Структура - см. функцию Ядро.Контракт_КонтрагентЭДО()
//
Процедура НастройкиПечатныхФорм_Записать(НастройкиПечатныхФорм, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт 
	
	КоллекцияСвойств = Новый Соответствие;
	
	Для Каждого ЭлементКоллекции Из НастройкиПечатныхФорм Цикл
		
		ВидДокумента			= ЭлементКоллекции.Ключ;
		НастройкаПечатныхФорм	= ЭлементКоллекции.Значение;
		
		Если Не НастройкаПечатныхФорм.ОсновнаяПечатнаяФорма = Неопределено Тогда
			КоллекцияСвойств.Вставить("ПФ_" + ВидДокумента, НастройкаПечатныхФорм.ОсновнаяПечатнаяФорма.УникальныйИдентификатор());
		КонецЕсли;
		
		НастройкиПечатныхФорм_Удалить(ВидДокумента, ДанныеОрганизации, ДанныеКонтрагента);
		
		НастройкиПечатныхФорм_СоздатьКоллекциюНастроек(ВидДокумента, КоллекцияСвойств, НастройкаПечатныхФорм);
		
	КонецЦикла;
	
	Настройки_ЗаписатьЗначенияСвойств(КоллекцияСвойств, ДанныеОрганизации, ДанныеКонтрагента);
	
КонецПроцедуры

Процедура НастройкиПечатныхФорм_СоздатьКоллекциюНастроек(ВидДокумента, КоллекцияСвойств, НастройкаПечатныхФорм)
	
	Для сц = 1 По НастройкаПечатныхФорм.ДополнительныеПечатныеФормы.Количество() Цикл
		
		ПечатнаяФорма = НастройкаПечатныхФорм.ДополнительныеПечатныеФормы[сц - 1];
		КоллекцияСвойств.Вставить("ПФ_" + Формат(сц, "ЧГ=0") + "_" + ВидДокумента, ПечатнаяФорма.УникальныйИдентификатор());
		
	КонецЦикла;
	
КонецПроцедуры

// Возвращает сохраненные в информационной базе настройки печатных форм в разрезе видов документов для организации или контрагента,
// если для него были включена индивидуальная настройка.
//
// Параметры:
//  ИдентификаторыВидовДокументов	 - Массив 		- массив строк идентификаторов видов документов
//														(пример ид. видов документов: ID_УПД, ID_УКД, ID_ТОРГ12, ID_СчетФактура и др).
//  ДанныеОрганизации				 - Структура	- заполненный контракт данных организации (см. Контракт_ОрганизацияЭДО)
//  ДанныеКонтрагента				 - Структура	- заполненный контракт данных контрагента (см. Контракт_КонтрагентЭДО). Необязательный параметр. 
// 
// Возвращаемое значение:
//  Соответствие - Ключ - ид. вида документа, значение - настройки ПФ (см. НастройкиПечатныхФорм_НовыеНастройкиПоВидамДокументов)
//
Функция НастройкиПечатныхФорм_ПоВидамДокументов(ИдентификаторыВидовДокументов, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт 
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = НастройкиПечатныхФорм_ПоВидамДокументов_Диадок(ИдентификаторыВидовДокументов, ДанныеОрганизации, ДанныеКонтрагента);
	Иначе
		Результат = НастройкиПечатныхФорм_ПоВидамДокументов_1С(ИдентификаторыВидовДокументов, ДанныеОрганизации, ДанныеКонтрагента);
	КонецЕсли;
		
	Возврат Результат;
	
КонецФункции

// см. НастройкиПечатныхФорм_ПоВидамДокументов
//
Функция НастройкиПечатныхФорм_ПоВидамДокументов_Диадок(ИдентификаторыВидовДокументов, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) 

	Результат = НастройкиПечатныхФорм_НовыеНастройкиПоВидамДокументов(ИдентификаторыВидовДокументов);
	
	Если ДанныеКонтрагента = Неопределено Тогда
		ТекстЗапроса = ТекстЗапроса_НастройкиПечатныхФормОрганизацииПоВидамДокументов_Диадок(ИдентификаторыВидовДокументов);
	Иначе
		ТекстЗапроса = ТекстЗапроса_НастройкиПечатныхФормКонтрагентаПоВидамДокументов_Диадок(ИдентификаторыВидовДокументов);
	КонецЕсли;

	Запрос = Новый Запрос(ТекстЗапроса);
	
	Настройки_ЗаполнитьОбщиеПараметрыЗапроса_Диадок(Запрос, ДанныеОрганизации, ДанныеКонтрагента);

	ВыборкаВидовДокументов = Запрос.Выполнить().Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	Пока ВыборкаВидовДокументов.Следующий() Цикл
		
		НовыеНастройки = Результат.Получить(ВыборкаВидовДокументов.ВидДокумента);
		
		ИмяСвойстваОсновнойПечатнойФормы = "ПФ_" + ВыборкаВидовДокументов.ВидДокумента;
		
		Выборка = ВыборкаВидовДокументов.Выбрать();
		НастройкиПечатныхФорм_Заполнить(НовыеНастройки, Выборка, ИмяСвойстваОсновнойПечатнойФормы);
		
		НастройкиПечатныхФорм_ДополнитьСсылкамиНаВнешниеПечатныеФормы(НовыеНастройки);
		
		Результат.Вставить(ВыборкаВидовДокументов.ВидДокумента, НовыеНастройки);
		
	КонецЦикла;
	
	Возврат Результат;

КонецФункции

// см. НастройкиПечатныхФорм_ПоВидамДокументов
//
Функция НастройкиПечатныхФорм_ПоВидамДокументов_1С(ИдентификаторыВидовДокументов, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) 
	
	Результат = НастройкиПечатныхФорм_НовыеНастройкиПоВидамДокументов(ИдентификаторыВидовДокументов);

	ТекстЗапроса = ТекстЗапроса_НастройкиПечатныхФормПоВидамДокументов_1С(ИдентификаторыВидовДокументов);

	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("Организация", ДанныеОрганизации.СвязанныйСправочник1);
	
	Если ДанныеКонтрагента = Неопределено Тогда
		Запрос.УстановитьПараметр("Контрагент", ДанныеОрганизации.СвязанныйСправочник1);
	Иначе
		Запрос.УстановитьПараметр("Контрагент", ДанныеКонтрагента.СвязанныйСправочник1);
	КонецЕсли;

	ВыборкаВидовДокументов = Запрос.Выполнить().Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	Пока ВыборкаВидовДокументов.Следующий() Цикл
		
		НовыеНастройки = Результат.Получить(ВыборкаВидовДокументов.ИдВидаДокумента);
		
		ИмяСвойстваОсновнойПечатнойФормы = ПрефиксНастройки_1С(Истина) + "ПФ_" + ВыборкаВидовДокументов.ИдВидаДокумента;
		
		Выборка = ВыборкаВидовДокументов.Выбрать();
		НастройкиПечатныхФорм_Заполнить(НовыеНастройки, Выборка, ИмяСвойстваОсновнойПечатнойФормы);
		
		НастройкиПечатныхФорм_ДополнитьСсылкамиНаВнешниеПечатныеФормы(НовыеНастройки);
		
		Результат.Вставить(ВыборкаВидовДокументов.ИдВидаДокумента, НовыеНастройки);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Дополняет контракт с настройками печатных форм ссылка на элемент справочника доп. обработок (внешнюю печатную форму)
//
Процедура НастройкиПечатныхФорм_ДополнитьСсылкамиНаВнешниеПечатныеФормы(Настройки)
	
	ИмяСправочника = Свойства_ИмяСправочникаДополнительныхОбработок();
	
	Если ЗначениеЗаполнено(Настройки.ОсновнаяПечатнаяФорма) Тогда
		ИдентификаторПечатнойФормы = Новый УникальныйИдентификатор(Настройки.ОсновнаяПечатнаяФорма);
		Настройки.ОсновнаяПечатнаяФорма = Справочники[ИмяСправочника].ПолучитьСсылку(ИдентификаторПечатнойФормы);
	КонецЕсли;
	
	Для ИндексСтроки = 0 По Настройки.ДополнительныеПечатныеФормы.ВГраница() Цикл
		ИдентификаторПечатнойФормы = Новый УникальныйИдентификатор(Настройки.ДополнительныеПечатныеФормы[ИндексСтроки]);
		Настройки.ДополнительныеПечатныеФормы[ИндексСтроки] = Справочники[ИмяСправочника].ПолучитьСсылку(ИдентификаторПечатнойФормы);
	КонецЦикла;
	
КонецПроцедуры

// Возвращает коллекцию контрактов настроек печатных форм в разрезе видов документов 
//
// Параметры:
//  ИдентификаторыВидовДокументов	 - Массив 		- массив строк идентификаторов видов документов
//														(пример ид. видов документов: ID_УПД, ID_УКД, ID_ТОРГ12, ID_СчетФактура и др).
// 
// Возвращаемое значение:
//  Соответствие - Ключ - ид. вида документа, значение - контракт настроек ПФ (см. Ядро.Новый_НастройкиПечатныхФорм)
//
Функция НастройкиПечатныхФорм_НовыеНастройкиПоВидамДокументов(ИдентификаторыВидовДокументов)
	
	Результат = Новый Соответствие;
	
	Для Каждого ИдВидаДокумента Из ИдентификаторыВидовДокументов Цикл
		
		НовыеНастройки = Ядро.Новый_НастройкиПечатныхФорм();	
		
		Результат.Вставить(ИдВидаДокумента, НовыеНастройки);
		
	КонецЦикла;
		
	Возврат Результат;
	
КонецФункции

// Считывает из базы настройки печатных форм для вида документа по указанной организации и/или контрагенту
//
// Параметры:
//  ВидДокумента		 - 	Строка - Идентификатор вида документа: "ID_АктФормализованный", "ID_ТОРГ12" и т.д.
//  ДанныеОрганизации	 - 	Структура - см. функцию Ядро.Контракт_ОрганизацияЭДО()
//  ДанныеКонтрагента	 - 	Структура - см. функцию Ядро.Контракт_КонтрагентЭДО()
// 
// Возвращаемое значение:
//  Структура - см. функцию Ядро.Новый_НастройкиПечатныхФорм()
//
Функция НастройкиПечатныхФорм_Прочитать(ВидДокумента, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт 
	
	ИдВидовДокументов = Новый Массив;
	ИдВидовДокументов.Добавить(ВидДокумента);
	
	НастройкиПФ = НастройкиПечатныхФорм_ПоВидамДокументов(ИдВидовДокументов, ДанныеОрганизации, ДанныеКонтрагента);
	
	Результат = НастройкиПФ.Получить(ВидДокумента);
	
	Возврат Результат;
	
КонецФункции

// Заполняет настройки печатных форм данными заполнения
//
// Параметры:
//  НастройкиПечатныхФорм				 - 	Структура - см. функцию Ядро.Новый_НастройкиПечатныхФорм()
//  ДанныеЗаполнения					 - 	ВыборкаИзРезультатаЗапроса - Выборка с полями "ИмяСвойства", "Значение"
//  ИмяСвойстваОсновнойПечатнойФормы	 - 	Строка - Имя свойства вида : "ПФ_ID_АктФормализованный", "DDPro_ПФ_ID_АктФормализованный" и т.д.
//
Процедура НастройкиПечатныхФорм_Заполнить(НастройкиПечатныхФорм, ДанныеЗаполнения, ИмяСвойстваОсновнойПечатнойФормы)
	
	Пока ДанныеЗаполнения.Следующий() Цикл
		
		Если ДанныеЗаполнения.ИмяСвойства = ИмяСвойстваОсновнойПечатнойФормы Тогда
			НастройкиПечатныхФорм.ОсновнаяПечатнаяФорма = ДанныеЗаполнения.Значение;
		Иначе
			НастройкиПечатныхФорм.ДополнительныеПечатныеФормы.Добавить(ДанныеЗаполнения.Значение);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура НастройкиПечатныхФорм_Удалить(ВидДокумента, ДанныеОрганизации, ДанныеКонтрагента = Неопределено) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		НастройкиПечатныхФорм_Удалить_Диадок(ВидДокумента, ДанныеОрганизации, ДанныеКонтрагента);
	Иначе
		НастройкиПечатныхФорм_Удалить_1С(ВидДокумента, ДанныеОрганизации, ДанныеКонтрагента);
	КонецЕсли;
	
КонецПроцедуры

Процедура НастройкиПечатныхФорм_Удалить_Диадок(ВидДокумента, ДанныеОрганизации, ДанныеКонтрагента)
	
	Организация = ДанныеОрганизации.Ссылка;
	Контрагент = ?(ДанныеКонтрагента = Неопределено, Контрагенты_ПустаяСсылка_Диадок(), ДанныеКонтрагента.Ссылка);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Диадок_НастройкиКонтрагентов.ИмяСвойства КАК ИмяСвойства
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
	|ГДЕ
	|	Диадок_НастройкиКонтрагентов.Организация = &Организация
	|	И Диадок_НастройкиКонтрагентов.Контрагент = &Контрагент
	|	И Диадок_НастройкиКонтрагентов.ИмяСвойства ПОДОБНО &ИмяСвойства";
	
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("Контрагент", Контрагент);
	Запрос.УстановитьПараметр("ИмяСвойства", "ПФ_%" + ВидДокумента);
	
	Результат = Запрос.Выполнить();
	
	Если Результат.Пустой() Тогда
		Возврат;
	КонецЕсли;
	
	НаборЗаписей = РегистрыСведений.Диадок_НастройкиКонтрагентов.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.Организация.Установить(Организация);
	НаборЗаписей.Отбор.Контрагент.Установить(Контрагент);
	
	Выборка = Результат.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		НаборЗаписей.Отбор.ИмяСвойства.Установить(Выборка.ИмяСвойства);
		НаборЗаписей.Записать();
	КонецЦикла;
	
КонецПроцедуры

Процедура НастройкиПечатныхФорм_Удалить_1С(ВидДокумента, ДанныеОрганизации, ДанныеКонтрагента)
	
	ХранителиСвойств = СвязанныеСправочники(ДанныеОрганизации, ДанныеКонтрагента);
				
	Если ХранителиСвойств.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
			
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЗначенияСвойствОбъектов.Свойство КАК Свойство
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|		ЛЕВОЕ СОЕДИНЕНИЕ ПланВидовХарактеристик.СвойстваОбъектов КАК СвойстваОбъектов
	|		ПО ЗначенияСвойствОбъектов.Свойство = СвойстваОбъектов.Ссылка
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Объект = &Объект
	|	И СвойстваОбъектов.Наименование ПОДОБНО &ИмяСвойства";
	
	ИмяСвойства = ПрефиксНастройки_1С(Истина) + "ПФ_%" + ВидДокумента;
	
	Запрос.УстановитьПараметр("Объект"		, ХранителиСвойств[0].Ссылка);
	Запрос.УстановитьПараметр("ИмяСвойства"	, ИмяСвойства);
	
	ИмяРегистра = Свойства_ИмяРегистраСведений();
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	ИмяПланаВидовХарактеристик = Свойства_ИмяПланаВидовХарактеристик();
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "ПланВидовХарактеристик.СвойстваОбъектов", "ПланВидовХарактеристик." + ИмяПланаВидовХарактеристик);
	
	Результат = Запрос.Выполнить();
	
	Если НЕ Результат.Пустой() Тогда
		
		Выборка = Результат.Выбрать();
		
		Пока Выборка.Следующий() Цикл
			
			Для Каждого ИскомыйОбъект Из ХранителиСвойств Цикл
				
				НаборЗаписей = РегистрыСведений[ИмяРегистра].СоздатьНаборЗаписей();
				НаборЗаписей.Отбор.Объект.Установить(ИскомыйОбъект.Ссылка);
				НаборЗаписей.Отбор.Свойство.Установить(Выборка.Свойство);
				НаборЗаписей.Записать();
				
			КонецЦикла;
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

// Тексты запросов

Функция ТекстЗапроса_НастройкиПечатныхФормКонтрагентаПоВидамДокументов_Диадок(ИдентификаторыВидовДокументов)

	ТекстЗапросаНастроекОрганизации = ТекстЗапроса_НастройкиПечатныхФормПоВидамДокументов_Диадок(
		ИдентификаторыВидовДокументов, "ВремНастройкиПФОрганизации");
	ТекстЗапросаНастроекКонтрагента = ТекстЗапроса_НастройкиПечатныхФормПоВидамДокументов_Диадок(
		ИдентификаторыВидовДокументов, "ВремНастройкиПФКонтрагента");

	ТекстЗапросаНастроекОрганизации = СтрЗаменить(ТекстЗапросаНастроекОрганизации, "&КонтрагентЭДО", "ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)");
	
	ТекстДопУсловия = "И НастройкиКонтрагентов.Контрагент <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)";
	ТекстЗапросаНастроекКонтрагента = СтрЗаменить(ТекстЗапросаНастроекКонтрагента, "//ДОП_УСЛОВИЯ", ТекстДопУсловия);

	Результат = ТекстЗапросаНастроекКонтрагента + ТекстЗапроса_РазделительЗапросов() + ТекстЗапросаНастроекОрганизации;
	
	Результат = Результат + ТекстЗапроса_РазделительЗапросов() +
	"ВЫБРАТЬ
	|	НастройкиПФКонтрагента.ИмяСвойства 		КАК ИмяСвойства,
	|	НастройкиПФКонтрагента.Значение 		КАК Значение,
	|	НастройкиПФКонтрагента.ВидДокумента 	КАК ВидДокумента
	|ИЗ
	|	ВремНастройкиПФКонтрагента КАК НастройкиПФКонтрагента
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	НастройкиПФОрганизации.ИмяСвойства,
	|	НастройкиПФОрганизации.Значение,
	|	НастройкиПФОрганизации.ВидДокумента
	|ИЗ
	|	ВремНастройкиПФОрганизации КАК НастройкиПФОрганизации
	|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ КОЛИЧЕСТВО(Значение) КАК Количество ИЗ ВремНастройкиПФКонтрагента) КАК ВремКоличествоНастроекКонтрагента
	|		ПО (ИСТИНА)
	|ГДЕ
	|	ЕСТЬNULL(ВремКоличествоНастроекКонтрагента.Количество, 0) = 0
	|
	|ИТОГИ ПО
	|	ВидДокумента";
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_НастройкиПечатныхФормОрганизацииПоВидамДокументов_Диадок(ИдентификаторыВидовДокументов)

	Результат = ТекстЗапроса_НастройкиПечатныхФормПоВидамДокументов_Диадок(ИдентификаторыВидовДокументов);
	
	ИменаПолейИтогов = "ВидДокумента";

	Результат = Результат + ТекстЗапроса_ПоляИтогов(ИменаПолейИтогов);
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_НастройкиПечатныхФормПоВидамДокументов_Диадок(ИдентификаторыВидовДокументов, Знач ИмяВременнойТаблицы = "")
	
	Результат = "";
	
	ШаблонТекстаЗапроса = ТекстЗапроса_НастройкиКонтрагента_Диадок();
	
	УдаляемоеУсловие 	= "И НастройкиКонтрагентов.ИмяСвойства В (&ИменаСвойств)";
	НовоеУсловие 		= "И НастройкиКонтрагентов.ИмяСвойства ПОДОБНО ""%2""";
	
	ШаблонТекстаЗапроса = СтрЗаменить(ШаблонТекстаЗапроса, УдаляемоеУсловие, НовоеУсловие);
	ШаблонТекстаЗапроса = СтрЗаменить(ШаблонТекстаЗапроса, "//ДОП_ПОЛЯ", ", ""%1"" КАК ВидДокумента");
	
	ПрефиксНастроекПФ = "ПФ_%";
	
	ШаблонДопПоляЗапроса = ", %1 КАК ВидДокумента";
	
	Для Каждого ВидДокумента Из ИдентификаторыВидовДокументов Цикл
		
		ШаблонИмениСвойства = ПрефиксНастроекПФ + ВидДокумента;
		
		ТекстЗапроса = СтрШаблонЛок(ШаблонТекстаЗапроса, ВидДокумента, ШаблонИмениСвойства);
		
		Если НЕ ПустаяСтрока(ИмяВременнойТаблицы) Тогда
			ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "//ИМЯ_ВРЕМЕННОЙ_ТАБЛИЦЫ", "ПОМЕСТИТЬ " + ИмяВременнойТаблицы);
			ИмяВременнойТаблицы = "";
		КонецЕсли;
		
		Если НЕ ПустаяСтрока(Результат) Тогда
			Результат = Результат + ТекстЗапроса_ОбъединениеЗапросов();
		КонецЕсли;
		
		Результат = Результат + ТекстЗапроса; 
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ВременнаяТаблицаСвойствИспользуемыхДляХраненияНастроекПечатныхФорм_1С(ИдентификаторыВидовДокументов)
	
	Результат = "";
	
	ШаблонТекстаЗапроса_СвойстваНастроекПФ = 
	"ВЫБРАТЬ
	|	Свойства.Ссылка 		КАК Ссылка,
	|	Свойства.Наименование 	КАК ИмяСвойства,
	|	""%1"" 					КАК ИдВидаДокумента
	|%2
	|ИЗ
	|	ПланВидовХарактеристик.СвойстваОбъектов КАК Свойства
	|ГДЕ
	|	Свойства.Наименование ПОДОБНО ""%3""";
	
	ИмяВременнойТаблицы = "ПОМЕСТИТЬ ВремСвойстваПФ";
	ПрефиксНастроекПФ 	= ПрефиксНастройки_1С(Истина) + "ПФ_%";
	
	Для Каждого ИдВидаДокумента Из ИдентификаторыВидовДокументов Цикл
		
		Если НЕ ПустаяСтрока(Результат) Тогда
			Результат = Результат + ТекстЗапроса_ОбъединениеЗапросов();
			ИмяВременнойТаблицы = "";
		КонецЕсли;
		
		ИмяСвойства = ПрефиксНастроекПФ + ИдВидаДокумента;
		
		ВремТекстЗапроса = СтрШаблонЛок(
			ШаблонТекстаЗапроса_СвойстваНастроекПФ,
			ИдВидаДокумента,
			ИмяВременнойТаблицы,
			ИмяСвойства);
		
		Результат = Результат + ВремТекстЗапроса;
		
	КонецЦикла;
	
	ТекстЗапроса_УстановитьИмяПланаВидовХарактеристик(Результат);

	Возврат Результат;
	
КонецФункции

///////////////////////////////////////////////////////////////////////////////
//}		НАСТРОЙКИ ПЕЧАТНЫХ ФОРМ


///////////////////////////////////////////////////////////////////////////////
//{		НАСТРОЙКИ МОДУЛЯ СТАНДАРТ

// Заполняет настройки модуля Страндарт
//
// Возвращаемое значение:
//  Результат - Структура - см. функцию Ядро.Контракт_НастройкиМодуляСтандарт()
//
Функция НастройкиМодуляСтандарт() Экспорт
	
	Результат = Кэш_Прочитать("НастройкиМодуляСтандарт");
	
	Если Результат <> Неопределено Тогда
		Возврат Результат;
	КонецЕсли;

	Результат = Контракт_НастройкиМодуляСтандарт();
	
	НастройкиМодуляСтандарт_ЗаполнитьНастройкиПоДаннымРС(Результат);
	НастройкиМодуляСтандарт_ОбработатьНастройки(Результат);	
	
	Кэш_Поместить("НастройкиМодуляСтандарт", Результат);
	
	Возврат Результат;
	
КонецФункции

// Заполняет настройки модуля Страндарт
//
// Параметры:
//  Результат - Структура - см. функцию Ядро.Контракт_НастройкиМодуляСтандарт()
//
Процедура НастройкиМодуляСтандарт_ЗаполнитьНастройкиПоДаннымРС(Результат)
	
	ИмяРегистра	= Свойства_ИмяРегистраСведений();
	
	Если НЕ ЗначениеЗаполнено(ИмяРегистра) Тогда
		Возврат;
	КонецЕсли;
	
	СоответствиеСвойств = НастройкиМодуляСтандарт_НаименованияСвойствПВХ(ИмяРегистра);

	ОбъединенияЗапросов = "	
	|
	| ОБЪЕДИНИТЬ ВСЕ
	| ";
	
	ТекстЗапроса = "";
	
	Для Каждого Свойство Из СоответствиеСвойств Цикл
		
		Если Не ПустаяСтрока(ТекстЗапроса) Тогда
			ТекстЗапроса = ТекстЗапроса + ОбъединенияЗапросов;
		КонецЕсли;
		
		ТекстЗапроса = ТекстЗапроса + НастройкиМодуляСтандарт_ТекстЗапроса_ДанныеРС(ИмяРегистра, Свойство.Значение, Свойство.Ключ);
		
	КонецЦикла;
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	
	Запрос.УстановитьПараметр("Объект",	ОбъектОбщихНастроек_1С());
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Если Не РезультатЗапроса.Пустой() Тогда
		
		ВыборкаЗапроса = РезультатЗапроса.Выбрать();
	
		Пока ВыборкаЗапроса.Следующий() Цикл
			
			Ключ		= ВыборкаЗапроса.ИмяСвойства;
			Значение	= ВыборкаЗапроса.ЗначениеСвойства;
							
			Результат.Вставить(Ключ, Значение);
				
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

// Подготавливает данные для формирования запроса по доп. сведениям модуля Стандарт
//
// Параметры:
//	ИмяРегистра	- Строка - см. функцию Свойства_ИмяРегистраСведений()
//
// Возвращаемое значение: Соответствие
//
Функция НастройкиМодуляСтандарт_НаименованияСвойствПВХ(ИмяРегистра)
	
	Результат = Новый Соответствие;
	
	Если ИмяРегистра = "ЗначенияСвойствОбъектов" Тогда

		Результат.Вставить("ИспользоватьПодключаемыйМодуль"		, "ДиадокПодключаемыйМодульТип");
		Результат.Вставить("МестонахождениеПодключаемогоМодуля"	, "ДиадокПодключаемыйМодульТип");
		Результат.Вставить("ПодключаемыйМодульПуть"				, "ДиадокПодключаемыйМодульПуть");
		Результат.Вставить("ПодключаемыйМодульСсылка"			, "ДиадокПодключаемыйМодульGUID");
		Результат.Вставить("ПодключаемыйМодульИмяОбработки"		, "ДиадокПодключаемыйМодульИмяОбработки");
		Результат.Вставить("ПодключаемыйМодульИмяСправочника"	, "ДиадокПодключаемыйМодульВидСправочника");
		
	ИначеЕсли ИмяРегистра = "ДополнительныеСведения" Тогда

		Результат.Вставить("ИспользоватьПодключаемыйМодуль"		, "ИспользоватьПодключаемыйМодуль");
		Результат.Вставить("МестонахождениеПодключаемогоМодуля"	, "РасположениеПодключаемогоМодуля");
		Результат.Вставить("ПодключаемыйМодульПуть"				, "ПутьКПодключаемомуМодулю");
		Результат.Вставить("ПодключаемыйМодульСсылка"			, "ПутьКПодключаемомуМодулю");
		Результат.Вставить("ПодключаемыйМодульИмяОбработки"		, "ПутьКПодключаемомуМодулю");
		
	КонецЕсли;
	
	Результат.Вставить("РежимСовместимостиФорматов155и820", "РежимСовместимостиФорматов155и820");
	
	Возврат Результат;
	
КонецФункции	

// Добавляет в текст запроса блок по каждому свойству 
// см. функцию НастройкиМодуляСтандарт_НаименованияСвойствПВХ()
//
// Параметры:
//	ИмяРегистра		- Строка - см. функцию Свойства_ИмяРегистраСведений()
//	ИмяСвойства		- Строка - см. функцию НастройкиМодуляСтандарт_НаименованияСвойствПВХ()
//	ИмяПараметра	- Строка - см. функцию НастройкиМодуляСтандарт_НаименованияСвойствПВХ()
//
// Возвращаемое значение: Строка
//
Функция НастройкиМодуляСтандарт_ТекстЗапроса_ДанныеРС(ИмяРегистра, ИмяСвойства, ИмяПараметра)
	
	ТекстЗапроса = 
	"ВЫБРАТЬ
	|	&ИмяПараметра КАК ИмяСвойства,
	|	ЕСТЬNULL(ЗначенияСвойствОбъектов.Значение,"""") КАК ЗначениеСвойства
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Объект = &Объект
	|	И ЗначенияСвойствОбъектов.Свойство.Наименование = &ИмяСвойства";
	 
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ИмяПараметра",	"""" + ИмяПараметра + """");
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ИмяСвойства",	"""" + ИмяСвойства  + """");
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Возврат ТекстЗапроса;
	
КонецФункции

// Обрабатывает и дозаполняет значения после выполнения запроса
//
// Параметры:
//  Результат	- Структура - см. функцию Ядро.Контракт_НастройкиМодуляСтандарт()
//
Процедура НастройкиМодуляСтандарт_ОбработатьНастройки(Результат)
	
	ПодключаемыйМодульСсылка = Неопределено;
	
	ИмяРегистра	= Свойства_ИмяРегистраСведений();
	
	Если ИмяРегистра = "ЗначенияСвойствОбъектов" Тогда
			
		Результат.ИспользоватьПодключаемыйМодуль = Результат.МестонахождениеПодключаемогоМодуля		= "Диск" 
												Или Результат.МестонахождениеПодключаемогоМодуля	= "1С"
												Или Результат.МестонахождениеПодключаемогоМодуля	= "Конфигурация";

		Если Результат.МестонахождениеПодключаемогоМодуля = "1С" Тогда
			ПодключаемыйМодульСсылка = ПодключаемыйМодульСсылка(Результат);
		КонецЕсли;
		
	ИначеЕсли ИмяРегистра = "ДополнительныеСведения" Тогда
		
		Если Результат.ИспользоватьПодключаемыйМодуль = "" Тогда
			Результат.ИспользоватьПодключаемыйМодуль = Ложь;		
		КонецЕсли;

		Если Результат.МестонахождениеПодключаемогоМодуля = "НаДиске" Тогда
			Результат.МестонахождениеПодключаемогоМодуля = "Диск";
		ИначеЕсли Результат.МестонахождениеПодключаемогоМодуля = "ВБазеДанных" Тогда
			Результат.МестонахождениеПодключаемогоМодуля = "1С";
		ИначеЕсли Результат.МестонахождениеПодключаемогоМодуля = "ВСоставеКонфигурации" Тогда
			Результат.МестонахождениеПодключаемогоМодуля = "Конфигурация";
		КонецЕсли;
			
		Если Результат.МестонахождениеПодключаемогоМодуля = "1С"
			И ЗначениеЗаполнено(Результат.ПодключаемыйМодульСсылка) Тогда	  
			
			Результат.ПодключаемыйМодульПуть			= "";
			Результат.ПодключаемыйМодульИмяОбработки	= "";
			
			ЧтениеXML = Новый ЧтениеXML();
		    ЧтениеXML.УстановитьСтроку(Результат.ПодключаемыйМодульСсылка);
			
			ПодключаемыйМодульСсылка = ПрочитатьXML(ЧтениеXML);

		КонецЕсли;
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПодключаемыйМодульСсылка) Тогда
		Результат.ПодключаемыйМодульСсылка = ПодключаемыйМодульСсылка;
	КонецЕсли;	
	
	Результат.ПодключаемыйМодульПредставление = Ядро.ПредставлениеПодключаемогоМодуля(Результат);
	
	Если Результат.РежимСовместимостиФорматов155и820 = "" Тогда
		Результат.РежимСовместимостиФорматов155и820 = Ложь;
	КонецЕсли;
	
КонецПроцедуры	

//}		НАСТРОЙКИ МОДУЛЯ СТАНДАРТ


// {	КОНВЕРТАЦИЯ НАСТРОЕК МОДУЛЯ СТАНДАРТ
Процедура КонвертацияНастроек_Сконвертировать() Экспорт
	
	КонвертацияНастроек_СконвертироватьФорматы();
	КонвертацияНастроек_СконвертироватьПечатныеФормы();
	КонвертацияНастроек_СконвертироватьПодразделения();
	КонвертацияНастроек_СконвертироватьКомментарии();
	КонвертацияНастроек_СконвертироватьАвтообновление();
	
КонецПроцедуры

Процедура КонвертацияНастроек_СконвертироватьФорматы()
	
	ПВХ = ПланыВидовХарактеристик[Свойства_ИмяПланаВидовХарактеристик()];
	
	СвойствоДляОрганизации = ПВХ.НайтиПоНаименованию("ДиадокФормироватьУПД",			Истина);
	СвойствоДляКонтрагента = ПВХ.НайтиПоНаименованию("ДиадокФормироватьУПДКонтрагент",	Истина);
	
	СписокСвойств = Новый Массив;
	СписокСвойств.Добавить(СвойствоДляОрганизации);
	СписокСвойств.Добавить(СвойствоДляКонтрагента);
	
	Выборка = КонвертацияНастроек_ВыборкаСвойств(СписокСвойств);
	
	Пока Выборка.Следующий() Цикл
		КонвертацияНастроек_СконвертироватьНастройкуФорматов(Выборка.Объект, Выборка.Значение);
	КонецЦикла;
	
КонецПроцедуры

Функция КонвертацияНастроек_ВыборкаСвойств(СписокСвойств)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЗначенияСвойствОбъектов.Объект КАК Объект,
	|	ЗначенияСвойствОбъектов.Свойство КАК Свойство,
	|	ЗначенияСвойствОбъектов.Значение КАК Значение
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Свойство В(&СписокСвойств)";
	
	Запрос.УстановитьПараметр("СписокСвойств", СписокСвойств);
	
	ИмяРегистра = Свойства_ИмяРегистраСведений();
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Результат = Запрос.Выполнить().Выбрать();
	
	Возврат Результат;
	
КонецФункции

Процедура КонвертацияНастроек_СконвертироватьНастройкуФорматов(ОбъектНастройки, ЗначениеНастройки)
	
	СоответвиеНастроек = КонвертацияНастроек_СоответвиеНастроекФорматов();
	НастройкаДляЗаписи = СоответвиеНастроек.Получить(ЗначениеНастройки);
	
	Если ЗначениеЗаполнено(НастройкаДляЗаписи) Тогда
		
		ТаблицаВидовПакетов		= СопоставлениеДанныхПМ_ТаблицаВидовСправочника("ВидыПакетов");
		ТаблицаВидовДокументов	= СопоставлениеДанныхПМ_ТаблицаВидовСправочника("ВидыДокументов");
		
		КатегорииПакетоДляНастройки = Новый Массив;
		КатегорииПакетоДляНастройки.Добавить("ОсновнойПакет");
		КатегорииПакетоДляНастройки.Добавить("КорректировочныйПакет");
		
		Для Каждого КатегорияПакета Из КатегорииПакетоДляНастройки Цикл
			
			ВидПакетаДляКатегории = НастройкаДляЗаписи[КатегорияПакета];
			
			ДопРеквизиты_УстановитьЗначение_1С(ОбъектНастройки, КатегорияПакета, ВидПакетаДляКатегории);
			
			ОписаниеПакета = ТаблицаВидовПакетов.Найти(ВидПакетаДляКатегории);
			
			Если ОписаниеПакета <> Неопределено Тогда
				
				СоставПакета = РазделитьСтрокуЛок(ОписаниеПакета.СоставПакета);
				
				Для Каждого ВидДокумента Из СоставПакета Цикл
					
					ОписаниеДокумента = ТаблицаВидовДокументов.Найти(ВидДокумента);
					
					Если ОписаниеДокумента <> Неопределено Тогда
						
						КлючФормата = Лев(ОписаниеДокумента.ТипКонтентаAPI, 3);
						
						ЗначениеФормата = Неопределено;
						
						Если НастройкаДляЗаписи.Свойство(КлючФормата, ЗначениеФормата) Тогда
							ДопРеквизиты_УстановитьЗначение_1С(ОбъектНастройки, "Формат_" + ВидДокумента, ЗначениеФормата);
						КонецЕсли;
						
					КонецЕсли;
					
				КонецЦикла;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Функция КонвертацияНастроек_СоответвиеНастроекФорматов()
	
	Результат = Новый Соответствие;
	
	КлючиНастройки = "ОсновнойПакет, КорректировочныйПакет, utd, ucd";
	utd820 = "utd820";
	ucd736 = "ucd736";
	
	// BSLLS:NumberOfValuesInStructureConstructor-off
	СтруктураУПДУКД = Новый Структура(КлючиНастройки, "ID_УПД", "ID_УКД", utd820, ucd736);
	СтруктураУПДУКДРаздельно = Новый Структура(КлючиНастройки, "ID_УПД_раздельно", "ID_УКД_раздельно", utd820, ucd736);
	СтруктураОсновнойПакетКСФ = Новый Структура(КлючиНастройки, "ID_ОсновнойПакет", "ID_КСФ", utd820, ucd736);
	// BSLLS:NumberOfValuesInStructureConstructor-on
	
	Результат.Вставить("СЧФДОП_820", СтруктураУПДУКД);
	Результат.Вставить("СЧФДОП_820_736", СтруктураУПДУКД);
	Результат.Вставить("СЧФ_ДОП_820", СтруктураУПДУКДРаздельно);
	Результат.Вставить("СЧФ_ДОП_820_736", СтруктураУПДУКДРаздельно);
	Результат.Вставить("СФТОРГ12АКТ_820", СтруктураОсновнойПакетКСФ);
	Результат.Вставить("СФТОРГ12АКТ_820_736", СтруктураОсновнойПакетКСФ);
	
	Возврат Результат;
	
КонецФункции

Процедура КонвертацияНастроек_СконвертироватьПечатныеФормы()
	
	ПВХ = ПланыВидовХарактеристик[Свойства_ИмяПланаВидовХарактеристик()];
	
	СвойствоВПФДляАкта 					= ПВХ.НайтиПоНаименованию("ДиадокВнешняяПечатнаяФормаАкта",			Истина);
	
	СвойствоВПФДляАктаСверки 			= ПВХ.НайтиПоНаименованию("ДиадокВнешняяПечатнаяФормаАктаСверки",	Истина);
	СвойствоВПФДляАктаСверкиОФ 			= ПВХ.НайтиПоНаименованию("ДиадокВнешняяПечатнаяФормаСверки",		Истина);
	
	СвойствоВПФДляСчета					= ПВХ.НайтиПоНаименованию("ДиадокВнешняяПечатнаяФормаСчета",		Истина);
	СвойствоВПФДляСчетаОФ				= ПВХ.НайтиПоНаименованию("ДиадокВнешняяПечатнаяФорма",				Истина);
	СвойствоСтандартнаяФормаДляСчета	= ПВХ.НайтиПоНаименованию("ДиадокСтандартнаяФормаСчетаНаОплату",	Истина);
	
	СписокСвойств = Новый Массив;
	СписокСвойств.Добавить(СвойствоВПФДляАкта);
	
	КонвертацияНастроек_СконвертироватьВПФ(СписокСвойств, "ID_АктФормализованный");
	
	СписокСвойств = Новый Массив;
	СписокСвойств.Добавить(СвойствоВПФДляАктаСверки);
	СписокСвойств.Добавить(СвойствоВПФДляАктаСверкиОФ);
	
	КонвертацияНастроек_СконвертироватьВПФ(СписокСвойств, "ID_АктСверки");
	
	СписокСвойств = Новый Массив;
	СписокСвойств.Добавить(СвойствоВПФДляСчета);
	СписокСвойств.Добавить(СвойствоВПФДляСчетаОФ);
	
	КонвертацияНастроек_СконвертироватьВПФ(СписокСвойств, "ID_Счет");
	
	СписокСвойств = Новый Массив;
	СписокСвойств.Добавить(СвойствоСтандартнаяФормаДляСчета);
	
	КонвертацияНастроек_УстановитьИспользованиеСтандартнойПечатнойФормы(СписокСвойств, "ID_Счет");
	
	КонвертацияНастроек_СконвертироватьДополнительныеВПФ();
	
КонецПроцедуры

Процедура КонвертацияНастроек_СконвертироватьВПФ(СписокСвойств, ВидДокумента)
	
	Выборка = КонвертацияНастроек_ВыборкаСвойств(СписокСвойств);
	
	Пока Выборка.Следующий() Цикл
		
		Если ЗначениеЗаполнено(Выборка.Значение) Тогда
			
			ИмяСвойства = "ПФ_" + ВидДокумента;
			ДопРеквизиты_УстановитьЗначение_1С(Выборка.Объект, ИмяСвойства, Выборка.Значение);
			
			ИмяСвойства = "Формат_" + ВидДокумента;
			ДопРеквизиты_УстановитьЗначение_1С(Выборка.Объект, ИмяСвойства, "epf");
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура КонвертацияНастроек_УстановитьИспользованиеСтандартнойПечатнойФормы(СписокСвойств, ВидДокумента)
	
	Выборка = КонвертацияНастроек_ВыборкаСвойств(СписокСвойств);
	
	Пока Выборка.Следующий() Цикл
		
		Если Выборка.Значение = Истина Тогда
			ИмяСвойства = "Формат_" + ВидДокумента;
			ДопРеквизиты_УстановитьЗначение_1С(Выборка.Объект, ИмяСвойства, "pf_1c");
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура КонвертацияНастроек_СконвертироватьДополнительныеВПФ()
	
	ПВХ = ПланыВидовХарактеристик[Свойства_ИмяПланаВидовХарактеристик()];
	
	СвойствоИспользованияВПФ	= ПВХ.НайтиПоНаименованию("ДиадокСписокДополнительныхВПФ",	Истина);
	СвойствоДополнительнойВПФ	= ПВХ.НайтиПоНаименованию("ДиадокНомерВПФ",					Истина);
	
	СписокСвойств = Новый Массив;
	СписокСвойств.Добавить(СвойствоИспользованияВПФ);
	
	ВыборкаИспользованияВПФ = КонвертацияНастроек_ВыборкаСвойств(СписокСвойств);
	
	СписокСвойств = Новый Массив;
	СписокСвойств.Добавить(СвойствоДополнительнойВПФ);
	
	ВыборкаДополнительныхВПФ = КонвертацияНастроек_ВыборкаСвойств(СписокСвойств);
	
	Пока ВыборкаИспользованияВПФ.Следующий() Цикл
		
		ОбъектНастройки	= ВыборкаИспользованияВПФ.Объект;
		НастройкаВПФ	= Строка(ВыборкаИспользованияВПФ.Значение);
		
		Если ЗначениеЗаполнено(НастройкаВПФ) Тогда
			
			НомераИспользуемыхВПФ = Новый Массив;
			
			Для Позиция = 1 По СтрДлина(НастройкаВПФ) Цикл
				Если Сред(НастройкаВПФ, Позиция, 1) = "1" Тогда
					НомераИспользуемыхВПФ.Добавить(Позиция);
				КонецЕсли;
			КонецЦикла;
			
			ИспользуемыеВПФ = Новый Массив;
			
			ВыборкаДополнительныхВПФ.Сбросить();
			
			Пока ВыборкаДополнительныхВПФ.Следующий() Цикл
				
				НомерВПФ = Число(ВыборкаДополнительныхВПФ.Значение);
				
				Если НомераИспользуемыхВПФ.Найти(НомерВПФ) <> Неопределено Тогда
					ИспользуемыеВПФ.Добавить(ВыборкаДополнительныхВПФ.Объект);
				КонецЕсли;
				
			КонецЦикла;
			
			КонвертацияНастроек_УстановитьДополнительныеВПФ(ОбъектНастройки, ИспользуемыеВПФ);
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура КонвертацияНастроек_УстановитьДополнительныеВПФ(ОбъектНастройки, ИспользуемыеВПФ)
	
	ВидыДокументов = Новый Массив;
	ВидыДокументов.Добавить("ID_УПД");
	ВидыДокументов.Добавить("ID_УПД_ДОП");
	ВидыДокументов.Добавить("ID_ТОРГ12");
	ВидыДокументов.Добавить("ID_АктФормализованный");
	
	Для НомерВПФ = 1 По ИспользуемыеВПФ.Количество() Цикл
		
		ЗначениеСвойства = ИспользуемыеВПФ[НомерВПФ - 1].УникальныйИдентификатор();
		
		Для Каждого ВидДокумента Из ВидыДокументов Цикл
			
			ИмяСвойства = "ПФ_" + НомерВПФ + "_" + ВидДокумента;
			ДопРеквизиты_УстановитьЗначение_1С(ОбъектНастройки, ИмяСвойства, ЗначениеСвойства);
			
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура КонвертацияНастроек_ТочностьЦены() Экспорт 
	
	ТочностьЦены = НастройкаМодуляСтандарт_ТочностьЦены();
	
	ОбщиеНастройки = Настройки_ОбщиеНастройки();
	ОбщиеНастройки.ТочностьЦены = ТочностьЦены;
	
	Настройки_ЗаписатьОбщиеНастройки(ОбщиеНастройки);
	
	КлючНастроек = НастройкаМодуляСтандарт_ИмяНастройкиТочностьЦены();
	ИмяПользователя = ПользователиИнформационнойБазы.ТекущийПользователь().Имя;
	ХранилищеОбщихНастроек.Удалить("НастройкаОпций_Диадок", КлючНастроек, ИмяПользователя);
	
КонецПроцедуры

// Проверяет наличие сохраненных настроек прокси-сервера модуля Стандарт.
// Настройки хранятся в регистре сведений в разрезе пользователей.
//
// Возвращаемое значение:
//	Булево - Истина, если есть настройки хотя бы у одного пользователя, Ложь - нет настроек.
//
Функция КонвертацияНастроек_ЕстьНастройкиПроксиВМодулеСтандарт() Экспорт
	
	Запрос = Новый Запрос;
	
	ТекстЗапроса = 
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Объект
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов
	|ГДЕ
	|	Свойство = &Свойство
	|	И Значение В (&Значения)";
	
	ИмяРегистра 	= Свойства_ИмяРегистраСведений();
	ТекстЗапроса	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Запрос.Текст 	= ТекстЗапроса;
	
	ИмяПланаВидовХарактеристик = Свойства_ИмяПланаВидовХарактеристик();
	ПВХ = ПланыВидовХарактеристик[ИмяПланаВидовХарактеристик];
	
	СвойствоНастройкиПрокси = ПВХ.НайтиПоНаименованию("ДиадокНастройкиПрокси", Истина);
	
	ЗначенияСвойстваНастройкиПрокси = Новый Массив;
	ЗначенияСвойстваНастройкиПрокси.Добавить("ИспользоватьНастройкиIE");
	ЗначенияСвойстваНастройкиПрокси.Добавить("ИспользоватьПроксиСервер");
	
	Запрос.УстановитьПараметр("Свойство", СвойствоНастройкиПрокси);
	Запрос.УстановитьПараметр("Значения", ЗначенияСвойстваНастройкиПрокси);

	РезультатЗапроса = Запрос.Выполнить();
	
	ЕстьНастройкиПрокси = Не РезультатЗапроса.Пустой();
	
	Если Не ЕстьНастройкиПрокси
		И Метаданные.РегистрыСведений.Найти("НастройкиПользователей") <> Неопределено Тогда
		
		Запрос.Текст = 
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	Пользователь КАК Объект
		|ИЗ
		|	РегистрСведений.НастройкиПользователей
		|ГДЕ
		|	Настройка = &Свойство
		|	И Значение В (&Значения)";
		
		ТипИзмерения = Метаданные.РегистрыСведений["НастройкиПользователей"].Измерения.Настройка.Тип.Типы()[0];
		ИмяПланаВидовХарактеристик = Метаданные.НайтиПоТипу(ТипИзмерения).Имя;
		
		ПВХ = ПланыВидовХарактеристик[ИмяПланаВидовХарактеристик];
		СвойствоНастройкиПрокси = ПВХ.НайтиПоНаименованию("ДиадокНастройкиПрокси", Истина);
		
		Запрос.УстановитьПараметр("Свойство", СвойствоНастройкиПрокси);
		
		РезультатЗапроса = Запрос.Выполнить();
		
		ЕстьНастройкиПрокси = Не РезультатЗапроса.Пустой();
		
	КонецЕсли;
	
	Возврат ЕстьНастройкиПрокси;
	
КонецФункции

Процедура КонвертацияНастроек_СконвертироватьПодразделения()
	
	ИмяСвойстваМодуляСтандарт 	= "Подр_DepartmentID";
	ИмяСвойстваМодуляПРО		= "IDПодразделенияОрганизации";
	
	СвойствоМодуляСтандарт = Свойства_СсылкаНаСвойство(ИмяСвойстваМодуляСтандарт, Ложь);
	
	СписокСвойств = Новый Массив;
	СписокСвойств.Добавить(СвойствоМодуляСтандарт);
	
	ВыборкаДанныхМодуляСтандарт = КонвертацияНастроек_ВыборкаСвойств(СписокСвойств);
	Пока ВыборкаДанныхМодуляСтандарт.Следующий() Цикл
		
		Значение = ВыборкаДанныхМодуляСтандарт.Значение;
		
		Если ЗначениеЗаполнено(Значение) Тогда
			
			Объект = ВыборкаДанныхМодуляСтандарт.Объект;
			
			ДопРеквизиты_УстановитьЗначение_1С(Объект, ИмяСвойстваМодуляПРО, Значение);
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура КонвертацияНастроек_СконвертироватьКомментарии()
	
	ИмяСвойстваМодуляСтандарт 	= "ТекстКомментарияДиадок";
	ИмяСвойстваМодуляПРО		= "ЗаписыватьКомментарииВДокументы1С";
	
	СвойствоМодуляСтандарт = Свойства_СсылкаНаСвойство(ИмяСвойстваМодуляСтандарт, Ложь);
	
	СписокСвойств = Новый Массив;
	СписокСвойств.Добавить(СвойствоМодуляСтандарт);
	
	ВыборкаДанныхМодуляСтандарт = КонвертацияНастроек_ВыборкаСвойств(СписокСвойств);
	Пока ВыборкаДанныхМодуляСтандарт.Следующий() Цикл
		
		Значение = ВыборкаДанныхМодуляСтандарт.Значение;
		
		Если ЗначениеЗаполнено(Значение) Тогда
			
			Объект = ВыборкаДанныхМодуляСтандарт.Объект;
			
			ДопРеквизиты_УстановитьЗначение_1С(Объект, ИмяСвойстваМодуляПРО, Истина);
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура КонвертацияНастроек_СконвертироватьАвтообновление()
	
	ИмяСвойстваМодуляСтандарт = "ДиадокОтключитьАвтоматическоеОбновлениеМодуля";
	
	СвойствоМодуляСтандарт = Свойства_НайтиСвойство(ИмяСвойстваМодуляСтандарт, Ложь);
	Если Не ЗначениеЗаполнено(СвойствоМодуляСтандарт) Тогда
		Возврат;
	КонецЕсли;
	
	СписокСвойств = Новый Массив;
	СписокСвойств.Добавить(СвойствоМодуляСтандарт);
	
	ИмяСвойстваМодуляПРО = "АвтоматическиОбновлятьМодуль";
	
	ВыборкаДанныхМодуляСтандарт = КонвертацияНастроек_ВыборкаСвойств(СписокСвойств);
	Пока ВыборкаДанныхМодуляСтандарт.Следующий() Цикл
		
		ЗначениеСтандарт = ВыборкаДанныхМодуляСтандарт.Значение;
		
		Если ТипЗнч(ЗначениеСтандарт) = Тип("Булево") Тогда
			
			Объект = ВыборкаДанныхМодуляСтандарт.Объект;
			ЗначениеПро = Не ЗначениеСтандарт;
			
			ДопРеквизиты_УстановитьЗначение_1С(Объект, ИмяСвойстваМодуляПРО, ЗначениеПро);
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

// }	КОНВЕРТАЦИЯ НАСТРОЕК МОДУЛЯ СТАНДАРТ


///////////////////////////////////////////////////////////////////////////////
//{		ОБРАБОТКА ОБЛАЧНЫХ НАСТРОЕК

// Управляет настройкой использования ПМ модуля Стандарт
//
// Параметры:
//  ЗначениеИспользоватьПМСтандарта - Булево, Строка - Значение настройки ИспользоватьПодключаемыйМодуль
//
Процедура УстановитьНастройкуИспользоватьПМСтандарта(ЗначениеИспользоватьПМСтандарта) Экспорт
	
	ВключитьИспользованиеПМ = (Строка(ЗначениеИспользоватьПМСтандарта) <> "Нет");
	
	Если ВключитьИспользованиеПМ Тогда
		ПодключитьПМСтандарта(ЗначениеИспользоватьПМСтандарта);
	Иначе
		ОтключитьПМСтандарта(ЗначениеИспользоватьПМСтандарта);
	КонецЕсли;
	
КонецПроцедуры

// Отключает использование подключаемого модуда Стандарта
//
// Параметры:
//  ЗначениеНастройки - Булево, Строка - Значение настройки ИспользоватьПодключаемыйМодуль
//
Процедура ОтключитьПМСтандарта(ЗначениеНастройки)
	
	Настройка = НастройкиМодуляСтандарт();
	
	Если Настройка.ИспользоватьПодключаемыйМодуль Тогда
		
		УстановитьНастройкуСтандарта_ИспользоватьПодключаемыйМодуль(ЗначениеНастройки);
		
		Кэш_Поместить("НастройкиМодуляСтандарт", Неопределено);
		
	КонецЕсли;
	
КонецПроцедуры

// Включает использование подкулючаемого модуля Стандарта
//
// Параметры:
//  ЗначениеНастройки - Булево, Строка - Значение настройки ИспользоватьПодключаемыйМодуль
//
Процедура ПодключитьПМСтандарта(ЗначениеНастройки)
	
	Если МожноПодключитьПМСтандарта(ЗначениеНастройки) Тогда
		
		УстановитьНастройкуСтандарта_ИспользоватьПодключаемыйМодуль(ЗначениеНастройки);
		
		Кэш_Поместить("НастройкиМодуляСтандарт", Неопределено);
		
	КонецЕсли;
	
КонецПроцедуры

Функция МожноПодключитьПМСтандарта(ЗначениеНастройки)
	
	Настройка = НастройкиМодуляСтандарт();
	
	ИмяРегистра = Свойства_ИмяРегистраСведений();
	Если ИмяРегистра = "ЗначенияСвойствОбъектов" Тогда
		
		ЕстьНастройкиПМНаДиске = (ЗначениеНастройки = "Диск"
			И ЗначениеЗаполнено(Настройка.ПодключаемыйМодульПуть));
		
		ЕстьНастройкиПМВСправочнике = (ЗначениеНастройки = "1С"
			И ЗначениеЗаполнено(Настройка.ПодключаемыйМодульСсылка));
		
		ЕстьНастройкиПМВКонфигурации = (ЗначениеНастройки = "Конфигурация"
			И ЗначениеЗаполнено(Настройка.ПодключаемыйМодульИмяОбработки));
		
	ИначеЕсли ИмяРегистра = "ДополнительныеСведения" Тогда
		
		ЕстьНастройкиПМНаДиске = (Настройка.МестонахождениеПодключаемогоМодуля = "Диск"
			И ЗначениеЗаполнено(Настройка.ПодключаемыйМодульПуть));
		
		ЕстьНастройкиПМВСправочнике = (Настройка.МестонахождениеПодключаемогоМодуля = "1С"
			И ЗначениеЗаполнено(Настройка.ПодключаемыйМодульСсылка));
		
		ЕстьНастройкиПМВКонфигурации = (Настройка.МестонахождениеПодключаемогоМодуля = "Конфигурация"
			И ЗначениеЗаполнено(Настройка.ПодключаемыйМодульИмяОбработки));
		
	Иначе
		
		ТекстОшибки = НСтр("ru='Не определено имя регистра хранения настроек'");
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	Результат = (НЕ Настройка.ИспользоватьПодключаемыйМодуль
		И (ЕстьНастройкиПМНаДиске
		ИЛИ ЕстьНастройкиПМВСправочнике
		ИЛИ ЕстьНастройкиПМВКонфигурации));
	
	Возврат Результат;
	
КонецФункции

Процедура УстановитьНастройкуСтандарта_ИспользоватьПодключаемыйМодуль(ЗначениеНастройки)
	
	Объект = ОбъектОбщихНастроек_1С();
	ИмяСвойства = НастройкиМодуляСтандарт_СвойствоИспользоватьПодключаемыйМодуль();
	СПрефиксом = Ложь;
	
	ДопРеквизиты_УстановитьЗначение_1С(
		Объект,
		ИмяСвойства,
		ЗначениеНастройки,
		СПрефиксом
	);
	
КонецПроцедуры

Функция НастройкиМодуляСтандарт_СвойствоИспользоватьПодключаемыйМодуль()
	
	ИдентификаторСвойства = "ИспользоватьПодключаемыйМодуль";
	ИмяРегистра = Свойства_ИмяРегистраСведений();
	ИменаСвойств = НастройкиМодуляСтандарт_НаименованияСвойствПВХ(ИмяРегистра);
	
	Результат = ИменаСвойств.Получить(ИдентификаторСвойства);
	
	Возврат Результат;
	
КонецФункции

// Управляет настройкой использования конвертации форматов из 820@ в 970@
//
// Параметры:
//  ВключитьРежимСовместимости - Булево - Истина, если требуется установить настройку конвертации в значение "Авто"
//
Процедура УстановитьНастройкуРежимСовместимости_820_970(ВключитьРежимСовместимости) Экспорт
	
	ОбщиеНастройки = Настройки_ОбщиеНастройки();
	
	ТекущийРежимСовместимости = ОбщиеНастройки.РежимСовместимостиФорматов820и970;
	
	Если ВключитьРежимСовместимости Тогда
		ВключитьРежимСовместимости_820_970(ТекущийРежимСовместимости);
	Иначе
		ОтключитьРежимСовместимости_820_970(ТекущийРежимСовместимости);
	КонецЕсли;
	
КонецПроцедуры

Процедура ВключитьРежимСовместимости_820_970(ТекущееЗначениеНастройки)
	
	РежимыСовместимости = Настройки_Конвертация970_РежимыСовместимостиФорматов820и970();
	
	Если ТекущееЗначениеНастройки <> РежимыСовместимости.Авто Тогда
		
		СтруктураНастроек = Настройки_ОбщиеНастройки();
		СтруктураНастроек.Вставить("РежимСовместимостиФорматов820и970", РежимыСовместимости.Авто);
		
		Настройки_ЗаписатьОбщиеНастройки(СтруктураНастроек);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ОтключитьРежимСовместимости_820_970(ТекущееЗначениеНастройки)
	
	РежимыСовместимости = Настройки_Конвертация970_РежимыСовместимостиФорматов820и970();
	
	Если ТекущееЗначениеНастройки <> РежимыСовместимости.НеИспользуется Тогда
		
		СтруктураНастроек = Настройки_ОбщиеНастройки();
		СтруктураНастроек.Вставить("РежимСовместимостиФорматов820и970", РежимыСовместимости.НеИспользуется);
		
		Настройки_ЗаписатьОбщиеНастройки(СтруктураНастроек);
		
	КонецЕсли;
	
КонецПроцедуры

// Настройки документов на отправку по сопоставленным организациям
//
// Параметры:
//  ВидыДокументов - Массив - Идентификаторы видов документов на отправку
// 
// Возвращаемое значение:
//  ТаблицаЗначений - Настройки по сопоставленным организациям
//   * Организация - Ссылка - Ссылка на организацию-владельца настроек видов документов на отправку
//   * Свойство - Строка, ПланВидовХарактеристикСсылка - Дополнительное свойство формата документа на отправку
//   * Значение - Строка - Значение настройки вида документана отправку
//
Функция НастройкиДокументовНаОтправкуПоСопоставленнымОрганизациям(ВидыДокументов) Экспорт
	
	ДопСвойства_ВидыДокументов = ДопСвойства_ВидыПервичныхДокументов(ВидыДокументов);
	
	ТекстЗапроса = ТекстЗапроса_ОбщиеНастройкиДокументовНаОтправку();
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("ДопСвойства_ВидыДокументов", ДопСвойства_ВидыДокументов);
	
	// BSLLS:SetPrivilegedMode-off
	УстановитьПривилегированныйРежим(Истина);
	// BSLLS:SetPrivilegedMode-on
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Если ПривилегированныйРежим() Тогда
		УстановитьПривилегированныйРежим(Ложь);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Записывает настройку для организации или контрагента
//
// Параметры:
//  КлючНастройки - Строка, ПланВидовХарактеристикСсылка - Дополнительное свойство формата документа на отправку
//  ЗначениеЗастройки - Строка - Значение настройки вида документана отправку
//  ОрганизацияЭДО - Ссылка - Ссылка на организацию-владельца настройки вида документа на отправку
//  КонтрагентЭДО - Ссылка, Неопределено - Ссылка на контрагента-владельца настройки вида документа на отправку
//
Процедура ЗаписатьНастройкуУчастникаДО(
		КлючНастройки,
		ЗначениеЗастройки,
		ОрганизацияЭДО,
		КонтрагентЭДО = Неопределено) Экспорт
	
	// BSLLS:SetPrivilegedMode-off
	УстановитьПривилегированныйРежим(Истина);
	// BSLLS:SetPrivilegedMode-on
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		
		ВладелецНастройкиКонтрагент = Контрагенты_ПустаяСсылка_Диадок();
		Если ЗначениеЗаполнено(КонтрагентЭДО) Тогда
			ВладелецНастройкиКонтрагент = КонтрагентЭДО;
		КонецЕсли;
		
		СтруктураДанных = Новый Структура;
		СтруктураДанных.Вставить("ОрганизацияЭДО", ОрганизацияЭДО);
		СтруктураДанных.Вставить("КонтрагентЭДО", ВладелецНастройкиКонтрагент);
		СтруктураДанных.Вставить("ИмяСвойства", КлючНастройки);
		СтруктураДанных.Вставить("Значение", ЗначениеЗастройки);
		
		Справочники_ЗаписатьЗначениеНастроекКонтрагентов_Диадок(СтруктураДанных);
		
	Иначе
		
		ВладелецНастройки = ОрганизацияЭДО;
		Если ЗначениеЗаполнено(КонтрагентЭДО) Тогда
			ВладелецНастройки = КонтрагентЭДО;
		КонецЕсли;
		
		ДопРеквизиты_УстановитьЗначение(ВладелецНастройки, КлючНастройки, ЗначениеЗастройки);
		
	КонецЕсли;
	
	Если ПривилегированныйРежим() Тогда
		УстановитьПривилегированныйРежим(Ложь);
	КонецЕсли;
	
КонецПроцедуры

Функция ТекстЗапроса_ОбщиеНастройкиДокументовНаОтправку()
	
	ЭтоСХ1_Расширение = (ИспользоватьПодсистемуДиадока()
		И ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц());
	
	ЭтоСХ1_Конфигурация = (ИспользоватьПодсистемуДиадока()
		И НЕ ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц());
	
	ЭтоСХ2 = ИспользоватьТиповуюПодсистему1С();
	
	Если ЭтоСХ1_Расширение Тогда
		Результат = ТекстЗапроса_ОбщиеНастройкиДокументовНаОтправку_Расширение();
	ИначеЕсли ЭтоСХ1_Конфигурация Тогда
		Результат = ТекстЗапроса_ОбщиеНастройкиДокументовНаОтправку_Диадок();
	ИначеЕсли ЭтоСХ2 Тогда
		Результат = ТекстЗапроса_ОбщиеНастройкиДокументовНаОтправку_1С();
	Иначе
		
		ТекстОшибки = НСтр("ru='Не удалось определить систему хранения'");
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ОбщиеНастройкиДокументовНаОтправку_Расширение()
	
	Результат =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Т.Организация КАК Организация,
	|	Т.ИмяСвойства КАК Свойство,
	|	Т.Значение КАК Значение
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Т
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛица_Организация
	|		ПО Т.Организация = Диадок_ЮрФизЛица_Организация.Ссылка
	|			И Диадок_ЮрФизЛица_Организация.ИмяРеквизита = ""СвязанныйСправочник1""
	|ГДЕ
	|	Т.Организация <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И Т.Контрагент = ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И Диадок_ЮрФизЛица_Организация.Значение <> НЕОПРЕДЕЛЕНО
	|	И Т.ИмяСвойства В(&ДопСвойства_ВидыДокументов)";
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ОбщиеНастройкиДокументовНаОтправку_Диадок()
	
	Результат =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Т.Организация КАК Организация,
	|	Т.ИмяСвойства КАК Свойство,
	|	Т.Значение КАК Значение
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Т
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица_Организация
	|		ПО Т.Организация = Диадок_ЮрФизЛица_Организация.Ссылка
	|ГДЕ
	|	Т.Организация <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И Т.Контрагент = ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И Диадок_ЮрФизЛица_Организация.СвязанныйСправочник1 <> НЕОПРЕДЕЛЕНО
	|	И Т.ИмяСвойства В(&ДопСвойства_ВидыДокументов)";
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ОбщиеНастройкиДокументовНаОтправку_1С()
	
	Результат =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВЫРАЗИТЬ(Т.Объект КАК Справочник.Организации) КАК Организация,
	|	Т.Свойство КАК Свойство,
	|	Т.Значение КАК Значение
	|ИЗ
	|	РегистрСведений.ДополнительныеСведения КАК Т
	|ГДЕ
	|	Т.Объект ССЫЛКА Справочник.Организации
	|	И Т.Свойство В(&ДопСвойства_ВидыДокументов)";
	
	ОбъектМд = МетаданныеСправочникаОрганизации();
	Результат = СтрЗаменить(Результат, "Справочник.Организации", ОбъектМд.ПолноеИмя());
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Результат);
	
	Возврат Результат;
	
КонецФункции

Функция ДопСвойства_ВидыПервичныхДокументов(ВидыДокументов)
	
	Результат = Новый Массив;
	
	Для Каждого Элемент Из ВидыДокументов Цикл
		Результат.Добавить(Свойства_ФорматОтправкиПоВидуДокумента(Элемент));
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Настройки документов на отправку по сопоставленным контрагентам
//
// Параметры:
//  ВидыДокументов - Массив - Идентификаторы видов документов на отправку
// 
// Возвращаемое значение:
//  ТаблицаЗначений - Настройки по сопоставленным контрагентам
//   * Организация - Ссылка - Ссылка на организацию-владельца настроек видов документов на отправку
//   * Контрагент - Ссылка - Ссылка на контрагента-владельца настроек видов документов на отправку
//   * Свойство - Строка, ПланВидовХарактеристикСсылка - Дополнительное свойство формата документа на отправку
//   * Значение - Строка - Значение настройки вида документана отправку
//
Функция НастройкиДокументовНаОтправкуПоСопоставленнымКонтрагентам(ВидыДокументов) Экспорт
	
	ДопСвойства_ВидыДокументов = ДопСвойства_ВидыПервичныхДокументов(ВидыДокументов);
	
	ТекстЗапроса = ТекстЗапроса_ИндивидуальныеНастройкиДокументовНаОтправку();
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("ДопСвойства_ВидыДокументов", ДопСвойства_ВидыДокументов);
	
	// BSLLS:SetPrivilegedMode-off
	УстановитьПривилегированныйРежим(Истина);
	// BSLLS:SetPrivilegedMode-on
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Если ПривилегированныйРежим() Тогда
		УстановитьПривилегированныйРежим(Ложь);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ИндивидуальныеНастройкиДокументовНаОтправку()
	
	ЭтоСХ1_Расширение = (ИспользоватьПодсистемуДиадока()
		И ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц());
	
	ЭтоСХ1_Конфигурация = (ИспользоватьПодсистемуДиадока()
		И НЕ ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц());
	
	ЭтоСХ2 = ИспользоватьТиповуюПодсистему1С();
	
	Если ЭтоСХ1_Расширение Тогда
		Результат = ТекстЗапроса_ИндивидуальныеНастройкиДокументовНаОтправку_Расширение();
	ИначеЕсли ЭтоСХ1_Конфигурация Тогда
		Результат = ТекстЗапроса_ИндивидуальныеНастройкиДокументовНаОтправку_Диадок();
	ИначеЕсли ЭтоСХ2 Тогда
		Результат = ТекстЗапроса_ИндивидуальныеНастройкиДокументовНаОтправку_1С();
	Иначе
		
		ТекстОшибки = НСтр("ru='Не удалось определить систему хранения'");
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ИндивидуальныеНастройкиДокументовНаОтправку_Расширение()
	
	Результат =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Т.Организация КАК Организация,
	|	Т.Контрагент КАК Контрагент,
	|	Т.ИмяСвойства КАК Свойство,
	|	Т.Значение КАК Значение
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Т
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛица_Организация
	|		ПО Т.Организация = Диадок_ЮрФизЛица_Организация.Ссылка
	|			И Диадок_ЮрФизЛица_Организация.ИмяРеквизита = ""СвязанныйСправочник1""
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛица_Контрагент
	|		ПО Т.Контрагент = Диадок_ЮрФизЛица_Контрагент.Ссылка
	|			И Диадок_ЮрФизЛица_Контрагент.ИмяРеквизита = ""СвязанныйСправочник1""
	|ГДЕ
	|	Т.Организация <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И Т.Контрагент <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И Диадок_ЮрФизЛица_Организация.Значение <> НЕОПРЕДЕЛЕНО
	|	И Диадок_ЮрФизЛица_Контрагент.Значение <> НЕОПРЕДЕЛЕНО
	|	И Т.ИмяСвойства В(&ДопСвойства_ВидыДокументов)";
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ИндивидуальныеНастройкиДокументовНаОтправку_Диадок()
	
	Результат =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Т.Организация КАК Организация,
	|	Т.Контрагент КАК Контрагент,
	|	Т.ИмяСвойства КАК Свойство,
	|	Т.Значение КАК Значение
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Т
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица_Организация
	|		ПО Т.Организация = Диадок_ЮрФизЛица_Организация.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица_Контрагент
	|		ПО Т.Контрагент = Диадок_ЮрФизЛица_Контрагент.Ссылка
	|ГДЕ
	|	Т.Организация <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И Т.Контрагент <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И Диадок_ЮрФизЛица_Организация.СвязанныйСправочник1 <> НЕОПРЕДЕЛЕНО
	|	И Диадок_ЮрФизЛица_Контрагент.СвязанныйСправочник1 <> НЕОПРЕДЕЛЕНО
	|	И Т.ИмяСвойства В(&ДопСвойства_ВидыДокументов)";
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ИндивидуальныеНастройкиДокументовНаОтправку_1С()
	
	Результат =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВЫРАЗИТЬ(Т.Объект КАК Справочник.Организации) КАК Организация,
	|	ВЫРАЗИТЬ(Т.Объект КАК Справочник.Контрагенты) КАК Контрагент,
	|	Т.Свойство КАК Свойство,
	|	Т.Значение КАК Значение
	|ИЗ
	|	РегистрСведений.ДополнительныеСведения КАК Т
	|ГДЕ
	|	Т.Объект ССЫЛКА Справочник.Контрагенты
	|	И Т.Свойство В(&ДопСвойства_ВидыДокументов)";
	
	ОбъектМд = МетаданныеСправочникаКонтрагенты();
	Результат = СтрЗаменить(Результат, "Справочник.Контрагенты", ОбъектМд.ПолноеИмя());
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Результат);
	
	Возврат Результат;
	
КонецФункции

// }	ОБРАБОТКА ОБЛАЧНЫХ НАСТРОЕК


///////////////////////////////////////////////////////////////////////////////
//{		ПАКЕТЫ

Функция Пакеты_ДанныеПакета(Идентификаторы) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Пакеты_ДанныеПакета_Диадок(Идентификаторы);
	Иначе
		Результат = Пакет_ДанныеПакета_1С(Идентификаторы);
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция Пакеты_МассивКонтрактовДокументовПакета(Идентификаторы) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Пакеты_МассивКонтрактовДокументовПакета_Диадок(Идентификаторы);
	Иначе
		Результат = Пакеты_МассивКонтрактовДокументовПакета_1С(Идентификаторы);
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция Пакеты_Записать(Пакет) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Пакеты_Записать_Диадок(Пакет);
	Иначе
		Результат = Пакеты_Записать_1С(Пакет);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_ЗаписатьПоДокументу(Документ)
	
	Результат = Неопределено;
	
	Пакет = Контракт_Пакет();
	
	ЗаполнитьЗначенияСвойств(Пакет,					Документ, "Направление, ДокументУчета, ДатаЭДО, Состояние, Заблокирован, ИдентификаторВида");
	ЗаполнитьЗначенияСвойств(Пакет.Реквизиты,		Документ.Реквизиты);
	ЗаполнитьЗначенияСвойств(Пакет.Идентификаторы,	Документ.Идентификаторы);
	ЗаполнитьЗначенияСвойств(Пакет.Отправитель,		Документ.Отправитель);
	ЗаполнитьЗначенияСвойств(Пакет.Получатель,		Документ.Получатель);
	
	ЗашифрованныйПакет = СвойствоСтруктуры(Документ.СлужебнаяИнформация, "ЗашифрованныйПакет", Ложь);
	Пакет.СлужебнаяИнформация.Вставить("ЗашифрованныйПакет", ЗашифрованныйПакет);
	
	Результат = Пакеты_Записать(Пакет);

	Возврат Результат;
	
КонецФункции

Функция Пакеты_ДокументУчета(Идентификаторы) Экспорт 
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Пакеты_ДокументУчета_Диадок(Идентификаторы);
	Иначе
		Результат = Пакеты_ДокументУчета_1С(Идентификаторы);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_ПереотправляемыйПакет(ДокументУчета) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Неопределено;
	Иначе
		Результат = Пакеты_ПереотправляемыйПакет_1С(ДокументУчета);
	КонецЕсли;
	
	Возврат Результат;

КонецФункции
 
//Очищает подготовленные пакеты которые уже были отправлены
//
Процедура Пакеты_ВыполнитьОчисткуУстаревшихПодготовленных() Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Пакеты_ВыполнитьОчисткуУстаревшихПодготовленных_Диадок();
	КонецЕсли;
	
КонецПроцедуры

Функция Пакеты_СвязанныеПакеты(ИдентификаторыПакета) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Пакеты_СвязанныеПакеты_Диадок(ИдентификаторыПакета);
	Иначе
		Результат = Пакеты_СвязанныеПакеты_1С(ИдентификаторыПакета);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_СвязанныеПакеты_Диадок(ИдентификаторыПакета)

	Результат = Новый Массив;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	РеквизитыСвязанногоПакета.Ссылка КАК Ссылка,
	|	РеквизитыСвязанногоПакета.Документ КАК ДокументУчета,
	|	ДопРеквизитыВидыПакетов.Значение КАК ИдентификаторВидаПакета
	|ИЗ
	|	Справочник.Диадок_ПакетыДокументов КАК РеквизитыСвязанногоПакета
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ПакетыДокументов.ДополнительныеРеквизиты КАК ДопРеквизитыСвязанногоПакета
	|		ПО (ДопРеквизитыСвязанногоПакета.Ссылка = РеквизитыСвязанногоПакета.Ссылка)
	|			И (ДопРеквизитыСвязанногоПакета.ИмяРеквизита = ""ОсновнойСвязанныйПакет"")
	|			И (ДопРеквизитыСвязанногоПакета.Значение = &ОсновнойСвязанныйПакет)
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ДополнительныеСправочники.ДополнительныеРеквизиты КАК ДопРеквизитыВидыПакетов
	|		ПО РеквизитыСвязанногоПакета.ВидПакета = ДопРеквизитыВидыПакетов.Ссылка
	|			И (ДопРеквизитыВидыПакетов.ИмяРеквизита = ""ID"")
	|ГДЕ
	|	НЕ ДопРеквизитыСвязанногоПакета.Ссылка ЕСТЬ NULL
	|	И НЕ РеквизитыСвязанногоПакета.ПометкаУдаления"); 
	
	Запрос.УстановитьПараметр("ОсновнойСвязанныйПакет", ИдентификаторыПакета.ПакетЭДО);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		ОписаниеСвязанногоПакета = Ядро.Контракт_ОписаниеСвязанногоПакета();
		
		ОписаниеСвязанногоПакета.ИдентификаторВида 	= Выборка.ИдентификаторВидаПакета;
		ОписаниеСвязанногоПакета.ДокументУчета 		= Выборка.ДокументУчета;
		
		Результат.Добавить(ОписаниеСвязанногоПакета);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_СвязанныеПакеты_1С(ИдентификаторыПакета)
	
	Результат = Новый Массив;
	
	Перечисления_ИменаСвойств = Перечисление_ИменаСвойствНастроек_1С();
	
	Свойство_ВидПакета = Свойства_СсылкаНаСвойство(Перечисления_ИменаСвойств.ВидПакета, Истина);
	Свойство_ВидПакетаСчет = Свойства_СсылкаНаСвойство(Перечисления_ИменаСвойств.ВидПакетаСчет, Истина);
	Свойство_ОсновнойСвязанныйПакет = Свойства_СсылкаНаСвойство(Перечисления_ИменаСвойств.ОсновнойСвязанныйПакет, Истина);
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	СвязанныеДокументыУчета.Объект КАК ДокументУчета,
	|	ИдентификаторВидаПакета.Значение КАК ИдентификаторВидаПакета
	|ИЗ
	|	РегистрСведений.ДополнительныеСведения КАК СвязанныеДокументыУчета
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторВидаПакета
	|		ПО СвязанныеДокументыУчета.Объект = ИдентификаторВидаПакета.Объект
	|ГДЕ
	|	СвязанныеДокументыУчета.Свойство = &Свойство_ОсновнойСвязанныйПакет
	|	И СвязанныеДокументыУчета.Значение = &LetterId
	|	И ИдентификаторВидаПакета.Свойство = &Свойство_ВидПакета
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	СвязанныеДокументыУчета.Объект КАК ДокументУчета,
	|	ИдентификаторВидаПакета.Значение КАК ИдентификаторВидаПакета
	|ИЗ
	|	РегистрСведений.ДополнительныеСведения КАК СвязанныеДокументыУчета
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторВидаПакета
	|		ПО СвязанныеДокументыУчета.Объект = ИдентификаторВидаПакета.Объект
	|ГДЕ
	|	СвязанныеДокументыУчета.Свойство = &Свойство_ОсновнойСвязанныйПакет
	|	И СвязанныеДокументыУчета.Значение = &LetterId
	|	И ИдентификаторВидаПакета.Свойство = &Свойство_ВидПакетаСчет");
	
	Запрос.УстановитьПараметр("Свойство_ВидПакета", Свойство_ВидПакета);
	Запрос.УстановитьПараметр("Свойство_ВидПакетаСчет", Свойство_ВидПакетаСчет);
	Запрос.УстановитьПараметр("Свойство_ОсновнойСвязанныйПакет", Свойство_ОсновнойСвязанныйПакет);
	Запрос.УстановитьПараметр("LetterId", ИдентификаторыПакета.LetterId);
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Запрос.Текст);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		ОписаниеСвязанногоПакета = Ядро.Контракт_ОписаниеСвязанногоПакета();
		
		ОписаниеСвязанногоПакета.ИдентификаторВида 	= Выборка.ИдентификаторВидаПакета;
		ОписаниеСвязанногоПакета.ДокументУчета 		= Выборка.ДокументУчета;
		
		Результат.Добавить(ОписаниеСвязанногоПакета);
		
	КонецЦикла;
		
	Возврат Результат;
	
КонецФункции


Функция Пакеты_ОсновнойСвязанныйПакет(ИдентификаторыПакета) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Неопределено;
	Иначе
		Результат = Пакеты_ОсновнойСвязанныйПакет_1С(ИдентификаторыПакета);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_ОсновнойСвязанныйПакет_1С(ИдентификаторыПакета)
	
	Результат = Неопределено;
	
	Перечисления_ИменаСвойств = Перечисление_ИменаСвойствНастроек_1С();
	
	Свойство_ВидПакета				= Свойства_СсылкаНаСвойство(Перечисления_ИменаСвойств.ВидПакета, Истина);
	Свойство_ДокументУчетаПакета	= Свойства_СсылкаНаСвойство(Перечисления_ИменаСвойств.ДокументУчетаПакета, Истина);
	Свойство_ОсновнойСвязанныйПакет	= Свойства_СсылкаНаСвойство(Перечисления_ИменаСвойств.ОсновнойСвязанныйПакет, Истина);
			
	Запрос = Новый Запрос(
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВидПакета.Значение КАК ИдентификаторВидаПакета,
	|	ДокументУчетаПакета.Объект КАК ДокументУчета
	|ИЗ
	|	РегистрСведений.ДополнительныеСведения КАК ОсновнойСвязанныйПакет
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ДокументУчетаПакета
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ВидПакета
	|			ПО ДокументУчетаПакета.Объект = ВидПакета.Объект
	|				И (ВидПакета.Свойство = &Свойство_ВидПакета)
	|		ПО (ДокументУчетаПакета.Свойство = &Свойство_ДокументУчетаПакета)
	|			И ОсновнойСвязанныйПакет.Значение = ДокументУчетаПакета.Значение
	|ГДЕ
	|	ОсновнойСвязанныйПакет.Свойство = &Свойство_ОсновнойСвязанныйПакет
	|	И ОсновнойСвязанныйПакет.Значение = &LetterId");
	
	Запрос.УстановитьПараметр("Свойство_ВидПакета", Свойство_ВидПакета);
	Запрос.УстановитьПараметр("Свойство_ДокументУчетаПакета", Свойство_ДокументУчетаПакета);
	Запрос.УстановитьПараметр("Свойство_ОсновнойСвязанныйПакет", Свойство_ОсновнойСвязанныйПакет);
	Запрос.УстановитьПараметр("LetterId", ИдентификаторыПакета.LetterId);
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Запрос.Текст);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		
		Результат = Ядро.Контракт_ОписаниеСвязанногоПакета();
		
		Результат.ИдентификаторВида 	= Выборка.ИдентификаторВидаПакета;
		Результат.ДокументУчета 		= Выборка.ДокументУчета;
		
	КонецЕсли;
		
	Возврат Результат;
	
КонецФункции


//*************************************************
// ПАКЕТЫ: ПОДСИСТЕМА ДИАДОКА

Функция Пакеты_ДанныеПакета_Диадок(Идентификаторы)
	
	Результат = Контракт_Пакет();
	
	ПакетЭДО	= СвойствоСтруктуры(Идентификаторы, "ПакетЭДО",	Неопределено);
	BoxID		= СвойствоСтруктуры(Идентификаторы, "BoxID",	"");
	LetterID	= СвойствоСтруктуры(Идентификаторы, "LetterID",	"");
	
	Если ЗначениеЗаполнено(ПакетЭДО) Тогда
		Отбор = "ПакетыДокументов.Ссылка = &Пакет";
	Иначе
		Отбор = "ПакетыДокументов.BoxId = &BoxId И ПакетыДокументов.LetterId = &LetterId";
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = Пакеты_ТекстЗапросаДляЗаполненияКонтракта_Диадок(Отбор);
	
	Запрос.УстановитьПараметр("Пакет",		ПакетЭДО);
	Запрос.УстановитьПараметр("BoxID",		BoxID);
	Запрос.УстановитьПараметр("LetterId",	LetterId);
	
	Выборка = Запрос.Выполнить().Выбрать();
		
	Если Выборка.Следующий() Тогда
		
		Результат				= Пакеты_КонтрактПоВыборкеДанных_Диадок(Выборка);
		Результат.Документы		= Пакеты_МассивКонтрактовДокументовПакета_Диадок(Идентификаторы);
		
	КонецЕсли;
			
	Возврат Результат;
	
КонецФункции

Функция Пакеты_ДокументыПакета_Диадок(Идентификаторы)

	ПакетЭДО	= СвойствоСтруктуры(Идентификаторы, "ПакетЭДО",	Неопределено);
	BoxID		= СвойствоСтруктуры(Идентификаторы, "BoxID",	"");
	LetterID	= СвойствоСтруктуры(Идентификаторы, "LetterID",	"");
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	Диадок_Документы.Ссылка КАК ДокументЭДО,
	|	Диадок_Документы.BoxId КАК BoxId,
	|	Диадок_Документы.LetterId КАК LetterId,
	|	Диадок_Документы.DocumentId КАК DocumentId
	|ИЗ
	|	Справочник.Диадок_Документы КАК Диадок_Документы
	|ГДЕ "
	+ ?(ЗначениеЗаполнено(ПакетЭДО), "
	|	Диадок_Документы.Пакет = &Пакет", "
	|	Диадок_Документы.BoxId = &BoxId
	|	И Диадок_Документы.Пакет.LetterId = &LetterId") + " 
	|	И НЕ Диадок_Документы.ПометкаУдаления");
	
	Запрос.УстановитьПараметр("Пакет", 		ПакетЭДО);
	Запрос.УстановитьПараметр("BoxId", 		BoxId);
	Запрос.УстановитьПараметр("LetterId", 	LetterId);
	
	РезультатЗапроса = Запрос.Выполнить().Выгрузить();
	
	Результат = ТаблицаЗначенийВМассивСтруктур(РезультатЗапроса);
	
	Возврат Результат;

КонецФункции

Функция Пакеты_Записать_Диадок(Пакет)
	
	Результат = Неопределено;
	
	РеквизитыПакета			= Пакет.Реквизиты;
	Идентификаторы			= Пакет.Идентификаторы;
	Отправитель				= Пакет.Отправитель;
	Получатель				= Пакет.Получатель;
	ДополнительныеРеквизиты	= Пакет.ДопРеквизиты;
	ПараметрыШаблона		= Пакет.ПараметрыШаблона;
	ВидПакета				= ДопСправочники_СсылкаПоИдентификатору_Диадок("ВидыПакетов", Пакет.ИдентификаторВида);
	
	НачатьТранзакцию();
	
	Попытка
		
		Если Идентификаторы.ПакетЭДО = Неопределено Тогда
			ОбъектПакета = Справочники.Диадок_ПакетыДокументов.СоздатьЭлемент();
		Иначе	
			ОбъектПакета = Идентификаторы.ПакетЭДО.ПолучитьОбъект();
		КонецЕсли;
		
		Пакеты_ПредОбработкаПодготовленного_Диадок(Пакет);
		
		Направление = Пакет.Направление;
		
		ОбъектПакета.Направление			= Направление;
		ОбъектПакета.Документ				= Пакет.ДокументУчета;
		ОбъектПакета.ДатаЗагрузки			= Пакет.ДатаЭДО;
		ОбъектПакета.Метка					= Пакет.Состояние;
		ОбъектПакета.ЗаблокированныйПакет	= Пакет.Заблокирован;
		ОбъектПакета.ВидПакета				= ВидПакета;
		
		ОбъектПакета.НомерДокумента			= РеквизитыПакета.Номер;
		ОбъектПакета.ДатаДокумента			= РеквизитыПакета.Дата;
		ОбъектПакета.СуммаДокумента			= РеквизитыПакета.Сумма;
		
		ОбъектПакета.BoxId					= АдресЯщикаВИдентификатор(Идентификаторы.BoxId);
		ОбъектПакета.LetterId				= Идентификаторы.LetterId;
		
		ЭтоИсходящийПакет = (Направление = Перечисление_Направления().Исходящее);
		
		ОбъектПакета.ОрганизацияДиадок		= ?(ЭтоИсходящийПакет, Отправитель.ОрганизацияЭДО, 	Получатель.ОрганизацияЭДО);
		ОбъектПакета.Организация			= ?(ЭтоИсходящийПакет, Отправитель.Организация, 	Получатель.Организация);
		ОбъектПакета.КонтрагентДиадок		= ?(ЭтоИсходящийПакет, Получатель.ОрганизацияЭДО, 	Отправитель.ОрганизацияЭДО);
		ОбъектПакета.Контрагент				= ?(ЭтоИсходящийПакет, Получатель.Организация, 		Отправитель.Организация);
		
		ДопРеквизиты_УстановитьЗначение(ОбъектПакета, "FromDepartmentId",	Отправитель.DepartmentId);
		ДопРеквизиты_УстановитьЗначение(ОбъектПакета, "ToDepartmentId",		Получатель.DepartmentId);
		
		Если Направление = Перечисление_Направления().Исходящее Тогда
			ОбъектПакета.ТекстОшибки = СвойствоСтруктуры(Пакет.СлужебнаяИнформация, "ОшибкиОтправки");
		КонецЕсли;
		
		ЗашифрованныйПакет = СвойствоСтруктуры(Пакет.СлужебнаяИнформация, "ЗашифрованныйПакет");
		ДопРеквизиты_УстановитьЗначение(ОбъектПакета, "ЗашифрованныйПакет", ЗашифрованныйПакет);
		
		ПакетОтправлен = ЗначениеЗаполнено(Идентификаторы.LetterId);
		Если НЕ ПакетОтправлен Тогда
			ПромежуточныйПолучатель = СвойствоСтруктуры(Пакет.СлужебнаяИнформация.ПараметрыОтправки, "ПромежуточныйПолучатель");
			ДопРеквизиты_УстановитьЗначение(ОбъектПакета, "ProxyBoxId", 		ПромежуточныйПолучатель.BoxId);
			ДопРеквизиты_УстановитьЗначение(ОбъектПакета, "ProxyDepartmentId", 	ПромежуточныйПолучатель.DepartmentId);
		КонецЕсли;
		
		ПараметрыПереотправки = СвойствоСтруктуры(Пакет.СлужебнаяИнформация, "ПараметрыПереотправки");
		ДопРеквизиты_УстановитьЗначение(ОбъектПакета, "ПараметрыПереотправки", ПараметрыПереотправки);
		
		Для Каждого ТекРеквизит Из ДополнительныеРеквизиты Цикл
			ДопРеквизиты_УстановитьЗначение(ОбъектПакета, ТекРеквизит.Ключ, ТекРеквизит.Значение);
		КонецЦикла;
		
		Если Пакет.Шаблон Тогда
			
			ДопРеквизиты_УстановитьЗначение(ОбъектПакета, "Шаблон", Истина);
			
			Для Каждого Элемент Из ПараметрыШаблона Цикл
				ДопРеквизиты_УстановитьЗначение(ОбъектПакета, Элемент.Ключ, Элемент.Значение);
			КонецЦикла;
			
		КонецЕсли;
		
		ОбъектПакета.Записать();
		
		Для Каждого Документ Из Пакет.Документы Цикл
			
			Документ.Идентификаторы.ПакетЭДО = ОбъектПакета.Ссылка;
			СохраненныйДокумент = Документы_Записать_Диадок(Документ);
			Документ.Идентификаторы.ДокументЭДО	= СохраненныйДокумент;
			
		КонецЦикла;
		
		Пакеты_ЗаписатьСвязанныеПакеты_Диадок(Пакет, ОбъектПакета);
		
		Результат = ОбъектПакета.Ссылка;
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		Если ТранзакцияАктивна() Тогда
			ОтменитьТранзакцию();
		КонецЕсли;
		
		_Ошибка = ОписаниеОшибки();
		
		ИнформацияОбОшибке 	= ИнформацияОбОшибке();
		
		ВидОперации			= "Диадок.Запись пакета";
		ТекстОшибки			= ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
		ДопИнформация		= ДополнительнаяИнформацияОПакете(РеквизитыПакета, Идентификаторы);
		ТекстСообщения 		= "Не удалось записать пакет.";

		Ядро.Ошибка_Обработать(	ВидОперации, 
								ТекстОшибки, 
								ДопИнформация, 
								ТекстСообщения);
		
		ВызватьИсключение;
		
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции 

// Создает новый элемент справочника Диадок_ПакетыДокументов для связанного пакета.
//
// Параметры:
//  Пакет					 - Структура - заполненный связанный пакет
//  ОбъектОсновногоПакета	 - ДиадокОбъект.Диадок_ПакетыДокументов - заполненный объект основного пакета
//
Процедура Пакеты_ЗаписатьСвязанныеПакеты_Диадок(Пакет, ОбъектОсновногоПакета)
	
	Перем ПараметрыОтправки, СвязанныеПакеты;
	
	Если НЕ ЗначениеЗаполнено(Пакет.Идентификаторы.ПакетЭДО) Тогда
		
		Если Пакет.СлужебнаяИнформация.Свойство("ПараметрыОтправки", ПараметрыОтправки)
			И ПараметрыОтправки.Свойство("СвязанныеПакеты", СвязанныеПакеты) Тогда
			
			Для Каждого ОписаниеСвязанногоПакета Из СвязанныеПакеты Цикл
				
				ВидПакета = ДопСправочники_СсылкаПоИдентификатору_Диадок("ВидыПакетов", ОписаниеСвязанногоПакета.ИдентификаторВида);
				
				ОбъектПакета = Справочники.Диадок_ПакетыДокументов.СоздатьЭлемент();
				
				ОбъектПакета.ВидПакета = ВидПакета;
				ОбъектПакета.Документ = ОписаниеСвязанногоПакета.ДокументУчета;
				
				РеквизитыДляЗаполнения = "Направление, Метка, ОрганизацияДиадок, Организация, КонтрагентДиадок, Контрагент";
				ЗаполнитьЗначенияСвойств(ОбъектПакета, ОбъектОсновногоПакета, РеквизитыДляЗаполнения);
				
				ДопРеквизиты_УстановитьЗначение(ОбъектПакета, "ОсновнойСвязанныйПакет", ОбъектОсновногоПакета.Ссылка);
				
				ОбъектПакета.Записать();
				
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ДополнительнаяИнформацияОПакете(РеквизитыПакета, Идентификаторы)
	Результат = Новый Массив;
	
	СтруктураПолей = Новый Структура;
	СтруктураПолей.Вставить("Номер", 	РеквизитыПакета.Номер);
	СтруктураПолей.Вставить("Дата", 	РеквизитыПакета.Дата);
	СтруктураПолей.Вставить("BoxId", 	Идентификаторы.BoxId);
	СтруктураПолей.Вставить("LetterId", Идентификаторы.LetterId);
	
	Для Каждого ПолеИнформации Из СтруктураПолей Цикл
		ЗначениеПоля = ПолеИнформации.Значение;
		Если ЗначениеЗаполнено(ЗначениеПоля) Тогда
			Результат.Добавить(ПолеИнформации.Ключ + ": " + ЗначениеПоля);
		КонецЕсли;
	КонецЦикла; 
	
	Возврат Результат;
КонецФункции


Функция Пакеты_ТекстЗапросаДляЗаполненияКонтракта_Диадок(Отбор = "Истина")
	
	ТекстЗапроса_РазделительЗапросов = ТекстЗапроса_РазделительЗапросов();
	
	ТекстЗапросаВсеПодразделения = Пакеты_ТекстЗапросаВсеПодразделения_Диадок();
	ТекстЗапросаВидыПакетов = Пакеты_ТекстЗапросаВидыПакетов_Диадок();
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		
		ТекстЗапросаОсновной = Пакеты_ТекстЗапросаОсновной_ДиадокРасширение();
		
	Иначе
		
		ТекстЗапросаОсновной = Пакеты_ТекстЗапросаОсновной_ДиадокСХ1();
		
	КонецЕсли;
	
	ЧастиТекстаЗапроса = Новый Массив;
	
	ЧастиТекстаЗапроса.Добавить(ТекстЗапросаВсеПодразделения);
	ЧастиТекстаЗапроса.Добавить(ТекстЗапросаВидыПакетов);
	ЧастиТекстаЗапроса.Добавить(ТекстЗапросаОсновной);
	
	Результат = СоединитьСтроку(ЧастиТекстаЗапроса, ТекстЗапроса_РазделительЗапросов);
	Результат = СтрЗаменить(Результат, "&Отбор", Отбор);
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_ТекстЗапросаВсеПодразделения_Диадок()
	
	Результат = 
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица_Подразделения.Ссылка КАК Ссылка,
	|	Диадок_ЮрФизЛица_Подразделения.ID КАК ID,
	|	Диадок_ЮрФизЛица_Подразделения.ID_ВладелецПодразделения КАК ID_ВладелецПодразделения
	|ПОМЕСТИТЬ ВТ_ВсеПодразделения
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица_Подразделения
	|ГДЕ
	|	Диадок_ЮрФизЛица_Подразделения.ИмяСправочника = ""ПодразделенияОрганизаций""
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ID";
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_ТекстЗапросаВидыПакетов_Диадок()
	
	Результат = 
	"ВЫБРАТЬ
	|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка КАК ВидПакета,
	|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Значение КАК ИдентификаторВида,
	|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка.Наименование КАК НаименованиеВида
	|ПОМЕСТИТЬ ВТ_ВидыПакетов
	|ИЗ
	|	Справочник.Диадок_ДополнительныеСправочники.ДополнительныеРеквизиты КАК Диадок_ДополнительныеСправочникиДополнительныеРеквизиты
	|ГДЕ
	|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка.ИмяСправочника = ""ВидыПакетов""
	|	И Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.ИмяРеквизита = ""ID""";
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_ТекстЗапросаОсновной_ДиадокРасширение()
	
	Результат = 
	"ВЫБРАТЬ
	|	ВТ_ВидыПакетов.ИдентификаторВида КАК ИдентификаторВида,
	|	ВТ_ВидыПакетов.НаименованиеВида КАК ВидПакета,
	|	ВТ_ВсеПодразделения.Ссылка КАК Подразделение,
	|	ПакетыДокументов.Ссылка КАК ПакетЭДО,
	|	ПакетыДокументов.BoxId КАК BoxId,
	|	ПакетыДокументов.LetterId КАК LetterId,
	|	ПакетыДокументов.Направление КАК Направление,
	|	ПакетыДокументов.НомерДокумента КАК Номер,
	|	ПакетыДокументов.ДатаДокумента КАК Дата,
	|	ПакетыДокументов.СуммаДокумента КАК Сумма,
	|	ПакетыДокументов.ДатаЗагрузки КАК ДатаЭДО,
	|	ПакетыДокументов.Метка КАК Состояние,
	|	ПакетыДокументов.ЗаблокированныйПакет КАК Заблокирован,
	|	ПакетыДокументов.АнализПроведен КАК АнализПроведен,
	|	ПакетыДокументов.ТекстОшибки КАК ТекстОшибки,
	|	ПакетыДокументов.ОрганизацияДиадок КАК Организация_ОрганизацияЭДО,
	|	ПакетыДокументов.КонтрагентДиадок КАК Контрагент_ОрганизацияЭДО,
	|	ПакетыДокументов.ОрганизацияДиадок.ID КАК Организация_BoxId,
	|	ПакетыДокументов.КонтрагентДиадок.ID КАК Контрагент_BoxId,
	|	ПакетыДокументов.ОрганизацияДиадок.ИдентификаторУчастникаЭДО КАК Организация_FNSParticipantId,
	|	ПакетыДокументов.КонтрагентДиадок.ИдентификаторУчастникаЭДО КАК Контрагент_FNSParticipantId,
	|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_КонтрагентТип.Значение, """") КАК КонтрагентТипОрганизации,
	|	Диадок_ЮрФизЛица_Организация.Значение КАК Организация_Организация,
	|	Диадок_ЮрФизЛица_Контрагент.Значение КАК Контрагент_Организация,
	|	ПакетыДокументов.Документ КАК ДокументУчета
	|ИЗ
	|	Справочник.Диадок_ПакетыДокументов КАК ПакетыДокументов
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ВидыПакетов КАК ВТ_ВидыПакетов
	|		ПО ПакетыДокументов.ВидПакета = ВТ_ВидыПакетов.ВидПакета
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ПакетыДокументов.ДополнительныеРеквизиты КАК РеквизитFromDepartmentId
	|			ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ВсеПодразделения КАК ВТ_ВсеПодразделения
	|			ПО РеквизитFromDepartmentId.Значение = ВТ_ВсеПодразделения.ID
	|		ПО ПакетыДокументов.Ссылка = РеквизитFromDepartmentId.Ссылка
	|			И (РеквизитFromDepartmentId.ИмяРеквизита = ""FromDepartmentId"")
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛица_Организация
	|		ПО ПакетыДокументов.ОрганизацияДиадок = Диадок_ЮрФизЛица_Организация.Ссылка 
	|			И Диадок_ЮрФизЛица_Организация.ИмяРеквизита = ""СвязанныйСправочник1""
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛица_Контрагент
	|		ПО ПакетыДокументов.КонтрагентДиадок = Диадок_ЮрФизЛица_Контрагент.Ссылка 
	|			И Диадок_ЮрФизЛица_Контрагент.ИмяРеквизита = ""СвязанныйСправочник1""
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_КонтрагентТип
	|			ПО ПакетыДокументов.КонтрагентДиадок = ЮрФизЛица_ДопРеквизиты_КонтрагентТип.Ссылка
	|			И (ЮрФизЛица_ДопРеквизиты_КонтрагентТип.ИмяРеквизита = ""Тип"")
	|ГДЕ
	|	&Отбор
	|	И НЕ ПакетыДокументов.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_ТекстЗапросаОсновной_ДиадокСХ1()
	
	Результат = 
	"ВЫБРАТЬ
	|	ВТ_ВидыПакетов.ИдентификаторВида КАК ИдентификаторВида,
	|	ВТ_ВидыПакетов.НаименованиеВида КАК ВидПакета,
	|	ВТ_ВсеПодразделения.Ссылка КАК Подразделение,
	|	ПакетыДокументов.Ссылка КАК ПакетЭДО,
	|	ПакетыДокументов.BoxId КАК BoxId,
	|	ПакетыДокументов.LetterId КАК LetterId,
	|	ПакетыДокументов.Направление КАК Направление,
	|	ПакетыДокументов.НомерДокумента КАК Номер,
	|	ПакетыДокументов.ДатаДокумента КАК Дата,
	|	ПакетыДокументов.СуммаДокумента КАК Сумма,
	|	ПакетыДокументов.ДатаЗагрузки КАК ДатаЭДО,
	|	ПакетыДокументов.Метка КАК Состояние,
	|	ПакетыДокументов.ЗаблокированныйПакет КАК Заблокирован,
	|	ПакетыДокументов.АнализПроведен КАК АнализПроведен,
	|	ПакетыДокументов.ТекстОшибки КАК ТекстОшибки,
	|	ПакетыДокументов.ОрганизацияДиадок КАК Организация_ОрганизацияЭДО,
	|	ПакетыДокументов.КонтрагентДиадок КАК Контрагент_ОрганизацияЭДО,
	|	ПакетыДокументов.ОрганизацияДиадок.ID КАК Организация_BoxId,
	|	ПакетыДокументов.КонтрагентДиадок.ID КАК Контрагент_BoxId,
	|	ПакетыДокументов.ОрганизацияДиадок.ИдентификаторУчастникаЭДО КАК Организация_FNSParticipantId,
	|	ПакетыДокументов.КонтрагентДиадок.ИдентификаторУчастникаЭДО КАК Контрагент_FNSParticipantId,
	|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_КонтрагентТип.Значение, """") КАК КонтрагентТипОрганизации,
	|	Диадок_ЮрФизЛица_Организация.СвязанныйСправочник1 КАК Организация_Организация,
	|	Диадок_ЮрФизЛица_Контрагент.СвязанныйСправочник1 КАК Контрагент_Организация,
	|	ПакетыДокументов.Документ КАК ДокументУчета
	|ИЗ
	|	Справочник.Диадок_ПакетыДокументов КАК ПакетыДокументов
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ВидыПакетов КАК ВТ_ВидыПакетов
	|		ПО ПакетыДокументов.ВидПакета = ВТ_ВидыПакетов.ВидПакета
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ПакетыДокументов.ДополнительныеРеквизиты КАК РеквизитFromDepartmentId
	|			ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ВсеПодразделения КАК ВТ_ВсеПодразделения
	|			ПО РеквизитFromDepartmentId.Значение = ВТ_ВсеПодразделения.ID
	|		ПО ПакетыДокументов.Ссылка = РеквизитFromDepartmentId.Ссылка
	|			И (РеквизитFromDepartmentId.ИмяРеквизита = ""FromDepartmentId"")
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица_Организация
	|		ПО ПакетыДокументов.ОрганизацияДиадок = Диадок_ЮрФизЛица_Организация.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица_Контрагент
	|		ПО ПакетыДокументов.КонтрагентДиадок = Диадок_ЮрФизЛица_Контрагент.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_КонтрагентТип
	|			ПО ПакетыДокументов.КонтрагентДиадок = ЮрФизЛица_ДопРеквизиты_КонтрагентТип.Ссылка
	|			И (ЮрФизЛица_ДопРеквизиты_КонтрагентТип.ИмяРеквизита = ""Тип"")
	|ГДЕ
	|	&Отбор
	|	И НЕ ПакетыДокументов.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_КонтрактПоВыборкеДанных_Диадок(Выборка)
	
	Результат = Контракт_Пакет();
	
	ЗаполнитьЗначенияСвойств(Результат, 				Выборка, "Направление, ДокументУчета, ДатаЭДО, Состояние, Заблокирован, ИдентификаторВида");
	ЗаполнитьЗначенияСвойств(Результат.Реквизиты, 		Выборка, "Дата, Номер, Сумма");
	ЗаполнитьЗначенияСвойств(Результат.Идентификаторы, 	Выборка, "BoxId, LetterId, ПакетЭДО");
	
	ЗаполнитьУчастниковДокументооборота(Результат.Отправитель, Результат.Получатель, Выборка);
	
	Если ЗначениеЗаполнено(Выборка.ТекстОшибки)
		И Результат.Направление = Перечисление_Направления().Исходящее Тогда
		Результат.СлужебнаяИнформация.Вставить("ОшибкиОтправки", Выборка.ТекстОшибки);
	КонецЕсли;
	
	ПакетОтправлен = ЗначениеЗаполнено(Результат.Идентификаторы.LetterId);
	Если Не ПакетОтправлен Тогда
		ПараметрыОтправки = Контракт_ПараметрыОтправкиПакета();
		Результат.СлужебнаяИнформация.Вставить("ПараметрыОтправки", ПараметрыОтправки);
	КонецЕсли;
	
	СтруктураДопРеквизитов = ДопРеквизиты_СписокРеквизитов(Выборка.ПакетЭДО);
	
	Для Каждого ДопРеквизит Из СтруктураДопРеквизитов Цикл
		
		Если ДопРеквизит.Ключ = "FromDepartmentId" Тогда
			Результат.Отправитель.DepartmentId = ДопРеквизит.Значение;
		ИначеЕсли ДопРеквизит.Ключ = "ToDepartmentId" Тогда
			Результат.Получатель.DepartmentId = ДопРеквизит.Значение;
		ИначеЕсли ДопРеквизит.Ключ = "ЗашифрованныйПакет" Тогда
			Результат.СлужебнаяИнформация.Вставить(ДопРеквизит.Ключ, ДопРеквизит.Значение);
		ИначеЕсли ДопРеквизит.Ключ = "ProxyBoxId" Тогда
			ПромежуточныйПолучатель = Результат.СлужебнаяИнформация.ПараметрыОтправки.ПромежуточныйПолучатель;
			ПромежуточныйПолучатель.BoxId = ДопРеквизит.Значение;
		ИначеЕсли ДопРеквизит.Ключ = "ProxyDepartmentId" Тогда
			ПромежуточныйПолучатель = Результат.СлужебнаяИнформация.ПараметрыОтправки.ПромежуточныйПолучатель;
			ПромежуточныйПолучатель.DepartmentId = ДопРеквизит.Значение;
		ИначеЕсли ДопРеквизит.Ключ = "ПараметрыПереотправки" Тогда
			Результат.СлужебнаяИнформация.Вставить(ДопРеквизит.Ключ, ДопРеквизит.Значение);
		ИначеЕсли ДопРеквизит.Ключ = "Шаблон" Тогда
			Результат.Шаблон = ДопРеквизит.Значение;
		ИначеЕсли ДопРеквизит.Ключ = "MessageFromBoxId" Тогда
			Результат.ПараметрыШаблона.MessageFromBoxId = ДопРеквизит.Значение;
		ИначеЕсли ДопРеквизит.Ключ = "MessageToBoxId" Тогда
			Результат.ПараметрыШаблона.MessageToBoxId = ДопРеквизит.Значение;
		ИначеЕсли ДопРеквизит.Ключ = "MessageToDepartmentId" Тогда
			Результат.ПараметрыШаблона.MessageToDepartmentId = ДопРеквизит.Значение;
		ИначеЕсли ДопРеквизит.Ключ = "LockMode" Тогда
			Результат.ПараметрыШаблона.LockMode = ДопРеквизит.Значение;
		Иначе
			Результат.ДопРеквизиты.Вставить(ДопРеквизит.Ключ, ДопРеквизит.Значение);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_МассивКонтрактовДокументовПакета_Диадок(Идентификаторы)

	Результат = Новый Массив;
	
	МассивДокументовПакета = Пакеты_ДокументыПакета_Диадок(Идентификаторы);
	
	Для Каждого ИдентификаторыДокумента Из МассивДокументовПакета Цикл
		
		Документ = Документы_ДанныеДокумента_Диадок(ИдентификаторыДокумента);
		Результат.Добавить(Документ);
		
	КонецЦикла;	
	
	Возврат Результат;

КонецФункции

Функция Пакеты_СсылкаПоИдентификаторам_Диадок(Идентификаторы)
	
	Результат = Неопределено;
	
	BoxId	 = СвойствоСтруктуры(Идентификаторы, "BoxId",		"");
	LetterId = СвойствоСтруктуры(Идентификаторы, "LetterId",	"");
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	Диадок_ПакетыДокументов.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.Диадок_ПакетыДокументов КАК Диадок_ПакетыДокументов
	|ГДЕ
	|	Диадок_ПакетыДокументов.BoxId = &BoxId
	|	И Диадок_ПакетыДокументов.LetterId = &LetterId");
	
	Запрос.УстановитьПараметр("BoxId",		АдресЯщикаВИдентификатор(BoxId));
	Запрос.УстановитьПараметр("LetterId",	LetterId);
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Если ВыборкаЗапроса.Следующий() Тогда
		Результат = ВыборкаЗапроса.Ссылка;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_ДокументУчета_Диадок(Идентификаторы)
	
	Результат = Неопределено;
	
	ПакетЭДО = СвойствоСтруктуры(Идентификаторы, "ПакетЭДО");

	Если ПакетЭДО = Неопределено Тогда
		ПакетЭДО = Пакеты_СсылкаПоИдентификаторам_Диадок(Идентификаторы);
	КонецЕсли;
	
	Если ПакетЭДО <> Неопределено Тогда
		Результат = ПакетЭДО.Документ;
	КонецЕсли;	
	
	Возврат Результат;
	
КонецФункции

Процедура Пакеты_ВыполнитьОчисткуУстаревшихПодготовленных_Диадок()
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Диадок_ПакетыДокументов.Ссылка КАК Пакет,
	|	Диадок_ПакетыДокументов.Документ КАК Документ1С
	|ПОМЕСТИТЬ ВТ_ПодготовленныеПакеты
	|ИЗ
	|	Справочник.Диадок_ПакетыДокументов КАК Диадок_ПакетыДокументов
	|ГДЕ
	|	Диадок_ПакетыДокументов.Метка = &МеткаПодготовлен
	|	И Диадок_ПакетыДокументов.LetterId = &ПустойLetterId
	|	И НЕ Диадок_ПакетыДокументов.ПометкаУдаления
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Документ1С
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_ПодготовленныеПакеты.Документ1С КАК Документ1С
	|ПОМЕСТИТЬ ВТ_ДокументыСДублямиПакетов
	|ИЗ
	|	Справочник.Диадок_ПакетыДокументов КАК Диадок_ПакетыДокументов
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ПодготовленныеПакеты КАК ВТ_ПодготовленныеПакеты
	|		ПО Диадок_ПакетыДокументов.Документ = ВТ_ПодготовленныеПакеты.Документ1С
	|			И Диадок_ПакетыДокументов.Ссылка <> ВТ_ПодготовленныеПакеты.Пакет
	|ГДЕ
	|	Диадок_ПакетыДокументов.Метка <> &МеткаПодготовлен
	|	И Диадок_ПакетыДокументов.Метка <> """"
	|	И НЕ Диадок_ПакетыДокументов.ПометкаУдаления
	|
	|СГРУППИРОВАТЬ ПО
	|	ВТ_ПодготовленныеПакеты.Документ1С
	|
	|ИМЕЮЩИЕ
	|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ Диадок_ПакетыДокументов.Ссылка) > 0
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ВТ_ПодготовленныеПакеты.Пакет КАК Пакет
	|ИЗ
	|	ВТ_ПодготовленныеПакеты КАК ВТ_ПодготовленныеПакеты
	|ГДЕ
	|	ВТ_ПодготовленныеПакеты.Документ1С В
	|			(ВЫБРАТЬ
	|				ВТ_ДокументыСДублямиПакетов.Документ1С КАК Документ1С
	|			ИЗ
	|				ВТ_ДокументыСДублямиПакетов КАК ВТ_ДокументыСДублямиПакетов)";
	
	Запрос.УстановитьПараметр("ПустойLetterId", "                                    ");
	Запрос.УстановитьПараметр("МеткаПодготовлен", Метка_Подготовлен());
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		ПакетОбъект = ВыборкаДетальныеЗаписи.Пакет.ПолучитьОбъект();
		ПакетОбъект.УстановитьПометкуУдаления(Истина);
		
	КонецЦикла;
	
	КоличествоУдаленных = ВыборкаДетальныеЗаписи.Количество();
	Если КоличествоУдаленных > 0 Тогда
	
		ЗаписьЖурналаРегистрации("Диадок.Автоматическая очистка подготовленных пакетов"
			, УровеньЖурналаРегистрации.Информация
			,
			,
			, "Удалено пакетов: " + КоличествоУдаленных
		);
		
	КонецЕсли;
	
КонецПроцедуры

//**********************************************
// ПАКЕТЫ: 1С

Функция Пакет_ДанныеПакета_1С(Идентификаторы)
	
	Результат = Контракт_Пакет();
	
	ЗаполнитьЗначенияСвойств(Результат.Идентификаторы, Идентификаторы);
	
	Результат.ДокументУчета = Пакеты_ДокументУчета_1С(Результат.Идентификаторы);
	Результат.Документы = Пакеты_МассивКонтрактовДокументовПакета_1С(Результат.Идентификаторы);
	
	Если Результат.Документы.Количество() > 0 Тогда
		
		ДокументПакета = Результат.Документы[0];
		
		Результат.ИдентификаторВида = ДокументПакета.ИдентификаторВидаПакета;
		Результат.Направление = ДокументПакета.Направление;
		ЗаполнитьЗначенияСвойств(Результат.Отправитель, ДокументПакета.Отправитель);
		ЗаполнитьЗначенияСвойств(Результат.Получатель, ДокументПакета.Получатель);
		ЗаполнитьЗначенияСвойств(Результат.Реквизиты, ДокументПакета.Реквизиты);
		
		ЗашифрованныйПакет = СвойствоСтруктуры(ДокументПакета.СлужебнаяИнформация, "ЗашифрованныйПакет", Ложь);
		
		Результат.СлужебнаяИнформация.Вставить("ЗашифрованныйПакет", ЗашифрованныйПакет);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_Записать_1С(Пакет)
	
	Результат = Неопределено;
	
	ДокументУчета = Пакет.ДокументУчета;
	LetterId = Пакет.Идентификаторы.LetterId;
	
	ИменаСвойствНастроек_1С = Перечисление_ИменаСвойствНастроек_1С();
	
	Если ЗначениеЗаполнено(ДокументУчета)
		И ЗначениеЗаполнено(LetterId) Тогда
		
		ПереопределяемыеИменаСвойств1С = Пакеты_ПереопределяемыеИменаСвойств1С(Пакет);
		
		ДопРеквизиты_УстановитьЗначение_1С(ДокументУчета, ПереопределяемыеИменаСвойств1С.ДокументУчетаПакета, LetterId);
		
		Если Пакет.Документы.Количество() > 0 Тогда
			
			МаксИндекс = Пакет.Документы.ВГраница();
			
			Для ОбратныйИндекс = -МаксИндекс По 0 Цикл
				
				Документ = Пакет.Документы[-ОбратныйИндекс];
				Документы_Записать_1С(Документ);
				
			КонецЦикла;
			
		КонецЕсли;
		
		Пакеты_ЗаписатьСвязанныеПакеты_1C(Пакет, ИменаСвойствНастроек_1С);
		
	ИначеЕсли ЗначениеЗаполнено(ДокументУчета)
		И Пакет.СлужебнаяИнформация.Свойство("ПараметрыПереотправки")
		И Пакет.СлужебнаяИнформация.ПараметрыПереотправки.ВыполнитьУдалениеНеактуальныхДокументовПакета Тогда
		
		Идентификаторы = Пакет.СлужебнаяИнформация.ПараметрыПереотправки.ПереотправляемыйПакет;
		Идентификаторы.BoxId = Пакет.Идентификаторы.BoxId;
		
		Пакеты_ОчиститьНастройки_1С(Идентификаторы);
		
		ДопРеквизиты_УстановитьЗначение_1С(
			ДокументУчета,
			ИменаСвойствНастроек_1С.ПереотправляемыйПакет,
			Идентификаторы.LetterId);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Заполняет для документа учета связанного пакета доп.свойство с идентификатором основного пакета (документа)
//
// Параметры:
//  Пакет					 - Структура - заполненный контракт связанного пакета
//  ИменаСвойствНастроек_1С	 - Структура - программное перечисление имен свойств (см. Перечисление_ИменаСвойствНастроек_1С)
//
Процедура Пакеты_ЗаписатьСвязанныеПакеты_1C(Пакет, ИменаСвойствНастроек_1С)
	
	LetterId = Пакет.Идентификаторы.LetterId;
		
	ПараметрыОтправки 	= СвойствоСтруктуры(Пакет.СлужебнаяИнформация, "ПараметрыОтправки", Неопределено);
	СвязанныеПакеты 	= СвойствоСтруктуры(ПараметрыОтправки, "СвязанныеПакеты", Новый Массив);
	
	Для Каждого ОписаниеСвязанногоПакета Из СвязанныеПакеты Цикл
		
		ИмяСвойства_ОсновнойСвязанныйПакет = ИменаСвойствНастроек_1С.ОсновнойСвязанныйПакет;	
		
		ДопРеквизиты_УстановитьЗначение_1С(ОписаниеСвязанногоПакета.ДокументУчета, ИмяСвойства_ОсновнойСвязанныйПакет, LetterId);
		
	КонецЦикла;
	
КонецПроцедуры

Функция Пакеты_МассивКонтрактовДокументовПакета_1С(Идентификаторы)

	Результат = Новый Массив;
	
	МассивИдентификаторовДокументов	= Пакеты_ДокументыПакетаАПИ(Идентификаторы);
	
	Для Каждого ИдентификаторыДокумента Из МассивИдентификаторовДокументов Цикл
		
		Документ = Документы_ДанныеДокумента_1С(ИдентификаторыДокумента, ИдентификаторыДокумента.ДокументАПИ);
		Документы_ДополнитьДаннымиИнформационнойБазы_1С(Документ);
		
		Результат.Добавить(Документ);
		
	КонецЦикла;
	
	Пакеты_ДозаполнитьИдентификаторВидаПакета_1С(Результат);
	
	Возврат Результат;

КонецФункции

Функция Пакеты_ДокументУчета_1С(Идентификаторы)
	
	Результат = Неопределено;
	
	LetterId = СвойствоСтруктуры(Идентификаторы, "LetterId", "");
	ПеречислениеНастроек = Перечисление_ИменаСвойствНастроек_1С();
	
	Если LetterId <> "" Тогда
		
		Результат = Свойства_ЗначениеОбъекта(ПеречислениеНастроек.ДокументУчетаПакета, LetterId);
		Если НЕ ЗначениеЗаполнено(Результат) Тогда
			Результат = Свойства_ЗначениеОбъекта(ПеречислениеНастроек.ДокументУчетаПакетаСчет, LetterId);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура Пакеты_ОчиститьНастройки_1С(Идентификаторы)
	
	ДокументУчета = Пакеты_ДокументУчета_1С(Идентификаторы);
	ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_1С();
	
	ДопРеквизиты_УдалитьЗначение_1С(ДокументУчета, ИменаСвойствНастроек.ДокументУчетаПакета);
	ДопРеквизиты_УдалитьЗначение_1С(ДокументУчета, ИменаСвойствНастроек.ДокументУчетаПакетаСчет);
	
	ДокументыПакета = Пакеты_МассивКонтрактовДокументовПакета_1С(Идентификаторы);
	
	Для Каждого Документ Из ДокументыПакета Цикл
		Документы_ОчиститьНастройки_1С(Документ.Идентификаторы);
	КонецЦикла;
	
КонецПроцедуры

Функция Пакеты_ПереотправляемыйПакет_1С(ДокументУчета)
	
	Результат = Свойства_ЗначениеСвойства(Перечисление_ИменаСвойствНастроек_1С().ПереотправляемыйПакет, ДокументУчета);
	
	Возврат Результат;
	
КонецФункции	

Процедура Пакеты_ДозаполнитьИдентификаторВидаПакета_1С(ДокументыПакета)
	
	ИдентификаторВидаПакета = Неопределено;
	
	Для Каждого Документ Из ДокументыПакета Цикл
		
		Если ЗначениеЗаполнено(Документ.ИдентификаторВидаПакета) Тогда
			ИдентификаторВидаПакета = Документ.ИдентификаторВидаПакета;
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	Если ЗначениеЗаполнено(ИдентификаторВидаПакета) Тогда
		
		Для Каждого Документ Из ДокументыПакета Цикл
			
			Если НЕ ЗначениеЗаполнено(Документ.ИдентификаторВидаПакета) Тогда
				Документ.ИдентификаторВидаПакета = ИдентификаторВидаПакета;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

//}		ПАКЕТЫ


///////////////////////////////////////////////////////////////////////////////
//{		ДОКУМЕНТЫ

Функция Документы_ДанныеДокумента(Идентификаторы) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_ДанныеДокумента_Диадок(Идентификаторы);
	Иначе
		Результат = Документы_ДанныеДокумента_1С(Идентификаторы);
		Документы_ДополнитьДаннымиИнформационнойБазы_1С(Результат);
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция Документы_НаличиеДанных1С(Идентификаторы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_НаличиеДанных1С_Диадок(Идентификаторы);
	Иначе
		Результат = Новый Массив;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ДублиДокумента(Идентификаторы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_ДублиДокумента_Диадок(Идентификаторы);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ИдентификаторыОтправленныхПоДокументам1С(Документ, ДокументыОснования) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_ИдентификаторыОтправленныхПоДокументам1С_Диадок(Документ, ДокументыОснования);
	Иначе
		Результат = Документы_ИдентификаторыОтправленныхПоДокументам1С_1С(ДокументыОснования);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура Документы_ОбновлениеРеквизитов(Идентификаторы, СтруктураРеквизитов) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Документы_ОбновлениеРеквизитов_Диадок(Идентификаторы, СтруктураРеквизитов);
	Иначе
		Документы_ОбновлениеРеквизитов_1С(Идентификаторы, СтруктураРеквизитов);
	КонецЕсли; 
	
КонецПроцедуры

Функция Документы_Записать(Документ) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_Записать_Диадок(Документ);
	Иначе
		Результат = Документы_Записать_1С(Документ);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ДанныеХранилища(Идентификаторы, ИмяХранилища, НомерПодписи = 1) Экспорт 
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_ДанныеХранилища_Диадок(Идентификаторы, ИмяХранилища, НомерПодписи);
	Иначе
		Результат = Документы_ДанныеХранилища_1С(Идентификаторы, ИмяХранилища, НомерПодписи);
	КонецЕсли;
	
	Возврат Результат;
		
КонецФункции

Функция Документы_СсылкаНаДанныеХранилища(Идентификаторы, ИмяХранилища) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_СсылкаНаДанныеХранилища_Диадок(Идентификаторы, ИмяХранилища);
	Иначе
		Результат = Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ДанныеПодписи(Идентификаторы, ИмяХранилища, НомерПодписи = 1) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_ДанныеПодписи_Диадок(Идентификаторы, ИмяХранилища, НомерПодписи);
	Иначе
		Результат = Документы_ДанныеПодписи_1С(Идентификаторы, ИмяХранилища);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ВыборкаДанныхХранилищаИзБазы(Идентификаторы, ИмяХранилища)
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_ВыборкаДанныхХранилищаИзБазы_Диадок(Идентификаторы, ИмяХранилища);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура Документы_ЗаписатьДанныеХранилища(ДанныеХранения, ИмяХранилища, ДокументЭДО) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Документы_ЗаписатьДанныеХранилища_Диадок(ДанныеХранения, ИмяХранилища, ДокументЭДО);
	Иначе	
		// Не сохраняем	
	КонецЕсли;
	
КонецПроцедуры

Функция Документы_ПометитьНедействительныеДанныеХранилища(Идентификаторы, ИмяХранилища) Экспорт 
	
	ДокументЭДО	= СвойствоСтруктуры(Идентификаторы, "ДокументЭДО");
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_ПометитьНедействительныеДанныеХранилища_Диадок(ДокументЭДО, ИмяХранилища);
	Иначе
		Результат = Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции	

Функция Документы_ОписаниеДанныхХранилища(Идентификаторы) Экспорт 
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_ОписаниеДанныхХранилища_Диадок(Идентификаторы);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции	

Функция Документы_СсылкаНаДокументЭДО(Идентификаторы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_СсылкаНаДокументЭДО_Диадок(Идентификаторы);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура Документы_СохранитьРезультатАнализа(Идентификаторы, РезультатАнализа) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Документы_СохранитьРезультатАнализа_Диадок(Идентификаторы, РезультатАнализа);
	КонецЕсли;
	
КонецПроцедуры

Процедура Документы_СброситьРезультатАнализа(Идентификаторы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Документы_СброситьРезультатАнализа_Диадок(Идентификаторы);
	КонецЕсли;
	
КонецПроцедуры

Функция Документы_МассивДокументов(ДокументыЭДО) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_МассивДокументов_Диадок(ДокументыЭДО);
	Иначе
		Результат = Новый Массив;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_СопоставленныйВ1С(Идентификаторы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Неопределено;
	Иначе
		Результат = Документы_СопоставленныйВ1С_1С(Идентификаторы);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура Документы_ОбновитьКомментарийДокумента1С(Документ1С, ДобавляемыйКомментарий) Экспорт
	
	Если Документ1С = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	//проверка на "Общие реквизиты"
	Документ1СКомментарий = Неопределено;
	Попытка 
		Документ1СКомментарий = Документ1С.Комментарий;
	Исключение
	КонецПопытки;
	
	Если Документ1СКомментарий = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ЭтоОбъект	= ЭтоОбъект(Документ1С);
	ДокОбъект	= ?(ЭтоОбъект, Документ1С, Документ1С.ПолучитьОбъект());
	
	ТекущийКомментарий = ДокОбъект.Комментарий;
	
	Если ЗначениеЗаполнено(ТекущийКомментарий) Тогда
		
		Если Найти(ТекущийКомментарий, ДобавляемыйКомментарий) > 0 Тогда
			Возврат;
		КонецЕсли;
	
		НачалоКомментария = ТекущийКомментарий + " ";
	Иначе
		НачалоКомментария = "";
	КонецЕсли;
	
	ДокОбъект.Комментарий = НачалоКомментария + ДобавляемыйКомментарий;
	
	ДокОбъект.ОбменДанными.Загрузка = Истина;

	Попытка
		
		ДокОбъект.Заблокировать();
		ДокОбъект.Записать();
		
	Исключение
		
		ВидОперации		= "Обновление комментария в документе 1С";
		ТекстОшибки		= ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ДопИнформация	= СтрШаблонЛок("Документ: %1", Документ1С);
		
		ТекстСообщения	= "Не удалось обновить комментарий в документе %1";
		ТекстСообщения	= СтрШаблонЛок(ТекстСообщения, Документ1С);
		
		Ошибка_Обработать(ВидОперации, ТекстОшибки, ДопИнформация, ТекстСообщения);
		
	КонецПопытки;
	
КонецПроцедуры

// Проверяет, что учетный документ связан с электронным документом
//
// Параметры:
//  Документ1С - ДокументСсылка - Документ учета информационной базы, являющийся основанием для формирования электронного документа
// 
// Возвращаемое значение:
//  Булево - Истина, если есть связанный электронный документ
//
Функция Документы_ЕстьЭлектронныйДокументПоДокументу1С(Документ1С) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_ЕстьЭлектронныйДокументПоДокументу1С_Диадок(Документ1С);
	Иначе
		Результат = Документы_ЕстьЭлектронныйДокументПоДокументу1С_1С(Документ1С);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ОтправленныеУчетныеДокументы(ИдентификаторВидаПакета, УчетныеДокументы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Документы_ОтправленныеУчетныеДокументы_Диадок(ИдентификаторВидаПакета, УчетныеДокументы);
	Иначе
		Результат = Документы_ОтправленныеУчетныеДокументы_1С(ИдентификаторВидаПакета, УчетныеДокументы);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

//*************************************************
// ДОКУМЕНТЫ: ПОДСИСТЕМА ДИАДОКА

Функция Документы_ДанныеДокумента_Диадок(Идентификаторы)
	
	Результат = Контракт_Документ();
	
	Выборка	= Документы_ВыборкаДанныхДокументаИзБазы_Диадок(Идентификаторы);
		
	Если Выборка.Следующий() Тогда
		
		Результат = Документы_КонтрактДокументаПоВыборке_Диадок(Выборка);
		
		ПакетОтправлен = ЗначениеЗаполнено(Результат.Идентификаторы.LetterId);
		Если Не ПакетОтправлен Тогда
			ПараметрыОтправки = Контракт_ПараметрыОтправкиДокумента();
			Результат.СлужебнаяИнформация.Вставить("ПараметрыОтправки", ПараметрыОтправки);
		КонецЕсли;
		
		СтруктураДопРеквизитов = ДопРеквизиты_СписокРеквизитов(Выборка.ДокументЭДО);
		
		Для Каждого ДопРеквизит Из СтруктураДопРеквизитов Цикл
			
			Если ДопРеквизит.Ключ = "InitialDocumentIds" Тогда
				Результат.Связи.InitialDocumentIDs = ДопРеквизит.Значение;
			ИначеЕсли ДопРеквизит.Ключ = "SubordinateDocumentIds" Тогда
				Результат.Связи.SubordinateDocumentIds = ДопРеквизит.Значение;
			ИначеЕсли ДопРеквизит.Ключ = "TransformedToLetterIds" Тогда
				Результат.Связи.TransformedToLetterIds = ДопРеквизит.Значение;
			ИначеЕсли ДопРеквизит.Ключ = "OriginMessageId" Тогда
				Результат.Связи.OriginMessageId = ДопРеквизит.Значение;
			ИначеЕсли ДопРеквизит.Ключ = "CustomDocumentId" Тогда
				Результат.Связи.CustomDocumentId = ДопРеквизит.Значение;	
			ИначеЕсли ДопРеквизит.Ключ = "Шаблон" Тогда
				Результат.Шаблон = ДопРеквизит.Значение;
			ИначеЕсли ДопРеквизит.Ключ = "MessageFromBoxId" Тогда
				Результат.ПараметрыШаблона.MessageFromBoxId = ДопРеквизит.Значение;
			ИначеЕсли ДопРеквизит.Ключ = "MessageToBoxId" Тогда
				Результат.ПараметрыШаблона.MessageToBoxId = ДопРеквизит.Значение;
			ИначеЕсли ДопРеквизит.Ключ = "MessageToDepartmentId" Тогда
				Результат.ПараметрыШаблона.MessageToDepartmentId = ДопРеквизит.Значение;
			ИначеЕсли ДопРеквизит.Ключ = "EditingSettingId" Тогда
				Результат.ПараметрыШаблона.EditingSettingId = ДопРеквизит.Значение;
			ИначеЕсли ДопРеквизит.Ключ = "LockMode" Тогда
				Результат.ПараметрыШаблона.LockMode = ДопРеквизит.Значение;
			ИначеЕсли ДопРеквизит.Ключ = "ЗапрашиватьИзвещение" И Не ПакетОтправлен Тогда
				Результат.СлужебнаяИнформация.ПараметрыОтправки.ЗапрашиватьИзвещение = ДопРеквизит.Значение;
			ИначеЕсли ДопРеквизит.Ключ = "ЗапрашиватьОтветнуюПодпись" И Не ПакетОтправлен Тогда
				Результат.СлужебнаяИнформация.ПараметрыОтправки.ЗапрашиватьОтветнуюПодпись = ДопРеквизит.Значение;
			ИначеЕсли ДопРеквизит.Ключ = "Комментарий" Тогда
				Результат.Комментарий = ДопРеквизит.Значение;
			ИначеЕсли Найти(ДопРеквизит.Ключ, "ВнешнийСтатус_") > 0 Тогда
				Результат.Статусы.ВнешниеСтатусы.Вставить(ДопРеквизит.Ключ, ДопРеквизит.Значение);
			ИначеЕсли ДопРеквизит.Ключ = "Промежуточный" Тогда
				Результат.СлужебнаяИнформация.Промежуточный = ДопРеквизит.Значение;
			Иначе
				Результат.ДопРеквизиты.Вставить(ДопРеквизит.Ключ, ДопРеквизит.Значение);
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
			
	Возврат Результат;
	
КонецФункции

Функция Документы_ВыборкаДанныхДокументаИзБазы_Диадок(Идентификаторы)
	
	ДокументЭДО	= СвойствоСтруктуры(Идентификаторы, "ДокументЭДО",	Неопределено);
	BoxID		= СвойствоСтруктуры(Идентификаторы, "BoxID",		"");
	LetterID	= СвойствоСтруктуры(Идентификаторы, "LetterID",		"");
	DocumentID	= СвойствоСтруктуры(Идентификаторы, "DocumentID",	"");
	
	Если ЗначениеЗаполнено(ДокументЭДО) Тогда
		Отбор = "Документы.Ссылка = &Документ";
	Иначе
		Отбор = "Документы.BoxId = &BoxId И Документы.LetterId = &LetterId И Документы.DocumentID = &DocumentID";
	КонецЕсли;
	
	Запрос = Новый Запрос;
	
	Запрос.Текст = Документы_ТекстЗапросаЗаполненияКонтракта(Отбор);
	
	Запрос.УстановитьПараметр("Документ",	ДокументЭДО);
	Запрос.УстановитьПараметр("BoxID",		BoxID);
	Запрос.УстановитьПараметр("LetterId",	LetterId);
	Запрос.УстановитьПараметр("DocumentID",	DocumentID);
	
	Результат = Запрос.Выполнить().Выбрать();
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаменитьСвязиДляСправочникаДиадокЮрФизЛица_ДиадокСХ1(ТекстЗапроса)
	
	ПодставляемоеЗначение = "
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица_Организация
	|		ПО Документы.ОрганизацияДиадок = Диадок_ЮрФизЛица_Организация.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица_Контрагент
	|		ПО Документы.КонтрагентДиадок = Диадок_ЮрФизЛица_Контрагент.Ссылка";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&СвязиТаблицСопоставленныхСправочников", ПодставляемоеЗначение);
	
КонецПроцедуры

Процедура ЗаменитьСвязиДляСправочникаДиадокЮрФизЛица_ДиадокРасширение(ТекстЗапроса)
	
	ПодставляемоеЗначение = "
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты_Организация
	|			ПО Документы.ОрганизацияДиадок = Диадок_ЮрФизЛицаДополнительныеРеквизиты_Организация.Ссылка 
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты_Организация.ИмяРеквизита = ""СвязанныйСправочник1"")
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты_Контрагент
	|			ПО Документы.КонтрагентДиадок = Диадок_ЮрФизЛицаДополнительныеРеквизиты_Контрагент.Ссылка
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты_Контрагент.ИмяРеквизита = ""СвязанныйСправочник1"")";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&СвязиТаблицСопоставленныхСправочников", ПодставляемоеЗначение);
	
КонецПроцедуры

// BSLLS:MethodSize-off
//
// Параметры:
//  ОтборНаДокументы - Строка - параметр запроса "Отбор"
//  ОтборНаРезультат - Строка - параметр запроса "ОтборНаРезультат"
// 
// Возвращаемое значение:
// Строка - текст запроса заполнения контракта
//
Функция Документы_ТекстЗапросаЗаполненияКонтракта(
		ОтборНаДокументы = "Истина",
		ОтборНаРезультат = "Истина")
	
	Результат =
		
		"ВЫБРАТЬ
		|	Документы.Ссылка КАК Ссылка,
		|	Документы.DepartmentId КАК DepartmentId,
		|	Документы.ВидДокумента КАК ВидДокумента,
		|	Документы.Пакет КАК Пакет,
		|	Диадок_ПакетыДокументов.ВидПакета КАК ПакетВидПакета
		|ПОМЕСТИТЬ ВТ_Документы
		|ИЗ
		|	Справочник.Диадок_Документы КАК Документы
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ПакетыДокументов КАК Диадок_ПакетыДокументов
		|		ПО Документы.Пакет = Диадок_ПакетыДокументов.Ссылка
		|ГДЕ
		|	&Отбор
		|	И НЕ Документы.ПометкаУдаления
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Ссылка,
		|	DepartmentId,
		|	ВидДокумента,
		|	ПакетВидПакета
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	Диадок_ЮрФизЛица_Подразделения.Ссылка КАК Ссылка,
		|	Диадок_ЮрФизЛица_Подразделения.Наименование КАК Наименование,
		|	Диадок_ЮрФизЛица_Подразделения.ID КАК ID,
		|	Диадок_ЮрФизЛица_Подразделения.ID_ВладелецПодразделения КАК ID_ВладелецПодразделения
		|ПОМЕСТИТЬ ВТ_ВсеПодразделения
		|ИЗ
		|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица_Подразделения
		|ГДЕ
		|	Диадок_ЮрФизЛица_Подразделения.ИмяСправочника = ""ПодразделенияОрганизаций""
		|	И Диадок_ЮрФизЛица_Подразделения.ID В
		|			(ВЫБРАТЬ
		|				ВТ_Документы.DepartmentId КАК DepartmentId
		|			ИЗ
		|				ВТ_Документы КАК ВТ_Документы)
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	ID
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка КАК ВидДокумента,
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Значение КАК ИдентификаторВида,
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка.Наименование КАК НаименованиеВида
		|ПОМЕСТИТЬ ВТ_ВидыДокументов
		|ИЗ
		|	Справочник.Диадок_ДополнительныеСправочники.ДополнительныеРеквизиты КАК Диадок_ДополнительныеСправочникиДополнительныеРеквизиты
		|ГДЕ
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка.ИмяСправочника = ""ВидыДокументов""
		|	И Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.ИмяРеквизита = ""ID""
		|	И Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка В
		|			(ВЫБРАТЬ
		|				ВТ_Документы.ВидДокумента КАК ВидДокумента
		|			ИЗ
		|				ВТ_Документы КАК ВТ_Документы)
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка КАК ВидПакета,
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Значение КАК ИдентификаторВида,
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка.Наименование КАК НаименованиеВида
		|ПОМЕСТИТЬ ВТ_ВидыПакетов
		|ИЗ
		|	Справочник.Диадок_ДополнительныеСправочники.ДополнительныеРеквизиты КАК Диадок_ДополнительныеСправочникиДополнительныеРеквизиты
		|ГДЕ
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка.ИмяСправочника = ""ВидыПакетов""
		|	И Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.ИмяРеквизита = ""ID""
		|	И Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка В
		|			(ВЫБРАТЬ
		|				ВТ_Документы.ПакетВидПакета КАК ПакетВидПакета
		|			ИЗ
		|				ВТ_Документы КАК ВТ_Документы)
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТ_Документы.Ссылка КАК Ссылка,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = ""Шаблон""
		|				ТОГДА Диадок_ДокументыДополнительныеРеквизиты.Значение
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК Шаблон,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = ""TargetResolution""
		|				ТОГДА Диадок_ДокументыДополнительныеРеквизиты.Значение
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК ОбъектМаршрутизации,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = ""TargetResolution""
		|				ТОГДА ВЫРАЗИТЬ(Диадок_ДокументыДополнительныеРеквизиты.ЗначениеСтрока КАК СТРОКА(1024))
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК ОбъектМаршрутизацииСтрока,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = &ИмяРеквизитаВнешнийСтатусГИСМТ
		|				ТОГДА Диадок_ДокументыДополнительныеРеквизиты.Значение
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК ВнешнийСтатусГИСМТ,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = &ИмяРеквизитаВнешнийСтатусКонтурЛогистика
		|				ТОГДА Диадок_ДокументыДополнительныеРеквизиты.Значение
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК ВнешнийСтатусКонтурЛогистика,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = &ИмяРеквизитаСтатусМЧД
		|				ТОГДА Диадок_ДокументыДополнительныеРеквизиты.Значение
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК СтатусМЧД,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = &ИмяРеквизитаСтатусМЧДКритичность
		|				ТОГДА Диадок_ДокументыДополнительныеРеквизиты.Значение
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК СтатусМЧДКритичность,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = &ИмяРеквизитаСтатусПКФНС
		|				ТОГДА Диадок_ДокументыДополнительныеРеквизиты.Значение
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК СтатусПКФНС,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = &ИмяРеквизитаСтатусПКФНСКритичность
		|				ТОГДА Диадок_ДокументыДополнительныеРеквизиты.Значение
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК СтатусПКФНСКритичность,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = &ИмяРеквизитаСтатусДТС
		|				ТОГДА Диадок_ДокументыДополнительныеРеквизиты.Значение
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК СтатусДТС,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = &ИмяРеквизитаFromDepartmentID
		|				ТОГДА Диадок_ДокументыДополнительныеРеквизиты.Значение
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК FromDepartmentID,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = &ИмяРеквизитаToDepartmentID
		|				ТОГДА Диадок_ДокументыДополнительныеРеквизиты.Значение
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК ToDepartmentID,
		|	МАКСИМУМ(
		|		ВЫБОР
		|			КОГДА Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = &ИмяРеквизитаПромежуточный
		|				ТОГДА Диадок_ДокументыДополнительныеРеквизиты.Значение
		|			ИНАЧЕ NULL
		|		КОНЕЦ
		|	) КАК Промежуточный
		|ПОМЕСТИТЬ ВТ_ДопРеквизитыPivot
		|ИЗ
		|	ВТ_Документы КАК ВТ_Документы
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_Документы.ДополнительныеРеквизиты КАК Диадок_ДокументыДополнительныеРеквизиты
		|		ПО ВТ_Документы.Ссылка = Диадок_ДокументыДополнительныеРеквизиты.Ссылка
		|			И Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита В (
		|				""Шаблон"",
		|				""TargetResolution"",
		|				&ИмяРеквизитаВнешнийСтатусГИСМТ,
		|				&ИмяРеквизитаВнешнийСтатусКонтурЛогистика,
		|				&ИмяРеквизитаСтатусМЧД,
		|				&ИмяРеквизитаСтатусМЧДКритичность,
		|				&ИмяРеквизитаСтатусПКФНС,
		|				&ИмяРеквизитаСтатусПКФНСКритичность,
		|				&ИмяРеквизитаСтатусДТС,
		|				&ИмяРеквизитаFromDepartmentID,
		|				&ИмяРеквизитаToDepartmentID,
		|				&ИмяРеквизитаПромежуточный
		|			)
		|СГРУППИРОВАТЬ ПО
		|	ВТ_Документы.Ссылка
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Ссылка
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	Документы.Ссылка КАК Ссылка,
		|	Документы.Пакет КАК Пакет,
		|	Документы.BoxId КАК BoxId,
		|	Документы.LetterId КАК LetterId,
		|	Документы.DocumentId КАК DocumentId,
		|	Документы.Документ КАК Документ,
		|	Документы.Направление КАК Направление,
		|	Документы.ИмяФайла КАК ИмяФайла,
		|	Документы.ВидДокумента КАК ВидДокумента,
		|	Документы.Метка КАК Метка,
		|	Документы.ДатаЗагрузки КАК ДатаЗагрузки,
		|	Документы.ДатаДокумента КАК ДатаДокумента,
		|	Документы.НомерДокумента КАК НомерДокумента,
		|	Документы.СуммаДокумента КАК СуммаДокумента,
		|	Документы.СуммаНДС КАК СуммаНДС,
		|	Документы.Валюта КАК Валюта,
		|	Документы.Статус КАК Статус,
		|	Документы.ДополнительныйСтатус КАК ДополнительныйСтатус,
		|	Документы.КонтрагентДиадок КАК КонтрагентДиадок,
		|	Документы.КонтрагентДиадок.ID КАК Контрагент_BoxId,
		|	Документы.КонтрагентДиадок.Наименование КАК ПредставлениеКонтрагентаЭДО,
		|	Документы.Контрагент КАК Контрагент,
		|	Документы.КонтрагентДиадок.ИдентификаторУчастникаЭДО КАК Контрагент_FNSParticipantId,
		|	Документы.ОрганизацияДиадок КАК ОрганизацияДиадок,
		|	Документы.ОрганизацияДиадок.ID КАК Организация_BoxId,
		|	Документы.Организация КАК Организация,
		|	Документы.ОрганизацияДиадок.ИдентификаторУчастникаЭДО КАК Организация_FNSParticipantId,
		|	Документы.DepartmentId КАК DepartmentId,
		|	Документы.АнализПроведен КАК АнализПроведен,
		|	Документы.ТекстОшибки КАК ТекстОшибки,
		|	Документы.ТребуетсяОбработка КАК ТребуетсяОбработка,
		|	Документы.ТребуемоеДействиеВ1С КАК ТребуемоеДействиеВ1С,
		|	Документы.ТребуемоеДействиеВДД КАК ТребуемоеДействиеВДД,
		|	Документы.ТипДокумента КАК ТипДокумента,
		|	Документы.ТипКонтента КАК ТипКонтента,
		|	Документы.ФункцияДокумента КАК ФункцияДокумента,
		|	Документы.Хранилище КАК Хранилище,
		|	Документы.ХранилищеОтвета КАК ХранилищеОтвета,
		|	Документы.PacketId КАК PacketId,
		|	Документы.WorkflowId КАК WorkflowId,
		|	ПакетДокумента.ЗаблокированныйПакет КАК ЗаблокированныйПакет,
		|	ВТ_ВидыПакетов.ИдентификаторВида КАК ИдентификаторВидаПакета,
		|	ВТ_ВидыДокументов.ИдентификаторВида КАК ИдентификаторВида,
		|	ВЫБОР
		|		КОГДА Документы.Направление = ""Входящее""
			|			ТОГДА """"
		|		ИНАЧЕ ВТ_ВидыДокументов.НаименованиеВида
		|	КОНЕЦ КАК Представление,
		|	ВТ_ВидыПакетов.НаименованиеВида КАК ВидПакета
		|ПОМЕСТИТЬ ВТ_Документы_Общая
		|ИЗ
		|	Справочник.Диадок_Документы КАК Документы
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Документы КАК ВТ_Документы
		|		ПО Документы.Ссылка = ВТ_Документы.Ссылка
		|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ВидыДокументов КАК ВТ_ВидыДокументов
		|		ПО Документы.ВидДокумента = ВТ_ВидыДокументов.ВидДокумента
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ПакетыДокументов КАК ПакетДокумента
		|		ПО Документы.Пакет = ПакетДокумента.Ссылка
		|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ВидыПакетов КАК ВТ_ВидыПакетов
		|		ПО ПакетДокумента.ВидПакета = ВТ_ВидыПакетов.ВидПакета
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Ссылка,
		|	DepartmentId,
		|	ВидДокумента,
		|	Пакет
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	Документы.Ссылка КАК ДокументЭДО,
		|	Документы.Пакет КАК ПакетЭДО,
		|	Документы.BoxId КАК BoxId,
		|	Документы.LetterId КАК LetterId,
		|	Документы.DocumentId КАК DocumentId,
		|	Документы.Документ КАК ДокументУчета,
		|	Документы.Направление КАК Направление,
		|	Документы.ИмяФайла КАК ИмяФайла,
		|	Документы.ВидДокумента КАК ВидДокумента,
		|	Документы.Метка КАК Состояние,
		|	Документы.ДатаЗагрузки КАК ДатаЭДО,
		|	Документы.ДатаДокумента КАК Дата,
		|	Документы.НомерДокумента КАК Номер,
		|	Документы.СуммаДокумента КАК Сумма,
		|	Документы.СуммаНДС КАК СуммаНДС,
		|	Документы.Валюта КАК КодВалюты,
		|	Документы.Статус КАК Статус,
		|	Документы.ДополнительныйСтатус КАК ДополнительныйСтатус,
		|	Документы.КонтрагентДиадок КАК Контрагент_ОрганизацияЭДО,
		|	Документы.Контрагент_BoxId КАК Контрагент_BoxId,
		|	Документы.ПредставлениеКонтрагентаЭДО КАК ПредставлениеКонтрагентаЭДО,
		|	ВЫБОР
		|		КОГДА Документы.Контрагент = Неопределено
		|			ТОГДА &ПолеВыборкиСвязанногоКонтрагента
		|		ИНАЧЕ Документы.Контрагент
		|	КОНЕЦ КАК Контрагент_Организация,
		|	Документы.Контрагент_FNSParticipantId КАК Контрагент_FNSParticipantId,
		|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_КонтрагентТип.Значение, """") КАК КонтрагентТипОрганизации,
		|	Документы.ОрганизацияДиадок КАК Организация_ОрганизацияЭДО,
		|	Документы.ОрганизацияДиадок.ID КАК Организация_BoxId,
		|	Документы.ОрганизацияДиадок.Наименование КАК ПредставлениеОрганизацииЭДО,
		|	ВЫБОР 
		|		КОГДА Документы.Организация = Неопределено 
		|			ТОГДА &ПолеВыборкиСвязаннойОрганизации 
		|		ИНАЧЕ Документы.Организация 
		|	КОНЕЦ КАК Организация_Организация,
		|	Документы.Организация_FNSParticipantId КАК Организация_FNSParticipantId,
		|	Документы.DepartmentId КАК DepartmentId,
		|	Документы.АнализПроведен КАК АнализПроведен,
		|	Документы.ТекстОшибки КАК ТекстОшибки,
		|	Документы.ТребуетсяОбработка КАК ТребуетсяОбработка,
		|	Документы.ТребуемоеДействиеВ1С КАК ТребуемоеДействиеВ1С,
		|	Документы.ТребуемоеДействиеВДД КАК ТребуемоеДействиеВДД,
		|	Документы.ТипДокумента КАК ТипДокумента,
		|	Документы.ТипКонтента КАК ТипКонтента,
		|	Документы.ФункцияДокумента КАК ФункцияДокумента,
		|	Документы.Хранилище КАК Хранилище,
		|	Документы.ХранилищеОтвета КАК ХранилищеОтвета,
		|	Документы.PacketId КАК PacketId,
		|	Документы.WorkflowId КАК WorkflowId,
		|	ВТ_ВсеПодразделения.Ссылка КАК Подразделение,
		|	Документы.ИдентификаторВидаПакета КАК ИдентификаторВидаПакета,
		|	Документы.ИдентификаторВида КАК ИдентификаторВида,
		|	Документы.Представление КАК Представление,
		|	ЕСТЬNULL(Диадок_ХранениеДанных.ТребуетсяРасшифровка, ЛОЖЬ) КАК ТребуетсяРасшифровка,
		|	ЕСТЬNULL(Диадок_ХранениеДанных.ИмяФайла, """") КАК FileName,
		|	Документы.ВидПакета КАК ВидПакета,
		|	Документы.ЗаблокированныйПакет КАК Заблокирован,
		|	ЕСТЬNULL(ВТ_ДопРеквизитыPivot.Шаблон, ЛОЖЬ) КАК Шаблон,
		|	ВЫБОР
		|		КОГДА ВТ_ВсеПодразделения_ОбъектМаршрутизации.Ссылка ЕСТЬ NULL
		|			ТОГДА ЕСТЬNULL(ВТ_ДопРеквизитыPivot.ОбъектМаршрутизацииСтрока, """")
		|		ИНАЧЕ ВТ_ВсеПодразделения_ОбъектМаршрутизации.Наименование
		|	КОНЕЦ КАК ОбъектМаршрутизацииНаименование,
		|	ВЫБОР
		|		КОГДА ВТ_ВсеПодразделения_ОбъектМаршрутизации.ID ЕСТЬ NULL
		|			ТОГДА ЕСТЬNULL(ВТ_ДопРеквизитыPivot.ОбъектМаршрутизации, """")
		|		ИНАЧЕ ВТ_ВсеПодразделения_ОбъектМаршрутизации.ID
		|	КОНЕЦ КАК ОбъектМаршрутизацииИдентификатор,
		|	ЕСТЬNULL(ВТ_ДопРеквизитыPivot.ВнешнийСтатусГИСМТ, """") КАК ВнешнийСтатусГИСМТ,
		|	ЕСТЬNULL(ВТ_ДопРеквизитыPivot.ВнешнийСтатусКонтурЛогистика, """") КАК ВнешнийСтатусКонтурЛогистика,
		|	ЕСТЬNULL(ВТ_ДопРеквизитыPivot.СтатусМЧД, """") КАК СтатусМЧД,
		|	ЕСТЬNULL(ВТ_ДопРеквизитыPivot.СтатусМЧДКритичность, """") КАК СтатусМЧДКритичность,
		|	ЕСТЬNULL(ВТ_ДопРеквизитыPivot.СтатусПКФНС, """") КАК СтатусПКФНС,
		|	ЕСТЬNULL(ВТ_ДопРеквизитыPivot.СтатусПКФНСКритичность, """") КАК СтатусПКФНСКритичность,
		|	ЕСТЬNULL(ВТ_ДопРеквизитыPivot.СтатусДТС, """") КАК СтатусДТС,
		|	ЕСТЬNULL(ВТ_ДопРеквизитыPivot.FromDepartmentID, """") КАК FromDepartmentID,
		|	ЕСТЬNULL(ВТ_ДопРеквизитыPivot.ToDepartmentID, """") КАК ToDepartmentID,
		|	ЕСТЬNULL(ВТ_ДопРеквизитыPivot.Промежуточный, """") КАК Промежуточный
		|ИЗ
		|	ВТ_Документы_Общая КАК Документы
		|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ДопРеквизитыPivot КАК ВТ_ДопРеквизитыPivot
		|		ПО Документы.Ссылка = ВТ_ДопРеквизитыPivot.Ссылка
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ХранениеДанных КАК Диадок_ХранениеДанных
		|		ПО Документы.Ссылка = Диадок_ХранениеДанных.Владелец
		|			И (Диадок_ХранениеДанных.Наименование = ""ХранилищеКонтента"")
		|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ВсеПодразделения КАК ВТ_ВсеПодразделения
		|		ПО Документы.DepartmentId = ВТ_ВсеПодразделения.ID
		|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ВсеПодразделения КАК ВТ_ВсеПодразделения_ОбъектМаршрутизации
		|		ПО ВТ_ДопРеквизитыPivot.ОбъектМаршрутизации = ВТ_ВсеПодразделения_ОбъектМаршрутизации.ID
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_КонтрагентТип
		|			ПО Документы.КонтрагентДиадок = ЮрФизЛица_ДопРеквизиты_КонтрагентТип.Ссылка
		|			И (ЮрФизЛица_ДопРеквизиты_КонтрагентТип.ИмяРеквизита = ""Тип"")
		|		&СвязиТаблицСопоставленныхСправочников
		|ГДЕ
		|	ИСТИНА
		|	И &ОтборНаРезультат
		|
		|УПОРЯДОЧИТЬ ПО
		|	ДатаЭДО УБЫВ,
		|	ПакетЭДО,
		|	LetterId";
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		
		Результат = СтрЗаменить(
			Результат, 
			"&ПолеВыборкиСвязаннойОрганизации", 
			"Диадок_ЮрФизЛицаДополнительныеРеквизиты_Организация.Значение"
		);
		
		Результат = СтрЗаменить(
			Результат, 
			"&ПолеВыборкиСвязанногоКонтрагента", 
			"Диадок_ЮрФизЛицаДополнительныеРеквизиты_Контрагент.Значение"
		);
		
		ЗаменитьСвязиДляСправочникаДиадокЮрФизЛица_ДиадокРасширение(Результат);
		
	Иначе
		
		Результат = СтрЗаменить(
			Результат, 
			"&ПолеВыборкиСвязаннойОрганизации", 
			"Диадок_ЮрФизЛица_Организация.СвязанныйСправочник1"
		);
		
		Результат = СтрЗаменить(
			Результат, 
			"&ПолеВыборкиСвязанногоКонтрагента", 
			"Диадок_ЮрФизЛица_Контрагент.СвязанныйСправочник1"
		);
		
		ЗаменитьСвязиДляСправочникаДиадокЮрФизЛица_ДиадокСХ1(Результат);
		
	КонецЕсли;
	
	Результат = СтрЗаменить(Результат, "&ОтборНаРезультат", ОтборНаРезультат);
	Результат = СтрЗаменить(Результат, "&Отбор", ОтборНаДокументы);
	
	ИмяДопРеквизитаВнешнийСтатусГИСМТ = """" + Документы_ИмяДопРеквизита_ВнешнийСтатусГИСМТ() + """";
	Результат = СтрЗаменить(Результат, "&ИмяРеквизитаВнешнийСтатусГИСМТ", ИмяДопРеквизитаВнешнийСтатусГИСМТ);
	
	ИмяДопРеквизитаВнешнийСтатусКонтурЛогистика = """" + Документы_ИмяДопРеквизита_ВнешнийСтатусКонтурЛогистика() + """";
	Результат = СтрЗаменить(
			Результат,
			"&ИмяРеквизитаВнешнийСтатусКонтурЛогистика",
			ИмяДопРеквизитаВнешнийСтатусКонтурЛогистика);
	
	Перечисление_ИменаСвойств = Перечисление_ИменаСвойствНастроек_Диадок();
	Результат = СтрЗаменить(
			Результат,
			"&ИмяРеквизитаСтатусМЧДКритичность",
			"""" + Перечисление_ИменаСвойств.СтатусМЧДКритичность + """");
	Результат = СтрЗаменить(
			Результат,
			"&ИмяРеквизитаСтатусМЧД",
			"""" + Перечисление_ИменаСвойств.СтатусМЧД + """");
	
	Результат = СтрЗаменить(
			Результат,
			"&ИмяРеквизитаСтатусПКФНСКритичность",
			"""" + Перечисление_ИменаСвойств.СтатусПКФНСКритичность + """");
			
	Результат = СтрЗаменить(
			Результат,
			"&ИмяРеквизитаСтатусПКФНС",
			"""" + Перечисление_ИменаСвойств.СтатусПКФНС + """");
			
	Результат = СтрЗаменить(
			Результат,
			"&ИмяРеквизитаСтатусДТС",
			"""" + Перечисление_ИменаСвойств.СтатусДТС + """");
			
	Результат = СтрЗаменить(
			Результат,
			"&ИмяРеквизитаFromDepartmentID",
			"""" + Перечисление_ИменаСвойств.FromDepartmentID + """");
			
	Результат = СтрЗаменить(
			Результат,
			"&ИмяРеквизитаToDepartmentID",
			"""" + Перечисление_ИменаСвойств.ToDepartmentID + """");
	
	Результат = СтрЗаменить(
			Результат,
			"&ИмяРеквизитаПромежуточный",
			"""" + Перечисление_ИменаСвойств.Промежуточный + """");
	
	Возврат Результат;
	
КонецФункции

// BSLLS:MethodSize-on

Функция Документы_КонтрактДокументаПоВыборке_Диадок(Выборка)
	
	Результат = Контракт_Документ();
	
	// BSLLS:LineLength-off
	ЗаполнитьЗначенияСвойств(Результат, Выборка, "Направление, ДокументУчета, ИмяФайла, ДатаЭДО, Состояние, Заблокирован, ИдентификаторВидаПакета, ИдентификаторВида, Шаблон");
	ЗаполнитьЗначенияСвойств(Результат.Реквизиты, Выборка, "Дата, Номер, Сумма, СуммаНДС, КодВалюты");
	ЗаполнитьЗначенияСвойств(Результат.Идентификаторы, Выборка, "BoxId, LetterId, DocumentID, ДокументЭДО, ПакетЭДО");
	ЗаполнитьЗначенияСвойств(Результат.РезультатАнализа, Выборка, "АнализПроведен, ТребуемоеДействиеВ1С, ТребуемоеДействиеВДД, ТекстОшибки");
	ЗаполнитьЗначенияСвойств(Результат.Тип, Выборка, "ТипДокумента, ТипКонтента, ФункцияДокумента, Представление, WorkflowId");
	ЗаполнитьЗначенияСвойств(Результат.Статусы, Выборка, "Статус, ДополнительныйСтатус, ТребуетсяОбработка, СтатусМЧДКритичность, СтатусМЧД, СтатусПКФНСКритичность, СтатусПКФНС");
	ЗаполнитьЗначенияСвойств(Результат.Связи, Выборка, "PacketID");
	// BSLLS:LineLength-on
	
	Документы_СтатусыОбъектМаршрутизации_Диадок(Выборка, Результат);
	
	Тип = Результат.Тип;
	
	ТипДокумента = Тип.ТипДокумента;
	ТипКонтента = Тип.ТипКонтента;
	ФункцияДокумента = Тип.ФункцияДокумента;
	
	ОписаниеТипаДокумента = Списки_ОписаниеТипаДокументаAPI(ТипДокумента, ТипКонтента, ФункцияДокумента);
	ФорматКонтента = Списки_ОписаниеФорматаКонтента(ТипКонтента);
	
	Тип.ФункцияДокумента = Документы_ФункцияДокументаДляХранения(Тип.ФункцияДокумента);
	Тип.Формализованный = ФорматКонтента.Формализованный;
	Тип.Представление = ОписаниеТипаДокумента.Title;
	
	Если Результат.Направление = "Входящее" Тогда
		Результат.Получатель.DepartmentId = СокрЛП(Выборка.DepartmentId);
	ИначеЕсли Результат.Направление = "Внутреннее" Тогда
		Результат.Отправитель.DepartmentId = СокрЛП(Выборка.FromDepartmentId);
		Результат.Получатель.DepartmentId = СокрЛП(Выборка.ToDepartmentId);
	Иначе
		Результат.Отправитель.DepartmentId = СокрЛП(Выборка.DepartmentId);
	КонецЕсли;
	
	ЗаполнитьУчастниковДокументооборота(Результат.Отправитель, Результат.Получатель, Выборка);
	
	МетаданныеДокумента = Выборка.Хранилище.Получить();
	
	Если МетаданныеДокумента <> Неопределено Тогда
		Результат.Метаданные = МетаданныеДокумента;
	КонецЕсли;
	
	КонтентОтвета = Выборка.ХранилищеОтвета.Получить();
	
	Если КонтентОтвета <> Неопределено Тогда
		Результат.СлужебнаяИнформация.Вставить("КонтентОтвета", КонтентОтвета);
	КонецЕсли;
	
	Результат.СлужебнаяИнформация.Вставить("ТребуетсяРасшифровка", Выборка.ТребуетсяРасшифровка);
	Результат.СлужебнаяИнформация.Вставить("Промежуточный", Выборка.Промежуточный);
	
	Возврат Результат;
	
КонецФункции

Процедура Документы_СтатусыОбъектМаршрутизации_Диадок(Выборка, Результат)
	
	ОбъектМаршрутизаци = Результат.Статусы.ОбъектМаршрутизации;
	
	ОбъектМаршрутизаци.Наименование = Выборка.ОбъектМаршрутизацииНаименование;
	ОбъектМаршрутизаци.Идентификатор = Выборка.ОбъектМаршрутизацииИдентификатор;
	
КонецПроцедуры

Функция Документы_НаличиеДанных1С_Диадок(ИдентификаторыДокументов)
	
	Результат = Новый Структура;
	
	ИменаРеквизитовДокумента =
	"BoxId,
	|LetterId,
	|DocumentId,
	|ДокументЭДО,
	|PacketId,
	|DepartmentId,
	|FromDepartmentId,
	|ToDepartmentId,
	|СтатусМЧД,
	|СтатусМЧДКритичность,
	|СтатусДТС,
	|Статус,
	|ОбъектМаршрутизации,
	|ДополнительныйСтатус,
	|ТребуетсяОбработка,
	|ВнешниеСтатусы";
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса_Документы_НаличиеДанных1С_Диадок();
	
	Перечисление_ИменаСвойств = Перечисление_ИменаСвойствНастроек_Диадок();
	
	Запрос.Параметры.Вставить("ИдентификаторыДокументов", ИдентификаторыДокументов);
	Запрос.Параметры.Вставить("ИмяРеквизита_СтатусМЧД", Перечисление_ИменаСвойств.СтатусМЧД);
	Запрос.Параметры.Вставить("ИмяРеквизита_СтатусМЧДКритичность", Перечисление_ИменаСвойств.СтатусМЧДКритичность);
	Запрос.Параметры.Вставить("ИмяРеквизита_СтатусГИСМТ", Документы_ИмяДопРеквизита_ВнешнийСтатусГИСМТ());
	Запрос.Параметры.Вставить("ИмяРеквизита_СтатусКонтурЛогистика", Документы_ИмяДопРеквизита_ВнешнийСтатусКонтурЛогистика());
	Запрос.Параметры.Вставить("ИмяРеквизита_СтатусДТС", Перечисление_ИменаСвойств.СтатусДТС);
	Запрос.Параметры.Вставить("ИмяРеквизита_FromDepartmentID", Перечисление_ИменаСвойств.FromDepartmentID);
	Запрос.Параметры.Вставить("ИмяРеквизита_ToDepartmentID", Перечисление_ИменаСвойств.ToDepartmentID);
	Запрос.Параметры.Вставить("ИмяРеквизита_ОбъектМаршрутизации", Перечисление_ИменаСвойств.ОбъектМаршрутизации);
	Запрос.Параметры.Вставить("ИмяРеквизита_Промежуточный", Перечисление_ИменаСвойств.Промежуточный);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		ТекущиеДанные = Новый Структура(ИменаРеквизитовДокумента);
		Ключ = Новый_КлючИдентификатораДокумента(Выборка.LetterId, Выборка.DocumentId);
		ЗаполнитьЗначенияСвойств(ТекущиеДанные, Выборка);
		ТекущиеДанные.PacketId = СокрЛП(Выборка.PacketId);
		
		ВнешниеСтатусы = Новый Структура;
		Если ЗначениеЗаполнено(Выборка.ВнешнийСтатусГИСМТ) Тогда
			ВнешниеСтатусы.Вставить(Ядро_ИдентификаторВнешнегоДО_ГИСМТ(), Выборка.ВнешнийСтатусГИСМТ);
		КонецЕсли;
		Если ЗначениеЗаполнено(Выборка.ВнешнийСтатусКонтурЛогистика) Тогда
			ВнешниеСтатусы.Вставить(Ядро_ИдентификаторВнешнегоДО_КонтурЛогистика(), Выборка.ВнешнийСтатусКонтурЛогистика);
		КонецЕсли;
		
		ТекущиеДанные.ВнешниеСтатусы = ВнешниеСтатусы;
		
		ОбъектМаршрутизации = Контракт_ОбъектМаршрутизации();
		ОбъектМаршрутизации.Идентификатор = Выборка.ОбъектМаршрутизацииИдентификатор;
		ОбъектМаршрутизации.Наименование = Выборка.ОбъектМаршрутизацииНаименование;
		
		ТекущиеДанные.ОбъектМаршрутизации = ОбъектМаршрутизации;
		
		Результат.Вставить(Ключ, ТекущиеДанные);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_Документы_НаличиеДанных1С_Диадок()
	
	Результат =
	"ВЫБРАТЬ
	|	ИдентификаторыДокументов.BoxId КАК BoxId,
	|	ИдентификаторыДокументов.LetterId КАК LetterId,
	|	ИдентификаторыДокументов.DocumentId КАК DocumentId
	|ПОМЕСТИТЬ ИдентификаторыДокументов
	|ИЗ
	|	&ИдентификаторыДокументов КАК ИдентификаторыДокументов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ИдентификаторыДокументов.BoxId КАК BoxId,
	|	ИдентификаторыДокументов.LetterId КАК LetterId,
	|	ИдентификаторыДокументов.DocumentId КАК DocumentId,
	|	Диадок_Документы.Ссылка КАК ДокументЭДО,
	|	Диадок_Документы.PacketId КАК PacketId,
	|	Диадок_Документы.DepartmentId КАК DepartmentId,
	|	Диадок_Документы.Статус КАК Статус,
	|	Диадок_Документы.ДополнительныйСтатус КАК ДополнительныйСтатус,
	|	Диадок_Документы.ТребуетсяОбработка КАК ТребуетсяОбработка
	|ПОМЕСТИТЬ ПредварительнаяВыборка
	|ИЗ
	|	ИдентификаторыДокументов КАК ИдентификаторыДокументов
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_Документы КАК Диадок_Документы
	|		ПО (Диадок_Документы.BoxId = ИдентификаторыДокументов.BoxId)
	|			И (Диадок_Документы.DocumentId = ИдентификаторыДокументов.DocumentId)
	|			И (Диадок_Документы.LetterId = ИдентификаторыДокументов.LetterId)
	|			И (НЕ Диадок_Документы.ПометкаУдаления)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ПредварительнаяВыборка.BoxId КАК BoxId,
	|	ПредварительнаяВыборка.LetterId КАК LetterId,
	|	ПредварительнаяВыборка.DocumentId КАК DocumentId,
	|	ПредварительнаяВыборка.ДокументЭДО КАК ДокументЭДО,
	|	ПредварительнаяВыборка.PacketId КАК PacketId,
	|	ПредварительнаяВыборка.DepartmentId КАК DepartmentId,
	|	ПредварительнаяВыборка.Статус КАК Статус,
	|	ПредварительнаяВыборка.ДополнительныйСтатус КАК ДополнительныйСтатус,
	|	ПредварительнаяВыборка.ТребуетсяОбработка КАК ТребуетсяОбработка,
	|	ЕСТЬNULL(Диадок_СтатусМЧД.Значение, """") КАК СтатусМЧД,
	|	ЕСТЬNULL(Диадок_СтатусМЧДКритичность.Значение, """") КАК СтатусМЧДКритичность,
	|	ЕСТЬNULL(Диадок_ДокументыДополнительныеРеквизиты.Значение, """") КАК ВнешнийСтатусГИСМТ,
	|	ЕСТЬNULL(Диадок_ЛогистическиеСтатусыДокументов.Значение, """") КАК ВнешнийСтатусКонтурЛогистика,
	|	ЕСТЬNULL(Диадок_СтатусДТС.Значение, """") КАК СтатусДТС,
	|	ЕСТЬNULL(РеквизитFromDepartmentID.Значение, ""&ПустойИдентификатор"") КАК FromDepartmentId,
	|	ЕСТЬNULL(РеквизитToDepartmentID.Значение, ""&ПустойИдентификатор"") КАК ToDepartmentId,
	|	ЕСТЬNULL(РеквизитОбъектМаршрутизации.Значение, """") КАК ОбъектМаршрутизацииИдентификатор,
	|	ЕСТЬNULL(РеквизитОбъектМаршрутизации.ЗначениеСтрока, """") КАК ОбъектМаршрутизацииНаименование,
	|	ЕСТЬNULL(Диадок_Промежуточный.Значение, """") КАК Промежуточный
	|ИЗ
	|	ПредварительнаяВыборка КАК ПредварительнаяВыборка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_Документы.ДополнительныеРеквизиты КАК Диадок_СтатусМЧД
	|		ПО ПредварительнаяВыборка.ДокументЭДО = Диадок_СтатусМЧД.Ссылка
	|			И (Диадок_СтатусМЧД.ИмяРеквизита = &ИмяРеквизита_СтатусМЧД)
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_Документы.ДополнительныеРеквизиты КАК Диадок_СтатусМЧДКритичность
	|		ПО ПредварительнаяВыборка.ДокументЭДО = Диадок_СтатусМЧДКритичность.Ссылка
	|			И (Диадок_СтатусМЧДКритичность.ИмяРеквизита = &ИмяРеквизита_СтатусМЧДКритичность)
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_Документы.ДополнительныеРеквизиты КАК Диадок_Промежуточный
	|		ПО ПредварительнаяВыборка.ДокументЭДО = Диадок_Промежуточный.Ссылка
	|			И (Диадок_Промежуточный.ИмяРеквизита = &ИмяРеквизита_Промежуточный)
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_Документы.ДополнительныеРеквизиты КАК Диадок_ДокументыДополнительныеРеквизиты
	|		ПО ПредварительнаяВыборка.ДокументЭДО = Диадок_ДокументыДополнительныеРеквизиты.Ссылка
	|			И (Диадок_ДокументыДополнительныеРеквизиты.ИмяРеквизита = &ИмяРеквизита_СтатусГИСМТ)
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_Документы.ДополнительныеРеквизиты КАК Диадок_ЛогистическиеСтатусыДокументов
	|		ПО ПредварительнаяВыборка.ДокументЭДО = Диадок_ЛогистическиеСтатусыДокументов.Ссылка
	|			И (Диадок_ЛогистическиеСтатусыДокументов.ИмяРеквизита = &ИмяРеквизита_СтатусКонтурЛогистика)
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_Документы.ДополнительныеРеквизиты КАК Диадок_СтатусДТС
	|		ПО ПредварительнаяВыборка.ДокументЭДО = Диадок_СтатусДТС.Ссылка
	|			И (Диадок_СтатусДТС.ИмяРеквизита = &ИмяРеквизита_СтатусДТС)
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_Документы.ДополнительныеРеквизиты КАК РеквизитFromDepartmentID
	|		ПО ПредварительнаяВыборка.ДокументЭДО = РеквизитFromDepartmentID.Ссылка
	|			И (РеквизитFromDepartmentID.ИмяРеквизита = &ИмяРеквизита_FromDepartmentID)
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_Документы.ДополнительныеРеквизиты КАК РеквизитToDepartmentID
	|		ПО ПредварительнаяВыборка.ДокументЭДО = РеквизитToDepartmentID.Ссылка
	|			И (РеквизитToDepartmentID.ИмяРеквизита = &ИмяРеквизита_ToDepartmentID)
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_Документы.ДополнительныеРеквизиты КАК РеквизитОбъектМаршрутизации
	|		ПО ПредварительнаяВыборка.ДокументЭДО = РеквизитОбъектМаршрутизации.Ссылка
	|			И (РеквизитОбъектМаршрутизации.ИмяРеквизита = &ИмяРеквизита_ОбъектМаршрутизации)";
	
	Результат = СтрЗаменить(Результат, "&ПустойИдентификатор", ПустойИдентификатор());
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ДублиДокумента_Диадок(Идентификаторы)
	
	Результат = Новый Массив;
	
	ДокументЭДО	= СвойствоСтруктуры(Идентификаторы, "ДокументЭДО",	Неопределено);
	BoxID		= СвойствоСтруктуры(Идентификаторы, "BoxID",		"");
	LetterID	= СвойствоСтруктуры(Идентификаторы, "LetterID",		"");
	DocumentID	= СвойствоСтруктуры(Идентификаторы, "DocumentID",	"");
	
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Диадок_Документы.Ссылка
	|ИЗ
	|	Справочник.Диадок_Документы КАК Диадок_Документы
	|ГДЕ
	|	Диадок_Документы.Ссылка <> &Ссылка
	|	И Диадок_Документы.BoxId = &BoxId
	|	И Диадок_Документы.LetterId = &LetterId
	|	И Диадок_Документы.DocumentId = &DocumentId
	|	И НЕ Диадок_Документы.ПометкаУдаления");
	
	Запрос.УстановитьПараметр("BoxId",		BoxId);
	Запрос.УстановитьПараметр("LetterId",	LetterId);
	Запрос.УстановитьПараметр("DocumentId",	DocumentId);
	Запрос.УстановитьПараметр("Ссылка",		ДокументЭДО);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		Результат.Добавить(Выборка.Ссылка);
	КонецЕсли;	
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ИдентификаторыОтправленныхПоДокументам1С_Диадок(Документ, ДокументыОснования)
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	Диадок_Документы.LetterId,
	|	Диадок_Документы.DocumentId
	|ИЗ
	|	Справочник.Диадок_Документы КАК Диадок_Документы
	|ГДЕ
	|	&ОтборПоОрганизации
	|	И &ОтборПоКонтрагенту
	|	И Диадок_Документы.Документ В(&Документы1С)
	|	И Диадок_Документы.LetterId <> """"
	|	И Диадок_Документы.DocumentId <> """"
	|	И НЕ Диадок_Документы.ПометкаУдаления
	|	И Диадок_Документы.Метка <> ""Переотправлен""");
	
	Запрос.УстановитьПараметр("Документы1С", ДокументыОснования);
	
	Если ЗначениеЗаполнено(Документ) Тогда
		
		Запрос.Текст = СтрЗаменить(
			Запрос.Текст,
			"&ОтборПоОрганизации",
			"Диадок_Документы.ОрганизацияДиадок = &ОрганизацияЭДО"
		);
		Запрос.Текст = СтрЗаменить(
			Запрос.Текст,
			"&ОтборПоКонтрагенту",
			"Диадок_Документы.КонтрагентДиадок = &КонтрагентЭДО"
		);
		
		Запрос.УстановитьПараметр("ОрганизацияЭДО", Документ.Отправитель.ОрганизацияЭДО);
		Запрос.УстановитьПараметр("КонтрагентЭДО", Документ.Получатель.ОрганизацияЭДО);
		
	Иначе
		
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ОтборПоОрганизации", "Истина");
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ОтборПоКонтрагенту", "Истина");
		
	КонецЕсли;
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		DocumentId = Новый Структура;
		DocumentId.Вставить("MessageId",	Выборка.LetterId);
		DocumentId.Вставить("EntityId",		Выборка.DocumentId);
		
		Результат.Добавить(DocumentId);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура Документы_УстановитьЗначениеРеквизита_ФункцияДокумента(ОбъектДокумента, ФункцияДокумента)
	
	МетаданныеРеквизита = Метаданные.Справочники.Диадок_Документы.Реквизиты.ФункцияДокумента;
	МаксимальнаяДлина = МетаданныеРеквизита.Тип.КвалификаторыСтроки.Длина;
	
	СохраняемоеЗначение = Документы_ФункцияДокументаДляХранения(ФункцияДокумента);
	
	Если СтрДлина(СохраняемоеЗначение) > МаксимальнаяДлина Тогда
		
		КомментарийЖР = "Длина функции (%1) превышает масимально возможную длину (%2)";
		КомментарийЖР = СтрЗаменить(КомментарийЖР, "%1", СохраняемоеЗначение);
		КомментарийЖР = СтрЗаменить(КомментарийЖР, "%2", XMLСтрока(МаксимальнаяДлина));
		
		ЗаписьЖурналаРегистрации(
			"Диадок",
			УровеньЖурналаРегистрации.Предупреждение,
			МетаданныеРеквизита,
			ОбъектДокумента,
			КомментарийЖР
		);
		
	КонецЕсли;
	
	ОбъектДокумента.ФункцияДокумента = СохраняемоеЗначение;
	
КонецПроцедуры

Функция Документы_Записать_Диадок(Документ)
	
	Результат = Неопределено;
	
	РеквизитыДокумента = Документ.Реквизиты;
	Идентификаторы = Документ.Идентификаторы;
	Отправитель = Документ.Отправитель;
	Получатель = Документ.Получатель;
	СтруктураСтатусов = Документ.Статусы;
	СтруктураТипа = Документ.Тип;
	ДопРеквизиты = Документ.ДопРеквизиты;
	Связи = Документ.Связи;
	Документооборот = Документ.Документооборот;
	ПараметрыШаблона = Документ.ПараметрыШаблона;
	
	Если Идентификаторы.ДокументЭДО = Неопределено Тогда
		ОбъектДокумента = Справочники.Диадок_Документы.СоздатьЭлемент();
	Иначе
		ОбъектДокумента = Идентификаторы.ДокументЭДО.ПолучитьОбъект();
	КонецЕсли;
	
	Если Идентификаторы.ПакетЭДО = Неопределено Тогда
		
		ПакетЭДО = Пакеты_СсылкаПоИдентификаторам_Диадок(Идентификаторы);
		
		Если ПакетЭДО = Неопределено Тогда
			ПакетЭДО = Пакеты_ЗаписатьПоДокументу(Документ);
		КонецЕсли;
		
	Иначе
		ПакетЭДО = Идентификаторы.ПакетЭДО;
	КонецЕсли;
	
	Направление = Документ.Направление;
	
	ОбъектДокумента.Пакет = ПакетЭДО;
	ОбъектДокумента.Направление = Направление;
	ОбъектДокумента.Документ = Документ.ДокументУчета;
	ОбъектДокумента.ИмяФайла = Документ.ИмяФайла;
	ОбъектДокумента.Метка = Документ.Состояние;
	ОбъектДокумента.ДатаЗагрузки = Документ.ДатаЭДО;
	ОбъектДокумента.ВидДокумента =
		ДопСправочники_СсылкаПоИдентификатору_Диадок("ВидыДокументов", Документ.ИдентификаторВида);
	
	ОбъектДокумента.НомерДокумента = РеквизитыДокумента.Номер;
	ОбъектДокумента.ДатаДокумента = РеквизитыДокумента.Дата;
	ОбъектДокумента.СуммаДокумента = РеквизитыДокумента.Сумма;
	ОбъектДокумента.СуммаНДС = РеквизитыДокумента.СуммаНДС;
	ОбъектДокумента.Валюта = РеквизитыДокумента.КодВалюты;
	
	ОбъектДокумента.BoxId = АдресЯщикаВИдентификатор(Идентификаторы.BoxId);
	ОбъектДокумента.LetterId = Идентификаторы.LetterId;
	ОбъектДокумента.DocumentId = Идентификаторы.DocumentId;
	ОбъектДокумента.PacketId = Документ.Связи.PacketId;
	ОбъектДокумента.DepartmentId = ?(Направление = "Исходящее", Отправитель.DepartmentId, Получатель.DepartmentId);
	
	ОбъектДокумента.ОрганизацияДиадок =
		?(Направление = "Исходящее", Отправитель.ОрганизацияЭДО, Получатель.ОрганизацияЭДО);
	ОбъектДокумента.Организация = ?(Направление = "Исходящее", Отправитель.Организация, Получатель.Организация);
	ОбъектДокумента.КонтрагентДиадок = ?(Направление = "Исходящее", Получатель.ОрганизацияЭДО, Отправитель.ОрганизацияЭДО);
	ОбъектДокумента.Контрагент = ?(Направление = "Исходящее", Получатель.Организация, Отправитель.Организация);
	
	ОбъектДокумента.Статус = СтруктураСтатусов.Статус;
	ОбъектДокумента.ДополнительныйСтатус = СтруктураСтатусов.ДополнительныйСтатус;
	
	ОбъектМаршрутизации_УстановитьЗначение_Диадок(ОбъектДокумента, СтруктураСтатусов);
	
	ОбъектДокумента.ТребуетсяОбработка = СтруктураСтатусов.ТребуетсяОбработка;
	
	ОбъектДокумента.ТипДокумента = СтруктураТипа.ТипДокумента;
	ОбъектДокумента.ТипКонтента = СтруктураТипа.ТипКонтента;
	ОбъектДокумента.WorkflowId = СтруктураТипа.WorkflowId;
	
	Документы_УстановитьЗначениеРеквизита_ФункцияДокумента(ОбъектДокумента, СтруктураТипа.ФункцияДокумента);
	
	ОбъектДокумента.Хранилище = Новый ХранилищеЗначения(Документ.Метаданные);
	
	ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, "Комментарий", Документ.Комментарий, "");
	
	Если Документ.Шаблон Тогда
		
		ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, "Шаблон", Истина, "");
		
		Для Каждого Элемент Из ПараметрыШаблона Цикл
			ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, Элемент.Ключ, Элемент.Значение, "");
		КонецЦикла;
		
	КонецЕсли;
	
	ПараметрыОтправки = Неопределено;
	
	Если Документ.СлужебнаяИнформация.Свойство("ПараметрыОтправки", ПараметрыОтправки) Тогда
		
		Если ПараметрыОтправки.ЗапрашиватьИзвещение Тогда
			ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, "ЗапрашиватьИзвещение", Истина, "");
		КонецЕсли;
		
		Если ПараметрыОтправки.ЗапрашиватьОтветнуюПодпись Тогда
			ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, "ЗапрашиватьОтветнуюПодпись", Истина, "");
		КонецЕсли;
		
	КонецЕсли;
	
	Промежуточный = Неопределено;
	
	Если Документ.СлужебнаяИнформация.Свойство("Промежуточный", Промежуточный) Тогда
		ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, "Промежуточный", Промежуточный, "");
	КонецЕсли;

	Для Каждого ТекРеквизит Из ДопРеквизиты Цикл
		ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, ТекРеквизит.Ключ, ТекРеквизит.Значение, "");
	КонецЦикла;
	
	Для Каждого ТекРеквизит Из Связи Цикл
		
		Если ТекРеквизит.Ключ = "CustomDocumentId" ИЛИ ТекРеквизит.Ключ = "OriginMessageId" Тогда
			ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, ТекРеквизит.Ключ, ТекРеквизит.Значение, "");
		ИначеЕсли ТекРеквизит.Ключ = "PacketId" Тогда
			Продолжить; // Записан в шапке документа
		Иначе
			ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, ТекРеквизит.Ключ,
				Новый ХранилищеЗначения(ТекРеквизит.Значение), "");
		КонецЕсли;
		
	КонецЦикла;
	
	Если ЗначениеЗаполнено(Документ.Отправитель.DepartmentId) Тогда
		
		ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_Диадок();
		
		ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, ИменаСвойствНастроек.FromDepartmentId,
			Документ.Отправитель.DepartmentId, "");
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Документ.Получатель.DepartmentId) Тогда
		
		ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_Диадок();
		
		ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, ИменаСвойствНастроек.ToDepartmentID,
			Документ.Получатель.DepartmentId, "");
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтруктураСтатусов.СтатусМЧД) Тогда
		
		ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_Диадок();
		
		ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, ИменаСвойствНастроек.СтатусМЧД,
			СтруктураСтатусов.СтатусМЧД, "");
		ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, ИменаСвойствНастроек.СтатусМЧДКритичность,
			СтруктураСтатусов.СтатусМЧДКритичность, "");
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтруктураСтатусов.СтатусПКФНС) Тогда
		
		ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_Диадок();
		
		ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, ИменаСвойствНастроек.СтатусПКФНС,
			СтруктураСтатусов.СтатусПКФНС, "");
		ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, ИменаСвойствНастроек.СтатусПКФНСКритичность,
			СтруктураСтатусов.СтатусПКФНСКритичность, "");
		
	КонецЕсли;
	
	Для Каждого КлючЗначение Из СтруктураСтатусов.ВнешниеСтатусы Цикл
		
		ИдентификаторВнешнегоСтатуса = КлючЗначение.Ключ;
		ЗначениеВнешнегоСатуса = КлючЗначение.Значение;
		
		ИмяДопРеквизита = Документы_ИмяДопРеквизита_ВнешнийСтатус(ИдентификаторВнешнегоСтатуса);
		
		ОбновитьЗначениеДопРеквизитаДиадок(ОбъектДокумента, ИмяДопРеквизита, ЗначениеВнешнегоСатуса, "");
		
	КонецЦикла;
	
	ОбъектДокумента.Записать();
	
	Для Каждого ТекХранилище Из Документооборот Цикл
		
		ИмяХранилища = ТекХранилище.Ключ;
		ДанныеХранения = ТекХранилище.Значение;
		Документы_ЗаписатьДанныеХранилища(ДанныеХранения, ИмяХранилища, ОбъектДокумента.Ссылка);
		
	КонецЦикла;
	
	Документ.Идентификаторы.ПакетЭДО = ПакетЭДО;
	Документ.Идентификаторы.ДокументЭДО = ОбъектДокумента.Ссылка;
	
	Результат = Документ.Идентификаторы.ДокументЭДО;
	
	Возврат Результат;
	
КонецФункции

Процедура ОбъектМаршрутизации_УстановитьЗначение_Диадок(ОбъектДокумента, СтруктураСтатусов)
	
	ОбъектМаршрутизации = Неопределено;
	
	Если НЕ СтруктураСтатусов.Свойство("ОбъектМаршрутизации", ОбъектМаршрутизации) Тогда
		Возврат;
	КонецЕсли;
	
	ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_Диадок();
	ОбъектМаршрутизации = СтруктураСтатусов.ОбъектМаршрутизации;
	
	ИдентификаторОбъектаМаршрутизации = СвойствоСтруктуры(ОбъектМаршрутизации, "Идентификатор");
	НаименованиеОбъектаМаршрутизации = СвойствоСтруктуры(ОбъектМаршрутизации, "Наименование");
	
	ОбновитьЗначениеДопРеквизитаДиадок(
		ОбъектДокумента,
		ИменаСвойствНастроек.ОбъектМаршрутизации,
		ИдентификаторОбъектаМаршрутизации,
		НаименованиеОбъектаМаршрутизации
	);
	
КонецПроцедуры

Функция Документы_СсылкаНаДокументЭДО_Диадок(Идентификаторы)
	
	Результат = Неопределено;
	
	BoxID		= СвойствоСтруктуры(Идентификаторы, "BoxID",		"");
	LetterID	= СвойствоСтруктуры(Идентификаторы, "LetterID",		"");
	DocumentID	= СвойствоСтруктуры(Идентификаторы, "DocumentID",	"");
	
	Запрос = Новый Запрос;
	
	Запрос.Текст = 
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Диадок_Документы.Ссылка
	|ИЗ
	|	Справочник.Диадок_Документы КАК Диадок_Документы
	|ГДЕ
	|	Диадок_Документы.BoxId = &BoxId
	|	И Диадок_Документы.LetterId = &LetterId
	|	И Диадок_Документы.DocumentId = &DocumentId";
	
	Запрос.УстановитьПараметр("BoxId",		BoxId);
	Запрос.УстановитьПараметр("LetterId",	LetterId);
	Запрос.УстановитьПараметр("DocumentId",	DocumentId);
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Пока ВыборкаЗапроса.Следующий() Цикл
		Результат = ВыборкаЗапроса.Ссылка;
	КонецЦикла;	
	
	Возврат Результат;
	
КонецФункции

Процедура Документы_ОбновлениеРеквизитов_Диадок(Идентификаторы, СтруктураРеквизитов)
	
	ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_Диадок();
	
	ДокументЭДО = СвойствоСтруктуры(Идентификаторы, "ДокументЭДО");
	
	ДокументОбъект = ДокументЭДО.ПолучитьОбъект();
	
	Если СтруктураРеквизитов.Свойство("DepartmentId") Тогда
		ДокументОбъект.DepartmentId = СтруктураРеквизитов.DepartmentId;
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("PacketId") Тогда
		ДокументОбъект.PacketId = СтруктураРеквизитов.PacketId;
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("Статус") Тогда
		ДокументОбъект.Статус = СтруктураРеквизитов.Статус;
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ДополнительныйСтатус") Тогда
		ДокументОбъект.ДополнительныйСтатус = СтруктураРеквизитов.ДополнительныйСтатус;
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ВнешниеСтатусы") Тогда
		
		ЗначениеВнешнегоСатуса = Ядро_СтатусГИСМТ(СтруктураРеквизитов.ВнешниеСтатусы);
		
		Если ЗначениеВнешнегоСатуса <> Неопределено Тогда
			ИмяДопРеквизита = Документы_ИмяДопРеквизита_ВнешнийСтатусГИСМТ();
			ОбновитьЗначениеДопРеквизитаДиадок(ДокументОбъект, ИмяДопРеквизита, ЗначениеВнешнегоСатуса, "");
		КонецЕсли;
		
		ЗначениеВнешнегоСатуса = Ядро_СтатусКонтурЛогистика(СтруктураРеквизитов.ВнешниеСтатусы);
		
		Если ЗначениеВнешнегоСатуса <> Неопределено Тогда
			ИмяДопРеквизита = Документы_ИмяДопРеквизита_ВнешнийСтатусКонтурЛогистика();
			ОбновитьЗначениеДопРеквизитаДиадок(ДокументОбъект, ИмяДопРеквизита, ЗначениеВнешнегоСатуса, "");
		КонецЕсли;
		
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ТребуетсяОбработка") Тогда
		ДокументОбъект.ТребуетсяОбработка = СтруктураРеквизитов.ТребуетсяОбработка;
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ТребуемоеДействиеВДД") Тогда
		ДокументОбъект.ТребуемоеДействиеВДД = СтруктураРеквизитов.ТребуемоеДействиеВДД;
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ТребуемоеДействиеВ1С") Тогда
		ДокументОбъект.ТребуемоеДействиеВ1С = СтруктураРеквизитов.ТребуемоеДействиеВ1С;
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("АнализПроведен") Тогда
		ДокументОбъект.АнализПроведен = СтруктураРеквизитов.АнализПроведен;
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ДокументУчета") Тогда
		ДокументОбъект.Документ = СтруктураРеквизитов.ДокументУчета;
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("Организация") Тогда
		ДокументОбъект.Организация = СтруктураРеквизитов.Организация;
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("Контрагент") Тогда
		ДокументОбъект.Контрагент = СтруктураРеквизитов.Контрагент;
	КонецЕсли;
	
	СтатусМЧД = Неопределено;
	Если СтруктураРеквизитов.Свойство("СтатусМЧД", СтатусМЧД) Тогда
		ОбновитьЗначениеДопРеквизитаДиадок(ДокументОбъект, ИменаСвойствНастроек.СтатусМЧД, СтатусМЧД, "");
	КонецЕсли;
	
	СтатусМЧДКритичность = Неопределено;
	Если СтруктураРеквизитов.Свойство("СтатусМЧДКритичность", СтатусМЧДКритичность) Тогда
		ОбновитьЗначениеДопРеквизитаДиадок(ДокументОбъект, ИменаСвойствНастроек.СтатусМЧДКритичность,
			СтатусМЧДКритичность, "");
	КонецЕсли;
	
	СтатусПКФНС = Неопределено;
	Если СтруктураРеквизитов.Свойство("СтатусПКФНС", СтатусПКФНС) Тогда
		ОбновитьЗначениеДопРеквизитаДиадок(ДокументОбъект, ИменаСвойствНастроек.СтатусПКФНС, СтатусПКФНС, "");
	КонецЕсли;
	
	СтатусПКФНСКритичность = Неопределено;
	Если СтруктураРеквизитов.Свойство("СтатусПКФНСКритичность", СтатусПКФНСКритичность) Тогда
		ОбновитьЗначениеДопРеквизитаДиадок(ДокументОбъект, ИменаСвойствНастроек.СтатусПКФНСКритичность,
			СтатусПКФНСКритичность, "");
	КонецЕсли;
	
	СтатусДТС = Неопределено;
	Если СтруктураРеквизитов.Свойство("СтатусДТС", СтатусДТС) Тогда
		ОбновитьЗначениеДопРеквизитаДиадок(ДокументОбъект, ИменаСвойствНастроек.СтатусДТС, СтатусДТС, "");
	КонецЕсли;
	
	FromDepartmentId = Неопределено;
	Если СтруктураРеквизитов.Свойство("FromDepartmentId", FromDepartmentId) Тогда
		ОбновитьЗначениеДопРеквизитаДиадок(ДокументОбъект, ИменаСвойствНастроек.FromDepartmentId, FromDepartmentId, "");
	КонецЕсли;
	
	ToDepartmentId = Неопределено;
	Если СтруктураРеквизитов.Свойство("ToDepartmentId", ToDepartmentId) Тогда
		ОбновитьЗначениеДопРеквизитаДиадок(ДокументОбъект, ИменаСвойствНастроек.ToDepartmentId, ToDepartmentId, "");
	КонецЕсли;
	
	ОбъектМаршрутизации_УстановитьЗначение_Диадок(ДокументОбъект, СтруктураРеквизитов);
	
	ДокументОбъект.Записать();
	
КонецПроцедуры

Процедура ОбновитьЗначениеДопРеквизитаДиадок(ДокументОбъект, ИмяРеквизита, ЗначениеРеквизита, ЗначениеСтрока) Экспорт
	
	СтрокаРеквизита = ДокументОбъект.ДополнительныеРеквизиты.Найти(ИмяРеквизита, "ИмяРеквизита");
	
	Если СтрокаРеквизита = Неопределено Тогда
		
		ЗначениеДляУстановкиНеЗаполнено =
			(НЕ ЗначениеЗаполнено(ЗначениеРеквизита) ИЛИ ЗначениеРеквизита = Ложь ИЛИ ЗначениеРеквизита = ПустойИдентификатор())
			И (НЕ ЗначениеЗаполнено(ЗначениеСтрока) ИЛИ ЗначениеСтрока = ПустойИдентификатор());
		
		Если ЗначениеДляУстановкиНеЗаполнено Тогда
			Возврат;
		КонецЕсли;
		
		СтрокаРеквизита = ДокументОбъект.ДополнительныеРеквизиты.Добавить();
		СтрокаРеквизита.ИмяРеквизита = ИмяРеквизита;
		
	КонецЕсли;
	
	Если ТипЗнч(ЗначениеРеквизита) = Тип("ХранилищеЗначения") Тогда
		СтрокаРеквизита.ЗначениеХранилищеЗначения = ЗначениеРеквизита;
	ИначеЕсли ТипЗнч(ЗначениеРеквизита) = Тип("Структура") Тогда
		СтрокаРеквизита.ЗначениеХранилищеЗначения = Новый ХранилищеЗначения(ЗначениеРеквизита);
	Иначе
		СтрокаРеквизита.Значение = ЗначениеРеквизита;
	КонецЕсли;
	
	СтрокаРеквизита.ЗначениеСтрока = ЗначениеСтрока;
	
КонецПроцедуры

Функция Ядро_СтатусГИСМТ(ВнешниеСтатусы)
	
	ЗначениеСтатуса = Ядро.ВнешниеСтатусы_СтатусГИСМТ(ВнешниеСтатусы);
	
	Возврат ЗначениеСтатуса;
	
КонецФункции

Функция Ядро_СтатусКонтурЛогистика(ВнешниеСтатусы)
	
	ЗначениеСтатуса = Ядро.ВнешниеСтатусы_СтатусКонтурЛогистика(ВнешниеСтатусы);
	
	Возврат ЗначениеСтатуса;
	
КонецФункции

Функция Ядро_ПеревозочныеДокументы_ТипДокументаAPI_ЭТрН()
	
	Результат = Ядро.ПеревозочныеДокументы_ТипДокументаAPI_ЭТрН();
	
	Возврат Результат;
	
КонецФункции

Функция Ядро_Перевозочные_ГрузополучательПоИмениФайла(FileName, BoxId)
	
	Результат = Ядро.Перевозочные_ГрузополучательПоИмениФайла(FileName, BoxId);
	
	Возврат Результат;
	
КонецФункции

Процедура Документы_СохранитьРезультатАнализа_Диадок(Идентификаторы, РезультатАнализа)
	
	ДокументЭДО	= СвойствоСтруктуры(Идентификаторы, "ДокументЭДО");

	ДокументОбъект = ДокументЭДО.ПолучитьОбъект();
	
	ДокументОбъект.АнализПроведен		= Истина;
	ДокументОбъект.ТекстОшибки			= РезультатАнализа.Ошибки;
	ДокументОбъект.ТребуемоеДействиеВ1С	= РезультатАнализа.ДействиеВ1С;
	ДокументОбъект.ТребуемоеДействиеВДД	= РезультатАнализа.ДействиеВДД;
	ДокументОбъект.Документ 			= РезультатАнализа.ДокументДляСопоставления;
	
	Если РезультатАнализа.Свойство("КонтентОтвета") Тогда
		ДокументОбъект.ХранилищеОтвета = Новый ХранилищеЗначения(РезультатАнализа.КонтентОтвета)
	КонецЕсли;
	
	ДокументОбъект.Записать();
	
КонецПроцедуры

Процедура Документы_СброситьРезультатАнализа_Диадок(Идентификаторы)
	
	ДокументЭДО	= СвойствоСтруктуры(Идентификаторы, "ДокументЭДО");

	ДокументОбъект = ДокументЭДО.ПолучитьОбъект();
	
	ДокументОбъект.АнализПроведен		= Ложь;
	ДокументОбъект.ТекстОшибки			= "";
	ДокументОбъект.ТребуемоеДействиеВ1С	= "";
	ДокументОбъект.ТребуемоеДействиеВДД	= "";
	ДокументОбъект.ХранилищеОтвета		= Неопределено;
	
	ДокументОбъект.Записать();
	
КонецПроцедуры

Функция Документы_МассивДокументов_Диадок(ДокументыЭДО)
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ДокументыЭДО", ДокументыЭДО);
	
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Диадок_Документы.Ссылка КАК Ссылка,
	|	Диадок_Документы.Пакет,
	|	Диадок_Документы.Пакет.ЗаблокированныйПакет КАК ЗаблокированныйПакет
	|ПОМЕСТИТЬ ВТ_ВыбранныеДокументы
	|ИЗ
	|	Справочник.Диадок_Документы КАК Диадок_Документы
	|ГДЕ
	|	Диадок_Документы.Ссылка В(&ДокументыЭДО)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Диадок_Документы.Ссылка
	|ПОМЕСТИТЬ ВТ_ДополненнаяТаблица
	|ИЗ
	|	ВТ_ВыбранныеДокументы КАК ВТ_ВыбранныеДокументы
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_Документы КАК Диадок_Документы
	|		ПО ВТ_ВыбранныеДокументы.Пакет = Диадок_Документы.Пакет
	|			И (ВТ_ВыбранныеДокументы.ЗаблокированныйПакет)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ВТ_ВыбранныеДокументы.Ссылка
	|ИЗ
	|	ВТ_ВыбранныеДокументы КАК ВТ_ВыбранныеДокументы
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_ДополненнаяТаблица.Ссылка
	|ИЗ
	|	ВТ_ДополненнаяТаблица КАК ВТ_ДополненнаяТаблица
	|
	|СГРУППИРОВАТЬ ПО
	|	ВТ_ДополненнаяТаблица.Ссылка";
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Пока ВыборкаЗапроса.Следующий() Цикл
		
		ИдентификаторыДокумента = Новый Структура;
		ИдентификаторыДокумента.Вставить("ДокументЭДО", ВыборкаЗапроса.Ссылка);
		
		Документ = Документы_ДанныеДокумента_Диадок(ИдентификаторыДокумента);
		
		Результат.Добавить(Документ);
		
	КонецЦикла;	
	
	Возврат Результат;
	
КонецФункции

// Устанавливает правило формирования имени дополнительного реквизита "Внешний статус"
//
// Параметры:
//	ИменованныйИдентификатор - Строка - именованный идентификатор внешнего документооборота(DocflowNamedId)
//
// Возвращаемое значение:
//	Строка
Функция Документы_ИмяДопРеквизита_ВнешнийСтатус(ИменованныйИдентификатор)
	
	Результат = "ВнешнийСтатус_" + ИменованныйИдентификатор;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ИмяДопРеквизита_ВнешнийСтатусГИСМТ()
	
	ИменованныйИдентификатор = Ядро_ИдентификаторВнешнегоДО_ГИСМТ();
	
	Результат = Документы_ИмяДопРеквизита_ВнешнийСтатус(ИменованныйИдентификатор);
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ИмяДопРеквизита_ВнешнийСтатусКонтурЛогистика()
	
	ИменованныйИдентификатор = Ядро_ИдентификаторВнешнегоДО_КонтурЛогистика();
	
	Результат = Документы_ИмяДопРеквизита_ВнешнийСтатус(ИменованныйИдентификатор);
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ЕстьЭлектронныйДокументПоДокументу1С_Диадок(Документ1С)
	
	Документ = Неопределено;
	
	ДокументыОснования = Новый Массив;
	ДокументыОснования.Добавить(Документ1С);
	
	ИдентификаторыДокумента = Документы_ИдентификаторыОтправленныхПоДокументам1С_Диадок(Документ, ДокументыОснования);
	
	Результат = ЗначениеЗаполнено(ИдентификаторыДокумента);
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ОтправленныеУчетныеДокументы_Диадок(ИдентификаторВидаПакета, УчетныеДокументы)
	
	Результат = Новый Соответствие;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	Диадок_ПакетыДокументов.Документ КАК Документ
	|ИЗ
	|	Справочник.Диадок_ПакетыДокументов КАК Диадок_ПакетыДокументов
	|ГДЕ
	|	Диадок_ПакетыДокументов.ВидПакета = &ВидПакета
	|	И Диадок_ПакетыДокументов.Документ В(&УчетныеДокументы)
	|	И Диадок_ПакетыДокументов.LetterId <> """"
	|	И НЕ Диадок_ПакетыДокументов.ПометкаУдаления
	|	И Диадок_ПакетыДокументов.Метка = ""Отправлен""");
	
	ВидПакета = ДопСправочники_СсылкаПоИдентификатору_Диадок("ВидыПакетов", ИдентификаторВидаПакета);
	
	Запрос.УстановитьПараметр("ВидПакета", ВидПакета);
	Запрос.УстановитьПараметр("УчетныеДокументы", УчетныеДокументы);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Результат.Вставить(Выборка.Документ, Истина);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

//**********************************************
// ДОКУМЕНТЫ: 1С

Функция Документы_ДанныеДокумента_1С(Идентификаторы, ДокументАПИ = Неопределено)
	
	BoxID		= Идентификаторы.BoxID;
	LetterId	= Идентификаторы.LetterId;
	DocumentId	= Идентификаторы.DocumentId;
	
	Если ДокументАПИ = Неопределено Тогда
		ДокументАПИ = API_GetDocument(BoxId, LetterId, DocumentId);
	КонецЕсли;
	
	Результат = Документы_КонтрактПоДокументуАПИ(BoxID, ДокументАПИ);
	
	Результат.Идентификаторы.BoxID = BoxID;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_КоллекцияСопоставленныхВ1С(Идентификаторы)

	Запрос = Новый Запрос;
	
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЗначенияСвойствОбъектов.Объект
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов_Организации
	|		ПО ЗначенияСвойствОбъектов.Объект = ЗначенияСвойствОбъектов_Организации.Объект
	|			И (ЗначенияСвойствОбъектов.Свойство В (&СвойстваИдентификаторовДокументов))
	|			И (ЗначенияСвойствОбъектов_Организации.Свойство В (&СвойстваИдентификаторовЯщика))
	|			И (ЗначенияСвойствОбъектов_Организации.Значение = &IDОрганизации)
	|			И (ЗначенияСвойствОбъектов.Значение = &ИдентификаторДокумента)";
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Запрос.Текст);
	
	IDОрганизации			= ИдентификаторВАдресЯщика(Идентификаторы.BoxID);
	ИдентификаторДокумента	= Документы_ИдентификаторДокумента_1С(Идентификаторы);
	
	Запрос.УстановитьПараметр("ИдентификаторДокумента",	ИдентификаторДокумента);
	Запрос.УстановитьПараметр("IDОрганизации",			IDОрганизации);
	
	Запросы_ЗаполнитьПараметрыСвойствИдентификаторовДокумента(Запрос);
	
	Результат = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Объект");
	
	Возврат Результат;

КонецФункции

Функция Документы_СопоставленныйВ1С_1С(Идентификаторы)

	Результат = Неопределено;
	
	КоллекцияДокументов = Документы_КоллекцияСопоставленныхВ1С(Идентификаторы);
	
	Если КоллекцияДокументов.Количество() > 0 Тогда
		Результат = КоллекцияДокументов[0];
	КонецЕсли;	
	
	Возврат Результат;

КонецФункции

Функция Документы_ИдентификаторыОтправленныхПоДокументам1С_1С(ДокументыОснования)
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос;
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	ЗначенияСвойствОбъектов.Значение КАК ИдентификаторДокумента
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Свойство В(&СвойстваИдентификаторовДокументов)
	|	И ЗначенияСвойствОбъектов.Объект В(&МассивДокументов1С)";
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	ТекстЗапроса	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Запрос.Текст = ТекстЗапроса;
	
	ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_1С();
	
	СвойстваИдентификаторовДокументов = Новый Массив;
	СвойстваИдентификаторовДокументов.Добавить(Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.ИдентификаторДокумента, Ложь));
	СвойстваИдентификаторовДокументов.Добавить(Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.ИдентификаторСчетаУФ, Ложь));
	СвойстваИдентификаторовДокументов.Добавить(Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.ИдентификаторСчетаОФ, Ложь));
	
	Запрос.УстановитьПараметр("СвойстваИдентификаторовДокументов",	СвойстваИдентификаторовДокументов);
	Запрос.УстановитьПараметр("МассивДокументов1С",					ДокументыОснования);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		СтруктураИдентификаторов = Документы_СтруктураИдентифкаторов_1С(Выборка.ИдентификаторДокумента);
		
		DocumentId = Новый Структура;
		DocumentId.Вставить("MessageId",	СтруктураИдентификаторов.LetterId);
		DocumentId.Вставить("EntityId",		СтруктураИдентификаторов.DocumentId);
		
		Результат.Добавить(DocumentId);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_Записать_1С(Документ)
	
	Результат = Неопределено;
	
	ДокументУчета = Документ.ДокументУчета;
	
	Если ЗначениеЗаполнено(ДокументУчета) Тогда
		
		ИдентификаторДокумента = Документы_ИдентификаторДокумента_1С(Документ.Идентификаторы);
		ИдентификаторЯщикаДокумента = ИдентификаторВАдресЯщика(Документ.Идентификаторы.BoxID);
		
		ПереопределяемыеИменаСвойств = Документы_ПереопределяемыеИменаСвойств1С(Документ);
		
		ИмяСвойства_ВидПакета = ПереопределяемыеИменаСвойств.ВидПакета;
		ИмяСвойства_ИдентификаторДокумента = ПереопределяемыеИменаСвойств.ИдентификаторДокумента;
		ИмяСвойства_ИдентификаторЯщикаДокумента = ПереопределяемыеИменаСвойств.ИдентификаторЯщикаДокумента;
		
		ДопРеквизиты_УстановитьЗначение_1С(
			ДокументУчета,
			ИмяСвойства_ИдентификаторДокумента,
			ИдентификаторДокумента,
			Ложь);
		
		ДопРеквизиты_УстановитьЗначение_1С(
			ДокументУчета,
			ИмяСвойства_ИдентификаторЯщикаДокумента,
			ИдентификаторЯщикаДокумента,
			Ложь);
		
		ДопРеквизиты_УстановитьЗначение_1С(
			ДокументУчета,
			ИмяСвойства_ВидПакета,
			Документ.ИдентификаторВидаПакета);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Переопределяет имена свойств документа задействованных в подсистеме хранения 1С 
//
// Параметры:
//	Документ - Структура - контракт документа (см. Ядро.Контракт_Документ())
//
// Возвращаемое значение:
//	Структура - с ключами:	"ИдентификаторДокумента"
//							"ИдентификаторЯщикаДокумента"
Функция Документы_ПереопределяемыеИменаСвойств1С(Документ)
	
	ИменаСвойствНастроек_1С = Перечисление_ИменаСвойствНастроек_1С();
	
	Результат = Новый Структура;
	
	Результат.Вставить("ВидПакета", ИменаСвойствНастроек_1С.ВидПакета);
	Результат.Вставить("ИдентификаторДокумента", ИменаСвойствНастроек_1С.ИдентификаторДокумента);
	Результат.Вставить("ИдентификаторЯщикаДокумента", ИменаСвойствНастроек_1С.ИдентификаторЯщикаДокумента);
	
	ИспользуютсяНастройкиПоУмолчаниюДляСчетов = Ядро.Настройки_ОснованияВидовДокументов_ИспользуютсяНастройкиПоУмолчанию();
	ЭтоИдентификаторВидаСчет = Ядро.ЭтоИдентификаторВидаСчет(Документ.ИдентификаторВида);
	
	Если НЕ ИспользуютсяНастройкиПоУмолчаниюДляСчетов И ЭтоИдентификаторВидаСчет Тогда
		
		ЭтоДокументКонфигурацииСчетНаОплату = Ядро.Общее_ЭтоДокументКонфигурацииСчетНаОплату(Документ.ДокументУчета);
		Если НЕ ЭтоДокументКонфигурацииСчетНаОплату Тогда
			
			Результат.ВидПакета = ИменаСвойствНастроек_1С.ВидПакетаСчет;
			Результат.ИдентификаторДокумента = ИменаСвойствНастроек_1С.ИдентификаторДокументаСчет;
			Результат.ИдентификаторЯщикаДокумента = ИменаСвойствНастроек_1С.ИдентификаторЯщикаДокументаСчет;
			
		КонецЕсли;
		
	КонецЕсли;
	
	ЭтоИдентификаторВидаЭТрН = Ядро.ПеревозочныеДокументы_ЭтоИдентификаторВидаЭТрН(Документ.ИдентификаторВида);
	
	Если ЭтоИдентификаторВидаЭТрН Тогда
		
		Результат.ВидПакета = ИменаСвойствНастроек_1С.ВидПакетаЭТрН;
		Результат.ИдентификаторДокумента = ИменаСвойствНастроек_1С.ИдентификаторДокументаЭТрН;
		Результат.ИдентификаторЯщикаДокумента = ИменаСвойствНастроек_1С.ИдентификаторЯщикаДокументаЭТрН;
		
	КонецЕсли;
	
	ЭтоИдентификаторВидаЗаказаЗаявки = Ядро.ПеревозочныеДокументы_ЭтоИдентификаторВидаЗаказЗаявка(
		Документ.ИдентификаторВида);
	
	Если ЭтоИдентификаторВидаЗаказаЗаявки Тогда
		
		Результат.ВидПакета = ИменаСвойствНастроек_1С.ВидПакетаЗЗ;
		Результат.ИдентификаторДокумента = ИменаСвойствНастроек_1С.ИдентификаторДокументаЗЗ;
		Результат.ИдентификаторЯщикаДокумента = ИменаСвойствНастроек_1С.ИдентификаторЯщикаДокументаЗЗ;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура Документы_ОбновлениеРеквизитов_1С(Идентификаторы, СтруктураРеквизитов)
	
	Если СтруктураРеквизитов.Свойство("ДокументУчета") Тогда
		
		ДокументУчета = СтруктураРеквизитов.ДокументУчета;
		
		Документы_ОчиститьНастройки_1С(Идентификаторы);
		
		Если ДокументУчета <> Неопределено Тогда 
			
			ИдентификаторДокумента		= Документы_ИдентификаторДокумента_1С(Идентификаторы);
			ИдентификаторЯщикаДокумента	= ИдентификаторВАдресЯщика(Идентификаторы.BoxID);
			
			ДопРеквизиты_УстановитьЗначение_1С(ДокументУчета, Перечисление_ИменаСвойствНастроек_1С().ИдентификаторДокумента, ИдентификаторДокумента, Ложь);
			ДопРеквизиты_УстановитьЗначение_1С(ДокументУчета, Перечисление_ИменаСвойствНастроек_1С().ИдентификаторЯщикаДокумента, ИдентификаторЯщикаДокумента, Ложь);
		
		КонецЕсли;
		
	КонецЕсли;
		
КонецПроцедуры

Функция Документы_ИдентификаторДокумента_1С(Идентификаторы)
	
	GUID = Идентификаторы.LetterID + Идентификаторы.DocumentID;
	
	Результат = GUID_2_Base64(GUID);
	
	Возврат Результат;
	
КонецФункции

Функция Документы_СтруктураИдентифкаторов_1С(ИдентификаторДокумента)
	
	GUID = Base64_2_GUID(ИдентификаторДокумента);
	
	Результат = Новый Структура;
	Результат.Вставить("LetterID",		Лев(GUID, 36));
	Результат.Вставить("DocumentID",	Прав(GUID, 36));
	
	Возврат Результат;
	
КонецФункции	

// Дополняет контракт документа значением из информационной базы.
//
// Параметры:
//  Документ - Структура - контракт документа (см. Ядро.Контракт_Документ())
// 
Процедура Документы_ДополнитьДаннымиИнформационнойБазы_1С(Документ)
	
	ДокументУчета = Документы_СопоставленныйВ1С_1С(Документ.Идентификаторы);
	
	Если ЗначениеЗаполнено(ДокументУчета) Тогда
		
		МассивДокументов = Новый Массив;
		МассивДокументов.Добавить(ДокументУчета);
		Выборка = ПолучитьВыборкуПоСвойствамДокументовУчета_1С(МассивДокументов);
		
		Если Выборка.Следующий() Тогда
			
			ВидПакетаID = ВидПакетаПоДокументуУчета_1С(
				Документ.Тип.ТипДокумента,
				ДокументУчета,
				СокрЛП(Выборка.ИдентификаторВидаПакетаСчет),
				СокрЛП(Выборка.ИдентификаторВидаПакета)
			);
			
			Документ.ИдентификаторВидаПакета = ВидПакетаID;
			
		КонецЕсли;
		
		Документ.ДокументУчета = ДокументУчета;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ВидПакетаПоДокументуУчета_1С(ТипДокумента, ДокументУчета, ИдентификаторВидаПакетаСчет, ИдентификаторВидаПакета)
	
	ВидПакетаID = ИдентификаторВидаПакета;

	Если ЭтоСчетНаОплату(ТипДокумента)
		И Не Ядро.Общее_ЭтоДокументКонфигурацииСчетНаОплату(ДокументУчета) 
		И ЗначениеЗаполнено(ИдентификаторВидаПакетаСчет) Тогда
		ВидПакетаID = ИдентификаторВидаПакетаСчет;
	КонецЕсли;
	
	Возврат ВидПакетаID;

КонецФункции

Функция ПолучитьВыборкуПоСвойствамДокументовУчета_1С(ДокументыУчета)
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЗначенияСвойствОбъектов.Объект КАК Документ,
	|	МАКСИМУМ(ВЫБОР
	|			КОГДА ЗначенияСвойствОбъектов.Свойство = &СвойствоВидПакетаСчет
	|				ТОГДА ВЫРАЗИТЬ(ЗначенияСвойствОбъектов.Значение КАК СТРОКА(100))
	|			ИНАЧЕ НЕОПРЕДЕЛЕНО
	|		КОНЕЦ) КАК ИдентификаторВидаПакетаСчет,
	|	МАКСИМУМ(ВЫБОР
	|			КОГДА ЗначенияСвойствОбъектов.Свойство = &СвойствоВидПакета
	|				ТОГДА ВЫРАЗИТЬ(ЗначенияСвойствОбъектов.Значение КАК СТРОКА(100))
	|			ИНАЧЕ НЕОПРЕДЕЛЕНО
	|		КОНЕЦ) КАК ИдентификаторВидаПакета
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Свойство В(&СвойстваВидаПакета)
	|	И ЗначенияСвойствОбъектов.Объект В(&ДокументыУчета)
	|	И (ВЫРАЗИТЬ(ЗначенияСвойствОбъектов.Значение КАК СТРОКА(100))) <> """"
	|
	|СГРУППИРОВАТЬ ПО
	|	ЗначенияСвойствОбъектов.Объект");
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Запрос.Текст);
	
	ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_1С();
	
	СвойствоВидПакета = Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.ВидПакета);
	СвойствоВидПакетаСчет = Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.ВидПакетаСчет);
	
	СвойстваВидаПакета = Новый Массив;
	СвойстваВидаПакета.Добавить(СвойствоВидПакета);
	СвойстваВидаПакета.Добавить(СвойствоВидПакетаСчет);
	
	Запрос.УстановитьПараметр("СвойстваВидаПакета", СвойстваВидаПакета);
	Запрос.УстановитьПараметр("СвойствоВидПакетаСчет", СвойствоВидПакетаСчет);
	Запрос.УстановитьПараметр("СвойствоВидПакета", СвойствоВидПакета);
	Запрос.УстановитьПараметр("ДокументыУчета", ДокументыУчета);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Возврат Выборка;

КонецФункции

Процедура Документы_ОчиститьНастройки_1С(Идентификаторы)
	
	КоллекцияДокументовУчета = Документы_КоллекцияСопоставленныхВ1С(Идентификаторы);
	
	Для Каждого Элемент Из КоллекцияДокументовУчета Цикл

		ДопРеквизиты_УдалитьЗначение_1С(Элемент, Свойства_ИдентификаторДокумента());
		ДопРеквизиты_УдалитьЗначение_1С(Элемент, Свойства_ИдентификаторЯщикаДокумента());
		ДопРеквизиты_УдалитьЗначение_1С(Элемент, Свойства_ИдентификаторЯщикаДокументаСчет());
		ДопРеквизиты_УдалитьЗначение_1С(Элемент, Свойства_ВидПакета());
		ДопРеквизиты_УдалитьЗначение_1С(Элемент, Свойства_ВидПакетаСчет());
		ДопРеквизиты_УдалитьЗначение_1С(Элемент, Свойства_ДокументУчетаПакета());
		ДопРеквизиты_УдалитьЗначение_1С(Элемент, Свойства_ДокументУчетаПакетаСчет());
		ДопРеквизиты_УдалитьЗначение_1С(Элемент, Свойства_ДиадокCustomDocumentIdСчет());
		ДопРеквизиты_УдалитьЗначение_1С(Элемент, Свойства_ИдентификаторСчетаОФ());
		ДопРеквизиты_УдалитьЗначение_1С(Элемент, Свойства_ИдентификаторСчетаУФ());
		ДопРеквизиты_УдалитьЗначение_1С(Элемент, Свойства_ИдентификаторЯщикаСчета());
		ДопРеквизиты_УдалитьЗначение_1С(Элемент, Свойства_ДиадокCustomDocumentId());
		
	КонецЦикла;
	
КонецПроцедуры

Функция Документы_ЕстьЭлектронныйДокументПоДокументу1С_1С(Документ1С)
	
	ДокументыОснования = Новый Массив;
	ДокументыОснования.Добавить(Документ1С);
	
	ИдентификаторыДокумента = Документы_ИдентификаторыОтправленныхПоДокументам1С_1С(ДокументыОснования);
	
	Результат = ЗначениеЗаполнено(ИдентификаторыДокумента);
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ОтправленныеУчетныеДокументы_1С(ИдентификаторВидаПакета, УчетныеДокументы)
	
	Результат = Новый Соответствие;
	
	Запрос = Новый Запрос;
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	ЗначенияСвойствОбъектов.Объект КАК Документ
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Объект В(&УчетныеДокументы)
	|	И ЗначенияСвойствОбъектов.Значение = &ВидПакета";
	
	ИмяРегистра = Свойства_ИмяРегистраСведений();
	Запрос.Текст = СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Запрос.УстановитьПараметр("ВидПакета", ИдентификаторВидаПакета);
	Запрос.УстановитьПараметр("УчетныеДокументы", УчетныеДокументы);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Результат.Вставить(Выборка.Документ, Истина);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

//**********************************************
// ДОКУМЕНТЫ: ДАННЫЕ ХРАНИЛИЩА

Функция Документы_СсылкаНаДанныеХранилища_Диадок(Идентификаторы, ИмяХранилища)
	
	Выборка	= Документы_ВыборкаДанныхХранилищаИзБазы_Диадок(Идентификаторы, ИмяХранилища);
		
	Если Выборка.Следующий() Тогда
		Результат = Выборка.Ссылка;
	Иначе
		Результат = Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ДанныеХранилища_Диадок(Идентификаторы, ИмяХранилища, НомерПодписи)

	Результат = Контракт_ДанныеХранения();
	
	Выборка	= Документы_ВыборкаДанныхХранилищаИзБазы_Диадок(Идентификаторы, ИмяХранилища);
		
	Если Выборка.Следующий() Тогда
		
		Результат = Документы_КонтрактДанныхХранилищаПоВыборке_Диадок(Выборка, ИмяХранилища);
		
		Если Истина
			И НЕ ЗначениеЗаполнено(Результат.Данные)
			И НЕ ЗначениеЗаполнено(Результат.ЗашифрованныеДанные) Тогда
			
			ПакетОтправлен = ЗначениеЗаполнено(Выборка.LetterId);
			Если ПакетОтправлен Тогда
				
				Если Не ЗначениеЗаполнено(Идентификаторы.BoxId) Тогда
					Документ = Документы_ДанныеДокумента_Диадок(Идентификаторы);
					ЗаполнитьЗначенияСвойств(Идентификаторы, Документ.Идентификаторы, "BoxId, LetterId, DocumentID");
				КонецЕсли;
				
				Результат = Документы_ДанныхХранения_АПИ(Идентификаторы, ИмяХранилища);
				Документы_ЗаписатьДанныеХранилища(Результат, ИмяХранилища, Выборка.ДокументЭДО);
				
			КонецЕсли;
			
		Иначе
			Результат.Подпись = Документы_ДанныеПодписи_Диадок(Идентификаторы, ИмяХранилища, НомерПодписи);
		КонецЕсли;
		
	Иначе
		
		ПакетОтправлен = ЗначениеЗаполнено(Идентификаторы.LetterId);
		
		Если ПакетОтправлен Тогда
			
			Если Не ЗначениеЗаполнено(Идентификаторы.BoxId) Тогда
				Документ = Документы_ДанныеДокумента_Диадок(Идентификаторы);
				ЗаполнитьЗначенияСвойств(Идентификаторы, Документ.Идентификаторы, "BoxId, LetterId, DocumentID");
			КонецЕсли;
			
			Результат = Документы_ДанныхХранения_АПИ(Идентификаторы, ИмяХранилища);
			
		КонецЕсли;
		
	КонецЕсли;
			
	Возврат Результат;
	
КонецФункции

Функция Документы_ВыборкаДанныхХранилищаИзБазы_Диадок(Идентификаторы, ИмяХранилища = "")
	
	ДокументЭДО	= СвойствоСтруктуры(Идентификаторы, "ДокументЭДО",	Неопределено);
	BoxID		= СвойствоСтруктуры(Идентификаторы, "BoxID",		"");
	LetterID	= СвойствоСтруктуры(Идентификаторы, "LetterID",		"");
	DocumentID	= СвойствоСтруктуры(Идентификаторы, "DocumentID",	"");

	Если ЗначениеЗаполнено(ДокументЭДО) Тогда
		Отбор = "ХранениеДанных.Владелец = &Документ";
	Иначе
		Отбор = "ХранениеДанных.Владелец.BoxId = &BoxId И ХранениеДанных.Владелец.LetterId = &LetterId И ХранениеДанных.Владелец.DocumentId = &DocumentId";
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ИмяХранилища) Тогда
		Отбор = Отбор + "
				|	И ХранениеДанных.Наименование = &Наименование";
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = Документы_ТекстЗапросаДанныхХранилищаДляКонтракта_Диадок(Отбор);
	
	Запрос.УстановитьПараметр("Документ",		ДокументЭДО);
	Запрос.УстановитьПараметр("BoxID",			BoxID);
	Запрос.УстановитьПараметр("LetterId",		LetterId);
	Запрос.УстановитьПараметр("DocumentID",		DocumentID);
	Запрос.УстановитьПараметр("Наименование",	ИмяХранилища);
	
	Результат = Запрос.Выполнить().Выбрать();
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ТекстЗапросаДанныхХранилищаДляКонтракта_Диадок(Отбор = "Истина")
	
	Результат = 
	"ВЫБРАТЬ
	|	ХранениеДанных.Ссылка КАК Ссылка,
	|	ХранениеДанных.ХранилищеДанных КАК ХранилищеДанных,
	|	ХранениеДанных.ДатаСоздания КАК ДатаСоздания,
	|	ХранениеДанных.Описание КАК Описание,
	|	ХранениеДанных.ИмяФайла КАК ИмяФайла,
	|	ХранениеДанных.РасширениеФайла КАК РасширениеФайла,
	|	ХранениеДанных.ХранилищеЗашифрованныхДанных КАК ХранилищеЗашифрованныхДанных,
	|	ХранениеДанных.ТребуетсяРасшифровка КАК ТребуетсяРасшифровка,
	|	ХранениеДанных.Владелец.Пакет.Код КАК КодПакета,
	|	ХранениеДанных.Владелец.Код КАК КодДокумента,
	|	ХранениеДанных.ХранилищеДанных.Размер КАК Размер,
	|	ХранениеДанных.Владелец.BoxId КАК BoxId,
	|	ХранениеДанных.Владелец.LetterId КАК LetterId,
	|	ХранениеДанных.Владелец.DocumentId КАК DocumentId,
	|	ХранениеДанных.Владелец КАК ДокументЭДО,
	|	ХранениеДанных.Наименование КАК ИмяХранилища
	|ИЗ
	|	Справочник.Диадок_ХранениеДанных КАК ХранениеДанных
	|ГДЕ
	|	&Отбор";
	
	Результат = СтрЗаменить(Результат, "&Отбор", Отбор);
	
	Возврат Результат;
	
КонецФункции

Функция Документы_КонтрактДанныхХранилищаПоВыборке_Диадок(Выборка, ИмяХранилища)
	
	Результат = Контракт_ДанныеХранения();
	
	ЗаполнитьЗначенияСвойств(Результат, Выборка, "ИмяФайла, Размер, ДатаСоздания, Описание, ТребуетсяРасшифровка");

	Если Настройки_ОбщиеНастройки().ХранитьДокументыНаДиске Тогда
		
		СтруктураДанных = Документы_ДвоичныеДанныеХранилища_ВнешнееХранение(Результат, Выборка.КодПакета, Выборка.КодДокумента, ИмяХранилища);
		ЗаполнитьЗначенияСвойств(Результат, СтруктураДанных, "Данные, ЗашифрованныеДанные");
		
	Иначе
		
		Результат.Данные				= Выборка.ХранилищеДанных.Хранилище.Получить();
		Результат.ЗашифрованныеДанные	= Выборка.ХранилищеЗашифрованныхДанных.Хранилище.Получить();
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ДвоичныеДанныеХранилища_ВнешнееХранение(ДанныеХранения, КодПакета, КодДокумента, ИмяХранилища)
	
	Результат = Новый Структура;
	Результат.Вставить("Данные",				Неопределено);
	Результат.Вставить("ЗашифрованныеДанные",	Неопределено);
	
	//данные
	РасположениеФайла	= ВнешнееХранение_ИмяФайлаНаДиске(КодПакета, КодДокумента, ИмяХранилища);
	ФайлДанныхХранилища	= Новый Файл(РасположениеФайла.ИмяФайла);
	
	Если ФайлДанныхХранилища.Существует() Тогда
		Данные	= Новый ДвоичныеДанные(РасположениеФайла.ИмяФайла);
		Результат.Вставить("Данные", Данные);
	КонецЕсли;
	
	//зашифрованные данные
	РасположениеФайла	= ВнешнееХранение_ИмяФайлаНаДиске(КодПакета, КодДокумента, ИмяХранилища, Истина);
	ФайлДанныхХранилища	= Новый Файл(РасположениеФайла.ИмяФайла);
	
	Если ФайлДанныхХранилища.Существует() Тогда
		ЗашифрованныеДанные	= Новый ДвоичныеДанные(РасположениеФайла.ИмяФайла);
		Результат.Вставить("ЗашифрованныеДанные", ЗашифрованныеДанные);
	КонецЕсли;
		
	Возврат Результат;
	
КонецФункции


Функция Документы_ДанныеПодписи_Диадок(Идентификаторы, ИмяХранилища, НомерПодписи)

	Результат = Контракт_Подпись();
	
	Выборка	= Документы_ВыборкаДанныхПодписиИзБазы_Диадок(Идентификаторы, ИмяХранилища, НомерПодписи);
		
	Если Выборка.Следующий() Тогда
		
		Результат = Документы_КонтрактПодписиПоВыборке_Диадок(Выборка, ИмяХранилища);
		
		Если НЕ ЗначениеЗаполнено(Результат.ДанныеПодписи) Тогда
			
			ПакетОтправлен = ЗначениеЗаполнено(Выборка.LetterId);
			
			Если ПакетОтправлен Тогда
				
				КонтрактПодписи	= Документы_ДанныеПодписи_АПИ(Идентификаторы, ИмяХранилища);
				ЗаполнитьЗначенияСвойств(Результат, КонтрактПодписи); 
				Документы_ЗаписатьДанныеПодписи_Диадок(Результат, ИмяХранилища, Выборка.ДокументЭДО);
				
			КонецЕсли;
			
		КонецЕсли;
				
	КонецЕсли;
			
	Возврат Результат;
	
КонецФункции

Функция Документы_ВыборкаДанныхПодписиИзБазы_Диадок(Идентификаторы, ИмяХранилища, НомерПодписи)
	
	ДокументЭДО	= СвойствоСтруктуры(Идентификаторы, "ДокументЭДО",	Неопределено);
	BoxID		= СвойствоСтруктуры(Идентификаторы, "BoxID",		"");
	LetterID	= СвойствоСтруктуры(Идентификаторы, "LetterID",		"");
	DocumentID	= СвойствоСтруктуры(Идентификаторы, "DocumentID",	"");
	
	Если ЗначениеЗаполнено(ДокументЭДО) Тогда
		Отбор = "ПодписиХранилища.Ссылка.Владелец = &Документ";
	Иначе
		Отбор = "ПодписиХранилища.Ссылка.Владелец.BoxId = &BoxId И ПодписиХранилища.Ссылка.Владелец.LetterId = &LetterId И ПодписиХранилища.Ссылка.Владелец.DocumentId = &DocumentId";
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = Документы_ТекстЗапросаДанныхПодписиДляКонтракта_Диадок(Отбор);
	
	Запрос.УстановитьПараметр("Документ",		ДокументЭДО);
	Запрос.УстановитьПараметр("BoxID",			BoxID);
	Запрос.УстановитьПараметр("LetterId",		LetterId);
	Запрос.УстановитьПараметр("DocumentID",		DocumentID);
	Запрос.УстановитьПараметр("Наименование",	ИмяХранилища);
	Запрос.УстановитьПараметр("НомерСтроки",	НомерПодписи);
	
	Результат = Запрос.Выполнить().Выбрать();
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ТекстЗапросаДанныхПодписиДляКонтракта_Диадок(Отбор = "Истина")
	
	Результат = 
	"ВЫБРАТЬ
	|	ПодписиХранилища.Ссылка КАК Ссылка,
	|	ПодписиХранилища.ХранилищеПодписи.Хранилище КАК ХранилищеПодписи,
	|	ПодписиХранилища.ДатаПодписи КАК ДатаПодписи,
	|	ПодписиХранилища.ПодписьВерна КАК ПодписьВерна,
	|	ПодписиХранилища.Ссылка.Владелец.Пакет.Код КАК КодПакета,
	|	ПодписиХранилища.Ссылка.Владелец.Код КАК КодДокумента,
	|	ПодписиХранилища.Ссылка.Владелец КАК ДокументЭДО,
	|	ПодписиХранилища.Ссылка.Владелец.BoxId КАК BoxId,
	|	ПодписиХранилища.Ссылка.Владелец.LetterId КАК LetterId,
	|	ПодписиХранилища.Ссылка.Владелец.DocumentId КАК DocumentId
	|ИЗ
	|	Справочник.Диадок_ХранениеДанных.Подписи КАК ПодписиХранилища
	|ГДЕ
	|	&Отбор
	|	И ПодписиХранилища.НомерСтроки = &НомерСтроки
	|	И ПодписиХранилища.Ссылка.Наименование = &Наименование";
	
	Результат = СтрЗаменить(Результат, "&Отбор", Отбор);
	
	Возврат Результат;
	
КонецФункции

Функция Документы_КонтрактПодписиПоВыборке_Диадок(Выборка, ИмяХранилища)
	
	Результат = Контракт_Подпись();
	
	ЗаполнитьЗначенияСвойств(Результат, Выборка, "ДатаПодписи, ПодписьВерна");
	
	Если Настройки_ОбщиеНастройки().ХранитьДокументыНаДиске Тогда
		ДанныеПодписи			= Документы_ДвоичныеДанныеПодписи_ВнешнееХранение(Выборка.КодПакета, Выборка.КодДокумента, ИмяХранилища);
		Результат.ДанныеПодписи = ДанныеПодписи;
	ИначеЕсли ЗначениеЗаполнено(Выборка.ХранилищеПодписи) Тогда
		Результат.ДанныеПодписи	= Выборка.ХранилищеПодписи.Получить();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ДвоичныеДанныеПодписи_ВнешнееХранение(КодПакета, КодДокумента, ИмяХранилища)
	
	Результат = Неопределено;
	
	РасположениеФайлаПодписи	= ВнешнееХранение_ИмяФайлаНаДиске(КодПакета, КодДокумента, ИмяХранилища, Ложь, Истина);
	ФайлДанныхПодписи			= Новый Файл(РасположениеФайлаПодписи.ИмяФайла);
	
	Если ФайлДанныхПодписи.Существует() Тогда
		Результат = Новый ДвоичныеДанные(РасположениеФайлаПодписи.ИмяФайла);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции


Процедура Документы_ЗаписатьДанныеХранилища_Диадок(ДанныеХранения, ИмяХранилища, ДокументЭДО)
	
	СсылкаХраненияДанных = Справочники.Диадок_ХранениеДанных.НайтиПоНаименованию(ИмяХранилища, Истина, , ДокументЭДО);
	
	Если ЗначениеЗаполнено(СсылкаХраненияДанных) Тогда
		ОбъектХраненияДанных = СсылкаХраненияДанных.ПолучитьОбъект();
	Иначе
		
		Если  НЕ ЗначениеЗаполнено(ДанныеХранения.Данные)
			И НЕ ЗначениеЗаполнено(ДанныеХранения.ЗашифрованныеДанные)
			И НЕ ЗначениеЗаполнено(ДанныеХранения.Подпись.ДанныеПодписи) Тогда
			Возврат;
		КонецЕсли;
		
		ОбъектХраненияДанных = Справочники.Диадок_ХранениеДанных.СоздатьЭлемент();
		ОбъектХраненияДанных.Владелец		= ДокументЭДО;
		ОбъектХраненияДанных.ДатаСоздания	= ТекущаяДата();
		ОбъектХраненияДанных.Наименование	= ИмяХранилища;
		
	КонецЕсли;
	
	ОбъектХраненияДанных.ДатаСоздания	= ДанныеХранения.ДатаСоздания;
	ОбъектХраненияДанных.ИмяФайла		= ДанныеХранения.ИмяФайла;
	ОбъектХраненияДанных.Описание		= ДанныеХранения.Описание;
		
	МассивПодстрок = РазделитьСтрокуЛок(ДанныеХранения.ИмяФайла, ".");
	
	Если МассивПодстрок.Количество() > 1 Тогда
		ОбъектХраненияДанных.РасширениеФайла = МассивПодстрок[МассивПодстрок.ВГраница()];
	КонецЕсли;		
	
	ОбъектХраненияДанных.Записать();
	
	//данные
	Если ТипЗнч(ДанныеХранения.Данные) = Тип("Строка") Тогда
		Данные = Base64Значение(ДанныеХранения.Данные);
	Иначе
		Данные = ДанныеХранения.Данные;
	КонецЕсли;
	
	Если Данные <> Неопределено Тогда
		Если Настройки_ОбщиеНастройки().ХранитьДокументыНаДиске Тогда
			Документы_ЗаписатьДанныеХранилища_НаДиске(Данные, ДокументЭДО, ИмяХранилища);
		Иначе
			ОбъектХраненияДанных.ХранилищеДанных = Документы_ЗаписатьДанныеХранилища_ВСправочнике(Данные, ОбъектХраненияДанных.ХранилищеДанных);
		КонецЕсли;
	КонецЕсли;
	
	//зашифрованные данные
	Если ТипЗнч(ДанныеХранения.ЗашифрованныеДанные) = Тип("Строка") Тогда
		ЗашифрованныеДанные = Base64Значение(ДанныеХранения.ЗашифрованныеДанные);
	Иначе
		ЗашифрованныеДанные = ДанныеХранения.ЗашифрованныеДанные;
	КонецЕсли;
	
	Если ЗашифрованныеДанные <> Неопределено Тогда
		Если Настройки_ОбщиеНастройки().ХранитьДокументыНаДиске Тогда
			Документы_ЗаписатьДанныеХранилища_НаДиске(ЗашифрованныеДанные, ДокументЭДО, ИмяХранилища, Истина);
		Иначе
			ОбъектХраненияДанных.ХранилищеЗашифрованныхДанных = Документы_ЗаписатьДанныеХранилища_ВСправочнике(ЗашифрованныеДанные, ОбъектХраненияДанных.ХранилищеЗашифрованныхДанных);
		КонецЕсли;
	КонецЕсли;
	
	ОбъектХраненияДанных.ТребуетсяРасшифровка = ДанныеХранения.ТребуетсяРасшифровка;
	
	Если ЗначениеЗаполнено(ДанныеХранения.Подпись.ДанныеПодписи) Тогда
		Документы_ЗаписатьДанныеПодписи_Диадок(ДанныеХранения.Подпись, ИмяХранилища, ДокументЭДО, ОбъектХраненияДанных);
	Иначе
		ОбъектХраненияДанных.Записать();
	КонецЕсли;
	
КонецПроцедуры

Процедура Документы_ЗаписатьДанныеПодписи_Диадок(Контракт_ДанныеПодписи, ИмяХранилища, ДокументЭДО, ОбъектХраненияДанных  = Неопределено)
	
	Если ОбъектХраненияДанных = Неопределено Тогда
		
		СсылкаХраненияДанных = Справочники.Диадок_ХранениеДанных.НайтиПоНаименованию(ИмяХранилища, Истина, , ДокументЭДО);
		
		Если ЗначениеЗаполнено(СсылкаХраненияДанных) Тогда
			ОбъектХраненияДанных = СсылкаХраненияДанных.ПолучитьОбъект();
		Иначе
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	ДанныеПодписи = Контракт_ДанныеПодписи.ДанныеПодписи;
	
	НомерПодписи = 0;  // пока сохраняем только одну подпись
	СтрокаПодписи = ?(ОбъектХраненияДанных.Подписи.Количество() = 0, ОбъектХраненияДанных.Подписи.Добавить(), ОбъектХраненияДанных.Подписи[НомерПодписи]);
	
	Если Настройки_ОбщиеНастройки().ХранитьДокументыНаДиске Тогда
		Документы_ЗаписатьДанныеХранилища_НаДиске(ДанныеПодписи, ДокументЭДО, ИмяХранилища, , НомерПодписи);
	Иначе
		СтрокаПодписи.ХранилищеПодписи = Документы_ЗаписатьДанныеХранилища_ВСправочнике(ДанныеПодписи, СтрокаПодписи.ХранилищеПодписи);
	КонецЕсли;
	
	СтрокаПодписи.ДатаПодписи = Контракт_ДанныеПодписи.ДатаПодписи;
	
	СтрокаПодписи = ?(ОбъектХраненияДанных.Подписи.Количество() = 0, ОбъектХраненияДанных.Подписи.Добавить(), ОбъектХраненияДанных.Подписи[0]);
	СтрокаПодписи.ПодписьВерна = Контракт_ДанныеПодписи.ПодписьВерна;
	
	ОбъектХраненияДанных.Записать();
	
КонецПроцедуры	

Процедура Документы_ЗаписатьДанныеХранилища_НаДиске(ДвоичныеДанные, ДокументЭДО, ИмяХранилища, ЭтоЗашифрованныеДанные = Ложь, НомерПодписи = Неопределено)
	
	Если ЗначениеЗаполнено(ДвоичныеДанные) Тогда
		
		Идентификаторы = Новый Структура;
		Идентификаторы.Вставить("ДокументЭДО", ДокументЭДО);
		
		Выборка = Документы_ВыборкаДанныхХранилищаИзБазы(Идентификаторы, ИмяХранилища);
		
		Если Выборка.Следующий() Тогда
			
			РасположениеФайла = ВнешнееХранение_ИмяФайлаНаДиске(Выборка.КодПакета, Выборка.КодДокумента, ИмяХранилища, ЭтоЗашифрованныеДанные, НомерПодписи);
			
			СоздатьКаталог(РасположениеФайла.Каталог);
			
			Если ЗначениеЗаполнено(Выборка.BoxId) Тогда
				
				ИмяФайлаURL	= РасположениеФайла.Каталог + "\" + "web.url";
				ФайлURL		= Новый Файл(ИмяФайлаURL);
				
				Если Не ФайлURL.Существует() Тогда
					
					ТД = Новый ТекстовыйДокумент;
					
					СодержимоеЯрлыка = 
					"[InternetShortcut]
					|URL=" + СформироватьСсылкуВБраузере(Выборка.BoxId, Выборка.LetterId, Выборка.DocumentId);
					
					ТД.УстановитьТекст(СодержимоеЯрлыка);
					ТД.Записать(ИмяФайлаURL);
					
				КонецЕсли;
				
			КонецЕсли;
			
			ДвоичныеДанные.Записать(РасположениеФайла.ИмяФайла);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция Документы_ЗаписатьДанныеХранилища_ВСправочнике(ДвоичныеДанные, Ссылка_Диадок_Файлы = Неопределено) 
	
	Результат = Неопределено;
	
	Если Не ЗначениеЗаполнено(Ссылка_Диадок_Файлы) Тогда
		
		ОбъектСправочника = Справочники.Диадок_Файлы.СоздатьЭлемент();
		
	Иначе
		
		ОбъектСправочника = Ссылка_Диадок_Файлы.ПолучитьОбъект();
		СохраненныеДанные = ОбъектСправочника.Хранилище.Получить();
		
		Если ТипЗнч(ДвоичныеДанные) = Тип("ДвоичныеДанные")
			И ТипЗнч(СохраненныеДанные) = Тип("ДвоичныеДанные")
			И ДвоичныеДанные = СохраненныеДанные Тогда
			
			Результат = Ссылка_Диадок_Файлы;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если Результат = Неопределено Тогда
		
		ОбъектСправочника.Хранилище = Новый ХранилищеЗначения(ДвоичныеДанные, Новый СжатиеДанных(9));
		
		Если ДвоичныеДанные <> Неопределено Тогда
			ОбъектСправочника.Размер = ДвоичныеДанные.Размер();
		КонецЕсли;
		
		Если ОбъектСправочника.ЭтоНовый() Тогда
			ОбъектСправочника.УстановитьНовыйКод();
		КонецЕсли;
		
		ОбъектСправочника.Записать();
		
		Результат = ОбъектСправочника.Ссылка;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции


Функция Документы_ПометитьНедействительныеДанныеХранилища_Диадок(ДокументЭДО, ИмяХранилища)
	
	ХранениеДанныхСсылка = Справочники.Диадок_ХранениеДанных.НайтиПоНаименованию(ИмяХранилища, Истина, , ДокументЭДО);
	
	Если ЗначениеЗаполнено(ХранениеДанныхСсылка) Тогда
		
		ХранениеДанныхОбъект = ХранениеДанныхСсылка.ПолучитьОбъект();
		ХранениеДанныхОбъект.Наименование = "DEL " + ХранениеДанныхОбъект.Наименование;
		ХранениеДанныхОбъект.Записать();
		
	КонецЕсли;	
	
	Результат = ХранениеДанныхСсылка;
	
	Возврат Результат;
	
КонецФункции	

Функция Документы_ОписаниеДанныхХранилища_Диадок(Идентификаторы)
	
	Результат = Новый Структура;
	
	ДокументЭДО	= СвойствоСтруктуры(Идентификаторы, "ДокументЭДО",	Неопределено);
	BoxID		= СвойствоСтруктуры(Идентификаторы, "BoxID",		"");
	LetterID	= СвойствоСтруктуры(Идентификаторы, "LetterID",		"");
	DocumentID	= СвойствоСтруктуры(Идентификаторы, "DocumentID",	"");
		
	Если ЗначениеЗаполнено(ДокументЭДО) Тогда
		Отбор = "ХранениеДанных.Владелец = &Документ";
	Иначе
		Отбор = "ХранениеДанных.Владелец.BoxId = &BoxId И ХранениеДанных.Владелец.LetterId = &LetterId И ХранениеДанных.Владелец.DocumentId = &DocumentId";
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = Документы_ТекстЗапросаОписаниеДанныхХранилища_Диадок(Отбор);
	
	Запрос.УстановитьПараметр("Документ",	ДокументЭДО);
	Запрос.УстановитьПараметр("BoxID",		BoxID);
	Запрос.УстановитьПараметр("LetterId",	LetterId);
	Запрос.УстановитьПараметр("DocumentID",	DocumentID);
	
	ВыборкаХранилищ = Запрос.Выполнить().Выбрать();
	
	Пока ВыборкаХранилищ.Следующий() Цикл
		
		Если Лев(ВыборкаХранилищ.Наименование, 3) <> "DEL" Тогда
			
			Значение = Новый Структура;
			Значение.Вставить("Id", 				ВыборкаХранилищ.Описание);
			Значение.Вставить("ДатаСоздания", 		ВыборкаХранилищ.ДатаСоздания);
			Значение.Вставить("ПодписьЗагружена",	ВыборкаХранилищ.ПодписьЗагружена);
			Значение.Вставить("ДанныеЗагружены",	ВыборкаХранилищ.ДанныеЗагружены);
			
			Результат.Вставить(ВыборкаХранилищ.Наименование, Значение);
			
		КонецЕсли;
		
	КонецЦикла;	
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ТекстЗапросаОписаниеДанныхХранилища_Диадок(Отбор = "Истина")
	
	Результат = 
	"ВЫБРАТЬ
	|	ХранениеДанных.Наименование КАК Наименование,
	|	ХранениеДанных.Описание КАК Описание,
	|	ХранениеДанных.ДатаСоздания,
	|	ВЫБОР
	|		КОГДА ХранениеДанныхПодписи.ХранилищеПодписи ЕСТЬ NULL 
	|			ТОГДА ЛОЖЬ
	|		ИНАЧЕ ИСТИНА
	|	КОНЕЦ КАК ПодписьЗагружена,
	|	ВЫБОР
	|		КОГДА ХранениеДанных.ХранилищеДанных ЕСТЬ NULL 
	|			ТОГДА ЛОЖЬ
	|		ИНАЧЕ ИСТИНА
	|	КОНЕЦ КАК ДанныеЗагружены
	|ИЗ
	|	Справочник.Диадок_ХранениеДанных КАК ХранениеДанных
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ХранениеДанных.Подписи КАК ХранениеДанныхПодписи
	|		ПО (ХранениеДанныхПодписи.Ссылка = ХранениеДанных.Ссылка)
	|			И (ХранениеДанныхПодписи.НомерСтроки = 1)
	|ГДЕ
	|	&Отбор
	|	И НЕ ХранениеДанных.ПометкаУдаления";
	
	Результат = СтрЗаменить(Результат, "&Отбор", Отбор);
	
	Возврат Результат;
	
КонецФункции

//**********************************************
// ДОКУМЕНТЫ: ДАННЫЕ ХРАНИЛИЩА 1С-ЭДО

Функция Документы_ДанныеХранилища_1С(Идентификаторы, ИмяХранилища, НомерПодписи)

	Результат = Контракт_ДанныеХранения();

	Если ЗначениеЗаполнено(Идентификаторы.LetterID)
		И ЗначениеЗаполнено(Идентификаторы.DocumentID) Тогда
		
		Результат = Документы_ДанныхХранения_АПИ(Идентификаторы, ИмяХранилища);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ДанныеПодписи_1С(Идентификаторы, ИмяХранилища)

	Результат = Документы_ДанныеПодписи_АПИ(Идентификаторы, ИмяХранилища);
	
	Возврат Результат;
	
КонецФункции

//}		ДОКУМЕНТЫ


///////////////////////////////////////////////////////////////////////////////
//{		СПИСКИ ДОКУМЕНТОВ

Функция СписокДокументов_Подготовленные(Параметры) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = СписокДокументов_Подготовленные_Диадок(Параметры);
	Иначе
	    Результат = Новый_СписокДокументов();
	КонецЕсли;

	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_Отправленные(Параметры) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = СписокДокументов_Отправленные_Диадок(Параметры);
	Иначе
		Результат = СписокДокументов_Отправленные_1С(Параметры);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_Полученные(Параметры) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = СписокДокументов_Полученные_Диадок(Параметры);
	Иначе
		Результат = СписокДокументов_Полученные_1С(Параметры);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_Внутренние(Параметры) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = СписокДокументов_Внутренние_Диадок(Параметры);
	Иначе
		Результат = СписокДокументов_Внутренние_1С(Параметры);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_ДополненныеПараметрыНаОтправку(Параметры) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = СписокДокументов_ДополненныеПараметрыНаОтправку_Диадок(Параметры);
	Иначе
		Результат = СписокДокументов_ДополненныеПараметрыНаОтправку_1С(Параметры);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает текст запроса, формирующий временные таблицы для построения списка пакетов на отправку:
//  - ВТ_КэшКонтрагентовПоВидамПакетов
//  - ВТ_ОтправленныеДокументы
// 
// Возвращаемое значение:
//  Строка - текст запроса.
//
Функция СписокНаОтправку_ТекстЗапроса_ВременныеТаблицы() Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = СписокНаОтправку_ТекстЗапроса_ВременныеТаблицы_Диадок();
	Иначе
		Результат = СписокНаОтправку_ТекстЗапроса_ВременныеТаблицы_1С();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции


Функция СписокНаОтправку_ТекстЗапроса_ВременныеТаблицы_Диадок()
	
	ТекстЗапроса_РазделительЗапросов = ТекстЗапроса_РазделительЗапросов();
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		ТекстЗапроса_СопоставленныеКонтрагенты = СписокНаОтправку_ТекстЗапроса_СопоставленныеКонтрагенты_ДиадокРасширение();
	Иначе
		ТекстЗапроса_СопоставленныеКонтрагенты = СписокНаОтправку_ТекстЗапроса_СопоставленныеКонтрагенты_ДиадокСХ1();
	КонецЕсли;
	
	ТекстЗапроса_НастройкиВидовПакетов = СписокНаОтправку_ТекстЗапроса_НастройкиВидовПакетов_Диадок();
	ТекстЗапроса_ОтправленныеДокументы = СписокНаОтправку_ТекстЗапроса_ОтправленныеДокументы_Диадок();
	ТекстЗапроса_УничтожениеВТ = СписокНаОтправку_ТекстЗапроса_УничтожениеВТ_Диадок();
	
	ЧастиТекстаЗапроса = Новый Массив;
	
	ЧастиТекстаЗапроса.Добавить(ТекстЗапроса_СопоставленныеКонтрагенты);
	ЧастиТекстаЗапроса.Добавить(ТекстЗапроса_НастройкиВидовПакетов);
	ЧастиТекстаЗапроса.Добавить(ТекстЗапроса_ОтправленныеДокументы);
	ЧастиТекстаЗапроса.Добавить(ТекстЗапроса_УничтожениеВТ);
	
	Результат = СоединитьСтроку(ЧастиТекстаЗапроса, ТекстЗапроса_РазделительЗапросов);
				
	Возврат Результат;
	
КонецФункции

Функция СписокНаОтправку_ТекстЗапроса_СопоставленныеКонтрагенты_ДиадокРасширение()
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_НастройкиКонтрагентов.Организация КАК Организация,
	|	Диадок_НастройкиКонтрагентов.Контрагент КАК Контрагент
	|ПОМЕСТИТЬ ВТ_МоиКонтрагентыЭДО
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
	|ГДЕ
	|	Диадок_НастройкиКонтрагентов.ИмяСвойства = ""Статус""
	|	И Диадок_НастройкиКонтрагентов.Значение = ""IsMyCounteragent""
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация,
	|	Контрагент
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_МоиКонтрагенты.Организация КАК Организация,
	|	ВТ_МоиКонтрагенты.Контрагент КАК Контрагент,
	|	ВЫРАЗИТЬ(Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК Справочник.Организации) КАК СвязанныйСправочник
	|ПОМЕСТИТЬ ВТ_СопоставленныеОрганизации
	|ИЗ
	|	ВТ_МоиКонтрагентыЭДО КАК ВТ_МоиКонтрагенты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ПО ВТ_МоиКонтрагенты.Организация = Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ШаблонИмениРеквизита_СвязанныйСправочник)
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение <> НЕОПРЕДЕЛЕНО)
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация,
	|	Контрагент
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_МоиКонтрагенты.Организация КАК Организация,
	|	ВТ_МоиКонтрагенты.Контрагент КАК Контрагент,
	|	ВЫРАЗИТЬ(Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК Справочник.Контрагенты) КАК СвязанныйСправочник
	|ПОМЕСТИТЬ ВТ_СопоставленныеКонтрагенты
	|ИЗ
	|	ВТ_МоиКонтрагентыЭДО КАК ВТ_МоиКонтрагенты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ПО ВТ_МоиКонтрагенты.Контрагент = Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ШаблонИмениРеквизита_СвязанныйСправочник)
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение <> НЕОПРЕДЕЛЕНО)
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация,
	|	Контрагент
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_СопоставленныеОрганизации.СвязанныйСправочник КАК Организация1С,
	|	ВТ_СопоставленныеКонтрагенты.СвязанныйСправочник КАК Контрагент1С,
	|	ВТ_МоиКонтрагенты.Организация КАК Организация,
	|	ВТ_МоиКонтрагенты.Контрагент КАК Контрагент
	|ПОМЕСТИТЬ ВТ_КэшКонтрагентов
	|ИЗ
	|	ВТ_МоиКонтрагентыЭДО КАК ВТ_МоиКонтрагенты
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_СопоставленныеОрганизации КАК ВТ_СопоставленныеОрганизации
	|		ПО ВТ_МоиКонтрагенты.Организация = ВТ_СопоставленныеОрганизации.Организация
	|			И ВТ_МоиКонтрагенты.Контрагент = ВТ_СопоставленныеОрганизации.Контрагент
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_СопоставленныеКонтрагенты КАК ВТ_СопоставленныеКонтрагенты
	|		ПО ВТ_МоиКонтрагенты.Контрагент = ВТ_СопоставленныеКонтрагенты.Контрагент
	|			И ВТ_МоиКонтрагенты.Организация = ВТ_СопоставленныеКонтрагенты.Организация
	|ГДЕ
	|	НЕ ВТ_СопоставленныеОрганизации.СвязанныйСправочник ЕСТЬ NULL
	|	И НЕ ВТ_СопоставленныеКонтрагенты.СвязанныйСправочник ЕСТЬ NULL
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация1С,
	|	Контрагент1С";
	
	Возврат Результат;

КонецФункции

Функция СписокНаОтправку_ТекстЗапроса_СопоставленныеКонтрагенты_ДиадокСХ1()
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_НастройкиКонтрагентов.Организация КАК Организация,
	|	Диадок_НастройкиКонтрагентов.Контрагент КАК Контрагент
	|ПОМЕСТИТЬ ВТ_МоиКонтрагентыЭДО
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
	|ГДЕ
	|	Диадок_НастройкиКонтрагентов.ИмяСвойства = ""Статус""
	|	И Диадок_НастройкиКонтрагентов.Значение = ""IsMyCounteragent""
	|	И Диадок_НастройкиКонтрагентов.Организация.СвязанныйСправочник1 <> НЕОПРЕДЕЛЕНО
	|	И Диадок_НастройкиКонтрагентов.Контрагент.СвязанныйСправочник1 <> НЕОПРЕДЕЛЕНО
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Диадок_НастройкиКонтрагентов.Организация,
	|	Диадок_НастройкиКонтрагентов.Контрагент
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_МоиКонтрагенты.Организация КАК Организация,
	|	ВТ_МоиКонтрагенты.Контрагент КАК Контрагент,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник1 КАК СвязанныйСправочник
	|ПОМЕСТИТЬ ВТ_СопоставленныеОрганизации
	|ИЗ
	|	ВТ_МоиКонтрагентыЭДО КАК ВТ_МоиКонтрагенты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО ВТ_МоиКонтрагенты.Организация = Диадок_ЮрФизЛица.Ссылка
	|			И (Диадок_ЮрФизЛица.СвязанныйСправочник1 <> НЕОПРЕДЕЛЕНО)
	|
	|ОБЪЕДИНИТЬ
	|
	|ВЫБРАТЬ
	|	ВТ_МоиКонтрагенты.Организация,
	|	ВТ_МоиКонтрагенты.Контрагент,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник2
	|ИЗ
	|	ВТ_МоиКонтрагентыЭДО КАК ВТ_МоиКонтрагенты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО ВТ_МоиКонтрагенты.Организация = Диадок_ЮрФизЛица.Ссылка
	|			И (Диадок_ЮрФизЛица.СвязанныйСправочник2 <> НЕОПРЕДЕЛЕНО)
	|
	|ОБЪЕДИНИТЬ
	|
	|ВЫБРАТЬ
	|	ВТ_МоиКонтрагенты.Организация,
	|	ВТ_МоиКонтрагенты.Контрагент,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник3
	|ИЗ
	|	ВТ_МоиКонтрагентыЭДО КАК ВТ_МоиКонтрагенты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО ВТ_МоиКонтрагенты.Организация = Диадок_ЮрФизЛица.Ссылка
	|			И (Диадок_ЮрФизЛица.СвязанныйСправочник3 <> НЕОПРЕДЕЛЕНО)
	|
	|ОБЪЕДИНИТЬ
	|
	|ВЫБРАТЬ
	|	ВТ_МоиКонтрагенты.Организация,
	|	ВТ_МоиКонтрагенты.Контрагент,
	|	ВЫРАЗИТЬ(Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК Справочник.Организации)
	|ИЗ
	|	ВТ_МоиКонтрагентыЭДО КАК ВТ_МоиКонтрагенты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ПО ВТ_МоиКонтрагенты.Организация = Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ШаблонИмениРеквизита_СвязанныйСправочник)
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение <> НЕОПРЕДЕЛЕНО)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_МоиКонтрагенты.Организация КАК Организация,
	|	ВТ_МоиКонтрагенты.Контрагент КАК Контрагент,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник1 КАК СвязанныйСправочник
	|ПОМЕСТИТЬ ВТ_СопоставленныеКонтрагенты
	|ИЗ
	|	ВТ_МоиКонтрагентыЭДО КАК ВТ_МоиКонтрагенты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО ВТ_МоиКонтрагенты.Контрагент = Диадок_ЮрФизЛица.Ссылка
	|			И (Диадок_ЮрФизЛица.СвязанныйСправочник1 <> НЕОПРЕДЕЛЕНО)
	|
	|ОБЪЕДИНИТЬ
	|
	|ВЫБРАТЬ
	|	ВТ_МоиКонтрагенты.Организация,
	|	ВТ_МоиКонтрагенты.Контрагент,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник2
	|ИЗ
	|	ВТ_МоиКонтрагентыЭДО КАК ВТ_МоиКонтрагенты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО ВТ_МоиКонтрагенты.Контрагент = Диадок_ЮрФизЛица.Ссылка
	|			И (Диадок_ЮрФизЛица.СвязанныйСправочник2 <> НЕОПРЕДЕЛЕНО)
	|
	|ОБЪЕДИНИТЬ
	|
	|ВЫБРАТЬ
	|	ВТ_МоиКонтрагенты.Организация,
	|	ВТ_МоиКонтрагенты.Контрагент,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник3
	|ИЗ
	|	ВТ_МоиКонтрагентыЭДО КАК ВТ_МоиКонтрагенты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО ВТ_МоиКонтрагенты.Контрагент = Диадок_ЮрФизЛица.Ссылка
	|			И (Диадок_ЮрФизЛица.СвязанныйСправочник3 <> НЕОПРЕДЕЛЕНО)
	|
	|ОБЪЕДИНИТЬ
	|
	|ВЫБРАТЬ
	|	ВТ_МоиКонтрагенты.Организация,
	|	ВТ_МоиКонтрагенты.Контрагент,
	|	ВЫРАЗИТЬ(Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК Справочник.Контрагенты)
	|ИЗ
	|	ВТ_МоиКонтрагентыЭДО КАК ВТ_МоиКонтрагенты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ПО ВТ_МоиКонтрагенты.Контрагент = Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ШаблонИмениРеквизита_СвязанныйСправочник)
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение <> НЕОПРЕДЕЛЕНО)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_СопоставленныеОрганизации.СвязанныйСправочник КАК Организация1С,
	|	ВТ_СопоставленныеКонтрагенты.СвязанныйСправочник КАК Контрагент1С,
	|	ВТ_МоиКонтрагенты.Организация КАК Организация,
	|	ВТ_МоиКонтрагенты.Контрагент КАК Контрагент
	|ПОМЕСТИТЬ ВТ_КэшКонтрагентов
	|ИЗ
	|	ВТ_МоиКонтрагентыЭДО КАК ВТ_МоиКонтрагенты
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_СопоставленныеОрганизации КАК ВТ_СопоставленныеОрганизации
	|		ПО ВТ_МоиКонтрагенты.Организация = ВТ_СопоставленныеОрганизации.Организация
	|			И ВТ_МоиКонтрагенты.Контрагент = ВТ_СопоставленныеОрганизации.Контрагент
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_СопоставленныеКонтрагенты КАК ВТ_СопоставленныеКонтрагенты
	|		ПО ВТ_МоиКонтрагенты.Контрагент = ВТ_СопоставленныеКонтрагенты.Контрагент
	|			И ВТ_МоиКонтрагенты.Организация = ВТ_СопоставленныеКонтрагенты.Организация
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация1С,
	|	Контрагент1С";

    Возврат Результат;

КонецФункции

Функция СписокНаОтправку_ТекстЗапроса_ОтправленныеДокументы_Диадок()
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_ПакетыДокументов.Ссылка КАК Ссылка,
	|	Диадок_ПакетыДокументов.Документ КАК Документ,
	|	Диадок_ПакетыДокументов.ВидПакета КАК ВидПакета,
	|	ВТ_КэшВидовПакетов.ВидПакетаID КАК ВидПакетаID,
	|	ВТ_КэшВидовПакетов.КатегорияПакета КАК КатегорияПакета
	|ПОМЕСТИТЬ ВТ_ОтправленныеДокументы
	|ИЗ
	|	Справочник.Диадок_ПакетыДокументов КАК Диадок_ПакетыДокументов
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_КэшВидовПакетовИзСправочника КАК ВТ_КэшВидовПакетов
	|		ПО Диадок_ПакетыДокументов.ВидПакета = ВТ_КэшВидовПакетов.ВидПакета
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Документ,
	|	КатегорияПакета,
	|	ВидПакета";
	
	Возврат Результат;
	
КонецФункции

Функция СписокНаОтправку_ТекстЗапроса_НастройкиВидовПакетов_Диадок()
	
	Результат =
	"ВЫБРАТЬ
	|	ВидыПакетов.КатегорияПакета КАК КатегорияПакета,
	|	ВидыПакетов.ID КАК ВидПакетаID,
	|	ВидыПакетов.ПоУмолчанию КАК ПоУмолчанию
	|ПОМЕСТИТЬ ВТ_КэшВидовПакетов
	|ИЗ
	|	&ТаблицаВидовПакетов КАК ВидыПакетов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_КэшВидовПакетов.КатегорияПакета КАК КатегорияПакета,
	|	ВТ_КэшВидовПакетов.ВидПакетаID КАК ВидПакетаID
	|ПОМЕСТИТЬ ВТ_КэшВидовПакетовПоУмолчанию
	|ИЗ
	|	ВТ_КэшВидовПакетов КАК ВТ_КэшВидовПакетов
	|ГДЕ
	|	ВТ_КэшВидовПакетов.ПоУмолчанию
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка КАК ВидПакета,
	|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Значение КАК ВидПакетаID,
	|	ВТ_КэшВидовПакетов.КатегорияПакета КАК КатегорияПакета
	|ПОМЕСТИТЬ ВТ_КэшВидовПакетовИзСправочника
	|ИЗ
	|	Справочник.Диадок_ДополнительныеСправочники.ДополнительныеРеквизиты КАК Диадок_ДополнительныеСправочникиДополнительныеРеквизиты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_КэшВидовПакетов КАК ВТ_КэшВидовПакетов
	|		ПО Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Значение = ВТ_КэшВидовПакетов.ВидПакетаID
	|ГДЕ
	|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка.ИмяСправочника = ""ВидыПакетов""
	|	И Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.ИмяРеквизита = ""ID""
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_КэшКонтрагентов.Организация1С КАК Организация1С,
	|	ВТ_КэшКонтрагентов.Контрагент1С КАК Контрагент1С,
	|	ВТ_КэшКонтрагентов.Организация КАК Организация,
	|	ВТ_КэшКонтрагентов.Контрагент КАК Контрагент,
	|	ВТ_КэшВидовПакетовПоУмолчанию.КатегорияПакета КАК КатегорияПакета,
	|	ВТ_КэшВидовПакетовПоУмолчанию.ВидПакетаID КАК ПоУмолчаниюВидПакетаID
	|ПОМЕСТИТЬ ВТ_КэшКонтрагентовПоКатегориям
	|ИЗ
	|	ВТ_КэшКонтрагентов КАК ВТ_КэшКонтрагентов,
	|	ВТ_КэшВидовПакетовПоУмолчанию КАК ВТ_КэшВидовПакетовПоУмолчанию
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация1С,
	|	Контрагент1С,
	|	КатегорияПакета
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	Диадок_ЮрФизЛица_ДопРеквизиты.Ссылка КАК Организация,
	|	ВЫРАЗИТЬ(Диадок_ЮрФизЛица_ДопРеквизиты.Значение КАК БУЛЕВО) КАК ОтправлятьНепроведенные
	|ПОМЕСТИТЬ ВТ_КэшНастроекОтправкиНепроведенных
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛица_ДопРеквизиты
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_СопоставленныеОрганизации КАК ВТ_СопоставленныеОрганизации
	|		ПО (Диадок_ЮрФизЛица_ДопРеквизиты.Ссылка.ИмяСправочника = ""Организации"")
	|			И Диадок_ЮрФизЛица_ДопРеквизиты.Ссылка = ВТ_СопоставленныеОрганизации.Организация
	|ГДЕ
	|	Диадок_ЮрФизЛица_ДопРеквизиты.ИмяРеквизита = ""ОтправлятьНепроведенныеДокументы""
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Диадок_НастройкиКонтрагентов.Организация КАК Организация,
	|	Диадок_НастройкиКонтрагентов.Контрагент КАК Контрагент,
	|	Диадок_НастройкиКонтрагентов.ИмяСвойства КАК КатегорияПакета,
	|	Диадок_НастройкиКонтрагентов.Значение КАК ВидПакета,
	|	ЕСТЬNULL(ВТ_КэшВидовПакетовИзСправочника.ВидПакетаID, НЕОПРЕДЕЛЕНО) КАК ВидПакетаID
	|ПОМЕСТИТЬ ВТ_КэшНастроекВидовПакетов
	|ИЗ
	|	ВТ_КэшВидовПакетовПоУмолчанию КАК ВТ_КэшВидовПакетовПоУмолчанию
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
	|		ПО ВТ_КэшВидовПакетовПоУмолчанию.КатегорияПакета = Диадок_НастройкиКонтрагентов.ИмяСвойства
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КэшВидовПакетовИзСправочника КАК ВТ_КэшВидовПакетовИзСправочника
	|		ПО (Диадок_НастройкиКонтрагентов.Значение = ВТ_КэшВидовПакетовИзСправочника.ВидПакета)
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация,
	|	Контрагент
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_КэшНастроекВидовПакетов.Организация КАК Организация,
	|	ВТ_КэшНастроекВидовПакетов.Контрагент КАК Контрагент,
	|	ВТ_КэшНастроекВидовПакетов.КатегорияПакета КАК КатегорияПакета,
	|	ВТ_КэшНастроекВидовПакетов.ВидПакетаID КАК ВидПакетаID
	|ПОМЕСТИТЬ ВТ_КэшНастроекВидовПакетов_Приоритет1
	|ИЗ
	|	ВТ_КэшНастроекВидовПакетов КАК ВТ_КэшНастроекВидовПакетов
	|ГДЕ
	|	ВТ_КэшНастроекВидовПакетов.Организация <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И ВТ_КэшНастроекВидовПакетов.Контрагент <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация,
	|	Контрагент,
	|	КатегорияПакета
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_КэшНастроекВидовПакетов.Контрагент КАК Контрагент,
	|	ВТ_КэшНастроекВидовПакетов.КатегорияПакета КАК КатегорияПакета,
	|	ВТ_КэшНастроекВидовПакетов.ВидПакетаID КАК ВидПакетаID
	|ПОМЕСТИТЬ ВТ_КэшНастроекВидовПакетов_Приоритет2
	|ИЗ
	|	ВТ_КэшНастроекВидовПакетов КАК ВТ_КэшНастроекВидовПакетов
	|ГДЕ
	|	ВТ_КэшНастроекВидовПакетов.Организация = ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И ВТ_КэшНастроекВидовПакетов.Контрагент <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Контрагент,
	|	КатегорияПакета
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_КэшНастроекВидовПакетов.Организация КАК Организация,
	|	ВТ_КэшНастроекВидовПакетов.КатегорияПакета КАК КатегорияПакета,
	|	ВТ_КэшНастроекВидовПакетов.ВидПакетаID КАК ВидПакетаID
	|ПОМЕСТИТЬ ВТ_КэшНастроекВидовПакетов_Приоритет3
	|ИЗ
	|	ВТ_КэшНастроекВидовПакетов КАК ВТ_КэшНастроекВидовПакетов
	|ГДЕ
	|	ВТ_КэшНастроекВидовПакетов.Организация <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И ВТ_КэшНастроекВидовПакетов.Контрагент = ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация,
	|	КатегорияПакета
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_КэшКонтрагентовПоКатегориям.Организация1С КАК Организация1С,
	|	ВТ_КэшКонтрагентовПоКатегориям.Контрагент1С КАК Контрагент1С,
	|	ЕСТЬNULL(ЕСТЬNULL(ЕСТЬNULL(ВТ_КэшНастроекВидовПакетов_Приоритет1.ВидПакетаID, ВТ_КэшНастроекВидовПакетов_Приоритет2.ВидПакетаID), ВТ_КэшНастроекВидовПакетов_Приоритет3.ВидПакетаID), ВТ_КэшКонтрагентовПоКатегориям.ПоУмолчаниюВидПакетаID) КАК ВидПакетаID,
	|	ВТ_КэшКонтрагентовПоКатегориям.Организация КАК Организация,
	|	ВТ_КэшКонтрагентовПоКатегориям.Контрагент КАК Контрагент,
	|	ВТ_КэшКонтрагентовПоКатегориям.КатегорияПакета КАК КатегорияПакета,
	|	ВТ_КэшКонтрагентовПоКатегориям.ПоУмолчаниюВидПакетаID КАК ПоУмолчаниюВидПакетаID,
	|	ЕСТЬNULL(ВТ_КэшНастроекОтправкиНепроведенных.ОтправлятьНепроведенные, ЛОЖЬ) КАК ОтправлятьНепроведенные
	|ПОМЕСТИТЬ ВТ_КэшКонтрагентовПоВидамПакетов
	|ИЗ
	|	ВТ_КэшКонтрагентовПоКатегориям КАК ВТ_КэшКонтрагентовПоКатегориям
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КэшНастроекВидовПакетов_Приоритет1 КАК ВТ_КэшНастроекВидовПакетов_Приоритет1
	|		ПО ВТ_КэшКонтрагентовПоКатегориям.Организация = ВТ_КэшНастроекВидовПакетов_Приоритет1.Организация
	|			И ВТ_КэшКонтрагентовПоКатегориям.Контрагент = ВТ_КэшНастроекВидовПакетов_Приоритет1.Контрагент
	|			И ВТ_КэшКонтрагентовПоКатегориям.КатегорияПакета = ВТ_КэшНастроекВидовПакетов_Приоритет1.КатегорияПакета
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КэшНастроекВидовПакетов_Приоритет2 КАК ВТ_КэшНастроекВидовПакетов_Приоритет2
	|		ПО ВТ_КэшКонтрагентовПоКатегориям.Контрагент = ВТ_КэшНастроекВидовПакетов_Приоритет2.Контрагент
	|			И ВТ_КэшКонтрагентовПоКатегориям.КатегорияПакета = ВТ_КэшНастроекВидовПакетов_Приоритет2.КатегорияПакета
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КэшНастроекВидовПакетов_Приоритет3 КАК ВТ_КэшНастроекВидовПакетов_Приоритет3
	|		ПО ВТ_КэшКонтрагентовПоКатегориям.Организация = ВТ_КэшНастроекВидовПакетов_Приоритет3.Организация
	|			И ВТ_КэшКонтрагентовПоКатегориям.КатегорияПакета = ВТ_КэшНастроекВидовПакетов_Приоритет3.КатегорияПакета
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КэшНастроекОтправкиНепроведенных КАК ВТ_КэшНастроекОтправкиНепроведенных
	|		ПО ВТ_КэшКонтрагентовПоКатегориям.Организация = ВТ_КэшНастроекОтправкиНепроведенных.Организация
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация1С,
	|	Контрагент1С
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВидыДокументов.ВидДокументаID		КАК ВидДокументаID,
	|	ВидыДокументов.СвойствоТипОснования КАК СвойствоТипОснования
	|ПОМЕСТИТЬ ВТ_КэшВидовДокументов
	|ИЗ
	|	&ТаблицаВидовДокументов КАК ВидыДокументов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_КэшКонтрагентов.Организация1С,
	|	ВТ_КэшКонтрагентов.Контрагент1С, 
	|	ВТ_КэшКонтрагентов.Организация, 
	|	ВТ_КэшКонтрагентов.Контрагент,
	|	ВТ_ВидыДокументов.ВидДокументаID КАК ВидДокументаID,
	|	ВТ_ВидыДокументов.СвойствоТипОснования КАК СвойствоТипОснования
	|ПОМЕСТИТЬ ВТ_КэшКонтрагентовПоТипуОснования
	|ИЗ
	|	ВТ_КэшКонтрагентов КАК ВТ_КэшКонтрагентов,
	|	ВТ_КэшВидовДокументов КАК ВТ_ВидыДокументов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Диадок_НастройкиКонтрагентов.Организация	КАК Организация,
	|	Диадок_НастройкиКонтрагентов.Контрагент		КАК Контрагент,
	|	Диадок_НастройкиКонтрагентов.Значение		КАК ФормироватьНаОсновании,
	|	ВТ_КэшВидовДокументов.ВидДокументаID		КАК ВидДокументаID
	|ПОМЕСТИТЬ ВТ_ТипОснования_Приоритет1
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_КэшВидовДокументов КАК ВТ_КэшВидовДокументов
	|		ПО Диадок_НастройкиКонтрагентов.ИмяСвойства = ВТ_КэшВидовДокументов.СвойствоТипОснования
	|ГДЕ
	|	Диадок_НастройкиКонтрагентов.Организация <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И Диадок_НастройкиКонтрагентов.Контрагент <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация,
	|	Контрагент
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Диадок_НастройкиКонтрагентов.Организация	КАК Организация,
	|	Диадок_НастройкиКонтрагентов.Контрагент		КАК Контрагент,
	|	Диадок_НастройкиКонтрагентов.Значение		КАК ФормироватьНаОсновании,
	|	ВТ_КэшВидовДокументов.ВидДокументаID		КАК ВидДокументаID
	|ПОМЕСТИТЬ ВТ_ТипОснования_Приоритет2
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_КэшВидовДокументов КАК ВТ_КэшВидовДокументов
	|		ПО Диадок_НастройкиКонтрагентов.ИмяСвойства = ВТ_КэшВидовДокументов.СвойствоТипОснования
	|ГДЕ
	|	Диадок_НастройкиКонтрагентов.Организация <> ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|	И Диадок_НастройкиКонтрагентов.Контрагент = ЗНАЧЕНИЕ(Справочник.Диадок_ЮрФизЛица.ПустаяСсылка)
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация,
	|	Контрагент
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ВТ_КэшКонтрагентовПоТипуОснования.Организация1С	КАК Организация1С,
	|	ВТ_КэшКонтрагентовПоТипуОснования.Контрагент1С КАК Контрагент1С,
	|	ВТ_КэшКонтрагентовПоТипуОснования.ВидДокументаID КАК ВидДокументаID,
	|	ЕСТЬNULL(ЕСТЬNULL(ВТ_ТипОснования_Приоритет1.ФормироватьНаОсновании, ВТ_ТипОснования_Приоритет2.ФормироватьНаОсновании), ""СчетНаОплату"") КАК ФормироватьНаОсновании
	|ПОМЕСТИТЬ ВТ_КэшКонтрагентовПоВидамДокументов
	|ИЗ
	|	ВТ_КэшКонтрагентовПоТипуОснования КАК ВТ_КэшКонтрагентовПоТипуОснования
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ТипОснования_Приоритет1 КАК ВТ_ТипОснования_Приоритет1
	|		ПО ВТ_КэшКонтрагентовПоТипуОснования.Организация = ВТ_ТипОснования_Приоритет1.Организация
	|			И ВТ_КэшКонтрагентовПоТипуОснования.Контрагент = ВТ_ТипОснования_Приоритет1.Контрагент
	|			И ВТ_КэшКонтрагентовПоТипуОснования.ВидДокументаID = ВТ_ТипОснования_Приоритет1.ВидДокументаID
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ТипОснования_Приоритет2 КАК ВТ_ТипОснования_Приоритет2
	|		ПО ВТ_КэшКонтрагентовПоТипуОснования.Организация = ВТ_ТипОснования_Приоритет2.Организация
	|			И ВТ_КэшКонтрагентовПоТипуОснования.ВидДокументаID = ВТ_ТипОснования_Приоритет2.ВидДокументаID
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Организация1С,
	|	Контрагент1С";
	
	Возврат Результат;
	
КонецФункции

Функция СписокНаОтправку_ТекстЗапроса_УничтожениеВТ_Диадок()
	
	Результат =
	"УНИЧТОЖИТЬ ВТ_КэшВидовПакетов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_КэшВидовПакетовПоУмолчанию
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_КэшВидовПакетовИзСправочника
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_МоиКонтрагентыЭДО
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_СопоставленныеОрганизации
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_СопоставленныеКонтрагенты
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_КэшКонтрагентов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_КэшКонтрагентовПоКатегориям
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_КэшНастроекОтправкиНепроведенных
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_КэшНастроекВидовПакетов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_КэшНастроекВидовПакетов_Приоритет1
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_КэшНастроекВидовПакетов_Приоритет2
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_КэшНастроекВидовПакетов_Приоритет3
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_КэшВидовДокументов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_КэшКонтрагентовПоТипуОснования
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_ТипОснования_Приоритет1
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_ТипОснования_Приоритет2";
	
	Возврат Результат;
	
КонецФункции


Функция СписокНаОтправку_ТекстЗапроса_ВременныеТаблицы_1С()
	
	Результат =
		"ВЫБРАТЬ
		|	ВидыПакетов.КатегорияПакета КАК КатегорияПакета,
		|	ВидыПакетов.КатегорияПакетаСсылка КАК КатегорияПакетаСсылка,
		|	ВидыПакетов.ID КАК ВидПакетаID,
		|	ВидыПакетов.ПоУмолчанию КАК ПоУмолчанию
		|ПОМЕСТИТЬ ВТ_КэшВидовПакетов
		|ИЗ
		|	&ТаблицаВидовПакетов КАК ВидыПакетов
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТ_КэшВидовПакетов.КатегорияПакета КАК КатегорияПакета,
		|	ВТ_КэшВидовПакетов.КатегорияПакетаСсылка КАК КатегорияПакетаСсылка,
		|	ВТ_КэшВидовПакетов.ВидПакетаID КАК ВидПакетаID
		|ПОМЕСТИТЬ ВТ_КэшВидовПакетовПоУмолчанию
		|ИЗ
		|	ВТ_КэшВидовПакетов КАК ВТ_КэшВидовПакетов
		|ГДЕ
		|	ВТ_КэшВидовПакетов.ПоУмолчанию
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ТаблицаКонтрагентов.Организация1С КАК Организация1С,
		|	ТаблицаКонтрагентов.Контрагент1С КАК Контрагент1С
		|ПОМЕСТИТЬ ВТ_КэшКонтрагентов
		|ИЗ
		|	&ТаблицаКонтрагентов КАК ТаблицаКонтрагентов
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Организация1С,
		|	Контрагент1С
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТ_КэшКонтрагентов.Организация1С КАК Организация1С,
		|	ВТ_КэшКонтрагентов.Контрагент1С КАК Контрагент1С,
		|	ВТ_КэшВидовПакетовПоУмолчанию.КатегорияПакета КАК КатегорияПакета,
		|	ВТ_КэшВидовПакетовПоУмолчанию.ВидПакетаID КАК ПоУмолчаниюВидПакетаID
		|ПОМЕСТИТЬ ВТ_КэшКонтрагентовПоКатегориям
		|ИЗ
		|	ВТ_КэшКонтрагентов КАК ВТ_КэшКонтрагентов,
		|	ВТ_КэшВидовПакетовПоУмолчанию КАК ВТ_КэшВидовПакетовПоУмолчанию
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Организация1С,
		|	Контрагент1С,
		|	КатегорияПакета
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ЗначенияСвойствОбъектов.Объект КАК Организация1С,
		|	ВЫБОР
		|		КОГДА ТИПЗНАЧЕНИЯ(ЗначенияСвойствОбъектов.Значение) = ТИП(СТРОКА)
		|			ТОГДА ВЫБОР
		|					КОГДА ЗначенияСвойствОбъектов.Значение = ""Да""
		|						ТОГДА ИСТИНА
		|					ИНАЧЕ ЛОЖЬ
		|				КОНЕЦ
		|		ИНАЧЕ ВЫРАЗИТЬ(ЗначенияСвойствОбъектов.Значение КАК БУЛЕВО)
		|	КОНЕЦ КАК ОтправлятьНепроведенные
		|ПОМЕСТИТЬ ВТ_КэшНастроекОтправкиНепроведенных
		|ИЗ
		|	РегистрСведений.ДополнительныеСведения КАК ЗначенияСвойствОбъектов
		|ГДЕ
		|	ТИПЗНАЧЕНИЯ(ЗначенияСвойствОбъектов.Объект) = ТИП(Справочник.Организации)
		|	И ЗначенияСвойствОбъектов.Свойство = &Свойство_ОтправлятьНепроведенныеДокументы
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ЗначенияСвойствОбъектов.Объект КАК ЮрЛицо,
		|	ЗначенияСвойствОбъектов.Значение КАК ВидПакетаID,
		|	ВТ_КэшВидовПакетовПоУмолчанию.КатегорияПакета КАК КатегорияПакета
		|ПОМЕСТИТЬ ВТ_КэшНастроекВидовПакетов
		|ИЗ
		|	ВТ_КэшВидовПакетовПоУмолчанию КАК ВТ_КэшВидовПакетовПоУмолчанию
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ЗначенияСвойствОбъектов
		|		ПО ВТ_КэшВидовПакетовПоУмолчанию.КатегорияПакетаСсылка = ЗначенияСвойствОбъектов.Свойство
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТ_КэшНастроекВидовПакетов.ЮрЛицо КАК Контрагент1С,
		|	ВТ_КэшНастроекВидовПакетов.КатегорияПакета КАК КатегорияПакета,
		|	ВТ_КэшНастроекВидовПакетов.ВидПакетаID КАК ВидПакетаID
		|ПОМЕСТИТЬ ВТ_КэшНастроекВидовПакетов_Приоритет1
		|ИЗ
		|	ВТ_КэшНастроекВидовПакетов КАК ВТ_КэшНастроекВидовПакетов
		|ГДЕ
		|	ВТ_КэшНастроекВидовПакетов.ЮрЛицо ССЫЛКА Справочник.Контрагенты
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Контрагент1С,
		|	КатегорияПакета
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТ_КэшНастроекВидовПакетов.ЮрЛицо КАК Организация1С,
		|	ВТ_КэшНастроекВидовПакетов.КатегорияПакета КАК КатегорияПакета,
		|	ВТ_КэшНастроекВидовПакетов.ВидПакетаID КАК ВидПакетаID
		|ПОМЕСТИТЬ ВТ_КэшНастроекВидовПакетов_Приоритет2
		|ИЗ
		|	ВТ_КэшНастроекВидовПакетов КАК ВТ_КэшНастроекВидовПакетов
		|ГДЕ
		|	ВТ_КэшНастроекВидовПакетов.ЮрЛицо ССЫЛКА Справочник.Организации
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Организация1С,
		|	КатегорияПакета
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВидыДокументов.ВидДокументаID		КАК ВидДокументаID,
		|	ВидыДокументов.СвойствоТипОснования	КАК СвойствоТипОснования
		|ПОМЕСТИТЬ ВТ_КэшВидовДокументов
		|ИЗ
		|	&ТаблицаВидовДокументов КАК ВидыДокументов
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТ_КэшКонтрагентов.Организация1С КАК Организация1С,
		|	ВТ_КэшКонтрагентов.Контрагент1С КАК Контрагент1С, 
		|	ВТ_ВидыДокументов.ВидДокументаID КАК ВидДокументаID,
		|	ВТ_ВидыДокументов.СвойствоТипОснования КАК СвойствоТипОснования
		|ПОМЕСТИТЬ ВТ_КэшКонтрагентовПоТипуОснования
		|ИЗ
		|	ВТ_КэшКонтрагентов КАК ВТ_КэшКонтрагентов,
		|	ВТ_КэшВидовДокументов КАК ВТ_ВидыДокументов
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ЗначенияСвойствОбъектов.Объект			КАК Контрагент1С,
		|	ЗначенияСвойствОбъектов.Значение		КАК ФормироватьНаОсновании,
		|	ВТ_КэшВидовДокументов.ВидДокументаID	КАК ВидДокументаID
		|ПОМЕСТИТЬ ВТ_ТипОснования_Приоритет1
		|ИЗ
		|	РегистрСведений.ДополнительныеСведения КАК ЗначенияСвойствОбъектов
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_КэшВидовДокументов КАК ВТ_КэшВидовДокументов
		|		ПО ЗначенияСвойствОбъектов.Свойство = ВТ_КэшВидовДокументов.СвойствоТипОснования
		|ГДЕ
		|	ТИПЗНАЧЕНИЯ(ЗначенияСвойствОбъектов.Объект) = ТИП(Справочник.Контрагенты)
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Контрагент1С
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ЗначенияСвойствОбъектов.Объект 			КАК Организация1С,
		|	ЗначенияСвойствОбъектов.Значение 		КАК ФормироватьНаОсновании,
		|	ВТ_КэшВидовДокументов.ВидДокументаID	КАК ВидДокументаID
		|ПОМЕСТИТЬ ВТ_ТипОснования_Приоритет2
		|ИЗ
		|	РегистрСведений.ДополнительныеСведения КАК ЗначенияСвойствОбъектов
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_КэшВидовДокументов КАК ВТ_КэшВидовДокументов
		|		ПО ЗначенияСвойствОбъектов.Свойство = ВТ_КэшВидовДокументов.СвойствоТипОснования
		|ГДЕ
		|	ТИПЗНАЧЕНИЯ(ЗначенияСвойствОбъектов.Объект) = ТИП(Справочник.Организации)
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Организация1С
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	ВТ_КэшКонтрагентовПоТипуОснования.Организация1С КАК Организация1С,
		|	ВТ_КэшКонтрагентовПоТипуОснования.Контрагент1С КАК Контрагент1С,
		|	ВТ_КэшКонтрагентовПоТипуОснования.ВидДокументаID КАК ВидДокументаID,
		|	ЕСТЬNULL(ЕСТЬNULL(ВТ_ТипОснования_Приоритет1.ФормироватьНаОсновании, ВТ_ТипОснования_Приоритет2.ФормироватьНаОсновании), ""СчетНаОплату"") КАК ФормироватьНаОсновании
		|ПОМЕСТИТЬ ВТ_КэшКонтрагентовПоВидамДокументов
		|ИЗ
		|	ВТ_КэшКонтрагентовПоТипуОснования КАК ВТ_КэшКонтрагентовПоТипуОснования
		|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ТипОснования_Приоритет1 КАК ВТ_ТипОснования_Приоритет1
		|		ПО ВТ_КэшКонтрагентовПоТипуОснования.Контрагент1С = ВТ_ТипОснования_Приоритет1.Контрагент1С
		|			И ВТ_КэшКонтрагентовПоТипуОснования.ВидДокументаID = ВТ_ТипОснования_Приоритет1.ВидДокументаID
		|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ТипОснования_Приоритет2 КАК ВТ_ТипОснования_Приоритет2
		|		ПО ВТ_КэшКонтрагентовПоТипуОснования.Организация1С = ВТ_ТипОснования_Приоритет2.Организация1С
		|			И ВТ_КэшКонтрагентовПоТипуОснования.ВидДокументаID = ВТ_ТипОснования_Приоритет2.ВидДокументаID
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Организация1С,
		|	Контрагент1С
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТ_КэшКонтрагентовПоКатегориям.Организация1С КАК Организация1С,
		|	ВТ_КэшКонтрагентовПоКатегориям.Контрагент1С КАК Контрагент1С,
		|	ЕСТЬNULL(ЕСТЬNULL(ВТ_КэшНастроекВидовПакетов_Приоритет1.ВидПакетаID, ВТ_КэшНастроекВидовПакетов_Приоритет2.ВидПакетаID), ВТ_КэшКонтрагентовПоКатегориям.ПоУмолчаниюВидПакетаID) КАК ВидПакетаID,
		|	ВТ_КэшКонтрагентовПоКатегориям.КатегорияПакета КАК КатегорияПакета,
		|	ВТ_КэшКонтрагентовПоКатегориям.ПоУмолчаниюВидПакетаID КАК ПоУмолчаниюВидПакетаID,
		|	ЕСТЬNULL(ВТ_КэшНастроекОтправкиНепроведенных.ОтправлятьНепроведенные, ЛОЖЬ) КАК ОтправлятьНепроведенные
		|ПОМЕСТИТЬ ВТ_КэшКонтрагентовПоВидамПакетов
		|ИЗ
		|	ВТ_КэшКонтрагентовПоКатегориям КАК ВТ_КэшКонтрагентовПоКатегориям
		|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КэшНастроекВидовПакетов_Приоритет1 КАК ВТ_КэшНастроекВидовПакетов_Приоритет1
		|		ПО ВТ_КэшКонтрагентовПоКатегориям.Контрагент1С = ВТ_КэшНастроекВидовПакетов_Приоритет1.Контрагент1С
		|			И ВТ_КэшКонтрагентовПоКатегориям.КатегорияПакета = ВТ_КэшНастроекВидовПакетов_Приоритет1.КатегорияПакета
		|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КэшНастроекВидовПакетов_Приоритет2 КАК ВТ_КэшНастроекВидовПакетов_Приоритет2
		|		ПО ВТ_КэшКонтрагентовПоКатегориям.Организация1С = ВТ_КэшНастроекВидовПакетов_Приоритет2.Организация1С
		|			И ВТ_КэшКонтрагентовПоКатегориям.КатегорияПакета = ВТ_КэшНастроекВидовПакетов_Приоритет2.КатегорияПакета
		|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КэшНастроекОтправкиНепроведенных КАК ВТ_КэшНастроекОтправкиНепроведенных
		|		ПО ВТ_КэшКонтрагентовПоКатегориям.Организация1С = ВТ_КэшНастроекОтправкиНепроведенных.Организация1С
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Организация1С,
		|	Контрагент1С
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	ЗначенияСвойствОбъектов.Объект КАК Документ,
		|	ВТ_КэшВидовПакетов.ВидПакетаID КАК ВидПакета,
		|	ВТ_КэшВидовПакетов.КатегорияПакета КАК КатегорияПакета
		|ПОМЕСТИТЬ ВТ_КэшДокументовПоВидамПакета
		|ИЗ
		|	РегистрСведений.ДополнительныеСведения КАК ЗначенияСвойствОбъектов
		|	ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КэшВидовПакетов КАК ВТ_КэшВидовПакетов
		|	ПО ВТ_КэшВидовПакетов.ВидПакетаID = ЗначенияСвойствОбъектов.Значение
		|ГДЕ
		|	ЗначенияСвойствОбъектов.Свойство.Наименование = ""DDPro_ВидПакета""
		|	ИЛИ ЗначенияСвойствОбъектов.Свойство.Наименование = ""DDPro_ВидПакетаСчет""
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Документ
		|
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	ЗначенияСвойствОбъектов.Объект КАК Документ,
		|	ВТ_КэшДокументовПоВидамПакета.ВидПакета КАК ВидПакета,
		|	ВТ_КэшДокументовПоВидамПакета.КатегорияПакета КАК КатегорияПакета
		|ПОМЕСТИТЬ ВТ_ОтправленныеДокументы
		|ИЗ
		|	РегистрСведений.ДополнительныеСведения КАК ЗначенияСвойствОбъектов
		|	ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КэшДокументовПоВидамПакета КАК ВТ_КэшДокументовПоВидамПакета
		|	ПО ЗначенияСвойствОбъектов.Объект = ВТ_КэшДокументовПоВидамПакета.Документ
		|ГДЕ
		|	ЗначенияСвойствОбъектов.Свойство В(&СвойстваИдентификаторовДокументов)
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Документ
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_КэшВидовПакетов
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_КэшВидовПакетовПоУмолчанию
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_КэшКонтрагентов
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_КэшКонтрагентовПоКатегориям
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_КэшНастроекОтправкиНепроведенных
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_КэшНастроекВидовПакетов
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_КэшНастроекВидовПакетов_Приоритет1
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_КэшНастроекВидовПакетов_Приоритет2
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_КэшВидовДокументов
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_КэшКонтрагентовПоТипуОснования
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_КэшДокументовПоВидамПакета
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_ТипОснования_Приоритет1
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|УНИЧТОЖИТЬ ВТ_ТипОснования_Приоритет2";
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Результат);
	ТекстЗапроса_УстановитьИмяТаблицыОрганизаций(Результат);
	ТекстЗапроса_УстановитьИмяТаблицыКонтрагентов(Результат);
	
	Возврат Результат;
	
КонецФункции


// Формирует временную таблицу ВТ_ОтправленныеДокументы с колонками
//  * Документ - ЛюбаяСсылка - учетный документ
//  * ВидПакета - СправочникСсылка.Диадок_ДополнительныеСправочники, Неопределено - ссылка на описание вида пакета (заполняется только для СХ-1)
//  * ВидПакетаID - Строка - идентификатор вида пакета
//  * КатегорияПакета - Строка - идентификатор категории пакета
//
// Параметры:
//  МенеджерВТ - МенеджерВременныхТаблиц - хранилище временных таблиц
//  ВидыПакетов - ТаблицаЗначений - используемые виды пакетов (см. Ядро.Списки_ТаблицаИспользуемыхВидовПакетов)
//
Процедура СписокНаОтправку_ПодготовитьОтправленныеДокументы(МенеджерВТ, ВидыПакетов) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = МенеджерВТ;
	Запрос.УстановитьПараметр("ВидыПакетов", ВидыПакетов);
	
	ВТ_КэшВидовПакетов =
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	Т.ID КАК ВидПакетаID,
	|	Т.КатегорияПакета КАК КатегорияПакета
	|ПОМЕСТИТЬ ВТ_КэшВидовПакетов
	|ИЗ
	|	&ВидыПакетов КАК Т";
	
	ЧастиЗапроса = Новый Массив;
	ЧастиЗапроса.Добавить(ВТ_КэшВидовПакетов);
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		
		ВТ_КэшВидовПакетовИзСправочника =
		"ВЫБРАТЬ
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка КАК ВидПакета,
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Значение КАК ВидПакетаID,
		|	ВТ_КэшВидовПакетов.КатегорияПакета КАК КатегорияПакета
		|ПОМЕСТИТЬ ВТ_КэшВидовПакетовИзСправочника
		|ИЗ
		|	Справочник.Диадок_ДополнительныеСправочники.ДополнительныеРеквизиты КАК Диадок_ДополнительныеСправочникиДополнительныеРеквизиты
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_КэшВидовПакетов КАК ВТ_КэшВидовПакетов
		|		ПО Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Значение = ВТ_КэшВидовПакетов.ВидПакетаID
		|ГДЕ
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка.ИмяСправочника = ""ВидыПакетов""
		|	И Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.ИмяРеквизита = ""ID""";
		
		ЧастиЗапроса.Добавить(ВТ_КэшВидовПакетовИзСправочника);
		ЧастиЗапроса.Добавить(СписокНаОтправку_ТекстЗапроса_ОтправленныеДокументы_Диадок());
		ЧастиЗапроса.Добавить("УНИЧТОЖИТЬ ВТ_КэшВидовПакетовИзСправочника");
		
	Иначе
		
		ВТ_ОтправленныеДокументы =
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	ЗначенияСвойствОбъектов.Объект КАК Документ,
		|	ВТ_КэшВидовПакетов.ВидПакетаID КАК ВидПакета,
		|	ВТ_КэшВидовПакетов.КатегорияПакета КАК КатегорияПакета
		|ПОМЕСТИТЬ ВТ_КэшДокументовПоВидамПакета
		|ИЗ
		|	РегистрСведений.ДополнительныеСведения КАК ЗначенияСвойствОбъектов
		|	ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КэшВидовПакетов КАК ВТ_КэшВидовПакетов
		|	ПО ВТ_КэшВидовПакетов.ВидПакетаID = ЗначенияСвойствОбъектов.Значение
		|ГДЕ
		|	ЗначенияСвойствОбъектов.Свойство.Наименование = ""DDPro_ВидПакета""
		|	ИЛИ ЗначенияСвойствОбъектов.Свойство.Наименование = ""DDPro_ВидПакетаСчет""
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Документ
		|
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	ЗначенияСвойствОбъектов.Объект КАК Документ,
		|	НЕОПРЕДЕЛЕНО КАК ВидПакета,
		|	ВТ_КэшДокументовПоВидамПакета.ВидПакета КАК ВидПакетаID,
		|	ВТ_КэшДокументовПоВидамПакета.КатегорияПакета КАК КатегорияПакета
		|ПОМЕСТИТЬ ВТ_ОтправленныеДокументы
		|ИЗ
		|	РегистрСведений.ДополнительныеСведения КАК ЗначенияСвойствОбъектов
		|	ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КэшДокументовПоВидамПакета КАК ВТ_КэшДокументовПоВидамПакета
		|	ПО ЗначенияСвойствОбъектов.Объект = ВТ_КэшДокументовПоВидамПакета.Документ
		|ГДЕ
		|	ЗначенияСвойствОбъектов.Свойство В(&СвойстваИдентификаторовДокументов)
		|
		|ИНДЕКСИРОВАТЬ ПО
		|	Документ";
		
		ТекстЗапроса_УстановитьИмяРегистраСведений(ВТ_ОтправленныеДокументы);
		
		ЧастиЗапроса.Добавить(ВТ_ОтправленныеДокументы);
		ЧастиЗапроса.Добавить("УНИЧТОЖИТЬ ВТ_КэшДокументовПоВидамПакета");
		
		Запросы_ЗаполнитьПараметрыСвойствИдентификаторовДокумента(Запрос);
		
	КонецЕсли;
	
	ЧастиЗапроса.Добавить("УНИЧТОЖИТЬ ВТ_КэшВидовПакетов");
	
	Запрос.Текст = СоединитьСтроку(ЧастиЗапроса, ТекстЗапроса_РазделительЗапросов());
	Запрос.ВыполнитьПакет();
	
КонецПроцедуры

// Формирует пустую временную таблицу ВТ_ОтправленныеДокументы с колонками
//  * Документ - ЛюбаяСсылка - учетный документ
//  * ВидПакета - СправочникСсылка.Диадок_ДополнительныеСправочники, Неопределено - ссылка на описание вида пакета (заполняется только для СХ-1)
//  * ВидПакетаID - Строка - идентификатор вида пакета
//  * КатегорияПакета - Строка - идентификатор категории пакета
//
// Параметры:
//  МенеджерВТ - МенеджерВременныхТаблиц - хранилище временных таблиц
//
Процедура СписокНаОтправку_ПодготовитьПустуюТаблицуОтправленныеДокументы(МенеджерВТ) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = МенеджерВТ;
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 0
	|	НЕОПРЕДЕЛЕНО КАК Документ,
	|	НЕОПРЕДЕЛЕНО КАК ВидПакета,
	|	"""" КАК ВидПакетаID,
	|	"""" КАК КатегорияПакета
	|ПОМЕСТИТЬ ВТ_ОтправленныеДокументы";
	
	Запрос.ВыполнитьПакет();
	
КонецПроцедуры

//**********************************************
// СПИСКИ ДОКУМЕНТОВ: ПОДСИСТЕМА ДИАДОКА

Функция СписокДокументов_Подготовленные_Диадок(Параметры)
	
	Результат = Новый_СписокДокументов();
	
	ОрганизацииИзКонтекста = Организации_ОрганизацииЭДОИзКонтекста();
	
	Отбор = "
	|	(ПакетыДокументов.Метка = ""Подготовлен""
	|			ИЛИ ПакетыДокументов.Метка = ""Не отправлен"")
	|	И ПакетыДокументов.ДатаДокумента МЕЖДУ &НачалоПериода И &КонецПериода
	|	И ПакетыДокументов.ВидПакета В(&ВидыПакетов)
	|	И ПакетыДокументов.ОрганизацияДиадок В (&ОрганизацииИзКонтекста)
	|	И &ОтборПоИдентификаторуПакета
	|	И ВЫБОР
	|			КОГДА &ВключитьОтборПоОрганизации
	|				ТОГДА &ПолеОтборПоОрганизации = &ОтборПоОрганизации
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВЫБОР
	|			КОГДА &ВключитьОтборПоКонтрагенту
	|				ТОГДА &ПолеОтборПоКонтрагенту = &ОтборПоКонтрагенту
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ";
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		
		Отбор = СтрЗаменить(Отбор, "&ПолеОтборПоОрганизации", "Диадок_ЮрФизЛица_Организация.Значение");
		Отбор = СтрЗаменить(Отбор, "&ПолеОтборПоКонтрагенту", "Диадок_ЮрФизЛица_Контрагент.Значение");
		
	Иначе
		
		Отбор = СтрЗаменить(Отбор, "&ПолеОтборПоОрганизации", "Диадок_ЮрФизЛица_Организация.СвязанныйСправочник1");
		Отбор = СтрЗаменить(Отбор, "&ПолеОтборПоКонтрагенту", "Диадок_ЮрФизЛица_Контрагент.СвязанныйСправочник1");
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Параметры.ОтборПоПакету) Тогда
		Отбор = СтрЗаменить(Отбор, "&ОтборПоИдентификаторуПакета", "ПакетыДокументов.LetterId = &ИдентификаторПакета");		
	Иначе
		Отбор = СтрЗаменить(Отбор, "&ОтборПоИдентификаторуПакета", "Истина");
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = Пакеты_ТекстЗапросаДляЗаполненияКонтракта_Диадок(Отбор);
	
	ВидыПакетов			= ДопСправочники_СсылкиПоИдентификаторам_Диадок("ВидыПакетов", Параметры.МассивВыбранныхВидов);
	ИдентификаторПакета	= СокрЛП(СвойствоСтруктуры(Параметры.ОтборПоПакету, "ИдентификаторПакета"));
	
	Запрос.УстановитьПараметр("ВидыПакетов",				ВидыПакетов);
	Запрос.УстановитьПараметр("НачалоПериода",				Параметры.НачалоПериода);
	Запрос.УстановитьПараметр("КонецПериода",				Параметры.КонецПериода);
	Запрос.УстановитьПараметр("ОтборПоОрганизации",			Параметры.ОтборПоОрганизации);
	Запрос.УстановитьПараметр("ОтборПоКонтрагенту",			Параметры.ОтборПоКонтрагенту);
	Запрос.УстановитьПараметр("ВключитьОтборПоКонтрагенту",	Параметры.ВключитьОтборПоКонтрагенту);
	Запрос.УстановитьПараметр("ВключитьОтборПоОрганизации",	Параметры.ВключитьОтборПоОрганизации);
	Запрос.УстановитьПараметр("ОрганизацииИзКонтекста",		ОрганизацииИзКонтекста);
	Запрос.УстановитьПараметр("ИдентификаторПакета",		ИдентификаторПакета);
	
	Выборка = Запрос.Выполнить().Выбрать();
		
	Пока Выборка.Следующий() Цикл
		
		СтрокаСписка = Результат.Добавить();
		СписокДокументов_ЗаполнитьСтрокуСпискаПоВыборке_Диадок(СтрокаСписка, Выборка, Ложь);
		
		СтруктураДопРеквизитов = ДопРеквизиты_СписокРеквизитов(Выборка.ПакетЭДО);
		
		Для Счетчик = 1 По 5 Цикл
			
			ИмяДопРеквизита = "ДопРеквизит" + Строка(Счетчик);
			СтрокаСписка[ИмяДопРеквизита] = СвойствоСтруктуры(СтруктураДопРеквизитов, ИмяДопРеквизита);
			
		КонецЦикла;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ДопСправочники_СсылкиПоИдентификаторам_Диадок(ИмяСправочника, Идентификаторы)
	
	Результат = Новый Массив;
	
	Для Каждого Идентификатор Из Идентификаторы Цикл
		
		Если ТипЗнч(Идентификатор) = Тип("Строка") Тогда
			ВидПакета = ДопСправочники_СсылкаПоИдентификатору_Диадок(ИмяСправочника, Идентификатор);
		Иначе
			ВидПакета = Идентификатор;
		КонецЕсли;
		
		Результат.Добавить(ВидПакета);
		
	КонецЦикла;

	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_Отправленные_Диадок(Параметры)
	
	Результат = Новый_СписокДокументов();
	
	КонтрагентДляОтбора = Параметры.ОтборПоКонтрагенту;
	КонтрагентыСервиса = Справочники_СвязанныеКонтрагентыСервиса_Диадок(КонтрагентДляОтбора);
	
	ОрганизацияДляОтбора = Параметры.ОтборПоОрганизации;
	ОрганизацииСервиса = Справочники_СвязанныеОрганизацииСервиса_Диадок(ОрганизацияДляОтбора);
	
	ОрганизацииИзКонтекста = Организации_ОрганизацииЭДОИзКонтекста();
	
	Отбор = "
	|	ВЫБОР
	|			КОГДА &РежимОтбораПоПериоду = ""ПоДатеДокумента""
	|				ТОГДА Документы.ДатаДокумента МЕЖДУ &НачалоПериода И &КонецПериода
	|			ИНАЧЕ Документы.ДатаЗагрузки МЕЖДУ &НачалоПериода И &КонецПериода
	|		КОНЕЦ
	|	И Документы.Направление В (&СписокНаправлений)
	|	И Документы.Статус <> ""Не отправлен""
	|	И НЕ Документы.Метка В (&ИсключаемыеМетки)
	|	И &ДополнительныйОтбор
	|	И &ОтборПоИдентификаторуПакета
	|	И &ОтборПоТипамДокументовAPI
	|	И Документы.ОрганизацияДиадок В (&ОрганизацииИзКонтекста)
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПоОрганизации
	|				ТОГДА Документы.ОрганизацияДиадок В(&ОрганизацииСервиса)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПоКонтрагенту
	|				ТОГДА Документы.КонтрагентДиадок В(&КонтрагентыСервиса)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВЫБОР
	|			КОГДА &ОтборПоСтатусу <> НЕОПРЕДЕЛЕНО
	|				ТОГДА Документы.Статус ПОДОБНО &ОтборПоСтатусу
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ";
	
	Если ЗначениеЗаполнено(Параметры.МассивВыбранныхВидов) Тогда
		Отбор = СтрЗаменить(Отбор, "&ДополнительныйОтбор", "Документы.ВидДокумента В(&МассивВыбранныхВидов)");
	Иначе
		Отбор = СтрЗаменить(Отбор, "&ДополнительныйОтбор", "Истина");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Параметры.ОтборПоПакету) Тогда
		Отбор = СтрЗаменить(Отбор, "&ОтборПоИдентификаторуПакета", "Документы.LetterId = &ИдентификаторПакета");		
	Иначе
		Отбор = СтрЗаменить(Отбор, "&ОтборПоИдентификаторуПакета", "Истина");
	КонецЕсли;
	
	ОтборПоТипамДокументовAPI = СвойствоСтруктуры(Параметры, "ОтборПоТипамДокументовAPI");
	
	Если ЗначениеЗаполнено(ОтборПоТипамДокументовAPI) Тогда
		Отбор = СтрЗаменить(Отбор, "&ОтборПоТипамДокументовAPI", "Документы.ТипДокумента В(&ОтборПоТипамДокументовAPI)");
	Иначе
		Отбор = СтрЗаменить(Отбор, "&ОтборПоТипамДокументовAPI", "Истина");
	КонецЕсли;
	
	Запрос = Новый Запрос;
	
	Запрос.Текст = Документы_ТекстЗапросаЗаполненияКонтракта(Отбор);
	
	ИсключаемыеМетки	= ИсключаемыеМетки_ОтправленныеДокументы(); 
	ИдентификаторПакета	= СокрЛП(СвойствоСтруктуры(Параметры.ОтборПоПакету, "ИдентификаторПакета"));
	
	Запрос.УстановитьПараметр("НачалоПериода",			Параметры.НачалоПериода);
	Запрос.УстановитьПараметр("КонецПериода",			Параметры.КонецПериода);
	Запрос.УстановитьПараметр("МассивВыбранныхВидов",	Параметры.МассивВыбранныхВидов);
	Запрос.УстановитьПараметр("ОтборПоСтатусу",			"%" + Параметры.ОтборПоСтатусу + "%");
	Запрос.УстановитьПараметр("РежимОтбораПоПериоду",	Параметры.РежимОтбораПоПериоду);
	Запрос.УстановитьПараметр("ОрганизацииИзКонтекста",	ОрганизацииИзКонтекста); 
	Запрос.УстановитьПараметр("ИсключаемыеМетки",		ИсключаемыеМетки);
	Запрос.УстановитьПараметр("ИдентификаторПакета",	ИдентификаторПакета);
	Запрос.УстановитьПараметр("ЕстьОтборПоКонтрагенту", ЗначениеЗаполнено(КонтрагентДляОтбора));
	Запрос.УстановитьПараметр("КонтрагентыСервиса", КонтрагентыСервиса);
	Запрос.УстановитьПараметр("ЕстьОтборПоОрганизации", ЗначениеЗаполнено(ОрганизацияДляОтбора));
	Запрос.УстановитьПараметр("ОрганизацииСервиса", ОрганизацииСервиса);
	Запрос.УстановитьПараметр("ОтборПоТипамДокументовAPI", ОтборПоТипамДокументовAPI);
	
	СписокНаправлений = Новый Массив;
	СписокНаправлений.Добавить("Исходящее");

	Запрос.УстановитьПараметр("СписокНаправлений", СписокНаправлений);
	
	Выборка = Запрос.Выполнить().Выбрать();
		
	Пока Выборка.Следующий() Цикл
				
		СтрокаСписка = Результат.Добавить();
		СписокДокументов_ЗаполнитьСтрокуСпискаПоВыборке_Диадок(СтрокаСписка, Выборка);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_Полученные_Диадок(Параметры)
	
	Результат = Новый_СписокДокументов();
	
	КонтрагентДляОтбора = Параметры.ОтборПоКонтрагенту;
	КонтрагентыСервиса = Справочники_СвязанныеКонтрагентыСервиса_Диадок(КонтрагентДляОтбора);
	
	ОрганизацияДляОтбора = Параметры.ОтборПоОрганизации;
	ОрганизацииСервиса = Справочники_СвязанныеОрганизацииСервиса_Диадок(ОрганизацияДляОтбора);
	
	ОрганизацииИзКонтекста = Организации_ОрганизацииЭДОИзКонтекста();
	
	Отбор = "
	|	ВЫБОР
	|			КОГДА &РежимОтбораПоПериоду = ""ПоДатеДокумента""
	|				ТОГДА Документы.ДатаДокумента МЕЖДУ &НачалоПериода И &КонецПериода
	|			ИНАЧЕ Документы.ДатаЗагрузки МЕЖДУ &НачалоПериода И &КонецПериода
	|		КОНЕЦ
	|	И &ДополнительныйОтбор
	|	И &ОтборПоИдентификаторуПакета
	|	И &ПоказыватьЗавершенные
	|	И Документы.Направление = ""Входящее""
	|	И Документы.ОрганизацияДиадок В (&ОрганизацииИзКонтекста)
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПоОрганизации
	|				ТОГДА Документы.ОрганизацияДиадок В(&ОрганизацииСервиса)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПоКонтрагенту
	|				ТОГДА Документы.КонтрагентДиадок В(&КонтрагентыСервиса)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВЫБОР
	|			КОГДА &ОтборПоСтатусу <> НЕОПРЕДЕЛЕНО
	|				ТОГДА Документы.Статус = &ОтборПоСтатусу
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ";
	
	ОтборНаРезультат = "Истина";
	
	Если Параметры.ПоказыватьЗавершенные Тогда
		Отбор = СтрЗаменить(Отбор, "&ПоказыватьЗавершенные", "Истина");
	Иначе
		Отбор = СтрЗаменить(Отбор, "&ПоказыватьЗавершенные",
		"	НЕ (Документы.АнализПроведен
		|		И Документы.ТребуемоеДействиеВ1С = """"
		|		И Документы.ТребуемоеДействиеВДД = """")");
		
		ОтборНаРезультат = "ЕСТЬNULL(РеквизитШаблон.Значение, Ложь) = Ложь";
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Параметры.МассивВыбранныхВидов) Тогда
		Отбор = СтрЗаменить(Отбор, "&ДополнительныйОтбор", "Документы.ТипДокумента В (&МассивТиповДокументов)");		
	Иначе
		Отбор = СтрЗаменить(Отбор, "&ДополнительныйОтбор", "Истина");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Параметры.ОтборПоПакету) Тогда
		Отбор = СтрЗаменить(Отбор, "&ОтборПоИдентификаторуПакета", "Документы.LetterId = &ИдентификаторПакета");		
	Иначе
		Отбор = СтрЗаменить(Отбор, "&ОтборПоИдентификаторуПакета", "Истина");
	КонецЕсли;
	
	Запрос = Новый Запрос;
	
	Запрос.Текст = Документы_ТекстЗапросаЗаполненияКонтракта(Отбор);
		
	ИдентификаторПакета = СокрЛП(СвойствоСтруктуры(Параметры.ОтборПоПакету, "ИдентификаторПакета"));
	
	Запрос.УстановитьПараметр("НачалоПериода",			Параметры.НачалоПериода);
	Запрос.УстановитьПараметр("КонецПериода",			Параметры.КонецПериода);
	Запрос.УстановитьПараметр("ОтборПоСтатусу",			Параметры.ОтборПоСтатусу);
	Запрос.УстановитьПараметр("РежимОтбораПоПериоду",	Параметры.РежимОтбораПоПериоду);
	Запрос.УстановитьПараметр("ОрганизацииИзКонтекста",	ОрганизацииИзКонтекста);
	Запрос.УстановитьПараметр("МассивТиповДокументов",	МассивВидовГруппировок_В_МассивВидовДокументов(Параметры.МассивВыбранныхВидов));
	Запрос.УстановитьПараметр("ИдентификаторПакета",	ИдентификаторПакета);
	Запрос.УстановитьПараметр("ЕстьОтборПоКонтрагенту", ЗначениеЗаполнено(КонтрагентДляОтбора));
	Запрос.УстановитьПараметр("КонтрагентыСервиса", КонтрагентыСервиса);
	Запрос.УстановитьПараметр("ЕстьОтборПоОрганизации", ЗначениеЗаполнено(ОрганизацияДляОтбора));
	Запрос.УстановитьПараметр("ОрганизацииСервиса", ОрганизацииСервиса);
	
	Выборка = Запрос.Выполнить().Выбрать();
		
	Пока Выборка.Следующий() Цикл
		
		СтрокаСписка = Результат.Добавить();
		СписокДокументов_ЗаполнитьСтрокуСпискаПоВыборке_Диадок(СтрокаСписка, Выборка);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_Внутренние_Диадок(Параметры)
	
	Результат = Новый_СписокДокументов();
	
	ОрганизацияДляОтбора = Параметры.ОтборПоОрганизации;
	ОрганизацииСервиса = Справочники_СвязанныеОрганизацииСервиса_Диадок(ОрганизацияДляОтбора);
	
	ОрганизацииИзКонтекста = Организации_ОрганизацииЭДОИзКонтекста();
	
	Отбор = "
	|	ВЫБОР
	|			КОГДА &РежимОтбораПоПериоду = ""ПоДатеДокумента""
	|				ТОГДА Документы.ДатаДокумента МЕЖДУ &НачалоПериода И &КонецПериода
	|			ИНАЧЕ Документы.ДатаЗагрузки МЕЖДУ &НачалоПериода И &КонецПериода
	|		КОНЕЦ
	|	И &ДополнительныйОтбор
	|	И &ОтборПоИдентификаторуПакета
	|	И &ОтборПоТипамДокументовAPI
	|	И &ПоказыватьЗавершенные
	|	И Документы.Направление = ""Внутреннее""
	|	И Документы.ОрганизацияДиадок В (&ОрганизацииИзКонтекста)
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПоОрганизации
	|				ТОГДА Документы.ОрганизацияДиадок В(&ОрганизацииСервиса)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВЫБОР
	|			КОГДА &ОтборПоСтатусу <> НЕОПРЕДЕЛЕНО
	|				ТОГДА Документы.Статус = &ОтборПоСтатусу
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ";
	
	ОтборНаРезультат = "Истина";
	
	Если Параметры.ПоказыватьЗавершенные Тогда
		Отбор = СтрЗаменить(Отбор, "&ПоказыватьЗавершенные", "Истина");
	Иначе
		Отбор = СтрЗаменить(Отбор, "&ПоказыватьЗавершенные",
		"	НЕ (Документы.АнализПроведен
		|		И Документы.ТребуемоеДействиеВ1С = """"
		|		И Документы.ТребуемоеДействиеВДД = """")");
		
		ОтборНаРезультат = "ЕСТЬNULL(РеквизитШаблон.Значение, Ложь) = Ложь";
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Параметры.МассивВыбранныхВидов) Тогда
		Отбор = СтрЗаменить(Отбор, "&ДополнительныйОтбор", "Документы.ТипДокумента В (&МассивТиповДокументов)");		
	Иначе
		Отбор = СтрЗаменить(Отбор, "&ДополнительныйОтбор", "Истина");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Параметры.ОтборПоПакету) Тогда
		Отбор = СтрЗаменить(Отбор, "&ОтборПоИдентификаторуПакета", "Документы.LetterId = &ИдентификаторПакета");		
	Иначе
		Отбор = СтрЗаменить(Отбор, "&ОтборПоИдентификаторуПакета", "Истина");
	КонецЕсли;
	
	ОтборПоТипамДокументовAPI = СвойствоСтруктуры(Параметры, "ОтборПоТипамДокументовAPI");
	
	Если ЗначениеЗаполнено(ОтборПоТипамДокументовAPI) Тогда
		Отбор = СтрЗаменить(Отбор, "&ОтборПоТипамДокументовAPI", "Документы.ТипДокумента В(&ОтборПоТипамДокументовAPI)");
	Иначе
		Отбор = СтрЗаменить(Отбор, "&ОтборПоТипамДокументовAPI", "Истина");
	КонецЕсли;
	
	Запрос = Новый Запрос;
	
	Запрос.Текст = Документы_ТекстЗапросаЗаполненияКонтракта(Отбор);
		
	ИдентификаторПакета = СокрЛП(СвойствоСтруктуры(Параметры.ОтборПоПакету, "ИдентификаторПакета"));
	
	Запрос.УстановитьПараметр("НачалоПериода",			Параметры.НачалоПериода);
	Запрос.УстановитьПараметр("КонецПериода",			Параметры.КонецПериода);
	Запрос.УстановитьПараметр("ОтборПоСтатусу",			Параметры.ОтборПоСтатусу);
	Запрос.УстановитьПараметр("РежимОтбораПоПериоду",	Параметры.РежимОтбораПоПериоду);
	Запрос.УстановитьПараметр("ОрганизацииИзКонтекста",	ОрганизацииИзКонтекста);
	Запрос.УстановитьПараметр("МассивТиповДокументов",	МассивВидовГруппировок_В_МассивВидовДокументов(Параметры.МассивВыбранныхВидов));
	Запрос.УстановитьПараметр("ИдентификаторПакета",	ИдентификаторПакета);
	Запрос.УстановитьПараметр("ЕстьОтборПоОрганизации", ЗначениеЗаполнено(ОрганизацияДляОтбора));
	Запрос.УстановитьПараметр("ОрганизацииСервиса", ОрганизацииСервиса);
	Запрос.УстановитьПараметр("ОтборПоТипамДокументовAPI", ОтборПоТипамДокументовAPI);
	
	Выборка = Запрос.Выполнить().Выбрать();
		
	Пока Выборка.Следующий() Цикл
		
		СтрокаСписка = Результат.Добавить();
		СписокДокументов_ЗаполнитьСтрокуСпискаПоВыборке_Диадок(СтрокаСписка, Выборка);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура СписокДокументов_ЗаполнитьСтрокуСпискаПоВыборке_Диадок(СтрокаСписка, Выборка, ЭтоВыборкаДокументов = Истина)
	
	Если ЭтоВыборкаДокументов Тогда
		
		СтрокаСписка.ПредставлениеДокументаДиадок	= Выборка.ИмяФайла;
		СтрокаСписка.ВидПакетаID					= Выборка.ИдентификаторВидаПакета;
		СтрокаСписка.ВидЭлДокументаID				= Выборка.ИдентификаторВида;
		СтрокаСписка.ВидЭлДокумента					= Выборка.ВидДокумента;
		СтрокаСписка.ДокументЭДО					= Выборка.ДокументЭДО;
		СтрокаСписка.ТипДокумента					= Выборка.ТипДокумента;
		СтрокаСписка.ТипКонтента					= Выборка.ТипКонтента;
		СтрокаСписка.ФункцияДокумента				= Выборка.ФункцияДокумента;
		СтрокаСписка.АнализПроведен					= Выборка.АнализПроведен;
		СтрокаСписка.ДействиеВ1С					= Выборка.ТребуемоеДействиеВ1С;
		СтрокаСписка.ДействиеВДД					= Выборка.ТребуемоеДействиеВДД;
		СтрокаСписка.Статус							= Выборка.Статус;
		СтрокаСписка.ДополнительныйСтатус			= Выборка.ДополнительныйСтатус;
		СтрокаСписка.ОбъектМаршрутизации			= Выборка.ОбъектМаршрутизацииНаименование;
		СтрокаСписка.Шаблон							= Выборка.Шаблон;
		СтрокаСписка.ТребуетсяРасшифровка			= Выборка.ТребуетсяРасшифровка;
		СтрокаСписка.DocumentId						= Выборка.DocumentId;
		СтрокаСписка.СтатусМЧД						= Выборка.СтатусМЧД;
		СтрокаСписка.СтатусМЧДКритичность			= Выборка.СтатусМЧДКритичность;
		СтрокаСписка.СтатусПКФНС					= Выборка.СтатусПКФНС;
		СтрокаСписка.СтатусПКФНСКритичность			= Выборка.СтатусПКФНСКритичность;
		СтрокаСписка.СтатусТЭДО						= Выборка.СтатусДТС;
		
		ВнешниеСтатусы = Новый_ВнешниеСтатусы(Выборка.ВнешнийСтатусГИСМТ, Выборка.ВнешнийСтатусКонтурЛогистика);
		СтрокаСписка.ВнешнийСтатус = Ядро_ПредставлениеВнешнихСтатусов(ВнешниеСтатусы);
		СтрокаСписка.ПредставлениеКонтрагентаЭДО = Выборка.ПредставлениеКонтрагентаЭДО;
		СтрокаСписка.ПредставлениеОрганизацииЭДО = Выборка.ПредставлениеОрганизацииЭДО;
		
		Если Выборка.Направление = "Внутреннее" Тогда 
			
			ИдентификаторПодразделенияОтправителя = Выборка.FromDepartmentId;
			ИдентификаторПодразделенияПолучателя = Выборка.ToDepartmentId;
		
			ИдСправочникаПодразделенияОтправителя = Новый_ИдентификаторыСправочника(, ИдентификаторПодразделенияОтправителя);
			ИдСправочникаПодразделенияПолучателя = Новый_ИдентификаторыСправочника(, ИдентификаторПодразделенияПолучателя);
		
			ПодразделениеОтправитель = Подразделения_СсылкаПоИдентификатору(ИдСправочникаПодразделенияОтправителя, Истина);
			ПодразделениеПолучатель = Подразделения_СсылкаПоИдентификатору(ИдСправочникаПодразделенияПолучателя, Истина); 
			
			СтрокаСписка.ПодразделениеОтправитель = ПодразделениеОтправитель;
			СтрокаСписка.ПодразделениеПолучатель = ПодразделениеПолучатель;
			
		КонецЕсли;

		Если Выборка.ТипДокумента = Ядро_ПеревозочныеДокументы_ТипДокументаAPI_ЭТрН() Тогда
			ПеревозочныеГрузополучатель =
				Ядро_Перевозочные_ГрузополучательПоИмениФайла(Выборка.FileName, Выборка.BoxId);
			СтрокаСписка.ПромежуточныйПолучатель = ПеревозочныеГрузополучатель;
			СтрокаСписка.ПредставлениеПромежуточногоПолучателяЭДО = ПеревозочныеГрузополучатель.Наименование;
		КонецЕсли;
		
		СтрокаСписка.ИмяФайла = Выборка.FileName;
		
	Иначе
		СтрокаСписка.ВидПакетаID = Выборка.ИдентификаторВида;
	КонецЕсли;
	
	СтрокаСписка.BoxId					= Выборка.BoxId;
	СтрокаСписка.LetterId				= Выборка.LetterId;
	СтрокаСписка.ВидПакета				= Выборка.ВидПакета;
	СтрокаСписка.Направление			= Выборка.Направление;
	СтрокаСписка.ДатаДокумента			= Выборка.Дата;
	СтрокаСписка.НомерДокумента			= Выборка.Номер;
	СтрокаСписка.СуммаДокумента			= Выборка.Сумма;
	СтрокаСписка.Документ				= Выборка.ДокументУчета;
	СтрокаСписка.Метка					= Выборка.Состояние;
	СтрокаСписка.ДатаОтправки      		= Выборка.ДатаЭДО;
	СтрокаСписка.ДатаПолучения      	= Выборка.ДатаЭДО;
	СтрокаСписка.ЗаблокированныйПакет	= Выборка.Заблокирован;
	СтрокаСписка.ОшибкиПроверкиТекст	= Выборка.ТекстОшибки;
	СтрокаСписка.Пакет					= Выборка.ПакетЭДО;
	СтрокаСписка.Организация			= Выборка.Организация_Организация;
	СтрокаСписка.Контрагент				= Выборка.Контрагент_Организация;
	СтрокаСписка.Подразделение			= Выборка.Подразделение;
	СтрокаСписка.КонтрагентТипОрганизации = Выборка.КонтрагентТипОрганизации;
	
КонецПроцедуры

Функция Новый_ВнешниеСтатусы(СтатусГИСМТ, СтатусКонтурЛогистика)
	
	Результат = Новый Структура;
	
	Если ЗначениеЗаполнено(СтатусГИСМТ) Тогда
		Ключ = Ядро_ИдентификаторВнешнегоДО_ГИСМТ();
		Результат.Вставить(Ключ, СтатусГИСМТ);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтатусКонтурЛогистика) Тогда
		Ключ = Ядро_ИдентификаторВнешнегоДО_КонтурЛогистика();
		Результат.Вставить(Ключ, СтатусКонтурЛогистика);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_ДополненныеПараметрыНаОтправку_Диадок(Параметры)
	
	Результат = Параметры;
	
	Результат.Вставить("ШаблонИмениРеквизита_СвязанныйСправочник", ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник());
						
	Возврат Результат;
	
КонецФункции

//**********************************************
// СПИСКИ ДОКУМЕНТОВ: 1С

Функция СписокДокументов_Отправленные_1С(Параметры)
	
	Результат = Новый_СписокДокументов();

	Если ЗначениеЗаполнено(Параметры.ОтборПоПакету) Тогда
		
		Идентификаторы	= Новый Структура("BoxID, LetterID, DocumentID, ДокументАПИ");
		ЗаполнитьЗначенияСвойств(Идентификаторы, Параметры.ОтборПоПакету);
		
		Попытка
			
			Идентификаторы.ДокументАПИ = API_GetDocument(Идентификаторы.BoxId, Идентификаторы.LetterId, Идентификаторы.DocumentId);
			
			МассивИдентификаторовДокументов = Новый Массив();
			МассивИдентификаторовДокументов.Добавить(Идентификаторы);
			
			СписокДокументов_ЗаполнитьДаннымиДокументовАПИ_1С(Результат, МассивИдентификаторовДокументов, Параметры.ОтборПоСтатусу);
			СписокДокументов_ДополнитьСписокИдентификаторамиВидовПакетов(Результат);
			
		Исключение
			
			Ошибка = ИнформацияОбОшибке();
			
			Если Найти(Ошибка.Описание, "Код ошибки: 403") > 0 Тогда 
				//передали в другое подразделение на которое нет прав
			Иначе 
				ВызватьИсключение;
			КонецЕсли;
			
		КонецПопытки;
		
	Иначе
		
		ТипыДокументов = СвойствоСтруктуры(Параметры, "ОтборПоТипамДокументовAPI");
		
		Если НЕ ЗначениеЗаполнено(ТипыДокументов) Тогда
			ТипыДокументов = ТипыДокументовПоВидамДокументов(Параметры.МассивВыбранныхВидов);
		КонецЕсли;
		
		Для Каждого ТипДокумента Из ТипыДокументов Цикл
			
			Параметры.Вставить("ТипДокумента", ТипДокумента);
			
			МассивИдентификаторовДокументов	= СписокДокументов_ИдентификаторыОтправленныхДокументовАПИ(Параметры);
			
			СписокДокументов_ЗаполнитьДаннымиДокументовАПИ_1С(Результат, МассивИдентификаторовДокументов, Параметры.ОтборПоСтатусу);
			СписокДокументов_ДополнитьСписокИдентификаторамиВидовПакетов(Результат);
			
		КонецЦикла;
		
		Результат.Сортировать("ДатаОтправки УБЫВ, LetterId");

	КонецЕсли;
	
	СписокДокументов_ДозаполнитьВидыПакетов_1С(Результат);
	
	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_Полученные_1С(Параметры)
	
	Результат = Новый_СписокДокументов();

	Если ЗначениеЗаполнено(Параметры.ОтборПоПакету) Тогда
		
		Идентификаторы	= Новый Структура("BoxID, LetterID, DocumentID, ДокументАПИ");
		ЗаполнитьЗначенияСвойств(Идентификаторы, Параметры.ОтборПоПакету);
		
		Попытка
			
			Идентификаторы.ДокументАПИ = API_GetDocument(Идентификаторы.BoxId, Идентификаторы.LetterId, Идентификаторы.DocumentId);
			
			МассивИдентификаторовДокументов = Новый Массив();
			МассивИдентификаторовДокументов.Добавить(Идентификаторы);
			
			СписокДокументов_ЗаполнитьДаннымиДокументовАПИ_1С(Результат, МассивИдентификаторовДокументов, Параметры.ОтборПоСтатусу);
			
		Исключение
			
			Ошибка = ИнформацияОбОшибке();
			
			Если Найти(Ошибка.Описание, "Код ошибки: 403") > 0 Тогда 
				//передали в другое подразделение на которое нет прав
			Иначе 
				ВызватьИсключение;
			КонецЕсли;
			
		КонецПопытки;
		
	Иначе
	
		ТипыДокументов = ТипыДокументовПоВидамДокументовПолученных(Параметры.МассивВыбранныхВидов);
			
		Для Каждого ТипДокумента Из ТипыДокументов Цикл
			
			Параметры.Вставить("ТипДокумента", ТипДокумента);
			
			МассивИдентификаторовДокументов	= СписокДокументов_ИдентификаторыПолученныхДокументовАПИ(Параметры);
			
			СписокДокументов_ЗаполнитьДаннымиДокументовАПИ_1С(Результат, МассивИдентификаторовДокументов, Параметры.ОтборПоСтатусу);
			
		КонецЦикла;
		
		Результат.Сортировать("ДатаОтправки УБЫВ, LetterId");
	
	КонецЕсли;

	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_Внутренние_1С(Параметры)
	
	Результат = Новый_СписокДокументов();

	Если ЗначениеЗаполнено(Параметры.ОтборПоПакету) Тогда
		
		Идентификаторы	= Новый Структура("BoxID, LetterID, DocumentID, ДокументАПИ");
		ЗаполнитьЗначенияСвойств(Идентификаторы, Параметры.ОтборПоПакету);
		
		Попытка
			
			Идентификаторы.ДокументАПИ = API_GetDocument(Идентификаторы.BoxId, Идентификаторы.LetterId, Идентификаторы.DocumentId);
			
			МассивИдентификаторовДокументов = Новый Массив();
			МассивИдентификаторовДокументов.Добавить(Идентификаторы);
			
			СписокДокументов_ЗаполнитьДаннымиДокументовАПИ_1С(Результат, МассивИдентификаторовДокументов, Параметры.ОтборПоСтатусу);
			СписокДокументов_ДополнитьСписокИдентификаторамиВидовПакетов(Результат);
			
		Исключение
			
			Ошибка = ИнформацияОбОшибке();
			
			Если Найти(Ошибка.Описание, "Код ошибки: 403") > 0 Тогда 
				//передали в другое подразделение на которое нет прав
			Иначе 
				ВызватьИсключение;
			КонецЕсли;
			
		КонецПопытки;
		
	Иначе
		
		ТипыДокументов = СвойствоСтруктуры(Параметры, "ОтборПоТипамДокументовAPI");
		
		Если НЕ ЗначениеЗаполнено(ТипыДокументов) Тогда
			ТипыДокументов = ТипыДокументовПоВидамДокументовПолученных(Параметры.МассивВыбранныхВидов);
		КонецЕсли;
		
		Для Каждого ТипДокумента Из ТипыДокументов Цикл
			
			Параметры.Вставить("ТипДокумента", ТипДокумента);
			
			МассивИдентификаторовДокументов	= СписокДокументов_ИдентификаторыВнутреннихДокументовАПИ(Параметры);
			
			СписокДокументов_ЗаполнитьДаннымиДокументовАПИ_1С(Результат, МассивИдентификаторовДокументов, Параметры.ОтборПоСтатусу);
			СписокДокументов_ДополнитьСписокИдентификаторамиВидовПакетов(Результат);
			
		КонецЦикла;
		
		Результат.Сортировать("ДатаОтправки УБЫВ, LetterId");
	
	КонецЕсли;

	Возврат Результат;
	
КонецФункции

// Заполняет список документов данными документов из API и дополняет данными информационной базы 
//
// Параметры:
//  СписокДокументов - ТаблицаЗначений 	- см. Ядро.Новый_СписокДокументов()
//  ДокументыАПИ	 - Массив	 		- массив структур, содержащий идентификаторы документов и данные документов, полученные из API
//  ОтборПоСтатусу	 - Строка	 		- значение статуса документа, по которому выполняется отбор, если заполнен 
//
Процедура СписокДокументов_ЗаполнитьДаннымиДокументовАПИ_1С(СписокДокументов, ДокументыАПИ, ОтборПоСтатусу)
	
	ТаблицаДокументовАПИ = СписокДокументов_МассивДокументовАПИвТаблицуЗначений(ДокументыАПИ, ОтборПоСтатусу);
	
	ИдентификаторыДокументов = ТаблицаДокументовАПИ.Скопировать(, "АдресЯщика, ИдентификаторДокумента");
	ДокументыИБ = СписокДокументов_ДокументыИнформационнойБазы(ИдентификаторыДокументов);
	
	Для Каждого СтрокаДокументАПИ Из ТаблицаДокументовАПИ Цикл
		
		КонтрактДокументаЭДО = СтрокаДокументАПИ.Документ;

		Отбор = Новый Структура("АдресЯщика, ИдентификаторДокумента");
		ЗаполнитьЗначенияСвойств(Отбор, СтрокаДокументАПИ);
				
		НайденныеСтроки = ДокументыИБ.НайтиСтроки(Отбор);
		Если НайденныеСтроки.Количество() > 0 Тогда
			КонтрактДокументаЭДО.ДокументУчета = НайденныеСтроки[0].Документ;
		КонецЕсли;
		
		СтрокаСписка = СписокДокументов.Добавить();
		СписокДокументов_ЗаполнитьСтрокуСпискаПоДокументу_1С(СтрокаСписка, КонтрактДокументаЭДО);
		
	КонецЦикла;
	
КонецПроцедуры

Функция СписокДокументов_МассивДокументовАПИвТаблицуЗначений(ДокументыАПИ, ОтборПоСтатусу)
	
	Результат = Новый_ТаблицаДокументовАПИ();
	
	Для Каждого Идентификаторы Из ДокументыАПИ Цикл
		
		ДокументАПИ	= СвойствоСтруктуры(Идентификаторы, "ДокументАПИ");
		
		ДокументПодходитПоСтатусу = СписокДокументов_ДокументПодходитПоСтатусу_1С(ДокументАПИ, ОтборПоСтатусу);
		
		Если ДокументПодходитПоСтатусу Тогда
			
			Документ 	= Документы_ДанныеДокумента_1С(Идентификаторы, ДокументАПИ);
			ИдДокумента = Документы_ИдентификаторДокумента_1С(Идентификаторы);
            АдресЯщика 	= ИдентификаторВАдресЯщика(Идентификаторы.BoxID);
			
			НоваяСтрока = Результат.Добавить();
						
			НоваяСтрока.ИдентификаторДокумента 	= ИдДокумента;
			НоваяСтрока.АдресЯщика 				= АдресЯщика;
			НоваяСтрока.Документ 				= Документ;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Формирует коллекцию документов информационной базы с идентификаторами API
//
// Параметры:
//  ИдентификаторыДокументов - ТаблицаЗначений	 - см. Новый_КоллекцияКлючейДокументовИнформационнойБазы()
// 
// Возвращаемое значение:
//  ТаблицаЗначений - ссылки на документы и значения их свойств
//		* Документ 					- Ссылка - ссылка на документ информационной базы
//		* ИдентификаторДокумента 	- Строка - идентификатор документа в информационной базе, составленный из пары LetterID и DocumentID
//		* АдресЯщика				- Строка - идентфикатор ящика в виде адреса <guid>@diadoc.ru
//		* ВидПакета					- Строка - идентфикатор вида пакета (напр. ID_УПД, ID_УПД_раздельно, ID_УКД и др.)
//
Функция СписокДокументов_ДокументыИнформационнойБазы(ИдентификаторыДокументов)

	МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц;
	
	Запрос.Текст =
	"ВЫБРАТЬ
	|	Таб.АдресЯщика,
	|	Таб.ИдентификаторДокумента
	|ПОМЕСТИТЬ ВремИдДокументов
	|ИЗ
	|	&ВнешняяТаблица КАК Таб
	|;
	|
	|//////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЗначенияСвойствОбъектов.Объект КАК Документ,
	|	ВремИдДокументов.ИдентификаторДокумента,
	|	ВЫРАЗИТЬ(ЗначенияСвойствОбъектов_Организации.Значение КАК СТРОКА(100)) КАК АдресЯщика
	|ПОМЕСТИТЬ ВремВсеДокументы
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВремИдДокументов КАК ВремИдДокументов
	|		ПО ВЫРАЗИТЬ(ЗначенияСвойствОбъектов.Значение КАК СТРОКА(100)) = ВремИдДокументов.ИдентификаторДокумента
	|			И (ЗначенияСвойствОбъектов.Свойство В (&СвойстваИдентификаторовДокументов))
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов_Организации
	|		ПО ЗначенияСвойствОбъектов.Объект = ЗначенияСвойствОбъектов_Организации.Объект
	|			И (ЗначенияСвойствОбъектов_Организации.Свойство В (&СвойстваИдентификаторовЯщика))
	|;
	|
	|//////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВремВсеДокументы.Документ,
	|	ВремИдДокументов.ИдентификаторДокумента,
	|	ВремИдДокументов.АдресЯщика
	|ИЗ
	|	ВремИдДокументов КАК ВремИдДокументов
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВремВсеДокументы КАК ВремВсеДокументы
	|		ПО ВремИдДокументов.АдресЯщика = ВремВсеДокументы.АдресЯщика
	|			И ВремИдДокументов.ИдентификаторДокумента = ВремВсеДокументы.ИдентификаторДокумента";
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Запрос.Текст);
	
	Запрос.УстановитьПараметр("ВнешняяТаблица", ИдентификаторыДокументов);
	Запросы_ЗаполнитьПараметрыСвойствИдентификаторовДокумента(Запрос);
	
	РезультатЗапроса = Запрос.Выполнить();
	Результат = РезультатЗапроса.Выгрузить();

	Результат.Индексы.Добавить("АдресЯщика, ИдентификаторДокумента");
	
	МенеджерВременныхТаблиц.Закрыть();
	
	Возврат Результат;

КонецФункции

Процедура СписокДокументов_ДополнитьСписокИдентификаторамиВидовПакетов(СписокДокументов)
	
	СписокДокументов.Индексы.Добавить("Документ");
	
	ДокументыИБ = СписокДокументов.ВыгрузитьКолонку("Документ");
	Общее_УдалитьПустыеЭлементыМассива(ДокументыИБ);
	
	Выборка = ПолучитьВыборкуПоСвойствамДокументовУчета_1С(ДокументыИБ);
	Пока Выборка.Следующий() Цикл
		
		ПараметрыПоискаСтрок = Новый Структура("Документ", Выборка.Документ);
		СтрокиПоиска = СписокДокументов.НайтиСтроки(ПараметрыПоискаСтрок);
		
		Для Каждого СтрокаСписка Из СтрокиПоиска Цикл
			
			ВидПакетаID = ВидПакетаПоДокументуУчета_1С(
				СтрокаСписка.ТипДокумента,
				СтрокаСписка.Документ,
				СокрЛП(Выборка.ИдентификаторВидаПакетаСчет),
				СокрЛП(Выборка.ИдентификаторВидаПакета)
			);
			
			СтрокаСписка.ВидПакетаID = ВидПакетаID;
			ВидПакета = Списки_ОписаниеВидаПакета(ВидПакетаID);
			
			Если ВидПакета <> Неопределено Тогда
				СтрокаСписка.ВидПакета = ВидПакета.Наименование;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

Функция Тип_СчетНаОплату()

	Возврат "ProformaInvoice";

КонецФункции

Функция Тип_СчетНаОплатуФормализованный()
	Возврат "XmlProformaInvoice";
КонецФункции

Функция Тип_СчетОфертаНаОплатуФормализованный()
	Возврат "XmlInvoiceOffer";
КонецФункции

Функция ЭтоСчетНаОплату(ТипДокумента)
	
	ТипыСчетов = Новый Массив;
	ТипыСчетов.Добавить(Тип_СчетНаОплату());
	ТипыСчетов.Добавить(Тип_СчетНаОплатуФормализованный());
	ТипыСчетов.Добавить(Тип_СчетОфертаНаОплатуФормализованный());
	
	Результат = (ТипыСчетов.Найти(ТипДокумента) <> Неопределено);
	
	Возврат Результат;
	
КонецФункции

Функция Новый_ТаблицаДокументовАПИ()
	
	ТипЗначенияКолонки = Новый ОписаниеТипов("Строка", , , , Новый КвалификаторыСтроки(100));
	
	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("ИдентификаторДокумента", ТипЗначенияКолонки);
	Результат.Колонки.Добавить("АдресЯщика", ТипЗначенияКолонки);
	Результат.Колонки.Добавить("Документ");
	
	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_ДополненныеПараметрыНаОтправку_1С(Параметры)
	
	Если Параметры.ТаблицаКонтрагентов = Неопределено Тогда
		ВызватьИсключение НСтр("ru='Нет данных о сопоставленных контрагентах'");
	КонецЕсли;
	
	Результат = Параметры;
	
	Результат.Вставить("Свойство_IDОрганизации",					Неопределено);
	Результат.Вставить("Свойство_IDКонтрагента",					Неопределено);
	Результат.Вставить("Свойство_ВидПакета",						Неопределено);
	Результат.Вставить("Свойство_ОсновнойПакет",					Неопределено);
	Результат.Вставить("Свойство_ОтправлятьНепроведенныеДокументы",	Неопределено);
	Результат.Вставить("СвойстваИдентификаторовДокументов",			Новый Массив);
	
	ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_1С();
	
	Результат.Свойство_IDОрганизации					= Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.IDОрганизации, Ложь);
	Результат.Свойство_IDКонтрагента					= Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.IDКонтрагента, Ложь);
	Результат.Свойство_ВидПакета						= Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.ВидПакета);
	Результат.Свойство_ОсновнойПакет					= Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.ОсновнойПакет);
	Результат.Свойство_ОтправлятьНепроведенныеДокументы	= Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.ОтправлятьНепроведенныеДокументы, Ложь);
	
	Результат.СвойстваИдентификаторовДокументов.Добавить(Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.ИдентификаторДокумента, Ложь));
	Результат.СвойстваИдентификаторовДокументов.Добавить(Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.ИдентификаторСчетаУФ, Ложь));
	Результат.СвойстваИдентификаторовДокументов.Добавить(Свойства_СсылкаНаСвойство(ИменаСвойствНастроек.ИдентификаторСчетаОФ, Ложь));

	ТаблицаВидовПакетов = Параметры.ТаблицаВидовПакетов;
	
	ИмяТипаСвойств = "ПланВидовХарактеристикСсылка." + Свойства_ИмяПланаВидовХарактеристик();
	ТаблицаВидовПакетов.Колонки.Добавить("КатегорияПакетаСсылка", Новый ОписаниеТипов(ИмяТипаСвойств));
		
	Для Каждого ВидПакета Из ТаблицаВидовПакетов Цикл
		
		Если ЗначениеЗаполнено(ВидПакета.КатегорияПакета) Тогда
			ВидПакета.КатегорияПакетаСсылка = Свойства_СсылкаНаСвойство(ВидПакета.КатегорияПакета);
		КонецЕсли;
						
	КонецЦикла;
					
	Возврат Результат;
	
КонецФункции

Процедура СписокДокументов_ЗаполнитьСтрокуСпискаПоДокументу_1С(Результат, Документ)
	
	Результат.ВидПакетаID		= Документ.ИдентификаторВидаПакета;
	Результат.ВидЭлДокументаID	= Документ.ИдентификаторВида;
	Результат.ДокументЭДО		= Документ.Идентификаторы.ДокументЭДО;
	Результат.Документ			= Документ.ДокументУчета;
	
	Результат.ТипДокумента		= Документ.Тип.ТипДокумента;
	Результат.ФункцияДокумента	= Документ.Тип.ФункцияДокумента;
	
	Результат.Направление			= Документ.Направление;
	Результат.Документ				= Документ.ДокументУчета;
	Результат.Метка					= Документ.Состояние;
	Результат.ДатаОтправки      	= Документ.ДатаЭДО;
	Результат.ДатаПолучения      	= Документ.ДатаЭДО;
	Результат.ЗаблокированныйПакет	= Документ.Заблокирован;
	Результат.Шаблон				= Документ.Шаблон;
	Результат.BoxId					= Документ.Идентификаторы.BoxId;
	Результат.LetterId				= Документ.Идентификаторы.LetterId;
	Результат.DocumentId			= Документ.Идентификаторы.DocumentId;
	Результат.ТребуетсяРасшифровка	= СвойствоСтруктуры(Документ.СлужебнаяИнформация, "ТребуетсяРасшифровка", Ложь);
	
	Реквизиты = Документ.Реквизиты;
	Результат.ДатаДокумента		= Реквизиты.Дата;
	Результат.НомерДокумента	= Реквизиты.Номер;
	Результат.СуммаДокумента	= Реквизиты.Сумма;
	
	Статусы = Документ.Статусы;
	Результат.Статус = Статусы.Статус;
	Результат.ДополнительныйСтатус = Статусы.ДополнительныйСтатус;
	
	Результат.ОбъектМаршрутизации = Статусы.ОбъектМаршрутизации.Наименование;
	
	Результат.ВнешнийСтатус = Ядро_ПредставлениеВнешнихСтатусов(Статусы.ВнешниеСтатусы);
	Результат.СтатусМЧД = Статусы.СтатусМЧД;
	Результат.СтатусМЧДКритичность = Статусы.СтатусМЧДКритичность;
	
	Результат.СтатусПКФНС = Статусы.СтатусПКФНС;
	Результат.СтатусПКФНСКритичность = Статусы.СтатусПКФНСКритичность;
	
	Результат.СтатусТЭДО = Статусы.СтатусДТС;
	
	Результат.АнализПроведен		= Ложь;
	Результат.ОшибкиПроверкиТекст	= "";
	Результат.ДействиеВ1С			= "";
	Результат.ДействиеВДД			= "";
	Результат.ОшибкиПроверкиТекст	= "";
	
	Если Документ.Направление = Перечисление_Направления().Исходящее Тогда
		
		Результат.Организация	= Документ.Отправитель.Организация;
		Результат.Контрагент	= Документ.Получатель.Организация;
		Результат.КонтрагентТипОрганизации = Документ.Получатель.ТипОрганизации;
		Результат.ПредставлениеОрганизацииЭДО = СокрЛП(Документ.Отправитель.Наименование);
		Результат.ПредставлениеКонтрагентаЭДО = СокрЛП(Документ.Получатель.Наименование);
		
		ИдентификаторыОрганизации	= Новый_ИдентификаторыСправочника(, Документ.Отправитель.BoxId);
		ИдентификаторыПодразделения = Новый_ИдентификаторыСправочника(, Документ.Отправитель.DepartmentId);
		
		ДанныеВладельца = ХранениеДанных_ДанныеОрганизации(ИдентификаторыОрганизации);
		Подразделение	= Подразделения_ДанныеПодразделения_1С(ИдентификаторыПодразделения, ДанныеВладельца, Истина);
		Результат.Подразделение = Подразделение.Наименование;
		
		Результат.ПромежуточныйПолучатель	= Документ.ПромежуточныйПолучатель.Организация;
		Результат.ПредставлениеПромежуточногоПолучателяЭДО = СокрЛП(Документ.ПромежуточныйПолучатель.Наименование);
		
	ИначеЕсли Документ.Направление = Перечисление_Направления().Внутреннее Тогда
		
		Результат.Организация	= Документ.Отправитель.Организация;
		Результат.Контрагент	= Документ.Отправитель.Организация;
		Результат.КонтрагентТипОрганизации = Документ.Отправитель.ТипОрганизации;
		Результат.ПредставлениеОрганизацииЭДО = СокрЛП(Документ.Отправитель.Наименование);
		Результат.ПредставлениеКонтрагентаЭДО = СокрЛП(Документ.Отправитель.Наименование);
		
		ИдентификаторыОрганизации	= Новый_ИдентификаторыСправочника(, Документ.Получатель.BoxId);
		ИдентификаторыПодразделенияПолучателя = Новый_ИдентификаторыСправочника(, Документ.Получатель.DepartmentId);
		ИдентификаторыПодразделенияОтправителя = Новый_ИдентификаторыСправочника(, Документ.Отправитель.DepartmentId);
		
		ДанныеВладельца = ХранениеДанных_ДанныеОрганизации(ИдентификаторыОрганизации);
		ПодразделениеПолучатель = Подразделения_ДанныеПодразделения_1С(ИдентификаторыПодразделенияПолучателя, ДанныеВладельца, Истина); 
		ПодразделениеОтправитель = Подразделения_ДанныеПодразделения_1С(ИдентификаторыПодразделенияОтправителя, ДанныеВладельца, Истина);
		Подразделение	= Подразделения_ДанныеПодразделения_1С(ПодразделениеПолучатель, ДанныеВладельца, Истина);
		
		Результат.Подразделение = Подразделение.Наименование;
		Результат.ПодразделениеПолучатель = ПодразделениеПолучатель.Наименование;
		Результат.ПодразделениеОтправитель = ПодразделениеОтправитель.Наименование;
		
	Иначе
		
		Результат.Организация	= Документ.Получатель.Организация;
		Результат.Контрагент	= Документ.Отправитель.Организация;
		Результат.КонтрагентТипОрганизации = Документ.Отправитель.ТипОрганизации;
		Результат.ПредставлениеОрганизацииЭДО = СокрЛП(Документ.Получатель.Наименование);
		Результат.ПредставлениеКонтрагентаЭДО = СокрЛП(Документ.Отправитель.Наименование);
		
		ИдентификаторыОрганизации	= Новый_ИдентификаторыСправочника(, Документ.Получатель.BoxId);
		ИдентификаторыПодразделения = Новый_ИдентификаторыСправочника(, Документ.Получатель.DepartmentId);
		
		ДанныеВладельца = ХранениеДанных_ДанныеОрганизации(ИдентификаторыОрганизации);
		Подразделение	= Подразделения_ДанныеПодразделения_1С(ИдентификаторыПодразделения, ДанныеВладельца, Истина);
		Результат.Подразделение = Подразделение.Наименование;
		
		Результат.ПромежуточныйПолучатель	= Документ.ПромежуточныйПолучатель.Организация;
		Результат.ПредставлениеПромежуточногоПолучателяЭДО = СокрЛП(Документ.ПромежуточныйПолучатель.Наименование);
		
	КонецЕсли;
	
	ВидПакета = Списки_ОписаниеВидаПакета(Результат.ВидПакетаID);
	
	Если ВидПакета <> Неопределено Тогда
		Результат.ВидПакета	= ВидПакета.Наименование;
	КонецЕсли;
	
	ВидДокумента = Списки_ОписаниеВидаДокумента(Результат.ВидЭлДокументаID);
	
	Если ВидДокумента <> Неопределено Тогда
		Результат.ВидЭлДокумента = ВидДокумента.Наименование;
	КонецЕсли;
	
	Результат.ПредставлениеДокументаДиадок = Документ.ИмяФайла;
	Результат.ИмяФайла = Документ.Метаданные.FileName;
	
КонецПроцедуры

Функция СписокДокументов_ДокументПодходитПоСтатусу_1С(ДокументАПИ, ОтборПоСтатусу)
	
	Перем LastOuterDocflows;
	
	Результат = Истина;
	
	СтатусАПИ = ДокументАПИ.DocflowStatus.PrimaryStatus;
	
	Если СтатусАПИ.Свойство("StatusText") Тогда
		
		СтатусДокументаАПИ = СтатусАПИ.StatusText;
		
		Если ДокументАПИ.Свойство("LastOuterDocflows", LastOuterDocflows) Тогда
			
			СтатусДокументаАПИ = Ядро.ДокументAPI_СтатусСУчетомМодульнойДоставки(СтатусДокументаАПИ, LastOuterDocflows);
			
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ОтборПоСтатусу)
			И Найти(СтатусДокументаАПИ, ОтборПоСтатусу) = 0 Тогда
			
			Результат = Ложь;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура СписокДокументов_ДозаполнитьВидыПакетов_1С(СписокДокументов)
	
	ИдентификаторыВидовПакетов	= Новый Соответствие;
	НаименованияВидовПакетов	= Новый Соответствие;
	
	Для Каждого СтрокаСписка Из СписокДокументов Цикл
		
		Если ЗначениеЗаполнено(СтрокаСписка.ВидПакетаID) Тогда
			
			ИдентификаторыВидовПакетов.Вставить(СтрокаСписка.LetterId,  СтрокаСписка.ВидПакетаID);
			НаименованияВидовПакетов.Вставить(СтрокаСписка.LetterId, СтрокаСписка.ВидПакета);
			
		КонецЕсли;
		
	КонецЦикла;
	
	Для Каждого СтрокаСписка Из СписокДокументов Цикл
		
		Если НЕ ЗначениеЗаполнено(СтрокаСписка.ВидПакетаID) Тогда
			
			ВидПакетаID	= ИдентификаторыВидовПакетов.Получить(СтрокаСписка.LetterId);
			ВидПакета	= НаименованияВидовПакетов.Получить(СтрокаСписка.LetterId);
			
			Если ВидПакетаID <> Неопределено Тогда
				
				СтрокаСписка.ВидПакетаID	= ВидПакетаID;
				СтрокаСписка.ВидПакета		= ВидПакета;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция Ядро_ПредставлениеВнешнихСтатусов(ВнешниеСтатусы)
	
	Результат = Ядро.СписокДокументов_ПредставлениеВнешнихСтатусов(ВнешниеСтатусы);
	Возврат Результат;
	
КонецФункции

//Функция Ядро_ЭтоВидДокументаСчет(ВидДокументаID)
//	
//	ЭтоНеформализованныйСчет	= Ядро.ЭтоИдентификаторВидаНеформализованныйСчет(ВидДокументаID);
//	ЭтоФормализованныйСчет		= Ядро.ЭтоИдентификаторВидаФормализованныйСчет(ВидДокументаID);
//	
//	Результат = (ЭтоНеформализованныйСчет Или ЭтоФормализованныйСчет);
//	
//	Возврат Результат;
//	
//КонецФункции

//}		СПИСКИ ДОКУМЕНТОВ


///////////////////////////////////////////////////////////////////////////////
//{		СПРАВОЧНИКИ: ОРГАНИЗАЦИИ, КОНТРАГЕНТЫ, ПОДРАЗДЕЛЕНИЯ

Функция Организации_ПустаяСсылка() Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Организации_ПустаяСсылка_Диадок();
	Иначе
		Результат = Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ДанныеОрганизации(Идентификаторы) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Организации_ДанныеОрганизации_Диадок(Идентификаторы);
	Иначе
		Результат = Организации_ДанныеОрганизации_1С(Идентификаторы);
	КонецЕсли; 
	
	Возврат Результат;

КонецФункции

Функция Организации_СопоставленнаяВ1С(Идентификаторы) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Организации_СопоставленнаяВ1С_Диадок(Идентификаторы);
	Иначе
		Результат = Организации_СопоставленнаяВ1С_1С(Идентификаторы);
	КонецЕсли; 
	
	Возврат Результат;

КонецФункции

// Возвращает массив организаций 1С сопоставленных организации Диадок и номер связи 
//
// Параметры:
//	Идентификаторы - Структура - см. метод "Контракт_ИдентификаторыСправочника()"
//
// Возвращаемое значение:
// 	Массив структур
Функция Организации_СопоставленныеВ1С(Идентификаторы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Организации_СопоставленныеВ1С_Диадок(Идентификаторы);
	Иначе
		Результат = Организации_СопоставленныеВ1С_1С(Идентификаторы);	
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает массив организаций 1С сопоставленных организации Диадок
//
// Параметры:
//	Идентификаторы - Структура - см. метод "Контракт_ИдентификаторыСправочника()"
//
// Возвращаемое значение:
//	Массив
Функция Организации_МассивСопоставленныхВ1С(Идентификаторы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Организации_МассивСопоставленныхВ1С_Диадок(Идентификаторы);
	Иначе
		Результат = Организации_МассивСопоставленныхВ1С_1С(Идентификаторы);	
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Организации_СсылкаПоИдентификатору(Идентификаторы) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Организации_СсылкаПоИдентификатору_Диадок(Идентификаторы);
	Иначе
		Результат = Организации_СсылкаПоИдентификатору_1С(Идентификаторы);
	КонецЕсли; 
	
	Возврат Результат;

КонецФункции

Функция Организации_УчастникДО(Идентификаторы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Организации_УчастникДО_Диадок(Идентификаторы);
	Иначе
		Результат = Организации_УчастникДО_1С(Идентификаторы);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает идентификатор ящика организации в сервисе Диадок.
//
// Параметры:
//  Идентификаторы - Структура - см. Ядро.Новый_ИдентификаторыСправочника
// 
// Возвращаемое значение:
//  Строка - BoxID или пустое значение, если организация не сопоставлена
//
Функция Организации_ИдентификаторПоСсылке(Идентификаторы) Экспорт
	
	Результат = Новый_ИдентификаторыСправочника();
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Организации_ИдентификаторПоСсылке_Диадок(Идентификаторы); 
	Иначе 
		Результат = Организации_ИдентификаторПоСсылке_1С(Идентификаторы);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Организации_Записать(Организация) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Организации_Записать_Диадок(Организация);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Организации_НастройкиСертификатовНаСервере1С() Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Организации_НастройкиСертификатовНаСервере1С_Диадок();
	Иначе
		Результат = Организации_НастройкиСертификатовНаСервере1С_1С();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ТаблицаСопоставленныхОрганизаций() Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Организации_ТаблицаСопоставленныхОрганизаций_Диадок();
	Иначе
		Результат = Организации_ТаблицаСопоставленныхОрганизаций_1С();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции	

Процедура Организации_ОбновлениеРеквизитов(Организация, СтруктураРеквизитов) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Организации_ОбновлениеРеквизитов_Диадок(Организация, СтруктураРеквизитов);
	Иначе
		Организации_ОбновлениеРеквизитов_1С(Организация, СтруктураРеквизитов);
	КонецЕсли; 
	
КонецПроцедуры

Функция Контрагенты_ДанныеКонтрагента(Идентификаторы) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_ДанныеКонтрагента_Диадок(Идентификаторы);
	Иначе
		Результат = Контрагенты_ДанныеКонтрагента_1С(Идентификаторы);
	КонецЕсли; 
	
	Возврат Результат;

КонецФункции

Функция Контрагенты_СопоставленныйВ1С(Идентификаторы) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_СопоставленныйВ1С_Диадок(Идентификаторы);
	Иначе	
		Результат = Контрагенты_СопоставленныйВ1С_1С(Идентификаторы);
	КонецЕсли; 
	
	Возврат Результат;

КонецФункции

// Возвращает массив контрагентов 1С сопоставленных контрагенту Диадок и номер связи 
//
// Параметры:
//	Идентификаторы - Структура - см. метод "Контракт_ИдентификаторыСправочника()"
//
// Возвращаемое значение:
// 	Массив структур
Функция Контрагенты_СопоставленныеВ1С(Идентификаторы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_СопоставленныеВ1С_Диадок(Идентификаторы);
	Иначе
		Результат = Контрагенты_СопоставленныеВ1С_1С(Идентификаторы);	
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает массив контрагентов 1С сопоставленных контрагенту Диадок
//
// Параметры:
//	Идентификаторы - Структура - см. метод "Контракт_ИдентификаторыСправочника()"
//
// Возвращаемое значение:
//	Массив
Функция Контрагенты_МассивСопоставленныхВ1С(Идентификаторы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_МассивСопоставленныхВ1С_Диадок(Идентификаторы);
	Иначе
		Результат = Контрагенты_МассивСопоставленныхВ1С_1С(Идентификаторы);	
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_УчастникДО(ИдентификаторыКонтрагента, ИдентификаторыОрганизации) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_УчастникДО_Диадок(ИдентификаторыКонтрагента, ИдентификаторыОрганизации);
	Иначе
		Результат = Контрагенты_УчастникДО_1С(ИдентификаторыКонтрагента, ИдентификаторыОрганизации);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ПустаяСсылка() Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_ПустаяСсылка_Диадок();
	Иначе
		Результат = Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_НастройкиПоОрганизациям(Идентификаторы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_НастройкиПоОрганизациям_Диадок(Идентификаторы);
	Иначе
		Результат = Новый ТаблицаЗначений;
	КонецЕсли; 
	
	Возврат Результат;
	
КонецФункции

Процедура Контрагенты_Записать(Контрагент) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Контрагенты_Записать_Диадок(Контрагент);
	КонецЕсли;
	
КонецПроцедуры

Процедура Контрагенты_ОбновлениеРеквизитов(Идентификаторы, СтруктураРеквизитов) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Контрагенты_ОбновлениеРеквизитов_Диадок(Идентификаторы, СтруктураРеквизитов);
	Иначе
		Контрагенты_ОбновлениеРеквизитов_1С(Идентификаторы, СтруктураРеквизитов);
	КонецЕсли; 
	
КонецПроцедуры

Процедура Контрагенты_ЗаписатьНастройкиПоОрганизациям(Контрагент, НастройкиПоОрганизациям) Экспорт 
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Контрагенты_ЗаписатьНастройкиПоОрганизациям_Диадок(Контрагент, НастройкиПоОрганизациям);
	КонецЕсли;
	
КонецПроцедуры

// Возвращает идентификатор ящика контрагента в сервисе Диадок.
//
// Параметры:
//  Идентификаторы - Структура - см. Ядро.Новый_ИдентификаторыСправочника
// 
// Возвращаемое значение:
//  Строка - BoxID или пустое значение, если контрагент не сопоставлен
//
Функция Контрагенты_ИдентификаторПоСсылке(Идентификаторы) Экспорт
	
	Результат = "";
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_ИдентификаторПоСсылке_Диадок(Идентификаторы)  
	Иначе
		Результат = Контрагенты_ИдентификаторПоСсылке_1С(Идентификаторы);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ИдентификаторПоСсылке_Диадок(ИдентификаторыКонтрагента) 
	
	УчастникДО = Контрагенты_УчастникДО_Диадок(ИдентификаторыКонтрагента, Неопределено);
	
	Результат = УчастникДО.BoxId;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_СконвертироватьСоответствиеКонтрагентов() Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_СконвертироватьСоответствиеКонтрагентов_Диадок();
	Иначе
		Результат = Новый Массив;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_УчастникДО_ФНС(ИдентификаторФНС, ИдентификаторыОрганизации) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_УчастникДО_ФНС_Диадок(ИдентификаторФНС, ИдентификаторыОрганизации);
	Иначе
		Результат = Контрагенты_УчастникДО_ФНС_1С(ИдентификаторФНС, ИдентификаторыОрганизации);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_УчастникДО_ФНС_1С(ИдентификаторФНС, ИдентификаторыОрганизации)
	
	Результат = Контракт_УчастникДокументооборота();
	
	Если ЗначениеЗаполнено(ИдентификаторФНС) Тогда
		
		СтруктураКонтрагента = Контрагенты_РеквизитыКонтрагентаПоФНС_АПИ(ИдентификаторФНС);
		
		BoxID = ИдентификаторВАдресЯщика(СтруктураКонтрагента.ID);
		Идентификаторы = Новый_ИдентификаторыСправочника(, СтруктураКонтрагента.ID);
		
		Результат.BoxId				= BoxID;
		Результат.Наименование		= СтруктураКонтрагента.Наименование;
		Результат.FNSParticipantId	= СтруктураКонтрагента.ИдентификаторУчастникаЭДО;
		Результат.ТипОрганизации	= СтруктураКонтрагента.Тип;
		
		КонтрагентыВ1С = Контрагенты_СопоставленныеВ1С(Идентификаторы);
		
		Если ЗначениеЗаполнено(КонтрагентыВ1С) Тогда
			
			КонтрагентВ1С = КонтрагентыВ1С[0].Ссылка;
			
			Результат.Организация	= КонтрагентВ1С;
			Результат.DepartmentId =
				ДопРеквизиты_Значение_1С(
					КонтрагентВ1С,
					Перечисление_ИменаСвойствНастроек_1С().IDОсновноеПодразделениеКонтрагента,
					Ложь);
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_УчастникДО_ФНС_Диадок(ИдентификаторФНС, ИдентификаторыОрганизации)
	
	Результат = Контракт_УчастникДокументооборота();
	
	Запрос = Новый Запрос;
	Запрос.Текст = Контрагенты_ТекстЗапросаУчастникаЭДО_ФНС_Диадок(ИдентификаторФНС, ИдентификаторыОрганизации);
	
	Запрос.УстановитьПараметр("ИдентификаторУчастникаЭДО", ИдентификаторФНС);
	Запрос.УстановитьПараметр("Статус_ПартнерскиеОтношения", Перечисление_СтатусыКонтрагентов().ПартнерскиеОтношения);
	Запрос.УстановитьПараметр("ШаблонИмениРеквизита_СвязанныйСправочник",
		ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник());
	
	Если ИдентификаторыОрганизации <> Неопределено Тогда
		
		Запрос.УстановитьПараметр("ОрганизацияID", ИдентификаторВАдресЯщика(ИдентификаторыОрганизации.ID));
		Запрос.УстановитьПараметр("Организация", ИдентификаторыОрганизации.Ссылка);
		
	КонецЕсли;
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Если ВыборкаЗапроса.Следующий() Тогда
		ЗаполнитьЗначенияСвойств(Результат, ВыборкаЗапроса);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапросаУчастникаЭДО_ФНС_Диадок(ИдентификаторФНС, ИдентификаторыОрганизации)
	
	Если ИдентификаторыОрганизации = Неопределено Тогда
		ОтборОрганизация = "Истина";
	ИначеЕсли ЗначениеЗаполнено(ИдентификаторыОрганизации.Ссылка) Тогда
		ОтборОрганизация = "Диадок_НастройкиКонтрагентов.Организация = &Организация";
	Иначе
		ОтборОрганизация = "Диадок_НастройкиКонтрагентов.Организация.ID = &ОрганизацияID";
	КонецЕсли;
	
	ОтборКонтрагент = "Диадок_ЮрФизЛица.ИмяСправочника = ""Контрагенты""";
	ОтборКонтрагент = ОтборКонтрагент + " И Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО = &ИдентификаторУчастникаЭДО";
		
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		Результат = Контрагенты_ТекстЗапроса_УчастникЭДО_ФНС_ДиадокРасширение();
	Иначе
		Результат = Контрагенты_ТекстЗапроса_УчастникЭДО_ФНС_ДиадокСХ1();
	КонецЕсли;
	
	Результат = Результат + ТекстЗапроса_РазделительЗапросов()
				+ Контрагенты_ТекстЗапросаДляОтбораКонтрагентовПоСтатусуПартнерскихОтношений_Диадок();
	
	Результат = СтрЗаменить(Результат, "&ОтборКонтрагент",	ОтборКонтрагент);
	Результат = СтрЗаменить(Результат, "&ОтборОрганизация",	ОтборОрганизация);
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапроса_УчастникЭДО_ФНС_ДиадокСХ1()
	// BSLLS:UnionAll-off
	Результат =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.ID КАК BoxId,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.Ссылка КАК ОрганизацияЭДО,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник1 КАК Организация,
	|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_Тип.Значение, """") КАК ТипОрганизации,
	|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО КАК FNSParticipantId,
	|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение КАК DepartmentId
	|ПОМЕСТИТЬ ВТ_Контрагенты
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_Тип
	|		ПО (ЮрФизЛица_ДопРеквизиты_Тип.Ссылка = Диадок_ЮрФизЛица.Ссылка)
	|			И (ЮрФизЛица_ДопРеквизиты_Тип.ИмяРеквизита = ""Тип"")
	|ГДЕ
	|	&ОтборКонтрагент
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	// BSLLS:UnionAll-on
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапроса_УчастникЭДО_ФНС_ДиадокРасширение()
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.ID КАК BoxId,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.Ссылка КАК ОрганизацияЭДО,
	|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_Тип.Значение, """") КАК ТипОрганизации,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК Организация,
	|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО КАК FNSParticipantId,
	|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение КАК DepartmentId
	|ПОМЕСТИТЬ ВТ_Контрагенты
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ПО (Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка)
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита = ""СвязанныйСправочник1"")
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_Тип
	|		ПО (ЮрФизЛица_ДопРеквизиты_Тип.Ссылка = Диадок_ЮрФизЛица.Ссылка)
	|			И (ЮрФизЛица_ДопРеквизиты_Тип.ИмяРеквизита = ""Тип"")
	|ГДЕ
	|	&ОтборКонтрагент
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции

Функция Подразделения_ДанныеПодразделения(Идентификаторы, ДанныеВладельца, ДляОрганизации) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Подразделения_ДанныеПодразделения_Диадок(Идентификаторы, ДляОрганизации);
	Иначе
		Результат = Подразделения_ДанныеПодразделения_1С(Идентификаторы, ДанныеВладельца, ДляОрганизации);
	КонецЕсли; 
	
	Возврат Результат;

КонецФункции

Функция Подразделения_СопоставленныйВ1С(Идентификаторы, ДляОрганизации = Истина) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Подразделения_СопоставленныйВ1С_Диадок(Идентификаторы, ДляОрганизации);
	Иначе
		Результат = Подразделения_СопоставленныйВ1С_1С(Идентификаторы, ДляОрганизации);
	КонецЕсли; 
	
	Возврат Результат;

КонецФункции

// Возвращает идентификатор подразделения в Диадоке, с которым сопоставлен указанный объект ИБ.
//
// Папаметры:
//  СвязанныеСправочники - Массив - объекты ИБ сопоставленные с подразделениями организации или контрагента
//  IdВладельца - Строка - идентификатор ящика организации или контрагента
//  ДляОрганизации - Булево - Истина для получения подразделения организации, Ложь для получения подразделения контрагента
//
// Возвращаемое значение:
//  Строка - идентификатор подразделения в организации или контрагента.
//
Функция Подразделения_ИдентификаторПоСвязаннымСправочникам(СвязанныйСправочник, IdВладельца, ДляОрганизации = Истина) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Подразделения_ИдентификаторПоСвязаннымСправочникам_Диадок(IdВладельца, СвязанныйСправочник, ДляОрганизации);
	Иначе
		Результат = Подразделения_ИдентификаторПоСвязаннымСправочникам_1С(СвязанныйСправочник, ДляОрганизации);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Подразделения_СсылкаПоИдентификатору(Идентификаторы, ДляОрганизации = Истина) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Подразделения_СсылкаПоИдентификатору_Диадок(Идентификаторы, ДляОрганизации);
	КонецЕсли; 
	
	Возврат Результат;

КонецФункции

Функция Подразделения_ТаблицаПодразделенийВладельца(ДанныеВладельца, ДанныеОрганизации = Неопределено) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Подразделения_ТаблицаПодразделенийВладельца_Диадок(ДанныеВладельца, ДанныеОрганизации);
	Иначе
		Результат = Подразделения_ТаблицаПодразделенийВладельца_1С(ДанныеВладельца, ДанныеОрганизации);
	КонецЕсли; 
	
	Возврат Результат;
	
КонецФункции

Процедура Подразделения_Записать(Подразделение, Владелец) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Подразделения_Записать_Диадок(Подразделение, Владелец);
	КонецЕсли;
	
КонецПроцедуры

Процедура Подразделения_ОбновлениеРеквизитов(Подразделение, СтруктураРеквизитов, ДляОрганизации = Истина) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Подразделения_ОбновлениеРеквизитов_Диадок(Подразделение, СтруктураРеквизитов);
	Иначе
		Подразделения_ОбновлениеРеквизитов_1С(Подразделение, СтруктураРеквизитов, ДляОрганизации);
	КонецЕсли; 
	
КонецПроцедуры

// Возвращает массив подразделений 1С сопоставленных подразделению Диадок и номер связи 
//
// Параметры:
//	Идентификаторы 	- Структура - см. метод "Контракт_ИдентификаторыСправочника()"
//	ДляОрганизации	- Булево	- признак является ли подразделение подразделением оргнадизации
//
// Возвращаемое значение:
// 	Массив структур
Функция Подразделения_СопоставленныеВ1С(Идентификаторы, ДляОрганизации = Истина) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Подразделения_СопоставленныеВ1С_Диадок(Идентификаторы, ДляОрганизации);
	Иначе
		Результат = Подразделения_СопоставленныеВ1С_1С(Идентификаторы, ДляОрганизации);	
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Обновляет информацию по подразделениям юр./физ. лица по данным сервиса
//
// Параметры:
// 	ТаблицаКонтрактов 	- ТаблицаЗначений 	- см. Модуль_Ядро.Подразделения_ТаблицаКонтрактовПоDepartments()
//	Владелец 			- Структура 		- см. Организации_ДанныеОрганизации()
//
Процедура Подразделения_ОбновитьСправочник(ТаблицаКонтрактов, Владелец) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Подразделения_ОбновитьСправочник_Диадок(ТаблицаКонтрактов, Владелец);
	КонецЕсли; 

КонецПроцедуры

// Обновляет данные по подразделениям в справочнике по таблице контрактов
//
// Параметры:
//	Подразделения 			 - ТаблицаЗначений - данные подразделений контрагентов (см. Модуль_Ядро.Подразделения_НоваяТаблицаКонтрактов)
//	ИдентификаторыВладельцев - ТаблицаЗначений - содержит идентификаторы контрагентов, владельцев подразделений.
//												 Обязательная колонка ID. Например, таблица контрактов контрагентов.
//												 (см. метод Модуль_Ядро.Контрагенты_НоваяТаблицаКонтрактов)
//
Процедура Подразделения_ВыполнитьПакетноеОбновлениеСправочника(Подразделения, ИдентификаторыВладельцев) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Подразделения_ВыполнитьПакетноеОбновлениеСправочника_Диадок(Подразделения, ИдентификаторыВладельцев);
	КонецЕсли;
	
КонецПроцедуры

//**********************************************
// СПРАВОЧНИКИ: ПОДСИСТЕМА ДИАДОКА

// Приводит значения ключей организации к формату хранения.
// Идентификатор ящика организации записывается в базу в формате
// адреса электронной почты, а во входящем параметре идентификатор
// ящика может быть указан в формате уникального идентификатора
//
// Параметры:
//  Идентификаторы - Структура - Новый_ИдентификаторыСправочника
// 
// Возвращаемое значение:
//  Структура - Новый_ИдентификаторыСправочника, где ID в формате e-mail
//
Функция ИдентификаторыОрганизации(Знач Идентификаторы)
	
	Если Не ЗначениеЗаполнено(Идентификаторы.ID)
		Или Найти(Идентификаторы.ID, "@") > 0 Тогда
		Возврат Идентификаторы;
	КонецЕсли;
	
	АдресЯщика = ИдентификаторВАдресЯщика(Идентификаторы.ID);
	Результат = Новый_ИдентификаторыСправочника(Идентификаторы.Ссылка, АдресЯщика);
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ПустаяСсылка_Диадок()
	
	Результат = Справочники.Диадок_ЮрФизЛица.ПустаяСсылка();
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ДанныеОрганизации_Диадок(Идентификаторы)
	
	Результат = Контракт_ОрганизацияЭДО();
	ВыборкаДанных = Справочники_ВыборкаДанныхСправочника_Диадок("Организации", Идентификаторы);
	
	Если ВыборкаДанных <> Неопределено Тогда
		
		Результат.ID = ВыборкаДанных.ID;
		Результат.ИНН = ВыборкаДанных.ИНН;
		Результат.КПП = ВыборкаДанных.КПП;
		Результат.Наименование = ВыборкаДанных.Наименование;
		Результат.Ссылка = ВыборкаДанных.Ссылка;
		Результат.СвязанныйСправочник1 = ВыборкаДанных.СвязанныйСправочник1;
		Результат.ID_ОсновноеПодразделение = ВыборкаДанных.ID_ОсновноеПодразделение;
		Результат.ИдентификаторУчастникаЭДО = ВыборкаДанных.ИдентификаторУчастникаЭДО;
		
		ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_Диадок();
		
		МассивДопРеквизитов = Новый Массив;
		МассивДопРеквизитов.Добавить(ИменаСвойствНастроек.AfterIndexKey);
		МассивДопРеквизитов.Добавить(ИменаСвойствНастроек.ДатаГраницыСинхронизации);
		МассивДопРеквизитов.Добавить(ИменаСвойствНастроек.ОтпечатокСертификатаНаСервере);
		МассивДопРеквизитов.Добавить(ИменаСвойствНастроек.OrgId);
		МассивДопРеквизитов.Добавить(ИменаСвойствНастроек.УказыватьОтсутствиеОснованияУПД);
		МассивДопРеквизитов.Добавить(ИменаСвойствНастроек.ОтправлятьНепроведенныеДокументы);
		МассивДопРеквизитов.Добавить(ИменаСвойствНастроек.ЗаписыватьКомментарииВДокументы1С);
		МассивДопРеквизитов.Добавить(ИменаСвойствНастроек.ТекстКомментарияОтправка);
		МассивДопРеквизитов.Добавить(ИменаСвойствНастроек.ТекстКомментарияПолучение);
		МассивДопРеквизитов.Добавить(ИменаСвойствНастроек.ДатаСинхронизацииСтатусовКонтрагентов);
		МассивДопРеквизитов.Добавить("Тип");
		МассивДопРеквизитов.Добавить("Экстраверт");
		
		ИменаДопРеквизитов = СоединитьСтроку(МассивДопРеквизитов, ",");
		ДопРеквизиты = ДопРеквизиты_СписокРеквизитов_Диадок(Результат.Ссылка, ИменаДопРеквизитов);
		ЗаполнитьЗначенияСвойств(Результат, ДопРеквизиты);
		
		Результат.ЗаписыватьКомментарииВДокументы1С =
			?(Результат.ЗаписыватьКомментарииВДокументы1С = Неопределено,
				Ложь,
				Результат.ЗаписыватьКомментарииВДокументы1С);
		Результат.УказыватьОтсутствиеОснованияУПД =
			?(Результат.УказыватьОтсутствиеОснованияУПД = Неопределено,
				Ложь,
				Результат.УказыватьОтсутствиеОснованияУПД);
		Результат.ОтправлятьНепроведенныеДокументы =
			?(Результат.ОтправлятьНепроведенныеДокументы = Неопределено,
				Ложь,
				Результат.ОтправлятьНепроведенныеДокументы);
		Результат.ДатаСинхронизацииСтатусовКонтрагентов =
			?(Результат.ДатаСинхронизацииСтатусовКонтрагентов = Неопределено,
				Дата("00010101"),
				Результат.ДатаСинхронизацииСтатусовКонтрагентов);
		Результат.Экстраверт =
			?(Результат.Экстраверт = Неопределено,
				Ложь,
				Результат.Экстраверт);
		
		Результат.ОсновнойПакет = ДопРеквизиты_ЗначениеОсновногоПакета_Диадок(Результат.Ссылка);
		
		Если НЕ ЗначениеЗаполнено(Идентификаторы.Ссылка) Тогда
			Идентификаторы.Ссылка = Результат.Ссылка;
		КонецЕсли;
		
		Результат.ШифроватьДокументы = ДопРеквизиты_ЗначениеШифрованияДокумента_Диадок(Идентификаторы, Новый Структура);
		Результат.ШифроватьДокументы = ?(Результат.ШифроватьДокументы = Неопределено, Ложь, Результат.ШифроватьДокументы);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Организации_СопоставленнаяВ1С_Диадок(Идентификаторы)

	Результат = Справочники_СвязанныйСправочник_Диадок("Организации", Идентификаторы);
	
	Возврат Результат;
	
КонецФункции

Функция Организации_СопоставленныеВ1С_Диадок(ИсходныеИдентификаторы)
	
	Идентификаторы = ИдентификаторыОрганизации(ИсходныеИдентификаторы);
	
	ИмяСправочника = Перечисление_ИменаСправочников_Диадок().Организации;
	
	Результат = Справочники_СопоставленныеВ1С_Диадок(ИмяСправочника, Идентификаторы);
	
	Возврат Результат;
	
КонецФункции

Функция Организации_МассивСопоставленныхВ1С_Диадок(ИсходныеИдентификаторы)
	
	Идентификаторы = ИдентификаторыОрганизации(ИсходныеИдентификаторы);
	
	Результат = Новый Массив;
	
	ИмяСправочника 	= Перечисление_ИменаСправочников_Диадок().Организации;
	ИмяСвойства 	= Перечисление_ИменаСвойствНастроек_Диадок().СвязанныйСправочник;
	
	СвязанныеСправочники = Справочники_СвязанныеСправочники_Диадок(ИмяСправочника, Идентификаторы);
	
	Для каждого ЭлементТЗ Из СвязанныеСправочники Цикл
		
		Если ЗначениеЗаполнено(ЭлементТЗ.ОбъектИБ) Тогда
			Результат.Добавить(ЭлементТЗ.ОбъектИБ);
		КонецЕсли;
		
	КонецЦикла;
			
	Возврат Результат;
	
КонецФункции

Функция Организации_СсылкаПоИдентификатору_Диадок(Идентификаторы)

	Результат = Справочники_СсылкаПоИдентификатору_Диадок(Идентификаторы, "Организации");
	
	Возврат Результат;

КонецФункции

Функция Организации_ИдентификаторПоСсылке_Диадок(Идентификаторы)
	
	УчастникДО = Организации_УчастникДО_Диадок(Идентификаторы);
	
	Результат = УчастникДО.BoxId;
	
	Возврат Результат;
	
КонецФункции

Функция Организации_УчастникДО_Диадок(Идентификаторы)
	
	Результат = Контракт_УчастникДокументооборота();
	
	Запрос = Новый Запрос;
	Запрос.Текст = Организации_ТекстЗапросаУчастникаЭДО_Диадок(Идентификаторы);
	
	Запрос.УстановитьПараметр("ID",		ИдентификаторВАдресЯщика(Идентификаторы.ID));
	Запрос.УстановитьПараметр("Ссылка",	Идентификаторы.Ссылка);
	Запрос.УстановитьПараметр("ШаблонИмениРеквизита_СвязанныйСправочник", ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник());
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Если ВыборкаЗапроса.Следующий() Тогда
		ЗаполнитьЗначенияСвойств(Результат, ВыборкаЗапроса);
	Иначе	
		Результат.BoxId				= Идентификаторы.ID;
		Результат.ОрганизацияЭДО	= Идентификаторы.Ссылка;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ТекстЗапросаУчастникаЭДО_Диадок(Идентификаторы)
	
	ОрганизацияСсылка = Идентификаторы.Ссылка;
	Отбор = "Диадок_ЮрФизЛица.ИмяСправочника = ""Организации""";
	ОтборПоСопоставленнойОрганизации = Ложь;
	
	Если ТипЗнч(ОрганизацияСсылка) = ТипЗнч(Организации_ПустаяСсылка_Диадок()) Тогда
		
		Отбор = Отбор + " И Диадок_ЮрФизЛица.Ссылка = &Ссылка";
		
	ИначеЕсли ЗначениеЗаполнено(ОрганизацияСсылка) Тогда
		
		ОтборПоСопоставленнойОрганизации = Истина;
		
	Иначе
		Отбор = Отбор + " И Диадок_ЮрФизЛица.ID = &ID";
	КонецЕсли;
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		Результат = Организации_ТекстЗапроса_УчастникЭДО_ДиадокРасширение(ОтборПоСопоставленнойОрганизации);
	Иначе
		Результат = Организации_ТекстЗапроса_УчастникЭДО_ДиадокСХ1(ОтборПоСопоставленнойОрганизации);
	КонецЕсли;
	
	Результат = СтрЗаменить(Результат, "&Отбор", Отбор);
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ТекстЗапроса_УчастникЭДО_ДиадокРасширение(ОтборПоСопоставленнойОрганизации)
	
	Если ОтборПоСопоставленнойОрганизации Тогда
		Результат = Организации_ТекстЗапроса_УчастникЭДОПоСопоставленнойСвязи_ДиадокРасширение();
	Иначе
		Результат = Организации_ТекстЗапроса_УчастникЭДОПоИдентификатору_ДиадокРасширение();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ТекстЗапроса_УчастникЭДОПоСопоставленнойСвязи_ДиадокРасширение()
	
	Результат = 
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.ID КАК BoxId,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.Ссылка КАК ОрганизацияЭДО,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК Организация,
	|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_Тип.Значение, """") КАК ТипОрганизации,
	|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО КАК FNSParticipantId,
	|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение КАК DepartmentId
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка
	|			И (Диадок_ЮрФизЛица.ИмяСправочника = ""Организации"")
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_Тип
	|		ПО (ЮрФизЛица_ДопРеквизиты_Тип.Ссылка = Диадок_ЮрФизЛица.Ссылка)
	|			И (ЮрФизЛица_ДопРеквизиты_Тип.ИмяРеквизита = ""Тип"")
	|ГДЕ
	|   &Отбор
	|   И НЕ Диадок_ЮрФизЛица.ПометкаУдаления
	|	И Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ШаблонИмениРеквизита_СвязанныйСправочник
	|	И Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение = &Ссылка";
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ТекстЗапроса_УчастникЭДОПоИдентификатору_ДиадокРасширение()
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.ID КАК BoxId,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.Ссылка КАК ОрганизацияЭДО,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК Организация,
	|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_Тип.Значение, """") КАК ТипОрганизации,
	|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО КАК FNSParticipantId,
	|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение КАК DepartmentId
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ПО (Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка)
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита = ""СвязанныйСправочник1"")
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_Тип
	|		ПО (ЮрФизЛица_ДопРеквизиты_Тип.Ссылка = Диадок_ЮрФизЛица.Ссылка)
	|			И (ЮрФизЛица_ДопРеквизиты_Тип.ИмяРеквизита = ""Тип"")
	|ГДЕ
	|	&Отбор
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ТекстЗапроса_УчастникЭДО_ДиадокСХ1(ОтборПоСопоставленнойОрганизации)
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.ID КАК BoxId,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.Ссылка КАК ОрганизацияЭДО,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник1 КАК Организация,
	|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_Тип.Значение, """") КАК ТипОрганизации,
	|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО КАК FNSParticipantId,
	|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение КАК DepartmentId
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_Тип
	|		ПО (ЮрФизЛица_ДопРеквизиты_Тип.Ссылка = Диадок_ЮрФизЛица.Ссылка)
	|			И (ЮрФизЛица_ДопРеквизиты_Тип.ИмяРеквизита = ""Тип"")
	|ГДЕ
	|	&Отбор
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Если ОтборПоСопоставленнойОрганизации Тогда
		
		Результат = Результат + "
		| И (Диадок_ЮрФизЛица.СвязанныйСправочник1 = &Ссылка
		| 	ИЛИ Диадок_ЮрФизЛица.СвязанныйСправочник2 = &Ссылка
		| 	ИЛИ Диадок_ЮрФизЛица.СвязанныйСправочник3 = &Ссылка)
		|
		|ОБЪЕДИНИТЬ
		|
		|ВЫБРАТЬ
		|	Диадок_ЮрФизЛица.ID,
		|	Диадок_ЮрФизЛица.Наименование,
		|	Диадок_ЮрФизЛица.Ссылка,
		|	Диадок_ЮрФизЛица.СвязанныйСправочник1,
		|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_Тип.Значение, """") КАК ТипОрганизации,
		|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО,
		|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение
		|ИЗ
		|	Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
		|		ПО Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка
		|			И (Диадок_ЮрФизЛица.ИмяСправочника = ""Организации"")
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_Тип
		|		ПО (ЮрФизЛица_ДопРеквизиты_Тип.Ссылка = Диадок_ЮрФизЛица.Ссылка)
		|			И (ЮрФизЛица_ДопРеквизиты_Тип.ИмяРеквизита = ""Тип"")
		|ГДЕ
		|   НЕ Диадок_ЮрФизЛица.ПометкаУдаления
		|	И Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ШаблонИмениРеквизита_СвязанныйСправочник
		|	И Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение = &Ссылка";
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции


Функция Организации_Записать_Диадок(Организация)
	
	ОрганизацияСсылка = Организация.Ссылка;
	
	Если ЗначениеЗаполнено(ОрганизацияСсылка) Тогда
		ЮрЛицо = ОрганизацияСсылка.ПолучитьОбъект();
	Иначе
		ЮрЛицо = Справочники.Диадок_ЮрФизЛица.СоздатьЭлемент();
	КонецЕсли;
	
	ЮрЛицо.ИмяСправочника = "Организации";
	ЮрЛицо.ID = Организация.ID;
	ЮрЛицо.Наименование = Организация.Наименование;
	ЮрЛицо.ИНН = Организация.ИНН;
	ЮрЛицо.КПП = Организация.КПП;
	ЮрЛицо.ID_ОсновноеПодразделение = Организация.ID_ОсновноеПодразделение;
	ЮрЛицо.ИдентификаторУчастникаЭДО = Организация.ИдентификаторУчастникаЭДО;
	
	ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_Диадок();
	
	ДопРеквизиты_УстановитьЗначение(
		ЮрЛицо,
		ИменаСвойствНастроек.AfterIndexKey,
		Организация.AfterIndexKey);
	ДопРеквизиты_УстановитьЗначение(
		ЮрЛицо,
		ИменаСвойствНастроек.ДатаГраницыСинхронизации,
		Организация.ДатаГраницыСинхронизации);
	ДопРеквизиты_УстановитьЗначение(
		ЮрЛицо,
		ИменаСвойствНастроек.ОтпечатокСертификатаНаСервере,
		Организация.ОтпечатокСертификатаНаСервере);
	ДопРеквизиты_УстановитьЗначение(
		ЮрЛицо,
		ИменаСвойствНастроек.OrgId,
		Организация.OrgId);
	ДопРеквизиты_УстановитьЗначение(
		ЮрЛицо,
		ИменаСвойствНастроек.УказыватьОтсутствиеОснованияУПД,
		Организация.УказыватьОтсутствиеОснованияУПД);
	ДопРеквизиты_УстановитьЗначение(
		ЮрЛицо,
		ИменаСвойствНастроек.ОтправлятьНепроведенныеДокументы,
		Организация.ОтправлятьНепроведенныеДокументы);
	ДопРеквизиты_УстановитьЗначение(
		ЮрЛицо,
		ИменаСвойствНастроек.ЗаписыватьКомментарииВДокументы1С,
		Организация.ЗаписыватьКомментарииВДокументы1С);
	ДопРеквизиты_УстановитьЗначение(
		ЮрЛицо,
		ИменаСвойствНастроек.ТекстКомментарияОтправка,
		Организация.ТекстКомментарияОтправка);
	ДопРеквизиты_УстановитьЗначение(
		ЮрЛицо,
		ИменаСвойствНастроек.ТекстКомментарияПолучение,
		Организация.ТекстКомментарияПолучение);
	ДопРеквизиты_УстановитьЗначение(
		ЮрЛицо,
		ИменаСвойствНастроек.ДатаСинхронизацииСтатусовКонтрагентов,
		Организация.ДатаСинхронизацииСтатусовКонтрагентов);
	ДопРеквизиты_УстановитьЗначение(ЮрЛицо, "Тип", Организация.Тип);
	ДопРеквизиты_УстановитьЗначение(ЮрЛицо, "Экстраверт", Организация.Экстраверт);
	
	ЮрЛицо.Записать();
	
	ОрганизацияЭДО = ЮрЛицо.Ссылка;
	
	ДопРеквизиты_ЗаписатьОсновнойПакет_Диадок(ОрганизацияЭДО, Организация.ОсновнойПакет);
	ДопРеквизиты_ЗаписатьШифрованиеДокумента_Диадок(
		ОрганизацияЭДО,
		Неопределено,
		Организация.ШифроватьДокументы);
	
	Результат = ОрганизацияЭДО;
	
	Возврат Результат;
	
КонецФункции

Функция Организации_НастройкиСертификатовНаСервере1С_Диадок()
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка.ID КАК BoxId,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка.ИНН КАК ИНН,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка.КПП КАК КПП,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка КАК ОрганизацияДиадокСсылка,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК ОтпечатокСертификата,
	|	ЛОЖЬ КАК Облачный
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка
	|ГДЕ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита = ""ОтпечатокСертификатаНаСервере""
	|	И Диадок_ЮрФизЛица.ИмяСправочника = ""Организации""
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		Если ЗначениеЗаполнено(ВыборкаДетальныеЗаписи.ОтпечатокСертификата) Тогда
			
			СтруктураНастройки = Новый Структура("BoxId, Наименование, ИНН, КПП, ОрганизацияДиадокСсылка, ОтпечатокСертификата, Облачный");
			ЗаполнитьЗначенияСвойств(СтруктураНастройки, ВыборкаДетальныеЗаписи);
			СтруктураНастройки.ОтпечатокСертификата = ВРег(СтруктураНастройки.ОтпечатокСертификата);
			
			Результат.Добавить(СтруктураНастройки);
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура Организации_ОбновлениеРеквизитов_Диадок(Организация, СтруктураРеквизитов)
	
	ОрганизацияЭДО = СвойствоСтруктуры(Организация, "Ссылка");
	
	ОрганизацияОбъект = ОрганизацияЭДО.ПолучитьОбъект();
	ОрганизацияОбъект.Заблокировать();
	
	ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_Диадок();
	
	Если СтруктураРеквизитов.Свойство("СвязанныеСправочники") Тогда
		
		Справочники_ЮрФизЛицоОбъект_ОчиститьСвязанныеСправочники(ОрганизацияОбъект);
		
		СвязанныеСправочники = СтруктураРеквизитов.СвязанныеСправочники;
		Справочники_ЮрФизЛицоОбъект_ЗаполнитьСвязанныеСправочники(ОрганизацияОбъект, СвязанныеСправочники);
		
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("AfterIndexKey") Тогда
		ДопРеквизиты_УстановитьЗначение(
			ОрганизацияОбъект,
			ИменаСвойствНастроек.AfterIndexKey,
			СтруктураРеквизитов.AfterIndexKey);
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ДатаГраницыСинхронизации") Тогда
		ДопРеквизиты_УстановитьЗначение(
			ОрганизацияОбъект,
			ИменаСвойствНастроек.ДатаГраницыСинхронизации,
			СтруктураРеквизитов.ДатаГраницыСинхронизации);
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ID_ОсновноеПодразделение") Тогда
		ОрганизацияОбъект.ID_ОсновноеПодразделение = СтруктураРеквизитов.ID_ОсновноеПодразделение;
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ОтпечатокСертификатаНаСервере") Тогда
		ДопРеквизиты_УстановитьЗначение(
			ОрганизацияОбъект,
			ИменаСвойствНастроек.ОтпечатокСертификатаНаСервере,
			СтруктураРеквизитов.ОтпечатокСертификатаНаСервере);
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ОсновнойПакет") Тогда
		ДопРеквизиты_ЗаписатьОсновнойПакет_Диадок(
			ОрганизацияОбъект.Ссылка,
			СтруктураРеквизитов.ОсновнойПакет);
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ШифроватьДокументы") Тогда
		ДопРеквизиты_ЗаписатьШифрованиеДокумента_Диадок(
			ОрганизацияОбъект.Ссылка,
			Неопределено,
			СтруктураРеквизитов.ШифроватьДокументы);
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("УказыватьОтсутствиеОснованияУПД") Тогда
		ДопРеквизиты_УстановитьЗначение(
			ОрганизацияОбъект,
			ИменаСвойствНастроек.УказыватьОтсутствиеОснованияУПД,
			СтруктураРеквизитов.УказыватьОтсутствиеОснованияУПД);
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ОтправлятьНепроведенныеДокументы") Тогда
		ДопРеквизиты_УстановитьЗначение(
			ОрганизацияОбъект,
			ИменаСвойствНастроек.ОтправлятьНепроведенныеДокументы,
			СтруктураРеквизитов.ОтправлятьНепроведенныеДокументы);
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ЗаписыватьКомментарииВДокументы1С") Тогда
		ДопРеквизиты_УстановитьЗначение(
			ОрганизацияОбъект,
			ИменаСвойствНастроек.ЗаписыватьКомментарииВДокументы1С,
			СтруктураРеквизитов.ЗаписыватьКомментарииВДокументы1С);
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ТекстКомментарияОтправка") Тогда
		ДопРеквизиты_УстановитьЗначение(
			ОрганизацияОбъект,
			ИменаСвойствНастроек.ТекстКомментарияОтправка,
			СтруктураРеквизитов.ТекстКомментарияОтправка);
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ТекстКомментарияПолучение") Тогда
		ДопРеквизиты_УстановитьЗначение(
			ОрганизацияОбъект,
			ИменаСвойствНастроек.ТекстКомментарияПолучение,
			СтруктураРеквизитов.ТекстКомментарияПолучение);
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ДатаСинхронизацииСтатусовКонтрагентов") Тогда
		ДопРеквизиты_УстановитьЗначение(
			ОрганизацияОбъект,
			ИменаСвойствНастроек.ДатаСинхронизацииСтатусовКонтрагентов,
			СтруктураРеквизитов.ДатаСинхронизацииСтатусовКонтрагентов);
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("Тип") Тогда
		ДопРеквизиты_УстановитьЗначение(ОрганизацияОбъект, "Тип", СтруктураРеквизитов.Тип);
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("Экстраверт") Тогда
		ДопРеквизиты_УстановитьЗначение(ОрганизацияОбъект, "Экстраверт", СтруктураРеквизитов.Экстраверт);
	КонецЕсли;
	
	ОрганизацияОбъект.Записать();
	
КонецПроцедуры

Функция Организации_ТаблицаСопоставленныхОрганизаций_Диадок()
	
	Запрос = Новый Запрос;
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		ТекстЗапроса = Организации_ТексЗапроса_СопоставленныеОрганизации_ДиадокРасширение();
		Запрос.УстановитьПараметр("ШаблонИмениРеквизита_СвязанныйСправочник", ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник());
	Иначе
		ТекстЗапроса = Организации_ТексЗапроса_СопоставленныеОрганизации_ДиадокСХ1();
	КонецЕсли;
	
	ОрганизацииМД = МетаданныеСправочникаОрганизации();
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "Справочник.Организации", ОрганизацииМД.ПолноеИмя());
	
	Запрос.Текст = ТекстЗапроса;
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ТексЗапроса_СопоставленныеОрганизации_ДиадокРасширение()
	
	Результат = 
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	Диадок_ЮрФизЛица.ID КАК BoxID,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка
	|ГДЕ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка.ИмяСправочника = ""Организации""
	|	И Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ШаблонИмениРеквизита_СвязанныйСправочник
	|	И Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение <> НЕОПРЕДЕЛЕНО
	|	И Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение <> ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка)";
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ТексЗапроса_СопоставленныеОрганизации_ДиадокСХ1()
	
	Результат =
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	Диадок_ЮрФизЛица.ID КАК BoxID,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|ГДЕ
	|	Диадок_ЮрФизЛица.ИмяСправочника = ""Организации""
	|	И Диадок_ЮрФизЛица.СвязанныйСправочник1 <> НЕОПРЕДЕЛЕНО
	|	И Диадок_ЮрФизЛица.СвязанныйСправочник1 <> ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка)";
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ПустаяСсылка_Диадок()
	
	Результат = Справочники.Диадок_ЮрФизЛица.ПустаяСсылка();
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ДанныеКонтрагента_Диадок(Идентификаторы)

	Результат		= Контракт_КонтрагентЭДО();
	ВыборкаДанных	= Справочники_ВыборкаДанныхСправочника_Диадок("Контрагенты", Идентификаторы);
	
	Если ВыборкаДанных <> Неопределено Тогда
		
		ЗаполнитьЗначенияСвойств(Результат, ВыборкаДанных);
		
		ИменаДопРеквизитов	= "ДатаЛиквидации, OrgId, Тип, Экстраверт";
		ДопРеквизиты		= ДопРеквизиты_СписокРеквизитов_Диадок(Результат.Ссылка, ИменаДопРеквизитов);
		ЗаполнитьЗначенияСвойств(Результат, ДопРеквизиты);
		Результат.Экстраверт =
			?(Результат.Экстраверт = Неопределено,
				Ложь,
				Результат.Экстраверт);
		
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция Контрагенты_СопоставленныйВ1С_Диадок(Идентификаторы)

	Результат = Справочники_СвязанныйСправочник_Диадок("Контрагенты", Идентификаторы);
	
	Возврат Результат;

КонецФункции

Функция Контрагенты_СопоставленныеВ1С_Диадок(Идентификаторы)
	
	ИмяСправочника = Перечисление_ИменаСправочников_Диадок().Контрагенты;
	
	Результат = Справочники_СопоставленныеВ1С_Диадок(ИмяСправочника, Идентификаторы);
			
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_МассивСопоставленныхВ1С_Диадок(Идентификаторы)
	
	Результат = Новый Массив;
			
	ИмяСправочника 	= Перечисление_ИменаСправочников_Диадок().Контрагенты;
	ИмяСвойства 	= Перечисление_ИменаСвойствНастроек_Диадок().СвязанныйСправочник;
	
	СвязанныеСправочники = Справочники_СвязанныеСправочники_Диадок(ИмяСправочника, Идентификаторы);
		
	Для каждого ЭлементТЗ Из СвязанныеСправочники Цикл
		
		Если ЗначениеЗаполнено(ЭлементТЗ.ОбъектИБ) Тогда
			Результат.Добавить(ЭлементТЗ.ОбъектИБ);	
		КонецЕсли;
		
	КонецЦикла;
			
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_УчастникДО_Диадок(ИдентификаторыКонтрагента, ИдентификаторыОрганизации)
	
	Результат = Контракт_УчастникДокументооборота();
	
	Запрос = Новый Запрос;
	Запрос.Текст = Контрагенты_ТекстЗапросаУчастникаЭДО_Диадок(ИдентификаторыКонтрагента, ИдентификаторыОрганизации);
	
	Запрос.УстановитьПараметр("ID", ИдентификаторВАдресЯщика(ИдентификаторыКонтрагента.ID));
	Запрос.УстановитьПараметр("Ссылка", ИдентификаторыКонтрагента.Ссылка);
	Запрос.УстановитьПараметр("Статус_ПартнерскиеОтношения", Перечисление_СтатусыКонтрагентов().ПартнерскиеОтношения);
	Запрос.УстановитьПараметр("ШаблонИмениРеквизита_СвязанныйСправочник", ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник());
	
	Если ИдентификаторыОрганизации <> Неопределено Тогда
		
		Запрос.УстановитьПараметр("ОрганизацияID", ИдентификаторВАдресЯщика(ИдентификаторыОрганизации.ID));
		Запрос.УстановитьПараметр("Организация", ИдентификаторыОрганизации.Ссылка);
		
	КонецЕсли;
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Если ВыборкаЗапроса.Следующий() Тогда
		ЗаполнитьЗначенияСвойств(Результат, ВыборкаЗапроса);
	Иначе	
		Результат.BoxId				= ИдентификаторыКонтрагента.ID;
		Результат.ОрганизацияЭДО	= ИдентификаторыКонтрагента.Ссылка;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапросаУчастникаЭДО_Диадок(ИдентификаторыКонтрагента, ИдентификаторыОрганизации)
	
	Если ИдентификаторыОрганизации = Неопределено Тогда
		ОтборОрганизация = "Истина";
	ИначеЕсли ЗначениеЗаполнено(ИдентификаторыОрганизации.Ссылка) Тогда
		ОтборОрганизация = "Диадок_НастройкиКонтрагентов.Организация = &Организация";
	Иначе
		ОтборОрганизация = "Диадок_НастройкиКонтрагентов.Организация.ID = &ОрганизацияID";
	КонецЕсли;
	
	КонтрагентСсылка = ИдентификаторыКонтрагента.Ссылка;
	ОтборКонтрагент = "Диадок_ЮрФизЛица.ИмяСправочника = ""Контрагенты""";
	ОтборПоСопоставленномуКонтрагенту = Ложь;
	
	Если ТипЗнч(КонтрагентСсылка) = ТипЗнч(Контрагенты_ПустаяСсылка_Диадок()) Тогда
		
		ОтборКонтрагент = ОтборКонтрагент + " И Диадок_ЮрФизЛица.Ссылка = &Ссылка";
		
	ИначеЕсли ЗначениеЗаполнено(КонтрагентСсылка) Тогда
		
		ОтборПоСопоставленномуКонтрагенту = Истина;
		
	Иначе
		ОтборКонтрагент = ОтборКонтрагент + " И Диадок_ЮрФизЛица.ID = &ID";
	КонецЕсли;
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		Результат = Контрагенты_ТекстЗапроса_УчастникЭДО_ДиадокРасширение(ОтборПоСопоставленномуКонтрагенту);
	Иначе
		Результат = Контрагенты_ТекстЗапроса_УчастникЭДО_ДиадокСХ1(ОтборПоСопоставленномуКонтрагенту);
	КонецЕсли;
	
	Результат = Результат + ТекстЗапроса_РазделительЗапросов()
				+ Контрагенты_ТекстЗапросаДляОтбораКонтрагентовПоСтатусуПартнерскихОтношений_Диадок();
	
	Результат = СтрЗаменить(Результат, "&ОтборКонтрагент",	ОтборКонтрагент);
	Результат = СтрЗаменить(Результат, "&ОтборОрганизация",	ОтборОрганизация);
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапроса_УчастникЭДО_ДиадокРасширение(ОтборПоСопоставленномуКонтрагенту)
	
	Если ОтборПоСопоставленномуКонтрагенту Тогда
		Результат = Контрагенты_ТекстЗапроса_УчастникЭДОПоСопоставленнойСвязи_ДиадокРасширение();
	Иначе
		Результат = Контрагенты_ТекстЗапроса_УчастникЭДОПоИдентификатору_ДиадокРасширение();
	КонецЕсли;
	
	Результат = Результат + "
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ОрганизацияЭДО";
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапроса_УчастникЭДОПоСопоставленнойСвязи_ДиадокРасширение()
	
	Результат = 
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.ID КАК BoxId,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.Ссылка КАК ОрганизацияЭДО,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК Организация,
	|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_Тип.Значение, """") КАК ТипОрганизации,
	|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО КАК FNSParticipantId,
	|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение КАК DepartmentId
	|ПОМЕСТИТЬ ВТ_Контрагенты
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка
	|			И (Диадок_ЮрФизЛица.ИмяСправочника = ""Контрагенты"")
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_Тип
	|		ПО (ЮрФизЛица_ДопРеквизиты_Тип.Ссылка = Диадок_ЮрФизЛица.Ссылка)
	|			И (ЮрФизЛица_ДопРеквизиты_Тип.ИмяРеквизита = ""Тип"")
	|ГДЕ
	|   &ОтборКонтрагент
	|   И НЕ Диадок_ЮрФизЛица.ПометкаУдаления
	|	И Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ШаблонИмениРеквизита_СвязанныйСправочник
	|	И Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение = &Ссылка";
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапроса_УчастникЭДОПоИдентификатору_ДиадокРасширение()
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.ID КАК BoxId,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.Ссылка КАК ОрганизацияЭДО,
	|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_Тип.Значение, """") КАК ТипОрганизации,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК Организация,
	|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО КАК FNSParticipantId,
	|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение КАК DepartmentId
	|ПОМЕСТИТЬ ВТ_Контрагенты
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ПО (Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка)
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита = ""СвязанныйСправочник1"")
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_Тип
	|		ПО (ЮрФизЛица_ДопРеквизиты_Тип.Ссылка = Диадок_ЮрФизЛица.Ссылка)
	|			И (ЮрФизЛица_ДопРеквизиты_Тип.ИмяРеквизита = ""Тип"")
	|ГДЕ
	|	&ОтборКонтрагент
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапроса_УчастникЭДО_ДиадокСХ1(ОтборПоСопоставленномуКонтрагенту)
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.ID КАК BoxId,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.Ссылка КАК ОрганизацияЭДО,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник1 КАК Организация,
	|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_Тип.Значение, """") КАК ТипОрганизации,
	|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО КАК FNSParticipantId,
	|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение КАК DepartmentId
	|ПОМЕСТИТЬ ВТ_Контрагенты
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_Тип
	|		ПО (ЮрФизЛица_ДопРеквизиты_Тип.Ссылка = Диадок_ЮрФизЛица.Ссылка)
	|			И (ЮрФизЛица_ДопРеквизиты_Тип.ИмяРеквизита = ""Тип"")
	|ГДЕ
	|	&ОтборКонтрагент
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Если ОтборПоСопоставленномуКонтрагенту Тогда
		
		Результат = Результат + "
		| И (Диадок_ЮрФизЛица.СвязанныйСправочник1 = &Ссылка
		| 	ИЛИ Диадок_ЮрФизЛица.СвязанныйСправочник2 = &Ссылка
		| 	ИЛИ Диадок_ЮрФизЛица.СвязанныйСправочник3 = &Ссылка)
		|
		|ОБЪЕДИНИТЬ
		|
		|ВЫБРАТЬ
		|	Диадок_ЮрФизЛица.ID,
		|	Диадок_ЮрФизЛица.Наименование,
		|	Диадок_ЮрФизЛица.Ссылка,
		|	Диадок_ЮрФизЛица.СвязанныйСправочник1,
		|	ЕСТЬNULL(ЮрФизЛица_ДопРеквизиты_Тип.Значение, """") КАК ТипОрганизации,
		|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО,
		|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение
		|ИЗ
		|	Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
		|		ПО Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка
		|			И (Диадок_ЮрФизЛица.ИмяСправочника = ""Контрагенты"")
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛица_ДопРеквизиты_Тип
		|		ПО (ЮрФизЛица_ДопРеквизиты_Тип.Ссылка = Диадок_ЮрФизЛица.Ссылка)
		|			И (ЮрФизЛица_ДопРеквизиты_Тип.ИмяРеквизита = ""Тип"")
		|ГДЕ
		|   НЕ Диадок_ЮрФизЛица.ПометкаУдаления
		|	И Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ШаблонИмениРеквизита_СвязанныйСправочник
		|	И Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение = &Ссылка";
		
	КонецЕсли;
	
	Результат = Результат + "
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ОрганизацияЭДО";
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапросаДляОтбораКонтрагентовПоСтатусуПартнерскихОтношений_Диадок()
	
	Результат =
	"ВЫБРАТЬ
	|	ВТ_Контрагенты.*,
	|	ВЫБОР
	|		КОГДА Диадок_НастройкиКонтрагентов.Значение = &Статус_ПартнерскиеОтношения
	|			ТОГДА 1
	|		ИНАЧЕ 2
	|	КОНЕЦ КАК ПриоритетСтатуса
	|ИЗ
	|	ВТ_Контрагенты КАК ВТ_Контрагенты
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
	|		ПО ВТ_Контрагенты.ОрганизацияЭДО = Диадок_НастройкиКонтрагентов.Контрагент
	|			И &ОтборОрганизация
	|			И (Диадок_НастройкиКонтрагентов.ИмяСвойства = ""Статус"")
	|
	|УПОРЯДОЧИТЬ ПО
	|	ПриоритетСтатуса";

	Возврат Результат;
	
КонецФункции


Функция Контрагенты_НастройкиПоОрганизациям_Диадок(Идентификаторы)
	
	Если ЗначениеЗаполнено(Идентификаторы.Ссылка) Тогда
		Отбор = "Диадок_НастройкиКонтрагентов.Контрагент = &Ссылка";
	Иначе
		Отбор = "Диадок_НастройкиКонтрагентов.Контрагент.ID = &ID";
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = Контрагенты_ТекстЗапросаНастройкиПоОрганизациям_Диадок(Отбор);
	
	Запрос.УстановитьПараметр("ID",		Идентификаторы.ID);
	Запрос.УстановитьПараметр("Ссылка",	Идентификаторы.Ссылка);
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапросаНастройкиПоОрганизациям_Диадок(Отбор)
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_НастройкиКонтрагентов.Организация,
	|	Диадок_НастройкиКонтрагентов.Контрагент,
	|	Диадок_НастройкиКонтрагентов.ИмяСвойства,
	|	Диадок_НастройкиКонтрагентов.Значение
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
	|ГДЕ
	|	&Отбор";
	
	Результат = СтрЗаменить(Результат, "&Отбор", Отбор);
	
	Возврат Результат;
	
КонецФункции	

Процедура Контрагенты_Записать_Диадок(Контрагент)
	
	КонтрагентСсылка = Контрагент.Ссылка;
	
	Если ЗначениеЗаполнено(КонтрагентСсылка) Тогда
		ЮрЛицо = КонтрагентСсылка.ПолучитьОбъект();
	Иначе
		ЮрЛицо = Справочники.Диадок_ЮрФизЛица.СоздатьЭлемент();
	КонецЕсли;
	
	ЮрЛицо.ИмяСправочника	= "Контрагенты";
	ЮрЛицо.ID				= Контрагент.ID;	
	ЮрЛицо.Наименование		= Контрагент.Наименование;
	ЮрЛицо.ИНН				= Контрагент.ИНН;
	ЮрЛицо.КПП				= Контрагент.КПП;
	
	ЮрЛицо.ИдентификаторУчастникаЭДО = Контрагент.ИдентификаторУчастникаЭДО;
	
	ДопРеквизиты_УстановитьЗначение(ЮрЛицо, "OrgId", Контрагент.OrgId);
	ДопРеквизиты_УстановитьЗначение(ЮрЛицо, "ДатаЛиквидации", Контрагент.ДатаЛиквидации);
	ДопРеквизиты_УстановитьЗначение(ЮрЛицо, "Тип", Контрагент.Тип);
	ДопРеквизиты_УстановитьЗначение(ЮрЛицо, "Экстраверт", Контрагент.Экстраверт);
	
	ЮрЛицо.Записать();
	
	Контрагент.Ссылка = ЮрЛицо.Ссылка;
	
КонецПроцедуры

Процедура Контрагенты_ОбновлениеРеквизитов_Диадок(Идентификаторы, СтруктураРеквизитов)
	
	КонтрагентЭДО = СвойствоСтруктуры(Идентификаторы, "Ссылка");
	
	КонтрагентОбъект = КонтрагентЭДО.ПолучитьОбъект();
	
	Если СтруктураРеквизитов.Свойство("СвязанныеСправочники") Тогда
		
		Справочники_ЮрФизЛицоОбъект_ОчиститьСвязанныеСправочники(КонтрагентОбъект);
										
		СвязанныеСправочники = СтруктураРеквизитов.СвязанныеСправочники;
		Справочники_ЮрФизЛицоОбъект_ЗаполнитьСвязанныеСправочники(КонтрагентОбъект, СвязанныеСправочники);
						
	КонецЕсли;
	
	Если СтруктураРеквизитов.Свойство("ID_ОсновноеПодразделение") Тогда
		КонтрагентОбъект.ID_ОсновноеПодразделение = СтруктураРеквизитов.ID_ОсновноеПодразделение;
	КонецЕсли;	
	
	КонтрагентОбъект.Записать();
	
КонецПроцедуры

Процедура Контрагенты_ЗаписатьНастройкиПоОрганизациям_Диадок(Контрагент, НастройкиПоОрганизациям)
	
	НачатьТранзакцию();
	
	Попытка
		
		КонтрагентЭДО = СвойствоСтруктуры(Контрагент, "Ссылка");
		
		НаборЗаписей = РегистрыСведений.Диадок_НастройкиКонтрагентов.СоздатьНаборЗаписей();
		НаборЗаписей.Отбор.Контрагент.Установить(КонтрагентЭДО);
		
		Для Каждого Настройка Из НастройкиПоОрганизациям Цикл
			ЗаписьРегистра = НаборЗаписей.Добавить();
			ЗаполнитьЗначенияСвойств(ЗаписьРегистра, Настройка);
			ЗаписьРегистра.Контрагент = КонтрагентЭДО;
		КонецЦикла;
		
		НаборЗаписей.Записать();
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		Если ТранзакцияАктивна() Тогда
			ОтменитьТранзакцию();
		КонецЕсли;
		
		_Ошибка = ОписаниеОшибки();
		
		ВызватьИсключение;
		
	КонецПопытки;
	
КонецПроцедуры

Функция Контрагенты_СконвертироватьСоответствиеКонтрагентов_Диадок()
	
	Результат = Новый Массив;
	
	ТаблицаСоответствий = ТаблицаСтарыхСоответствийКонтрагентов();
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	ВЫРАЗИТЬ(парам.ID КАК СТРОКА(50)) КАК ID,
	|	ВЫРАЗИТЬ(парам.Контрагент КАК Справочник.Контрагенты) КАК Контрагент
	|ПОМЕСТИТЬ ВТ
	|ИЗ
	|	&ТаблицаСоответствий КАК парам
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ID
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.Ссылка,
	|	ВТ.Контрагент
	|ИЗ
	|	ВТ КАК ВТ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО ВТ.ID = Диадок_ЮрФизЛица.ID
	|			И ВТ.Контрагент <> Диадок_ЮрФизЛица.СвязанныйСправочник1
	|			И (Диадок_ЮрФизЛица.ИмяСправочника = ""Контрагенты"")"
	);
	
	Запрос.УстановитьПараметр("ТаблицаСоответствий", ТаблицаСоответствий);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		ОбъектКонтрагента = Выборка.Ссылка.ПолучитьОбъект();
		ОбъектКонтрагента.ОбменДанными.Загрузка	= Истина;
		ОбъектКонтрагента.СвязанныйСправочник1	= Выборка.Контрагент;
		ОбъектКонтрагента.Записать();
		
		Результат.Добавить(ОбъектКонтрагента.Наименование);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции


Функция Подразделения_ДанныеПодразделения_Диадок(Идентификаторы, ДляОрганизации)
	
	Результат = Контракт_Подразделение();
	
	ИмяСправочника	= ?(ДляОрганизации, "ПодразделенияОрганизаций", "ПодразделенияКонтрагентов");
	ВыборкаДанных	= Справочники_ВыборкаДанныхСправочника_Диадок(ИмяСправочника, Идентификаторы);
	
	Если ВыборкаДанных <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(Результат, ВыборкаДанных);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Подразделения_СопоставленныйВ1С_Диадок(Идентификаторы, ДляОрганизации)

	ИмяСправочника	= ?(ДляОрганизации, "ПодразделенияОрганизаций", "ПодразделенияКонтрагентов");
	Результат		= Справочники_СвязанныйСправочник_Диадок(ИмяСправочника, Идентификаторы);
	
	Возврат Результат;
	
КонецФункции

Функция Подразделения_СопоставленныеВ1С_Диадок(Идентификаторы, ДляОрганизации = Истина)
	
	Если ДляОрганизации Тогда
		ИмяСправочника = Перечисление_ИменаСправочников_Диадок().ПодразделенияОрганизаций;
	Иначе
		ИмяСправочника = Перечисление_ИменаСправочников_Диадок().ПодразделенияКонтрагентов;
	КонецЕсли;
	
	Результат = Справочники_СопоставленныеВ1С_Диадок(ИмяСправочника, Идентификаторы);
			
	Возврат Результат;
	
КонецФункции


Функция Подразделения_СсылкаПоИдентификатору_Диадок(Идентификаторы, ДляОрганизации)

	ИмяСправочника = ?(ДляОрганизации, "ПодразделенияОрганизаций", "ПодразделенияКонтрагентов");
	
	Результат = Справочники_СсылкаПоИдентификатору_Диадок(Идентификаторы, ИмяСправочника);
	
	Возврат Результат;

КонецФункции


Функция Подразделения_ТаблицаПодразделенийВладельца_Диадок(ДанныеВладельца, ДанныеОрганизации)
	
	ДляОрганизации	= (ДанныеОрганизации = Неопределено);
	ID_Владельца	= СвойствоСтруктуры(ДанныеВладельца, "ID", "");
	
	ИмяСправочника = ?(ДляОрганизации, "ПодразделенияОрганизаций", "ПодразделенияКонтрагентов");
	
	Запрос = Новый Запрос;
	Запрос.Текст = Справочники_ТекстЗапросаТаблицаПодразделенийВладельца_Диадок();
	
	Запрос.УстановитьПараметр("ИмяСправочника", ИмяСправочника);
	Запрос.УстановитьПараметр("Владелец",		ID_Владельца);
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Возврат Результат;
	
КонецФункции

Функция Справочники_ТекстЗапросаТаблицаПодразделенийВладельца_Диадок()
	
	Отбор = "Диадок_ЮрФизЛица.ИмяСправочника = &ИмяСправочника И Диадок_ЮрФизЛица.ID_ВладелецПодразделения = &Владелец";
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		Результат = Справочники_ТекстЗапроса_ДанныеСправочника_ДиадокРасширение();
	Иначе
		Результат = Справочники_ТекстЗапроса_ДанныеСправочника_ДиадокСХ1();
	КонецЕсли;
	
	Результат = СтрЗаменить(Результат, "&Отбор", Отбор);
	
	Возврат Результат;
	
КонецФункции


Процедура Подразделения_Записать_Диадок(Подразделение, Владелец)
	
	Если ЗначениеЗаполнено(Подразделение.Ссылка) Тогда
		ЮрЛицо = Подразделение.Ссылка.ПолучитьОбъект();
	Иначе
		ЮрЛицо = Справочники.Диадок_ЮрФизЛица.СоздатьЭлемент();
	КонецЕсли;
	
	ЮрЛицо.ИмяСправочника	= ?(Владелец.Ссылка.ИмяСправочника = "Организации", "ПодразделенияОрганизаций", "ПодразделенияКонтрагентов");
	ЮрЛицо.ID				= Подразделение.ID;	
	ЮрЛицо.Наименование		= Подразделение.Наименование;
	ЮрЛицо.ИНН				= Подразделение.ИНН;
	ЮрЛицо.КПП				= Подразделение.КПП;

	ЮрЛицо.Аббревиатура				= Подразделение.Аббревиатура;
	ЮрЛицо.ID_ВладелецПодразделения	= Подразделение.ID_ВладелецПодразделения;
	ЮрЛицо.ID_РодительПодразделения	= Подразделение.ID_РодительПодразделения;
	
	ЮрЛицо.Записать();
		
КонецПроцедуры

Процедура Подразделения_ОбновлениеРеквизитов_Диадок(Подразделение, СтруктураРеквизитов)
	
	ПодразделениеЭДО = СвойствоСтруктуры(Подразделение, "Ссылка");
	
	ПодразделениеОбъект = ПодразделениеЭДО.ПолучитьОбъект();
			
	Если СтруктураРеквизитов.Свойство("СвязанныеСправочники") Тогда
		
		Справочники_ЮрФизЛицоОбъект_ОчиститьСвязанныеСправочники(ПодразделениеОбъект);
		
		СвязанныеСправочники = СтруктураРеквизитов.СвязанныеСправочники;
		Справочники_ЮрФизЛицоОбъект_ЗаполнитьСвязанныеСправочники(ПодразделениеОбъект, СвязанныеСправочники);
				
	КонецЕсли;
	
	ПодразделениеОбъект.Записать();
	
КонецПроцедуры

Процедура Подразделения_ОбновитьСправочник_Диадок(ТаблицаКонтрактов, Владелец) 
	
	ИмяСправочника = ?(Владелец.Ссылка.ИмяСправочника = "Организации", "ПодразделенияОрганизаций", "ПодразделенияКонтрагентов");
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ТаблицаКонтрактов.ID КАК ID,
	|	ТаблицаКонтрактов.ИНН КАК ИНН,
	|	ТаблицаКонтрактов.КПП КАК КПП,
	|	ТаблицаКонтрактов.Наименование КАК Наименование,
	|	ТаблицаКонтрактов.Аббревиатура КАК Аббревиатура,
	|	ТаблицаКонтрактов.ID_ВладелецПодразделения КАК ID_ВладелецПодразделения,
	|	ТаблицаКонтрактов.ID_РодительПодразделения КАК ID_РодительПодразделения
	|ПОМЕСТИТЬ ВТ_ТаблицаКонтрактов
	|ИЗ
	|	&ТаблицаКонтрактов КАК ТаблицаКонтрактов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	Диадок_ЮрФизЛица.ID КАК ID,
	|	Диадок_ЮрФизЛица.ИНН КАК ИНН,
	|	Диадок_ЮрФизЛица.КПП КАК КПП,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.Аббревиатура КАК Аббревиатура,
	|	Диадок_ЮрФизЛица.ID_ВладелецПодразделения КАК ID_ВладелецПодразделения,
	|	Диадок_ЮрФизЛица.ID_РодительПодразделения КАК ID_РодительПодразделения,
	|	Диадок_ЮрФизЛица.Ссылка КАК ПодразделениеЭДО
	|ПОМЕСТИТЬ ВТ_ПодразделенияЭДО
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК ВладелецПодразделения
	|		ПО Диадок_ЮрФизЛица.ID_ВладелецПодразделения = ВладелецПодразделения.ID
	|			И (ВладелецПодразделения.Ссылка = &Владелец)
	|ГДЕ
	|	НЕ Диадок_ЮрФизЛица.ПометкаУдаления
	|	И Диадок_ЮрФизЛица.ИмяСправочника = &ИмяСправочника
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	""НОВЫЙ"" КАК Признак,
	|	ВТ_ТаблицаКонтрактов.ID КАК ID,
	|	ВТ_ТаблицаКонтрактов.ИНН КАК ИНН,
	|	ВТ_ТаблицаКонтрактов.КПП КАК КПП,
	|	ВТ_ТаблицаКонтрактов.Наименование КАК Наименование,
	|	ВТ_ТаблицаКонтрактов.Аббревиатура КАК Аббревиатура,
	|	ВТ_ТаблицаКонтрактов.ID_ВладелецПодразделения КАК ID_ВладелецПодразделения,
	|	ВТ_ТаблицаКонтрактов.ID_РодительПодразделения КАК ID_РодительПодразделения,
	|	ВТ_ПодразделенияЭДО.ПодразделениеЭДО КАК Ссылка,
	|	ЛОЖЬ КАК ПометкаУдаления
	|ИЗ
	|	ВТ_ТаблицаКонтрактов КАК ВТ_ТаблицаКонтрактов
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ПодразделенияЭДО КАК ВТ_ПодразделенияЭДО
	|		ПО ВТ_ТаблицаКонтрактов.ID = ВТ_ПодразделенияЭДО.ID
	|ГДЕ
	|	ВТ_ПодразделенияЭДО.ПодразделениеЭДО ЕСТЬ NULL
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	""ИЗМЕНЕН"" КАК Признак,
	|	ВТ_ТаблицаКонтрактов.ID КАК ID,
	|	ВТ_ТаблицаКонтрактов.ИНН КАК ИНН,
	|	ВТ_ТаблицаКонтрактов.КПП КАК КПП,
	|	ВТ_ТаблицаКонтрактов.Наименование КАК Наименование,
	|	ВТ_ТаблицаКонтрактов.Аббревиатура КАК Аббревиатура,
	|	ВТ_ТаблицаКонтрактов.ID_ВладелецПодразделения КАК ID_ВладелецПодразделения,
	|	ВТ_ТаблицаКонтрактов.ID_РодительПодразделения КАК ID_РодительПодразделения,
	|	ВТ_ПодразделенияЭДО.ПодразделениеЭДО КАК Ссылка,
	|	ЛОЖЬ КАК ПометкаУдаления
	|ИЗ
	|	ВТ_ТаблицаКонтрактов КАК ВТ_ТаблицаКонтрактов
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ПодразделенияЭДО КАК ВТ_ПодразделенияЭДО
	|		ПО ВТ_ТаблицаКонтрактов.ID = ВТ_ПодразделенияЭДО.ID
	|ГДЕ
	|	НЕ (ВТ_ТаблицаКонтрактов.ИНН = ВТ_ПодразделенияЭДО.ИНН
	|		И ВТ_ТаблицаКонтрактов.КПП = ВТ_ПодразделенияЭДО.КПП
	|		И ВТ_ТаблицаКонтрактов.Наименование = ВТ_ПодразделенияЭДО.Наименование
	|		И ВТ_ТаблицаКонтрактов.Аббревиатура = ВТ_ПодразделенияЭДО.Аббревиатура
	|		И ВТ_ТаблицаКонтрактов.ID_ВладелецПодразделения = ВТ_ПодразделенияЭДО.ID_ВладелецПодразделения
	|		И ВТ_ТаблицаКонтрактов.ID_РодительПодразделения = ВТ_ПодразделенияЭДО.ID_РодительПодразделения)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	""УДАЛЕН"",
	|	ВТ_ПодразделенияЭДО.ID,
	|	ВТ_ПодразделенияЭДО.ИНН,
	|	ВТ_ПодразделенияЭДО.КПП,
	|	ВТ_ПодразделенияЭДО.Наименование,
	|	ВТ_ПодразделенияЭДО.Аббревиатура,
	|	ВТ_ПодразделенияЭДО.ID_ВладелецПодразделения,
	|	ВТ_ПодразделенияЭДО.ID_РодительПодразделения,
	|	ВТ_ПодразделенияЭДО.ПодразделениеЭДО,
	|	ИСТИНА
	|ИЗ
	|	ВТ_ПодразделенияЭДО КАК ВТ_ПодразделенияЭДО
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ТаблицаКонтрактов КАК ВТ_ТаблицаКонтрактов
	|		ПО ВТ_ПодразделенияЭДО.ID = ВТ_ТаблицаКонтрактов.ID
	|ГДЕ
	|	ВТ_ТаблицаКонтрактов.ID ЕСТЬ NULL";

	Запрос.УстановитьПараметр("ТаблицаКонтрактов",	ТаблицаКонтрактов);
	Запрос.УстановитьПараметр("ИмяСправочника",		ИмяСправочника);
	Запрос.УстановитьПараметр("Владелец",			Владелец.Ссылка);
	
	РезультатЗапроса = Запрос.ВыполнитьПакет();
	
	ВыборкаНовыхИИзмененных = РезультатЗапроса[2].Выбрать();
	Пока ВыборкаНовыхИИзмененных.Следующий() Цикл
		
		Подразделение = Контракт_Подразделение();
		ЗаполнитьЗначенияСвойств(Подразделение, ВыборкаНовыхИИзмененных);
		
		Подразделения_Записать_Диадок(Подразделение, Владелец);
		
	КонецЦикла;	
	
	ВыборкаУдаленных = РезультатЗапроса[3].Выбрать();
	Пока ВыборкаУдаленных.Следующий() Цикл
		
		Попытка
			
			ПодразделениеОбъект = ВыборкаУдаленных.ПодразделениеЭДО.ПолучитьОбъект();
			ПодразделениеОбъект.УстановитьПометкуУдаления(Истина);
			
		Исключение
			
			ВидОперации		= "Обновление справочника ""Подразделения""";
			ТекстОшибки		= ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
			ДопИнформация	= СтрШаблонЛок("Юр/физ лицо: %1", Владелец.Наименование);
			
			ТекстСообщения	= "Не удалось пометить на удаление подразделение ""%1""";
			ТекстСообщения	= СтрШаблонЛок(ТекстСообщения, ПодразделениеОбъект.Наименование);
			
			Ошибка_Обработать(ВидОперации, ТекстОшибки, ДопИнформация, ТекстСообщения);

		КонецПопытки;
		
	КонецЦикла;
	
КонецПроцедуры

// Описание метода см. Подразделения_ВыполнитьПакетноеОбновлениеСправочника
//
Процедура Подразделения_ВыполнитьПакетноеОбновлениеСправочника_Диадок(Подразделения, КонтрагентыЭДО)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ТаблицаКонтрактов.ID КАК ID,
	|	ТаблицаКонтрактов.ИНН КАК ИНН,
	|	ТаблицаКонтрактов.КПП КАК КПП,
	|	ТаблицаКонтрактов.Наименование КАК Наименование,
	|	ТаблицаКонтрактов.Аббревиатура КАК Аббревиатура,
	|	ТаблицаКонтрактов.ID_ВладелецПодразделения КАК ID_ВладелецПодразделения,
	|	ТаблицаКонтрактов.ID_РодительПодразделения КАК ID_РодительПодразделения
	|ПОМЕСТИТЬ ВТ_ТаблицаКонтрактов
	|ИЗ
	|	&ТаблицаКонтрактов КАК ТаблицаКонтрактов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.ID КАК ID,
	|	Диадок_ЮрФизЛица.Ссылка КАК Ссылка
	|ПОМЕСТИТЬ ВТ_КонтрагентыЭДО
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|ГДЕ
	|	Диадок_ЮрФизЛица.ИмяСправочника = &ИмяСправочникаКонтрагенты
	|	И Диадок_ЮрФизЛица.ID В(&Владельцы)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	Диадок_ЮрФизЛица.ID КАК ID,
	|	Диадок_ЮрФизЛица.ИНН КАК ИНН,
	|	Диадок_ЮрФизЛица.КПП КАК КПП,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.Аббревиатура КАК Аббревиатура,
	|	Диадок_ЮрФизЛица.ID_ВладелецПодразделения КАК ID_ВладелецПодразделения,
	|	Диадок_ЮрФизЛица.ID_РодительПодразделения КАК ID_РодительПодразделения,
	|	Диадок_ЮрФизЛица.Ссылка КАК Ссылка,
	|	ВладелецПодразделения.Ссылка КАК ВладелецПодразделенияСсылка
	|ПОМЕСТИТЬ ВТ_ПодразделенияЭДО
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_КонтрагентыЭДО КАК ВладелецПодразделения
	|		ПО Диадок_ЮрФизЛица.ID_ВладелецПодразделения = ВладелецПодразделения.ID
	|ГДЕ
	|	НЕ Диадок_ЮрФизЛица.ПометкаУдаления
	|	И Диадок_ЮрФизЛица.ИмяСправочника = &ИмяСправочникаПодразделения
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	""НОВЫЙ"" КАК Признак,
	|	ВТ_ТаблицаКонтрактов.ID КАК ID,
	|	ВТ_ТаблицаКонтрактов.ИНН КАК ИНН,
	|	ВТ_ТаблицаКонтрактов.КПП КАК КПП,
	|	ВТ_ТаблицаКонтрактов.Наименование КАК Наименование,
	|	ВТ_ТаблицаКонтрактов.Аббревиатура КАК Аббревиатура,
	|	ВТ_ТаблицаКонтрактов.ID_ВладелецПодразделения КАК ID_ВладелецПодразделения,
	|	ВТ_ТаблицаКонтрактов.ID_РодительПодразделения КАК ID_РодительПодразделения,
	|	ВТ_ПодразделенияЭДО.Ссылка КАК Ссылка,
	|	ВТ_КонтрагентыЭДО.Ссылка КАК ВладелецПодразделенияСсылка,
	|	ЛОЖЬ КАК ПометкаУдаления
	|ИЗ
	|	ВТ_ТаблицаКонтрактов КАК ВТ_ТаблицаКонтрактов
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ПодразделенияЭДО КАК ВТ_ПодразделенияЭДО
	|		ПО ВТ_ТаблицаКонтрактов.ID = ВТ_ПодразделенияЭДО.ID
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КонтрагентыЭДО КАК ВТ_КонтрагентыЭДО
	|		ПО ВТ_ТаблицаКонтрактов.ID_ВладелецПодразделения = ВТ_КонтрагентыЭДО.ID
	|ГДЕ
	|	ВТ_ПодразделенияЭДО.Ссылка ЕСТЬ NULL
	|	И НЕ ВТ_КонтрагентыЭДО.Ссылка ЕСТЬ NULL
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	""ИЗМЕНЕН"",
	|	ВТ_ТаблицаКонтрактов.ID,
	|	ВТ_ТаблицаКонтрактов.ИНН,
	|	ВТ_ТаблицаКонтрактов.КПП,
	|	ВТ_ТаблицаКонтрактов.Наименование,
	|	ВТ_ТаблицаКонтрактов.Аббревиатура,
	|	ВТ_ТаблицаКонтрактов.ID_ВладелецПодразделения,
	|	ВТ_ТаблицаКонтрактов.ID_РодительПодразделения,
	|	ВТ_ПодразделенияЭДО.Ссылка,
	|	ВТ_КонтрагентыЭДО.Ссылка,
	|	ЛОЖЬ
	|ИЗ
	|	ВТ_ТаблицаКонтрактов КАК ВТ_ТаблицаКонтрактов
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ПодразделенияЭДО КАК ВТ_ПодразделенияЭДО
	|		ПО ВТ_ТаблицаКонтрактов.ID = ВТ_ПодразделенияЭДО.ID
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КонтрагентыЭДО КАК ВТ_КонтрагентыЭДО
	|		ПО ВТ_ТаблицаКонтрактов.ID_ВладелецПодразделения = ВТ_КонтрагентыЭДО.ID
	|ГДЕ
	|	НЕ(ВТ_ТаблицаКонтрактов.ИНН = ВТ_ПодразделенияЭДО.ИНН
	|				И ВТ_ТаблицаКонтрактов.КПП = ВТ_ПодразделенияЭДО.КПП
	|				И ВТ_ТаблицаКонтрактов.Наименование = ВТ_ПодразделенияЭДО.Наименование
	|				И ВТ_ТаблицаКонтрактов.Аббревиатура = ВТ_ПодразделенияЭДО.Аббревиатура
	|				И ВТ_ТаблицаКонтрактов.ID_ВладелецПодразделения = ВТ_ПодразделенияЭДО.ID_ВладелецПодразделения
	|				И ВТ_ТаблицаКонтрактов.ID_РодительПодразделения = ВТ_ПодразделенияЭДО.ID_РодительПодразделения)
	|	И НЕ ВТ_КонтрагентыЭДО.Ссылка ЕСТЬ NULL
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	""УДАЛЕН"" КАК Признак,
	|	ВТ_ПодразделенияЭДО.Ссылка,
	|	ВТ_КонтрагентыЭДО.Ссылка КАК ВладелецПодразделенияСсылка
	|ИЗ
	|	ВТ_ПодразделенияЭДО КАК ВТ_ПодразделенияЭДО
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ТаблицаКонтрактов КАК ВТ_ТаблицаКонтрактов
	|		ПО ВТ_ПодразделенияЭДО.ID = ВТ_ТаблицаКонтрактов.ID
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КонтрагентыЭДО КАК ВТ_КонтрагентыЭДО
	|		ПО ВТ_ПодразделенияЭДО.ID_ВладелецПодразделения = ВТ_КонтрагентыЭДО.ID
	|ГДЕ
	|	ВТ_ТаблицаКонтрактов.ID ЕСТЬ NULL
	|	И НЕ ВТ_КонтрагентыЭДО.Ссылка ЕСТЬ NULL";

	Запрос.УстановитьПараметр("ИмяСправочникаПодразделения",	"ПодразделенияКонтрагентов");
	Запрос.УстановитьПараметр("ИмяСправочникаКонтрагенты",		"Контрагенты");
	Запрос.УстановитьПараметр("ТаблицаКонтрактов",				Подразделения);
	Запрос.УстановитьПараметр("Владельцы",						КонтрагентыЭДО.ВыгрузитьКолонку("ID"));
	
	РезультатЗапроса = Запрос.ВыполнитьПакет();
	
	ВыборкаНовыхИИзмененных = РезультатЗапроса[3].Выбрать();
	Пока ВыборкаНовыхИИзмененных.Следующий() Цикл
		
		Подразделение = Контракт_Подразделение();
		ЗаполнитьЗначенияСвойств(Подразделение, ВыборкаНовыхИИзмененных);
		
		ИдентификаторыОбъекта = Новый_ИдентификаторыСправочника(ВыборкаНовыхИИзмененных.ВладелецПодразделенияСсылка);
		
		Подразделения_Записать_Диадок(Подразделение, ИдентификаторыОбъекта);
		
	КонецЦикла;	
	
	ВыборкаУдаленных = РезультатЗапроса[4].Выбрать();
	Пока ВыборкаУдаленных.Следующий() Цикл
		
		Попытка
			
			ПодразделениеОбъект = ВыборкаУдаленных.Ссылка.ПолучитьОбъект();
			ПодразделениеОбъект.УстановитьПометкуУдаления(Истина);
			
		Исключение
			
			ВидОперации		= "Обновление справочника ""Подразделения""";
			ТекстОшибки		= ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
			ДопИнформация	= СтрШаблонЛок("Юр/физ лицо: %1", ВыборкаУдаленных.ВладелецПодразделенияСсылка);
			
			ТекстСообщения	= "Не удалось пометить на удаление подразделение ""%1""";
			ТекстСообщения	= СтрШаблонЛок(ТекстСообщения, ПодразделениеОбъект.Наименование);
			
			Ошибка_Обработать(ВидОперации, ТекстОшибки, ДопИнформация, ТекстСообщения);

		КонецПопытки;
		
	КонецЦикла;
	
КонецПроцедуры

// См. метод Подразделения_ИдентификаторПоСвязаннымСправочникам
Функция Подразделения_ИдентификаторПоСвязаннымСправочникам_Диадок(IdВладельца, СвязанныйСправочник, ДляОрганизации)
	
	ИмяСправочника = Перечисление_ИменаСправочников_Диадок();
	
	Если ДляОрганизации Тогда
		ИмяСправочника = ИмяСправочника.ПодразделенияОрганизаций;
	Иначе
		ИмяСправочника = ИмяСправочника.ПодразделенияКонтрагентов;
	КонецЕсли;
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		ТекстЗапроса = Справочники_ТекстЗапроса_СвязанныеЮрФизЛица_ДиадокРасширение();
	Иначе
		ТекстЗапроса = Справочники_ТекстЗапроса_СвязанныеЮрФизЛица_ДиадокСХ1();
	КонецЕсли;
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&Отбор", "Диадок_ЮрФизЛица.ID_ВладелецПодразделения = &ID_ВладелецПодразделения");
	
	Запрос = Новый Запрос(ТекстЗапроса);
	
	Запрос.УстановитьПараметр("ИмяСправочника", ИмяСправочника);
	Запрос.УстановитьПараметр("ИмяРеквизита", ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник());
	Запрос.УстановитьПараметр("ID_ВладелецПодразделения", IdВладельца);
	Запрос.УстановитьПараметр("СсылкаНаОбъект", СвязанныйСправочник);
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Если ВыборкаЗапроса.Следующий() Тогда
		Результат = ВыборкаЗапроса.ID;
	Иначе
		Результат = "";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

//**********************************************
// СПРАВОЧНИКИ: 1С

Функция Организации_ДанныеОрганизации_1С(Идентификаторы)
	
	Результат = Контракт_ОрганизацияЭДО();
	
	BoxID = Идентификаторы.ID;
	
	СтрокаКонтекста = КонтекстСеанса_СтрокаКонтекста(BoxID);
	
	Результат.ID						= BoxID;
	Результат.OrgId						= СтрокаКонтекста.OrgId;
	Результат.ИНН						= СтрокаКонтекста.ИНН;
	Результат.КПП						= СтрокаКонтекста.КПП;
	Результат.Наименование				= СтрокаКонтекста.ОрганизацияНаименование;
	Результат.ИдентификаторУчастникаЭДО	= СтрокаКонтекста.Box.Organization.FnsParticipantId;
	Результат.Экстраверт 				= СтрокаКонтекста.Box.Organization.Sociability = "AllOrganizations";
	
	СвязанныйСправочник1 = Организации_СопоставленнаяВ1С_1С(Идентификаторы);
	
	Если СвязанныйСправочник1 <> Неопределено Тогда
		
		Результат.СвязанныйСправочник1 = СвязанныйСправочник1;
		
		ОсновнойПакет = ДопРеквизиты_Значение_1С(СвязанныйСправочник1, Перечисление_ИменаСвойствНастроек_1С().ОсновнойПакет);
		Результат.ОсновнойПакет	= ?(ОсновнойПакет = Неопределено, "", ОсновнойПакет);
		
		ОтпечатокСертификатаНаСервере = ДопРеквизиты_Значение_1С(СвязанныйСправочник1, Перечисление_ИменаСвойствНастроек_1С().ОтпечатокСертификатаНаСервере);
		Результат.ОтпечатокСертификатаНаСервере	= ?(ОтпечатокСертификатаНаСервере = Неопределено, "", ОтпечатокСертификатаНаСервере);
		
		ШифроватьДокументы = ДопРеквизиты_ЗначениеШифрованияДокумента_1С(Идентификаторы);
		Результат.ШифроватьДокументы = ?(ШифроватьДокументы = Неопределено, Ложь, ШифроватьДокументы);
		
		УказыватьОтсутствиеОснованияУПД = ДопРеквизиты_Значение_1С(СвязанныйСправочник1, Перечисление_ИменаСвойствНастроек_1С().УказыватьОтсутствиеОснованияУПД, Ложь);
		Результат.УказыватьОтсутствиеОснованияУПД = ?(УказыватьОтсутствиеОснованияУПД = Неопределено, Ложь, УказыватьОтсутствиеОснованияУПД);
		
		ОтправлятьНепроведенныеДокументы = Организации_НастройкаОтправлятьНепроведенныеДокументы_1С(СвязанныйСправочник1);
		Результат.ОтправлятьНепроведенныеДокументы = ?(ОтправлятьНепроведенныеДокументы = Неопределено, Ложь, ОтправлятьНепроведенныеДокументы);

		ЗаписыватьКомментарииВДокументы1С = ДопРеквизиты_Значение_1С(СвязанныйСправочник1, Перечисление_ИменаСвойствНастроек_1С().ЗаписыватьКомментарииВДокументы1С);
		Результат.ЗаписыватьКомментарииВДокументы1С = ?(ЗаписыватьКомментарииВДокументы1С = Неопределено, Ложь, ЗаписыватьКомментарииВДокументы1С);
		
		ТекстКомментарияОтправка = ДопРеквизиты_Значение_1С(СвязанныйСправочник1, Перечисление_ИменаСвойствНастроек_1С().ТекстКомментарияОтправка, Ложь);
		Результат.ТекстКомментарияОтправка = ?(ТекстКомментарияОтправка = Неопределено, "", ТекстКомментарияОтправка);
		
		ТекстКомментарияПолучение = ДопРеквизиты_Значение_1С(СвязанныйСправочник1, Перечисление_ИменаСвойствНастроек_1С().ТекстКомментарияПолучение);
		Результат.ТекстКомментарияПолучение = ?(ТекстКомментарияПолучение = Неопределено, "", ТекстКомментарияПолучение);
		
		ID_ОсновноеПодразделение = Организации_ОсновноеПодразделениеОрганизации_1С(СвязанныйСправочник1);
		Результат.ID_ОсновноеПодразделение = ?(ID_ОсновноеПодразделение = Неопределено, "", ID_ОсновноеПодразделение);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции
            
Функция Организации_НастройкаОтправлятьНепроведенныеДокументы_1С(Организация)
	
	Результат = Ложь;
	
	ОтправлятьНепроведенныеДокументы = ДопРеквизиты_Значение_1С(Организация, Перечисление_ИменаСвойствНастроек_1С().ОтправлятьНепроведенныеДокументы, Ложь);
	
	Если ОтправлятьНепроведенныеДокументы = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	// для совместимости с настрокой модулей Стандарт
	Если ТипЗнч(ОтправлятьНепроведенныеДокументы) = Тип("Строка") Тогда
		Результат = ОтправлятьНепроведенныеДокументы = "Да";
	Иначе
		Результат = ОтправлятьНепроведенныеДокументы;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Организации_СопоставленнаяВ1С_1С(Идентификаторы)

	BoxID = ИдентификаторВАдресЯщика(Идентификаторы.ID);
	
	Результат = Свойства_ЗначениеОбъекта(Перечисление_ИменаСвойствНастроек_1С().IDОрганизации, BoxID, Ложь);
	
	Возврат Результат;

КонецФункции

Функция Организации_СопоставленныеВ1С_1С(Идентификаторы)
			
	Результат = Организации_МассивСопоставленныхВ1С_1С(Идентификаторы);
		
	Возврат Результат;
	
КонецФункции

Функция Организации_МассивСопоставленныхВ1С_1С(Идентификаторы)
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос;
	
	Запрос.Текст = Свойства_ТекстЗапроса_ЗначениеОбъекта();
	
	BoxID 					= ИдентификаторВАдресЯщика(Идентификаторы.ID);
	Свойство_IDОрганизации	= Свойства_СсылкаНаСвойство(Перечисление_ИменаСвойствНастроек_1С().IDОрганизации, Ложь);
		
	Запрос.УстановитьПараметр("Значение", BoxID);
	Запрос.УстановитьПараметр("Свойство", Свойство_IDОрганизации);
			
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Пока ВыборкаЗапроса.Следующий() Цикл
		Результат.Добавить(ВыборкаЗапроса.Ссылка);
	КонецЦикла;

	Возврат Результат;
	
КонецФункции

Функция Организации_СсылкаПоИдентификатору_1С(Идентификаторы)

	Результат = Неопределено;
	
	Возврат Результат;

КонецФункции

Функция Организации_УчастникДО_1С(Идентификаторы)
	
	Результат = Контракт_УчастникДокументооборота();

	BoxID = ИдентификаторВАдресЯщика(Идентификаторы.ID);
	
	Если BoxID = "" Тогда
		BoxID = Организации_ИдентификаторПоСсылке_1С(Идентификаторы);
		Идентификаторы.ID = BoxID;
	КонецЕсли;	
	
	СтрокаКонтекста = КонтекстСеанса_СтрокаКонтекста(BoxID);
	
	ЗаполнитьЗначенияСвойств(Результат, СтрокаКонтекста);
	
	Результат.BoxId				= BoxID;
	Результат.Наименование		= СтрокаКонтекста.ОрганизацияНаименование;
	Результат.FNSParticipantId	= СтрокаКонтекста.Box.Organization.FnsParticipantId;
	Результат.ТипОрганизации = Ядро.ТипОрганизации(СтрокаКонтекста.Box.Organization.OrganizationType);
	
	ОрганизацииВ1С = Организации_СопоставленныеВ1С(Идентификаторы);
	
	Если ЗначениеЗаполнено(ОрганизацииВ1С) Тогда
		
		ОрганизацияВ1С = ОрганизацииВ1С[0].Ссылка;
		
		// для совместимости заполним реквизит "Организация"
		Результат.Организация = ОрганизацияВ1С;
					
		// при множественном сопоставлении не важно по какой из сопоставленных организаций 1С искать подразделение по умолчанию,
		// т.к. для всех сопоставленных настройка будет одинакова
		Результат.DepartmentId	= Организации_ОсновноеПодразделениеОрганизации_1С(ОрганизацияВ1С);
		
	КонецЕсли;	
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ОсновноеПодразделениеОрганизации_1С(ОрганизацияВ1С)
		
	ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_1С();
		
	Результат = ДопРеквизиты_Значение_1С(ОрганизацияВ1С, ИменаСвойствНастроек.IDОсновноеПодразделениеОрганизации, Ложь);
	
	ДлинаID = 36;
		
	Если СтрДлина(Результат) <> ДлинаID Тогда
		Результат = "";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Организации_НастройкиСертификатовНаСервере1С_1С()
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос;
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВЫРАЗИТЬ(ЗначенияСвойствОбъектов.Объект КАК Справочник.Организации) КАК Организация,
	|	ВЫРАЗИТЬ(ЗначенияСвойствОбъектов.Значение КАК СТРОКА(1000)) КАК ОтпечатокСертификата
	|ПОМЕСТИТЬ ВТ_Отпечатки
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Свойство = &Свойство_Отпечаток
	|	И (ВЫРАЗИТЬ(ЗначенияСвойствОбъектов.Значение КАК СТРОКА(1000))) <> """"
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВЫРАЗИТЬ(ЗначенияСвойствОбъектов.Объект КАК Справочник.Организации) КАК Организация,
	|	ВЫРАЗИТЬ(ЗначенияСвойствОбъектов.Значение КАК СТРОКА(1000)) КАК BoxID
	|ПОМЕСТИТЬ ВТ_Организации
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Свойство = &Свойство_ID
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Организации.BoxID,
	|	ВТ_Организации.Организация.ИНН КАК ИНН,
	|	ВТ_Организации.Организация.КПП КАК КПП,
	|	ВТ_Отпечатки.ОтпечатокСертификата
	|ИЗ
	|	ВТ_Отпечатки КАК ВТ_Отпечатки
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Организации КАК ВТ_Организации
	|		ПО ВТ_Отпечатки.Организация = ВТ_Организации.Организация";
	
	ОбъектМд = МетаданныеСправочникаОрганизации();
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "Справочник.Организации", ОбъектМд.ПолноеИмя());
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	ТекстЗапроса	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Запрос.Текст = ТекстЗапроса;
	
	Запрос.УстановитьПараметр("Свойство_Отпечаток",	Свойства_СсылкаНаСвойство(Перечисление_ИменаСвойствНастроек_1С().ОтпечатокСертификатаНаСервере));
	Запрос.УстановитьПараметр("Свойство_ID",		Свойства_СсылкаНаСвойство(Перечисление_ИменаСвойствНастроек_1С().IDОрганизации, Ложь));
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Пока ВыборкаЗапроса.Следующий() Цикл
		
		СтруктураНастройки = Новый Структура;
		СтруктураНастройки.Вставить("BoxId",					"");
		СтруктураНастройки.Вставить("Наименование",				"");
		СтруктураНастройки.Вставить("ИНН",						"");
		СтруктураНастройки.Вставить("КПП",						"");
		СтруктураНастройки.Вставить("ОрганизацияДиадокСсылка",	Неопределено);
		СтруктураНастройки.Вставить("ОтпечатокСертификата",		"");
		СтруктураНастройки.Вставить("Облачный",					Ложь);
		
		ЗаполнитьЗначенияСвойств(СтруктураНастройки, ВыборкаЗапроса);
		СтруктураНастройки.ОтпечатокСертификата = ВРег(СтруктураНастройки.ОтпечатокСертификата);
		
		Результат.Добавить(СтруктураНастройки);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Перезаписывает дополнительные реквизиты для Организации ЭДО
//
// Параметры:
//	Организация 		- Структура - данные организации ЭДО
//	СтруктураРеквизитов - Структура - реквизиты требующие обновления
Процедура Организации_ОбновлениеРеквизитов_1С(Организация, СтруктураРеквизитов)
	
	Если СтруктураРеквизитов.Свойство("СвязанныеСправочники") Тогда
		
		ID = СвойствоСтруктуры(Организация, "ID", "");
		
		Организации_ОчиститьНастройки_1С(ID);
		
		ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_1С();
		
		Для Каждого ЭлементКоллекции Из СтруктураРеквизитов.СвязанныеСправочники Цикл
			
			СвязанныйСправочник = ЭлементКоллекции.Ссылка;
			
			ДопРеквизиты_УстановитьЗначение_1С(СвязанныйСправочник, ИменаСвойствНастроек.IDОрганизации, ID, Ложь);
			
			Если СтруктураРеквизитов.Свойство("ОсновнойПакет") Тогда
				ОсновнойПакет = ?(СтруктураРеквизитов.ОсновнойПакет = Неопределено, "", СтруктураРеквизитов.ОсновнойПакет);
				ДопРеквизиты_УстановитьЗначение_1С(СвязанныйСправочник, ИменаСвойствНастроек.ОсновнойПакет, ОсновнойПакет);
			КонецЕсли;
			
			Если СтруктураРеквизитов.Свойство("ШифроватьДокументы") Тогда
				ДопРеквизиты_УстановитьЗначение_1С(СвязанныйСправочник, ИменаСвойствНастроек.ШифроватьДокументы, СтруктураРеквизитов.ШифроватьДокументы);
			КонецЕсли;
			
			Если СтруктураРеквизитов.Свойство("УказыватьОтсутствиеОснованияУПД") Тогда
				ДопРеквизиты_УстановитьЗначение_1С(СвязанныйСправочник, ИменаСвойствНастроек.УказыватьОтсутствиеОснованияУПД, СтруктураРеквизитов.УказыватьОтсутствиеОснованияУПД, Ложь);
			КонецЕсли;
			
			Если СтруктураРеквизитов.Свойство("ОтправлятьНепроведенныеДокументы") Тогда
				ДопРеквизиты_УстановитьЗначение_1С(СвязанныйСправочник, ИменаСвойствНастроек.ОтправлятьНепроведенныеДокументы, СтруктураРеквизитов.ОтправлятьНепроведенныеДокументы, Ложь);
			КонецЕсли;
			
			Если СтруктураРеквизитов.Свойство("ЗаписыватьКомментарииВДокументы1С") Тогда
				ДопРеквизиты_УстановитьЗначение_1С(СвязанныйСправочник, ИменаСвойствНастроек.ЗаписыватьКомментарииВДокументы1С, СтруктураРеквизитов.ЗаписыватьКомментарииВДокументы1С);
			КонецЕсли;
			
			Если СтруктураРеквизитов.Свойство("ТекстКомментарияОтправка") Тогда
				ДопРеквизиты_УстановитьЗначение_1С(СвязанныйСправочник, ИменаСвойствНастроек.ТекстКомментарияОтправка, СтруктураРеквизитов.ТекстКомментарияОтправка, Ложь);
			КонецЕсли;
			
			Если СтруктураРеквизитов.Свойство("ТекстКомментарияПолучение") Тогда
				ДопРеквизиты_УстановитьЗначение_1С(СвязанныйСправочник, ИменаСвойствНастроек.ТекстКомментарияПолучение, СтруктураРеквизитов.ТекстКомментарияПолучение);
			КонецЕсли;
			
			Если СтруктураРеквизитов.Свойство("ОтпечатокСертификатаНаСервере") Тогда
				ДопРеквизиты_УстановитьЗначение_1С(СвязанныйСправочник, ИменаСвойствНастроек.ОтпечатокСертификатаНаСервере, СтруктураРеквизитов.ОтпечатокСертификатаНаСервере);
			КонецЕсли;
			
			Если СтруктураРеквизитов.Свойство("ID_ОсновноеПодразделение") Тогда
				ДопРеквизиты_УстановитьЗначение_1С(СвязанныйСправочник, ИменаСвойствНастроек.IDОсновноеПодразделениеОрганизации, СтруктураРеквизитов.ID_ОсновноеПодразделение, Ложь);
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Функция Организации_ТаблицаСопоставленныхОрганизаций_1С()
	
	Запрос = Новый Запрос;
	
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВЫРАЗИТЬ(ЗначенияСвойствОбъектов.Значение КАК СТРОКА(1000)) КАК BoxID,
	|	"""" КАК Наименование,
	|	ЗначенияСвойствОбъектов.Объект КАК Ссылка
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Свойство = &Свойство_ID";
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	ТекстЗапроса	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Запрос.Текст = ТекстЗапроса;
	
	Запрос.УстановитьПараметр("Свойство_ID", Свойства_СсылкаНаСвойство(Перечисление_ИменаСвойствНастроек_1С().IDОрганизации, Ложь));
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Возврат Результат;
	
КонецФункции

// Очищает доп реквизиты организации ЭДО в подсистеме 1С
//
// Параметры:
//	ID - Строка - идентификатор организации ЭДО
Процедура Организации_ОчиститьНастройки_1С(ID)
	
	МассивСопоставленных = Организации_МассивСопоставленныхВ1С_1С(Новый Структура("ID", ID));
	
	Для Каждого СопоставленнаяОрганизация Из МассивСопоставленных Цикл
		
		ДопРеквизиты_УдалитьЗначение_1С(СопоставленнаяОрганизация, Свойства_IDОрганизации());
		ДопРеквизиты_УдалитьЗначение_1С(СопоставленнаяОрганизация, Свойства_IDОсновноеПодразделениеОрганизации());
		ДопРеквизиты_УдалитьЗначение_1С(СопоставленнаяОрганизация, Свойства_ОсновнойПакет());
		ДопРеквизиты_УдалитьЗначение_1С(СопоставленнаяОрганизация, Свойства_ШифроватьДокументы());
		ДопРеквизиты_УдалитьЗначение_1С(СопоставленнаяОрганизация, Свойства_УказыватьОтсутствиеОснованияУПД());
		ДопРеквизиты_УдалитьЗначение_1С(СопоставленнаяОрганизация, Свойства_ОтправлятьНепроведенныеДокументы());
		ДопРеквизиты_УдалитьЗначение_1С(СопоставленнаяОрганизация, Свойства_ЗаписыватьКомментарииВДокументы1С());
		ДопРеквизиты_УдалитьЗначение_1С(СопоставленнаяОрганизация, Свойства_ТекстКомментарияОтправка());
		ДопРеквизиты_УдалитьЗначение_1С(СопоставленнаяОрганизация, Свойства_ТекстКомментарияПолучение());
		ДопРеквизиты_УдалитьЗначение_1С(СопоставленнаяОрганизация, Свойства_ОтпечатокСертификатаНаСервере());
		
	КонецЦикла;	
	
КонецПроцедуры

Функция Организации_ИдентификаторПоСсылке_1С(Идентификаторы)
	
	Ссылка = Идентификаторы.Ссылка;
	
	Результат = ДопРеквизиты_Значение_1С(Ссылка, Перечисление_ИменаСвойствНастроек_1С().IDОрганизации, Ложь);
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ДанныеКонтрагента_1С(Идентификаторы)
	
	Результат = Контракт_КонтрагентЭДО();
	
	СтруктураКонтрагента = Контрагенты_РеквизитыКонтрагента_АПИ(Идентификаторы.ID);
	
	ЗаполнитьЗначенияСвойств(Результат, СтруктураКонтрагента);
	
	МассивСопоставленных = Контрагенты_МассивСопоставленныхВ1С_1С(Идентификаторы);
	
	Сч = 1;
	
	Для Каждого СвязанныйСправочник Из МассивСопоставленных Цикл
		
		Если Сч > 3 Тогда
			Продолжить;
		КонецЕсли;
		
		Результат["СвязанныйСправочник" + Сч] = СвязанныйСправочник;
		
		Сч = Сч + 1;
		
	КонецЦикла;
	
	Если ЗначениеЗаполнено(Результат.СвязанныйСправочник1) Тогда
		ID_ОсновноеПодразделение = ДопРеквизиты_Значение_1С(Результат.СвязанныйСправочник1, Перечисление_ИменаСвойствНастроек_1С().IDОсновноеПодразделениеКонтрагента, Ложь);
		Результат.ID_ОсновноеПодразделение = ?(ID_ОсновноеПодразделение = Неопределено, "", ID_ОсновноеПодразделение);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_СопоставленныйВ1С_1С(Идентификаторы)

	Результат = Неопределено;
	
	МассивСопоставленных = Контрагенты_МассивСопоставленныхВ1С_1С(Идентификаторы);
	
	Если МассивСопоставленных.Количество() > 0 Тогда
		Результат = МассивСопоставленных[0];
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция Контрагенты_СопоставленныеВ1С_1С(Идентификаторы)
	
	Результат = Контрагенты_МассивСопоставленныхВ1С_1С(Идентификаторы);
		
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_МассивСопоставленныхВ1С_1С(Идентификаторы)

	Результат = Новый Массив;
	
	BoxID					= ИдентификаторВАдресЯщика(Идентификаторы.ID);
	Свойство_IDКонтрагента	= Свойства_СсылкаНаСвойство(Перечисление_ИменаСвойствНастроек_1С().IDКонтрагента, Ложь);
	
	Запрос = Новый Запрос;
			
	Запрос.Текст = Свойства_ТекстЗапроса_ЗначениеОбъекта();
	
	Запрос.УстановитьПараметр("Значение",	BoxID);
	Запрос.УстановитьПараметр("Свойство",	Свойство_IDКонтрагента);
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Пока ВыборкаЗапроса.Следующий() Цикл
		Результат.Добавить(ВыборкаЗапроса.Ссылка);
	КонецЦикла;

	Возврат Результат;
	
КонецФункции

Функция Контрагенты_УчастникДО_1С(Идентификаторы, ОрганизацияЭДО)
	
	Результат = Контракт_УчастникДокументооборота();

	BoxID = ИдентификаторВАдресЯщика(Идентификаторы.ID);
	
	Если BoxID = "" Тогда
		BoxID = Контрагенты_ИдентификаторПоСсылке_1С(Идентификаторы);
		Идентификаторы.ID = BoxID;
	КонецЕсли;
	
	СтруктураКонтрагента = Контрагенты_РеквизитыКонтрагента_АПИ(BoxId);
	
	Результат.BoxId				= BoxID;
	Результат.Наименование		= СтруктураКонтрагента.Наименование;
	Результат.FNSParticipantId	= СтруктураКонтрагента.ИдентификаторУчастникаЭДО;
	Результат.ТипОрганизации = СтруктураКонтрагента.Тип;
	
	КонтрагентыВ1С = Контрагенты_СопоставленныеВ1С(Идентификаторы);
	
	Если ЗначениеЗаполнено(КонтрагентыВ1С) Тогда
		
		КонтрагентВ1С = КонтрагентыВ1С[0].Ссылка;
		
		// для совместимости заполним реквизит "Организация"
		Результат.Организация	= КонтрагентВ1С;
				
		// при множественном сопоставлении не важно по какому из сопоставленных контрагентов 1С искать подразделение по умолчанию,
		// т.к. для всех сопоставленных настройка будет одинакова
		Результат.DepartmentId	= ДопРеквизиты_Значение_1С(КонтрагентВ1С, Перечисление_ИменаСвойствНастроек_1С().IDОсновноеПодразделениеКонтрагента, Ложь);
		
	КонецЕсли;	
	
	Возврат Результат;
		
КонецФункции

Процедура Контрагенты_ОбновлениеРеквизитов_1С(Идентификаторы, СтруктураРеквизитов)
			
	Если СтруктураРеквизитов.Свойство("СвязанныеСправочники") Тогда
		
		ID = СвойствоСтруктуры(Идентификаторы, "ID", "");
		ID_ОсновноеПодразделение = СвойствоСтруктуры(СтруктураРеквизитов, "ID_ОсновноеПодразделение");
		
		Контрагенты_ОчиститьНастройки_1С(ID);
							
		Для каждого ЭлементКоллекции Из СтруктураРеквизитов.СвязанныеСправочники Цикл
			Если ЗначениеЗаполнено(ЭлементКоллекции.Ссылка) Тогда
				Контрагенты_УстановитьНастройки_1С(ЭлементКоллекции.Ссылка, ID, ID_ОсновноеПодразделение);
			КонецЕсли;	
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура Контрагенты_ОчиститьНастройки_1С(ID)
	
	МассивСопоставленных = Контрагенты_МассивСопоставленныхВ1С_1С(Новый Структура("ID", ID));
	
	Для Каждого СвязанныйСправочник Из МассивСопоставленных Цикл
		
		ДопРеквизиты_УдалитьЗначение_1С(СвязанныйСправочник, Свойства_IDКонтрагента());
		ДопРеквизиты_УдалитьЗначение_1С(СвязанныйСправочник, Свойства_IDОсновноеПодразделениеКонтрагента());
		
	КонецЦикла;	
	
КонецПроцедуры

Процедура Контрагенты_УстановитьНастройки_1С(СвязанныйСправочник, ID, ID_ОсновноеПодразделение)
	
	Если ЗначениеЗаполнено(СвязанныйСправочник) Тогда
		
		ДопРеквизиты_УстановитьЗначение_1С(СвязанныйСправочник, Перечисление_ИменаСвойствНастроек_1С().IDКонтрагента, ID, Ложь);
		
		Если ЗначениеЗаполнено(ID_ОсновноеПодразделение) Тогда
			ДопРеквизиты_УстановитьЗначение_1С(СвязанныйСправочник, Перечисление_ИменаСвойствНастроек_1С().IDОсновноеПодразделениеКонтрагента, ID_ОсновноеПодразделение, Ложь);
		КонецЕсли;
		
	КонецЕсли;	
	
КонецПроцедуры

Функция Контрагенты_ИдентификаторПоСсылке_1С(Идентификаторы)
	
	Ссылка = Идентификаторы.Ссылка;
	
	Результат = ДопРеквизиты_Значение_1С(Ссылка, Перечисление_ИменаСвойствНастроек_1С().IDКонтрагента, Ложь);
	
	Возврат Результат;
	
КонецФункции


Функция Подразделения_ДанныеПодразделения_1С(Идентификаторы, ДанныеВладельца, ДляОрганизации)

	Результат = Подразделения_РеквизитыПодразделения_АПИ(Идентификаторы, ДанныеВладельца, ДляОрганизации);
			
	Возврат Результат;

КонецФункции

Функция Подразделения_МассивСопоставленныхВ1С_1С(Идентификаторы, ДляОрганизации)

	Результат = Новый Массив;
	
	BoxID = Идентификаторы.ID;
	
	Свойство_IDПодразделения = ИмяСвойстваПодразделения(ДляОрганизации);
	
	Запрос = Новый Запрос;
	
	Запрос.Текст = Свойства_ТекстЗапроса_ЗначениеОбъекта();
			
	Запрос.УстановитьПараметр("Значение",	BoxID);
	Запрос.УстановитьПараметр("Свойство",	Свойство_IDПодразделения);
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Пока ВыборкаЗапроса.Следующий() Цикл
		Результат.Добавить(ВыборкаЗапроса.Ссылка);
	КонецЦикла;

	Возврат Результат;
	
КонецФункции

Функция Подразделения_СопоставленныйВ1С_1С(Идентификаторы, ДляОрганизации)

	Результат = Неопределено;
	
	МассивСопоставленных = Подразделения_МассивСопоставленныхВ1С_1С(Идентификаторы, ДляОрганизации);
	
	Если МассивСопоставленных.Количество() > 0 Тогда
		Результат = МассивСопоставленных[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Подразделения_СопоставленныеВ1С_1С(Идентификаторы, ДляОрганизации = Истина)
	
	Результат = Подразделения_МассивСопоставленныхВ1С_1С(Идентификаторы, ДляОрганизации);
		
	Возврат Результат;
	
КонецФункции

Функция Подразделения_ТаблицаПодразделенийВладельца_1С(ДанныеВладельца, ДанныеОрганизации)
	
	ДляОрганизации = (ДанныеОрганизации = Неопределено);
	
	ТаблицаКонтрактов = Подразделения_ТаблицаКонтрактовВладельца(ДанныеВладельца, ДляОрганизации);
	
	Результат = Подразделения_ЗаполненныеСвязиВТаблицеКонтрактов_1С(ТаблицаКонтрактов, ДляОрганизации);
	
	Возврат Результат;
	
КонецФункции

Функция Подразделения_ЗаполненныеСвязиВТаблицеКонтрактов_1С(ТаблицаКонтрактов, ДляОрганизации)
	
	Запрос = Новый Запрос;
	
	ТекстЗапроса = 
	"ВЫБРАТЬ
	|	ТаблицаКонтрактов.ID,
	|	ТаблицаКонтрактов.ИНН,
	|	ТаблицаКонтрактов.КПП,
	|	ТаблицаКонтрактов.Наименование,
	|	ТаблицаКонтрактов.Аббревиатура,
	|	ТаблицаКонтрактов.ID_ОсновноеПодразделение,
	|	ТаблицаКонтрактов.ID_ВладелецПодразделения,
	|	ТаблицаКонтрактов.ID_РодительПодразделения
	|ПОМЕСТИТЬ ВТ_ТаблицаКонтрактов
	|ИЗ
	|	&ТаблицаКонтрактов КАК ТаблицаКонтрактов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВТ_ТаблицаКонтрактов.ID,
	|	МАКСИМУМ(ЗначенияСвойствОбъектов.Объект) КАК Связь1
	|ПОМЕСТИТЬ ВТ_ПервыйСвязанный
	|ИЗ
	|	ВТ_ТаблицаКонтрактов КАК ВТ_ТаблицаКонтрактов
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|		ПО ВТ_ТаблицаКонтрактов.ID = ЗначенияСвойствОбъектов.Значение
	|			И (ЗначенияСвойствОбъектов.Свойство = &Свойство_BoxId)
	|
	|СГРУППИРОВАТЬ ПО
	|	ВТ_ТаблицаКонтрактов.ID
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|
	|ВЫБРАТЬ
	|	ВТ_ТаблицаКонтрактов.ID,
	|	ВТ_ТаблицаКонтрактов.ИНН,
	|	ВТ_ТаблицаКонтрактов.КПП,
	|	ВТ_ТаблицаКонтрактов.Наименование,
	|	ВТ_ТаблицаКонтрактов.Аббревиатура,
	|	ВТ_ТаблицаКонтрактов.ID_ОсновноеПодразделение,
	|	ВТ_ТаблицаКонтрактов.ID_ВладелецПодразделения,
	|	ВТ_ТаблицаКонтрактов.ID_РодительПодразделения,
	|	ВТ_ПервыйСвязанный.Связь1 КАК СвязанныйСправочник1,
	|	НЕОПРЕДЕЛЕНО КАК Ссылка
	|ИЗ
	|	ВТ_ТаблицаКонтрактов КАК ВТ_ТаблицаКонтрактов
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ПервыйСвязанный КАК ВТ_ПервыйСвязанный
	|		ПО ВТ_ТаблицаКонтрактов.ID = ВТ_ПервыйСвязанный.ID
	|;
    |
    |////////////////////////////////////////////////////////////////////////////////
    |УНИЧТОЖИТЬ ВТ_ТаблицаКонтрактов
	|;
    |
    |////////////////////////////////////////////////////////////////////////////////
    |УНИЧТОЖИТЬ ВТ_ПервыйСвязанный";
		
	ИмяСвойства = ИмяСвойстваПодразделения(ДляОрганизации);
	
	Запрос.УстановитьПараметр("Свойство_BoxId", 	ИмяСвойства);
	Запрос.УстановитьПараметр("ТаблицаКонтрактов",	ТаблицаКонтрактов);
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	ТекстЗапроса	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Запрос.Текст = ТекстЗапроса;
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Возврат Результат;
	
КонецФункции

Процедура Подразделения_ОбновлениеРеквизитов_1С(Подразделение, СтруктураРеквизитов, ДляОрганизации)
	
	Если СтруктураРеквизитов.Свойство("СвязанныеСправочники") Тогда
		
		ID = СвойствоСтруктуры(Подразделение, "ID", "");
		
		Подразделения_ОчиститьНастройки_1С(ID, ДляОрганизации);
		
		ИмяСвойства = ИмяСвойстваПодразделения(ДляОрганизации);
							
		Для каждого ЭлементКоллекции Из СтруктураРеквизитов.СвязанныеСправочники Цикл
			Если ЗначениеЗаполнено(ЭлементКоллекции.Ссылка) Тогда
				ДопРеквизиты_УстановитьЗначение_1С(ЭлементКоллекции.Ссылка, ИмяСвойства, ID);
			КонецЕсли;	
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура Подразделения_ОчиститьНастройки_1С(ID, ДляОрганизации)
	
	МассивСопоставленных	= Подразделения_МассивСопоставленныхВ1С_1С(Новый Структура("ID", ID), ДляОрганизации);
	ИмяСвойства				= ИмяСвойстваПодразделения(ДляОрганизации);
	
	Для Каждого СвязанныйСправочник Из МассивСопоставленных Цикл
		ДопРеквизиты_УдалитьЗначение_1С(СвязанныйСправочник, ИмяСвойства);
	КонецЦикла;	
	
КонецПроцедуры

// См. метод Подразделения_ИдентификаторПоСвязаннымСправочникам
Функция Подразделения_ИдентификаторПоСвязаннымСправочникам_1С(СвязанныйСправочник, ДляОрганизации = Истина)
	
	ЗначениеПоУмолчанию = "";
	
	СсылкаНаСвойство = ИмяСвойстваПодразделения(ДляОрганизации);
	
	ЗначениеСвойства = Свойства_ЗначениеСвойства(СсылкаНаСвойство, СвязанныйСправочник, Истина);
	
	Если ЗначениеСвойства = Неопределено Тогда
		Результат = ЗначениеПоУмолчанию;
	Иначе
		Результат = ЗначениеСвойства;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ИмяСвойстваПодразделения(ДляОрганизации)
	
	Если ДляОрганизации Тогда
		Результат = Свойства_IDПодразделенияОрганизации();
	Иначе
		Результат = Свойства_IDПодразделенияКонтрагента();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции	

// Исправляет некорректо указанные свойства в связях подразделений Диадок и подразделений 1С
// Данный метод заменяет некорректное свойство "DDPro_DDPro_IDПодразделенияКонтрагент"
// в записях регистра на "DDPro_IDПодразделенияКонтрагент".
// Исправление внесено в бэтмене 4.20.4. В дальнейшем данный функционал будет удален
//
Процедура Подразделения_ЗаменитьСвойстваПодразделенийКонтрагентовПриОбновлении_1С() Экспорт
	
	// BSLLS:SetPrivilegedMode-off
	УстановитьПривилегированныйРежим(Истина);
	// BSLLS:SetPrivilegedMode-on
	
	IDПодразделенияКонтрагента 	= Перечисление_ИменаСвойствНастроек_1С().IDПодразделенияКонтрагента;
	КорректноеИмяСвойства 		= ПрефиксНастройки_1С(Истина) + IDПодразделенияКонтрагента;
		
	СПрефиксом = Истина;
	НекорректноеСвойство = Свойства_НайтиСвойство(КорректноеИмяСвойства, СПрефиксом);
	Если Не ЗначениеЗаполнено(НекорректноеСвойство) Тогда
		Возврат;
	КонецЕсли;
	
	ТекстЗапроса = 
	"ВЫБРАТЬ
	|	*
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов
	|ГДЕ
	|	Свойство = &Свойство";
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	ТекстЗапроса	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);

	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("Свойство", НекорректноеСвойство);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Если Не РезультатЗапроса.Пустой() Тогда
		
		ДляОрганизации = Ложь;
		КорректноеСвойство = ИмяСвойстваПодразделения(ДляОрганизации);
		
		Выборка = РезультатЗапроса.Выбрать();
		Пока Выборка.Следующий() Цикл
			
			ЗаписьРегистра = РегистрыСведений[ИмяРегистра].СоздатьМенеджерЗаписи();
			ЗаполнитьЗначенияСвойств(ЗаписьРегистра, Выборка);
			
			ЗаписьРегистра.Прочитать();
			ЗаписьРегистра.Свойство = КорректноеСвойство;
			ЗаписьРегистра.Записать(Истина);
			
		КонецЦикла;
		
	КонецЕсли;
	
	Если ПривилегированныйРежим() Тогда
		УстановитьПривилегированныйРежим(Ложь);
	КонецЕсли;
	
КонецПроцедуры

//}		СПРАВОЧНИКИ: ОРГАНИЗАЦИИ, КОНТРАГЕНТЫ, ПОДРАЗДЕЛЕНИЯ


///////////////////////////////////////////////////////////////////////////////
//{		СПИСКИ КОНТРАГЕНТОВ

Функция Контрагенты_СписокСоСтатусами(ПараметрыСписка) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_СписокСоСтатусами_Диадок(ПараметрыСписка);
	Иначе
		Результат = Контрагенты_СписокСоСтатусами_1С(ПараметрыСписка);
	КонецЕсли;
	
	Возврат Результат;	
	
КонецФункции

Процедура Контрагенты_ДополнитьТаблицуКонтрактовДаннымиИБ(ТаблицаКонтрактов, ОрганизацияЭДО, ДобавитьРеквизитыКонтрагентовИБ) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Контрагенты_ДополнитьТаблицуКонтрактовДаннымиИБ_Диадок(ТаблицаКонтрактов, ОрганизацияЭДО, ДобавитьРеквизитыКонтрагентовИБ);
	Иначе
		Контрагенты_ДополнитьТаблицуКонтрактовДаннымиИБ_1С(ТаблицаКонтрактов, ОрганизацияЭДО, ДобавитьРеквизитыКонтрагентовИБ);
	КонецЕсли;
	
КонецПроцедуры

// Выполняет сопоставление контрагентов ЭДО с контрагентами 1С по ИНН и КПП.
// Результат сопоставления сохраняется в информационной базе в виде связи контрагентов.
//
// Параметры:
//  Организация			 - Структура - данные организации ЭДО (см. Модуль_Ядро.Контракт_ОрганизацияЭДО)
//  СтатусКонтрагентаЭДО - Строка	 - значение статуса контрагента ЭДО (варианты статусов см. Модуль_Ядро.Перечисление_СтатусыКонтрагентов)
// 
// Возвращаемое значение:
//  Структура - структура с описанием результата сопоставления. Состав полей:
//				- КоличествоСопоставленных	- Число 	- количество успешно сопоставленных контрагентов
//				- МассивОшибокДублей		- Массив 	- содержит строки с описанием дублей ящиков ЭДО по ИНН и КПП.
//				- МассивОшибокСопоставления	- Массив	- 
//
Функция Контрагенты_РезультатГрупповогоСопоставления(Организация, СтатусКонтрагентаЭДО = Неопределено) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_РезультатГрупповогоСопоставления_Диадок(Организация, СтатусКонтрагентаЭДО);
	Иначе
		Результат = Контрагенты_РезультатГрупповогоСопоставления_1С(Организация, СтатусКонтрагентаЭДО);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстСвязанныхСправочников()
	
	ТекстЗапроса =  
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	КонтрагентыСервиса.ИНН КАК ИНН,
	|	КонтрагентыСервиса.КПП КАК КПП,
	|	СУММА(КонтрагентыСервиса.КоличествоЯщиковДД) КАК КоличествоЯщиковДД,
	|	СУММА(КонтрагентыСервиса.КоличествоЯщиковРоуминг) КАК КоличествоЯщиковРоуминг
	|ПОМЕСТИТЬ ВремДублиПоИННКПП
	|ИЗ
	|	ВТ_КонтрагентыСервиса КАК КонтрагентыСервиса
	|
	|СГРУППИРОВАТЬ ПО
	|	КонтрагентыСервиса.ИНН,
	|	КонтрагентыСервиса.КПП
	|
	|ИМЕЮЩИЕ
	|	(СУММА(КонтрагентыСервиса.КоличествоЯщиковДД) > 1
	|		ИЛИ СУММА(КонтрагентыСервиса.КоличествоЯщиковДД) = 0
	|			И СУММА(КонтрагентыСервиса.КоличествоЯщиковРоуминг) > 1)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Контрагенты1С.Ссылка КАК КонтрагентИБ,
	|	КонтрагентыСервиса.Ссылка КАК КонтрагентСервиса_Ссылка,
	|	КонтрагентыСервиса.ID КАК КонтрагентСервиса_ID
	|ПОМЕСТИТЬ ВремСвязиКонтрагентовПоИНН
	|ИЗ
	|	Справочник.Контрагенты КАК Контрагенты1С
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КонтрагентыСервиса КАК КонтрагентыСервиса
	|		ПО Контрагенты1С.ИНН = КонтрагентыСервиса.ИНН
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВремДублиПоИННКПП КАК Дубли
	|		ПО Контрагенты1С.ИНН = Дубли.ИНН
	|			И Контрагенты1С.КПП = Дубли.КПП
	|ГДЕ
	|	НЕ Контрагенты1С.ПометкаУдаления
	|	И НЕ КонтрагентыСервиса.Ссылка ЕСТЬ NULL
	|	И Дубли.ИНН ЕСТЬ NULL
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	СвязиКонтрагентов.КонтрагентИБ,
	|	СвязиКонтрагентов.КонтрагентСервиса_Ссылка,
	|	СвязиКонтрагентов.КонтрагентСервиса_ID
	|ПОМЕСТИТЬ ВремНовыеСвязи
	|ИЗ
	|	ВремСвязиКонтрагентовПоИНН КАК СвязиКонтрагентов
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ТаблицаСоСвязями КАК ТаблицаСоСвязями
	|		ПО СвязиКонтрагентов.КонтрагентИБ = ТаблицаСоСвязями.Контрагент1С
	|			И СвязиКонтрагентов.КонтрагентСервиса_Ссылка = ТаблицаСоСвязями.КонтрагентЭДО
	|ГДЕ
	|	ТаблицаСоСвязями.ID ЕСТЬ NULL
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ 
	|	НастроенныеСвязиКонтрагентов.Контрагент1С КАК КонтрагентИБ,
	|	НастроенныеСвязиКонтрагентов.КонтрагентЭДО КАК КонтрагентСервиса_Ссылка,
	|	НастроенныеСвязиКонтрагентов.ID КАК КонтрагентСервиса_ID
	|ИЗ
	|	ВТ_ТаблицаСоСвязями КАК НастроенныеСвязиКонтрагентов
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВремНовыеСвязи КАК НовыеСвязи
	|		ПО НастроенныеСвязиКонтрагентов.ID = НовыеСвязи.КонтрагентСервиса_ID
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ВремНовыеСвязи.КонтрагентИБ,
	|	ВремНовыеСвязи.КонтрагентСервиса_Ссылка,
	|	ВремНовыеСвязи.КонтрагентСервиса_ID
	|ИЗ
	|	ВремНовыеСвязи КАК ВремНовыеСвязи
	|ИТОГИ ПО
	|	КонтрагентСервиса_ID";
	
	ОбъектМд = МетаданныеСправочникаКонтрагенты();
	Результат = СтрЗаменить(ТекстЗапроса, "Справочник.Контрагенты", ОбъектМд.ПолноеИмя());
	
	Если Не Общее_ЕстьРеквизитОбъекта("КПП", ОбъектМд) Тогда
		Результат = СтрЗаменить(Результат, "Контрагенты1С.КПП = Дубли.КПП", "ИСТИНА");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстДублейЯщиков()
	
	Результат = 
	"ВЫБРАТЬ
	|	Дубли.ИНН КАК ИНН,
	|	Дубли.КПП КАК КПП,
	|	СУММА(Дубли.КоличествоЯщиковДД) КАК КоличествоЯщиковДД,
	|	СУММА(Дубли.КоличествоЯщиковРоуминг) КАК КоличествоЯщиковРоуминг
	|ИЗ
	|	ВремДублиПоИННКПП КАК Дубли
	|
	|СГРУППИРОВАТЬ ПО
	|	Дубли.ИНН,
	|	Дубли.КПП
	|	
	|ИМЕЮЩИЕ
	|	СУММА(Дубли.КоличествоЯщиковДД) > 1 ИЛИ
	|	(СУММА(Дубли.КоличествоЯщиковДД) = 0 И СУММА(Дубли.КоличествоЯщиковРоуминг) > 1)";
		
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_УстановленныеСвязи(РезультатЗапроса)
	
	Результат = Новый Структура;
	
	КоличествоСопоставленных	= 0;
	МассивОшибокСопоставления	= Новый Массив;
	
	ВыборкаКонтрагентыЭДО = РезультатЗапроса.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	Пока ВыборкаКонтрагентыЭДО.Следующий() Цикл
		
		КонтрагентЭДО = Неопределено;
		СвязанныеСправочники = Новый Массив;
		
		ВыборкаКонтрагентов1С = ВыборкаКонтрагентыЭДО.Выбрать();
		Пока ВыборкаКонтрагентов1С.Следующий() Цикл
			
			Если КонтрагентЭДО = Неопределено Тогда
				КонтрагентЭДО = ВыборкаКонтрагентов1С.КонтрагентСервиса_Ссылка;
			КонецЕсли;
			
			СвязанныеСправочники.Добавить(ВыборкаКонтрагентов1С.КонтрагентИБ);
			
		КонецЦикла;
		
		ID				= ВыборкаКонтрагентыЭДО.КонтрагентСервиса_ID;
		Идентификаторы	= Новый_ИдентификаторыСправочника(КонтрагентЭДО, ID);
		
		СтруктураРеквизитов	= Новый Структура("СвязанныеСправочники", СвязанныеСправочники);
		
		Контрагенты_ОбновлениеРеквизитов(Идентификаторы, СтруктураРеквизитов);
		
		КоличествоСопоставленных = КоличествоСопоставленных + 1;
		
	КонецЦикла;
	
	Результат.Вставить("КоличествоСопоставленных",	КоличествоСопоставленных);
	Результат.Вставить("МассивОшибокСопоставления",	МассивОшибокСопоставления);
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ДополнитьОшибкиСопоставления(ВыборкаКонтрагентов)
	
	Результат = Новый Массив;
	
	Пока ВыборкаКонтрагентов.Следующий() Цикл
		
		КоличествоЯщиков = ВыборкаКонтрагентов.КоличествоЯщиковДД;
		
		Если КоличествоЯщиков > 1 Тогда
			
			ИНН = ВыборкаКонтрагентов.ИНН;
			КПП = ВыборкаКонтрагентов.КПП;
			
			ТекстОшибки = Контрагенты_ЗаполнитьОшибкуСопоставления(ИНН, КПП, КоличествоЯщиков, "ДублиДиадока");
			Результат.Добавить(ТекстОшибки);
						
		КонецЕсли;
		
		КоличествоЯщиков = ВыборкаКонтрагентов.КоличествоЯщиковРоуминг;
		
		Если КоличествоЯщиков > 1 Тогда
			
			ИНН = ВыборкаКонтрагентов.ИНН;
			КПП = ВыборкаКонтрагентов.КПП;
			
			ТекстОшибки = Контрагенты_ЗаполнитьОшибкуСопоставления(ИНН, КПП, КоличествоЯщиков, "ДублиРоуминга");
			Результат.Добавить(ТекстОшибки);
						
		КонецЕсли;
		
	КонецЦикла;
		
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ЗаполнитьОшибкуСопоставления(ИНН, КПП, КоличествоЯщиков, ВариантОшибки)
	
	Результат = "";
	
	ОписаниеОшибки	= НСтр("ru = 'Не удалось сопоставить контрагентов с ИНН/КПП %1/%2 по причине:'") + Символы.ПС;
	ОписаниеОшибки	= СтрШаблонЛок(ОписаниеОшибки
					, ?(ЗначениеЗаполнено(ИНН), ИНН, "<не указан>")
					, ?(ЗначениеЗаполнено(КПП), КПП, "<не указан>"));
					
	Если ВариантОшибки = "Дубли1С" Тогда
		ПричинаОшибки = НСтр("ru = 'Найдены дубли (%1 шт.) в справочнике 1С.'");
	ИначеЕсли ВариантОшибки = "ДублиДиадока" Тогда
		ПричинаОшибки = НСтр("ru = 'В сервисе Диадок заведено несколько ящиков (%1 шт.).'");
	ИначеЕсли ВариантОшибки = "ДублиРоуминга" Тогда
		ПричинаОшибки = НСтр("ru = 'В роуминге заведено несколько ящиков (%1 шт.).'");	
	КонецЕсли;	
	
	ПричинаОшибки	= СтрШаблонЛок(ПричинаОшибки, КоличествоЯщиков);
	
	Результат = ОписаниеОшибки + ПричинаОшибки;
	
	Возврат Результат;
	
КонецФункции	

//**********************************************
// СПИСКИ КОНТРАГЕНТОВ: ПОДСИСТЕМА ДИАДОКА

Функция Контрагенты_СписокСоСтатусами_Диадок(ПараметрыСписка)
	
	ТекстЗапроса = Контрагенты_ТекстЗапросаВременнаяТаблицаКонтрагентовОрганизацииСоСтатусами_Диадок();
	
	Если ПараметрыСписка.ДобавитьРеквизитыКонтрагентовИБ Тогда
		ТекстЗапроса = ТекстЗапроса + ТекстЗапроса_РазделительЗапросов()
			+ Справочники_ТекстЗапросаВременнойТаблицыСвязиОбъектов_Диадок("Контрагенты");	
	КонецЕсли;
	
	ТекстЗапроса = ТекстЗапроса + ТекстЗапроса_РазделительЗапросов()
		+ Контрагенты_ТекстЗапросаРеквизитыКонтрагентовОрганизации_Диадок();
		
	Если ПараметрыСписка.ДобавитьРеквизитыКонтрагентовИБ Тогда
		
		ТекстЗапросаРеквизитыКонтрагентовИБ = ",
		|	СвязиКонтрагентов.ОбъектИБ КАК КонтрагентИБ,
		|	СвязиКонтрагентов.НаименованиеОбъектаИБ КАК НаименованиеКонтрагентаИБ,
		|	СвязиКонтрагентов.ИННОбъектаИБ КАК ИННКонтрагентаИБ,
		|	СвязиКонтрагентов.КППОбъектаИБ КАК КППКонтрагентаИБ,
		|	СвязиКонтрагентов.ИмяРеквизита КАК ИмяРеквизита";
		
		ТекстЗапросаСоединениеСКонтрагентамиИБ = "
		|	ЛЕВОЕ СОЕДИНЕНИЕ ВремСвязиОбъектов КАК СвязиКонтрагентов
		|	ПО Диадок_ЮрФизЛица.Ссылка = СвязиКонтрагентов.ОбъектДиадок";
		
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "//ПОЛЯ"		, ТекстЗапросаРеквизитыКонтрагентовИБ);
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "//СОЕДИНЕНИЯ"	, ТекстЗапросаСоединениеСКонтрагентамиИБ);
		
	КонецЕсли;
		
	Если ПараметрыСписка.Упорядочить Тогда
		ТекстЗапроса = ТекстЗапроса + ТекстЗапроса_ПоляУпорядочивания("Наименование");
	КонецЕсли;
		
	Если ПараметрыСписка.ДобавитьРеквизитыКонтрагентовИБ Тогда
		ТекстЗапроса = ТекстЗапроса + ТекстЗапроса_ПоляИтогов("Ссылка");
	КонецЕсли;
		
	Запрос = Новый Запрос(ТекстЗапроса);
	
	Отбор = ПараметрыСписка.Отбор;
	
	Запрос.УстановитьПараметр("Организация",Отбор.ОрганизацияЭДО.Ссылка);
	Запрос.УстановитьПараметр("ИНН",		Отбор.ОрганизацияЭДО.ИНН);
	Запрос.УстановитьПараметр("КПП",		Отбор.ОрганизацияЭДО.КПП);
		
	Запрос.УстановитьПараметр("Статусы",	Отбор.Статусы);
	
	Запрос.УстановитьПараметр("ШаблонИмениРеквизита_СвязанныйСправочник", ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник());
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ТаблицаКонтрактов = Ядро.Контрагенты_НоваяТаблицаКонтрактов();
	
	Если ПараметрыСписка.ДобавитьРеквизитыКонтрагентовИБ Тогда
		
		ТаблицаКонтрактов.Колонки.Добавить("РеквизитыСвязанныхСправочников", Новый ОписаниеТипов("Массив"));
		
		ВыборкаКонтрагентовЭДО = РезультатЗапроса.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
		Пока ВыборкаКонтрагентовЭДО.Следующий() Цикл
			
			СтрокаКонтракта = ТаблицаКонтрактов.Добавить();
			
			СвязанныеКонтрагентыИБ = Новый Массив;
			
			ВыборкаДетальныхДанных = ВыборкаКонтрагентовЭДО.Выбрать();
			Пока ВыборкаДетальныхДанных.Следующий() Цикл
				
				Если Не ЗначениеЗаполнено(СтрокаКонтракта.ID) Тогда
					ЗаполнитьЗначенияСвойств(СтрокаКонтракта, ВыборкаДетальныхДанных);	
				КонецЕсли;
				
				Если Не ЗначениеЗаполнено(ВыборкаДетальныхДанных.КонтрагентИБ) Тогда
					Продолжить;
				КонецЕсли;
				
				ЗначенияРеквизитовКонтрагентаИБ = Контракт_РеквизитыКонтрагентов1С();
				ЗначенияРеквизитовКонтрагентаИБ.Ссылка 			= ВыборкаДетальныхДанных.КонтрагентИБ;
				ЗначенияРеквизитовКонтрагентаИБ.Наименование 	= ВыборкаДетальныхДанных.НаименованиеКонтрагентаИБ;
				ЗначенияРеквизитовКонтрагентаИБ.ИНН 			= ВыборкаДетальныхДанных.ИННКонтрагентаИБ;
				ЗначенияРеквизитовКонтрагентаИБ.КПП 			= ВыборкаДетальныхДанных.КППКонтрагентаИБ;
				
				СтрокаКонтракта.РеквизитыСвязанныхСправочников.Добавить(ЗначенияРеквизитовКонтрагентаИБ);
				
			КонецЦикла;
			
		КонецЦикла;	
		
	Иначе
		Выборка = РезультатЗапроса.Выбрать();
		Пока Выборка.Следующий() Цикл
			СтрокаКонтракта = ТаблицаКонтрактов.Добавить();
			ЗаполнитьЗначенияСвойств(СтрокаКонтракта, Выборка);
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат ТаблицаКонтрактов;	
	
КонецФункции

Процедура Контрагенты_ДополнитьТаблицуКонтрактовДаннымиИБ_Диадок(ТаблицаКонтрактов, ОрганизацияЭДО, ДобавитьРеквизитыКонтрагентовИБ)
	
	Если ДобавитьРеквизитыКонтрагентовИБ Тогда
		ТаблицаКонтрактов.Колонки.Добавить("РеквизитыСвязанныхСправочников", Новый ОписаниеТипов("Массив"));
	КонецЕсли;
	
	ТекстЗапроса =
		"ВЫБРАТЬ
		|	Диадок_НастройкиКонтрагентов.Контрагент КАК Ссылка
		|ПОМЕСТИТЬ ВТ_ЮрФизЛицаДиадок
		|ИЗ
		|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
		|ГДЕ
		|	Диадок_НастройкиКонтрагентов.Организация = &Организация
		|	И Диадок_НастройкиКонтрагентов.Контрагент.ID В(&МассивBoxId)
		|	И Диадок_НастройкиКонтрагентов.ИмяСвойства = ""Статус""";
	
	Если ДобавитьРеквизитыКонтрагентовИБ Тогда
		ТекстЗапроса = ТекстЗапроса + ТекстЗапроса_РазделительЗапросов()
			+ Справочники_ТекстЗапросаВременнойТаблицыСвязиОбъектов_Диадок("Контрагенты");
	КонецЕсли;
	
	ТекстЗапросаРеквизитыКонтрагентовОрганизации = 
		"ВЫБРАТЬ
		|	Диадок_ЮрФизЛица.ID КАК ID,
		|	Диадок_ЮрФизЛица.Ссылка КАК Ссылка,
		|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение КАК ID_ОсновноеПодразделение
		|	//ПОЛЯ
		|ИЗ
		|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ЮрФизЛицаДиадок КАК КонтрагентыЭДО
		|		ПО Диадок_ЮрФизЛица.Ссылка = КонтрагентыЭДО.Ссылка
		|		//СОЕДИНЕНИЯ
		|ГДЕ
		|	Диадок_ЮрФизЛица.ID В(&МассивBoxId)
		|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	ТекстЗапроса = ТекстЗапроса + ТекстЗапроса_РазделительЗапросов() + ТекстЗапросаРеквизитыКонтрагентовОрганизации;
	
	Если ДобавитьРеквизитыКонтрагентовИБ Тогда
		
		ТекстЗапросаРеквизитыКонтрагентовИБ = ",
		|	СвязиКонтрагентов.ОбъектИБ КАК КонтрагентИБ,
		|	СвязиКонтрагентов.НаименованиеОбъектаИБ КАК НаименованиеКонтрагентаИБ,
		|	СвязиКонтрагентов.ИННОбъектаИБ КАК ИННКонтрагентаИБ,
		|	СвязиКонтрагентов.КППОбъектаИБ КАК КППКонтрагентаИБ,
		|	СвязиКонтрагентов.ИмяРеквизита КАК ИмяРеквизита";
		
		ТекстЗапросаСоединениеСКонтрагентамиИБ = "
		|	ЛЕВОЕ СОЕДИНЕНИЕ ВремСвязиОбъектов КАК СвязиКонтрагентов
		|	ПО Диадок_ЮрФизЛица.Ссылка = СвязиКонтрагентов.ОбъектДиадок";
		
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "//ПОЛЯ"		, ТекстЗапросаРеквизитыКонтрагентовИБ);
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "//СОЕДИНЕНИЯ"	, ТекстЗапросаСоединениеСКонтрагентамиИБ);
		
	КонецЕсли;
	
	Если ДобавитьРеквизитыКонтрагентовИБ Тогда
		ТекстЗапроса = ТекстЗапроса + ТекстЗапроса_ПоляИтогов("Ссылка");
	КонецЕсли;
	
	ИдентификаторыКонтрагентовЭДО = ТаблицаКонтрактов.ВыгрузитьКолонку("ID");
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("Организация", ОрганизацияЭДО.Ссылка);
	Запрос.УстановитьПараметр("МассивBoxId", ИдентификаторыКонтрагентовЭДО);
	Запрос.УстановитьПараметр("ШаблонИмениРеквизита_СвязанныйСправочник", ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник());
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Если ДобавитьРеквизитыКонтрагентовИБ Тогда
		
		ВыборкаКонтрагентовЭДО = РезультатЗапроса.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
		Пока ВыборкаКонтрагентовЭДО.Следующий() Цикл
			
			СтрокаКонтракта = ТаблицаКонтрактов.Найти(ВыборкаКонтрагентовЭДО.ID, "ID");
			Если СтрокаКонтракта = Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			СвязанныеКонтрагентыИБ = Новый Массив;
			
			ВыборкаДетальныхДанных = ВыборкаКонтрагентовЭДО.Выбрать();
			Пока ВыборкаДетальныхДанных.Следующий() Цикл
				
				Если Не ЗначениеЗаполнено(СтрокаКонтракта.Ссылка) Тогда
					ЗаполнитьЗначенияСвойств(СтрокаКонтракта, ВыборкаДетальныхДанных);
				КонецЕсли;
				
				Если Не ЗначениеЗаполнено(ВыборкаДетальныхДанных.КонтрагентИБ) Тогда
					Продолжить;
				КонецЕсли;
				
				ЗначенияРеквизитовКонтрагентаИБ = Контракт_РеквизитыКонтрагентов1С();
				ЗначенияРеквизитовКонтрагентаИБ.Ссылка 			= ВыборкаДетальныхДанных.КонтрагентИБ;
				ЗначенияРеквизитовКонтрагентаИБ.Наименование 	= ВыборкаДетальныхДанных.НаименованиеКонтрагентаИБ;
				ЗначенияРеквизитовКонтрагентаИБ.ИНН 			= ВыборкаДетальныхДанных.ИННКонтрагентаИБ;
				ЗначенияРеквизитовКонтрагентаИБ.КПП 			= ВыборкаДетальныхДанных.КППКонтрагентаИБ;
				
				СтрокаКонтракта.РеквизитыСвязанныхСправочников.Добавить(ЗначенияРеквизитовКонтрагентаИБ);
				
			КонецЦикла;
			
		КонецЦикла;
		
	Иначе
		Выборка = РезультатЗапроса.Выбрать();
		Пока Выборка.Следующий() Цикл
			СтрокаКонтракта = ТаблицаКонтрактов.Добавить();
			ЗаполнитьЗначенияСвойств(СтрокаКонтракта, Выборка);
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Функция Контрагенты_РезультатГрупповогоСопоставления_Диадок(Организация, СтатусКонтрагентаЭДО)
	
	Результат = Новый Структура;
	Результат.Вставить("КоличествоСопоставленных",	0);
	Результат.Вставить("МассивОшибокДублей",		Новый Массив);
	Результат.Вставить("МассивОшибокСопоставления",	Новый Массив);
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц 	= Новый МенеджерВременныхТаблиц;
	
	Если Организация = Неопределено Тогда
		Запрос.УстановитьПараметр("Организация", Организация);
	Иначе
		Запрос.УстановитьПараметр("Организация", Организация.Ссылка);
	КонецЕсли;
	
	Запрос.УстановитьПараметр("СтатусКонтрагентаЭДО", СтатусКонтрагентаЭДО);
	Запрос.УстановитьПараметр("ИмяСправочника", "Контрагенты");
	Запрос.УстановитьПараметр("ИмяРеквизита", ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник());
	
	ТекстЗапроса_РазделительЗапросов = ТекстЗапроса_РазделительЗапросов();
	ТекстЗапросаВТ_КонтрагентыСервиса = Контрагенты_ТекстЗапросаВТ_КонтрагентыСервиса_Диадок(СтатусКонтрагентаЭДО);
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		ТекстЗапросаВТ_ТаблицаСоСвязями = Контрагенты_ТекстЗапроса_ТаблицаСоСвязями_ДиадокРасширение();
	Иначе
		ТекстЗапросаВТ_ТаблицаСоСвязями = Контрагенты_ТекстЗапроса_ТаблицаСоСвязями_ДиадокСХ1();
	КонецЕсли;
	
	ТексЗапросаСвязанныеСправочники = Контрагенты_ТекстСвязанныхСправочников();
		
	ЧастиТекстаЗапроса = Новый Массив;
	
	ЧастиТекстаЗапроса.Добавить(ТекстЗапросаВТ_КонтрагентыСервиса);
	ЧастиТекстаЗапроса.Добавить(ТекстЗапросаВТ_ТаблицаСоСвязями);
	ЧастиТекстаЗапроса.Добавить(ТексЗапросаСвязанныеСправочники);
	
	Запрос.Текст = СоединитьСтроку(ЧастиТекстаЗапроса, ТекстЗапроса_РазделительЗапросов);
	
	РезультатЗапросаСвязанныхКонтрагентов = Запрос.Выполнить();
	
	Запрос.Текст = Контрагенты_ТекстДублейЯщиков();
	ВыборкаДублейКонтрагентов = Запрос.Выполнить().Выбрать();
	
	РезультатСопоставления = Контрагенты_УстановленныеСвязи(РезультатЗапросаСвязанныхКонтрагентов);
	
	ЗаполнитьЗначенияСвойств(Результат, РезультатСопоставления);
	
	Результат.МассивОшибокДублей = Контрагенты_ДополнитьОшибкиСопоставления(ВыборкаДублейКонтрагентов);
	
	Запрос.МенеджерВременныхТаблиц.Закрыть();
	
	Возврат Результат;
	
КонецФункции	

Функция Контрагенты_ТекстЗапросаВТ_КонтрагентыСервиса_Диадок(СтатусКонтрагентаЭДО)

	Результат = 
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ЮрФизЛица.Ссылка КАК Ссылка,
	|	ЮрФизЛица.ID КАК ID,
	|	ЮрФизЛица.ИНН КАК ИНН,
	|	ЮрФизЛица.КПП КАК КПП,
	|	ЮрФизЛица.ИдентификаторУчастникаЭДО КАК ИдентификаторУчастникаЭДО,
	|	ПОДСТРОКА(ЮрФизЛица.ИдентификаторУчастникаЭДО, 1, 3) КАК КодОператораЭДО,
	|	ВЫБОР
	|		КОГДА ПОДСТРОКА(ЮрФизЛица.ИдентификаторУчастникаЭДО, 1, 3) = ""2BM""
	|			ТОГДА 1
	|		ИНАЧЕ 0
	|	КОНЕЦ КАК КоличествоЯщиковДД,
	|	ВЫБОР
	|		КОГДА ПОДСТРОКА(ЮрФизЛица.ИдентификаторУчастникаЭДО, 1, 3) <> ""2BM""
	|			ТОГДА 1
	|		ИНАЧЕ 0
	|	КОНЕЦ КАК КоличествоЯщиковРоуминг
	|ПОМЕСТИТЬ ВТ_КонтрагентыСервиса
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
	|		ПО ЮрФизЛица.Ссылка = Диадок_НастройкиКонтрагентов.Контрагент
	|			И (Диадок_НастройкиКонтрагентов.ИмяСвойства = ""Статус"")
	|			И (&ОтборПоСтатусу)
	|ГДЕ
	|	ЮрФизЛица.ИмяСправочника = &ИмяСправочника
	|	И НЕ ЮрФизЛица.ПометкаУдаления
	|	И (&Организация = НЕОПРЕДЕЛЕНО
	|			ИЛИ Диадок_НастройкиКонтрагентов.Организация = &Организация)";
	
	Если СтатусКонтрагентаЭДО = Неопределено Тогда
		ОтборПоСтатусу = "ИСТИНА";
	Иначе
		ОтборПоСтатусу = "Диадок_НастройкиКонтрагентов.Значение = &СтатусКонтрагентаЭДО";
	КонецЕсли;
	
	Результат = СтрЗаменить(Результат, "&ОтборПоСтатусу", ОтборПоСтатусу);
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапроса_ТаблицаСоСвязями_ДиадокРасширение()

	Результат = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЮрФизЛицаДопРеквизиты.Значение КАК Контрагент1С,
	|	ЮрФизЛица.Ссылка КАК КонтрагентЭДО,
	|	ЮрФизЛица.ID КАК ID,
	|	ЮрФизЛица.ИдентификаторУчастникаЭДО КАК ИдентификаторУчастникаЭДО
	|ПОМЕСТИТЬ ВТ_ТаблицаСоСвязями
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛицаДопРеквизиты
	|		ПО ЮрФизЛица.Ссылка = ЮрФизЛицаДопРеквизиты.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КонтрагентыСервиса КАК КонтрагентыСервиса
	|		ПО ЮрФизЛица.Ссылка = КонтрагентыСервиса.Ссылка
	|ГДЕ
	|	НЕ КонтрагентыСервиса.Ссылка ЕСТЬ NULL
	|	И ЮрФизЛицаДопРеквизиты.ИмяРеквизита ПОДОБНО &ИмяРеквизита
	|	И НЕ ЮрФизЛицаДопРеквизиты.Значение = НЕОПРЕДЕЛЕНО
	|	И НЕ ЮрФизЛицаДопРеквизиты.Значение = ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка)";
	
	ТекстЗапроса_УстановитьИмяТаблицыКонтрагентов(Результат);
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапроса_ТаблицаСоСвязями_ДиадокСХ1()

	Результат = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЮрФизЛица.СвязанныйСправочник1 КАК Контрагент1С,
	|	ЮрФизЛица.Ссылка КАК КонтрагентЭДО,
	|	ЮрФизЛица.ID КАК ID,
	|	ЮрФизЛица.ИдентификаторУчастникаЭДО КАК ИдентификаторУчастникаЭДО
	|ПОМЕСТИТЬ ВТ_ТаблицаСоСвязями
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КонтрагентыСервиса КАК КонтрагентыСервиса
	|		ПО ЮрФизЛица.Ссылка = КонтрагентыСервиса.Ссылка
	|ГДЕ
	|	НЕ КонтрагентыСервиса.Ссылка ЕСТЬ NULL
	|	И НЕ ЮрФизЛица.СвязанныйСправочник1 = НЕОПРЕДЕЛЕНО
	|	И НЕ ЮрФизЛица.СвязанныйСправочник1 = ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ЮрФизЛица.СвязанныйСправочник2,
	|	ЮрФизЛица.Ссылка,
	|	ЮрФизЛица.ID,
	|	ЮрФизЛица.ИдентификаторУчастникаЭДО КАК ИдентификаторУчастникаЭДО
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КонтрагентыСервиса КАК КонтрагентыСервиса
	|		ПО ЮрФизЛица.Ссылка = КонтрагентыСервиса.Ссылка
	|ГДЕ
	|	НЕ КонтрагентыСервиса.Ссылка ЕСТЬ NULL
	|	И НЕ ЮрФизЛица.СвязанныйСправочник2 = НЕОПРЕДЕЛЕНО
	|	И НЕ ЮрФизЛица.СвязанныйСправочник2 = ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ЮрФизЛица.СвязанныйСправочник3,
	|	ЮрФизЛица.Ссылка,
	|	ЮрФизЛица.ID,
	|	ЮрФизЛица.ИдентификаторУчастникаЭДО КАК ИдентификаторУчастникаЭДО
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КонтрагентыСервиса КАК КонтрагентыСервиса
	|		ПО ЮрФизЛица.Ссылка = КонтрагентыСервиса.Ссылка
	|ГДЕ
	|	НЕ КонтрагентыСервиса.Ссылка ЕСТЬ NULL
	|	И НЕ ЮрФизЛица.СвязанныйСправочник3 = НЕОПРЕДЕЛЕНО
	|	И НЕ ЮрФизЛица.СвязанныйСправочник3 = ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ЮрФизЛицаДопРеквизиты.Значение,
	|	ЮрФизЛица.Ссылка,
	|	ЮрФизЛица.ID,
	|	ЮрФизЛица.ИдентификаторУчастникаЭДО КАК ИдентификаторУчастникаЭДО
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛицаДопРеквизиты
	|		ПО ЮрФизЛица.Ссылка = ЮрФизЛицаДопРеквизиты.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КонтрагентыСервиса КАК КонтрагентыСервиса
	|		ПО ЮрФизЛица.Ссылка = КонтрагентыСервиса.Ссылка
	|ГДЕ
	|	НЕ КонтрагентыСервиса.Ссылка ЕСТЬ NULL
	|	И ЮрФизЛицаДопРеквизиты.ИмяРеквизита ПОДОБНО &ИмяРеквизита
	|	И НЕ ЮрФизЛицаДопРеквизиты.Значение = НЕОПРЕДЕЛЕНО
	|	И НЕ ЮрФизЛицаДопРеквизиты.Значение = ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка)";
	
	ТекстЗапроса_УстановитьИмяТаблицыКонтрагентов(Результат);
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапросаВременнаяТаблицаКонтрагентовОрганизацииСоСтатусами_Диадок()
	
	ТекстЗапроса =
		"ВЫБРАТЬ
		|	Диадок_НастройкиКонтрагентов.Контрагент КАК Ссылка,
		|	Диадок_НастройкиКонтрагентов.Значение КАК Статус,
		|	Диадок_НастройкиКонтрагентовДатаИзменения.Значение КАК ДатаИзменения
		|ПОМЕСТИТЬ ВТ_ЮрФизЛицаДиадок
		|ИЗ
		|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентовДатаИзменения
		|		ПО Диадок_НастройкиКонтрагентов.Контрагент = Диадок_НастройкиКонтрагентовДатаИзменения.Контрагент
		|			И Диадок_НастройкиКонтрагентов.Организация = Диадок_НастройкиКонтрагентовДатаИзменения.Организация
		|			И (Диадок_НастройкиКонтрагентовДатаИзменения.ИмяСвойства = ""ДатаИзмененияСтатуса"")
		|ГДЕ
		|	Диадок_НастройкиКонтрагентов.Организация = &Организация
		|	И Диадок_НастройкиКонтрагентов.ИмяСвойства = ""Статус""
		|	И Диадок_НастройкиКонтрагентов.Значение В(&Статусы)";
	
	Возврат ТекстЗапроса;
	
КонецФункции

Функция Контрагенты_ТекстЗапросаРеквизитыКонтрагентовОрганизации_Диадок()
	
	ТекстЗапроса =
		"ВЫБРАТЬ
		|	Диадок_ЮрФизЛица.ID КАК ID,
		|	Диадок_ЮрФизЛица.Ссылка КАК Ссылка,
		|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
		|	Диадок_ЮрФизЛица.ИНН КАК ИНН,
		|	Диадок_ЮрФизЛица.КПП КАК КПП,
		|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение КАК ID_ОсновноеПодразделение,
		|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО КАК ИдентификаторУчастникаЭДО,
		|	КонтрагентыЭДО.Статус КАК Статус,
		|	КонтрагентыЭДО.ДатаИзменения КАК ДатаИзменения,
		|	ДопРеквизитыИдентификаторы.Значение КАК OrgId,
		|	ЕСТЬNULL(ДопРеквизитыТипОрганизации.Значение, """") КАК Тип,
		|	ЕСТЬNULL(ДопРеквизитыЭкстраверт.Значение, ЛОЖЬ) КАК Экстраверт,
		|	ДопРеквизитыДатаЛиквидации.Значение КАК ДатаЛиквидации
		|	//ПОЛЯ
		|ИЗ
		|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
		|
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ЮрФизЛицаДиадок КАК КонтрагентыЭДО
		|		ПО Диадок_ЮрФизЛица.Ссылка = КонтрагентыЭДО.Ссылка
		|
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ДопРеквизитыИдентификаторы
		|		ПО Диадок_ЮрФизЛица.Ссылка = ДопРеквизитыИдентификаторы.Ссылка
		|			И (ДопРеквизитыИдентификаторы.ИмяРеквизита = ""OrgId"")
		|
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ДопРеквизитыТипОрганизации
		|		ПО Диадок_ЮрФизЛица.Ссылка = ДопРеквизитыТипОрганизации.Ссылка
		|			И (ДопРеквизитыТипОрганизации.ИмяРеквизита = ""Тип"")
		|
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ДопРеквизитыЭкстраверт
		|		ПО Диадок_ЮрФизЛица.Ссылка = ДопРеквизитыЭкстраверт.Ссылка
		|			И (ДопРеквизитыЭкстраверт.ИмяРеквизита = ""Экстраверт"")
		|
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ДопРеквизитыДатаЛиквидации
		|		ПО Диадок_ЮрФизЛица.Ссылка = ДопРеквизитыДатаЛиквидации.Ссылка
		|			И (ДопРеквизитыДатаЛиквидации.ИмяРеквизита = ""ДатаЛиквидации"")
		|		//СОЕДИНЕНИЯ
		|ГДЕ
		|	НЕ(Диадок_ЮрФизЛица.ИНН = &ИНН
		|				И Диадок_ЮрФизЛица.КПП = &КПП)
		|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Возврат ТекстЗапроса;
	
КонецФункции

// Функция -  контрагенты сервиса по ИНН из СХ1
//
// Параметры:
//  КоллекцияИНН - Массив - массив ИНН контрагентов
//	ОрганизацияЭДО - СправочникСсылка.Диадок_ЮрФизЛица - Ссылка на организацию в подсистеме Диадок
//
// Возвращаемое значение:
//  ТаблицаЗначений - содержит таблицу значений не связанных контрагентов
//	 * ID - Строка - Идентификатор контрагента в ДД
//	 * Ссылка - СправочникСсылка.Контрагенты
//	 * ИНН - Строка - ИНН контрагент
//	 * КПП - Строка - КПП контрагент
//
Функция Контрагенты_КонтрагентыСервисаПоИНН_Диадок(КоллекцияИНН, ОрганизацияЭДО) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	СправочникЮрФизЛица.ID КАК ID,
		|	СправочникЮрФизЛица.Ссылка КАК Ссылка,
		|	СправочникЮрФизЛица.ИНН КАК ИНН,
		|	СправочникЮрФизЛица.КПП КАК КПП,
		|	ВЫБОР
		|		КОГДА ПОДСТРОКА(ЕСТЬNULL(СправочникЮрФизЛица.ИдентификаторУчастникаЭДО, """"), 1, 3) = ""2BM""
		|			ТОГДА ЛОЖЬ
		|		ИНАЧЕ ИСТИНА
		|	КОНЕЦ КАК Роуминговый,
		|	Диадок_НастройкиКонтрагентов.Значение КАК Статус,
		|	ЕСТЬNULL(ДопРеквизитыДатаЛиквидации.Значение, ДАТАВРЕМЯ(1,1,1,0,0,0)) КАК ДатаЛиквидации
		|ИЗ
		|	Справочник.Диадок_ЮрФизЛица КАК СправочникЮрФизЛица
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
		|		ПО СправочникЮрФизЛица.Ссылка = Диадок_НастройкиКонтрагентов.Контрагент
		|			И (Диадок_НастройкиКонтрагентов.ИмяСвойства = ""Статус"")
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ДопРеквизитыДатаЛиквидации
		|		ПО СправочникЮрФизЛица.Ссылка = ДопРеквизитыДатаЛиквидации.Ссылка
		|			И (ДопРеквизитыДатаЛиквидации.ИмяРеквизита = ""ДатаЛиквидации"")
		|ГДЕ
		|	СправочникЮрФизЛица.ИмяСправочника = ""Контрагенты""
		|	И НЕ СправочникЮрФизЛица.ПометкаУдаления
		|	И СправочникЮрФизЛица.ИНН В(&МассивИНН)
		|	И Диадок_НастройкиКонтрагентов.Организация = &Организация";
	
	Запрос.Параметры.Вставить("МассивИНН", КоллекцияИНН);
	Запрос.Параметры.Вставить("Организация", ОрганизацияЭДО);
	
	ТаблицаКонтрактов = Запрос.Выполнить().Выгрузить();
	
	Возврат ТаблицаКонтрактов;
	
КонецФункции
//**********************************************
// СПИСКИ КОНТРАГЕНТОВ: 1С-ЭДО

Функция Контрагенты_СписокСоСтатусами_1С(ПараметрыСписка)
	
	Организация = ПараметрыСписка.Отбор.ОрганизацияЭДО;
	Статусы		= ПараметрыСписка.Отбор.Статусы;

	Статус = Контрагенты_СтатусАПИ(Статусы);	
	
	ID_Организация = СвойствоСтруктуры(Организация, "ID", "");
	
	МассивCounteragents	= Контрагенты_МассивCounteragents(Организация, Статус);
	ТаблицаКонтрактов	= Контрагенты_ТаблицаКонтрактовПоCounteragents(МассивCounteragents);
	
	НайденныйСтатус = Статусы.Найти(Перечисление_СтатусыКонтрагентов().РаботаетВДиадок);
	
	Если НайденныйСтатус <> Неопределено Тогда
		
		ТаблицаКонтрактов_Приглашение = Контрагенты_ПоискИПриглашениеДляОрганизации(Организация);
		
		Для Каждого ТекКонтрагент Из ТаблицаКонтрактов_Приглашение Цикл
			
			НоваяСтрока = ТаблицаКонтрактов.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, ТекКонтрагент);
			
		КонецЦикла;
		
	КонецЕсли;
	
	ДобавитьРеквизитыКонтрагентовИБ = ПараметрыСписка.ДобавитьРеквизитыКонтрагентовИБ;
	
	Контрагенты_ДополнитьТаблицуКонтрактовДаннымиИБ_1С(ТаблицаКонтрактов, Организация, ДобавитьРеквизитыКонтрагентовИБ);
	
	Если ПараметрыСписка.Упорядочить Тогда
		ТаблицаКонтрактов.Сортировать("Наименование");
	КонецЕсли;

	Возврат ТаблицаКонтрактов;	
	
КонецФункции

Функция Контрагенты_СтатусАПИ(Статусы)
	
	Если Статусы.Найти(Перечисление_СтатусыКонтрагентов().ЗаблокировалМеня) <> Неопределено
		ИЛИ Статусы.Найти(Перечисление_СтатусыКонтрагентов().ЗаблокированМной) <> Неопределено Тогда
		
		Результат = "Rejected";
		
	ИначеЕсли Статусы.Количество() = 1 Тогда
		
		Результат = Статусы[0];
		
	Иначе
		
		Результат = "";
		
	КонецЕсли;	
			
	Возврат Результат;
	
КонецФункции	

Процедура Контрагенты_ДополнитьТаблицуКонтрактовДаннымиИБ_1С(ТаблицаКонтрактов, ОрганизацияЭДО, ДобавитьРеквизитыКонтрагентовИБ)
	
	Если ДобавитьРеквизитыКонтрагентовИБ Тогда
		ТаблицаКонтрактов.Колонки.Добавить("РеквизитыСвязанныхСправочников", Новый ОписаниеТипов("Массив"));
	КонецЕсли;
	
	Свойство_IDОсновногоПодразделения 	= Свойство_IDОсновногоПодразделения();
	Свойство_IDКонтрагента 				= Свойство_IDКонтрагента();
	
	ИдентификаторыКонтрагентовЭДО = ТаблицаКонтрактов.ВыгрузитьКолонку("ID");
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Свойство_BoxId"			, Свойство_IDКонтрагента);
	Запрос.УстановитьПараметр("Свойство_IDПодразделения", Свойство_IDОсновногоПодразделения);
	Запрос.УстановитьПараметр("МассивBoxId"				, ИдентификаторыКонтрагентовЭДО);
	
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЗначенияСвойствОбъектов.Значение КАК ID,
	|	ЗначенияСвойствОбъектов.Объект КАК КонтрагентИБ,
	|	ЕСТЬNULL(ИдентификаторыПодразделений.Значение, """") КАК ID_ОсновноеПодразделение
	|	//ПОЛЯ
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|	ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ЗначенияСвойствОбъектов КАК ИдентификаторыПодразделений
	|	ПО ЗначенияСвойствОбъектов.Объект = ИдентификаторыПодразделений.Объект
	|		И (ИдентификаторыПодразделений.Свойство = &Свойство_IDПодразделения)
	|	//СОЕДИНЕНИЯ
	|	
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Свойство = &Свойство_BoxId
	|	И ЗначенияСвойствОбъектов.Значение В(&МассивBoxId)
	|ИТОГИ ПО
	|	ID";
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	ТекстЗапроса 	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Если ДобавитьРеквизитыКонтрагентовИБ Тогда
		
		ДополнительныеПоляЗапроса = ",
		|	ПРЕДСТАВЛЕНИЕ(ЗначенияСвойствОбъектов.Объект) 	КАК НаименованиеКонтрагентаИБ,
		|	ЕСТЬNULL(КонтрагентыИБ.ИНН, """") 			КАК ИННКонтрагентаИБ,
		|	ЕСТЬNULL(КонтрагентыИБ.КПП, """") 			КАК КППКонтрагентаИБ";
		
		СоединениеДопПоля = "
		|	ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Контрагенты КАК КонтрагентыИБ
		|	ПО ЗначенияСвойствОбъектов.Объект = КонтрагентыИБ.Ссылка";
		
		ОбъектМд = МетаданныеСправочникаКонтрагенты();
		СоединениеДопПоля = СтрЗаменить(СоединениеДопПоля, "Справочник.Контрагенты", ОбъектМд.ПолноеИмя());
		
		Если Не Общее_ЕстьРеквизитОбъекта("ИНН", ОбъектМд) Тогда
			ДополнительныеПоляЗапроса = СтрЗаменить(ДополнительныеПоляЗапроса, "ЕСТЬNULL(КонтрагентыИБ.ИНН, """")", """""");
		КонецЕсли;
		
		Если Не Общее_ЕстьРеквизитОбъекта("КПП", ОбъектМд) Тогда
			ДополнительныеПоляЗапроса = СтрЗаменить(ДополнительныеПоляЗапроса, "ЕСТЬNULL(КонтрагентыИБ.КПП, """")", """""");
		КонецЕсли;
		
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "//ПОЛЯ"		, ДополнительныеПоляЗапроса);
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "//СОЕДИНЕНИЯ"	, СоединениеДопПоля);
		
	КонецЕсли;
	
	Запрос.Текст = ТекстЗапроса;
	
	РезультатЗапроса = Запрос.Выполнить();
	
	МаксимальныйНомерСвязи = 3;
	
	ТаблицаКонтрактов.Индексы.Добавить("ID");
	
	ВыборкаКонтрагентовЭДО = РезультатЗапроса.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	Пока ВыборкаКонтрагентовЭДО.Следующий() Цикл
		
		СтрокаКонтракта = ТаблицаКонтрактов.Найти(ВыборкаКонтрагентовЭДО.ID, "ID");
		Если СтрокаКонтракта = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		ТекущийНомерСвязи = 1;
		
		ВыборкаДетальныхДанных = ВыборкаКонтрагентовЭДО.Выбрать();
		Пока ВыборкаДетальныхДанных.Следующий() Цикл
			
			Если Не ЗначениеЗаполнено(ВыборкаДетальныхДанных.КонтрагентИБ) Тогда
				Продолжить;
			КонецЕсли; 
			
			Если ТекущийНомерСвязи <= МаксимальныйНомерСвязи Тогда
				СтрокаКонтракта["СвязанныйСправочник" + ТекущийНомерСвязи] = ВыборкаДетальныхДанных.КонтрагентИБ;
			КонецЕсли;
			
			Если ТекущийНомерСвязи = 1 Тогда
				СтрокаКонтракта.ID_ОсновноеПодразделение = ВыборкаДетальныхДанных.ID_ОсновноеПодразделение;
			КонецЕсли;
			
			Если ДобавитьРеквизитыКонтрагентовИБ Тогда
				
				ЗначенияРеквизитовКонтрагентаИБ = Контракт_РеквизитыКонтрагентов1С();
				ЗначенияРеквизитовКонтрагентаИБ.Ссылка 			= ВыборкаДетальныхДанных.КонтрагентИБ;
				ЗначенияРеквизитовКонтрагентаИБ.Наименование 	= ВыборкаДетальныхДанных.НаименованиеКонтрагентаИБ;
				ЗначенияРеквизитовКонтрагентаИБ.ИНН 			= ВыборкаДетальныхДанных.ИННКонтрагентаИБ;
				ЗначенияРеквизитовКонтрагентаИБ.КПП 			= ВыборкаДетальныхДанных.КППКонтрагентаИБ;

				СтрокаКонтракта.РеквизитыСвязанныхСправочников.Добавить(ЗначенияРеквизитовКонтрагентаИБ);
				
			КонецЕсли;
			
			ТекущийНомерСвязи = ТекущийНомерСвязи + 1;
			
		КонецЦикла;	
		
	КонецЦикла;
	
КонецПроцедуры	

Функция Контрагенты_ЗаполненныеСвязиВТаблицеКонтрактов_1С(ТаблицаКонтрактов)
	
	Результат = ТаблицаКонтрактов;
	
	Запрос = Новый Запрос;
	
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЗначенияСвойствОбъектов.Значение КАК ID,
	|	ЗначенияСвойствОбъектов.Объект КАК СвязанныйСправочник
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Свойство = &Свойство_BoxId
	|	И ЗначенияСвойствОбъектов.Значение В(&МассивBoxId)
	|ИТОГИ ПО
	|	ID";
	
	Запрос.УстановитьПараметр("Свойство_BoxId",	Свойства_СсылкаНаСвойство(Перечисление_ИменаСвойствНастроек_1С().IDКонтрагента, Ложь));
	Запрос.УстановитьПараметр("МассивBoxId",	ТаблицаКонтрактов.ВыгрузитьКолонку("ID"));
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	ТекстЗапроса	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Запрос.Текст = ТекстЗапроса;
	
	ВыборкаID = Запрос.Выполнить().Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	
	Пока ВыборкаID.Следующий() Цикл
		
		НайденнаяСтрока = Результат.Найти(ВыборкаID.ID, "ID");
		
		ВыборкаСвязей = ВыборкаID.Выбрать();
		
		Сч = 1;
		
		Пока ВыборкаСвязей.Следующий() Цикл
			
			НайденнаяСтрока["СвязанныйСправочник" + Сч] = ВыборкаСвязей.СвязанныйСправочник;
			
			Если Сч = 1 Тогда
				ID_ОсновноеПодразделение = ДопРеквизиты_Значение_1С(ВыборкаСвязей.СвязанныйСправочник, Перечисление_ИменаСвойствНастроек_1С().IDОсновноеПодразделениеКонтрагента, Ложь);
				НайденнаяСтрока.ID_ОсновноеПодразделение = ?(ID_ОсновноеПодразделение = Неопределено, "", ID_ОсновноеПодразделение);
			КонецЕсли;
			
			Сч = Сч + 1;
			
			Если Сч > 3 Тогда
				Прервать;
			КонецЕсли;	
			
		КонецЦикла;	
		
	КонецЦикла;	
	
	Возврат Результат;
	
КонецФункции	

Функция Контрагенты_РезультатГрупповогоСопоставления_1С(Организация, СтатусКонтрагентаЭДО)
	
	Результат = Новый Структура;
	Результат.Вставить("КоличествоСопоставленных",	0);
	Результат.Вставить("МассивОшибокДублей",		Новый Массив);
	Результат.Вставить("МассивОшибокСопоставления",	Новый Массив);
	
	Если СтатусКонтрагентаЭДО <> Перечисление_СтатусыКонтрагентов().РаботаетВДиадок Тогда	
		МассивCounteragents	= Контрагенты_МассивCounteragents(Организация, СтатусКонтрагентаЭДО);
		ТаблицаКонтрактов	= Контрагенты_ТаблицаКонтрактовПоCounteragents(МассивCounteragents);
	КонецЕсли;
	
	Если СтатусКонтрагентаЭДО = Неопределено
		Или СтатусКонтрагентаЭДО = Перечисление_СтатусыКонтрагентов().РаботаетВДиадок Тогда
		
		КонтрагентыРаботающиеВДиадок = Контрагенты_ПоискИПриглашениеДляОрганизации(Организация);
		
		Если ЗначениеЗаполнено(ТаблицаКонтрактов) Тогда
			Для Каждого ДанныеКонтрагента Из КонтрагентыРаботающиеВДиадок Цикл
				НоваяСтрока = ТаблицаКонтрактов.Добавить();
				ЗаполнитьЗначенияСвойств(НоваяСтрока, ДанныеКонтрагента);
			КонецЦикла;
		Иначе
			ТаблицаКонтрактов = КонтрагентыРаботающиеВДиадок;
		КонецЕсли;
		
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	
	Запрос.УстановитьПараметр("ТаблицаКонтрактов", ТаблицаКонтрактов);
	
	Запрос.Текст = Контрагенты_ТекстЗапросаКонтрагентовСервиса_1С(Запрос.Параметры) + ТекстЗапроса_РазделительЗапросов() +
		Контрагенты_ТекстСвязанныхСправочников();
		
	РезультатЗапросаСвязанныхКонтрагентов = Запрос.Выполнить();
	
	Запрос.Текст = Контрагенты_ТекстДублейЯщиков();
	ВыборкаДублейКонтрагентов = Запрос.Выполнить().Выбрать();
	
	РезультатСопоставления = Контрагенты_УстановленныеСвязи(РезультатЗапросаСвязанныхКонтрагентов);
	
	ЗаполнитьЗначенияСвойств(Результат, РезультатСопоставления);
	
	Результат.МассивОшибокДублей = Контрагенты_ДополнитьОшибкиСопоставления(ВыборкаДублейКонтрагентов);
	
	Запрос.МенеджерВременныхТаблиц.Закрыть();
	
	Возврат Результат;
	
КонецФункции	

Функция Контрагенты_ТекстЗапросаКонтрагентовСервиса_1С(Параметры)
	
	Результат = 
	"ВЫБРАТЬ
	|	ТаблицаКонтрактов.ID,
	|	ТаблицаКонтрактов.ИНН,
	|	ТаблицаКонтрактов.КПП,
	|	ТаблицаКонтрактов.ИдентификаторУчастникаЭДО
	|ПОМЕСТИТЬ ВТ_ТаблицаКонтрактов
	|ИЗ
	|	&ТаблицаКонтрактов КАК ТаблицаКонтрактов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	НЕОПРЕДЕЛЕНО КАК Ссылка,
	|	ВТ_ТаблицаКонтрактов.ID КАК ID,
	|	ВТ_ТаблицаКонтрактов.ИНН КАК ИНН,
	|	ВТ_ТаблицаКонтрактов.КПП КАК КПП,
	|	ВТ_ТаблицаКонтрактов.ИдентификаторУчастникаЭДО КАК ИдентификаторУчастникаЭДО,
	|	ПОДСТРОКА(ВТ_ТаблицаКонтрактов.ИдентификаторУчастникаЭДО, 1, 3) КАК КодОператораЭДО,
	|	ВЫБОР
	|		КОГДА ПОДСТРОКА(ВТ_ТаблицаКонтрактов.ИдентификаторУчастникаЭДО, 1, 3) = ""2BM""
	|			ТОГДА 1
	|		ИНАЧЕ 0
	|	КОНЕЦ КАК КоличествоЯщиковДД,
	|	ВЫБОР
	|		КОГДА ПОДСТРОКА(ВТ_ТаблицаКонтрактов.ИдентификаторУчастникаЭДО, 1, 3) <> ""2BM""
	|			ТОГДА 1
	|		ИНАЧЕ 0
	|	КОНЕЦ КАК КоличествоЯщиковРоуминг
	|ПОМЕСТИТЬ ВТ_КонтрагентыСервиса
	|ИЗ
	|	ВТ_ТаблицаКонтрактов КАК ВТ_ТаблицаКонтрактов
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ИНН,
	|	КПП
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВТ_КонтрагентыСервиса.ID,
	|	ВТ_КонтрагентыСервиса.ИдентификаторУчастникаЭДО,
	|	ВТ_КонтрагентыСервиса.Ссылка КАК КонтрагентЭДО,
	|	ЕСТЬNULL(ЗначенияСвойствОбъектов.Объект, НЕОПРЕДЕЛЕНО) КАК Контрагент1С
	|ПОМЕСТИТЬ ВТ_ТаблицаСоСвязями
	|ИЗ
	|	ВТ_КонтрагентыСервиса КАК ВТ_КонтрагентыСервиса
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|		ПО ВТ_КонтрагентыСервиса.ID = ЗначенияСвойствОбъектов.Значение
	|			И (ЗначенияСвойствОбъектов.Свойство = &Свойство_BoxId)
	|ГДЕ
	|	НЕ ЗначенияСвойствОбъектов.Значение ЕСТЬ NULL";
	
	ИмяРегистра	= Свойства_ИмяРегистраСведений();
	Результат	= СтрЗаменить(Результат, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);

	Параметры.Вставить("Свойство_BoxId", Свойства_СсылкаНаСвойство(Перечисление_ИменаСвойствНастроек_1С().IDКонтрагента, Ложь));
		
	Возврат Результат;
	
КонецФункции	

//}		СПИСКИ КОНТРАГЕНТОВ


///////////////////////////////////////////////////////////////////////////////
//{		СИНХРОНИЗАЦИЯ КОНТРАГЕНТОВ

Функция Контрагенты_МассивКонтрагентовДляОбновленияРеквизитов(ТаблицаКонтрактов) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_МассивКонтрагентовДляОбновленияРеквизитов_Диадок(ТаблицаКонтрактов);
	КонецЕсли; 
	
	Возврат Результат;
	
КонецФункции	

Функция Контрагенты_МассивКонтрагентовДляОбновленияСтатусов(ТаблицаКонтрактов, Организация, Приглашения) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_МассивКонтрагентовДляОбновленияСтатусов_Диадок(ТаблицаКонтрактов, Организация, Приглашения);
	КонецЕсли; 
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_МассивКонтрагентовДляОбновленияДатыИзменения(ТаблицаКонтрактов, Организация) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_МассивКонтрагентовДляОбновленияДатыИзменения_Диадок(ТаблицаКонтрактов, Организация);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_МассивИННДляПриглашения(Организация) Экспорт
	
	ОбъектМд = МетаданныеСправочникаКонтрагенты();
	Если Не Общее_ЕстьРеквизитОбъекта("ИНН", ОбъектМд) Тогда
		Возврат Новый Массив;
	КонецЕсли;
	
	ТекстЗапроса =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	Контрагенты.ИНН КАК ИНН
		|ИЗ
		|	Справочник.Контрагенты КАК Контрагенты
		|ГДЕ
		|	НЕ Контрагенты.ПометкаУдаления
		|	И Контрагенты.ИНН <> """"";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "Справочник.Контрагенты", ОбъектМд.ПолноеИмя());
	
	Запрос = Новый Запрос(ТекстЗапроса);
	РезультатЗапроса = Запрос.Выполнить();
	ТаблицаЗапроса = РезультатЗапроса.Выгрузить();
	Результат = ТаблицаЗапроса.ВыгрузитьКолонку("ИНН");
	
	Возврат Результат;
	
КонецФункции 

// Получает список контрагентов информационной базы, которые не сопоставлены
// с контрагентами сервиса.
// 
// Возвращаемое значение:
// ТаблицаЗначений  - содержит таблицу значений несопоставленных контрагентов 
//               Колонки:
//						Ссылка 	- СправочникСсылка.Контрагенты
//						ИНН	- Строка - ИНН контрагент
//						КПП	- Строка - КПП контрагент
Функция Контрагенты_НеСопоставленныеКонтрагенты() Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = Контрагенты_НеСопоставленныеКонтрагенты_Диадок(); 
	Иначе
		Результат = Контрагенты_НеСопоставленныеКонтрагенты_1С();
	КонецЕсли; 
	
	Возврат Результат;	
	
КонецФункции

Процедура Контрагенты_ЗаписатьСтатус(Контрагент, Организация) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Контрагенты_ЗаписатьСтатус_Диадок(Контрагент, Организация);
	КонецЕсли;
	
КонецПроцедуры

Процедура Контрагенты_ЗаписатьДатуИзменения(Контрагент, Организация) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Контрагенты_ЗаписатьДатуИзменения_Диадок(Контрагент, Организация);
	КонецЕсли;
	
КонецПроцедуры

Процедура Контрагенты_ЗаписатьСтатусДляМассива(Контрагент, Организация, Статус) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Контрагенты_ЗаписатьСтатусДляМассива_Диадок(Контрагент, Организация, Статус);
	КонецЕсли;
	
КонецПроцедуры

// Записывает сопоставление контрагента 1С и контрагента ДД
//
// Параметры:
//  КонтрагентИБ	 - СправочникСсылка.Контрагенты	- ссылка справочника Контрагенты. 
//  ИдентификаторЭДО - Структура - см. описание Новый_ИдентификаторыСправочника()
//
Процедура Контрагенты_СопоставитьКонтрагентов(КонтрагентИБ, ИдентификаторЭДО) Экспорт
	
	СвязанныеСправочники = Контрагенты_МассивСопоставленныхВ1С(ИдентификаторЭДО);
	
	Если  НЕ СвязанныеСправочники.Найти(КонтрагентИБ) = Неопределено Тогда
		
		Возврат;
		
	КонецЕсли;
	
	СвязанныеСправочники.Добавить(КонтрагентИБ);
	
	СтруктураРеквизитов = Новый Структура("СвязанныеСправочники", СвязанныеСправочники);	
		
	Контрагенты_ОбновлениеРеквизитов(ИдентификаторЭДО, СтруктураРеквизитов);

КонецПроцедуры

// Возвращает таблицу значений контрагентов 1С, сопоставленных с контрагентами ЭДО.
// Сопоставление контрагентов выполняется по значениям реквизитов ИНН и КПП.
//
// Параметры:
//	КонтрагентыЭДО - ТаблицаЗначений - состав колонок см. Модуль_Ядро.Контрагенты_НоваяТаблицаКонтрактов
//
// Возвращаемое значение:
//	ТаблицаЗначений - содержит таблицу связанных контрагентов 1С.
//					Колонки:
//						КонтрагентЭДО 	- Строка - идентификатор контрагента ЭДО
//						Контрагент1С	- СправочникСсылка.Контрагенты
//
Функция Контрагенты_СопоставитьКонтрагентовПоИННКПП(КонтрагентыЭДО) Экспорт
	
	КонтрагентыМД = МетаданныеСправочникаКонтрагенты();
	
	Если Не Общее_ЕстьРеквизитОбъекта("ИНН", КонтрагентыМД) Тогда
		
		ИмяТипа = СтрШаблонЛок("СправочникСсылка.%1", КонтрагентыМД.Имя);
		
		// без ИНН невозможно подобрать связи
		Результат = Новый ТаблицаЗначений;
		Результат.Колонки.Добавить("КонтрагентЭДО", Новый ОписаниеТипов("Строка"), "Идентификатор контрагента");
		Результат.Колонки.Добавить("Контрагент1С", Новый ОписаниеТипов(ИмяТипа), "Контрагент 1С");
		Возврат Результат;
		
	КонецЕсли;
	
	ТекстЗапроса =
		"ВЫБРАТЬ
		|	КонтрагентыЭДО.ID,
		|	КонтрагентыЭДО.ИНН,
		|	КонтрагентыЭДО.КПП
		|ПОМЕСТИТЬ ВремКонтрагентыЭДО
		|ИЗ
		|	&ВнешняяТаблица КАК КонтрагентыЭДО
		|
		|;
		|////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	КонтрагентыЭДО.ID 		КАК КонтрагентЭДО,
		|	Контрагенты1С.Ссылка 	КАК Контрагент1С
		|ИЗ
		|	&ПолноеИмяТаблицыКонтрагенты КАК Контрагенты1С
		|		ЛЕВОЕ СОЕДИНЕНИЕ ВремКонтрагентыЭДО КАК КонтрагентыЭДО
		|		ПО Контрагенты1С.ИНН = КонтрагентыЭДО.ИНН
		|			И &СвязьПоКПП
		|ГДЕ
		|	НЕ КонтрагентыЭДО.ID ЕСТЬ NULL
		|	И НЕ Контрагенты1С.ПометкаУдаления";
	
	СвязьПоКПП = "Контрагенты1С.КПП = КонтрагентыЭДО.КПП";
	Если Не Общее_ЕстьРеквизитОбъекта("КПП", КонтрагентыМД) Тогда
		СвязьПоКПП = "ИСТИНА";
	КонецЕсли;
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ПолноеИмяТаблицыКонтрагенты", КонтрагентыМД.ПолноеИмя());
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&СвязьПоКПП", СвязьПоКПП);
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("ВнешняяТаблица", КонтрагентыЭДО);
	
	СвязиКонтрагентов = Запрос.Выполнить().Выгрузить();
	
	Возврат СвязиКонтрагентов;
	
КонецФункции

//**********************************************
// СИНХРОНИЗАЦИЯ КОНТРАГЕНТОВ: ПОДСИСТЕМА ДИАДОКА

Функция Контрагенты_МассивКонтрагентовДляОбновленияРеквизитов_Диадок(ТаблицаКонтрактов)
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ТаблицаКонтрактов.ID,
	|	ТаблицаКонтрактов.OrgId,
	|	ТаблицаКонтрактов.ИНН,
	|	ТаблицаКонтрактов.КПП,
	|	ТаблицаКонтрактов.Наименование,
	|	ТаблицаКонтрактов.ИдентификаторУчастникаЭДО,
	|	ТаблицаКонтрактов.Тип КАК Тип,
	|	ТаблицаКонтрактов.Экстраверт КАК Экстраверт,
	|	ТаблицаКонтрактов.ДатаЛиквидации
	|ПОМЕСТИТЬ ВТ_ТаблицаКонтрактов
	|ИЗ
	|	&ТаблицаКонтрактов КАК ТаблицаКонтрактов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	Диадок_ЮрФизЛица.Ссылка КАК КонтрагентЭДО,
	|	Диадок_ЮрФизЛица.ИНН,
	|	Диадок_ЮрФизЛица.КПП,
	|	Диадок_ЮрФизЛица.ID,
	|	ЕСТЬNULL(Диадок_ЮрФизЛицаOrgId.Значение, """") КАК OrgId,
	|	Диадок_ЮрФизЛица.Наименование,
	|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО,
	|	ЕСТЬNULL(ДопРеквизитыТипОрганизации.Значение, """") КАК Тип,
	|	ЕСТЬNULL(ДопРеквизитыЭкстраверт.Значение, ЛОЖЬ) КАК Экстраверт,
	|	ЕСТЬNULL(Диадок_ЮрФизЛицаДатаЛиквидации.Значение, ДАТАВРЕМЯ(1, 1, 1)) КАК ДатаЛиквидации
	|ПОМЕСТИТЬ ВТ_КонтрагентыЭДО
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДатаЛиквидации
	|		ПО Диадок_ЮрФизЛица.Ссылка = Диадок_ЮрФизЛицаДатаЛиквидации.Ссылка
	|			И (Диадок_ЮрФизЛицаДатаЛиквидации.ИмяРеквизита = ""ДатаЛиквидации"")
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаOrgId
	|		ПО Диадок_ЮрФизЛица.Ссылка = Диадок_ЮрФизЛицаOrgId.Ссылка
	|			И (Диадок_ЮрФизЛицаOrgId.ИмяРеквизита = ""OrgId"")
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ДопРеквизитыТипОрганизации
	|		ПО Диадок_ЮрФизЛица.Ссылка = ДопРеквизитыТипОрганизации.Ссылка
	|			И (ДопРеквизитыТипОрганизации.ИмяРеквизита = ""Тип"")
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ДопРеквизитыЭкстраверт
	|		ПО Диадок_ЮрФизЛица.Ссылка = ДопРеквизитыЭкстраверт.Ссылка
	|			И (ДопРеквизитыЭкстраверт.ИмяРеквизита = ""Экстраверт"")
	|ГДЕ
	|	НЕ Диадок_ЮрФизЛица.ПометкаУдаления
	|	И Диадок_ЮрФизЛица.ИмяСправочника = ""Контрагенты""
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_ТаблицаКонтрактов.ID,
	|	ВТ_ТаблицаКонтрактов.OrgId,
	|	ВТ_ТаблицаКонтрактов.ИНН,
	|	ВТ_ТаблицаКонтрактов.КПП,
	|	ВТ_ТаблицаКонтрактов.Наименование КАК Наименование,
	|	ВТ_ТаблицаКонтрактов.ИдентификаторУчастникаЭДО,
	|	ВТ_ТаблицаКонтрактов.ДатаЛиквидации,
	|	ВТ_ТаблицаКонтрактов.Тип КАК Тип,
	|	ВТ_ТаблицаКонтрактов.Экстраверт КАК Экстраверт,
	|	ВТ_КонтрагентыЭДО.КонтрагентЭДО КАК Ссылка
	|ИЗ
	|	ВТ_ТаблицаКонтрактов КАК ВТ_ТаблицаКонтрактов
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КонтрагентыЭДО КАК ВТ_КонтрагентыЭДО
	|		ПО ВТ_ТаблицаКонтрактов.ID = ВТ_КонтрагентыЭДО.ID
	|ГДЕ
	|	ВЫБОР
	|			КОГДА ВТ_КонтрагентыЭДО.КонтрагентЭДО ЕСТЬ NULL 
	|				ТОГДА ИСТИНА
	|			ИНАЧЕ ВЫБОР
	|					КОГДА ВТ_ТаблицаКонтрактов.ИНН = ВТ_КонтрагентыЭДО.ИНН
	|							И ВТ_ТаблицаКонтрактов.КПП = ВТ_КонтрагентыЭДО.КПП
	|							И ВТ_ТаблицаКонтрактов.Наименование = ВТ_КонтрагентыЭДО.Наименование
	|							И ВТ_ТаблицаКонтрактов.ИдентификаторУчастникаЭДО = ВТ_КонтрагентыЭДО.ИдентификаторУчастникаЭДО
	|							И ВТ_ТаблицаКонтрактов.ДатаЛиквидации = ВТ_КонтрагентыЭДО.ДатаЛиквидации
	|							И ВТ_ТаблицаКонтрактов.OrgId = ВТ_КонтрагентыЭДО.OrgId
	|							И ВТ_ТаблицаКонтрактов.Тип = ВТ_КонтрагентыЭДО.Тип
	|							И ВТ_ТаблицаКонтрактов.Экстраверт = ВТ_КонтрагентыЭДО.Экстраверт
	|						ТОГДА ЛОЖЬ
	|					ИНАЧЕ ИСТИНА
	|				КОНЕЦ
	|		КОНЕЦ
	|
	|УПОРЯДОЧИТЬ ПО
	|	Наименование
	|АВТОУПОРЯДОЧИВАНИЕ";

	Запрос.УстановитьПараметр("ТаблицаКонтрактов", ТаблицаКонтрактов);	
	
	РезультатЗапроса	= Запрос.Выполнить();
	ВыборкаЗапроса		= РезультатЗапроса.Выбрать();
	
	Пока ВыборкаЗапроса.Следующий() Цикл
		
		Контрагент = Контракт_КонтрагентЭДО();
		
		ЗаполнитьЗначенияСвойств(Контрагент, ВыборкаЗапроса);
		
		Результат.Добавить(Контрагент);
		
	КонецЦикла;	

	Возврат Результат;
	
КонецФункции

Функция Контрагенты_МассивКонтрагентовДляОбновленияСтатусов_Диадок(ТаблицаКонтрактов, Организация, Приглашения)
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ТаблицаКонтрактов.ID,
	|	ТаблицаКонтрактов.Статус
	|ПОМЕСТИТЬ ВТ_ТаблицаКонтрактов
	|ИЗ
	|	&ТаблицаКонтрактов КАК ТаблицаКонтрактов
	|ГДЕ
	|	ВЫБОР
	|			КОГДА &ЭтоПриглашения
	|				ТОГДА ТаблицаКонтрактов.Статус = &Статус_РаботаетВДиадок
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_ТаблицаКонтрактов.Статус,
	|	Диадок_ЮрФизЛица.Ссылка КАК КонтрагентЭДО
	|ПОМЕСТИТЬ ВТ_ТаблицаКонтрактовСопоставленная
	|ИЗ
	|	ВТ_ТаблицаКонтрактов КАК ВТ_ТаблицаКонтрактов
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО (Диадок_ЮрФизЛица.ИмяСправочника = ""Контрагенты"")
	|			И ВТ_ТаблицаКонтрактов.ID = Диадок_ЮрФизЛица.ID
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Диадок_НастройкиКонтрагентов.Контрагент,
	|	ВЫРАЗИТЬ(Диадок_НастройкиКонтрагентов.Значение КАК СТРОКА(30)) КАК ТекущийСтатус
	|ПОМЕСТИТЬ ВТ_Статусы
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
	|ГДЕ
	|	Диадок_НастройкиКонтрагентов.Организация = &Организация
	|	И Диадок_НастройкиКонтрагентов.ИмяСвойства = ""Статус""
	|	И ВЫБОР
	|			КОГДА &ЭтоПриглашения
	|				ТОГДА (ВЫРАЗИТЬ(Диадок_НастройкиКонтрагентов.Значение КАК СТРОКА(30))) = &Статус_РаботаетВДиадок
	|			ИНАЧЕ (ВЫРАЗИТЬ(Диадок_НастройкиКонтрагентов.Значение КАК СТРОКА(30))) <> &Статус_РаботаетВДиадок
	|		КОНЕЦ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ЕСТЬNULL(ВТ_ТаблицаКонтрактовСопоставленная.КонтрагентЭДО, ВТ_Статусы.Контрагент) КАК Ссылка,
	|	ЕСТЬNULL(ВТ_ТаблицаКонтрактовСопоставленная.Статус, """") КАК Статус
	|ИЗ
	|	ВТ_ТаблицаКонтрактовСопоставленная КАК ВТ_ТаблицаКонтрактовСопоставленная
	|		ПОЛНОЕ СОЕДИНЕНИЕ ВТ_Статусы КАК ВТ_Статусы
	|		ПО ВТ_ТаблицаКонтрактовСопоставленная.КонтрагентЭДО = ВТ_Статусы.Контрагент
	|ГДЕ
	|	ВЫБОР
	|			КОГДА ВТ_Статусы.Контрагент ЕСТЬ NULL 
	|					ИЛИ ВТ_ТаблицаКонтрактовСопоставленная.КонтрагентЭДО ЕСТЬ NULL 
	|				ТОГДА ИСТИНА
	|			ИНАЧЕ ВЫБОР
	|					КОГДА ВТ_Статусы.ТекущийСтатус = ВТ_ТаблицаКонтрактовСопоставленная.Статус
	|						ТОГДА ЛОЖЬ
	|					ИНАЧЕ ИСТИНА
	|				КОНЕЦ
	|		КОНЕЦ
	|АВТОУПОРЯДОЧИВАНИЕ";

	Запрос.УстановитьПараметр("Организация",			Организация.Ссылка);
	Запрос.УстановитьПараметр("ТаблицаКонтрактов",		ТаблицаКонтрактов);
	Запрос.УстановитьПараметр("ЭтоПриглашения",			Приглашения);
	Запрос.УстановитьПараметр("Статус_РаботаетВДиадок",	Перечисление_СтатусыКонтрагентов().РаботаетВДиадок);
	
	РезультатЗапроса	= Запрос.Выполнить();
	ВыборкаЗапроса		= РезультатЗапроса.Выбрать();
	
	Пока ВыборкаЗапроса.Следующий() Цикл
		
		Контрагент = Контракт_КонтрагентЭДО();
		
		Контрагент.Ссылка	= ВыборкаЗапроса.Ссылка;
		Контрагент.Статус	= ВыборкаЗапроса.Статус;
		
		Результат.Добавить(Контрагент);
		
	КонецЦикла;	

	Возврат Результат;
	
КонецФункции

Функция Контрагенты_МассивКонтрагентовДляОбновленияДатыИзменения_Диадок(ТаблицаКонтрактов, Организация)
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ТаблицаКонтрактов.ID КАК ID,
		|	ТаблицаКонтрактов.ДатаИзменения КАК ДатаИзменения
		|ПОМЕСТИТЬ ВТ_ТаблицаКонтрактов
		|ИЗ
		|	&ТаблицаКонтрактов КАК ТаблицаКонтрактов
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТ_ТаблицаКонтрактов.ДатаИзменения КАК ДатаИзменения,
		|	Диадок_ЮрФизЛица.Ссылка КАК КонтрагентЭДО
		|ПОМЕСТИТЬ ВТ_ТаблицаКонтрактовСопоставленная
		|ИЗ
		|	ВТ_ТаблицаКонтрактов КАК ВТ_ТаблицаКонтрактов
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
		|		ПО (Диадок_ЮрФизЛица.ИмяСправочника = ""Контрагенты"")
		|			И ВТ_ТаблицаКонтрактов.ID = Диадок_ЮрФизЛица.ID
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	Диадок_НастройкиКонтрагентов.Контрагент КАК Контрагент,
		|	ВЫРАЗИТЬ(Диадок_НастройкиКонтрагентов.Значение КАК Дата) КАК ДатаИзменения
		|ПОМЕСТИТЬ ВТ_ДатыИзменения
		|ИЗ
		|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
		|ГДЕ
		|	Диадок_НастройкиКонтрагентов.Организация = &Организация
		|	И Диадок_НастройкиКонтрагентов.ИмяСвойства = ""ДатаИзмененияСтатуса""
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ЕСТЬNULL(ВТ_ТаблицаКонтрактовСопоставленная.КонтрагентЭДО, ВТ_ДатыИзменения.Контрагент) КАК Ссылка,
		|	ЕСТЬNULL(ВТ_ТаблицаКонтрактовСопоставленная.ДатаИзменения, ДАТАВРЕМЯ(1,1,1,0,0,0)) КАК ДатаИзменения
		|ИЗ
		|	ВТ_ТаблицаКонтрактовСопоставленная КАК ВТ_ТаблицаКонтрактовСопоставленная
		|		ПОЛНОЕ СОЕДИНЕНИЕ ВТ_ДатыИзменения КАК ВТ_ДатыИзменения
		|		ПО ВТ_ТаблицаКонтрактовСопоставленная.КонтрагентЭДО = ВТ_ДатыИзменения.Контрагент
		|ГДЕ
		|	ВЫБОР
		|			КОГДА ВТ_ДатыИзменения.Контрагент ЕСТЬ NULL 
		|					ИЛИ ВТ_ТаблицаКонтрактовСопоставленная.КонтрагентЭДО ЕСТЬ NULL 
		|				ТОГДА ИСТИНА
		|			ИНАЧЕ ВЫБОР
		|					КОГДА ВТ_ДатыИзменения.ДатаИзменения = ВТ_ТаблицаКонтрактовСопоставленная.ДатаИзменения
		|						ТОГДА ЛОЖЬ
		|					ИНАЧЕ ИСТИНА
		|				КОНЕЦ
		|		КОНЕЦ
		|АВТОУПОРЯДОЧИВАНИЕ";
	
	Запрос.УстановитьПараметр("Организация", Организация.Ссылка);
	Запрос.УстановитьПараметр("ТаблицаКонтрактов", ТаблицаКонтрактов);
	
	РезультатЗапроса = Запрос.Выполнить();
	ВыборкаЗапроса = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаЗапроса.Следующий() Цикл
		
		Контрагент = Контракт_КонтрагентЭДО();
		
		Контрагент.Ссылка = ВыборкаЗапроса.Ссылка;
		Контрагент.ДатаИзменения = ВыборкаЗапроса.ДатаИзменения;
		
		Результат.Добавить(Контрагент);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура Контрагенты_ЗаписатьСтатус_Диадок(Контрагент, Организация)
	
	КонтрагентЭДО		= Контрагент.Ссылка;
	ОрганизацияЭДО		= Организация.Ссылка;
	
	СтруктураДанных = Новый Структура;
	СтруктураДанных.Вставить("ОрганизацияЭДО",	ОрганизацияЭДО);
	СтруктураДанных.Вставить("КонтрагентЭДО",	КонтрагентЭДО);
	СтруктураДанных.Вставить("ИмяСвойства",		"Статус");
	СтруктураДанных.Вставить("Значение",		Контрагент.Статус);
	
	Справочники_ЗаписатьЗначениеНастроекКонтрагентов_Диадок(СтруктураДанных);	
	
КонецПроцедуры

Процедура Контрагенты_ЗаписатьДатуИзменения_Диадок(Контрагент, Организация)
	
	КонтрагентЭДО = Контрагент.Ссылка;
	ОрганизацияЭДО = Организация.Ссылка;
	
	СтруктураДанных = Новый Структура;
	СтруктураДанных.Вставить("ОрганизацияЭДО", ОрганизацияЭДО);
	СтруктураДанных.Вставить("КонтрагентЭДО", КонтрагентЭДО);
	СтруктураДанных.Вставить("ИмяСвойства", "ДатаИзмененияСтатуса");
	СтруктураДанных.Вставить("Значение", Контрагент.ДатаИзменения);
	
	Справочники_ЗаписатьЗначениеНастроекКонтрагентов_Диадок(СтруктураДанных);
	
КонецПроцедуры

Процедура Контрагенты_ЗаписатьСтатусДляМассива_Диадок(МассивКонтрагентов, Организация, Статус)
	
	Для Каждого Контрагент Из МассивКонтрагентов Цикл
		
		Контрагент.Статус = Статус;
		Контрагенты_ЗаписатьСтатус_Диадок(Контрагент, Организация);
		
	КонецЦикла;	
	
КонецПроцедуры

Функция Контрагенты_НеСопоставленныеКонтрагенты_Диадок()
	
	Запрос = Новый Запрос;
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		ТекстЗапроса = Контрагенты_ТекстЗапроса_НесопоставленныеКонтрагенты_ДиадокРасширение();
	Иначе
		ТекстЗапроса = Контрагенты_ТекстЗапроса_НесопоставленныеКонтрагенты_ДиадокСХ1();
	КонецЕсли;
	
	ТекстЗапроса_УстановитьИмяТаблицыКонтрагентов(ТекстЗапроса);
	
	МетаданныеСправочника = МетаданныеСправочникаКонтрагенты();
	
	Если НЕ Общее_ЕстьРеквизитОбъекта("ИНН", МетаданныеСправочника) Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "КонтрагентыИБ.ИНН", """""");
	КонецЕсли;
	
	Если НЕ Общее_ЕстьРеквизитОбъекта("КПП", МетаданныеСправочника) Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "КонтрагентыИБ.КПП", """""");
	КонецЕсли;
	
	Если Общее_ЕстьСтандартныйРеквизитОбъекта("ЭтоГруппа", МетаданныеСправочника) Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&УсловиеПоЭтоГруппа", "НЕ КонтрагентыИБ.ЭтоГруппа");
	Иначе
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&УсловиеПоЭтоГруппа", "Истина");
	КонецЕсли;
	
	Запрос.Текст = ТекстЗапроса;
	
	Запрос.УстановитьПараметр("ИмяСправочника", "Контрагенты");
	Запрос.УстановитьПараметр("ИмяРеквизита", ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник());
	
	РезультатЗапроса = Запрос.Выполнить();
	Результат = РезультатЗапроса.Выгрузить();
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапроса_НесопоставленныеКонтрагенты_ДиадокРасширение()
	
	Результат = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	КонтрагентыИБ.Ссылка КАК Ссылка,
	|	КонтрагентыИБ.ИНН КАК ИНН,
	|	КонтрагентыИБ.КПП КАК КПП
	|ИЗ
	|	Справочник.Контрагенты КАК КонтрагентыИБ
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|			ПО Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка
	|				И (Диадок_ЮрФизЛица.ИмяСправочника = &ИмяСправочника)
	|		ПО (Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ИмяРеквизита)
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение = КонтрагентыИБ.Ссылка)
	|ГДЕ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка ЕСТЬ NULL
	|	И НЕ КонтрагентыИБ.ПометкаУдаления
	|	И &УсловиеПоЭтоГруппа";
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ТекстЗапроса_НесопоставленныеКонтрагенты_ДиадокСХ1()
	
	Результат =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЮрФизЛица.СвязанныйСправочник1 КАК Контрагент1С,
	|	ЮрФизЛица.Ссылка КАК КонтрагентЭДО
	|ПОМЕСТИТЬ ВТ_ТаблицаСоСвязями
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК ЮрФизЛица
	|ГДЕ
	|	НЕ ЮрФизЛица.СвязанныйСправочник1 = НЕОПРЕДЕЛЕНО
	|	И НЕ ЮрФизЛица.СвязанныйСправочник1 = ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка)
	|	И ЮрФизЛица.ИмяСправочника = &ИмяСправочника
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ЮрФизЛица.СвязанныйСправочник2,
	|	ЮрФизЛица.Ссылка
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК ЮрФизЛица
	|ГДЕ
	|	НЕ ЮрФизЛица.СвязанныйСправочник2 = НЕОПРЕДЕЛЕНО
	|	И НЕ ЮрФизЛица.СвязанныйСправочник2 = ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка)
	|	И ЮрФизЛица.ИмяСправочника = &ИмяСправочника
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ЮрФизЛица.СвязанныйСправочник3,
	|	ЮрФизЛица.Ссылка
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК ЮрФизЛица
	|ГДЕ
	|	НЕ ЮрФизЛица.СвязанныйСправочник3 = НЕОПРЕДЕЛЕНО
	|	И НЕ ЮрФизЛица.СвязанныйСправочник3 = ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка)
	|	И ЮрФизЛица.ИмяСправочника = &ИмяСправочника
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ЮрФизЛицаДопРеквизиты.Значение,
	|	ЮрФизЛица.Ссылка
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК ЮрФизЛица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК ЮрФизЛицаДопРеквизиты
	|		ПО ЮрФизЛица.Ссылка = ЮрФизЛицаДопРеквизиты.Ссылка
	|ГДЕ
	|	ЮрФизЛицаДопРеквизиты.ИмяРеквизита ПОДОБНО &ИмяРеквизита
	|	И НЕ ЮрФизЛицаДопРеквизиты.Значение = НЕОПРЕДЕЛЕНО
	|	И НЕ ЮрФизЛицаДопРеквизиты.Значение = ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка)
	|	И ЮрФизЛица.ИмяСправочника = &ИмяСправочника
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	КонтрагентыИБ.Ссылка КАК Ссылка,
	|	КонтрагентыИБ.ИНН КАК ИНН,
	|	КонтрагентыИБ.КПП КАК КПП
	|ИЗ
	|	Справочник.Контрагенты КАК КонтрагентыИБ
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ТаблицаСоСвязями КАК ВТ_ТаблицаСоСвязями
	|		ПО КонтрагентыИБ.Ссылка = ВТ_ТаблицаСоСвязями.Контрагент1С
	|ГДЕ
	|	НЕ КонтрагентыИБ.ПометкаУдаления
	|	И ВТ_ТаблицаСоСвязями.КонтрагентЭДО ЕСТЬ NULL
	|	И &УсловиеПоЭтоГруппа";
	
	Возврат Результат;
	
КонецФункции


//**********************************************
// СИНХРОНИЗАЦИЯ КОНТРАГЕНТОВ: 1С-ЭДО

Функция Контрагенты_НеСопоставленныеКонтрагенты_1С()
	
	Запрос = Новый Запрос;
	ТекстЗапроса =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	КонтрагентыИБ.ИНН КАК ИНН,
		|	КонтрагентыИБ.КПП КАК КПП,
		|	КонтрагентыИБ.Ссылка КАК Ссылка
		|ИЗ
		|	Справочник.Контрагенты КАК КонтрагентыИБ
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ДополнительныеСведения
		|		ПО КонтрагентыИБ.Ссылка = ДополнительныеСведения.Объект
		|			И (ДополнительныеСведения.Свойство = &Свойство_BoxId)
		|ГДЕ
		|	НЕ КонтрагентыИБ.ПометкаУдаления
		|   И &УсловиеПоЭтоГруппа
		|	И ДополнительныеСведения.Объект ЕСТЬ NULL";
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(ТекстЗапроса);
	
	МетаданныеСправочника = МетаданныеСправочникаКонтрагенты();
	
	Если НЕ Общее_ЕстьРеквизитОбъекта("КПП", МетаданныеСправочника) Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "КонтрагентыИБ.КПП", """""");
	КонецЕсли;
	
	Если Общее_ЕстьСтандартныйРеквизитОбъекта("ЭтоГруппа", МетаданныеСправочника) Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&УсловиеПоЭтоГруппа", "НЕ КонтрагентыИБ.ЭтоГруппа");
	Иначе
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&УсловиеПоЭтоГруппа", "Истина");
	КонецЕсли;
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Параметры.Вставить("Свойство_BoxId", Свойства_IDКонтрагента());
	
	РезультатЗапроса = Запрос.Выполнить();
	Результат = РезультатЗапроса.Выгрузить();
	
	Возврат Результат;
	
КонецФункции

//}		СИНХРОНИЗАЦИЯ КОНТРАГЕНТОВ


///////////////////////////////////////////////////////////////////////////////
//{		ДОП. СПРАВОЧНИКИ И РЕКВИЗИТЫ

Функция ДопРеквизиты_СписокРеквизитов(Ссылка, СписокДопРеквизитов = "") Экспорт 
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = ДопРеквизиты_СписокРеквизитов_Диадок(Ссылка, СписокДопРеквизитов);
	Иначе
		Результат = ДопРеквизиты_СписокРеквизитов_1С(Ссылка, СписокДопРеквизитов);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДопРеквизиты_Значение(Ссылка, ИмяРеквизита) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = ДопРеквизиты_Значение_Диадок(Ссылка, ИмяРеквизита);
	Иначе
		Результат = ДопРеквизиты_Значение_1С(Ссылка, ИмяРеквизита);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ДопРеквизиты_УстановитьЗначение(Элемент, ИмяРеквизита, Значение, ЗначениеСтрока = "") Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда		
		ДопРеквизиты_УстановитьЗначение_Диадок(Элемент, ИмяРеквизита, Значение, ЗначениеСтрока);
	Иначе
		ДопРеквизиты_УстановитьЗначение_1С(Элемент, ИмяРеквизита, Значение);
	КонецЕсли;

КонецПроцедуры

Функция ДопРеквизиты_ЗначениеШифрованияДокумента(ИдентификаторыОрганизации, ИдентификаторыКонтрагента) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = ДопРеквизиты_ЗначениеШифрованияДокумента_Диадок(ИдентификаторыОрганизации, ИдентификаторыКонтрагента);
	Иначе
		
		Результат = ДопРеквизиты_ЗначениеШифрованияДокумента_1С(ИдентификаторыКонтрагента, Ложь);
		
		Если Результат = Неопределено Тогда
			Результат = ДопРеквизиты_ЗначениеШифрованияДокумента_1С(ИдентификаторыОрганизации);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции	

Процедура ДопРеквизиты_ЗаписатьШифрованиеДокумента(ИдентификаторыОрганизации, ИдентификаторыКонтрагента, Значение) Экспорт

	Если ИспользоватьПодсистемуДиадока() Тогда
		
		ОрганизацияЭДО	= СвойствоСтруктуры(ИдентификаторыОрганизации, "Ссылка");
		КонтрагентЭДО	= СвойствоСтруктуры(ИдентификаторыКонтрагента, "Ссылка");
		
		ДопРеквизиты_ЗаписатьШифрованиеДокумента_Диадок(ОрганизацияЭДО, КонтрагентЭДО, Значение);
		
	Иначе
		
		КонтрагентВ1С	= Контрагенты_СопоставленныйВ1С_1С(ИдентификаторыКонтрагента);
		ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ШифроватьДокументы;
		
		ДопРеквизиты_УстановитьЗначение_1С(КонтрагентВ1С, ИмяРеквизита, Значение);
		
	КонецЕсли;

КонецПроцедуры

Функция ДопСправочники_СписокЭлементов(ИмяСправочника, ПереченьРеквизитов = Неопределено) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = ДопСправочники_СписокЭлементов_Диадок(ИмяСправочника, ПереченьРеквизитов);
	Иначе
		Результат = ДопСправочники_СписокЭлементов_1С(ИмяСправочника, ПереченьРеквизитов);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДопСправочники_СсылкаПоИдентификатору(ИмяСправочника, Идентификатор) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = ДопСправочники_СсылкаПоИдентификатору_Диадок(ИмяСправочника, Идентификатор);
	Иначе
		Результат = ДопСправочники_СсылкаПоИдентификатору_1С(ИмяСправочника, Идентификатор);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает коллекцию ссылок на элементы доп. справочника в разрезе идентификаторов этих элементов (для подсистемы Диадок).
// Для системы хранения на типовых объектах возвращаются значения самих идентификаторов.
//
// Параметры:
//  ИмяСправочника	 - Строка	 - имя доп. справочника, как оно задано в Справочники.Диадок_ДополнительныеСправочники
//  Идентификаторы	 - Массив	 - значения идентификаторов элементов справочника, которые указаны в таб. части доп. реквизитов в строке "ID".
// 
// Возвращаемое значение:
//  Соответствие - ключ - идентификатор элемента, значение - ссылка на элемент справочника.
//
Функция ДопСправочники_СсылкиНаЭлементыСправочникаПоИдентификаторам(ИмяСправочника, Идентификаторы) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = ДопСправочники_СсылкиНаЭлементыСправочникаПоИдентификаторам_Диадок(ИмяСправочника, Идентификаторы);
	Иначе
		Результат = ДопСправочники_СсылкиНаЭлементыСправочникаПоИдентификаторам_1С(ИмяСправочника, Идентификаторы);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДопСправочники_СсылкаПоНаименованию(ИмяСправочника, Наименование) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = ДопСправочники_НайтиПоНаименованию_Диадок(ИмяСправочника, Наименование);
	Иначе
		Результат = Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДопСправочники_СоздатьЭлемент(ИмяСправочника, Наименование) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = ДопСправочники_СоздатьЭлемент_Диадок(ИмяСправочника, Наименование);
	Иначе
		Результат = Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДопСправочники_ПустаяСсылка(ИмяСправочника) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = ДопСправочники_ПустаяСсылка_Диадок(ИмяСправочника);
	Иначе
		Результат = ДопСправочники_ПустаяСсылка_1С(ИмяСправочника);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ДопСправочники_ЗаписатьТипыДокументовАПИ(ТипыДокументовAPI) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		ДопСправочники_ЗаписатьТипыДокументовАПИ_Диадок(ТипыДокументовAPI);
	Иначе
		ДопСправочники_ЗаписатьТипыДокументовАПИ_1С(ТипыДокументовAPI);
	КонецЕсли;
	
КонецПроцедуры

//**********************************************
// ДОП. СПРАВОЧНИКИ: ПОДСИСТЕМА ДИАДОКА

Функция ДопРеквизиты_СписокРеквизитов_Диадок(Ссылка, СписокДопРеквизитов)
	
	Результат		= Новый Структура(СписокДопРеквизитов);
	ИмяМетаданных	= Ссылка.Метаданные().Имя;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	ДополнительныеРеквизиты.ИмяРеквизита КАК ИмяРеквизита,
	|	ДополнительныеРеквизиты.Значение КАК Значение,
	|	ДополнительныеРеквизиты.ЗначениеСтрока КАК ЗначениеСтрока,
	|	ДополнительныеРеквизиты.ЗначениеХранилищеЗначения КАК ЗначениеХранилищеЗначения
	|ИЗ
	|	Справочник." + ИмяМетаданных + ".ДополнительныеРеквизиты КАК ДополнительныеРеквизиты
	|ГДЕ
	|	ДополнительныеРеквизиты.Ссылка = &Ссылка");
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		ИмяРеквизита		= Выборка.ИмяРеквизита;
		ЗначениеРеквизита	= Справочники_ЗначениеДопРеквизита_Диадок(Выборка);
		
		Если СписокДопРеквизитов = ""
			ИЛИ Результат.Свойство(ИмяРеквизита) Тогда
			
			Результат.Вставить(ИмяРеквизита, ЗначениеРеквизита);
			
		КонецЕсли;	
		
	КонецЦикла;	
	
	Возврат Результат;
		
КонецФункции

Функция ДопРеквизиты_Значение_Диадок(Знач Ссылка, ИмяРеквизита) Экспорт
	
	Результат = Неопределено;
	
	Если Ссылка = Неопределено Тогда
		Ссылка = ДопСправочники_НайтиПоНаименованию_Диадок("ОбщиеНастройки", "ОбщиеНастройки");
	КонецЕсли;
	
	ВыборкаЗначений = Ссылка.ДополнительныеРеквизиты.Найти(ИмяРеквизита);
	
	Если ВыборкаЗначений <> Неопределено Тогда
		Результат = Справочники_ЗначениеДопРеквизита_Диадок(ВыборкаЗначений);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДопРеквизиты_ЗначениеОсновногоПакета_Диадок(ОрганизацияЭДО)
	
	СтруктураДанных = Новый Структура;
	СтруктураДанных.Вставить("ОрганизацияЭДО",	ОрганизацияЭДО);
	СтруктураДанных.Вставить("КонтрагентЭДО",	Контрагенты_ПустаяСсылка_Диадок());
	СтруктураДанных.Вставить("ИмяСвойства",		Перечисление_ИменаСвойствНастроек_Диадок().ОсновнойПакет);
	
	Результат = Справочники_ЗначениеЗаписиНастроекКонтрагентов_Диадок(СтруктураДанных);
	
	Возврат Результат;
	
КонецФункции

Функция ДопРеквизиты_ЗначениеШифрованияДокумента_Диадок(ИдентификаторыОрганизации, ИдентификаторыКонтрагента)
	
	ОрганизацияЭДО	= СвойствоСтруктуры(ИдентификаторыОрганизации, "Ссылка");
	КонтрагентЭДО	= СвойствоСтруктуры(ИдентификаторыКонтрагента, "Ссылка");
	
	Если КонтрагентЭДО = Неопределено Тогда
		КонтрагентЭДО = Контрагенты_ПустаяСсылка_Диадок();
	КонецЕсли;
	
	СтруктураДанных = Новый Структура;
	СтруктураДанных.Вставить("ОрганизацияЭДО",	ОрганизацияЭДО);
	СтруктураДанных.Вставить("КонтрагентЭДО",	КонтрагентЭДО);
	СтруктураДанных.Вставить("ИмяСвойства",		Перечисление_ИменаСвойствНастроек_Диадок().ШифроватьДокументы);
	
	Результат = Справочники_ЗначениеЗаписиНастроекКонтрагентов_Диадок(СтруктураДанных);
	
	Возврат Результат;
	
КонецФункции

Процедура ДопРеквизиты_УстановитьЗначение_Диадок(Знач Элемент, ИмяРеквизита, Значение, ЗначениеСтрока) Экспорт
	
	Если Элемент = Неопределено Тогда
		Элемент = ДопСправочники_НайтиПоНаименованию_Диадок("ОбщиеНастройки", "ОбщиеНастройки");
	КонецЕсли;
	
	ЭтоОбъект	= ЭтоОбъект(Элемент);
	Объект		= ?(ЭтоОбъект, Элемент, Элемент.ПолучитьОбъект());
	
	СтрокаРеквизита = Объект.ДополнительныеРеквизиты.Найти(ИмяРеквизита, "ИмяРеквизита");
	
	Если СтрокаРеквизита = Неопределено Тогда
		
		ЗначениеДляУстановкиНеЗаполнено = (НЕ ЗначениеЗаполнено(Значение) 
											ИЛИ Значение = Ложь 
											ИЛИ Значение = ПустойИдентификатор())
										И (НЕ ЗначениеЗаполнено(ЗначениеСтрока) 
											ИЛИ ЗначениеСтрока = ПустойИдентификатор());
		
		Если ЗначениеДляУстановкиНеЗаполнено Тогда
			Возврат;
		КонецЕсли;
		
		СтрокаРеквизита					= Объект.ДополнительныеРеквизиты.Добавить();
		СтрокаРеквизита.ИмяРеквизита	= ИмяРеквизита;
		
	КонецЕсли;
	
	Если ТипЗнч(Значение) = Тип("ХранилищеЗначения") Тогда
		СтрокаРеквизита.ЗначениеХранилищеЗначения = Значение;
	ИначеЕсли ТипЗнч(Значение) = Тип("Структура") Тогда
		СтрокаРеквизита.ЗначениеХранилищеЗначения = Новый ХранилищеЗначения(Значение);
	Иначе	
		СтрокаРеквизита.Значение = Значение;
	КонецЕсли;
	
	СтрокаРеквизита.ЗначениеСтрока = ЗначениеСтрока;
	
	Если Не ЭтоОбъект Тогда
		Объект.Записать();
	КонецЕсли;

КонецПроцедуры

Процедура ДопРеквизиты_ЗаписатьОсновнойПакет_Диадок(ОрганизацияЭДО, Значение)
	
	СтруктураДанных = Новый Структура;
	СтруктураДанных.Вставить("ОрганизацияЭДО",	ОрганизацияЭДО);
	СтруктураДанных.Вставить("КонтрагентЭДО",	Контрагенты_ПустаяСсылка_Диадок());
	СтруктураДанных.Вставить("ИмяСвойства",		Перечисление_ИменаСвойствНастроек_Диадок().ОсновнойПакет);
	СтруктураДанных.Вставить("Значение",		Значение);
	
	Если ЗначениеЗаполнено(Значение) Тогда
		Справочники_ЗаписатьЗначениеНастроекКонтрагентов_Диадок(СтруктураДанных);
	Иначе
		Справочники_УдалитьЗначениеНастроекКонтрагентов_Диадок(СтруктураДанных);
	КонецЕсли;	
	
КонецПроцедуры

Процедура ДопРеквизиты_ЗаписатьШифрованиеДокумента_Диадок(ОрганизацияЭДО, КонтрагентЭДО, Значение)
	
	Если КонтрагентЭДО = Неопределено Тогда
		КонтрагентЭДО = Контрагенты_ПустаяСсылка_Диадок();
	КонецЕсли;
	
	СтруктураДанных = Новый Структура;
	СтруктураДанных.Вставить("ОрганизацияЭДО",	ОрганизацияЭДО);
	СтруктураДанных.Вставить("КонтрагентЭДО",	КонтрагентЭДО);
	СтруктураДанных.Вставить("ИмяСвойства",		Перечисление_ИменаСвойствНастроек_Диадок().ШифроватьДокументы);
	СтруктураДанных.Вставить("Значение",		Значение);
	
	Справочники_ЗаписатьЗначениеНастроекКонтрагентов_Диадок(СтруктураДанных);
	
КонецПроцедуры

Процедура ДопРеквизиты_ОчиститьСвязанныеСправочники_Диадок(ЮрФизЛицоОбъект)
	
	ДополнительныеРеквизиты = ЮрФизЛицоОбъект.ДополнительныеРеквизиты;
	
	УдаляемыеСтрокиТЧ = Новый Массив;
	
	Для каждого СтрокаТЧ Из ДополнительныеРеквизиты Цикл
		
		ИмяРеквизита = СтрокаТЧ.ИмяРеквизита;
		
		Если Найти(ИмяРеквизита, Перечисление_ИменаСвойствНастроек_Диадок().СвязанныйСправочник) > 0 Тогда
			УдаляемыеСтрокиТЧ.Добавить(СтрокаТЧ);
		КонецЕсли;
		
	КонецЦикла;
	
	Для Каждого УдаляемаяСтрока Из УдаляемыеСтрокиТЧ Цикл
		ДополнительныеРеквизиты.Удалить(УдаляемаяСтрока);
	КонецЦикла;
	
КонецПроцедуры

Функция ДопСправочники_НайтиПоНаименованию_Диадок(ИмяСправочника, Наименование)
	
	Результат = Неопределено;
	
	Если ПроверитьНаличиеОбъектаМетаданных("Справочники.Диадок_ДополнительныеСправочники") Тогда
		
		Запрос = Новый Запрос(
		"ВЫБРАТЬ
		|	Диадок_ДополнительныеСправочники.Ссылка
		|ИЗ
		|	Справочник.Диадок_ДополнительныеСправочники КАК Диадок_ДополнительныеСправочники
		|ГДЕ
		|	Диадок_ДополнительныеСправочники.ИмяСправочника = &ИмяСправочника
		|	И Диадок_ДополнительныеСправочники.Наименование = &Наименование"
		);
		
		Запрос.УстановитьПараметр("ИмяСправочника",	ИмяСправочника);
		Запрос.УстановитьПараметр("Наименование",	Наименование);
		
		ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
		
		Если ВыборкаЗапроса.Следующий() Тогда
			Результат = ВыборкаЗапроса.Ссылка;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция ДопСправочники_СписокЭлементов_Диадок(ИмяСправочника, ПереченьРеквизитов) Экспорт
	
	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("Ссылка");
	Результат.Колонки.Добавить("Наименование");
	
	Если ПереченьРеквизитов = Неопределено
		И (ИмяСправочника = "ВидыПакетов" ИЛИ ИмяСправочника = "ВидыДокументов") Тогда
		
		ПереченьРеквизитов = "ID";

	КонецЕсли;
	
	СписокДопРеквизитов = РазделитьСтрокуЛок(ПереченьРеквизитов);
	
	Для Каждого ДопРеквизит Из СписокДопРеквизитов Цикл
		Результат.Колонки.Добавить(ДопРеквизит);
	КонецЦикла;
	
	Если ПроверитьНаличиеОбъектаМетаданных("Справочники.Диадок_ДополнительныеСправочники") Тогда
		
		Запрос = Новый Запрос( 
		"ВЫБРАТЬ
		|	Диадок_ДополнительныеСправочники.Ссылка КАК Ссылка,
		|	Диадок_ДополнительныеСправочники.Наименование КАК Наименование,
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.ИмяРеквизита КАК ИмяРеквизита,
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Значение,
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.ЗначениеСтрока,
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.ЗначениеХранилищеЗначения
		|ИЗ
		|	Справочник.Диадок_ДополнительныеСправочники КАК Диадок_ДополнительныеСправочники
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ДополнительныеСправочники.ДополнительныеРеквизиты КАК Диадок_ДополнительныеСправочникиДополнительныеРеквизиты
		|		ПО Диадок_ДополнительныеСправочники.Ссылка = Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка
		|			И (Диадок_ДополнительныеСправочники.ИмяСправочника = &ИмяСправочника)
		|
		|УПОРЯДОЧИТЬ ПО
		|	Диадок_ДополнительныеСправочники.Наименование"
		);
		
		Запрос.УстановитьПараметр("ИмяСправочника", ИмяСправочника);			   
		
		СтарЭлемент = Неопределено;
		
		Выборка = Запрос.Выполнить().Выбрать();
		
		Пока Выборка.Следующий() Цикл
			
			Если Выборка.Ссылка <> СтарЭлемент Тогда
				
				НоваяСтрока = Результат.Добавить();
				НоваяСтрока.Ссылка			= Выборка.Ссылка;
				НоваяСтрока.Наименование	= Выборка.Наименование;
				
				СтарЭлемент = Выборка.Ссылка;	
				
			КонецЕсли;
			
			Если СписокДопРеквизитов.Найти(Выборка.ИмяРеквизита) <> Неопределено Тогда
				НоваяСтрока[Выборка.ИмяРеквизита] = Справочники_ЗначениеДопРеквизита_Диадок(Выборка);
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Если ИмяСправочника = "ВидыГруппировкиПолученных"
		И ПереченьРеквизитов = "ТипыДокументов" Тогда
		ДопСправочники_ЗаполнитьВидыГруппировкиПолученных(Результат);
	КонецЕсли;	
	
	Возврат Результат;
	
КонецФункции

Функция ДопСправочники_СсылкаПоИдентификатору_Диадок(ИмяСправочника, Идентификатор) Экспорт
	
	Результат = Неопределено;
	
	Если ПроверитьНаличиеОбъектаМетаданных("Справочники.Диадок_ДополнительныеСправочники") Тогда
		
		Запрос = Новый Запрос(
		"ВЫБРАТЬ
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка
		|ИЗ
		|	Справочник.Диадок_ДополнительныеСправочники.ДополнительныеРеквизиты КАК Диадок_ДополнительныеСправочникиДополнительныеРеквизиты
		|ГДЕ
		|	Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Ссылка.ИмяСправочника = &ИмяСправочника
		|	И Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.ИмяРеквизита = ""ID""
		|	И Диадок_ДополнительныеСправочникиДополнительныеРеквизиты.Значение = &Идентификатор"
		);
		
		Запрос.УстановитьПараметр("ИмяСправочника",	ИмяСправочника);
		Запрос.УстановитьПараметр("Идентификатор",	Идентификатор);
		
		ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
		
		Если ВыборкаЗапроса.Следующий() Тогда
			Результат = ВыборкаЗапроса.Ссылка;
		КонецЕсли;	 
		
	КонецЕсли;

	Возврат Результат;

КонецФункции

// см. ДопСправочники_СсылкиНаЭлементыСправочникаПоИдентификаторам
//
Функция ДопСправочники_СсылкиНаЭлементыСправочникаПоИдентификаторам_Диадок(ИмяСправочника, Идентификаторы)
	
	Результат = Новый Соответствие;
	
	Если ПроверитьНаличиеОбъектаМетаданных("Справочники.Диадок_ДополнительныеСправочники") Тогда
		
		Запрос = Новый Запрос(
		"ВЫБРАТЬ
		|	Реквизиты.Ссылка,
		|	Реквизиты.Значение
		|ИЗ
		|	Справочник.Диадок_ДополнительныеСправочники.ДополнительныеРеквизиты КАК Реквизиты
		|ГДЕ
		|	Реквизиты.Ссылка.ИмяСправочника = &ИмяСправочника
		|	И Реквизиты.ИмяРеквизита = ""ID""
		|	И Реквизиты.Значение В (&Идентификаторы)"
		);
		
		Запрос.УстановитьПараметр("ИмяСправочника",	ИмяСправочника);
		Запрос.УстановитьПараметр("Идентификаторы",	Идентификаторы);
		
		ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
		
		Пока ВыборкаЗапроса.Следующий() Цикл
			Результат.Вставить(ВыборкаЗапроса.Значение, ВыборкаЗапроса.Ссылка);
		КонецЦикла;	 
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДопСправочники_СоздатьЭлемент_Диадок(ИмяСправочника, Наименование) Экспорт
	
	Результат = Неопределено;

	Если ПроверитьНаличиеОбъектаМетаданных("Справочники.Диадок_ДополнительныеСправочники") Тогда
		
		Результат = Справочники.Диадок_ДополнительныеСправочники.СоздатьЭлемент();
		
		Результат.ИмяСправочника= ИмяСправочника;
		Результат.Наименование	= Наименование;
		
		Результат.Записать();
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции	 

Функция ДопСправочники_ПустаяСсылка_Диадок(ИмяСправочника)
	
	Результат = Неопределено;
	
	Если ИмяСправочника = "ВидыПакетов" Тогда
		
		Результат = Справочники.Диадок_ДополнительныеСправочники.ПустаяСсылка();
		
	КонецЕсли;	
	
	Возврат Результат;
	
КонецФункции

Процедура ДопСправочники_ЗаписатьТипыДокументовАПИ_Диадок(ТипыДокументовAPI)
	
	Для Каждого ТипДокументаAPI Из ТипыДокументовAPI Цикл
		
		ID		= ТипДокументаAPI.Значение.Name;
		Ссылка	= ДопСправочники_СсылкаПоИдентификатору_Диадок("ТипыДокументовAPI", ID);
		
		Если ЗначениеЗаполнено(Ссылка) Тогда
			ТекОбъект = Ссылка.ПолучитьОбъект();
			ТекОбъект.ДополнительныеРеквизиты.Очистить();
		Иначе
			ТекОбъект = ДопСправочники_СоздатьЭлемент_Диадок("ТипыДокументовAPI", ТипДокументаAPI.Значение.Title);
		КонецЕсли;
		
		ДопРеквизиты_УстановитьЗначение_Диадок(ТекОбъект, "ID",					ТипДокументаAPI.Значение.Name,	ТипДокументаAPI.Значение.Title);
		ДопРеквизиты_УстановитьЗначение_Диадок(ТекОбъект, "ХранилищеОписания",	Новый ХранилищеЗначения(ТипДокументаAPI.Значение), "");
		
		ТекОбъект.Записать();
		
	КонецЦикла;
	
КонецПроцедуры	
	
//**********************************************
// ДОП. СПРАВОЧНИКИ: 1С

Функция ДопРеквизиты_СписокРеквизитов_1С(Ссылка, СписокДопРеквизитов)
	
	Результат = Новый Структура(СписокДопРеквизитов);
	
	Запрос = Новый Запрос(                                            

	"ВЫБРАТЬ РАЗРЕШЕННЫЕ                                             
	|	ЗначенияСвойствОбъектов.Объект,
	|	ЗначенияСвойствОбъектов.Свойство.Наименование КАК ИмяРеквизита,
	|	ЗначенияСвойствОбъектов.Значение КАК ЗначениеРеквизита
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Объект = &Ссылка");
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	Запрос.Текст	= СтрЗаменить(Запрос.Текст, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		ИмяРеквизита = Выборка.ИмяРеквизита;
		
		Если СписокДопРеквизитов = ""
			ИЛИ Результат.Свойство(ИмяРеквизита) Тогда
			
			Результат.Вставить(ИмяРеквизита, Выборка.ЗначениеРеквизита);
			
		КонецЕсли;	
		
	КонецЦикла;	
	
	Возврат Результат;
	
КонецФункции

Функция ДопРеквизиты_Значение_1С(ОбъектСсылка, НаименованиеСвойства, СПрефиксом = Истина) Экспорт
	
	Если ЗначениеЗаполнено(ОбъектСсылка) Тогда
		Объект = ОбъектСсылка;
	Иначе
		Объект = ОбъектОбщихНастроек_1С();
	КонецЕсли;	
	
	Результат = Свойства_ЗначениеСвойства(НаименованиеСвойства, Объект, СПрефиксом);
	
	Возврат Результат;	
	
КонецФункции

Функция ДопРеквизиты_ЗначениеШифрованияДокумента_1С(ИдентификаторыОрганизации, ДляОрганизации = Истина)
	
	Если ДляОрганизации Тогда
		Элемент = Организации_СопоставленнаяВ1С_1С(ИдентификаторыОрганизации);
	Иначе
		Элемент = Контрагенты_СопоставленныйВ1С_1С(ИдентификаторыОрганизации);
	КонецЕсли;
	
	Результат	= ДопРеквизиты_Значение_1С(Элемент, Перечисление_ИменаСвойствНастроек_1С().ШифроватьДокументы);
	
	Возврат Результат;
	
КонецФункции

Процедура ДопРеквизиты_УстановитьЗначение_1С(Элемент, ИмяСвойства, Значение, СПрефиксом = Истина) Экспорт
	
	Если Не ЗначениеЗаполнено(ИмяСвойства) Тогда
		Возврат;
	КонецЕсли;
	
	// BSLLS:SetPrivilegedMode-off
	УстановитьПривилегированныйРежим(Истина);
	// BSLLS:SetPrivilegedMode-on
	
	Свойство = ИмяСвойства;
	
	Если ТипЗнч(ИмяСвойства) = Тип("Строка") Тогда
		Свойство = Свойства_СсылкаНаСвойство(ИмяСвойства, СПрефиксом);
	КонецЕсли;
				
	Если ЗначениеЗаполнено(Элемент) Тогда
		Объект = Элемент.Ссылка;
	Иначе
		Объект = ОбъектОбщихНастроек_1С();
	КонецЕсли;
	
	ИмяРегистра = Свойства_ИмяРегистраСведений();
	Запись		= РегистрыСведений[ИмяРегистра].СоздатьМенеджерЗаписи();
	
	Запись.Объект	= Объект;
	Запись.Свойство = Свойство;
	Запись.Значение = Значение;
	
	Запись.Записать();
	
КонецПроцедуры

Процедура ДопРеквизиты_УдалитьЗначение_1С(Элемент, СсылкаНаСвойство)
	
	Если НЕ ЗначениеЗаполнено(СсылкаНаСвойство) Тогда
		Возврат;
	КонецЕсли;
	
	Свойство = СсылкаНаСвойство;
	
	Если ТипЗнч(СсылкаНаСвойство) = Тип("Строка") Тогда
		Свойство = Свойства_СсылкаНаСвойство(СсылкаНаСвойство);
	КонецЕсли;
		
	ИмяРегистра = Свойства_ИмяРегистраСведений();
	
	Запись = РегистрыСведений[ИмяРегистра].СоздатьМенеджерЗаписи();
	
	Запись.Объект	= Элемент;
	Запись.Свойство = Свойство;
	
	Запись.Удалить();
	
КонецПроцедуры

Функция ДопСправочники_СписокЭлементов_1С(ИмяСправочника, ПереченьРеквизитов)
	
	Результат = ДопСправочники_НоваяТаблица_1С(ПереченьРеквизитов);
	
	Если ИмяСправочника = "ТипыДокументовAPI" Тогда
		
		Таблица = Кэш_Прочитать("ТаблицаТиповДокументовAPI");
		
	ИначеЕсли ИмяСправочника = "ВидыПакетов"
		  ИЛИ ИмяСправочника = "ВидыДокументов" Тогда
		  
		Таблица = СопоставлениеДанныхПМ_ТаблицаВидовСправочника(ИмяСправочника);
		
	ИначеЕсли ИмяСправочника = "ВидыГруппировкиПолученных" Тогда
		
		Таблица = Кэш_Прочитать("ТаблицаВидовГруппировкиПолученных");
		
		Если Таблица = Неопределено Тогда
			
			Таблица = ДопСправочники_ВидыГруппировкиПолученных_1С();
			Кэш_Поместить("ТаблицаВидовГруппировкиПолученных", Таблица);
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если Таблица <> Неопределено Тогда
		Для Каждого ТекСтрока Из Таблица Цикл
			НоваяСтрока = Результат.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, ТекСтрока);
		КонецЦикла;
	КонецЕсли;
	
	Возврат Результат;               
	
КонецФункции

Функция ДопСправочники_НоваяТаблица_1С(ПереченьРеквизитов)
	
	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("Ссылка",		Новый ОписаниеТипов("Строка", Новый КвалификаторыСтроки(50)));
	Результат.Колонки.Добавить("Наименование",	Новый ОписаниеТипов("Строка", Новый КвалификаторыСтроки(150)));
	
	СписокДопРеквизитов = РазделитьСтрокуЛок(ПереченьРеквизитов);
	
	Для Каждого ДопРеквизит Из СписокДопРеквизитов Цикл
		
		Если ДопРеквизит = "УПД" Тогда
			Результат.Колонки.Добавить(ДопРеквизит, Новый ОписаниеТипов("Булево"));
		ИначеЕсли ДопРеквизит = "ID" Тогда
			Результат.Колонки.Добавить(ДопРеквизит, Новый ОписаниеТипов("Строка", Новый КвалификаторыСтроки(50)));	
		Иначе	
			Результат.Колонки.Добавить(ДопРеквизит);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции	

Функция ДопСправочники_СсылкаПоИдентификатору_1С(ИмяСправочника, Идентификатор)
	
	Результат = Идентификатор;
	
	Возврат Результат;
	
КонецФункции

// см. ДопСправочники_СсылкиНаЭлементыСправочникаПоИдентификаторам
//
Функция ДопСправочники_СсылкиНаЭлементыСправочникаПоИдентификаторам_1С(ИмяСправочника, Идентификаторы)

	Результат = Новый Соответствие;
	
	Для Каждого ИдентификаторЭлемента Из Идентификаторы Цикл
		Результат.Вставить(ИдентификаторЭлемента, ИдентификаторЭлемента);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ДопСправочники_ПустаяСсылка_1С(ИмяСправочника)
	
	Результат = Неопределено;
	
	Если ИмяСправочника = "ВидыПакетов" Тогда
		
		Результат = "";
		
	КонецЕсли;	
	
	Возврат Результат;
	
КонецФункции

Процедура ДопСправочники_ЗаписатьТипыДокументовАПИ_1С(ТипыДокументовAPI)
	
	ТаблицаТиповДокументовAPI = Новый ТаблицаЗначений;
	ТаблицаТиповДокументовAPI.Колонки.Добавить("Наименование");
	ТаблицаТиповДокументовAPI.Колонки.Добавить("ID");
	ТаблицаТиповДокументовAPI.Колонки.Добавить("ХранилищеОписания");
	
	Для Каждого ТипДокументаAPI Из ТипыДокументовAPI Цикл
		
		НоваяСтрока = ТаблицаТиповДокументовAPI.Добавить();
		
		НоваяСтрока.Наименование		= ТипДокументаAPI.Значение.Title;
		НоваяСтрока.ID					= ТипДокументаAPI.Значение.Name;
		НоваяСтрока.ХранилищеОписания	= ТипДокументаAPI.Значение;
		
	КонецЦикла;
	
	Кэш_Поместить("ТаблицаТиповДокументовAPI", ТаблицаТиповДокументовAPI);
	
КонецПроцедуры

Функция ДопСправочники_ВидыГруппировкиПолученных_1С()
	
	Результат = Новый ТаблицаЗначений;
	КолонкиТаблицы = "Наименование, ТипыДокументов, Ссылка";
	ДобавитьКолонкиВТаблицуЗначений(Результат, КолонкиТаблицы);
	
	ДопСправочники_ЗаполнитьВидыГруппировкиПолученных(Результат);
	
	Результат.Колонки.Добавить("ID");
	МассивНаименований = Результат.ВыгрузитьКолонку("Наименование");
	Результат.ЗагрузитьКолонку(МассивНаименований, "ID");
			
	Возврат Результат;
	
КонецФункции

//}		ДОП. СПРАВОЧНИКИ И РЕКВИЗИТЫ


///////////////////////////////////////////////////////////////////////////////
//{		ЗНАЧЕНИЯ СВОЙСТВ

Функция Свойства_ИдентификаторДокумента()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ИдентификаторДокумента;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ИдентификаторЯщикаДокумента()
	
	СПрефиксом = Ложь;
	ИмяРеквизита = Перечисление_ИменаСвойствНастроек_1С().ИдентификаторЯщикаДокумента;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ИдентификаторЯщикаДокументаСчет()
	
	СПрефиксом = Ложь;
	ИмяРеквизита = Перечисление_ИменаСвойствНастроек_1С().ИдентификаторЯщикаДокументаСчет;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ВидПакета()
	
	СПрефиксом = Ложь;
	ИмяРеквизита = Перечисление_ИменаСвойствНастроек_1С().ВидПакета;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ВидПакетаСчет()
	
	СПрефиксом = Ложь;
	ИмяРеквизита = Перечисление_ИменаСвойствНастроек_1С().ВидПакетаСчет;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ДокументУчетаПакета()
	
	СПрефиксом = Ложь;
	ИмяРеквизита = Перечисление_ИменаСвойствНастроек_1С().ДокументУчетаПакета;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ДокументУчетаПакетаСчет()
	
	СПрефиксом = Ложь;
	ИмяРеквизита = Перечисление_ИменаСвойствНастроек_1С().ДокументУчетаПакетаСчет;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ДиадокCustomDocumentIdСчет()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ДиадокCustomDocumentIdСчет;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ИдентификаторСчетаОФ()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ИдентификаторСчетаОФ;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ИдентификаторСчетаУФ()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ИдентификаторСчетаУФ;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ИдентификаторЯщикаСчета()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ИдентификаторЯщикаСчета;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ИдентификаторЯщикаДокументаЭТрН()
	
	СПрефиксом = Ложь;
	ИмяРеквизита = Перечисление_ИменаСвойствНастроек_1С().ИдентификаторЯщикаДокументаЭТрН;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ИдентификаторЯщикаДокументаЗЗ()
	
	СПрефиксом = Ложь;
	ИмяРеквизита = Перечисление_ИменаСвойствНастроек_1С().ИдентификаторЯщикаДокументаЗЗ;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ИдентификаторДокументаЭТрН()
	
	СПрефиксом = Ложь;
	ИмяРеквизита = Перечисление_ИменаСвойствНастроек_1С().ИдентификаторДокументаЭТрН;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ИдентификаторДокументаЗЗ()
	
	СПрефиксом = Ложь;
	ИмяРеквизита = Перечисление_ИменаСвойствНастроек_1С().ИдентификаторДокументаЗЗ;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ДиадокCustomDocumentId()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ДиадокCustomDocumentId;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_IDОрганизации()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().IDОрганизации;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_IDОсновноеПодразделениеОрганизации()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().IDОсновноеПодразделениеОрганизации;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ОсновнойПакет()
	
	СПрефиксом		= Истина;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ОсновнойПакет;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ШифроватьДокументы()
	
	СПрефиксом		= Истина;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ШифроватьДокументы;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_УказыватьОтсутствиеОснованияУПД()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().УказыватьОтсутствиеОснованияУПД;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ОтправлятьНепроведенныеДокументы()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ОтправлятьНепроведенныеДокументы;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ЗаписыватьКомментарииВДокументы1С()
	
	СПрефиксом		= Истина;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ЗаписыватьКомментарииВДокументы1С;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ТекстКомментарияОтправка()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ТекстКомментарияОтправка;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ТекстКомментарияПолучение()
	
	СПрефиксом		= Истина;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ТекстКомментарияПолучение;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ОтпечатокСертификатаНаСервере()
	
	СПрефиксом		= Истина;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().ОтпечатокСертификатаНаСервере;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_IDКонтрагента()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().IDКонтрагента;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_IDОсновноеПодразделениеКонтрагента()
	
	СПрефиксом		= Ложь;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().IDОсновноеПодразделениеКонтрагента;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_IDПодразделенияОрганизации()
	
	СПрефиксом		= Истина;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().IDПодразделенияОрганизации;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_IDПодразделенияКонтрагента()
	
	СПрефиксом		= Истина;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().IDПодразделенияКонтрагента;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_АвтоматическаяОтправкаПриглашений()
	
	СПрефиксом		= Истина;
	ИмяРеквизита	= Перечисление_ИменаСвойствНастроек_1С().АвтоматическаяОтправкаПриглашений;
	
	Результат = Свойства_СсылкаНаСвойство(ИмяРеквизита, СПрефиксом);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_СсылкаНаСвойство(Наименование, СПрефиксом = Истина)
	
	Результат = Кэш_Прочитать(ПрефиксНастройки_1С(СПрефиксом) + Наименование);
	
	Если Результат = Неопределено Тогда
		
		Результат = Свойства_НайтиСвойство(Наименование, СПрефиксом);
		
		Если НЕ ЗначениеЗаполнено(Результат) Тогда
			Результат = Свойства_СоздатьСвойство(Наименование, СПрефиксом);
		КонецЕсли;

		Кэш_Поместить(ПрефиксНастройки_1С(СПрефиксом) + Наименование, Результат);

	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_НайтиСвойство(Наименование, СПрефиксом = Истина)
	
	Результат = ПланыВидовХарактеристик[Свойства_ИмяПланаВидовХарактеристик()].НайтиПоНаименованию(ПрефиксНастройки_1С(СПрефиксом) + Наименование, Истина);
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ЗначениеСвойства(СтрокаИлиСсылка, ОбъектСсылка, СПрефиксом = Истина)
	
	Результат = Неопределено;
	
	ПараметрСвойство = СтрокаИлиСсылка;
	Если ТипЗнч(СтрокаИлиСсылка) = Тип("Строка") Тогда
		ПараметрСвойство = Свойства_СсылкаНаСвойство(СтрокаИлиСсылка, СПрефиксом);
	КонецЕсли;
	
	Запрос = Новый Запрос(
			"ВЫБРАТЬ РАЗРЕШЕННЫЕ
			|	ЗначенияСвойствОбъектов.Объект,
			|	ЗначенияСвойствОбъектов.Свойство.Наименование КАК ИмяСвойства,
			|	ЗначенияСвойствОбъектов.Значение КАК ЗначениеСвойства
			|ИЗ
			|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
			|ГДЕ
			|	ЗначенияСвойствОбъектов.Свойство = &Свойство
			|	И ЗначенияСвойствОбъектов.Объект = &Объект
			|
			|УПОРЯДОЧИТЬ ПО
			|	Объект");
	
	Запрос.УстановитьПараметр("Свойство", ПараметрСвойство);
	Запрос.УстановитьПараметр("Объект", ОбъектСсылка);
	
	ИмяРегистра = Свойства_ИмяРегистраСведений();
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Если ВыборкаЗапроса.Следующий() Тогда
		Результат = ВыборкаЗапроса.ЗначениеСвойства;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ЗначениеОбъекта(НаименованиеСвойства, Значение, СПрефиксом = Истина)
	
	Результат = Неопределено;
	
	Запрос = Новый Запрос;
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЗначенияСвойствОбъектов.Объект КАК Ссылка
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Значение = &Значение
	|	И ЗначенияСвойствОбъектов.Свойство = &Свойство";
	
	Запрос.УстановитьПараметр("Значение",	Значение);
	Запрос.УстановитьПараметр("Свойство",	Свойства_СсылкаНаСвойство(НаименованиеСвойства, СПрефиксом));
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	ТекстЗапроса	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Запрос.Текст = ТекстЗапроса;
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Если ВыборкаЗапроса.Следующий() Тогда
		Результат = ВыборкаЗапроса.Ссылка;
	КонецЕсли;

	Возврат Результат;
	
КонецФункции

Функция Свойства_ОбщиеНастройки()
	
	Результат = Новый Структура;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЗначенияСвойствОбъектов.Объект,
	|	ЗначенияСвойствОбъектов.Свойство.Наименование КАК ИмяСвойства,
	|	ЗначенияСвойствОбъектов.Значение КАК ЗначениеСвойства
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Объект = &Объект
	|	И ПОДСТРОКА(ЗначенияСвойствОбъектов.Свойство.Наименование, 1, 6) = &ПрефиксНастройки
	|
	|УПОРЯДОЧИТЬ ПО
	|	ЗначенияСвойствОбъектов.Объект");
	
	Запрос.УстановитьПараметр("Объект",				ОбъектОбщихНастроек_1С());
	Запрос.УстановитьПараметр("ПрефиксНастройки",	ПрефиксНастройки_1С());
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	Запрос.Текст	= СтрЗаменить(Запрос.Текст, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Пока ВыборкаЗапроса.Следующий() Цикл	
		
		Ключ		= СтрЗаменить(ВыборкаЗапроса.ИмяСвойства, ПрефиксНастройки_1С(), "");
		Значение	= ВыборкаЗапроса.ЗначениеСвойства;
		Результат.Вставить(Ключ, Значение);

	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_СправочникИспользуетсяДляХраненияСвойств(ИмяСправочника)
	
	Результат = Ложь;
	
	Если ПроверитьНаличиеОбъектаМетаданных("Справочники." + ИмяСправочника) Тогда
	
		ИмяРегистра = Свойства_ИмяРегистраСведений();
		МетаданныеРегистра = Метаданные.РегистрыСведений.Найти(ИмяРегистра);
		
		Если МетаданныеРегистра <> Неопределено Тогда
			
			ИзмерениеРегистра = МетаданныеРегистра.Измерения.Найти("Объект");
			
			Если ИзмерениеРегистра <> Неопределено Тогда
				
				ТипСправочника = Тип("СправочникСсылка." + ИмяСправочника);
				Результат = ИзмерениеРегистра.Тип.СодержитТип(ТипСправочника);
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ИмяРегистраСведений()
	
	Результат = Кэш_Прочитать("ИмяРегистраСведений");
	
	Если Результат = Неопределено Тогда

		ИмяРегистраДополнительныеСведения 	= "ДополнительныеСведения";
		ИмяРегистраЗначенияСвойствОбъектов 	= "ЗначенияСвойствОбъектов";

		Результат = "";
		
		ЕстьРегистрДополнительныеСведения 	= (Метаданные.РегистрыСведений.Найти(ИмяРегистраДополнительныеСведения) <> Неопределено);
		ЕстьРегистрЗначенияСвойствОбъектов 	= (Метаданные.РегистрыСведений.Найти(ИмяРегистраЗначенияСвойствОбъектов) <> Неопределено);
		
		Если ЕстьРегистрЗначенияСвойствОбъектов И ЕстьРегистрДополнительныеСведения Тогда
			
			Если Настройки_ИспользуетсяРегистрСведенийЗначенияСвойствОбъектов() Тогда
				Результат = ИмяРегистраЗначенияСвойствОбъектов;	
			Иначе
				Результат = ИмяРегистраДополнительныеСведения;	
			КонецЕсли;		
			
		ИначеЕсли ЕстьРегистрЗначенияСвойствОбъектов Тогда
			
			Результат = ИмяРегистраЗначенияСвойствОбъектов;
			
		ИначеЕсли ЕстьРегистрДополнительныеСведения Тогда
			
			Результат = ИмяРегистраДополнительныеСведения;
			
		КонецЕсли;
		
		Кэш_Поместить("ИмяРегистраСведений", Результат);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_ИмяПланаВидовХарактеристик()

	Результат = "";
	
	ИмяРегистраСведений	= Свойства_ИмяРегистраСведений();
	ТипИзмерения		= Метаданные.РегистрыСведений[ИмяРегистраСведений].Измерения.Свойство.Тип.Типы()[0];
	
	Результат = Метаданные.НайтиПоТипу(ТипИзмерения).Имя;
	
	Возврат Результат;

КонецФункции

Функция Свойства_ИмяСправочникаДополнительныхОбработок()
	
	Результат = Кэш_Прочитать("ИмяСправочникаДополнительныхОбработок");
	Если Результат = Неопределено Тогда
		
		ИменаСправочников = Новый Массив;
		ИменаСправочников.Добавить("ВнешниеОбработки");
		ИменаСправочников.Добавить("ДополнительныеОтчетыИОбработки");
		ИменаСправочников.Добавить("ВнешниеПечатныеФормы");
		
		Для Каждого ИмяСправочника Из ИменаСправочников Цикл
			
			Если Метаданные.Справочники.Найти(ИмяСправочника) <> Неопределено Тогда
				Результат = ИмяСправочника;
				Прервать;
			КонецЕсли;
			
		КонецЦикла;
		
		Кэш_Поместить("ИмяСправочникаДополнительныхОбработок", Результат);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Свойства_СоздатьСвойство(Наименование, СПрефиксом)
	
	// BSLLS:SetPrivilegedMode-off
	УстановитьПривилегированныйРежим(Истина);
	// BSLLS:SetPrivilegedMode-on
	
	ИмяПланаВидовХарактеристик = Свойства_ИмяПланаВидовХарактеристик();
	
	ОбъектМетаданных = Метаданные.ПланыВидовХарактеристик[ИмяПланаВидовХарактеристик];
	Если Не ПравоДоступа("Добавление", ОбъектМетаданных) Тогда
		Ошибка = "У текущего пользователя недостаточно прав для инициализации настроек модуля!";
		ВызватьИсключение Ошибка;
	КонецЕсли;
	
	СвойствоОбъект = ПланыВидовХарактеристик[ИмяПланаВидовХарактеристик].СоздатьЭлемент();
	
	СвойствоОбъект.Наименование	= ПрефиксНастройки_1С(СПрефиксом) + Наименование;
	СвойствоОбъект.ТипЗначения	= Новый ОписаниеТипов("Строка", Новый КвалификаторыСтроки(50));	
	СвойствоОбъект.УстановитьНовыйКод();
	
	СтрокаМетаданных = "ПланыВидовХарактеристик." + ИмяПланаВидовХарактеристик + ".Реквизиты.Заголовок";
	Если ПроверитьНаличиеОбъектаМетаданных(СтрокаМетаданных) Тогда
		СвойствоОбъект.Заголовок = СвойствоОбъект.Наименование;
	КонецЕсли;
	
	СвойствоОбъект.ОбменДанными.Загрузка = Истина;
	СвойствоОбъект.Записать();
	
	Результат = СвойствоОбъект.Ссылка;
	
	Возврат Результат; 
	
КонецФункции

Функция Свойства_ТекстЗапроса_ЗначениеОбъекта()
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЗначенияСвойствОбъектов.Объект КАК Ссылка
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
	|ГДЕ
	|	ЗначенияСвойствОбъектов.Значение = &Значение
	|	И ЗначенияСвойствОбъектов.Свойство = &Свойство
	|
	|УПОРЯДОЧИТЬ ПО
	|	Ссылка
	|АВТОУПОРЯДОЧИВАНИЕ";
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	ТекстЗапроса	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Возврат ТекстЗапроса;
	
КонецФункции

//}		ЗНАЧЕНИЯ СВОЙСТВ

///////////////////////////////////////////////////////////////////////////////
//{		СВОЙСТВА

Функция Свойство_IDОсновногоПодразделения()
	
	Результат = Неопределено;
	
	ИмяСвойства_ИдентификаторОсновногоПодразделения = Перечисление_ИменаСвойствНастроек_1С().IDОсновноеПодразделениеКонтрагента;
	Результат = Свойства_СсылкаНаСвойство(ИмяСвойства_ИдентификаторОсновногоПодразделения, Ложь);
	
	Возврат Результат;

КонецФункции

Функция Свойство_IDКонтрагента()
	
	Результат = Неопределено;
	
	ИмяСвойства_ИдентификаторКонтрагента = Перечисление_ИменаСвойствНастроек_1С().IDКонтрагента;
	Результат = Свойства_СсылкаНаСвойство(ИмяСвойства_ИдентификаторКонтрагента, Ложь);
	
	Возврат Результат;

КонецФункции

// Возвращает дополнительное свойство "ТипОснования" в зависимости от используемой системы хранения
//
// Параметры:
//	ВидДокумента - Строка - идентификатор вида документа: "ID_АктФормализованный", "ID_ТОРГ12" и т.д.
//
// ВозвращаемоеЗначение:
//	Строка, ПВХСсылка
Функция Свойства_ТипОснованияПоВидуДокумента(ВидДокумента)
	
	КлючСвойства = КлючСвойстваТипОснования(ВидДокумента);
			
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = КлючСвойства;
	Иначе
		Результат = Свойства_СсылкаНаСвойство(КлючСвойства);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция КлючСвойстваТипОснования(ВидДокумента)
	
	Результат = "ТипОсн_" + ВидДокумента;
	
	Возврат Результат;
	
КонецФункции

// Возвращает дополнительное свойство "Формат" в зависимости от используемой системы хранения
//
// Параметры:
//  ВидДокумента - Строка - идентификатор вида документа: "ID_АктФормализованный", "ID_ТОРГ12" и т.д.
// 
// Возвращаемое значение:
//  Строка, ПланВидовХарактеристикСсылка - Дополнительное свойство формата электронного документа на отправку
//
Функция Свойства_ФорматОтправкиПоВидуДокумента(ВидДокумента)
	
	КлючСвойства = КлючСвойстваФорматОтправки(ВидДокумента);
			
	Если ИспользоватьПодсистемуДиадока() Тогда
		Результат = КлючСвойства;
	Иначе
		Результат = Свойства_СсылкаНаСвойство(КлючСвойства);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция КлючСвойстваФорматОтправки(ВидДокумента)
	
	Результат = "Формат_" + ВидДокумента;
	
	Возврат Результат;
	
КонецФункции

//}		СВОЙСТВА

///////////////////////////////////////////////////////////////////////////////
//{		ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ

Функция СформироватьСсылкуВБраузере(BoxId, LetterId, DocumentId)
	
	Результат = Ядро.СформироватьСсылкуВБраузере(BoxId, LetterId, DocumentId);
	
	Возврат Результат;
	
КонецФункции

Процедура ВнешнееХранение_ВыполнитьОчисткуУстаревшихДокументов() Экспорт
	
	ОбщиеНастройки = Настройки_ОбщиеНастройки();
	
	Если ОбщиеНастройки.УдалятьСодержимоеУстаревшихДокументов <> Истина
		ИЛИ ОбщиеНастройки.СрокОчисткиДокументов <= 0 Тогда
		Возврат;
	КонецЕсли;
	
	КолСекундВСутках = 60 * 60 * 24;
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ДатаЗагрузки", ТекущаяДата() - ОбщиеНастройки.СрокОчисткиДокументов * КолСекундВСутках);
	
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Диадок_ПакетыДокументов.Код КАК Код
	|ИЗ
	|	Справочник.Диадок_ПакетыДокументов КАК Диадок_ПакетыДокументов
	|ГДЕ
	|	Диадок_ПакетыДокументов.ДатаЗагрузки < &ДатаЗагрузки
	|	И Диадок_ПакетыДокументов.Метка <> ""Подготовлен""";
	
	Выборка = Запрос.Выполнить().Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	
	Пока Выборка.Следующий() Цикл
		
		Каталог	= ВнешнееХранение_КаталогПакета(Выборка.Код);
		
		Файл = Новый Файл(Каталог);
		
		Если Файл.Существует() Тогда
			
			Попытка
				УдалитьФайлы(Каталог);
			Исключение
				
				ВидОперации		 = "Очистка устаревших документов";
				ТекстСообщения	 = "Не удалось удалить каталог %1 пакета %2";
				ТекстСообщения	 = СтрШаблонЛок(ТекстСообщения, Каталог, Выборка.Код);
				ТекстОшибки		 = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
				
				ДопИнформация	 = Новый Массив;
				ДопИнформация.Добавить(СтрШаблонЛок("Пакет: %1", Выборка.Код));
				ДопИнформация.Добавить(СтрШаблонЛок("Каталог: %1", Каталог));
				
				Ошибка_Обработать(ВидОперации, ТекстОшибки, ДопИнформация, ТекстСообщения);
				
			КонецПопытки;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция ВнешнееХранение_ИмяФайлаНаДиске(КодПакета, КодДокумента, ИмяХранилища, ЭтоЗашифрованныеДанные = Ложь, ЭтоПодпись = Неопределено)
	
	Результат = Новый Структура("Каталог, ИмяФайла");
	
	МассивЧастейПути = Новый Массив;
	
	МассивЧастейПути.Добавить(Настройки_ОбщиеНастройки().КорневойКаталогДокументов);
	МассивЧастейПути.Добавить(ВнешнееХранение_ПрефиксКаталогаПакета() + КодПакета);
	МассивЧастейПути.Добавить(ВнешнееХранение_ПрефиксКаталогаДокумента() + КодДокумента);
	
	Результат.Каталог = СоединитьСтроку(МассивЧастейПути, "\");
	
	МассивЧастейПути.Добавить(ИмяХранилища
							 + ?(ЭтоЗашифрованныеДанные, ".enc", "")
							 + ?(ЭтоПодпись <> Неопределено, ".sgn", "")
							 );
	
	Результат.ИмяФайла = СоединитьСтроку(МассивЧастейПути, "\");
	
	Возврат Результат;
	
КонецФункции

Функция ВнешнееХранение_КаталогПакета(КодПакета)
	
	ЧастиПути = Новый Массив;
	ЧастиПути.Добавить(Настройки_ОбщиеНастройки().КорневойКаталогДокументов);
	ЧастиПути.Добавить(ВнешнееХранение_ПрефиксКаталогаПакета() + КодПакета);
	
	Результат = СоединитьСтроку(ЧастиПути, "\");
	Возврат Результат;
	
КонецФункции

Функция ВнешнееХранение_ПрефиксКаталогаПакета()
	
	Возврат "П_";
	
КонецФункции

Функция ВнешнееХранение_ПрефиксКаталогаДокумента()
	
	Возврат "Д_";
	
КонецФункции

Процедура Пакеты_ПредОбработкаПодготовленного_Диадок(Пакет)
	
	ПараметрыПереотправки = Неопределено;
	
	Пакет.СлужебнаяИнформация.Свойство("ПараметрыПереотправки", ПараметрыПереотправки);
	
	Если ПараметрыПереотправки <> Неопределено Тогда
		
		ПереотправляемыйПакет = ПараметрыПереотправки.ПереотправляемыйПакет;
		
		Если ЗначениеЗаполнено(ПереотправляемыйПакет.ПакетЭДО)
			И ПереотправляемыйПакет.ПакетЭДО.Метка = Метка_Отправлен()
			И Настройки_ОбщиеНастройки().УдалятьПриПереотправкеПредыдущийДокумент Тогда
			
			Пакеты_ОбработкаПереотправки_Диадок(Пакет);
			
		КонецЕсли;	
		
		Если ПараметрыПереотправки.ВыполнитьУдалениеНеактуальныхДокументовПакета Тогда
			Пакеты_ПометитьНаУдалениеНеактуальныеДокументы_Диадок(Пакет);
		КонецЕсли;
		
		ПараметрыПереотправки = Неопределено;
		
	КонецЕсли;
	
КонецПроцедуры	

Процедура Пакеты_ОбработкаПереотправки_Диадок(Пакет)
	
	Идентификаторы = Пакет.СлужебнаяИнформация.ПараметрыПереотправки.ПереотправляемыйПакет;
	
	ДокументыПереотправляемогоПакета = Пакеты_ДокументыПакета_Диадок(Идентификаторы);
	
	Для Каждого ИдентификаторыДокумента Из ДокументыПереотправляемогоПакета Цикл				
		
		ТекДокумент = ИдентификаторыДокумента.ДокументЭДО;
		
		ПереотправляемыйДокументОбъект			= ТекДокумент.ПолучитьОбъект();
		ПереотправляемыйДокументОбъект.Метка	= Метка_Переотправлен();
		ПереотправляемыйДокументОбъект.Записать();
		
	КонецЦикла;
	
	ПереотправляемыйПакет = Идентификаторы.ПакетЭДО;
	
	ПереотправляемыйПакетОбъект = ПереотправляемыйПакет.ПолучитьОбъект();
	ПереотправляемыйПакетОбъект.Метка = Метка_Переотправлен();
	ПереотправляемыйПакетОбъект.Записать();
	
	Пакет.ДопРеквизиты.Вставить("ПереотправленныйПакет", ПереотправляемыйПакет);	
	
КонецПроцедуры

Процедура Пакеты_ПометитьНаУдалениеНеактуальныеДокументы_Диадок(Пакет)
	
	ТекущиеДокументыПакета = Новый Массив;
	
	Для Каждого СтрокаСостава Из Пакет.Документы Цикл
		
		ДокументЭДО = СтрокаСостава.Идентификаторы.ДокументЭДО;
		
		Если ДокументЭДО <> Неопределено Тогда
			ТекущиеДокументыПакета.Добавить(ДокументЭДО);
		КонецЕсли;
		
	КонецЦикла;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Диадок_Документы.Пакет КАК ПакетДД,
	|	Диадок_Документы.Ссылка КАК ДокументДД,
	|	Диадок_ХранениеДанных.Ссылка КАК ЗаписьХранилищаДД,
	|	Диадок_Документы.ДатаЗагрузки КАК ДатаЗагрузки
	|ИЗ
	|	Справочник.Диадок_Документы КАК Диадок_Документы
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ХранениеДанных КАК Диадок_ХранениеДанных
	|		ПО (Диадок_ХранениеДанных.Владелец = Диадок_Документы.Ссылка)
	|ГДЕ
	|	Диадок_Документы.Пакет = &Пакет
	|	И НЕ Диадок_Документы.Ссылка В (&ТекущиеДокументыПакета)
	|	И НЕ Диадок_Документы.ПометкаУдаления
	|ИТОГИ ПО
	|	ПакетДД,
	|	ДокументДД,
	|	ЗаписьХранилищаДД";
	
	Запрос.УстановитьПараметр("Пакет",					Пакет.Идентификаторы.ПакетЭДО);
	Запрос.УстановитьПараметр("ТекущиеДокументыПакета",	ТекущиеДокументыПакета);
	
	ВыборкаПакеты = Запрос.Выполнить().Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	
	Пока ВыборкаПакеты.Следующий() Цикл
		Пакеты_ОчиститьСохраненныеДанныеДокументов_Диадок(ВыборкаПакеты, Истина);
	КонецЦикла;
	
КонецПроцедуры

Процедура Пакеты_ОчиститьСохраненныеДанныеДокументов_Диадок(ВыборкаПакеты, УдалятьДокументыДД = Ложь)
	
	УдалятьНепосредственно = Ложь;
	
	МассивУдаляемыхСсылок		= Новый Массив;
	МассивУдаляемыхКаталогов	= Новый Массив;
	
	ВыборкаДокументы = ВыборкаПакеты.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	
	Пока ВыборкаДокументы.Следующий() Цикл
		
		Если Настройки_ОбщиеНастройки().ХранитьДокументыНаДиске = Истина Тогда
			
			РазмещениеФайла = ВнешнееХранение_ИмяФайлаНаДиске(ВыборкаДокументы.ДокументДД.Пакет.Код, ВыборкаДокументы.ДокументДД.Код, "");
			МассивУдаляемыхКаталогов.Добавить(РазмещениеФайла.Каталог);
			
		КонецЕсли;
		
		Если УдалятьДокументыДД Тогда
			МассивУдаляемыхСсылок.Добавить(ВыборкаДокументы.ДокументДД);
		КонецЕсли;
		
		ВыборкаЗаписиХранилища = ВыборкаДокументы.Выбрать();
		
		Пока ВыборкаЗаписиХранилища.Следующий() Цикл
			
			Если ЗначениеЗаполнено(ВыборкаЗаписиХранилища.ЗаписьХранилищаДД) Тогда
				
				МассивУдаляемыхСсылок.Добавить(ВыборкаЗаписиХранилища.ЗаписьХранилищаДД);
				МассивУдаляемыхСсылок.Добавить(ВыборкаЗаписиХранилища.ЗаписьХранилищаДД.ХранилищеДанных);
				МассивУдаляемыхСсылок.Добавить(ВыборкаЗаписиХранилища.ЗаписьХранилищаДД.ХранилищеЗашифрованныхДанных);
				
				Для Каждого СтрокаПодписи Из ВыборкаЗаписиХранилища.ЗаписьХранилищаДД.Подписи Цикл
					МассивУдаляемыхСсылок.Добавить(СтрокаПодписи.ХранилищеПодписи);
				КонецЦикла;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЦикла;
	
	Для Каждого Каталог Из МассивУдаляемыхКаталогов Цикл
		
		Ф = Новый Файл(Каталог);
		Если Ф.Существует() Тогда
			УдалитьФайлы(Каталог);
		КонецЕсли;
		
	КонецЦикла;
	
	РежимСовместимости_8_1 = (Метаданные.РежимСовместимости = Метаданные.СвойстваОбъектов.РежимСовместимости.Версия8_1);
	
	НачатьТранзакцию();
	
	Попытка
		
		Для Каждого Ссылка Из МассивУдаляемыхСсылок Цикл
			
			Если РежимСовместимости_8_1 Тогда
				ВерсияДанныхСсылки = "_";
			Иначе
				ВерсияДанныхСсылки = Ссылка.ВерсияДанных;
			КонецЕсли;
			
			Если Истина
				И ЗначениеЗаполнено(Ссылка)
				И ЗначениеЗаполнено(ВерсияДанныхСсылки) Тогда
				
				УдаляемыйОбъект = Ссылка.ПолучитьОбъект();
				
				Если УдалятьНепосредственно Тогда
					УдаляемыйОбъект.Удалить();
				Иначе
					
					Если Не УдаляемыйОбъект.ПометкаУдаления Тогда
						УдаляемыйОбъект.УстановитьПометкуУдаления(Истина);
					КонецЕсли;
					
				КонецЕсли;
			
			КонецЕсли;
			
		КонецЦикла;
		
		ЗафиксироватьТранзакцию();
	
	Исключение
		
		Если ТранзакцияАктивна() Тогда
			ОтменитьТранзакцию();
		КонецЕсли;
		
		_Ошибка = ОписаниеОшибки();
		
		ВызватьИсключение;
		
	КонецПопытки;
	
КонецПроцедуры

Функция МассивВидовГруппировок_В_МассивВидовДокументов(МассивВидовГруппировок)
	
	ВидыГруппировок = ДопСправочники_СписокЭлементов("ВидыГруппировкиПолученных", "ТипыДокументов");
	
	Результат = Новый Массив;
	
	Для Каждого ТекВид Из ВидыГруппировок Цикл
		
		Если МассивВидовГруппировок.Найти(ТекВид.Ссылка) = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		ТипыДокументовТекВидаМассивом = РазделитьСтрокуЛок(ТекВид.ТипыДокументов, ",");
		
		Для Каждого ТекТип Из ТипыДокументовТекВидаМассивом Цикл
			Результат.Добавить(ТекТип);
		КонецЦикла;
		
	КонецЦикла;
	
	Возврат Результат;
		
КонецФункции

Процедура ЗаполнитьУчастниковДокументооборота(Отправитель, Получатель, Выборка)
	
	Если Выборка.Направление = "Входящее" Тогда
		ДанныеКонтрагента = Отправитель;
		ДанныеОрганизации = Получатель;
	Иначе
		ДанныеОрганизации = Отправитель;
		ДанныеКонтрагента = Получатель;
	КонецЕсли;
	
	ДанныеКонтрагента.BoxId				= Выборка.Контрагент_BoxId;
	ДанныеКонтрагента.FNSParticipantId	= Выборка.Контрагент_FNSParticipantId;
	ДанныеКонтрагента.Организация		= Выборка.Контрагент_Организация;
	ДанныеКонтрагента.ОрганизацияЭДО	= Выборка.Контрагент_ОрганизацияЭДО;
	
	ДанныеОрганизации.BoxId				= Выборка.Организация_BoxId;
	ДанныеОрганизации.FNSParticipantId	= Выборка.Организация_FNSParticipantId;
	ДанныеОрганизации.Организация		= Выборка.Организация_Организация;
	ДанныеОрганизации.ОрганизацияЭДО	= Выборка.Организация_ОрганизацияЭДО;
	
КонецПроцедуры

// Находит всех контрагентов сервиса, сопоставленных с контрагентом информационной базы.
//
// Параметры:
//  КонтрагентИБ - ЛюбаяСсылка - контрагент ИБ, по связи с которым требуется отобрать контрагентов ДД.
// 
// Возвращаемое значение:
//  Массив из СправочникСсылка.Диадок_ЮрФизЛица - элементы справочника с типом "Контрагенты".
//
Функция Справочники_СвязанныеКонтрагентыСервиса_Диадок(КонтрагентИБ)
	
	Если Не ЗначениеЗаполнено(КонтрагентИБ) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ИмяСправочника = "Контрагенты";
	
	Результат = Справочники_СвязанныеЮрФизЛица_Диадок(КонтрагентИБ, ИмяСправочника);
	
	Возврат Результат;
	
КонецФункции

// Находит все организации сервиса, сопоставленные с организацией информационной базы.
//
// Параметры:
//  ОрганизацияИБ - ЛюбаяСсылка - организация ИБ, по связи с которой требуется отобрать организации ДД.
// 
// Возвращаемое значение:
//  Массив из СправочникСсылка.Диадок_ЮрФизЛица - элементы справочника с типом "Организации".
//
Функция Справочники_СвязанныеОрганизацииСервиса_Диадок(ОрганизацияИБ)
	
	Если Не ЗначениеЗаполнено(ОрганизацияИБ) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ИмяСправочника = "Организации";
	
	Результат = Справочники_СвязанныеЮрФизЛица_Диадок(ОрганизацияИБ, ИмяСправочника);
	
	Возврат Результат;
	
КонецФункции

// Получает все элементы справочника юридических и физических лиц,
// с которыми сопоставлена указанная сущность информационной базы
//
// Параметры:
//  СсылкаНаОбъект - ЛюбаяСсылка - сущность ИБ, по связи с которой требуется отобрать сущности сервиса
//  ИмяСправочника - Строка - тип сущностей сервиса ("Контрагенты", "Организации", "ПодразделенияКонтрагентов" или "ПодразделенияОрганизаций")
// 
// Возвращаемое значение:
//  Массив из СправочникСсылка.Диадок_ЮрФизЛица
//
Функция Справочники_СвязанныеЮрФизЛица_Диадок(СсылкаНаОбъект, ИмяСправочника)
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		ТекстЗапроса = Справочники_ТекстЗапроса_СвязанныеЮрФизЛица_ДиадокРасширение();
	Иначе
		ТекстЗапроса = Справочники_ТекстЗапроса_СвязанныеЮрФизЛица_ДиадокСХ1();
	КонецЕсли;
	
	Запрос = Новый Запрос(ТекстЗапроса);
	
	Запрос.УстановитьПараметр("ИмяСправочника", ИмяСправочника);
	Запрос.УстановитьПараметр("ИмяРеквизита", ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник());
	Запрос.УстановитьПараметр("СсылкаНаОбъект", СсылкаНаОбъект);
	Запрос.УстановитьПараметр("Отбор", Истина);
	
	Результат = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Ссылка");
	
	Возврат Результат;
	
КонецФункции

Функция Справочники_ТекстЗапроса_СвязанныеЮрФизЛица_ДиадокРасширение()

	Результат =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка КАК Ссылка,
	|	Диадок_ЮрФизЛица.ID КАК ID
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО (&Отбор)
	|			И Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ИмяРеквизита)
	|			И (Диадок_ЮрФизЛица.ИмяСправочника = &ИмяСправочника)
	|ГДЕ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение = &СсылкаНаОбъект
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции

Функция Справочники_ТекстЗапроса_СвязанныеЮрФизЛица_ДиадокСХ1()

	Результат =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.Ссылка КАК Ссылка,
	|	Диадок_ЮрФизЛица.ID КАК ID
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|ГДЕ
	|	&Отбор
	|	И Диадок_ЮрФизЛица.ИмяСправочника = &ИмяСправочника
	|	И Диадок_ЮрФизЛица.СвязанныйСправочник1 = &СсылкаНаОбъект
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления
	|
	|ОБЪЕДИНИТЬ
	|
	|ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.Ссылка,
	|	Диадок_ЮрФизЛица.ID
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|ГДЕ
	|	&Отбор
	|	И Диадок_ЮрФизЛица.ИмяСправочника = &ИмяСправочника
	|	И Диадок_ЮрФизЛица.СвязанныйСправочник2 = &СсылкаНаОбъект
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления
	|
	|ОБЪЕДИНИТЬ
	|
	|ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.Ссылка,
	|	Диадок_ЮрФизЛица.ID
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|ГДЕ
	|	&Отбор
	|	И Диадок_ЮрФизЛица.ИмяСправочника = &ИмяСправочника
	|	И Диадок_ЮрФизЛица.СвязанныйСправочник3 = &СсылкаНаОбъект
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления
	|
	|ОБЪЕДИНИТЬ
	|
	|ВЫБРАТЬ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка,
	|	Диадок_ЮрФизЛица.ID
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ПО (&Отбор)
	|			И Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ИмяРеквизита)
	|			И (Диадок_ЮрФизЛица.ИмяСправочника = &ИмяСправочника)
	|ГДЕ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение = &СсылкаНаОбъект
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции
	

Функция Справочники_СсылкаПоИдентификатору_Диадок(Идентификаторы, ИмяСправочника)

	Результат = Неопределено;
	
	BoxId = СвойствоСтруктуры(Идентификаторы, "ID",	"");
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|ГДЕ
	|	Диадок_ЮрФизЛица.ID = &BoxId
	|	И Диадок_ЮрФизЛица.ИмяСправочника = &ИмяСправочника
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Запрос.УстановитьПараметр("BoxId", BoxId); 
		
	Запрос.УстановитьПараметр("ИмяСправочника",	ИмяСправочника);
		
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Если ВыборкаЗапроса.Следующий() Тогда
		Результат = ВыборкаЗапроса.Ссылка;
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция Справочники_СопоставленныеВ1С_Диадок(ИмяСправочника, Идентификаторы)
	
	Результат = Новый Массив;
	
	ИмяСвойства = Перечисление_ИменаСвойствНастроек_Диадок().СвязанныйСправочник;
	
	Буфер = Новый ТаблицаЗначений;
	Буфер.Колонки.Добавить("Ссылка");
	Буфер.Колонки.Добавить("НомерСвязи");
	
	СвязанныеСправочники = Справочники_СвязанныеСправочники_Диадок(ИмяСправочника, Идентификаторы);
		
	Для каждого ЭлементТЗ Из СвязанныеСправочники Цикл
		
		// При переходе на множественное сопоставление организаций(контрагентов, подразделений)
		// возможны ситуации, когда реквизиты не заполнены.
		
		Если Не ЗначениеЗаполнено(ЭлементТЗ.ОбъектИБ) Тогда
			Продолжить;
		КонецЕсли;
		
		НомерСвойства = СтрЗаменить(СокрЛП(ЭлементТЗ.ИмяРеквизита), ИмяСвойства, "");
		
		НовыйЭлементБуфера = Буфер.Добавить();
		НовыйЭлементБуфера.Ссылка 		= ЭлементТЗ.ОбъектИБ;
		НовыйЭлементБуфера.НомерСвязи 	= Число(НомерСвойства);
						
	КонецЦикла;
	
	Буфер.Сортировать("НомерСвязи");
	
	Для Каждого ЭлементБуфера Из Буфер Цикл
		Результат.Добавить(ЭлементБуфера.Ссылка);
	КонецЦикла;
			
	Возврат Результат;
	
КонецФункции


Функция Справочники_ВыборкаДанныхСправочника_Диадок(ИмяСправочника, Идентификаторы)

	Результат = Неопределено;
	
	ID = Идентификаторы.ID;
	Ссылка = Идентификаторы.Ссылка;
	
	Запрос = Новый Запрос;
	Запрос.Текст = Справочники_ТекстЗапросаПоСправочникам_Диадок(Ссылка);
	
	Запрос.УстановитьПараметр("ID", ID);
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	Запрос.УстановитьПараметр("ИмяСправочника", ИмяСправочника);
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Если ВыборкаЗапроса.Следующий() Тогда
		Результат = ВыборкаЗапроса;
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция Справочники_ТекстЗапросаПоСправочникам_Диадок(Ссылка)
	
	Если ЗначениеЗаполнено(Ссылка) Тогда
		Отбор = "Диадок_ЮрФизЛица.Ссылка = &Ссылка";
	Иначе
		Отбор = "Диадок_ЮрФизЛица.ИмяСправочника = &ИмяСправочника И Диадок_ЮрФизЛица.ID = &ID";
	КонецЕсли;
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		Результат = Справочники_ТекстЗапроса_ДанныеСправочника_ДиадокРасширение();
	Иначе
		Результат = Справочники_ТекстЗапроса_ДанныеСправочника_ДиадокСХ1();
	КонецЕсли;
	
	Результат = СтрЗаменить(Результат, "&Отбор", Отбор);
	
	Возврат Результат;
	
КонецФункции

Функция Справочники_ТекстЗапроса_ДанныеСправочника_ДиадокРасширение()
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.Ссылка КАК Ссылка,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.ID КАК ID,
	|	Диадок_ЮрФизЛица.ИНН КАК ИНН,
	|	Диадок_ЮрФизЛица.КПП КАК КПП,
	|	Диадок_ЮрФизЛица.Аббревиатура КАК Аббревиатура,
	|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение КАК ID_ОсновноеПодразделение,
	|	Диадок_ЮрФизЛица.ID_ВладелецПодразделения КАК ID_ВладелецПодразделения,
	|	Диадок_ЮрФизЛица.ID_РодительПодразделения КАК ID_РодительПодразделения,
	|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО КАК ИдентификаторУчастникаЭДО,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК СвязанныйСправочник1
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ПО (Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = Диадок_ЮрФизЛица.Ссылка)
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита = ""СвязанныйСправочник1"")
	|ГДЕ
	|	&Отбор
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции

Функция Справочники_ТекстЗапроса_ДанныеСправочника_ДиадокСХ1()
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.Ссылка КАК Ссылка,
	|	Диадок_ЮрФизЛица.Наименование КАК Наименование,
	|	Диадок_ЮрФизЛица.ID КАК ID,
	|	Диадок_ЮрФизЛица.ИНН КАК ИНН,
	|	Диадок_ЮрФизЛица.КПП КАК КПП,
	|	Диадок_ЮрФизЛица.Аббревиатура КАК Аббревиатура,
	|	Диадок_ЮрФизЛица.ID_ОсновноеПодразделение КАК ID_ОсновноеПодразделение,
	|	Диадок_ЮрФизЛица.ID_ВладелецПодразделения КАК ID_ВладелецПодразделения,
	|	Диадок_ЮрФизЛица.ID_РодительПодразделения КАК ID_РодительПодразделения,
	|	Диадок_ЮрФизЛица.ИдентификаторУчастникаЭДО КАК ИдентификаторУчастникаЭДО,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник1 КАК СвязанныйСправочник1,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник2 КАК СвязанныйСправочник2,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник3 КАК СвязанныйСправочник3
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|ГДЕ
	|	&Отбор
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции


Функция Справочники_СвязанныйСправочник_Диадок(ИмяСправочника, Идентификаторы)
	
	Результат = Неопределено;

	Если ЗначениеЗаполнено(Идентификаторы.Ссылка) Тогда
		Отбор = "Диадок_ЮрФизЛица.Ссылка = &Ссылка";
	Иначе
		Отбор = "Диадок_ЮрФизЛица.ИмяСправочника = """ + ИмяСправочника + """ И Диадок_ЮрФизЛица.ID = &ID";
	КонецЕсли;
	
	Запрос = Новый Запрос;
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		Запрос.Текст = Справочники_ТекстЗапроса_ОсновныеСвязи_ДиадокРасширение();
	Иначе
		Запрос.Текст = Справочники_ТекстЗапроса_ОсновныеСвязи_ДиадокСХ1();
	КонецЕсли;
	
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&Отбор", Отбор);
	
	Запрос.УстановитьПараметр("ID",		Идентификаторы.ID);
	Запрос.УстановитьПараметр("Ссылка",	Идентификаторы.Ссылка);
	
	ВыборкаЗапроса = Запрос.Выполнить().Выбрать();
	
	Если ВыборкаЗапроса.Следующий() Тогда
		Результат = ВыборкаЗапроса.СвязанныйСправочник;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Справочники_ТекстЗапроса_ОсновныеСвязи_ДиадокРасширение()
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК СвязанныйСправочник
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ПО Диадок_ЮрФизЛица.Ссылка = Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка
	|			И (Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита = ""СвязанныйСправочник1"")
	|ГДЕ
	|	&Отбор
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции

Функция Справочники_ТекстЗапроса_ОсновныеСвязи_ДиадокСХ1()
	
	Результат =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.СвязанныйСправочник1 КАК СвязанныйСправочник
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|ГДЕ
	|	&Отбор
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции

Функция Справочники_СвязанныеСправочники_Диадок(ИмяСправочника, Идентификаторы)
	
	Если ЗначениеЗаполнено(Идентификаторы.Ссылка) Тогда
		Отбор = "Диадок_ЮрФизЛица.Ссылка = &Ссылка";
	Иначе
		Отбор = "Диадок_ЮрФизЛица.ИмяСправочника = """ + ИмяСправочника + """ И Диадок_ЮрФизЛица.ID = &ID";
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.Ссылка КАК Ссылка
	|ПОМЕСТИТЬ ВТ_ЮрФизЛицаДиадок
	|ИЗ        
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|ГДЕ
	|	&Отбор
	|	И НЕ Диадок_ЮрФизЛица.ПометкаУдаления
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|" + Справочники_ТекстЗапросаВременнойТаблицыСвязиОбъектов_Диадок(ИмяСправочника);
	
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&Отбор", Отбор);
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "ПОМЕСТИТЬ ВремСвязиОбъектов", "");
	
	Запрос.УстановитьПараметр("ID",		Идентификаторы.ID);
	Запрос.УстановитьПараметр("Ссылка",	Идентификаторы.Ссылка);
	
	Запрос.УстановитьПараметр("ШаблонИмениРеквизита_СвязанныйСправочник", ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник());
	
	Результат = Запрос.Выполнить().Выгрузить();
		
	Возврат Результат;
	
КонецФункции

Функция Справочники_ТекстЗапросаВременнойТаблицыСвязиОбъектов_Диадок(ИмяСправочника)
	
	Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		ТекстЗапроса = Справочники_ТекстЗапроса_ТаблицаСвязейОбъектов_ДиадокРасширение(ИмяСправочника);
	Иначе
		ТекстЗапроса = Справочники_ТекстЗапроса_ТаблицаСвязейОбъектов_ДиадокСХ1();
	КонецЕсли;
	
	ТекстЗапроса_УстановитьИмяТаблицыКонтрагентов(ТекстЗапроса);
	
	КонтрагентыМД = МетаданныеСправочникаКонтрагенты();
	
	Если Не Общее_ЕстьРеквизитОбъекта("ИНН", КонтрагентыМД) Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ЕСТЬNULL(КонтрагентыИБ.ИНН, """")", """""");
	КонецЕсли;
	
	Если Не Общее_ЕстьРеквизитОбъекта("КПП", КонтрагентыМД) Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ЕСТЬNULL(КонтрагентыИБ.КПП, """")", """""");
	КонецЕсли;
		
	Если ИмяСправочника = "Организации" Тогда
		ТекстЗапроса_УстановитьИмяТаблицыОрганизаций(ТекстЗапроса);
	КонецЕсли;
	
	Возврат ТекстЗапроса;
	
КонецФункции

Функция Справочники_ТекстЗапроса_ТаблицаСвязейОбъектов_ДиадокРасширение(ИмяСправочника)
	
	Результат =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка КАК ОбъектДиадок,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение КАК ОбъектИБ,
	|	ЕСТЬNULL(КонтрагентыИБ.Наименование, """") КАК НаименованиеОбъектаИБ,
	|	ЕСТЬNULL(КонтрагентыИБ.ИНН, """") КАК ИННОбъектаИБ,
	|	ЕСТЬNULL(КонтрагентыИБ.КПП, """") КАК КППОбъектаИБ,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита КАК ИмяРеквизита
	|ПОМЕСТИТЬ ВремСвязиОбъектов
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ЮрФизЛицаДиадок КАК ЮрФизЛицаДиадок
	|		ПО Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = ЮрФизЛицаДиадок.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Контрагенты КАК КонтрагентыИБ
	|		ПО Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение = КонтрагентыИБ.Ссылка
	|ГДЕ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ШаблонИмениРеквизита_СвязанныйСправочник";
	
	Если ИмяСправочника = "Организации" Тогда
		Результат = СтрЗаменить(Результат, "Справочник.Контрагенты", "Справочник." + ИмяСправочника);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Справочники_ТекстЗапроса_ТаблицаСвязейОбъектов_ДиадокСХ1()
	
	Результат =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Диадок_ЮрФизЛица.Ссылка 					КАК ОбъектДиадок,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник1 		КАК ОбъектИБ,
	|	ЕСТЬNULL(КонтрагентыИБ.Наименование, """") 	КАК НаименованиеОбъектаИБ,
	|	ЕСТЬNULL(КонтрагентыИБ.ИНН, """") 			КАК ИННОбъектаИБ,
	|	ЕСТЬNULL(КонтрагентыИБ.КПП, """") 			КАК КППОбъектаИБ,
	|	""СвязанныйСправочник1"" 					КАК ИмяРеквизита
	|ПОМЕСТИТЬ ВремСвязиОбъектов
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ЮрФизЛицаДиадок КАК ЮрФизЛицаДиадок
	|		ПО Диадок_ЮрФизЛица.Ссылка = ЮрФизЛицаДиадок.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Контрагенты КАК КонтрагентыИБ
	|		ПО Диадок_ЮрФизЛица.СвязанныйСправочник1 = КонтрагентыИБ.Ссылка
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.Ссылка,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник2,
	|	ЕСТЬNULL(КонтрагентыИБ.Наименование, """"),
	|	ЕСТЬNULL(КонтрагентыИБ.ИНН, """"),
	|	ЕСТЬNULL(КонтрагентыИБ.КПП, """"),
	|	""СвязанныйСправочник2""
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ЮрФизЛицаДиадок КАК ЮрФизЛицаДиадок
	|		ПО Диадок_ЮрФизЛица.Ссылка = ЮрФизЛицаДиадок.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Контрагенты КАК КонтрагентыИБ
	|		ПО Диадок_ЮрФизЛица.СвязанныйСправочник2 = КонтрагентыИБ.Ссылка
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Диадок_ЮрФизЛица.Ссылка,
	|	Диадок_ЮрФизЛица.СвязанныйСправочник3,
	|	ЕСТЬNULL(КонтрагентыИБ.Наименование, """"),
	|	ЕСТЬNULL(КонтрагентыИБ.ИНН, """"),
	|	ЕСТЬNULL(КонтрагентыИБ.КПП, """"),
	|	""СвязанныйСправочник3""
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица КАК Диадок_ЮрФизЛица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ЮрФизЛицаДиадок КАК ЮрФизЛицаДиадок
	|		ПО Диадок_ЮрФизЛица.Ссылка = ЮрФизЛицаДиадок.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Контрагенты КАК КонтрагентыИБ
	|		ПО Диадок_ЮрФизЛица.СвязанныйСправочник3 = КонтрагентыИБ.Ссылка
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка,
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение,
	|	ЕСТЬNULL(КонтрагентыИБ.Наименование, """"),
	|	ЕСТЬNULL(КонтрагентыИБ.ИНН, """"),
	|	ЕСТЬNULL(КонтрагентыИБ.КПП, """"),
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита
	|ИЗ
	|	Справочник.Диадок_ЮрФизЛица.ДополнительныеРеквизиты КАК Диадок_ЮрФизЛицаДополнительныеРеквизиты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ЮрФизЛицаДиадок КАК ЮрФизЛицаДиадок
	|		ПО Диадок_ЮрФизЛицаДополнительныеРеквизиты.Ссылка = ЮрФизЛицаДиадок.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Контрагенты КАК КонтрагентыИБ
	|		ПО Диадок_ЮрФизЛицаДополнительныеРеквизиты.Значение = КонтрагентыИБ.Ссылка
	|ГДЕ
	|	Диадок_ЮрФизЛицаДополнительныеРеквизиты.ИмяРеквизита ПОДОБНО &ШаблонИмениРеквизита_СвязанныйСправочник";
	
	Возврат Результат;
	
КонецФункции


Функция Справочники_ЗначениеДопРеквизита_Диадок(Выборка)
	
	Результат = Неопределено;
	
	Если ЗначениеЗаполнено(Выборка.Значение) Тогда
		Результат = Выборка.Значение;
	ИначеЕсли ЗначениеЗаполнено(Выборка.ЗначениеСтрока) Тогда
		Результат = Выборка.ЗначениеСтрока;
	Иначе
		Результат = Выборка.ЗначениеХранилищеЗначения.Получить();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Справочники_ЗначениеЗаписиНастроекКонтрагентов_Диадок(СтруктураДанных)
	
	Результат = Неопределено;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Диадок_НастройкиКонтрагентов.Значение КАК Значение
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
	|ГДЕ
	|	Диадок_НастройкиКонтрагентов.Организация = &Организация
	|	И Диадок_НастройкиКонтрагентов.Контрагент = &Контрагент
	|	И Диадок_НастройкиКонтрагентов.ИмяСвойства = &ИмяСвойства");
	
	Запрос.УстановитьПараметр("Организация",	СтруктураДанных.ОрганизацияЭДО);
	Запрос.УстановитьПараметр("Контрагент",		СтруктураДанных.КонтрагентЭДО);
	Запрос.УстановитьПараметр("ИмяСвойства",	СтруктураДанных.ИмяСвойства);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		Результат = Выборка.Значение;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура Справочники_ЗаписатьЗначениеНастроекКонтрагентов_Диадок(СтруктураДанных)
	
	ОрганизацияЭДО	= СтруктураДанных.ОрганизацияЭДО;
	КонтрагентЭДО	= СтруктураДанных.КонтрагентЭДО;
	ИмяСвойства		= СтруктураДанных.ИмяСвойства;
	Значение		= СтруктураДанных.Значение;
	
	НаборЗаписей	= РегистрыСведений.Диадок_НастройкиКонтрагентов.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.Организация.Установить(ОрганизацияЭДО);
	НаборЗаписей.Отбор.Контрагент.Установить(КонтрагентЭДО);
	НаборЗаписей.Отбор.ИмяСвойства.Установить(ИмяСвойства);
	
	НаборЗаписей.Очистить();
	
	ЗаписьРегистра	= НаборЗаписей.Добавить();
	ЗаписьРегистра.Контрагент	= КонтрагентЭДО;
	ЗаписьРегистра.Организация	= ОрганизацияЭДО;
	ЗаписьРегистра.ИмяСвойства	= ИмяСвойства;
	ЗаписьРегистра.Значение		= Значение;
	
	НаборЗаписей.Записать();
	
КонецПроцедуры

Процедура Справочники_УдалитьЗначениеНастроекКонтрагентов_Диадок(СтруктураДанных)
	
	МенеджерЗаписи = РегистрыСведений.Диадок_НастройкиКонтрагентов.СоздатьМенеджерЗаписи();
	
	МенеджерЗаписи.Организация	= СтруктураДанных.ОрганизацияЭДО;
	МенеджерЗаписи.Контрагент	= СтруктураДанных.КонтрагентЭДО;
	МенеджерЗаписи.ИмяСвойства	= СтруктураДанных.ИмяСвойства;
	
	МенеджерЗаписи.Удалить();
	
КонецПроцедуры

// Очищает сопоставленные объектам подсистемы Диадок справочники 1С(Организации, Контрагенты, Подразделения)
//
// Параметры:
//	Идентификаторы	- Структура	- коллекция индентификаторов для получения объекта исходя из выбранной системы хранения
//	ИмяСправочника	- Строка 	- значение псевдоперечисления "Перечисление_ИменаСправочников_Диадок()"
Процедура Справочники_ОчиститьСвязанныеСправочники(Идентификаторы, ИмяСправочника) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		Справочники_ОчиститьСвязанныеСправочники_Диадок(Идентификаторы, ИмяСправочника);
	Иначе
		Справочники_ОчиститьСвязанныеСправочники_1С(Идентификаторы, ИмяСправочника);
	КонецЕсли;
	
КонецПроцедуры

Процедура Справочники_ОчиститьСвязанныеСправочники_Диадок(Идентификаторы, ИмяСправочника)
	
	ЮрФизЛицоЭДО = СвойствоСтруктуры(Идентификаторы, "Ссылка");
	
	Если НЕ ЗначениеЗаполнено(ЮрФизЛицоЭДО) Тогда
		ЮрФизЛицоЭДО = Справочники_СсылкаПоИдентификатору_Диадок(Идентификаторы, ИмяСправочника);
	КонецЕсли;
	
	ЮрФизЛицоОбъект = ЮрФизЛицоЭДО.ПолучитьОбъект();
	
	Справочники_ЮрФизЛицоОбъект_ОчиститьСвязанныеСправочники(ЮрФизЛицоОбъект);
			
	ЮрФизЛицоОбъект.Записать();
	
КонецПроцедуры

Процедура Справочники_ОчиститьСвязанныеСправочники_1С(Идентификаторы, ИмяСправочника)
	
	ИменаСправочников = Перечисление_ИменаСправочников_Диадок();
	
	Если ИмяСправочника = ИменаСправочников.Организации Тогда
		
		МассивСопоставленных = Организации_МассивСопоставленныхВ1С_1С(Новый Структура("ID", Идентификаторы.ID));
		Свойство = Свойства_IDОрганизации();
		
	ИначеЕсли ИмяСправочника = ИменаСправочников.Контрагенты Тогда
		
		МассивСопоставленных = Контрагенты_МассивСопоставленныхВ1С_1С(Новый Структура("ID", Идентификаторы.ID));
		Свойство = Свойства_IDКонтрагента();
		
	ИначеЕсли ИмяСправочника = ИменаСправочников.ПодразделенияОрганизаций 
		Или ИмяСправочника = ИменаСправочников.ПодразделенияКонтрагентов Тогда
		
		ДляОрганизации = (ИмяСправочника = ИменаСправочников.ПодразделенияОрганизаций);
		
		МассивСопоставленных = Подразделения_МассивСопоставленныхВ1С_1С(Новый Структура("ID", Идентификаторы.ID), ДляОрганизации);
		Свойство = ИмяСвойстваПодразделения(ДляОрганизации);

	КонецЕсли;
		
	Для Каждого СопоставленныйСправочник Из МассивСопоставленных Цикл
		ДопРеквизиты_УдалитьЗначение_1С(СопоставленныйСправочник, Свойство);
	КонецЦикла;
		
КонецПроцедуры

Функция ТипыДокументовПоВидамДокументов(МассивВидовДокументов)
	
	Результат = Новый Массив;
	
	Если ЗначениеЗаполнено(МассивВидовДокументов) Тогда 
		
		ТаблицаВидовДокументов = ДопСправочники_СписокЭлементов_1С("ВидыДокументов", "ТипДокументаAPI");
		
		Для Каждого ВидДокумента Из МассивВидовДокументов Цикл
			
			НайденныйВид = ТаблицаВидовДокументов.Найти(ВидДокумента, "Ссылка");
			
			Если НайденныйВид <> Неопределено Тогда
				Результат.Добавить(НайденныйВид.ТипДокументаAPI);
			КонецЕсли;	
			
		КонецЦикла;
		
	Иначе
		Результат.Добавить("Any");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТипыДокументовПоВидамДокументовПолученных(МассивВидовДокументов)
	
	Результат = Новый Массив;
	
	Если ЗначениеЗаполнено(МассивВидовДокументов) Тогда 
		
		ТаблицаВидовДокументов = ДопСправочники_СписокЭлементов_1С("ВидыГруппировкиПолученных", "ТипыДокументов");
		
		Для Каждого ВидДокумента Из МассивВидовДокументов Цикл
			
			НайденныйВид = ТаблицаВидовДокументов.Найти(ВидДокумента, "Наименование");
			
			Если НайденныйВид <> Неопределено Тогда
				
				МассивТиповДокументов = РазделитьСтрокуЛок(НайденныйВид.ТипыДокументов);
				
				Для Каждого ТипДокумента Из МассивТиповДокументов Цикл
					Результат.Добавить(ТипДокумента);
				КонецЦикла;
				
			КонецЕсли;	
			
		КонецЦикла;
		
	Иначе
		Результат.Добавить("Any");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТаблицаСтарыхСоответствийКонтрагентов()
	
	Запрос = Новый Запрос;
	
	Свойство_IDКонтрагента = Свойства_СсылкаНаСвойство(Перечисление_ИменаСвойствНастроек_1С().IDКонтрагента, Ложь);
	
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ДополнительныеСведения.Объект КАК Контрагент,
	|	ВЫРАЗИТЬ(ДополнительныеСведения.Значение КАК СТРОКА(50)) КАК ID
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ДополнительныеСведения
	|ГДЕ
	|	ДополнительныеСведения.Свойство = &Свойство_IDКонтрагента";
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	ТекстЗапроса	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("Свойство_IDКонтрагента", Свойство_IDКонтрагента);
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Возврат Результат;
	
КонецФункции

Процедура ДопСправочники_ЗаполнитьВидыГруппировкиПолученных(ТаблицаВидовГруппировки)
	
	МассивДобавленныхТипов = Новый Массив;
	
	ТаблицаТиповДокументов = ПредопределенныеСписки_ТипыДокументовAPI();
	
	Для Каждого СтрокаТЗ Из ТаблицаТиповДокументов Цикл		
		
		Если ЗначениеЗаполнено(СтрокаТЗ.TypeNamedId) Тогда
			
			СтрокаРезультата = ТаблицаВидовГруппировки.Найти(СтрокаТЗ.ГруппировкаДокументов, "Наименование");
			
			Если СтрокаРезультата = Неопределено Тогда
				
				СтрокаРезультата = ТаблицаВидовГруппировки.Добавить();
				СтрокаРезультата.Наименование	= СтрокаТЗ.ГруппировкаДокументов;
				СтрокаРезультата.ТипыДокументов	= "";
				
			ИначеЕсли ЗначениеЗаполнено(СтрокаРезультата.Ссылка) Тогда
				Продолжить;
			КонецЕсли;
			
			Если МассивДобавленныхТипов.Найти(СтрокаТЗ.TypeNamedId) = Неопределено Тогда
				
				Если СтрокаРезультата.ТипыДокументов = "" Тогда
					СтрокаРезультата.ТипыДокументов = СтрокаТЗ.TypeNamedId;
				Иначе	
					СтрокаРезультата.ТипыДокументов = СтрокаРезультата.ТипыДокументов + "," + СтрокаТЗ.TypeNamedId;
				КонецЕсли;
				
				МассивДобавленныхТипов.Добавить(СтрокаТЗ.TypeNamedId);
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры	

// Получает значения реквизитов для коллекции элементов СправочникСсылка
//
// Параметры:
//	КоллекцияСсылок	- Массив 	- массив с элементами СправочникСсылка
//	ИменаРеквизитов	- Строка	- имена реквизитов через запятую, значения которых необходимо получить
//
// Возвращаемое значение:
//	массив структур
Функция Справочники_ЗначенияРеквизитовЭлементовСправочникаИБ(КоллекцияСсылок, ИменаРеквизитов) Экспорт
	
	КонструкторЗапроса = КонструкторЗапросаРеквизитыЭлементовСправочникаИБ(КоллекцияСсылок, ИменаРеквизитов); 
			
	Запрос = Новый Запрос;
			
	ОбъединенияЗапросов = "	
							|
							| ОБЪЕДИНИТЬ ВСЕ
							| ";
	ТекстЗапроса = "";
	
	Для Каждого КлючЗначение Из КонструкторЗапроса Цикл
		
		Если ЗначениеЗаполнено(ТекстЗапроса) Тогда
			ТекстЗапроса = ТекстЗапроса + ОбъединенияЗапросов;
		КонецЕсли;
						
		ТекстЗапроса = ТекстЗапроса + СвойствоСтруктуры(КлючЗначение.Значение, "ТекстЗапроса", "");
		
		ПараметрыПодзапроса = СвойствоСтруктуры(КлючЗначение.Значение, "ПараметрыЗапроса", Новый Массив);
			
		Запрос.УстановитьПараметр("СвязанныеСправочники_" + КлючЗначение.Ключ, ПараметрыПодзапроса); 
		
	КонецЦикла;
	
	Если ЗначениеЗаполнено(ТекстЗапроса) Тогда
		ТекстЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ" + Сред(ТекстЗапроса, СтрДлина("ВЫБРАТЬ") + 1);
		
		Запрос.Текст 		= ТекстЗапроса;
		РезультатЗапроса 	= Запрос.Выполнить().Выгрузить();
		
		Результат = ТаблицаЗначенийВМассивСтруктур(РезультатЗапроса);
	Иначе
		Результат = Новый Массив;
	КонецЕсли;
						
	Возврат Результат;
	
КонецФункции

Функция КонструкторЗапросаРеквизитыЭлементовСправочникаИБ(КоллекцияСсылок, ИменаРеквизитов)
	
	КонструкторЗапроса = Новый Структура;
	
	Ключ_ТекстЗапроса 		= "ТекстЗапроса";
	Ключ_ПараметрыЗапроса 	= "ПараметрыЗапроса";
		
	ТребуемыеРеквизиты = РазделитьСтрокуЛок(ИменаРеквизитов);
	
	Для Каждого СправочникСсылка Из КоллекцияСсылок Цикл
		
		ИмяТаблицы = СправочникСсылка.Метаданные().Имя;
		
		ЭлементыЗапроса = СвойствоСтруктуры(КонструкторЗапроса, ИмяТаблицы, Новый Структура);
						
		// Текст запроса
		ТекстЗапроса = СвойствоСтруктуры(ЭлементыЗапроса, Ключ_ТекстЗапроса, "");
						
		Если Не ЗначениеЗаполнено(ТекстЗапроса) Тогда
			ТекстЗапроса = Справочники_ТекстЗапросаКТаблице(ИмяТаблицы, ТребуемыеРеквизиты);
			ЭлементыЗапроса.Вставить(Ключ_ТекстЗапроса, ТекстЗапроса);
		КонецЕсли;
		
		// Параметры
		ПараметрыЗапроса = СвойствоСтруктуры(ЭлементыЗапроса, Ключ_ПараметрыЗапроса, Новый Массив); 
								
		Если ПараметрыЗапроса.Найти(СправочникСсылка) = Неопределено Тогда
			ПараметрыЗапроса.Добавить(СправочникСсылка);
			ЭлементыЗапроса.Вставить(Ключ_ПараметрыЗапроса, ПараметрыЗапроса);
		КонецЕсли;
		
		КонструкторЗапроса.Вставить(ИмяТаблицы, ЭлементыЗапроса);
		
	КонецЦикла;
	
	Возврат КонструкторЗапроса;
	
КонецФункции

Функция Справочники_ТекстЗапросаКТаблице(ИмяТаблицы, ТребуемыеРеквизиты)
	
	ТекстЗапроса_Реквизиты = "";
	
	Счетчик = 1;
	Для Каждого ИмяРеквизита Из ТребуемыеРеквизиты Цикл
		
		РеквизитТаблицы = "Т."  + ИмяРеквизита;
		
		ТекстЗапроса_Реквизиты = ТекстЗапроса_Реквизиты + "
									| " + РеквизитТаблицы + " КАК " + ИмяРеквизита
									+ ?(Счетчик = ТребуемыеРеквизиты.Количество(), "", ",");
			
		СтрокаМетаданных = "Справочники." + ИмяТаблицы + ".Реквизиты." + ИмяРеквизита;
		Если Не ПроверитьНаличиеОбъектаМетаданных(СтрокаМетаданных) Тогда
			ТекстЗапроса_Реквизиты = СтрЗаменить(ТекстЗапроса_Реквизиты, РеквизитТаблицы, "НЕОПРЕДЕЛЕНО");	
		КонецЕсли;	
			
		Счетчик = Счетчик + 1;
		
	КонецЦикла;
	
	Результат =
		"ВЫБРАТЬ
		| Т.Ссылка," +
        ТекстЗапроса_Реквизиты + "
		|ИЗ
       	|	Справочник.%ИмяТаблицы% КАК Т
	   	|ГДЕ
	   	|	Т.Ссылка В (&СвязанныеСправочники_%ИмяТаблицы%)";
			
	Результат = СтрЗаменить(Результат, "%ИмяТаблицы%", ИмяТаблицы);
	
	Возврат Результат;
			
КонецФункции

Функция СвязанныеСправочники(ДанныеОрганизации, ДанныеКонтрагента)
	
	// метод предназначен для поддержки совместимости с подключаемыми модулями прошлых версий,
	// написанными до реализации множественного сопоставления организаций, контрагентов, подразделений 
	
	Если ДанныеКонтрагента = Неопределено Тогда
		ОбрабатываемыеДанные = ДанныеОрганизации;
	Иначе
		ОбрабатываемыеДанные = ДанныеКонтрагента;
	КонецЕсли;
	
	Результат = СвойствоСтруктуры(ОбрабатываемыеДанные, "СвязанныеСправочники");
	
	Если Результат = Неопределено Тогда
		Результат = Новый Массив;
		СвязанныйСправочник = ОбрабатываемыеДанные.СвязанныйСправочник1;
		Результат.Добавить(СвязанныйСправочник);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура Справочники_ЮрФизЛицоОбъект_ЗаполнитьСвязанныеСправочники(СправочникДиадокОбъект, СвязанныеСправочники)
	
	ИмяСвойстваСвязанныйСправочник = Перечисление_ИменаСвойствНастроек_Диадок().СвязанныйСправочник;
	
	Для Индекс = 0 По СвязанныеСправочники.ВГраница() Цикл
			
		ЭлементКоллекции = СвязанныеСправочники.Получить(Индекс);
		
		ИмяСвойстваСНомером = ИмяСвойстваСвязанныйСправочник + Формат(Индекс + 1, "ЧГ=0");
		
		Если ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() ИЛИ Индекс > 2 Тогда
			
			ДопРеквизиты_УстановитьЗначение(СправочникДиадокОбъект, ИмяСвойстваСНомером, ЭлементКоллекции.Ссылка);
			
		Иначе
			
			СправочникДиадокОбъект[ИмяСвойстваСНомером] = ЭлементКоллекции.Ссылка;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура Справочники_ЮрФизЛицоОбъект_ОчиститьСвязанныеСправочники(ДиадокЮрФизЛицоОбъект)
	
	Если НЕ ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц() Тогда
		
		Для Сч = 1 По 3 Цикл 
			ДиадокЮрФизЛицоОбъект["СвязанныйСправочник" + Сч] = Неопределено;
		КонецЦикла;
		
	КонецЕсли;
	
	ДопРеквизиты_ОчиститьСвязанныеСправочники_Диадок(ДиадокЮрФизЛицоОбъект);
	
КонецПроцедуры

Функция ТекстЗапроса_РазделительЗапросов()
	
	Результат = "
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|";
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ОбъединениеЗапросов()
	
	Результат = "
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|";
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ПоляИтогов(ИменаПолей)
	
	Результат = "
	|ИТОГИ ПО
	|";
	
	Результат = Результат + ИменаПолей;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ПоляУпорядочивания(ИменаПолей)
	
	Результат = "
	|УПОРЯДОЧИТЬ ПО
	|";
	
	Результат = Результат + ИменаПолей;
	
	Возврат Результат;
	
КонецФункции

Процедура ТекстЗапроса_УстановитьИмяРегистраСведений(ТекстЗапроса)
	
	ИмяРегистра		= Свойства_ИмяРегистраСведений();
	ТекстЗапроса	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ЗначенияСвойствОбъектов", "РегистрСведений." + ИмяРегистра);
	ТекстЗапроса	= СтрЗаменить(ТекстЗапроса, "РегистрСведений.ДополнительныеСведения", "РегистрСведений." + ИмяРегистра);
	
КонецПроцедуры

Процедура ТекстЗапроса_УстановитьИмяТаблицыОрганизаций(ТекстЗапроса)
	
	ОбъектМд = МетаданныеСправочникаОрганизации();
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "Справочник.Организации", ОбъектМд.ПолноеИмя());
	
КонецПроцедуры

Процедура ТекстЗапроса_УстановитьИмяТаблицыКонтрагентов(ТекстЗапроса)
	
	ОбъектМд = МетаданныеСправочникаКонтрагенты();
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "Справочник.Контрагенты", ОбъектМд.ПолноеИмя());
	
КонецПроцедуры

Процедура ТекстЗапроса_УстановитьИмяПланаВидовХарактеристик(ТекстЗапроса)

	ИмяПВХ 			= Свойства_ИмяПланаВидовХарактеристик();
	ТекстЗапроса 	= СтрЗаменить(ТекстЗапроса, "ПланВидовХарактеристик.СвойстваОбъектов", "ПланВидовХарактеристик." + ИмяПВХ);
	
КонецПроцедуры

Процедура Запросы_ЗаполнитьПараметрыСвойствИдентификаторовДокумента(Запрос)
	
	СвойстваИдентификаторовДокументов = Новый Массив;
	СвойстваИдентификаторовДокументов.Добавить(Свойства_ИдентификаторДокумента());
	СвойстваИдентификаторовДокументов.Добавить(Свойства_ИдентификаторСчетаУФ());
	СвойстваИдентификаторовДокументов.Добавить(Свойства_ИдентификаторСчетаОФ());
	СвойстваИдентификаторовДокументов.Добавить(Свойства_ИдентификаторДокументаЭТрН());
	СвойстваИдентификаторовДокументов.Добавить(Свойства_ИдентификаторДокументаЗЗ());
	
	Запрос.УстановитьПараметр("СвойстваИдентификаторовДокументов", СвойстваИдентификаторовДокументов);
	
	СвойстваИдентификаторовЯщиков = Новый Массив;
	СвойстваИдентификаторовЯщиков.Добавить(Свойства_ИдентификаторЯщикаДокумента());
	СвойстваИдентификаторовЯщиков.Добавить(Свойства_ИдентификаторЯщикаДокументаСчет());
	СвойстваИдентификаторовЯщиков.Добавить(Свойства_ИдентификаторЯщикаСчета());
	СвойстваИдентификаторовЯщиков.Добавить(Свойства_ИдентификаторЯщикаДокументаЭТрН());
	СвойстваИдентификаторовЯщиков.Добавить(Свойства_ИдентификаторЯщикаДокументаЗЗ());
	
	Запрос.УстановитьПараметр("СвойстваИдентификаторовЯщика", СвойстваИдентификаторовЯщиков);
	
КонецПроцедуры

//}		ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ


///////////////////////////////////////////////////////////////////////////////
//{		СЛУЖЕБНЫЕ ФУНКЦИИ

Функция ИспользоватьПодсистемуДиадока() Экспорт
	
	Результат = Истина;
	
	ИспользоватьТиповуюПодсистему1С = ИспользоватьТиповуюПодсистему1С();
	
	Если ИспользоватьТиповуюПодсистему1С = Истина Тогда
	
		Результат = Ложь;
	
	КонецЕсли;
	
	
	Возврат Результат;
		
КонецФункции

Функция ИспользуетсяРасширениеПодсистемыХранения() Экспорт
	
	Результат = Ложь;
	
	Если ИспользоватьПодсистемуДиадока()
		И УстановленаПодсистемаДиадока()
		И НЕ ПриложениеСтаршеВерсии("8.3.8") Тогда
		
		РасширениеПодсистемыХранения = Метаданные.Справочники.Диадок_ПакетыДокументов.РасширениеКонфигурации();
		Результат = (РасширениеПодсистемыХранения <> Неопределено);
		
	КонецЕсли;
	
	Возврат Результат;	
	
КонецФункции

Функция ИспользуютсяСвязиТолькоИзТабличнойЧастиСправочникаЮрФизЛиц()
	
	ИмяКэша = "ХранениеСсылок_ИспользуютсяИдентификаторы";
	
	Результат = Кэш_Прочитать(ИмяКэша);
	
	Если Результат <> Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	ОбъектМетаданных = Метаданные.Справочники.Диадок_ЮрФизЛица;
	Результат = НЕ Общее_ЕстьРеквизитОбъекта("СвязанныйСправочник1", ОбъектМетаданных);
	
	Кэш_Поместить(ИмяКэша, Результат);			
				
	Возврат Результат;	
	
КонецФункции

Функция ИспользоватьТиповуюПодсистему1С()
	
	ИмяНастройки = Перечисление_ИменаСвойствНастроек_1С().ИспользоватьТиповуюПодсистему1С;
	
	Результат = Кэш_Прочитать(ИмяНастройки);
	
	Если Результат = Неопределено Тогда
		
		ОписаниеОшибкиИнициализации	= "";
		ОбъектОбщихНастроек			= ОбъектОбщихНастроек_1С(ОписаниеОшибкиИнициализации);
		ОшибкаИнициализации			= ЗначениеЗаполнено(ОписаниеОшибкиИнициализации);
		
		Если НЕ ОшибкаИнициализации Тогда
			
			Попытка
				Результат = Свойства_ЗначениеСвойства(ИмяНастройки, ОбъектОбщихНастроек);
			Исключение
				Ошибка						 = ИнформацияОбОшибке();
				ОписаниеОшибкиИнициализации	 = КраткоеПредставлениеОшибки(Ошибка);
				ОшибкаИнициализации			 = Истина;
			КонецПопытки;
			
		КонецЕсли;
		
		Если Результат = Ложь Тогда
			
			УстановленаПодсистемаДиадока = УстановленаПодсистемаДиадока();
			
			Если НЕ УстановленаПодсистемаДиадока Тогда
				Результат = Неопределено;
			КонецЕсли;
			
		КонецЕсли;
		
		Если ТипЗнч(Результат) <> Тип("Булево") Тогда
			
			УстановленаПодсистемаДиадока = УстановленаПодсистемаДиадока();
			УстановленРегистрДопСведений = УстановленРегистрДопСведений();
			
			МожноИспользоватьРегистрДопСведений = (УстановленРегистрДопСведений И НЕ ОшибкаИнициализации);
			
			Если УстановленаПодсистемаДиадока Тогда
				Результат = Ложь;
			ИначеЕсли МожноИспользоватьРегистрДопСведений Тогда
				Результат = Истина;
			Иначе
				
				Если ОшибкаИнициализации Тогда
					ТекстСообщения = ОписаниеОшибкиИнициализации;
				Иначе
					ТекстСообщения = " Не удалось определить систему хранения.";
				КонецЕсли;
				
				ТекстСообщения = "Ошибка инициализации модуля." + Символы.ПС + ТекстСообщения; 
				
				Если УстановленРегистрДопСведений И ОшибкаИнициализации Тогда
					// не найден ОбъектОбщихНастроек
					ВызватьИсключение ТекстСообщения;
				Иначе
					ЗаписьЖурналаРегистрации("Диадок.Определение системы хранения", УровеньЖурналаРегистрации.Ошибка, , , ТекстСообщения);
				КонецЕсли;
				
			КонецЕсли;
			
			Если МожноИспользоватьРегистрДопСведений Тогда
				ДопРеквизиты_УстановитьЗначение_1С(ОбъектОбщихНастроек, ИмяНастройки, Результат);
			КонецЕсли;
			
		КонецЕсли;
		
		Кэш_Поместить(ИмяНастройки, Результат);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ОбъектОбщихНастроек_1С(ОписаниеОшибки = "")
	
	ОписаниеРеквизита = Неопределено;
	
	Если Свойства_СправочникИспользуетсяДляХраненияСвойств("Валюты") Тогда
		
		ОписаниеРеквизита = "валюта с кодом 643";
		
		КодВалютыРубли = "643";
		Результат = Справочники.Валюты.НайтиПоКоду(КодВалютыРубли);
		
	ИначеЕсли Свойства_СправочникИспользуетсяДляХраненияСвойств("ПрофилиГруппДоступа") Тогда
		
		ОписаниеРеквизита = "профиль группы доступа Администратор";
		
		// Розница
		Результат = ПредопределенноеЗначение("Справочник.ПрофилиГруппДоступа.Администратор");	
		
	ИначеЕсли Свойства_СправочникИспользуетсяДляХраненияСвойств("Организации") Тогда
		
		ОписаниеРеквизита = "данные об организации";
		
		// BSLLS:SetPrivilegedMode-off
		УстановитьПривилегированныйРежим(Истина);
		// BSLLS:SetPrivilegedMode-on
		
		ВыборкаОрганизаций = Справочники.Организации.Выбрать();
		
		Пока ВыборкаОрганизаций.Следующий() Цикл
			Если НЕ ВыборкаОрганизаций.ПометкаУдаления Тогда
				Результат = ВыборкаОрганизаций.Ссылка;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если ПривилегированныйРежим() Тогда
			УстановитьПривилегированныйРежим(Ложь);
		КонецЕсли;
		
	Иначе
		Результат = Неопределено;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Результат) Тогда
		
		ОписаниеОшибки = "Не найден типовой реквизит";
		
		Если ОписаниеРеквизита <> Неопределено Тогда
			ОписаниеОшибки = ОписаниеОшибки + ": " + ОписаниеРеквизита;
		КонецЕсли;
		
		ЗаписьЖурналаРегистрации("Диадок", УровеньЖурналаРегистрации.Ошибка, , , ОписаниеОшибки);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоОбъект(Элемент)
	
	Результат = Ложь;
	
	Если Истина
		И Элемент <> Неопределено
		И XMLТипЗнч(Элемент) <> Неопределено Тогда
		
		ТипЗначенияСтрокой	= XMLТипЗнч(Элемент).ИмяТипа;
		Результат			= Найти(ТипЗначенияСтрокой, "Object.") > 0;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоСсылка(Элемент)
	
	Результат = Ложь;
	
	Если Истина
		И Элемент <> Неопределено
		И XMLТипЗнч(Элемент) <> Неопределено Тогда
		
		ТипЗначенияСтрокой	= XMLТипЗнч(Элемент).ИмяТипа;
		Результат			= Найти(ТипЗначенияСтрокой, "Ref.") > 0;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПрефиксНастройки_1С(СПрефиксом = Истина)
	
	Если СПрефиксом Тогда
		Результат = "DDPro_";
	Иначе
		Результат = "";
	КонецЕсли;	
	
	Возврат Результат;
	
КонецФункции

Функция Перечисление_ИмяСвойстваНастройки(ИмяСвойства) Экспорт
	
	Результат = Неопределено;
	
	Если ИспользоватьТиповуюПодсистему1С() Тогда
		ПеречислениеНастроек = Перечисление_ИменаСвойствНастроек_1С();
	Иначе
		ПеречислениеНастроек = Перечисление_ИменаСвойствНастроек_Диадок();
	КонецЕсли;
	
	ПеречислениеНастроек.Свойство(ИмяСвойства, Результат);
	
	Возврат Результат;
	
КонецФункции

Функция Перечисление_ИменаСвойствНастроек_1С() 
	
	Результат = Кэш_Прочитать("Перечисление_ИменаСвойствНастроек_1С");
	
	Если Результат = Неопределено Тогда
		
		Результат = Новый Структура;
		
		Результат.Вставить("IDОрганизации",							"ЯщикДиадокДляОрганизации");
		Результат.Вставить("IDОсновноеПодразделениеОрганизации",	"ПодрДиадокДляОрганизации");
		Результат.Вставить("IDПодразделенияОрганизации",			"IDПодразделенияОрганизации");
		Результат.Вставить("IDКонтрагента",							"ДДКонтр_");
		Результат.Вставить("IDОсновноеПодразделениеКонтрагента",	"ДДИДП_");
		Результат.Вставить("IDПодразделенияКонтрагента",			"IDПодразделенияКонтрагента");
		Результат.Вставить("ОтпечатокСертификатаНаСервере",			"ОтпечатокСертификатаНаСервере");
		Результат.Вставить("ШифроватьДокументы",					"ШифроватьДокументы");
		Результат.Вставить("УказыватьОтсутствиеОснованияУПД",		"ДиадокУказыватьОтсутствиеОснованияУПД");
		Результат.Вставить("ОтправлятьНепроведенныеДокументы",		"ДиадокОтправкаНепроведенных");
		Результат.Вставить("ЗаписыватьКомментарииВДокументы1С",		"ЗаписыватьКомментарииВДокументы1С");
		Результат.Вставить("ТекстКомментарияОтправка",				"ТекстКомментарияДиадок");
		Результат.Вставить("ТекстКомментарияПолучение",				"ТекстКомментарияПолучение");
		Результат.Вставить("ОсновнойПакет",							"ОсновнойПакет");
		Результат.Вставить("ВидПакета",								"ВидПакета");
		Результат.Вставить("ВидПакетаСчет",							"ВидПакетаСчет");
		Результат.Вставить("LetterID",								"LetterID");
		Результат.Вставить("ИдентификаторДокумента",				"ИдентификаторДокументаВДиадок");
		Результат.Вставить("ИдентификаторДокументаСчет",			"ИдентификаторДокументаВДиадокСчет");
		Результат.Вставить("ИдентификаторСчетаУФ",					"ИдентификаторДокументаВДиадокСчет");
		Результат.Вставить("ИдентификаторСчетаОФ",					"ИдентификаторДокументаВДиадокСч");
		Результат.Вставить("ИдентификаторЯщикаДокумента",			"ИдентификаторЯщикДокументаВДиадок");
		Результат.Вставить("ИдентификаторЯщикаДокументаСчет",		"ИдентификаторЯщикДокументаВДиадокСчет");
		Результат.Вставить("ИдентификаторЯщикаСчета",				"ИдентификаторЯщикДокументаВДиадокСч");
		Результат.Вставить("ДокументУчетаПакета",					"ДокументУчетаПакета");
		Результат.Вставить("ДокументУчетаПакетаСчет",				"ДокументУчетаПакетаСчет");
		Результат.Вставить("ПереотправляемыйПакет",					"ПереотправляемыйПакет");
		Результат.Вставить("ДиадокCustomDocumentId",				"ДиадокCustomDocumentId");
		Результат.Вставить("ДиадокCustomDocumentIdСчет",			"ДиадокCustomDocumentIdСчет");
		Результат.Вставить("ИспользоватьТиповуюПодсистему1С",		"ИспользоватьТиповуюПодсистему1С");
		Результат.Вставить("ДобавлятьСчетВПакет",					"ДобавлятьСчетВПакет");
		Результат.Вставить("ПередаватьМЧДФайлом",					"ПередаватьМЧДФайлом");
				
		Результат.Вставить("СтатусМЧД",								"СтатусМЧД");
		Результат.Вставить("СтатусМЧДКритичность",					"СтатусМЧДКритичность");
		Результат.Вставить("СтатусПКФНС",							"СтатусПКФНС");
		Результат.Вставить("СтатусПКФНСКритичность",				"СтатусПКФНСКритичность");
		
		Результат.Вставить("ОсновнойСвязанныйПакет",				"ОсновнойСвязанныйПакет");
		
		// Перевозочные документы
		Результат.Вставить("ВидПакетаЭТрН", "ВидПакетаЭТрН");
		Результат.Вставить("ВидПакетаЗЗ", "ВидПакетаЗЗ");
		Результат.Вставить("ИдентификаторЯщикаДокументаЭТрН", "ИдентификаторЯщикаДокументаЭТрН");
		Результат.Вставить("ИдентификаторЯщикаДокументаЗЗ", "ИдентификаторЯщикаДокументаЗЗ");
		Результат.Вставить("ИдентификаторДокументаЭТрН", "ИдентификаторДокументаЭТрН");
		Результат.Вставить("ИдентификаторДокументаЗЗ", "ИдентификаторДокументаЗЗ");
		Результат.Вставить("АвтоматическаяОтправкаПриглашений", "АвтоматическаяОтправкаПриглашений"); // Для организации
		
		Кэш_Поместить("Перечисление_ИменаСвойствНастроек_1С", Результат);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Перечисление_ИменаСвойствНастроек_Диадок()
	
	Результат = Новый Структура;
	
	Результат.Вставить("ШифроватьДокументы", "Шифровать документы");
	Результат.Вставить("ОсновнойПакет", "Основной пакет");
	
	Результат.Вставить("УказыватьОтсутствиеОснованияУПД", "УказыватьОтсутствиеОснованияУПД");
	Результат.Вставить("ОтправлятьНепроведенныеДокументы", "ОтправлятьНепроведенныеДокументы");
	Результат.Вставить("ЗаписыватьКомментарииВДокументы1С", "ЗаписыватьКомментарииВДокументы1С");
	Результат.Вставить("ТекстКомментарияОтправка", "ТекстКомментарияОтправка");
	Результат.Вставить("ТекстКомментарияПолучение", "ТекстКомментарияПолучение");
	Результат.Вставить("AfterIndexKey", "AfterIndexKey");
	Результат.Вставить("ДатаГраницыСинхронизации", "ДатаГраницыСинхронизации");
	Результат.Вставить("ОтпечатокСертификатаНаСервере", "ОтпечатокСертификатаНаСервере");
	Результат.Вставить("OrgId", "OrgId");
	Результат.Вставить("СвязанныйСправочник", "СвязанныйСправочник");
	Результат.Вставить("СтатусМЧД", "СтатусМЧД");
	Результат.Вставить("СтатусМЧДКритичность", "СтатусМЧДКритичность");
	Результат.Вставить("СтатусПКФНС", "СтатусПКФНС");
	Результат.Вставить("СтатусПКФНСКритичность", "СтатусПКФНСКритичность");
	Результат.Вставить("СтатусДТС", "СтатусДТС");
	Результат.Вставить("ОбъектМаршрутизации", "TargetResolution");
	Результат.Вставить("ДобавлятьСчетВПакет", "ДобавлятьСчетВПакет");
	Результат.Вставить("ПередаватьМЧДФайлом", "ПередаватьМЧДФайлом");
	Результат.Вставить("ДатаСинхронизацииСтатусовКонтрагентов", "ДатаСинхронизацииСтатусовКонтрагентов");
	Результат.Вставить("FromDepartmentId", "FromDepartmentId");
	Результат.Вставить("ToDepartmentId", "ToDepartmentId");
	Результат.Вставить("Промежуточный", "Промежуточный");
	
	Возврат Результат;
	
КонецФункции

Функция Перечисление_ИмяСвойствНастройки_ДобавлятьСчетВПакет() Экспорт 
	
	Результат = Перечисление_ИмяСвойстваНастройки("ДобавлятьСчетВПакет");
	
	Возврат Результат;
	
КонецФункции

Функция Перечисление_ИмяСвойствНастройки_ПередаватьМЧДФайлом() Экспорт 
	
	Результат = Перечисление_ИмяСвойстваНастройки("ПередаватьМЧДФайлом");
	
	Возврат Результат;
	
КонецФункции

Функция Ядро_ИдентификаторВнешнегоДО_ГИСМТ()
	
	Результат = Ядро.ИдентификаторВнешнегоДО_ГИСМТ();
	
	Возврат Результат;
	
КонецФункции

Функция Ядро_ИдентификаторВнешнегоДО_КонтурЛогистика()
	
	Результат = Ядро.ИдентификаторВнешнегоДО_КонтурЛогистика();
	
	Возврат Результат;
	
КонецФункции

Функция Ядро_ИдентификаторИдентификатора_КонтурЛогистика()
	
	Результат = Ядро.ИдентификаторИдентификатора_КонтурЛогистика();
	
	Возврат Результат;
	
КонецФункции

Функция ПривестиКоллекциюСопоставленныхКВариантуСХ1_1С(МассивСопоставленных)
	
	Результат = Новый Массив;
					
	ПорядковыйНомер = 0;
	
	Для Каждого ЭлементСопоставленных Из МассивСопоставленных Цикл
		
		ПорядковыйНомер = ПорядковыйНомер + 1;
		
		ЭлементРезультата = Новый Структура("Ссылка, НомерСвязи");
		
		ЭлементРезультата.Ссылка 		= ЭлементСопоставленных;
		ЭлементРезультата.НомерСвязи 	= ПорядковыйНомер;
		
		Результат.Добавить(ЭлементРезультата);
		
	КонецЦикла;
				
	Возврат Результат;
	
КонецФункции

Функция Перечисление_ИменаСправочников_Диадок()
	
	Результат = Ядро.Перечисление_ИменаСправочников_Диадок();
			
	Возврат Результат;

КонецФункции

Функция ИсключаемыеМетки_ОтправленныеДокументы()
	
	МассивМеток = Новый Массив;
	МассивМеток.Добавить(Метка_Переотправлен());
	МассивМеток.Добавить(Метка_Подготовлен());
	МассивМеток.Добавить(Метка_НеОтправлен());
	
	Возврат МассивМеток;
КонецФункции   

Функция Метка_Переотправлен()
	Возврат "Переотправлен";	
КонецФункции 

Функция Метка_Подготовлен()
	Возврат "Подготовлен";
КонецФункции

Функция Метка_Отправлен()
	Возврат "Отправлен";
КонецФункции

Функция Метка_НеОтправлен()
	Возврат "Не отправлен";	
КонецФункции

 //}	СЛУЖЕБНЫЕ ФУНКЦИИ


///////////////////////////////////////////////////////////////////////////////
//{		ОБЕРТКИ

Функция Кэш_Прочитать(ИмяЗначения)
	
	Если Ядро <> Неопределено Тогда
		Результат = Ядро.Кэш_Прочитать(ИмяЗначения);
	Иначе
		Результат = Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура Кэш_Поместить(ИмяЗначения, Значение)
	
	Если Ядро = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Ядро.Кэш_Поместить(ИмяЗначения, Значение);
	
КонецПроцедуры

Функция Контракт_ОбщиеНастройки()
	
	Результат = Новый Структура;
	
	// BSLLS:MagicNumber-off
	Результат.Вставить("ИспользоватьПодключаемыйМодуль",					Ложь);
	Результат.Вставить("МестонахождениеПодключаемогоМодуля",				"НеИспользовать");
	Результат.Вставить("ПодключаемыйМодульСсылка",							Неопределено);
	Результат.Вставить("ПодключаемыйМодульИмяОбработки",					"");
	Результат.Вставить("ПодключаемыйМодульПуть",							"");
	Результат.Вставить("СохраненнаяВерсияМодуля",							"0.0.0");
	Результат.Вставить("РежимПрокси",										"NoProxy");
	Результат.Вставить("АдресПроксиСервера",								"");
	Результат.Вставить("ПортПроксиСервера",									0);
	Результат.Вставить("ЛогинПроксиСервера",								"");
	Результат.Вставить("ПарольПроксиСервера",								"");
	Результат.Вставить("ИспользоватьТиповуюПодсистему1С",					Ложь);
	Результат.Вставить("СохранениеДокументовПуть",							"");
	Результат.Вставить("СохранениеДокументовСценарий",						"ВесьДокументооборот");
	Результат.Вставить("СохранениеДокументовГруппироватьПоПакетам",			Ложь);
	Результат.Вставить("СохранениеДокументовИмяКаталогаПакета",				Неопределено);
	Результат.Вставить("СохранениеДокументовИмяКаталогаПакетаПоУмолчанию",	Неопределено);
	Результат.Вставить("ЗагружатьДокументооборотЦеликом",					Ложь);
	Результат.Вставить("ХранитьДокументыНаДиске",							Ложь);
	Результат.Вставить("КорневойКаталогДокументов",							"");
	Результат.Вставить("СрокОчисткиДокументов",								0);
	Результат.Вставить("УдалятьСодержимоеУстаревшихДокументов",				Ложь);
	Результат.Вставить("ЗапретитьРаботуСМетриками",							Ложь);
	Результат.Вставить("РазрешитьДлительныеОперацииВФоне",					Ложь);
	Результат.Вставить("ИмяТиповогоМодуляИнтеграции",						"");
	Результат.Вставить("ЗагружатьИсходящиеДокументы",						Истина);
	Результат.Вставить("ЗагружатьВходящиеДокументы",						Истина);
	Результат.Вставить("ЗагружатьДокументыПоСопоставленнымКонтрагентам",	Ложь);
	Результат.Вставить("СлужебнаяУчетнаяЗаписьЛогин",						"");
	Результат.Вставить("СлужебнаяУчетнаяЗаписьПароль",						"");
	Результат.Вставить("ПутьКМодулюВСправочнике1С",							"");
	Результат.Вставить("ОтключитьПревьюНеформализованного",					Ложь);
	Результат.Вставить("ОтключитьКонтрольДублированияОтправкиПакетов",		Ложь);
	Результат.Вставить("СохраненнаяВерсияМодуляХранения",					"0.0.0");
	Результат.Вставить("РежимЛогирования",									Ложь);
	Результат.Вставить("КаталогЛогирования",								"");
	Результат.Вставить("РазворачиватьПакетыНаОтправкуПоДокументам",			Ложь);
	Результат.Вставить("ПоказыватьКоличествоДокументовВСписке",				Истина);
	Результат.Вставить("СоздаватьСчетаФактуры",								Истина);
	Результат.Вставить("СоздаватьПередаточныеДокументы",					Ложь);
	Результат.Вставить("УдалятьПриПереотправкеПредыдущийДокумент",			Ложь);
	Результат.Вставить("ОтключитьАнализДляВходящихДокументов",				Ложь);
	Результат.Вставить("ИдентификаторБазы1С",								"");
	Результат.Вставить("ТочностьЦены",										2);
	Результат.Вставить("ТочностьКоличества",								2);
	Результат.Вставить("ИспользоватьСервисКонтурМаркировка",				Ложь);
	Результат.Вставить("АвтоматическиОбновлятьМодуль",						Истина);
	Результат.Вставить("ИмяИнтеграционногоМодуля",							"");
	Результат.Вставить("НастройкиКонтурПлагины",							"");
	Результат.Вставить("ИспользоватьПлагины",								Ложь);
	Результат.Вставить("РазрешитьПодписаниеБезОтправки",					Ложь);
	Результат.Вставить("ИспользоватьСопоставлениеПодразделений",			Ложь);
	Результат.Вставить("ОрганизацияИздателяТестовогоСертификата",			"");
	Результат.Вставить("ЗапретитьВыгрузкуПМвСервисДиадок",					Ложь);
	Результат.Вставить("ЗапретитьОбновлениеПМизСервисаДиадок",				Ложь);
	Результат.Вставить("ИспользоватьВнешнийHTTP",							Ложь);
	Результат.Вставить("СозданиеДокументов_ИспользоватьДатуЭД",				Ложь);
	Результат.Вставить("РежимПроверкиСопоставленияНоменклатуры",		"БлокироватьСозданиеДокумента");
	Результат.Вставить("РежимСовместимостиФорматов820и970",					Неопределено);
	Результат.Вставить("ОблачныеНастройкиКлюч",								Неопределено);
	Результат.Вставить("ИспользоватьВнутреннийДокументооборот",				Ложь);
	Результат.Вставить("УведомлятьОНовыхСобытиях",						Истина);
	Результат.Вставить("ТранспортHTTP",										"Встроенный");
	Результат.Вставить("ФормироватьКвитанцииОбОтменеФиксации",		"default");
	Результат.Вставить("КонвертироватьСтавкиНДС", Ложь);

	Результат.Вставить("ПодписаниеГосключ",									Ложь);
	Результат.Вставить("ПодписаниеСертификат",								Истина);
	
	Результат.Вставить("ОтправкаПриглашенийАвтоматическаяОтправка", 		Ложь);
	Результат.Вставить("ОтправкаПриглашенийРежимОтправки", 					Неопределено);
	
	Результат.Вставить("ЭтоПервыйЗапускМодуля", Ложь);
	// BSLLS:MagicNumber-on
	
	Возврат Результат;
	
КонецФункции

Функция Контракт_НастройкиМодуляСтандарт()
	
	Результат = Ядро.Контракт_НастройкиМодуляСтандарт();
	
	Возврат Результат;
	
КонецФункции

Функция Контракт_ОрганизацияЭДО()
	
	Результат = Ядро.Контракт_ОрганизацияЭДО();
	
	Возврат Результат;
	
КонецФункции

Функция Контракт_КонтрагентЭДО()
	
	Результат = Ядро.Контракт_КонтрагентЭДО();
	
	Возврат Результат;
	
КонецФункции

Функция Контракт_Подразделение()
	
	Результат = Ядро.Контракт_ПодразделениеЭДО();
	
	Возврат Результат;
	
КонецФункции

Функция Контракт_Пакет()
	
	Результат = Ядро.Контракт_Пакет();
	
	Возврат Результат;
	
КонецФункции

Функция Контракт_Документ()
	
	Результат = Ядро.Контракт_Документ();
	
	Возврат Результат;
	
КонецФункции

Функция Контракт_ПараметрыОтправкиДокумента()
	
	Результат = Ядро.Контракт_ПараметрыОтправкиДокумента();
	Возврат Результат;
	
КонецФункции

Функция Контракт_ПараметрыОтправкиПакета()
	
	Результат = Ядро.Контракт_ПараметрыОтправкиПакета();
	
	Возврат Результат;
	
КонецФункции

Функция Контракт_ДанныеХранения()
	
	Результат = Ядро.Контракт_ДанныеХранения();
	
	Возврат Результат;
	
КонецФункции

Функция Контракт_Подпись()
	
	Результат = Ядро.Контракт_Подпись();
	
	Возврат Результат;
	
КонецФункции

Функция Контракт_УчастникДокументооборота()
	
	Результат = Ядро.Контракт_УчастникДокументооборота();
	
	Возврат Результат;
	
КонецФункции

Функция Контракт_РеквизитыКонтрагентов1С()
	
	Результат = Новый Структура;
	Результат.Вставить("Ссылка"			, Неопределено);
	Результат.Вставить("Наименование"	, "");
	Результат.Вставить("ИНН"			, "");
	Результат.Вставить("КПП"			, "");
	
	Возврат Результат;
	
КонецФункции

Функция Контракт_ОбъектМаршрутизации()
	
	Результат = Ядро.Контракт_ОбъектМаршрутизации();
	
	Возврат Результат;
	
КонецФункции

Функция Списки_ОписаниеТипаДокументаAPI(ТипДокумента, ТипКонтента, ФункцияДокумента)
	
	Результат = Ядро.Списки_ОписаниеТипаДокументаAPI(ТипДокумента, ТипКонтента, ФункцияДокумента);
		
	Возврат Результат;
	
КонецФункции

Функция Списки_ОписаниеФорматаКонтента(ТипКонтента)
	
	Результат = Ядро.Списки_ОписаниеФорматаКонтента(ТипКонтента);
		
	Возврат Результат;
	
КонецФункции

Функция Перечисление_Направления()
	
	Результат = Ядро.Перечисление_Направления();
		
	Возврат Результат;
	
КонецФункции

Функция ПустойИдентификатор()
	Возврат "00000000-0000-0000-0000-000000000000";
КонецФункции

Функция Документы_ФункцияДокументаДляХранения(ФункцияДокумента)
	
	Результат = Ядро.Документы_ФункцияДокументаДляХранения(ФункцияДокумента);
		
	Возврат Результат;
	
КонецФункции

Функция Документы_ФункцияДокументаДляXML(ТипДокумента, ФункцияДокумента)
	
	Результат = Ядро.Документы_ФункцияДокументаДляXML(ТипДокумента, ФункцияДокумента);
		
	Возврат Результат;
	
КонецФункции

Функция Документы_КонтрактПоДокументуАПИ(BoxID, ДокументAPI)

	Результат = Ядро.Документы_КонтрактПоДокументуАПИ(BoxId, ДокументAPI);
	
	Возврат Результат;

КонецФункции

Функция API_GetDocument(BoxId, LetterId, DocumentId) Экспорт
	
	ПараметрыСервиса = Ядро.ПараметрыСервиса_Диадок(BoxId);
	Результат = Ядро.АПИ_GetDocument(ПараметрыСервиса, BoxId, LetterId, DocumentId);
	
	Возврат Результат;
	
КонецФункции

Функция Документы_ДанныхХранения_АПИ(Идентификаторы, ИмяХранилища)

	Результат = Ядро.Документы_ДанныхХранения_АПИ(Идентификаторы, ИмяХранилища);
	
	Возврат Результат;

КонецФункции

Функция Документы_ДанныеПодписи_АПИ(Идентификаторы, ИмяХранилища)

	Результат = Ядро.Документы_ДанныеПодписи_АПИ(Идентификаторы, ИмяХранилища);
	
	Возврат Результат;

КонецФункции

Функция Документы_ДвоичныеДанныеEntity(BoxId, LetterId, EntityId)
	
	Результат = Ядро.Документы_ДвоичныеДанныеEntity(BoxId, LetterId, EntityId);
	
	Возврат Результат;
	
КонецФункции

Функция ПриложениеСтаршеВерсии(Версия)
	
	СистемнаяИнформация = Новый СистемнаяИнформация;
	РезультатСравнения = СтдСтр().СравнитьВерсииЛок(СистемнаяИнформация.ВерсияПриложения, Версия);
	Результат = РезультатСравнения < 0;
	
	Возврат Результат;
	
КонецФункции

Функция ПодключаемыйМодуль_ОбработатьСобытие(ИмяСобытия, Параметры = Неопределено)
	
	Результат = Ядро.ПодключаемыйМодуль_ОбработатьСобытие(ИмяСобытия, Параметры);
	
	Возврат Результат;
	
КонецФункции	

Функция СписокДокументов_ИдентификаторыОтправленныхДокументовАПИ(Параметры)
	
	Результат = Ядро.СписокДокументов_ИдентификаторыДокументовАПИ(Параметры, Перечисление_Направления().Исходящее);
	
	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_ИдентификаторыПолученныхДокументовАПИ(Параметры)
	
	Результат = Ядро.СписокДокументов_ИдентификаторыДокументовАПИ(Параметры, Перечисление_Направления().Входящее);
	
	Возврат Результат;
	
КонецФункции

Функция СписокДокументов_ИдентификаторыВнутреннихДокументовАПИ(Параметры)
	
	Результат = Ядро.СписокДокументов_ИдентификаторыДокументовАПИ(Параметры, Перечисление_Направления().Внутреннее);
	
	Возврат Результат;
	
КонецФункции

Функция Новый_КлючИдентификатораДокумента(LetterId, DocumentId)
	
	Результат = Ядро.Новый_КлючИдентификатораДокумента(LetterId, DocumentId);
	
	Возврат Результат;
	
КонецФункции

Функция Новый_СписокДокументов()
	
	Результат = Ядро.Новый_СписокДокументов();
	
	Возврат Результат;
	
КонецФункции

Функция Новый_СписокДокументовИзТабличнойЧасти(ТабличнаяЧасть) Экспорт
	
	Результат = ТабличнаяЧасть.ВыгрузитьКолонки();
	
	Если ИспользуетсяРасширениеПодсистемыХранения() Тогда
	
		Результат.Колонки.Удалить("ДокументЭДО");
		Результат.Колонки.Удалить("Пакет");
		
		Результат.Колонки.Добавить("ДокументЭДО", Новый ОписаниеТипов("СправочникСсылка.Диадок_Документы"));
		Результат.Колонки.Добавить("Пакет", Новый ОписаниеТипов("СправочникСсылка.Диадок_ПакетыДокументов"));
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ОбщиеНастройки_Прочитать(ИмяНастройки)
	
	Результат = Ядро.ОбщиеНастройки_Прочитать(ИмяНастройки);
	
	Возврат Результат;
	
КонецФункции	

Функция ТипЗначенияОбъекта(Синоним, Формат = "Полный")
	
	Результат = Ядро.Справочники_ТипЗначенияОбъекта(Синоним, Формат);
	
	Если Не ЗначениеЗаполнено(Результат) Тогда
		
		Ошибка = СтрШаблонЛок(
			"Не удалось определить тип справочника информационной базы:
			|Тип сущности = %1
			|Формат имени = %2",
			Синоним,
			Формат
		);
		
		ВызватьИсключение Ошибка;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция МетаданныеСправочникаОрганизации()
	
	ИмяОбъектаМД = ТипЗначенияОбъекта("Организации", "Краткий");
	ОбъектМД = Метаданные.Справочники.Найти(ИмяОбъектаМД);
	
	Если ОбъектМД = Неопределено Тогда
		Ошибка = СтрШаблонЛок("Не найдено хранилище сущностей 'Организации' (имя справочника '%1')", ИмяОбъектаМД);
		ВызватьИсключение Ошибка;
	КонецЕсли;
	
	Возврат ОбъектМД;
	
КонецФункции

Функция МетаданныеСправочникаКонтрагенты()
	
	ИмяОбъектаМД = ТипЗначенияОбъекта("Контрагенты", "Краткий");
	ОбъектМД = Метаданные.Справочники.Найти(ИмяОбъектаМД);
	
	Если ОбъектМД = Неопределено Тогда
		Ошибка = СтрШаблонЛок("Не найдено хранилище сущностей 'Контрагенты' (имя справочника '%1')", ИмяОбъектаМД);
		ВызватьИсключение Ошибка;
	КонецЕсли;
	
	Возврат ОбъектМД;
	
КонецФункции

Функция КонтекстСеанса_СтрокаКонтекста(BoxId, ВызыватьИсключение = Истина) 
	
	Результат = Ядро.КонтекстСеанса_СтрокаКонтекста(BoxId, ВызыватьИсключение);
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_МассивCounteragents(Организация, CounteragentStatus = Неопределено)
	
	Результат = Ядро.Контрагенты_МассивCounteragents(Организация, CounteragentStatus);
	
	Возврат Результат;
	
КонецФункции	

Функция Контрагенты_ТаблицаКонтрактовПоCounteragents(МассивCounteragents)
	
	Результат = Ядро.Контрагенты_ТаблицаКонтрактовПоCounteragents(МассивCounteragents);
	
	Возврат Результат;
	
КонецФункции	

Функция Контрагенты_РеквизитыКонтрагента_АПИ(BoxId)
	
	Результат = Ядро.Контрагенты_РеквизитыКонтрагента_АПИ(BoxId);
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_РеквизитыКонтрагентаПоФНС_АПИ(ИдентификаторФНС)
	
	Результат = Ядро.Контрагенты_РеквизитыКонтрагентаПоФНС_АПИ(ИдентификаторФНС);
	
	Возврат Результат;
	
КонецФункции

Функция Новый_ИдентификаторыСправочника(Ссылка = Неопределено, ID = "")
	
	Результат = Ядро.Новый_ИдентификаторыСправочника(Ссылка, ID);
	
	Возврат Результат;
	
КонецФункции

Функция Контрагенты_ПоискИПриглашениеДляОрганизации(Организация)
	
	Результат = Ядро.Контрагенты_ПоискИПриглашениеДляОрганизации(Организация);
	
	Возврат Результат;
	
КонецФункции

Функция Подразделения_ТаблицаКонтрактовВладельца(ДанныеВладельца, ДляОрганизации)
	
	Результат = Ядро.Подразделения_ТаблицаКонтрактовВладельца(ДанныеВладельца, ДляОрганизации);
	
	Возврат Результат;
	
КонецФункции

Функция Подразделения_РеквизитыПодразделения_АПИ(Идентификаторы, ДанныеВладельца, ДляОрганизации)
	
	Результат = Ядро.Подразделения_РеквизитыПодразделения_АПИ(Идентификаторы, ДанныеВладельца, ДляОрганизации);
	
	Возврат Результат;
	
КонецФункции

Функция Перечисление_СтатусыКонтрагентов()
	
	Результат = Ядро.Перечисление_СтатусыКонтрагентов();
	
	Возврат Результат;
	
КонецФункции

Функция ТекущийРежимЗапускаПриложения()
	
	ТекущийРежим = ТекущийРежимЗапуска();
	Если ТекущийРежим = РежимЗапускаКлиентскогоПриложения.ОбычноеПриложение Тогда
		Результат = "Обычное приложение";
	ИначеЕсли ТекущийРежим = РежимЗапускаКлиентскогоПриложения.УправляемоеПриложение Тогда
		Результат = "Управляемое приложение";
	Иначе
		Результат = "";
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Результат) Тогда
		Результат = "Фоновое задание";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СопоставлениеДанныхПМ_ТаблицаВидовСправочника(ИмяСправочника)
	
	Результат = Ядро.СопоставлениеДанныхПМ_ТаблицаВидовСправочника(ИмяСправочника);
	
	Возврат Результат;
	
КонецФункции

Функция СопоставлениеДанныхПМ_ТаблицаВидовГруппировок()
	
	Результат = Ядро.СопоставлениеДанныхПМ_ТаблицаВидовГруппировок();
	
	Возврат Результат;
	
КонецФункции

Функция ТаблицаЗначенийВМассивСтруктур(ТаблЗнач)
	
	Результат = Ядро.ТаблицаЗначенийВМассивСтруктур(ТаблЗнач);
	
	Возврат Результат;
	
КонецФункции

Функция Пакеты_ДокументыПакетаАПИ(Идентификаторы)
	
	Результат = Ядро.Пакеты_ДокументыПакетаАПИ(Идентификаторы);
	
	Возврат Результат;
	
КонецФункции	

Функция Перечисление_СтатусыПакетов()
	
	Результат = Ядро.Перечисление_СтатусыПакетов();
	
	Возврат Результат;
	
КонецФункции

Функция Организации_ОрганизацииЭДОИзКонтекста()
	
	Результат = Ядро.Организации_ОрганизацииЭДОИзКонтекста();
	
	Возврат Результат;
	
КонецФункции

Функция ПредопределенныеСписки_ТипыДокументовAPI()
	
	Результат = Ядро.ПредопределенныеСписки_ТипыДокументовAPI();
	
	Возврат Результат;
	
КонецФункции

Функция Списки_ОписаниеВидаПакета(ID)
	
	Результат = Ядро.Списки_ОписаниеВидаПакета(ID);
	
	Возврат Результат;
	
КонецФункции

Функция Списки_ОписаниеВидаДокумента(ID)
	
	Результат = Ядро.Списки_ОписаниеВидаДокумента(ID);
	
	Возврат Результат;
	
КонецФункции

Функция ХранениеДанных_ДанныеОрганизации(Идентификаторы)
	
	Результат = Ядро.ХранениеДанных_ДанныеОрганизации(Идентификаторы);
	
	Возврат Результат;
	
КонецФункции

//}		ОБЕРТКИ


//**********************************************
//{		ОБЩЕГО НАЗНАЧЕНИЯ

Функция СвойствоСтруктуры(Структура, Ключ, ЗначениеПоУмолчанию = Неопределено)
	
	Результат = Ядро.СвойствоСтруктуры(Структура, Ключ, ЗначениеПоУмолчанию);
	
	Возврат Результат;
	
КонецФункции

Функция СоединитьСтроку(Строки, Разделитель = "")
	
	Результат = Ядро.СоединитьСтроку(Строки, Разделитель);
	
	Возврат Результат;
	
КонецФункции	

Функция РазделитьСтрокуЛок(Знач Строка, Разделитель = ",")
	
	Результат = СтдСтр().СтрРазделитьЛок(Строка, Разделитель);
	
	Возврат Результат;
	
КонецФункции

Функция СтрНайтиЛок(Строка, ПодстрокаПоиска)
	
	// BSLLS:DeprecatedFind-off
	Результат = Найти(Строка, ПодстрокаПоиска);
	// BSLLS:DeprecatedFind-on
	
	Возврат Результат;
	
КонецФункции

Функция ИдентификаторВАдресЯщика(Идентификатор)
	
	Результат = Ядро.ИдентификаторВАдресЯщика(Идентификатор);
	
	Возврат Результат;
	
КонецФункции	

Функция АдресЯщикаВИдентификатор(Идентификатор)
	
	Результат = Ядро.АдресЯщикаВИдентификатор(Идентификатор);
	
	Возврат Результат;
	
КонецФункции	

Функция ПроверитьНаличиеОбъектаМетаданных(СтрокаМетаданных)
	
	Результат = Истина;
	
	ТекПолеМетаданных = Метаданные;
	МассивИмен = РазделитьСтрокуЛок(СтрокаМетаданных, ".");
	КоличествоИмен = МассивИмен.Количество();
	
	// BSLLS:MagicNumber-off
	Если НЕ (КоличествоИмен = 2 Или КоличествоИмен = 4 Или КоличествоИмен = 6) Тогда
		Результат = Ложь;
	Иначе
		
		КоличествоПоисков = КоличествоИмен / 2;
		
		Для н = 1 По КоличествоПоисков Цикл
			
			ТекПолеМетаданных = ТекПолеМетаданных[МассивИмен[н * 2 - 2]].Найти(МассивИмен[н * 2 - 1]);
			
			Если ТекПолеМетаданных = Неопределено Тогда
				Результат = Ложь;
				Прервать;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	// BSLLS:MagicNumber-on
	
	Возврат Результат;
	
КонецФункции

Процедура ПроверитьДоступностьОбъектаМетаданных(СписокОшибок, ПолноеИмя
	, ПравоЧтение		 = Истина
	, ПравоДобавление	 = Истина
	, ПравоИзменение	 = Истина
	, ПравоУдаление		 = Ложь)
	
	Ядро.ПроверитьДоступностьОбъектаМетаданных(СписокОшибок, ПолноеИмя
	, ПравоЧтение
	, ПравоДобавление
	, ПравоИзменение
	, ПравоУдаление);
	
КонецПроцедуры

Процедура Ошибка_Обработать(ВидОперации
	, ПодробныйТекстОшибки
	, ДопИнформация		 = Неопределено
	, ТекстСообщения	 = Неопределено
	, ИдентификаторФормы = Неопределено
	, ПолеФормы			 = Неопределено)
	
	Ядро.Ошибка_Обработать(ВидОперации, ПодробныйТекстОшибки, ДопИнформация, ТекстСообщения, ИдентификаторФормы, ПолеФормы);
	
КонецПроцедуры

// BSLLS:NumberOfOptionalParams-off

// Подставляет параметры в строку по номеру
//
// Параметры:
//	ШаблонСтроки - Строка - Строка, содержащая маркеры подстановки вида: "%1..%N".
//					Нумерация маркеров начинается с 1. N не может быть больше 5.
//  Параметр1 - Строка - значение подставляемого параметра.
//  Параметр2 - Строка
//  Параметр3 - Строка
//  Параметр4 - Строка
//  Параметр5 - Строка
//
// Возвращаемое значение:
//  Строка - текстовая строка с подставленными параметрами.
//
Функция СтрШаблонЛок(ШаблонСтроки, Параметр1
	, Параметр2 = Неопределено
	, Параметр3 = Неопределено
	, Параметр4 = Неопределено
	, Параметр5 = Неопределено)
	
	Возврат Ядро.Общее_ПодставитьПараметрыВСтроку(ШаблонСтроки, Параметр1, Параметр2, Параметр3, Параметр4, Параметр5);
	
КонецФункции
// BSLLS:NumberOfOptionalParams-on

Процедура ДобавитьКолонкиВТаблицуЗначений(Таблица, Колонки, ОписаниеТипов = Неопределено)
	
	Ядро.ДобавитьКолонкиВТаблицуЗначений(Таблица, Колонки, ОписаниеТипов);
	
КонецПроцедуры

Функция ШаблонПараметраЗапросаИмяРеквизитаСвязанныйСправочник()
	
	ИмяСвойстваСвязанныйСправочник = Перечисление_ИменаСвойствНастроек_Диадок().СвязанныйСправочник;
	
	Результат = ИмяСвойстваСвязанныйСправочник + "%[0-9]";
	
	Возврат Результат;
	
КонецФункции

Процедура Общее_УдалитьПустыеЭлементыМассива(Коллекция)
	
	Для ИндексЭлемента = -Коллекция.ВГраница() По 0 Цикл
		
		Если Не ЗначениеЗаполнено(Коллекция[-ИндексЭлемента]) Тогда 
			Коллекция.Удалить(-ИндексЭлемента);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

// Возвращает признак того, что реквизит входит в подмножество стандартных реквизитов.
//
// Параметры:
//  ИмяРеквизита         - Строка - реквизит, который необходимо проверить на принадлежность множеству стандартных
//                                  реквизитов.
//  МетаданныеОбъекта - ОбъектМетаданных - объект, в котором требуется проверить наличие стандартного реквизита.
//
// Возвращаемое значение:
//   Булево - Истина, если реквизит входит в подмножество стандартных реквизитов.
//
Функция Общее_ЕстьСтандартныйРеквизитОбъекта(ИмяРеквизита, МетаданныеОбъекта)
	
	СтандартныеРеквизиты = МетаданныеОбъекта.СтандартныеРеквизиты;
	Для Каждого Реквизит Из СтандартныеРеквизиты Цикл
		Если Реквизит.Имя = ИмяРеквизита Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
	Возврат Ложь;
	
КонецФункции

// Позволяет определить, есть ли среди реквизитов объекта реквизит с переданным именем.
//
// Параметры:
//  ИмяРеквизита - Строка - имя реквизита;
//  МетаданныеОбъекта - ОбъектМетаданных - объект, в котором требуется проверить наличие реквизита.
//
// Возвращаемое значение:
//  Булево - Истина, если есть.
//
Функция Общее_ЕстьРеквизитОбъекта(ИмяРеквизита, МетаданныеОбъекта)
	
	Реквизиты = МетаданныеОбъекта.Реквизиты; // КоллекцияОбъектовМетаданных
	Возврат НЕ (Реквизиты.Найти(ИмяРеквизита) = Неопределено);
	
КонецФункции

Функция СтдСтр()
	
	Если СтдСтрЛок = Неопределено Тогда
		Если _ЭтоВнешняяОбработка() Тогда
			СтдСтрЛок = Новый("ExternalDataProcessorObject.КонтурДиадокСтдСтр");
		Иначе
			СтдСтрЛок = Новый("DataProcessorObject.КонтурДиадокСтдСтр");
		КонецЕсли;
	КонецЕсли;
	
	Возврат СтдСтрЛок;
	
КонецФункции

Функция _ЭтоВнешняяОбработка()
	
	Если ЭтоВнешняяОбработкаКэш = Неопределено Тогда
		ПараметрыПроверки = Новый Структура;
		ПараметрыПроверки.Вставить("ИспользуемоеИмяФайла");
		ЗаполнитьЗначенияСвойств(ПараметрыПроверки, ЭтотОбъект);
		Если ПараметрыПроверки.ИспользуемоеИмяФайла = Неопределено Тогда
			ЭтоВнешняяОбработкаКэш = Ложь;
		Иначе
			ЭтоВнешняяОбработкаКэш = Истина;
		КонецЕсли;
	КонецЕсли;
	
	Возврат ЭтоВнешняяОбработкаКэш;
	
КонецФункции

//}		ОБЩЕГО НАЗНАЧЕНИЯ


//{		КОНВЕРТАЦИЯ BASE64

Функция GUID_2_Base64(GUID) 
		
	Число16	= Число16_Из_GUID(GUID);
	Base64	= Base64_Из_Числа16(Число16);
	
	Результат = Прав(Base64, Окр(СтрДлина(Число16) / 1.5));
	
	Возврат Результат;
		
КонецФункции

Функция Число16_Из_GUID(GUID)
	
	Результат = СтрЗаменить(GUID, "-", ""); 
	
	Возврат Результат;
	
КонецФункции

Функция Base64_Из_Числа16(Число)
	
	Результат = "";

	ТаблицаПерекодировки = ТаблицаПерекодировки();
	
	Число16 = Число;
	
	Пока СтрДлина(Число16) > 0 Цикл 
		
		Разряд = ДобавитьНулиВПредставлениеЧисла(Прав(Число16, 3), 3);
		
		СтруктураПоиска = Новый Структура;
		СтруктураПоиска.Вставить("Число16", Разряд);
		
		ВыборкаСтрок = ТаблицаПерекодировки.НайтиСтроки(СтруктураПоиска);
		
		Если ВыборкаСтрок.Количество() > 0 Тогда 
			Результат = ВыборкаСтрок[0].Число64 + Результат;
		КонецЕсли;
		
		ДлинаСтроки16	= СтрДлина(Число16);
		Число16			= ?(ДлинаСтроки16 >= 4, Лев(Число16, ДлинаСтроки16 - 3), "");
		
	КонецЦикла;	
	
	Возврат Результат;
	
КонецФункции 

Функция ТаблицаПерекодировки() Экспорт
		
	//Формируем таблицу соответствия 3-хсимвольных 16-ричных слов и 2-х символьных 64-ричных слов
	//Готовую сформированную таблицу берем из подготовленного макета
	
	Результат = Кэш_Прочитать("ТаблицаПерекодировки");
	
	Если Результат = Неопределено Тогда
		
		МассивТипов = Новый Массив;
		МассивТипов.Добавить(Тип("Строка"));
		
		Макет		= ПолучитьМакет("ТаблицаПерекодировки");
		ОбластьТела	= Макет.ПолучитьОбласть("ТелоМакета");
		
		ТабДок = Новый ТабличныйДокумент;
		ТабДок.Вывести(ОбластьТела);
		
		Результат = Новый ТаблицаЗначений;
		Результат.Колонки.Добавить("Число2",	Новый ОписаниеТипов(МассивТипов));
		Результат.Колонки.Добавить("Число16",	Новый ОписаниеТипов(МассивТипов));
		Результат.Колонки.Добавить("Число64",	Новый ОписаниеТипов(МассивТипов));
		
		Результат.Индексы.Добавить("Число16");
		
		Для НомерСтроки = 1 По 4096 Цикл
			
			СтрокаТЗ = Результат.Добавить();
			СтрокаТЗ.Число2		= ТабДок.Область(НомерСтроки, 1).Текст;
			СтрокаТЗ.Число16	= ТабДок.Область(НомерСтроки, 2).Текст;
			СтрокаТЗ.Число64	= ТабДок.Область(НомерСтроки, 3).Текст;
			
		КонецЦикла;
		
		Кэш_Поместить("ТаблицаПерекодировки", Результат);
		
	КонецЕсли;
	
	Возврат Результат;
		
КонецФункции	
	
Функция ДобавитьНулиВПредставлениеЧисла(ЧС, Длина)
		
	Результат = Формат(ЧС, "ЧН=; ЧГ=");
	
	Пока СтрДлина(Результат) < Длина Цикл 
		Результат = "0" + Результат;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции


Функция Base64_2_GUID(Base64)
	
	Число2	= Число2_Из_Base64(Base64);
	Число16 = Число16_Из_Числа2(Число2);
	
	Если СтрДлина(Число16) > 64 Тогда 
		Число16 = Прав(Число16, 64);
	КонецЕсли;
	
	Результат = GUID_Из_Числа16(Число16);
	
	Возврат Результат; 
		
КонецФункции

Функция Число2_Из_Base64(Base64)
	
	Результат = "";
	
	Число64 = Формат(Base64, "ЧН=;  ЧГ=");
	
	Для ц = 1 по СтрДлина(Число64) Цикл 
		
		Символ64 = Сред(Число64, ц, 1);
		
		Если Символ64 = "A" Тогда 
			Символ2 = "000000";
		ИначеЕсли Символ64 = "B" Тогда 
			Символ2 = "000001";
		ИначеЕсли Символ64 = "C" Тогда 
			Символ2 = "000010";
		ИначеЕсли Символ64 = "D" Тогда 
			Символ2 = "000011";
		ИначеЕсли Символ64 = "E" Тогда 
			Символ2 = "000100";
		ИначеЕсли Символ64 = "F" Тогда 
			Символ2 = "000101";
		ИначеЕсли Символ64 = "G" Тогда 
			Символ2 = "000110";
		ИначеЕсли Символ64 = "H" Тогда 
			Символ2 = "000111";
		ИначеЕсли Символ64 = "I" Тогда 
			Символ2 = "001000";
		ИначеЕсли Символ64 = "J" Тогда 
			Символ2 = "001001";
		ИначеЕсли Символ64 = "K" Тогда 
			Символ2 = "001010";
		ИначеЕсли Символ64 = "L" Тогда 
			Символ2 = "001011";
		ИначеЕсли Символ64 = "M" Тогда 
			Символ2 = "001100";
		ИначеЕсли Символ64 = "N" Тогда 
			Символ2 = "001101";
		ИначеЕсли Символ64 = "O" Тогда 
			Символ2 = "001110";
		ИначеЕсли Символ64 = "P" Тогда 
			Символ2 = "001111";
		ИначеЕсли Символ64 = "Q" Тогда 
			Символ2 = "010000";
		ИначеЕсли Символ64 = "R" Тогда 
			Символ2 = "010001";
		ИначеЕсли Символ64 = "S" Тогда 
			Символ2 = "010010";
		ИначеЕсли Символ64 = "T" Тогда 
			Символ2 = "010011";
		ИначеЕсли Символ64 = "U" Тогда 
			Символ2 = "010100";
		ИначеЕсли Символ64 = "V" Тогда 
			Символ2 = "010101";
		ИначеЕсли Символ64 = "W" Тогда 
			Символ2 = "010110";
		ИначеЕсли Символ64 = "X" Тогда 
			Символ2 = "010111";
		ИначеЕсли Символ64 = "Y" Тогда 
			Символ2 = "011000";
		ИначеЕсли Символ64 = "Z" Тогда 
			Символ2 = "011001";
		ИначеЕсли Символ64 = "a" Тогда 
			Символ2 = "011010";
		ИначеЕсли Символ64 = "b" Тогда 
			Символ2 = "011011";
		ИначеЕсли Символ64 = "c" Тогда 
			Символ2 = "011100";
		ИначеЕсли Символ64 = "d" Тогда 
			Символ2 = "011101";
		ИначеЕсли Символ64 = "e" Тогда 
			Символ2 = "011110";
		ИначеЕсли Символ64 = "f" Тогда 
			Символ2 = "011111";
		ИначеЕсли Символ64 = "g" Тогда 
			Символ2 = "100000";
		ИначеЕсли Символ64 = "h" Тогда 
			Символ2 = "100001";
		ИначеЕсли Символ64 = "i" Тогда 
			Символ2 = "100010";
		ИначеЕсли Символ64 = "j" Тогда 
			Символ2 = "100011";
		ИначеЕсли Символ64 = "k" Тогда 
			Символ2 = "100100";
		ИначеЕсли Символ64 = "l" Тогда 
			Символ2 = "100101";
		ИначеЕсли Символ64 = "m" Тогда 
			Символ2 = "100110";
		ИначеЕсли Символ64 = "n" Тогда 
			Символ2 = "100111";
		ИначеЕсли Символ64 = "o" Тогда 
			Символ2 = "101000";
		ИначеЕсли Символ64 = "p" Тогда 
			Символ2 = "101001";
		ИначеЕсли Символ64 = "q" Тогда 
			Символ2 = "101010";
		ИначеЕсли Символ64 = "r" Тогда 
			Символ2 = "101011";
		ИначеЕсли Символ64 = "s" Тогда 
			Символ2 = "101100";
		ИначеЕсли Символ64 = "t" Тогда 
			Символ2 = "101101";
		ИначеЕсли Символ64 = "u" Тогда 
			Символ2 = "101110";
		ИначеЕсли Символ64 = "v" Тогда 
			Символ2 = "101111";
		ИначеЕсли Символ64 = "w" Тогда 
			Символ2 = "110000";
		ИначеЕсли Символ64 = "x" Тогда 
			Символ2 = "110001";
		ИначеЕсли Символ64 = "y" Тогда 
			Символ2 = "110010";
		ИначеЕсли Символ64 = "z" Тогда 
			Символ2 = "110011"
			
		ИначеЕсли Символ64 = "0" Тогда 
			Символ2 = "110100";
		ИначеЕсли Символ64 = "1" Тогда 
			Символ2 = "110101";
		ИначеЕсли Символ64 = "2" Тогда 
			Символ2 = "110110";
		ИначеЕсли Символ64 = "3" Тогда 
			Символ2 = "110111";
		ИначеЕсли Символ64 = "4" Тогда 
			Символ2 = "111000";
		ИначеЕсли Символ64 = "5" Тогда 
			Символ2 = "111001";
		ИначеЕсли Символ64 = "6" Тогда 
			Символ2 = "111010";
		ИначеЕсли Символ64 = "7" Тогда 
			Символ2 = "111011";
		ИначеЕсли Символ64 = "8" Тогда 
			Символ2 = "111100";
		ИначеЕсли Символ64 = "9" Тогда 
			Символ2 = "111101";
		ИначеЕсли Символ64 = "+" Тогда 
			Символ2 = "111110";
		ИначеЕсли Символ64 = "/" Тогда 
			Символ2 = "111111";
		КонецЕсли; 
		
		Результат = Результат + Символ2;
		
	КонецЦикла;
	
	Возврат Результат;
		
КонецФункции	

Функция Число16_Из_Числа2(Число)
	
	Результат = "";

	Число2 = Формат(Число, "ЧН=; ЧВН=; ЧГ=");
	
	ОстатокСимволов = (4 - СтрДлина(Число2) % 4) % 4;
	
	Пока ОстатокСимволов > 0 Цикл 
		Число2			= "0" + Число2;
		ОстатокСимволов	= ОстатокСимволов - 1; 
	КонецЦикла;
	
	//отрезаем слева куски по 4 бит и делаем число-16
	Пока Число2 <> "" Цикл 
		
		Символ2 = Лев(Число2, 4);
		
		Если Символ2 = "0000" Тогда 
			Символ16 = "0";
		ИначеЕсли Символ2 = "0001" Тогда 
			Символ16  = "1";
		ИначеЕсли Символ2 = "0010" Тогда 
			Символ16  = "2";
		ИначеЕсли Символ2 = "0011" Тогда 
			Символ16  = "3";
		ИначеЕсли Символ2 = "0100" Тогда 
			Символ16  = "4";
		ИначеЕсли Символ2 = "0101" Тогда 
			Символ16  = "5";
		ИначеЕсли Символ2 = "0110" Тогда 
			Символ16  = "6";
		ИначеЕсли Символ2 = "0111" Тогда 
			Символ16  = "7";
		ИначеЕсли Символ2 = "1000" Тогда 
			Символ16  = "8";
		ИначеЕсли Символ2 = "1001" Тогда 
			Символ16  = "9";
		ИначеЕсли Символ2 = "1010" Тогда 
			Символ16  = "a";
		ИначеЕсли Символ2 = "1011" Тогда 
			Символ16  = "b";
		ИначеЕсли Символ2 = "1100" Тогда 
			Символ16  = "c";
		ИначеЕсли Символ2 = "1101" Тогда 
			Символ16  = "d";
		ИначеЕсли Символ2 = "1110" Тогда 
			Символ16  = "e";
		ИначеЕсли Символ2 = "1111" Тогда 
			Символ16  = "f";
		КонецЕсли;
		
		Результат = Результат + Символ16;
		
		Число2 = Прав(Число2, СтрДлина(Число2) - 4);
		
	КонецЦикла;
	
	Возврат Результат;
		
КонецФункции 	
	
Функция GUID_Из_Числа16(Число16)
	
	Результат = Сред(Число16, 1, 8) + "-"
	+ Сред(Число16, 9, 4) + "-"
	+ Сред(Число16, 13, 4) + "-"
	+ Сред(число16, 17, 4) + "-"
	+ Сред(число16, 21, 20) + "-"
	+ Сред(число16, 41, 4) + "-"
	+ Сред(число16, 45, 4) + "-"
	+ Сред(число16, 49, 4) + "-"
	+ Сред(число16, 53, 12);
	
	Возврат Результат
	
КонецФункции

// Возвращает признак использования регистра сведений ЗначенияСвойствОбъектов для хранения общих настроек.
// Определяется по наличию записей в регистре со свойством "ЯщикДиадокДляОрганизации".
//
// Возвращаемое значение:
//  Булево - Истина - используется регистр ЗначенияСвойствОбъектов, Ложь - нет
//
Функция Настройки_ИспользуетсяРегистрСведенийЗначенияСвойствОбъектов()
	
	ИменаСвойствНастроек = Перечисление_ИменаСвойствНастроек_1С();
	
	ИмяРегистраСведений	= "ЗначенияСвойствОбъектов";
	ТипИзмерения		= Метаданные.РегистрыСведений[ИмяРегистраСведений].Измерения.Свойство.Тип.Типы()[0];
	
	ИмяПланаВидовХарактеристик = Метаданные.НайтиПоТипу(ТипИзмерения).Имя;
	
	СвойствоЯщикДиадокДляОрганизации = ПланыВидовХарактеристик[ИмяПланаВидовХарактеристик].НайтиПоНаименованию(
		ИменаСвойствНастроек.IDОрганизации, Истина);
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Объект
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов
	|ГДЕ
	|	Свойство = &Свойство"
	);
	Запрос.УстановитьПараметр("Свойство", СвойствоЯщикДиадокДляОрганизации);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Результат = НЕ РезультатЗапроса.Пустой();
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_НастройкиПечатныхФормПоВидамДокументов_1С(ИдентификаторыВидовДокументов)
	
	Результат = "";
	
	Результат = ТекстЗапроса_ВременнаяТаблицаСвойствИспользуемыхДляХраненияНастроекПечатныхФорм_1С(ИдентификаторыВидовДокументов);
		
	Результат = Результат + ТекстЗапроса_РазделительЗапросов() +
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВремСвойства.ИмяСвойства 		КАК ИмяСвойства,
	|	ВремСвойства.Ссылка 			КАК Свойство,
	|	ВремСвойства.ИдВидаДокумента 	КАК ИдВидаДокумента,
	|	ДополнительныеСведения.Значение КАК Значение
	|ПОМЕСТИТЬ ВремНастройкиПФКонтрагента
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ДополнительныеСведения
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВремСвойстваПФ КАК ВремСвойства
	|		ПО ДополнительныеСведения.Свойство = ВремСвойства.Ссылка
	|ГДЕ
	|	ДополнительныеСведения.Объект = &Контрагент
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	НастройкиПФКонтрагента.ИмяСвойства 		КАК ИмяСвойства,
	|	НастройкиПФКонтрагента.ИдВидаДокумента	КАК ИдВидаДокумента,
	|	НастройкиПФКонтрагента.Значение 		КАК Значение
	|ИЗ
	|	ВремНастройкиПФКонтрагента КАК НастройкиПФКонтрагента
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ВремСвойства.ИмяСвойства,
	|	ВремСвойства.ИдВидаДокумента,
	|	ДополнительныеСведения.Значение
	|ИЗ
	|	РегистрСведений.ЗначенияСвойствОбъектов КАК ДополнительныеСведения
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВремСвойстваПФ КАК ВремСвойства
	|		ПО ДополнительныеСведения.Свойство = ВремСвойства.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ КОЛИЧЕСТВО(Свойство) КАК Количество ИЗ ВремНастройкиПФКонтрагента) КАК ВремКоличествоНастроекКонтрагента
	|		ПО (ИСТИНА)
	|ГДЕ
	|	ДополнительныеСведения.Объект = &Организация
	|	И ЕСТЬNULL(ВремКоличествоНастроекКонтрагента.Количество, 0) = 0
	|
	|ИТОГИ ПО
	|	ИдВидаДокумента";
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Результат);

	Возврат Результат;
	
КонецФункции

// Переопределяет имена свойств пакета задействованных в подсистеме хранения 1С 
//
// Параметры:
//	Пакет - Структура - контракт документа (см. Ядро.Контракт_Пакет())
//
// Возвращаемое значение:
//	Структура - с ключами:	"ДокументУчетаПакета"
Функция Пакеты_ПереопределяемыеИменаСвойств1С(Пакет)
	
	ИменаСвойствНастроек_1С = Перечисление_ИменаСвойствНастроек_1С();
	
	Результат = Новый Структура;
	Результат.Вставить("ДокументУчетаПакета", ИменаСвойствНастроек_1С.ДокументУчетаПакета);
	
	ИспользуютсяНастройкиПоУмолчаниюДляСчетов = Ядро.Настройки_ОснованияВидовДокументов_ИспользуютсяНастройкиПоУмолчанию();
	ЭтоИдентификаторВидаСчет = Ядро.ЭтоИдентификаторВидаСчет(Пакет.ИдентификаторВида);
	
	Если НЕ ИспользуютсяНастройкиПоУмолчаниюДляСчетов И ЭтоИдентификаторВидаСчет Тогда
		
		ЭтоДокументКонфигурацииСчетНаОплату = Ядро.Общее_ЭтоДокументКонфигурацииСчетНаОплату(Пакет.ДокументУчета);
		Если НЕ ЭтоДокументКонфигурацииСчетНаОплату Тогда
			
			Результат.ДокументУчетаПакета = ИменаСвойствНастроек_1С.ДокументУчетаПакетаСчет;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции
	
//}		КОНВЕРТАЦИЯ BASE64

//**********************************************
//{		КОНТУР.ПЛАГИНЫ

Процедура КонтурПлагины_СчетВПакете_УстановитьНастройкиПриПереходе(ВидыДокументовСчетов, ОснованиеДляСчетаИзПлагина) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		
		КонтурПлагины_СчетВПакете_УстановитьНастройкиПриПереходе_Диадок(ВидыДокументовСчетов, ОснованиеДляСчетаИзПлагина);
		
	Иначе
		
		КонтурПлагины_СчетВПакете_УстановитьНастройкиПриПереходе_1С(ВидыДокументовСчетов, ОснованиеДляСчетаИзПлагина);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура КонтурПлагины_СчетВПакете_УстановитьНастройкиПриПереходе_1С(ВидыДокументовСчетов, ОснованиеДляСчетаИзПлагина)
	
	Перечисления_ИменаСвойств = Перечисление_ИменаСвойствНастроек_1С();
	Свойство_ДобавлятьСчетВПакет = Свойства_СсылкаНаСвойство(Перечисления_ИменаСвойств.ДобавлятьСчетВПакет, Истина);
	
	СвойстваТиповОснованийДляВидовДокументов = Новый Массив;
	
	Для Каждого ВидДокумента Из ВидыДокументовСчетов Цикл
		
		ИмяСвойства = КлючСвойстваТипОснования(ВидДокумента);
		СсылкаНаСвойство = Свойства_СсылкаНаСвойство(ИмяСвойства, Истина);
		СвойстваТиповОснованийДляВидовДокументов.Добавить(СсылкаНаСвойство);
		
	КонецЦикла;
	
	КатегорияПакетаСчета = "СчетНаОплату";
	Свойство_СчетНаОплату = Свойства_СсылкаНаСвойство(КатегорияПакетаСчета, Истина);
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ДополнительныеСведенияВидПакета.Объект КАК Объект
	|ИЗ
	|	РегистрСведений.ДополнительныеСведения КАК ДополнительныеСведенияВидПакета
	|ГДЕ
	|	ДополнительныеСведенияВидПакета.Свойство = &Свойство_СчетНаОплату";
	
	Запрос.УстановитьПараметр("Свойство_СчетНаОплату", Свойство_СчетНаОплату);
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Запрос.Текст);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		ДопРеквизиты_УстановитьЗначение_1С(Выборка.Объект, Свойство_ДобавлятьСчетВПакет, Истина);
		
		Для Каждого СсылкаНаСвойство Из СвойстваТиповОснованийДляВидовДокументов Цикл
			ДопРеквизиты_УстановитьЗначение_1С(Выборка.Объект, СсылкаНаСвойство, ОснованиеДляСчетаИзПлагина);
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура КонтурПлагины_СчетВПакете_УстановитьНастройкиПриПереходе_Диадок(ВидыДокументовСчетов, ОснованиеДляСчетаИзПлагина)
	
	НастройкиДляУстановки = Новый Структура;
	
	Свойство_ДобавлятьСчетВПакет = "ДобавлятьСчетВПакет";
	НастройкиДляУстановки.Вставить(Свойство_ДобавлятьСчетВПакет, Истина);
	
	Для Каждого ВидДокумента Из ВидыДокументовСчетов Цикл
		
		ИмяСвойства = КлючСвойстваТипОснования(ВидДокумента);
		НастройкиДляУстановки.Вставить(ИмяСвойства, ОснованиеДляСчетаИзПлагина);
		
	КонецЦикла;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	Диадок_НастройкиКонтрагентов.Организация КАК Организация,
	|	Диадок_НастройкиКонтрагентов.Контрагент КАК Контрагент
	|ИЗ
	|	РегистрСведений.Диадок_НастройкиКонтрагентов КАК Диадок_НастройкиКонтрагентов
	|ГДЕ
	|	Диадок_НастройкиКонтрагентов.ИмяСвойства = ""СчетНаОплату""";

	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
        ДанныеОрганизации = Новый Структура("Ссылка", Выборка.Организация);
		ДанныеКонтрагента = Новый Структура("Ссылка", Выборка.Контрагент);
		
		Настройки_ЗаписатьЗначенияСвойств_Диадок(НастройкиДляУстановки, ДанныеОрганизации, ДанныеКонтрагента);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура КонтурПлагины_СчетВПакете_СконвертироватьОтправленныеПакеты(ДатаКонвертацииДанных, ОснованиеДляСчетаИзПлагина) Экспорт
	
	Если ИспользоватьПодсистемуДиадока() Тогда
		
		КонтурПлагины_СчетВПакете_СконвертироватьОтправленныеПакеты_Диадок(ДатаКонвертацииДанных);
		
	Иначе
		
		КонтурПлагины_СчетВПакете_СконвертироватьОтправленныеПакеты_1С(ОснованиеДляСчетаИзПлагина);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура КонтурПлагины_СчетВПакете_СконвертироватьОтправленныеПакеты_1С(ОснованиеДляСчетаИзПлагина)
	
	Если ОснованиеДляСчетаИзПлагина <> "ДокументПродажи" Тогда
		Возврат;
	КонецЕсли;
	
	Перечисления_ИменаСвойств = Перечисление_ИменаСвойствНастроек_1С();
	
	Свойство_ВидПакета = Свойства_СсылкаНаСвойство(Перечисления_ИменаСвойств.ВидПакета, Истина);
	Свойство_ВидПакетаСчет = Свойства_СсылкаНаСвойство(Перечисления_ИменаСвойств.ВидПакетаСчет, Истина);
	
	ИдентификаторыВидовПакета = Новый Массив;
	ИдентификаторыВидовПакета.Добавить("ID_ТОРГ12_Акт_БезНДС");
	ИдентификаторыВидовПакета.Добавить("ID_УПД_БезНДС");
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ДополнительныеСведенияВидПакета.Объект КАК Объект,
	|	ДополнительныеСведенияВидПакета.Свойство КАК Свойство,
	|	ДополнительныеСведенияВидПакета.Значение КАК Значение
	|ИЗ
	|	РегистрСведений.ДополнительныеСведения КАК ДополнительныеСведенияВидПакета
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ДополнительныеСведенияВидПакетаСчет
	|		ПО ДополнительныеСведенияВидПакета.Объект = ДополнительныеСведенияВидПакетаСчет.Объект
	|			И (ДополнительныеСведенияВидПакетаСчет.Свойство = &Свойство_ВидПакетаСчет)
	|ГДЕ
	|	ДополнительныеСведенияВидПакета.Свойство = &Свойство_ВидПакета
	|	И ДополнительныеСведенияВидПакета.Значение В(&ИдентификаторыВидовПакета)
	|	И ДополнительныеСведенияВидПакетаСчет.Объект ЕСТЬ NULL";
	
	Запрос.УстановитьПараметр("Свойство_ВидПакета", Свойство_ВидПакета);
	Запрос.УстановитьПараметр("Свойство_ВидПакетаСчет", Свойство_ВидПакетаСчет);
	Запрос.УстановитьПараметр("ИдентификаторыВидовПакета", ИдентификаторыВидовПакета);
	
	ТекстЗапроса_УстановитьИмяРегистраСведений(Запрос.Текст);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		ДопРеквизиты_УстановитьЗначение_1С(Выборка.Объект, Свойство_ВидПакетаСчет, "ID_СчетПлагина");
	КонецЦикла;
	
КонецПроцедуры

Процедура КонтурПлагины_СчетВПакете_СконвертироватьОтправленныеПакеты_Диадок(ДатаКонвертацииДанных)
	
	ИдентификаторыСчетовНаОплату = Ядро.ВидыЭлектронныхСчетовНаОплату();
	ВидыПакетовСчетов = Новый Массив;
	
	Для Каждого ИдентификаторСчета Из ИдентификаторыСчетовНаОплату Цикл
		
		ВидПакета = ДопСправочники_СсылкаПоИдентификатору_Диадок("ВидыПакетов", ИдентификаторСчета);
		
		Если ВидПакета <> Неопределено Тогда
			ВидыПакетовСчетов.Добавить(ВидПакета);
		КонецЕсли;
		
	КонецЦикла;
	
	Если ВидыПакетовСчетов.Количество() > 0 Тогда
		
		Запрос = Новый Запрос;
		Запрос.Текст =
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	Диадок_Документы.Документ,
		|	Диадок_Документы.Направление,
		|	Диадок_Документы.Организация,
		|	Диадок_Документы.Контрагент,
		|	Диадок_Документы.ОрганизацияДиадок,
		|	Диадок_Документы.КонтрагентДиадок,
		|	Диадок_Документы.Метка,
		|	Диадок_Документы.Пакет КАК ОсновнойСвязанныйПакет,
		|	Диадок_Документы.ДатаЗагрузки КАК ДатаОтправкиСчета
		|ИЗ
		|	Справочник.Диадок_Документы КАК Диадок_Документы
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Диадок_ПакетыДокументов КАК Диадок_ПакетыДокументов
		|		ПО Диадок_Документы.Направление = Диадок_ПакетыДокументов.Направление
		|			И Диадок_Документы.Документ = Диадок_ПакетыДокументов.Документ
		|			И (Диадок_ПакетыДокументов.ВидПакета В (&ВидыПакетовСчетов))
		|ГДЕ
		|	НЕ Диадок_Документы.ПометкаУдаления
		|	И Диадок_Документы.Направление = &Направление
		|	И Диадок_Документы.Метка = &Метка
		|	И Диадок_Документы.ТипДокумента = &ТипДокумента
		|	И Диадок_Документы.Документ <> НЕОПРЕДЕЛЕНО
		|	И Диадок_Документы.ДатаЗагрузки >= &ДатаЗагрузки
		|	И Диадок_ПакетыДокументов.Ссылка ЕСТЬ NULL
		|
		|УПОРЯДОЧИТЬ ПО
		|	ДатаОтправкиСчета УБЫВ";
		
		Запрос.УстановитьПараметр("Направление", Перечисление_Направления().Исходящее);
		Запрос.УстановитьПараметр("Метка", Перечисление_СтатусыПакетов().Отправлен);
		Запрос.УстановитьПараметр("ТипДокумента", Тип_СчетНаОплату());
		Запрос.УстановитьПараметр("ВидыПакетовСчетов", ВидыПакетовСчетов);
		Запрос.УстановитьПараметр("ДатаЗагрузки", ДатаКонвертацииДанных);
		
		Выборка = Запрос.Выполнить().Выбрать();
		
		Пока Выборка.Следующий() Цикл
			
			ОбъектПакета = Справочники.Диадок_ПакетыДокументов.СоздатьЭлемент();
			
			ЗаполнитьЗначенияСвойств(ОбъектПакета, Выборка);
			ОбъектПакета.ВидПакета = ВидыПакетовСчетов[0];
			ДопРеквизиты_УстановитьЗначение(ОбъектПакета, "ОсновнойСвязанныйПакет", Выборка.ОсновнойСвязанныйПакет);
			
			ОбъектПакета.Записать();
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

//}		КОНТУР.ПЛАГИНЫ
