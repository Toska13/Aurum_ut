// BSLLS:MissingVariablesDescription-off
// BSLLS:UnusedLocalVariable-off
Перем ОсновнойМодуль Экспорт;
// BSLLS:MissingVariablesDescription-on
// BSLLS:UnusedLocalVariable-on

//{ СлужебныйПрограммныйИнтерфейс

// Событие возникает перед выполнением запроса по шапке документа (см. ДанныеШапки).
// Позволяет переопределить выражения полей запроса для получения данных шапки.
//
// Параметры:
//  Реквизиты			 - Структура		 - коллекция описаний полей запроса: ключ - имя поля, значение - выражение;
//  МетаданныеИсточника	 - ОбъектМетаданных	 - Метаданные документа-источника;
//  ВидЭД 				 - Строка			 - вид подгатавливаемого электронного документа (см. Перечисление_ВидыЭД());
//
// Пример:
//		Если ЕстьРеквизитМетаданных(МетаданныеИсточника, "Грузоотправитель") Тогда 
//			
//			Реквизиты.Грузоотправитель = 
//			"ВЫБОР
//			|	КОГДА Грузоотправитель = ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка)
//			|		ТОГДА Организация
//			|	ИНАЧЕ Грузоотправитель
//			|КОНЕЦ";
//			
//		КонецЕсли;
//
Процедура ПриПодготовкеЗапросаПоШапкеДокумента(Реквизиты, МетаданныеИсточника, ВидЭД) Экспорт
	
	Если Реквизиты.Свойство("БанковскийСчетОрганизации")
		И Не ОсновнойМодуль.ЕстьРеквизитМетаданных(МетаданныеИсточника, "БанковскийСчетОрганизации")
		И ОсновнойМодуль.ЕстьРеквизитМетаданных(МетаданныеИсточника, "СтруктурнаяЕдиница") Тогда
		
		Реквизиты.БанковскийСчетОрганизации = 
		"ВЫБОР 
		|	КОГДА СтруктурнаяЕдиница ССЫЛКА Справочник.БанковскиеСчета
		|		ТОГДА СтруктурнаяЕдиница
		|	ИНАЧЕ НЕОПРЕДЕЛЕНО
		|КОНЕЦ";
		
	КонецЕсли;
	
	Если Реквизиты.Свойство("ДополнительныеУсловия")
		И ОсновнойМодуль.ЕстьРеквизитМетаданных(МетаданныеИсточника, "УсловиеПродаж") Тогда
		Реквизиты.ДополнительныеУсловия = "ПРЕДСТАВЛЕНИЕ(УсловиеПродаж)";
	КонецЕсли;
	
	Если Реквизиты.Свойство("СрокОплаты")
		И ОсновнойМодуль.ЕстьРеквизитМетаданных(МетаданныеИсточника, "ДатаОплаты") Тогда
		Реквизиты.СрокОплаты = "ДатаОплаты";
	КонецЕсли;
	
КонецПроцедуры

// Событие возникает после выполнения запроса по шапке документа (см. ДанныеШапки).
// Позволяет переопределить или дополнить данные шапки документа.
//
// Параметры:
//  Шапка - Структура - Структура со значениями реквизитов документа.
//
// Пример:
//   Шапка.БанковскийСчетОрганизации = СчетОрганизации(Шапка.Организация);
//
Процедура ПослеПолученияДанныхШапкиДокумента(Шапка) Экспорт
	
	
	
КонецПроцедуры

// Событие возникает при получении сведений об организации, контрагенте или физическом лице.
//
Процедура ПриПолученииРеквизитовЮрФизЛица(Реквизиты, ЮрФизЛицо, Период, БанковскийСчет, СтандартнаяОбработка) Экспорт
	
	
	
КонецПроцедуры

// Событие возникает при получении реквизитов договора, соглашения
// или иного основания передачи/перехода прав собственности.
//
Процедура ПриПолученииРеквизитовДоговора(Реквизиты, Шапка, СтандартнаяОбработка) Экспорт
	
	
	
КонецПроцедуры

// Событие возникает при определении фактического места хранения контактной информации объекта.
//
// Параметры:
//  ХранилищеКИ			 - ЛюбаяСсылка	 - место хранения контактной информации;
//  СсылкаНаОбъект		 - ЛюбаяСсылка	 - объект-владелец контактной информации;
//  СтандартнаяОбработка - Булево		 - Ложь, чтобы отключить стандартную обработку события;
//
// Пример:
//		Если ТипЗнч(СсылкаНаОбъект) = Тип("СправочникСсылка.Организации")
//			И СсылкаНаОбъект.ЮрФизЛицо = Перечисления.ЮрФизЛицо.ФизЛицо Тогда
//			
//			СтандартнаяОбработка = Ложь;
//			ХранилищеКИ = СсылкаНаОбъект.ИндивидуальныйПредприниматель;
//			
//		КонецЕсли;
//
Процедура ПриОпределенииМестаХраненияКонтактнойИнформацииОбъекта(ХранилищеКИ, СсылкаНаОбъект, СтандартнаяОбработка) Экспорт
	
	Если ТипЗнч(СсылкаНаОбъект) = Тип("СправочникСсылка.Организации")
		И СсылкаНаОбъект.ЮрФизЛицо = Перечисления.ЮрФизЛицо.ФизЛицо Тогда
		
		СтандартнаяОбработка = Ложь;
		ХранилищеКИ = СсылкаНаОбъект.ИндивидуальныйПредприниматель;
		
	КонецЕсли;
	
КонецПроцедуры

// Событие позволяет переопределить алгоритм вычисления вида контактной информации,
// соответствующего юр./физ. лицу и указанному типу контактной информации.
//
// Параметры:
//  ВидКИ - СправочникСсылка.ВидыКонтактнойИнформации - вид контактной информации;
//  ЮрФизЛицо - ЛюбаяСсылка	 - объект-владелец контактной информации;
//  ТипКИ - Строка - тип контактной информации (см. Перечисление_ВидыКИ);
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события;
//
// Пример:
//		
//		Если ТипКИ = Перечисление_ВидыКИ().ЮридическийАдрес Тогда
//			ВидКИ = Справочники.ВидыКонтактнойИнформации.АдресЮридический;
//		ИначеЕсли ТипКИ = Перечисление_ВидыКИ().ФактическийАдрес Тогда
//			ВидКИ = Справочники.ВидыКонтактнойИнформации.АдресФактический;
//		КонецЕсли;
//		
//		Если ЗначениеЗаполнено(ВидКИ) Тогда
//			СтандартнаяОбработка = Ложь;
//		КонецЕсли;
//
Процедура ПриПолученииВидаКонтактнойИнформации(ВидКИ, ЮрФизЛицо, ТипКИ, СтандартнаяОбработка) Экспорт
	
	
	
КонецПроцедуры

// Заполняет развернутую таблицу кодов маркировки штатными средствами конфигурации.
// В этой процедуре можно отключить стандартную обработку получения таблицы
// с помощью подсистемы БиблиотекаИнтеграцииИСМП.
//
// Параметры:
//  Результат - ТаблицаЗначений - Заполняемая таблица (см. МенеджерИнтеграции.НоваяРазвернутаяТаблицаУпаковок)
//  СсылкаНаДокумент - ЛюбаяСсылка - объект-владелец штрихкода
//  СтандартнаяОбработка - Булево - если установить Ложь, то заполнение таблицы кодов средствами
//                                  типовой библиотеки интеграции с ИС МП будет отключено.
//
Процедура ЗаполнитьРазвернутуюТаблицуКодов(Результат, СсылкаНаОбъект, СтандартнаяОбработка) Экспорт
	
	
	
КонецПроцедуры

// Загружает сведения о маркировке из товарной части ЭД в информационную базу.
//
// Параметры:
//  ДокументОбъект - ЛюбойОбъект - документ-владелец штрихкодов.
//  ТоварнаяЧасть - ТаблицаЗначений - данные товарной части электронного документа.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить загрузку средствами библиотеки интеграции ИС МП.
//
Процедура ЗагрузитьКодыМаркировки(ДокументОбъект, ТоварнаяЧасть, СтандартнаяОбработка) Экспорт
	
	
	
КонецПроцедуры

// Заполняет список правил создания и заполнения документов учета.
//
// Параметры:
//  ПравилаСоздания - СписокЗначений - (возвращаемый параметр) идентификаторы правил создания и их представления.
//  ФорматЭД - Структура - см. ОсновнойМодуль.НовоеОписаниеФорматаЭлектронногоДокумента.
//  СтандартнаяОбработка - Булево - Ложь, если требуется отключить стандартную обработку заполнения правил.
//
Процедура ПриЗаполненииПравилСозданияДокументовУчета(ПравилаСоздания, ФорматЭД, СтандартнаяОбработка) Экспорт
	
	ФорматыЭД = МодульФорматыЭлектронныхДокументов();
	
	Если ФорматыЭД.ЭтоФормализованныйСчетФНС(ФорматЭД) Тогда
		
		Операция = ОперацияЗаказПоставщику();
		Представление = "Заказ поставщику";
		ПравилаСоздания.Добавить(Операция, Представление);
		
		Операция = ОперацияЗаявкаНаРасходованиеСредств();
		Представление = "Заявка на расходование средств";
		ПравилаСоздания.Добавить(Операция, Представление);
		
		Операция = ОперацияРасходныйКассовыйОрдер();
		Представление = "Расходный кассовый ордер";
		ПравилаСоздания.Добавить(Операция, Представление);
		
		Операция = ОперацияСчетНаОплатуПоставщика();
		Представление = "Счет на оплату поставщика";
		ПравилаСоздания.Добавить(Операция, Представление);
		
	КонецЕсли;
	
КонецПроцедуры

// Обработка создания и заполнения документа учета по указанному правилу.
//
// Параметры:
//  ДокументОбъект - ДокументОбъект - (возвращаемый параметр) Заполняемый объект.
//  ИдентификаторОпераци - Строка - идентификатор правила создания и заполнения документа учета.
//  ПараметрыСоздания - Структура - см. ОсновнойМодуль.ПараметрыСозданияДокументаИнформационнойБазы.
//  ДанныеЭД - Структура - разобранное содержимое электронного документа (см. ОсновнойМодуль.ДанныеЭлектронногоДокумента)
//  СтандартнаяОбработка - Булево - Ложь, если требуется отключить стандартную обработку заполнения объекта.
//
Процедура ПриСозданииДокументаУчета(ДокументОбъект, ИдентификаторОперации, ПараметрыСоздания, ДанныеЭД, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	ФорматЭД = ДанныеЭД.Формат;
	ШапкаЭД = ДанныеЭД.Шапка;
	ТабличнаяЧастьЭД = ДанныеЭД.ТабличнаяЧасть;
	ИтогиЭД = ДанныеЭД.Итоги;
	
	Если ИдентификаторОперации = ОперацияПоступлениеТоваровУслуг() Тогда
		
		ДокументОбъект = Документы.ПоступлениеТоваровУслуг.СоздатьДокумент();
		ЗаполнитьШапкуПоступленияТоваровУслуг(ДокументОбъект, ПараметрыСоздания, ШапкаЭД);
		ЗаполнитьТабличнуюЧастьПоступленияТоваровУслуг(ДокументОбъект, ТабличнаяЧастьЭД);
		
	ИначеЕсли ИдентификаторОперации = ОперацияКорректировкаПоступления() Тогда
		
		ПроверитьЗаполнитьОснованиеКорректировкиПоступления(ПараметрыСоздания, ШапкаЭД, ФорматЭД);
		
		ДокументОбъект = Документы.КорректировкаПоступления.СоздатьДокумент();
		ЗаполнитьШапкуКорректировкиПоступления(ДокументОбъект, ПараметрыСоздания, ШапкаЭД);
		ЗаполнитьТабличнуюЧастьКорректировкиПоступления(ДокументОбъект, ТабличнаяЧастьЭД);
		
	ИначеЕсли ИдентификаторОперации = ОперацияСчетФактураПолученный() Тогда
		
		ДокументОбъект = Документы.СчетФактураПолученный.СоздатьДокумент();
		ЗаполнитьШапкуСчетаФактурыПолученного(ДокументОбъект, ПараметрыСоздания, ШапкаЭД);
		ЗаполнитьОсновныеРеквизитыСчетаФактурыПолученного(ДокументОбъект, ДанныеЭД, ФорматЭД);
		
	ИначеЕсли ИдентификаторОперации = ОперацияКорректировочныйСчетФактураПолученный() Тогда
		
		ДокументОбъект = Документы.СчетФактураПолученный.СоздатьДокумент();
		ЗаполнитьШапкуСчетаФактурыПолученного(ДокументОбъект, ПараметрыСоздания, ШапкаЭД);
		ЗаполнитьОсновныеРеквизитыКорректировочногоСчетаФактурыПолученного(ДокументОбъект, ДанныеЭД, ФорматЭД);
		
	ИначеЕсли ИдентификаторОперации = ОперацияПоступлениеДопРасходов() Тогда
		
		ДокументОбъект = Документы.ПоступлениеДопРасходов.СоздатьДокумент();
		ЗаполнитьШапкуПоступленияДопРасходов(ДокументОбъект, ПараметрыСоздания, ШапкаЭД);
		ЗаполнитьШапкуПоступленияДопРасходовПоТабличнойЧасти(ДокументОбъект, ТабличнаяЧастьЭД);
		
	ИначеЕсли ИдентификаторОперации = ОперацияАктСверкиВзаиморасчетов() Тогда
		
		ДокументОбъект = Документы.АктСверкиВзаиморасчетов.СоздатьДокумент();
		ЗаполнитьШапкуАктаСверкиВзаиморасчетов(ДокументОбъект, ПараметрыСоздания, ШапкаЭД, ИтогиЭД);
		ЗаполнитьТабличнуюЧастьАктаСверкиВзаиморасчетов(ДокументОбъект, ТабличнаяЧастьЭД);
		
	ИначеЕсли ИдентификаторОперации = ОперацияЗаказПоставщику() Тогда
		
		ДокументОбъект = Документы.ЗаказПоставщику.СоздатьДокумент();
		ЗаполнитьШапкуЗаказаПоставщику(ДокументОбъект, ПараметрыСоздания, ШапкаЭД, ИтогиЭД);
		ЗаполнитьТабличнуюЧастьЗаказаПоставщику(ДокументОбъект, ТабличнаяЧастьЭД);
		
	ИначеЕсли ИдентификаторОперации = ОперацияЗаявкаНаРасходованиеСредств() Тогда
		
		ДокументОбъект = Документы.ЗаявкаНаРасходованиеСредств.СоздатьДокумент();
		ЗаполнитьШапкуЗаявкиНаРасходованиеСредств(ДокументОбъект, ПараметрыСоздания, ШапкаЭД, ИтогиЭД);
		ЗаполнитьТабличнуюЧастьЗаявкиНаРасходованиеСредств(ДокументОбъект, ПараметрыСоздания, ШапкаЭД, ИтогиЭД);
		
	ИначеЕсли ИдентификаторОперации = ОперацияРасходныйКассовыйОрдер() Тогда
		
		ДокументОбъект = Документы.РасходныйКассовыйОрдер.СоздатьДокумент();
		ЗаполнитьШапкуРасходногоКассовогоОрдера(ДокументОбъект, ПараметрыСоздания, ШапкаЭД, ИтогиЭД);
		ЗаполнитьТабличнуюЧастьРасходногоКассовогоОрдера(ДокументОбъект, ПараметрыСоздания, ШапкаЭД, ИтогиЭД);
		
	ИначеЕсли ИдентификаторОперации = ОперацияСчетНаОплатуПоставщика() Тогда
		
		ДокументОбъект = Документы.СчетНаОплатуПоставщика.СоздатьДокумент();
		ЗаполнитьШапкуСчетаНаОплатуПоставщика(ДокументОбъект, ПараметрыСоздания, ШапкаЭД, ИтогиЭД);
		ЗаполнитьТабличнуюЧастьСчетаНаОплатуПоставщика(ДокументОбъект, ТабличнаяЧастьЭД);
		
	Иначе
		
		Ошибка = "Неизвестный идентификатор операции (%1)";
		Ошибка = СтрЗаменить(Ошибка, "%1", ИдентификаторОперации);
		ВызватьИсключение Ошибка;
		
	КонецЕсли;
	
КонецПроцедуры

// Выполяется при чтении соответствия номенклатуры контрагента и номенклатуры информационной базы.
//
// Параметры:
//  Результат - Соответствие - (возвращаемый параметр) содержит элементы:
//    * Ключ - Строка - идентификатор номенклатуры контрагента;
//    * Значение - Структура - номенклатура информационной базы (см. НоваяНоменклатураИнформационнойБазы)
//  ВладелецНоменклатуры - ЛюбаяСсылка - Владелец номенклатуры (контрагент/партнер).
//  ИдентификаторыНоменклатуры - Массив из Строка - идентификаторы номенклатуры контрагентов.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриЧтенииСоответствияНоменклатуры(Результат, ВладелецНоменклатуры, ИдентификаторыНоменклатуры, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	Если НЕ ЗначениеЗаполнено(ВладелецНоменклатуры) Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого Идентификатор Из ИдентификаторыНоменклатуры Цикл
		
		НоменклатураИБ = ПолучитьНоменклатуруИнформационнойБазы(ВладелецНоменклатуры, Идентификатор);
		
		Если НЕ ЗначениеЗаполнено(НоменклатураИБ) Тогда
			// Миграция данных из устаревшего хранилища по требованию
			НоменклатураИБ = ПеренестиСопоставлениеНоменклатуры_НоменклатураКонтрагентов(ВладелецНоменклатуры, Идентификатор);
		КонецЕсли;
		
		Результат.Вставить(Идентификатор, НоменклатураИБ);
		
	КонецЦикла;
	
КонецПроцедуры

// Выполяется при записи соответствия номенклатуры контрагента и номенклатуры информационной базы.
// Позволяет переопределить место хранения соответствия номенклатуры в текущей конфигурации.
//
// Параметры:
//  НоменклатураКонтрагента - Структура - данные номенклатуры контрагента (см. НоваяНоменклатураКонтрагента)
//  НоменклатураИБ - Структура - данные о номенклатуре информационной базы (имеющиеся или требующие подбора).
//                               См. НоваяНоменклатураИнформационнойБазы.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриЗаписиСоответствияНоменклатуры(НоменклатураКонтрагента, НоменклатураИБ, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	Номенклатура = НоменклатураИБ.Номенклатура;
	Характеристика = НоменклатураИБ.Характеристика;
	
	ВладелецНоменклатуры = НоменклатураКонтрагента.Владелец;
	ИдентификаторНоменклатуры = НоменклатураКонтрагента.Идентификатор;
	
	НачатьТранзакцию();
	
	Попытка
		
		// Удалить старые записи во избежание дублирования идентификаторов и номенклатуры
		УдалитьСопоставлениеНоменклатуры_НоменклатураКонтрагентов(ВладелецНоменклатуры, ИдентификаторНоменклатуры);
		ДобавитьСопоставлениеНоменклатуры(НоменклатураКонтрагента, Номенклатура, Характеристика);
		
		// Запись дублируется в устаревшее хранилище для обратной совместимости
		ДобавитьСопоставлениеНоменклатуры_НоменклатураКонтрагентов(НоменклатураКонтрагента, Номенклатура, Характеристика);
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		ОтменитьТранзакцию();
		
		Ошибка = ИнформацияОбОшибке();
		ЗаписатьОшибкуСопоставленияНоменклатуры(ИдентификаторНоменклатуры, ВладелецНоменклатуры, Ошибка);
		
		ВызватьИсключение;
		
	КонецПопытки;
	
КонецПроцедуры

// Заполняет варианты сопоставления номенклатурыИБ по полям НоменклатураКонтрагента
//
// Параметры:
//  Результат				 - Массив - Используется для заполнения контрактами НоваяНоменклатураИнформационнойБазы
//  НоменклатураКонтрагента	 - Структура - Структура полей см. Контракт_НоменклатураКонтрагента 
//  СтандартнаяОбработка	 - Булево - Ложь, чтобы переопределить стандартный обработчик
//
Процедура ПриЗаполненииВариантовСопоставленияНоменклатуры(Результат, НоменклатураКонтрагента, СтандартнаяОбработка) Экспорт
	
КонецПроцедуры

// Выполняется при заполнении описания и взаимосвязей объектов, составляющих номенклатуру информационной базы.
//
// Параметры:
//  МетаданныеСопоставления - Структура - метаданные и взаимосвязь объектов, составляющих номенклатуру ИБ.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриЗаполненииМетаданныхСопоставленияНоменклатуры(МетаданныеСопоставления, СтандартнаяОбработка) Экспорт
	
	
	
КонецПроцедуры

// Определяет свойства набора номенклатур информационной базы.
// Устанавливает обязательность использования характеристик и упаковок номенклатуры.
//
// Параметры:
//  СвойстваНоменклатурИБ - Соответствие - содержит:
//  * Ключ     - ОпределяемыйТип.НоменклатураБЭД - ссылка на номенклатуру ИБ.
//  * Значение - Структура                       - содержит свойства номенклатуры ИБ. См. НовыеСвойстваНоменклатурыИБ.
//  НаборНоменклатуры - Массив из ЛюбаяСсылка - ссылки на номенклатуру ИБ, для которой требуется определить свойства.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриОпределенииСвойствНоменклатурыИнформационнойБазы(СвойстваНоменклатурИБ, НаборНоменклатуры, СтандартнаяОбработка) Экспорт
	
	
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальнаяЛогика.ЗаполнитьОбстФормСЧФ820.
//
// Параметры:
//  СчетФактура - ДокументСсылка.СчетФактураВыданный	 - счет фактура на основании которой вычисляется ОбстФормСЧФ;
//
// Возвращаемое значение:
//  Строка - вычисленное значение ОбстФормСЧФ
//
Функция ПолучитьОбстоятельствоФормированияСчетФактуры(СчетФактура) Экспорт
	
	Результат = Неопределено;
	
	Возврат Результат;
	
КонецФункции

// Получает ссылку на документ, на который оформлена корректировка.
//
// Параметры:
//  КорректировочныйДокумент - ЛюбаяСсылка - Корректировочный документ (корректировка реализации или корректировочный счет-фактура)
// 
// Возвращаемое значение:
//  ЛюбаяСсылка, Неопределено - ссылка на корректируемый документ.
//
Функция КорректируемыйДокумент(КорректировочныйДокумент) Экспорт
	
	ТипДокумента = ТипЗнч(КорректировочныйДокумент);
	
	Если ТипДокумента = Тип("ДокументСсылка.СчетФактураВыданный") Тогда
		
		ИмяРеквизита = "ИсправляемыйСчетФактура";
		Результат = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(КорректировочныйДокумент, ИмяРеквизита);
		
	ИначеЕсли ТипДокумента = Тип("ДокументСсылка.КорректировкаРеализации") Тогда
		
		ИмяРеквизита = "ИсправляемыйДокументРеализации";
		Результат = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(КорректировочныйДокумент, ИмяРеквизита);
		
	Иначе
		
		КонтекстПроверки = "ИнтеграционныйМодуль.КорректируемыйДокумент";
		ПараметрИмя = "КорректировочныйДокумент";
		ПараметрПредставление = Строка(КорректировочныйДокумент);
		Если КорректировочныйДокумент = Неопределено Тогда
			ПараметрПредставление = "Неопределено";
		КонецЕсли;
		
		Ошибка = ОсновнойМодуль.ПодставитьПараметрыВСтроку(
			"Недопустимое значение параметра %1 (передано значение: %2) в %3",
			ПараметрИмя,
			ПараметрПредставление,
			КонтекстПроверки
		);
		
		ВызватьИсключение Ошибка;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

//} СлужебныйПрограммныйИнтерфейс

//{ СлужебныеПроцедурыИФункции

Функция ОперацияПоступлениеТоваровУслуг()
	Возврат "ПоступлениеТоваровУслуг";
КонецФункции

Функция ОперацияКорректировкаПоступления()
	Возврат "КорректировкаПоступления";
КонецФункции

Функция ОперацияСчетФактураПолученный()
	Возврат "СчетФактураПолученный";
КонецФункции

Функция ОперацияКорректировочныйСчетФактураПолученный()
	Возврат "КорректировочныйСчетФактураПолученный";
КонецФункции

Функция ОперацияПоступлениеДопРасходов()
	Возврат "ПоступлениеДопРасходов";
КонецФункции

Функция ОперацияАктСверкиВзаиморасчетов()
	Возврат "АктСверкиВзаиморасчетов";
КонецФункции

Функция ОперацияЗаказПоставщику()
	Возврат "ЗаказПоставщику";
КонецФункции

Функция ОперацияЗаявкаНаРасходованиеСредств()
	Возврат "ЗаявкаНаРасходованиеДенежныхСредств";
КонецФункции

Функция ОперацияРасходныйКассовыйОрдер()
	Возврат "РасходныйКассовыйОрдер";
КонецФункции

Функция ОперацияСчетНаОплатуПоставщика()
	Возврат "СчетНаОплатуПоставщика";
КонецФункции

Процедура ЗаполнитьШапкуПоступленияТоваровУслуг(ДокументОбъект, ПараметрыСоздания, РеквизитыЭлектронногоДокумента)
	
	УстановитьПараметрыУчетаНДС(ДокументОбъект, ПараметрыСоздания.ЦенаВключаетНДС);
	УстановитьОрганизациюДокумента(ДокументОбъект, ПараметрыСоздания.Организация);
	УстановитьКонтрагентаДокумента(ДокументОбъект, ПараметрыСоздания.Контрагент);
	УстановитьРеквизитыВходящегоДокумента(ДокументОбъект, РеквизитыЭлектронногоДокумента);
	УстановитьДоговорСПоставщиком(ДокументОбъект);
	УстановитьТипЦенДокумента(ДокументОбъект);
	
	УстановитьПараметрыОтраженияДокументаВУчете(ДокументОбъект);
	УстановитьОсновныеРеквизитыДокумента(ДокументОбъект);
	УстановитьПодразделениеДокумента(ДокументОбъект);
	УстановитьВалютуДокумента(ДокументОбъект);
	УстановитьСкладДокумента(ДокументОбъект);
	УстановитьВидПоступленияПокупка(ДокументОбъект);
	
	ЗаполнитьСчетаУчетаРасчетов(ДокументОбъект);
	
КонецПроцедуры

Функция НоменклатураЯвляетсяУслугойВЭД(ВидНоменклатурыЭД)
	
	Результат = Ложь;
	
	Если ВидНоменклатурыЭД = "2" Или ВидНоменклатурыЭД = "3" Тогда
		
		Результат = Истина;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьТабличнуюЧастьПоступленияТоваровУслуг(ДокументОбъект, ТабличнаяЧасть)
	
	ВладелецНоменклатуры = ДокументОбъект.Контрагент;
	
	МетаданныеОбъекта = ДокументОбъект.Метаданные();
	
	Для Каждого СтрокаДанных Из ТабличнаяЧасть Цикл
		
		НоменклатураИБ = СтрокаДанных.НоменклатураИнформационнойБазы;
		
		Если ЗначениеЗаполнено(НоменклатураИБ) Тогда
			Номенклатура = НоменклатураИБ.Номенклатура;
			Характеристика= НоменклатураИБ.Характеристика;
		Иначе
			Номенклатура = Неопределено;
			Характеристика= Неопределено;
		КонецЕсли;
		
		ЭтоУслуга = ЭтоНоменклатураУслуга(Номенклатура);
		НоменклатураЯвляетсяУслугойВЭД = НоменклатураЯвляетсяУслугойВЭД(СтрокаДанных.ВидНоменклатуры);

		Если (Номенклатура <> Неопределено И ЭтоУслуга) ИЛИ (Номенклатура = Неопределено И НоменклатураЯвляетсяУслугойВЭД) Тогда
			
			ИмяТЧ = "Услуги";
			
			СтрокаТЧ = ДокументОбъект.Услуги.Добавить();
			СтрокаТЧ.Номенклатура = Номенклатура;
			СтрокаТЧ.Содержание = СтрокаДанных.Наименование;
			
			ЗаполнитьСтатьюЗатратВПоступленииТоваровУслуг(СтрокаТЧ, Номенклатура);
			
		Иначе
			
			ИмяТЧ = "Товары";
			
			СтрокаТЧ = ДокументОбъект.Товары.Добавить();
			СтрокаТЧ.Номенклатура = Номенклатура;
			СтрокаТЧ.ХарактеристикаНоменклатуры = Характеристика;
			СтрокаТЧ.ЕдиницаИзмерения = ЕдиницаИзмеренияНоменклатуры(Номенклатура, СтрокаДанных.КодЕдИзм);
			СтрокаТЧ.Коэффициент = 1;
			
		КонецЕсли;
		
		Количество = СтрокаДанных.Количество;
		Если Не ЗначениеЗаполнено(Количество) Тогда
			Количество = 1;
		КонецЕсли;
		
		Сумма = СтрокаДанных.СуммаБезНДС;
		Цена = СтрокаДанных.Цена;
		
		Если ДокументОбъект.СуммаВключаетНДС Тогда
			Сумма = СтрокаДанных.СуммаСНДС;
			Цена = Сумма / Количество;
		КонецЕсли;
		
		СтрокаТЧ.Количество	= Количество;
		СтрокаТЧ.Цена		= Цена;
		СтрокаТЧ.Сумма		= Сумма;
		СтрокаТЧ.СуммаНДС	= СтрокаДанных.СуммаНДС;
		СтрокаТЧ.СтавкаНДС	= ПолучитьСтавкуНДСДиадок(СтрокаДанных.СтавкаНДС);
		
		ЗаполнитьСчетаУчетаСтрокиТабличнойЧасти(ИмяТЧ, СтрокаТЧ, ДокументОбъект);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьШапкуКорректировкиПоступления(ДокументОбъект, ПараметрыСоздания, РеквизитыЭлектронногоДокумента)
	
	ЗаполнитьПоДокументуОснованию(ДокументОбъект, ПараметрыСоздания.ДокументОснование);
	УстановитьОрганизациюДокумента(ДокументОбъект, ПараметрыСоздания.Организация);
	УстановитьКонтрагентаДокумента(ДокументОбъект, ПараметрыСоздания.Контрагент);
	УстановитьРеквизитыВходящегоДокумента(ДокументОбъект, РеквизитыЭлектронногоДокумента);
	УстановитьОсновныеРеквизитыДокумента(ДокументОбъект);
	УстановитьВидКорректировкиСогласованноеИзменение(ДокументОбъект);
	
КонецПроцедуры

Процедура ЗаполнитьТабличнуюЧастьКорректировкиПоступления(ДокументОбъект, ТабличнаяЧасть)
	
	ДокументОбъект.Товары.Очистить();
	ДокументОбъект.Услуги.Очистить();
	
	ВладелецНоменклатуры = ДокументОбъект.Контрагент;
	
	Для Каждого СтрокаДанных Из ТабличнаяЧасть Цикл
		
		НоменклатураНаименование = СтрокаДанных.Наименование;
		
		НоменклатураИБ = СтрокаДанных.НоменклатураИнформационнойБазы;
		
		Если ЗначениеЗаполнено(НоменклатураИБ) Тогда
			Номенклатура = НоменклатураИБ.Номенклатура;
			Характеристика= НоменклатураИБ.Характеристика;
		Иначе
			Номенклатура = Неопределено;
			Характеристика= Неопределено;
		КонецЕсли;
		
		РеквизитыНоменклатуры = РеквизитыНоменклатурыИнформационнойБазы(Номенклатура);
		
		Если РеквизитыНоменклатуры.Услуга Тогда
			
			ИмяТЧ = "Услуги";
			
			СтрокаТЧ = ДокументОбъект.Услуги.Добавить();
			СтрокаТЧ.Номенклатура = Номенклатура;
			СтрокаТЧ.Содержание = НоменклатураНаименование;
			СтрокаТЧ.СодержаниеДоИзменения = НоменклатураНаименование;
			
		Иначе
			
			ИмяТЧ = "Товары";
			
			СтрокаТЧ = ДокументОбъект.Товары.Добавить();
			СтрокаТЧ.Номенклатура = Номенклатура;
			СтрокаТЧ.ХарактеристикаНоменклатуры = Характеристика;
			СтрокаТЧ.ЕдиницаИзмерения = ЕдиницаИзмеренияНоменклатуры(Номенклатура, СтрокаДанных.КодЕдИзм);
			СтрокаТЧ.Коэффициент = 1;
			
		КонецЕсли;
		
		СтрокаТЧ.Количество				= СтрокаДанных.Количество;
		СтрокаТЧ.КоличествоДоИзменения	= СтрокаДанных.Количество_До;
		СтрокаТЧ.Цена					= СтрокаДанных.Цена;
		СтрокаТЧ.ЦенаДоИзменения		= СтрокаДанных.Цена_До;
		СтрокаТЧ.Сумма					= СтрокаДанных.СуммаБезНДС;
		СтрокаТЧ.СуммаДоИзменения		= СтрокаДанных.СуммаБезНДС_До;
		СтрокаТЧ.СуммаНДС				= СтрокаДанных.СуммаНДС;
		СтрокаТЧ.СуммаНДСДоИзменения	= СтрокаДанных.СуммаНДС_До;
		СтрокаТЧ.СтавкаНДС				= ПолучитьСтавкуНДСДиадок(СтрокаДанных.СтавкаНДС);
		СтрокаТЧ.СтавкаНДСДоИзменения	= ПолучитьСтавкуНДСДиадок(СтрокаДанных.СтавкаНДС_До);
		
		ЗаполнитьСчетаУчетаСтрокиТабличнойЧасти(ИмяТЧ, СтрокаТЧ, ДокументОбъект);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьШапкуСчетаФактурыПолученного(ДокументОбъект, ПараметрыСоздания, РеквизитыЭлектронногоДокумента)
	
	ЗаполнитьПоДокументуОснованию(ДокументОбъект, ПараметрыСоздания.ДокументОснование);
	УстановитьОрганизациюДокумента(ДокументОбъект, ПараметрыСоздания.Организация);
	УстановитьКонтрагентаДокумента(ДокументОбъект, ПараметрыСоздания.Контрагент);
	УстановитьРеквизитыВходящегоДокумента(ДокументОбъект, РеквизитыЭлектронногоДокумента);
	УстановитьРеквизитыИсправленияВходящегоДокумента(ДокументОбъект, РеквизитыЭлектронногоДокумента);
	УстановитьОсновныеРеквизитыДокумента(ДокументОбъект);
	УстановитьОбщиеРеквизитыСчетаФактуры(ДокументОбъект);
	
КонецПроцедуры

Процедура ЗаполнитьОсновныеРеквизитыСчетаФактурыПолученного(ДокументОбъект, ДанныеЭД, ФорматЭД)

	ЭтоСчетФактураНаАванс	= ДанныеЭД.ЭтоАванс;
	Итоги					= ДанныеЭД.Итоги;

	ДокументОбъект.СуммаДокумента		= Итоги.СуммаВсего;
	ДокументОбъект.СуммаНДСДокумента	= Итоги.СуммаНДС;
	
	Если ЭтоСчетФактураНаАванс Тогда
		
		Для Каждого СтрокаСФ Из ДанныеЭД.ТабличнаяЧасть Цикл
			
			НоваяСтрокаАвансы = ДокументОбъект.Авансы.Добавить();
			
			НоваяСтрокаАвансы.Сумма		= СтрокаСФ.СуммаСНДС;
			НоваяСтрокаАвансы.СуммаНДС	= СтрокаСФ.СуммаНДС;
			НоваяСтрокаАвансы.СтавкаНДС	= ПолучитьСтавкуНДСДиадок(СтрокаСФ.СтавкаНДС);
			
		КонецЦикла;
		
		ДокументОбъект.КодВидаОперации = "02";
		ДокументОбъект.ВидСчетаФактуры = Перечисления.ВидСчетаФактурыПолученного.НаАванс;
		
	Иначе
		
		ДокументОбъект.КодВидаОперации = "01";
		ДокументОбъект.ВидСчетаФактуры = Перечисления.ВидСчетаФактурыПолученного.НаПоступление;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьОсновныеРеквизитыКорректировочногоСчетаФактурыПолученного(ДокументОбъект, ДанныеЭД, ФорматЭД)

	ЭтоСчетФактураНаАванс	= ДанныеЭД.ЭтоАванс;	
	ИтогиУменьшение			= ДанныеЭД.ИтогиУменьшение;
	ИтогиУвеличение			= ДанныеЭД.ИтогиУвеличение;
	
	ДокументОбъект.СуммаУменьшение		= ИтогиУменьшение.СуммаВсего;
	ДокументОбъект.СуммаНДСУменьшение	= ИтогиУменьшение.СуммаНДС;
	
	ДокументОбъект.СуммаУвеличение		= ИтогиУвеличение.СуммаВсего;
	ДокументОбъект.СуммаНДСУвеличение	= ИтогиУвеличение.СуммаНДС;
	
	Если ЭтоСчетФактураНаАванс Тогда
		ДокументОбъект.КодВидаОперации = "02";
		ДокументОбъект.ВидСчетаФактуры = Перечисления.ВидСчетаФактурыПолученного.КорректировочныйНаАванс;
	Иначе
		ДокументОбъект.КодВидаОперации = "01";
		ДокументОбъект.ВидСчетаФактуры = Перечисления.ВидСчетаФактурыПолученного.Корректировочный;
	КонецЕсли;
	
КонецПроцедуры

Процедура УстановитьОбщиеРеквизитыСчетаФактуры(ДокументОбъект)

	ДокументОбъект.КодСпособаПолучения = 2;
	
	Если ДокументОбъект.ДокументыОснования.Количество() = 0 Тогда
		ДокументОбъект.ДокументыОснования.Добавить().ДокументОснование = Неопределено;
	КонецЕсли;

КонецПроцедуры

Процедура ЗаполнитьШапкуПоступленияДопРасходов(ДокументОбъект, ПараметрыСоздания, РеквизитыЭлектронногоДокумента)
	
	УстановитьПараметрыУчетаНДС(ДокументОбъект, ПараметрыСоздания.ЦенаВключаетНДС);
	УстановитьОрганизациюДокумента(ДокументОбъект, ПараметрыСоздания.Организация);
	УстановитьКонтрагентаДокумента(ДокументОбъект, ПараметрыСоздания.Контрагент);
	УстановитьДоговорСПоставщиком(ДокументОбъект);
	
	УстановитьРеквизитыВходящегоДокумента(ДокументОбъект, РеквизитыЭлектронногоДокумента);
	
	УстановитьВидОперацииПоступленияДопРасходов(ДокументОбъект);
	УстановитьПараметрыОтраженияДокументаВУчете(ДокументОбъект);
	УстановитьОсновныеРеквизитыДокумента(ДокументОбъект);
	УстановитьПодразделениеДокумента(ДокументОбъект);
	УстановитьВалютуДокумента(ДокументОбъект);
	
	ЗаполнитьСчетаУчетаРасчетов(ДокументОбъект);
	
КонецПроцедуры

Процедура ЗаполнитьШапкуПоступленияДопРасходовПоТабличнойЧасти(ДокументОбъект, ТабличнаяЧастьЭД)
	
	Если Не ЗначениеЗаполнено(ТабличнаяЧастьЭД) Тогда
		Возврат;
	КонецЕсли;
	
	СуммаСНалогом = 0;
	СуммаНалога = 0;
	
	ТипЧисло = Новый ОписаниеТипов("Число");
	
	Для Каждого СтрокаДанных Из ТабличнаяЧастьЭД Цикл
		
		СуммаСНалогом = СуммаСНалогом + ТипЧисло.ПривестиЗначение(СтрокаДанных.СуммаСНДС);
		СуммаНалога = СуммаНалога + ТипЧисло.ПривестиЗначение(СтрокаДанных.СуммаНДС);
		
	КонецЦикла;
	
	СуммаБезНалога = СуммаСНалогом - СуммаНалога;
	
	ПерваяСтрокаТЧ = ТабличнаяЧастьЭД[0];
	ДокументОбъект.СтавкаНДС = ПолучитьСтавкуНДСДиадок(ПерваяСтрокаТЧ.СтавкаНДС);
	ДокументОбъект.Содержание = ПерваяСтрокаТЧ.Наименование;
	ДокументОбъект.СуммаДокумента = СуммаСНалогом;
	ДокументОбъект.СуммаНДС = СуммаНалога;
	ДокументОбъект.Сумма = СуммаБезНалога;
	
	Если ДокументОбъект.СуммаВключаетНДС Тогда
		ДокументОбъект.Сумма = СуммаСНалогом;
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьШапкуАктаСверкиВзаиморасчетов(ДокументОбъект, ПараметрыСоздания, РеквизитыЭлектронногоДокумента, ИтогиТЧ)
	
	УстановитьОрганизациюДокумента(ДокументОбъект, ПараметрыСоздания.Организация);
	УстановитьКонтрагентаДокумента(ДокументОбъект, ПараметрыСоздания.Контрагент);
	
	УстановитьОсновныеРеквизитыДокумента(ДокументОбъект);
	
	УстановитьРеквизитыСверкиВзаиморасчетов(ДокументОбъект, РеквизитыЭлектронногоДокумента, ИтогиТЧ);
	
КонецПроцедуры

Процедура ЗаполнитьТабличнуюЧастьАктаСверкиВзаиморасчетов(ДокументОбъект, ТабличнаяЧастьЭД)
	
	Для Каждого СтрокаДоговора Из ТабличнаяЧастьЭД.Договор Цикл
		
		ДоговорКонтрагента = ДоговорПоСтрокеАктаСверки(ДокументОбъект, СтрокаДоговора);
		
		Для Каждого СтрокаДокумента Из СтрокаДоговора.Документ Цикл
			
			Для Каждого СтрокаОперации Из СтрокаДокумента.Операция Цикл
				
				СтрокаПоДаннымКонтрагента = ДокументОбъект.ПоДаннымКонтрагента.Добавить();
				СтрокаПоДаннымКонтрагента.ДоговорКонтрагента = ДоговорКонтрагента;
				СтрокаПоДаннымКонтрагента.Дата = СтрокаОперации.Дата;
				СтрокаПоДаннымКонтрагента.Представление = СтрокаОперации.Наименование;
				СтрокаПоДаннымКонтрагента.Дебет = СтрокаОперации.СуммаДебет;
				СтрокаПоДаннымКонтрагента.Кредит = СтрокаОперации.СуммаКредит;
				
			КонецЦикла;
			
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура УстановитьРеквизитыСверкиВзаиморасчетов(ДокументОбъект, РеквизитыЭлектронногоДокумента, ИтогиТЧ)
	
	ДокументОбъект.ВалютаДокумента = ВалютаРегламентированногоУчета();
	ДокументОбъект.ОстатокНаНачало = ИтогиТЧ.СальдоНачДеб - ИтогиТЧ.СальдоНачКр;
	ДокументОбъект.ДоговорКонтрагента = Справочники.ДоговорыКонтрагентов.ПустаяСсылка();
	ДокументОбъект.СверкаСогласована = Ложь;
	
	ДокументОбъект.ДатаНачала = РеквизитыЭлектронногоДокумента.ДатаНачала;
	ДокументОбъект.Датаокончания = РеквизитыЭлектронногоДокумента.Датаокончания;
	
КонецПроцедуры

Функция ДоговорПоСтрокеАктаСверки(ДокументОбъект, ДанныеДоговора)
	
	Результат = Справочники.ДоговорыКонтрагентов.ПустаяСсылка();
	
	ВалютаДоговора = ДокументОбъект.ВалютаДокумента;
	Организация = ДокументОбъект.Организация;
	Контрагент = ДокументОбъект.Контрагент;
	НомерДоговора = ДанныеДоговора.Номер;
	ДатаДоговора = ДанныеДоговора.Дата;
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса_ДоговорПоСтрокеАктаСверки();
	
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("Контрагент", Контрагент);
	Запрос.УстановитьПараметр("Дата", ДатаДоговора);
	Запрос.УстановитьПараметр("Номер", НомерДоговора);
	Запрос.УстановитьПараметр("ВалютаВзаиморасчетов", ВалютаДоговора);
	
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	
	Если Выборка.Следующий() Тогда
		Результат = Выборка.Ссылка;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ДоговорПоСтрокеАктаСверки()
	
	Результат =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	|	ТаблицаДоговорыКонтрагентов.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.ДоговорыКонтрагентов КАК ТаблицаДоговорыКонтрагентов
	|ГДЕ
	|	ТаблицаДоговорыКонтрагентов.Организация = &Организация
	|	И ТаблицаДоговорыКонтрагентов.Владелец = &Контрагент
	|	И ТаблицаДоговорыКонтрагентов.Дата = &Дата
	|	И ТаблицаДоговорыКонтрагентов.Номер = &Номер
	|	И ТаблицаДоговорыКонтрагентов.ВалютаВзаиморасчетов = &ВалютаВзаиморасчетов
	|	И НЕ ТаблицаДоговорыКонтрагентов.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьШапкуЗаказаПоставщику(ДокументОбъект, ПараметрыСоздания, ДанныеШапки, ИтогиТЧ)
	
	УстановитьОрганизациюДокумента(ДокументОбъект, ПараметрыСоздания.Организация);
	УстановитьКонтрагентаДокумента(ДокументОбъект, ПараметрыСоздания.Контрагент);
	УстановитьОсновныеРеквизитыДокумента(ДокументОбъект);
	УстановитьДоговорСПоставщиком(ДокументОбъект);
	
	ДокументОбъект.Склад = СкладПоУмолчанию();
	ДокументОбъект.НомерПоДаннымПоставщика = ДанныеШапки.Дата;
	ДокументОбъект.ДатаПоДаннымПоставщика = ДанныеШапки.Номер;
	ДокументОбъект.ДатаОплаты = ДанныеШапки.ДатаОплаты;
	ДокументОбъект.ДатаПоступления = ДанныеШапки.ДатаПоступления;
	ДокументОбъект.КурсВзаиморасчетов = ДанныеШапки.КурсВзаиморасчетов;
	ДокументОбъект.СуммаВключаетНДС = ПараметрыСоздания.ЦенаВключаетНДС;
	ДокументОбъект.ВидОперации = Перечисления.ВидыОперацийЗаказПоставщику.ПокупкаКомиссия;
	ДокументОбъект.ВалютаДокумента = Справочники.Валюты.НайтиПоКоду(ДанныеШапки.КодВалюты);
	
	Если ЗначениеЗаполнено(ДанныеШапки.НомерСчетаОрганизации) Тогда
		ДокументОбъект.СтруктурнаяЕдиница =
			БанковскийСчетОрганизации(ДанныеШапки.НомерСчетаОрганизации);
	КонецЕсли;
	
	Если ПараметрыСоздания.ЦенаВключаетНДС Тогда
		ДокументОбъект.СуммаДокумента = ИтогиТЧ.СуммаВсего;
	Иначе
		ДокументОбъект.СуммаДокумента = ИтогиТЧ.СуммаБезНДС;
	КонецЕсли;
	
КонецПроцедуры

Функция БанковскийСчетОрганизации(НомерСчета)
	
	Результат = Неопределено;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	|	БанковскиеСчета.Ссылка КАК Счет
	|ИЗ
	|	Справочник.БанковскиеСчета КАК БанковскиеСчета
	|ГДЕ
	|	БанковскиеСчета.НомерСчета = &НомерСчета
	|	И НЕ БанковскиеСчета.ПометкаУдаления";
	
	Запрос.УстановитьПараметр("НомерСчета", НомерСчета);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда 
		Результат = Выборка.Счет;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьТабличнуюЧастьЗаказаПоставщику(ДокументОбъект, ТабличнаяЧастьЭД)
	
	Для Каждого СтрокаДанных Из ТабличнаяЧастьЭД Цикл
		
		НоменклатураИБ = СтрокаДанных.НоменклатураИнформационнойБазы;
		
		Если ЗначениеЗаполнено(НоменклатураИБ) Тогда
			Номенклатура = НоменклатураИБ.Номенклатура;
		Иначе
			Номенклатура = Неопределено;
		КонецЕсли;
		
		ЭтоУслуга = ЭтоНоменклатураУслуга(Номенклатура);
		
		Если ЭтоУслуга Тогда
			
			СтрокаТЧ = ДокументОбъект.Услуги.Добавить();
			СтрокаТЧ.Номенклатура = Номенклатура;
			СтрокаТЧ.Содержание = СтрокаДанных.Наименование;
			Количество = СтрокаДанных.Количество;
			Если Не ЗначениеЗаполнено(Количество) Тогда
				Количество = 1;
			КонецЕсли;
			
			СтрокаТЧ.Количество = Количество;
			
		Иначе
			
			СтрокаТЧ = ДокументОбъект.Товары.Добавить();
			СтрокаТЧ.Номенклатура = Номенклатура;
			СтрокаТЧ.ЕдиницаИзмерения = ЕдиницаИзмеренияНоменклатуры(Номенклатура, СтрокаДанных.КодЕдИзм);
			СтрокаТЧ.ЕдиницаИзмеренияМест = ЕдиницаИзмеренияНоменклатуры(Номенклатура, СтрокаДанных.КодЕдИзм);
			СтрокаТЧ.Коэффициент = 1;
			Количество = СтрокаДанных.Количество;
			Если Не ЗначениеЗаполнено(Количество) Тогда
				Количество = 1;
			КонецЕсли;
			СтрокаТЧ.Количество = Количество;
			СтрокаТЧ.КоличествоМест = Количество;
			
		КонецЕсли;
		
		Сумма = СтрокаДанных.СуммаБезНДС;
		Цена = СтрокаДанных.Цена;
		
		Если ДокументОбъект.СуммаВключаетНДС Тогда
			Сумма = СтрокаДанных.СуммаСНДС;
			Цена = Сумма / СтрокаТЧ.Количество;
		КонецЕсли;
		
		СтрокаТЧ.Цена = Цена;
		СтрокаТЧ.Сумма = Сумма;
		СтрокаТЧ.СуммаНДС = СтрокаДанных.СуммаНДС;
		СтрокаТЧ.СтавкаНДС = ПолучитьСтавкуНДСДиадок(СтрокаДанных.СтавкаНДС);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьШапкуЗаявкиНаРасходованиеСредств(ДокументОбъект, ПараметрыСоздания, ДанныеШапки, ИтогиТЧ)
	
	УстановитьОрганизациюДокумента(ДокументОбъект, ПараметрыСоздания.Организация);
	УстановитьКонтрагентаДокумента(ДокументОбъект, ПараметрыСоздания.Контрагент);
	УстановитьОсновныеРеквизитыДокумента(ДокументОбъект);
	
	ДокументОбъект.ДатаРасхода = ДанныеШапки.ДатаОплаты;
	ДокументОбъект.ВидОперации = Перечисления.ВидыОперацийЗаявкиНаРасходование.ОплатаПоставщику;
	ДокументОбъект.ВалютаДокумента = Справочники.Валюты.НайтиПоКоду(ДанныеШапки.КодВалюты);
	ДокументОбъект.ФормаОплаты = Перечисления.ВидыДенежныхСредств.Безналичные;
	ДокументОбъект.КурсДокумента = ДанныеШапки.КурсВзаиморасчетов;
	ДокументОбъект.НазначениеПлатежа = ДанныеШапки.НазначениеПлатежа;
	ДокументОбъект.ИдентификаторПлатежа = ДанныеШапки.ИдентификаторПлатежа;
	ДокументОбъект.СуммаДокумента = ИтогиТЧ.СуммаВсего;
	
	Если ЗначениеЗаполнено(ДанныеШапки.НомерСчетаОрганизации) Тогда
		ДокументОбъект.БанковскийСчетКасса = БанковскийСчетОрганизации(ДанныеШапки.НомерСчетаОрганизации);
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьТабличнуюЧастьЗаявкиНаРасходованиеСредств(ДокументОбъект, ПараметрыСоздания, ДанныеШапки, ИтогиТЧ)
	
	СтрокаТЧ = ДокументОбъект.РасшифровкаПлатежа.Добавить();
	
	СтрокаТЧ.КурсВзаиморасчетов = ДанныеШапки.КурсВзаиморасчетов;
	СтрокаТЧ.СуммаПлатежа = ИтогиТЧ.СуммаВсего;
	СтрокаТЧ.СуммаВзаиморасчетов = ИтогиТЧ.СуммаВсего;
	
	УстановитьДоговорСПоставщикомВСтрокеТабличнойЧасти(СтрокаТЧ, ДокументОбъект);
	
КонецПроцедуры

Процедура ЗаполнитьШапкуРасходногоКассовогоОрдера(ДокументОбъект, ПараметрыСоздания, ДанныеШапки, ИтогиТЧ)
	
	УстановитьОрганизациюДокумента(ДокументОбъект, ПараметрыСоздания.Организация);
	УстановитьКонтрагентаДокумента(ДокументОбъект, ПараметрыСоздания.Контрагент);
	УстановитьОсновныеРеквизитыДокумента(ДокументОбъект);
	УстановитьДоговорСПоставщиком(ДокументОбъект);
	
	ДокументОбъект.ВалютаДокумента = Справочники.Валюты.НайтиПоКоду(ДанныеШапки.КодВалюты);
	ДокументОбъект.ОтражатьВУправленческомУчете = Истина;
	ДокументОбъект.ОтражатьВБухгалтерскомУчете = Ложь;
	ДокументОбъект.ВидОперации = Перечисления.ВидыОперацийРКО.ОплатаПоставщику;
	ДокументОбъект.СуммаДокумента = ИтогиТЧ.СуммаВсего;
	ДокументОбъект.ВидВыдачиДенежныхСредств = Перечисления.ВидВыдачиДенежныхСредств.КассеККМ;
	ДокументОбъект.Выдать = ДанныеШапки.НаименованиеПолучателя;
	
	Если ЗначениеЗаполнено(ДанныеШапки.НомерСчетаОрганизации) Тогда
		ДокументОбъект.СчетОрганизации =
			БанковскийСчетОрганизации(ДанныеШапки.НомерСчетаОрганизации);
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьТабличнуюЧастьРасходногоКассовогоОрдера(ДокументОбъект, ПараметрыСоздания, ДанныеШапки, ИтогиТЧ)
	
	СтрокаТЧ = ДокументОбъект.РасшифровкаПлатежа.Добавить();
	
	СтрокаТЧ.КурсВзаиморасчетов = ДанныеШапки.КурсВзаиморасчетов;
	СтрокаТЧ.СуммаПлатежа = ИтогиТЧ.СуммаВсего;
	СтрокаТЧ.СуммаВзаиморасчетов = ИтогиТЧ.СуммаВсего;
	СтрокаТЧ.СуммаНДС = ИтогиТЧ.СуммаНДС;
	
	УстановитьДоговорСПоставщикомВСтрокеТабличнойЧасти(СтрокаТЧ, ДокументОбъект);
	
КонецПроцедуры

Процедура ЗаполнитьШапкуСчетаНаОплатуПоставщика(ДокументОбъект, ПараметрыСоздания, ДанныеШапки, ИтогиТЧ)
	
	УстановитьОрганизациюДокумента(ДокументОбъект, ПараметрыСоздания.Организация);
	УстановитьКонтрагентаДокумента(ДокументОбъект, ПараметрыСоздания.Контрагент);
	УстановитьОсновныеРеквизитыДокумента(ДокументОбъект);
	УстановитьДоговорСПоставщиком(ДокументОбъект);
	
	ДокументОбъект.ВалютаДокумента = Справочники.Валюты.НайтиПоКоду(ДанныеШапки.КодВалюты);
	ДокументОбъект.ДатаОплаты = ДанныеШапки.ДатаОплаты;
	ДокументОбъект.НомерВходящегоДокумента = ДанныеШапки.Номер;
	ДокументОбъект.ДатаВходящегоДокумента = ДанныеШапки.Дата;
	ДокументОбъект.ДатаПоступления = ДанныеШапки.ДатаПоступления;
	ДокументОбъект.КурсВзаиморасчетов = ДанныеШапки.КурсВзаиморасчетов;
	ДокументОбъект.СуммаВключаетНДС = ПараметрыСоздания.ЦенаВключаетНДС;
	ДокументОбъект.КратностьВзаиморасчетов = 1;
	
	Если ПараметрыСоздания.ЦенаВключаетНДС Тогда
		ДокументОбъект.СуммаДокумента = ИтогиТЧ.СуммаВсего;
	Иначе
		ДокументОбъект.СуммаДокумента = ИтогиТЧ.СуммаБезНДС;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДанныеШапки.НомерСчетаОрганизации) Тогда
		ДокументОбъект.СтруктурнаяЕдиница =
			БанковскийСчетОрганизации(ДанныеШапки.НомерСчетаОрганизации);
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьТабличнуюЧастьСчетаНаОплатуПоставщика(ДокументОбъект, ТабличнаяЧастьЭД)
	
		Для Каждого СтрокаДанных Из ТабличнаяЧастьЭД Цикл
		
		НоменклатураИБ = СтрокаДанных.НоменклатураИнформационнойБазы;
		
		Если ЗначениеЗаполнено(НоменклатураИБ) Тогда
			Номенклатура = НоменклатураИБ.Номенклатура;
		Иначе
			Номенклатура = Неопределено;
		КонецЕсли;
		
		ЭтоУслуга = ЭтоНоменклатураУслуга(Номенклатура);
		
		Если ЭтоУслуга Тогда
			
			СтрокаТЧ = ДокументОбъект.Услуги.Добавить();
			СтрокаТЧ.Номенклатура = Номенклатура;
			СтрокаТЧ.Содержание = СтрокаДанных.Наименование;
			Количество = СтрокаДанных.Количество;
			Если Не ЗначениеЗаполнено(Количество) Тогда
				Количество = 1;
			КонецЕсли;
			
			СтрокаТЧ.Количество = Количество;
			
		Иначе
			
			СтрокаТЧ = ДокументОбъект.Товары.Добавить();
			СтрокаТЧ.Номенклатура = Номенклатура;
			СтрокаТЧ.ЕдиницаИзмерения = ЕдиницаИзмеренияНоменклатуры(Номенклатура, СтрокаДанных.КодЕдИзм);
			СтрокаТЧ.ЕдиницаИзмеренияМест = ЕдиницаИзмеренияНоменклатуры(Номенклатура, СтрокаДанных.КодЕдИзм);
			СтрокаТЧ.Коэффициент = 1;
			Количество = СтрокаДанных.Количество;
			Если Не ЗначениеЗаполнено(Количество) Тогда
				Количество = 1;
			КонецЕсли;
			СтрокаТЧ.Количество = Количество;
			СтрокаТЧ.КоличествоМест = Количество;
			
		КонецЕсли;
		
		Сумма = СтрокаДанных.СуммаБезНДС;
		Цена = СтрокаДанных.Цена;
		
		Если ДокументОбъект.СуммаВключаетНДС Тогда
			Сумма = СтрокаДанных.СуммаСНДС;
			Цена = Сумма / СтрокаТЧ.Количество;
		КонецЕсли;
		
		СтрокаТЧ.Цена = Цена;
		СтрокаТЧ.Сумма = Сумма;
		СтрокаТЧ.СуммаНДС = СтрокаДанных.СуммаНДС;
		СтрокаТЧ.СтавкаНДС = ПолучитьСтавкуНДСДиадок(СтрокаДанных.СтавкаНДС);
		
	КонецЦикла;
	
КонецПроцедуры

Функция РеквизитыНоменклатурыИнформационнойБазы(Номенклатура)
	
	ТребуемыеРеквизиты = Новый Структура;
	ТребуемыеРеквизиты.Вставить("Услуга");
	ТребуемыеРеквизиты.Вставить("СтатьяЗатрат");
	ТребуемыеРеквизиты.Вставить("НоменклатурнаяГруппа");
	
	Если ЗначениеЗаполнено(Номенклатура) Тогда
		Результат = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Номенклатура, ТребуемыеРеквизиты);
	Иначе
		Результат = ТребуемыеРеквизиты;
		Результат.Услуга = Истина;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоНоменклатураУслуга(Номенклатура)
	
	Результат = Ложь;
	
	Если ЗначениеЗаполнено(Номенклатура) Тогда
		Результат = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Номенклатура, "Услуга");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЕдиницаИзмеренияПоКлассификатору(КодЕИ)
	
	Результат = Справочники.КлассификаторЕдиницИзмерения.НайтиПоКоду(КодЕИ);
	Возврат Результат;
	
КонецФункции

Функция ЕдиницаИзмеренияНоменклатуры(Номенклатура, КодЕИ)
	
	ЕдиницаПоОКЕИ = ЕдиницаИзмеренияПоКлассификатору(КодЕИ);
	
	Результат = Справочники.ЕдиницыИзмерения.НайтиПоРеквизиту(
		"ЕдиницаПоКлассификатору",
		ЕдиницаПоОКЕИ,
		Неопределено,
		Номенклатура
	);
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьПоДокументуОснованию(ДокументОбъект, ДокументОснование)
	
	Если Не ЗначениеЗаполнено(ДокументОснование) Тогда
		Возврат;
	КонецЕсли;
	
	ДокументОбъект.Заполнить(ДокументОснование);
	
КонецПроцедуры

Процедура УстановитьОрганизациюДокумента(ДокументОбъект, Знач Организация)
	
	Если Не ЗначениеЗаполнено(Организация) Тогда
		Организация = ОрганизацияПоУмолчанию();
	КонецЕсли;
	
	ДокументОбъект.Организация = Организация;
	
КонецПроцедуры

Процедура УстановитьКонтрагентаДокумента(ДокументОбъект, Знач Контрагент)
	
	Если Не ЗначениеЗаполнено(Контрагент) Тогда
		Контрагент = ПоставщикПоУмолчанию();
	КонецЕсли;
	
	ДокументОбъект.Контрагент = Контрагент;
	
КонецПроцедуры

Процедура УстановитьПодразделениеДокумента(ДокументОбъект)
	
	ДокументОбъект.Подразделение = ПодразделениеПоУмолчанию();
	
КонецПроцедуры

Процедура УстановитьСкладДокумента(ДокументОбъект)
	
	ДокументОбъект.ВидПоступления = Перечисления.ВидыПоступленияТоваров.НаСклад;
	ДокументОбъект.СкладОрдер = СкладПоУмолчанию();
	
КонецПроцедуры

Процедура УстановитьВидОперацииПоступленияДопРасходов(ДокументОбъект)
	
	ДокументОбъект.ВидОперации = Перечисления.ВидыОперацийПоступлениеДопРасходов.УслугаСтороннейОрганизации;
	
КонецПроцедуры

Процедура УстановитьПараметрыОтраженияДокументаВУчете(ДокументОбъект)
	
	ДокументОбъект.ОтражатьВУправленческомУчете	= ОтражатьДокументыВУправленческомУчетеПоУмолчанию();
	ДокументОбъект.ОтражатьВБухгалтерскомУчете	= ОтражатьДокументыВБухгалтерскомУчетеПоУмолчанию();
	ДокументОбъект.ОтражатьВНалоговомУчете		= ОтражатьДокументыВНалоговомУчетеПоУмолчанию();
	
КонецПроцедуры

Процедура УстановитьВалютуДокумента(ДокументОбъект)
	
	ДокументОбъект.ВалютаДокумента = ВалютаРегламентированногоУчета();
	ДокументОбъект.КурсВзаиморасчетов = 1;
	ДокументОбъект.КратностьВзаиморасчетов = 1;
	
КонецПроцедуры

Процедура УстановитьОсновныеРеквизитыДокумента(ДокументОбъект)
	
	ДокументОбъект.Дата = ТекущаяДата();
	ДокументОбъект.Ответственный = ТекущийПользователь();
	
КонецПроцедуры

Процедура УстановитьРеквизитыВходящегоДокумента(ДокументОбъект, РеквизитыЭлектронногоДокумента)
	
	ДокументОбъект.ДатаВходящегоДокумента	= РеквизитыЭлектронногоДокумента.Дата;
	ДокументОбъект.НомерВходящегоДокумента	= РеквизитыЭлектронногоДокумента.Номер;
	
КонецПроцедуры

Процедура УстановитьРеквизитыИсправленияВходящегоДокумента(ДокументОбъект, РеквизитыЭлектронногоДокумента)

	ДокументОбъект.НомерИсправления	= РеквизитыЭлектронногоДокумента.НомерИсправления;
	ДокументОбъект.ДатаИсправления	= РеквизитыЭлектронногоДокумента.ДатаИсправления;
	
	Если ЗначениеЗаполнено(ДокументОбъект.НомерИсправления)
		И ЗначениеЗаполнено(ДокументОбъект.ДатаИсправления) Тогда
		ДокументОбъект.Исправление = Истина;
	КонецЕсли;
	
КонецПроцедуры

Процедура УстановитьДоговорСПоставщиком(ДокументОбъект)
	
	ВидыДоговоров = Новый Массив;
	ВидыДоговоров.Добавить(Перечисления.ВидыДоговоровКонтрагентов.СПоставщиком);
	
	УправлениеВзаиморасчетами.УстановитьДоговорКонтрагента(
		ДокументОбъект.ДоговорКонтрагента,
		ДокументОбъект.Контрагент,
		ДокументОбъект.Организация,
		ВидыДоговоров
	);
	
КонецПроцедуры

Процедура УстановитьДоговорСПоставщикомВСтрокеТабличнойЧасти(СтрокаТЧ, ДокументОбъект)
	
	ВидыДоговоров = Новый Массив;
	ВидыДоговоров.Добавить(Перечисления.ВидыДоговоровКонтрагентов.СПоставщиком);
	
	УправлениеВзаиморасчетами.УстановитьДоговорКонтрагента(
		СтрокаТЧ.ДоговорКонтрагента,
		ДокументОбъект.Контрагент,
		ДокументОбъект.Организация,
		ВидыДоговоров
	);
	
КонецПроцедуры

Процедура УстановитьТипЦенДокумента(ДокументОбъект)
	
	Если НЕ ЕстьРеквизитМетаданных(ДокументОбъект.Метаданные(), "ДоговорКонтрагента") Тогда
		Возврат;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ДокументОбъект.ДоговорКонтрагента) Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполнитьЗначенияСвойств(ДокументОбъект, ДокументОбъект.ДоговорКонтрагента, "ТипЦен");
	
КонецПроцедуры

Процедура УстановитьПараметрыУчетаНДС(ДокументОбъект, ЦенаВключаетНДС)
	
	ДокументОбъект.УчитыватьНДС = Истина;
	ДокументОбъект.СуммаВключаетНДС = ЦенаВключаетНДС;
	
КонецПроцедуры

Процедура УстановитьВидПоступленияПокупка(ДокументОбъект)
	
	ДокументОбъект.ВидОперации = Перечисления.ВидыОперацийПоступлениеТоваровУслуг.ПокупкаКомиссия;
	
КонецПроцедуры

Процедура УстановитьВидКорректировкиСогласованноеИзменение(ДокументОбъект)
	
	ДокументОбъект.ВидОперации = Перечисления.ВидыОперацийИсправленияПоступленияРеализации.СогласованноеИзменение;
	
КонецПроцедуры

Процедура ПроверитьЗаполнитьОснованиеКорректировкиПоступления(ПараметрыСоздания, ШапкаЭД, ФорматЭД)
	
	Если ЗначениеЗаполнено(ПараметрыСоздания.ДокументОснование) Тогда
		Возврат;
	КонецЕсли;
	
	ФорматыЭД = МодульФорматыЭлектронныхДокументов();
	
	ТипыОснований = Новый Массив;
	
	Если ФорматыЭД.ЭтоУниверсальныйКорректировочныйДокумент(ФорматЭД)
		Или ФорматыЭД.ЭтоКорректировочныйСчетФактура(ФорматЭД) Тогда
		
		ТипыОснований.Добавить("СчетФактураПолученный");
		
	ИначеЕсли ФорматыЭД.ЭтоСоглашениеОбИзмененииСтоимости(ФорматЭД) Тогда
		
		ТипыОснований.Добавить("ПоступлениеТоваровУслуг");
		ТипыОснований.Добавить("ПоступлениеДопРасходов");
		
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ТипыОснований) Тогда
		Возврат;
	КонецЕсли;
	
	Организация = ПараметрыСоздания.Организация;
	Контрагент = ПараметрыСоздания.Контрагент;
	НомерЭД = ШапкаЭД.Номер;
	ДатаЭД = ШапкаЭД.Дата;
	
	Для Каждого ВидДокумента Из ТипыОснований Цикл
		
		СсылкаНаОбъект = НайтиДокументПоРеквизитам(
			ВидДокумента,
			Организация,
			Контрагент,
			ДатаЭД,
			НомерЭД
		);
		
		Если ЗначениеЗаполнено(СсылкаНаОбъект) Тогда
			
			Если ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.СчетФактураПолученный") Тогда
				СсылкаНаОбъект = СсылкаНаОбъект.ДокументОснование;
			КонецЕсли;
			
			ПараметрыСоздания.ДокументОснование = СсылкаНаОбъект;
			Прервать;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция НайтиДокументПоРеквизитам(ВидДокумента, Организация, Контрагент, ДатаВходящегоДокумента, НомерВходящегоДокумента)
	
	ШаблонЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	|	Таблица.Ссылка КАК Ссылка,
	|	Таблица.Дата КАК Дата
	|ИЗ
	|	&ПолноеИмяТаблицы КАК Таблица
	|ГДЕ
	|	НЕ Таблица.ПометкаУдаления
	|	И Таблица.Организация = &Организация
	|	И Таблица.Контрагент = &Контрагент
	|	И Таблица.ДатаВходящегоДокумента = &ДатаДокумента
	|	И Таблица.НомерВходящегоДокумента = &НомерДокумента
	|
	|УПОРЯДОЧИТЬ ПО
	|	Дата УБЫВ";
	
	ПолноеИмяТаблицы = "Документ." + ВидДокумента;
	ТекстЗапроса = СтрЗаменить(ШаблонЗапроса, "&ПолноеИмяТаблицы", ПолноеИмяТаблицы);
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("Контрагент", Контрагент);
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("ДатаДокумента", ДатаВходящегоДокумента);
	Запрос.УстановитьПараметр("НомерДокумента", НомерВходящегоДокумента);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Результат = Неопределено;
	Если Выборка.Следующий() Тогда
		Результат = Выборка.Ссылка;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьСчетаУчетаРасчетов(ДокументОбъект)
	
	СчетаРасчетов = БухгалтерскийУчетРасчетовСКонтрагентами.ПолучитьСчетаРасчетовСКонтрагентом(
		ДокументОбъект.Организация,
		ДокументОбъект.Контрагент,
		ДокументОбъект.ДоговорКонтрагента
	);
	
	ДокументОбъект.СчетУчетаРасчетовСКонтрагентом = СчетаРасчетов.СчетРасчетов;
	ДокументОбъект.СчетУчетаРасчетовПоАвансам = СчетаРасчетов.СчетАвансов;
	
КонецПроцедуры

Процедура ЗаполнитьСчетаУчетаСтрокиТабличнойЧасти(ИмяТЧ, СтрокаТЧ, ДокументОбъект)
	
	СчетаУчетаВДокументах.ЗаполнитьСчетаУчетаТабличнойЧасти(
		ИмяТЧ,
		СтрокаТЧ,
		ДокументОбъект,
		ДокументОбъект.ОтражатьВБухгалтерскомУчете,
		ДокументОбъект.ОтражатьВНалоговомУчете
	);
	
КонецПроцедуры

Функция ВалютаРегламентированногоУчета()
	
	Результат = Константы.ВалютаРегламентированногоУчета.Получить();
	Возврат Результат;
	
КонецФункции

Функция ТекущийПользователь()
	
	Возврат ПараметрыСеанса.ТекущийПользователь;
	
КонецФункции

Функция ОрганизацияПоУмолчанию()
	
	Пользователь = ТекущийПользователь();
	Результат = УправлениеПользователями.ПолучитьЗначениеПоУмолчанию(Пользователь, "ОсновнаяОрганизация");
	
	Возврат Результат;
	
КонецФункции

Функция ПоставщикПоУмолчанию()
	
	Пользователь = ТекущийПользователь();
	Результат = УправлениеПользователями.ПолучитьЗначениеПоУмолчанию(Пользователь, "ОсновнойПоставщик");
	
	Возврат Результат;
	
КонецФункции

Функция ПодразделениеПоУмолчанию()
	
	Пользователь = ТекущийПользователь();
	Результат = УправлениеПользователями.ПолучитьЗначениеПоУмолчанию(Пользователь, "ОсновноеПодразделение");
	
	Возврат Результат;
	
КонецФункции

Функция СкладПоУмолчанию()
	
	Пользователь = ТекущийПользователь();
	Результат = УправлениеПользователями.ПолучитьЗначениеПоУмолчанию(Пользователь, "ОсновнойСклад");
	
	Возврат Результат;
	
КонецФункции

Функция ОтражатьДокументыВУправленческомУчетеПоУмолчанию()
	
	Пользователь = ТекущийПользователь();
	Результат = УправлениеПользователями.ПолучитьЗначениеПоУмолчанию(Пользователь, "ОтражатьДокументыВУправленческомУчете");
	
	Возврат Результат;
	
КонецФункции

Функция ОтражатьДокументыВБухгалтерскомУчетеПоУмолчанию()
	
	Пользователь = ТекущийПользователь();
	Результат = УправлениеПользователями.ПолучитьЗначениеПоУмолчанию(Пользователь, "ОтражатьДокументыВБухгалтерскомУчете");
	
	Возврат Результат;
	
КонецФункции

Функция ОтражатьДокументыВНалоговомУчетеПоУмолчанию()
	
	Пользователь = ТекущийПользователь();
	Результат = УправлениеПользователями.ПолучитьЗначениеПоУмолчанию(Пользователь, "ОтражатьДокументыВНалоговомУчете");
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьНоменклатуруИнформационнойБазы(Владелец, ИдентификаторНоменклатуры)
	
	ТаблицаСопоставленияНоменклатуры = ПолучитьТаблицуСопоставленияИзХранилища(Владелец);
	
	СтрокаПоиска = ТаблицаСопоставленияНоменклатуры.Найти(ИдентификаторНоменклатуры, "Идентификатор");
	
	Если НЕ СтрокаПоиска = Неопределено
		И НЕ ОбщегоНазначения.СсылкаСуществует(СтрокаПоиска.Номенклатура) Тогда
		ТаблицаСопоставленияНоменклатуры.Удалить(СтрокаПоиска);
		СтрокаПоиска = Неопределено;
	КонецЕсли;
	
	Результат = Неопределено;
	
	Если НЕ СтрокаПоиска = Неопределено Тогда
		
		Результат = НоваяНоменклатураИнформационнойБазы(
				СтрокаПоиска.Номенклатура,
				СтрокаПоиска.Характеристика,
				СтрокаПоиска.Упаковка
			);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ДобавитьСопоставлениеНоменклатуры(НоменклатураКонтрагента, Номенклатура, Характеристика)
	
	Владелец = НоменклатураКонтрагента.Владелец;
	
	ТаблицаСопоставленияНоменклатуры = ПолучитьТаблицуСопоставленияИзХранилища(Владелец);
	Идентификатор = НоменклатураКонтрагента.Идентификатор;
	
	СтрокаТаблицы = ТаблицаСопоставленияНоменклатуры.Найти(Идентификатор, "Идентификатор");
	Если СтрокаТаблицы = Неопределено Тогда
		СтрокаТаблицы = ТаблицаСопоставленияНоменклатуры.Добавить();
	КонецЕсли;
	
	СтрокаТаблицы.Идентификатор = Идентификатор;
	СтрокаТаблицы.Номенклатура = Номенклатура;
	СтрокаТаблицы.Характеристика = Характеристика;
	
	ЗаписатьСопоставлениеНоменклатурыВХранилище(Владелец, ТаблицаСопоставленияНоменклатуры);
	
КонецПроцедуры

Процедура ЗаписатьСопоставлениеНоменклатурыВХранилище(Владелец, ТаблицаСопоставленияНоменклатуры)
	
	ХранилищеСопоставления = НайтиСоздатьХранилищеСопоставленияНоменклатуры(Владелец);
	
	ХранилищеСопоставленияОбъект = ХранилищеСопоставления.ПолучитьОбъект();
	ХранилищеСопоставленияОбъект.Хранилище = Новый ХранилищеЗначения(ТаблицаСопоставленияНоменклатуры);
	ХранилищеСопоставленияОбъект.Записать();
	
КонецПроцедуры

Функция ПолучитьТаблицуСопоставленияИзХранилища(Владелец)
	
	ХранилищеСопоставления = НайтиСоздатьХранилищеСопоставленияНоменклатуры(Владелец);
	ТаблицаСопоставленияНоменклатуры = ХранилищеСопоставления.Хранилище.Получить();
	
	Если НЕ ЗначениеЗаполнено(ТаблицаСопоставленияНоменклатуры) Тогда
		ТаблицаСопоставленияНоменклатуры = Контракт_ХранениеТаблицыСопоставленияНоменклатуры();
	КонецЕсли;
	
	Если НЕ ТипЗнч(ТаблицаСопоставленияНоменклатуры) = Тип("ТаблицаЗначений") Тогда
		ШаблонОшибки = "Не удалось прочитать файл сопоставления номенклатуры по контрагенту """"%1"""". 
			|Подробности см. в журнале регистрации.";
		
		ТекстОшибки = СтрЗаменить(ШаблонОшибки, "%1", СокрЛП(Владелец));
		ВызватьИсключение ТекстОшибки;
	КонецЕсли;
	
	Возврат ТаблицаСопоставленияНоменклатуры;
	
КонецФункции

Функция Контракт_ХранениеТаблицыСопоставленияНоменклатуры()
	
	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("Идентификатор");
	Результат.Колонки.Добавить("Номенклатура");
	Результат.Колонки.Добавить("Характеристика");
	Результат.Колонки.Добавить("Упаковка");
	
	Возврат Результат;
	
КонецФункции

Процедура УдалитьСопоставлениеНоменклатуры_НоменклатураКонтрагентов(Владелец, ИдентификаторНоменклатуры)
	
	ТекстЗапроса =
		"ВЫБРАТЬ
		|	*
		|ИЗ
		|	РегистрСведений.НоменклатураКонтрагентов КАК Т
		|ГДЕ
		|	Т.Контрагент = &Владелец
		|	И Т.Идентификатор = &Идентификатор";
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("Владелец", Владелец);
	Запрос.УстановитьПараметр("Идентификатор", ИдентификаторНоменклатуры);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	МенеджерТаблицы = РегистрыСведений.НоменклатураКонтрагентов;
	
	Пока Выборка.Следующий() Цикл
		Запись = МенеджерТаблицы.СоздатьМенеджерЗаписи();
		ЗаполнитьЗначенияСвойств(Запись, Выборка);
		Запись.Удалить();
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаписатьОшибкуСопоставленияНоменклатуры(ИдентификаторНоменклатуры, ПредставлениеВладельца, Ошибка)
	
	ПодробностиОшибки = ПодробноеПредставлениеОшибки(Ошибка);
	
	ТекстОшибки = "Не удалось записать сопоставление номенклатуры (Идентификатор = %1, контрагент = %2)";
	ТекстОшибки = СтрЗаменить(ТекстОшибки, "%1", ИдентификаторНоменклатуры);
	ТекстОшибки = СтрЗаменить(ТекстОшибки, "%2", ПредставлениеВладельца);
	ТекстОшибки = ТекстОшибки + Символы.ПС + ПодробностиОшибки;
	
	ЗаписьЖурналаРегистрации(
		"Диадок.Сопоставление номенклатуры",
		УровеньЖурналаРегистрации.Ошибка,
		Метаданные.РегистрыСведений.НоменклатураКонтрагентов,
		Неопределено,
		ТекстОшибки
	);
	
КонецПроцедуры

Функция НайтиСоздатьХранилищеСопоставленияНоменклатуры(Контрагент)
	
	Наименование = НаименованиеСлужебногоХранилищаСопоставления();
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	Т.Ссылка КАК Ссылка
		|ИЗ
		|	Справочник.ХранилищеДополнительнойИнформации КАК Т
		|ГДЕ
		|	Т.Объект = &Объект
		|	И НЕ Т.ПометкаУдаления
		|	И Т.Наименование = &Наименование";
	
	Запрос.УстановитьПараметр("Наименование", Наименование);
	Запрос.УстановитьПараметр("Объект", Контрагент);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		
		Результат = ВыборкаДетальныеЗаписи.Ссылка;
		
	Иначе
		
		НовыйОбъект = Справочники.ХранилищеДополнительнойИнформации.СоздатьЭлемент();
		НовыйОбъект.ВидДанных = Перечисления.ВидыДополнительнойИнформацииОбъектов.Файл;
		НовыйОбъект.Объект = Контрагент;
		НовыйОбъект.ИмяФайла = Контрагент;
		НовыйОбъект.Наименование = Наименование;
		НовыйОбъект.Записать();
		
		Результат = НовыйОбъект.Ссылка;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция НаименованиеСлужебногоХранилищаСопоставления()
	Возврат "Диадок_СопоставлениеНоменклатуры";
КонецФункции

// Получает сопоставление номенклатуры из устаревшего хранилища и переносит в актуальное.
//
// Параметры:
//  ВладелецНоменклатуры - СправочникСсылка.Контрагенты - контрагент-владелец номенклатуры;
//  ИдентификаторНоменклатуры - Строка - идентификатор номенклатуры контрагента;
// 
// Возвращаемое значение:
//  Структура - номенклатура информационной базы (сведения из устаревшего хранилища);
//  Неопределено - если сопоставление не найдено.
//
Функция ПеренестиСопоставлениеНоменклатуры_НоменклатураКонтрагентов(ВладелецНоменклатуры, ИдентификаторНоменклатуры)
	
	НоменклатураИБ = ПолучитьНоменклатуруИнформационнойБазы_НоменклатураКонтрагентов(
			ВладелецНоменклатуры,
			ИдентификаторНоменклатуры);
	
	Если ЗначениеЗаполнено(НоменклатураИБ) Тогда
		
		КлючевыеПоля = Новый Структура;
		КлючевыеПоля.Вставить("Владелец", ВладелецНоменклатуры);
		КлючевыеПоля.Вставить("Идентификатор", ИдентификаторНоменклатуры);
		
		ДобавитьСопоставлениеНоменклатуры(
			КлючевыеПоля,
			НоменклатураИБ.Номенклатура,
			НоменклатураИБ.Характеристика
		);
		
	КонецЕсли;
	
	Возврат НоменклатураИБ;
	
КонецФункции

Функция ПолучитьНоменклатуруИнформационнойБазы_НоменклатураКонтрагентов(Владелец, ИдентификаторНоменклатуры)
	
	ТекстЗапроса =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	Т.Номенклатура КАК Номенклатура,
		|	Т.ХарактеристикаНоменклатуры КАК Характеристика,
		|	НЕОПРЕДЕЛЕНО КАК Упаковка
		|ИЗ
		|	РегистрСведений.НоменклатураКонтрагентов КАК Т
		|ГДЕ
		|	Т.Контрагент = &Владелец
		|	И Т.Идентификатор = &Идентификатор";
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("Владелец", Владелец);
	Запрос.УстановитьПараметр("Идентификатор", ИдентификаторНоменклатуры);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Результат = Неопределено;
	
	Если Выборка.Следующий() Тогда
		
		Результат = НоваяНоменклатураИнформационнойБазы(
				Выборка.Номенклатура,
				Выборка.Характеристика,
				Выборка.Упаковка
			);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ДобавитьСопоставлениеНоменклатуры_НоменклатураКонтрагентов(НоменклатураКонтрагента, Номенклатура, Характеристика)
	
	НовыйОбъект = РегистрыСведений.НоменклатураКонтрагентов.СоздатьМенеджерЗаписи();
	
	ЗаполнитьЗначенияСвойств(НовыйОбъект, НоменклатураКонтрагента);
	
	НовыйОбъект.Контрагент = НоменклатураКонтрагента.Владелец;
	НовыйОбъект.КодНоменклатурыКонтрагента = НоменклатураКонтрагента.Код;
	НовыйОбъект.АртикулНоменклатурыКонтрагента = НоменклатураКонтрагента.Артикул;
	НовыйОбъект.НаименованиеНоменклатурыКонтрагента = НоменклатураКонтрагента.Наименование;
	
	НовыйОбъект.Номенклатура = Номенклатура;
	НовыйОбъект.ХарактеристикаНоменклатуры = Характеристика;
	
	НовыйОбъект.Записать(Истина);
	
КонецПроцедуры

Функция НоваяНоменклатураИнформационнойБазы(Номенклатура, Характеристика = Неопределено, Упаковка = Неопределено)
	
	Результат = Новый Структура;
	Результат.Вставить("Номенклатура", Номенклатура);
	Результат.Вставить("Характеристика");
	Результат.Вставить("Упаковка");
	
	Если ЗначениеЗаполнено(Характеристика) Тогда
		Результат.Характеристика = Характеристика;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Упаковка) Тогда
		Результат.Упаковка = Упаковка;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция МодульФорматыЭлектронныхДокументов()
	
	ИмяМодуля = "ФорматыЭД";
	Результат = ПолучитьМодульПрог(ИмяМодуля);
	
	Возврат Результат;
	
КонецФункции

//} СлужебныеПроцедурыИФункции


//{ ТЕЛО МОДУЛЯ

Функция ПолучитьМодульПрог(ИмяМодуля)
	
	Возврат ОсновнойМодуль.ПолучитьМодульПрог(ИмяМодуля);
	
КонецФункции 

Функция ПолучитьПредставлениеСтавкиНДС(СтавкаНДС, дляСФ = Истина) Экспорт
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьПредставлениеСтавкиНДС(СтавкаНДС, дляСФ);
КонецФункции 

Функция ПолучитьНомерНаПечатьДиадок(ДокументСсылка) Экспорт
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьНомерНаПечатьДиадок(ДокументСсылка);
КонецФункции

Функция ЭтоСчетФактуранаАванс(СФ) Экспорт
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЭтоСчетФактуранаАванс(СФ);
КонецФункции	

Функция ПолучитьСуммуДокументаВВалютеРегламентированногоУчета_Диадок(ДокументСсылка) Экспорт 
	
	Если УчетНДС.ПолучитьУПУпрощенныйУчетНДС(ДокументСсылка.Организация, ДокументСсылка.Дата) Тогда
		
		ТекстЗапроса = 
		"ВЫБРАТЬ
		|	СУММА(ЕСТЬNULL(НДСЗаписиКнигиПродажОбороты.СуммаБезНДСОборот, 0) + ЕСТЬNULL(НДСЗаписиКнигиПродажОбороты.НДСОборот, 0)) КАК СуммаВзаиморасчетов
		|ИЗ
		|	РегистрНакопления.НДСЗаписиКнигиПродаж.Обороты(, , Регистратор, СчетФактура = &Ссылка) КАК НДСЗаписиКнигиПродажОбороты
		|ГДЕ
		|	НДСЗаписиКнигиПродажОбороты.Регистратор = &Ссылка";
		
	ИначеЕсли ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.ВозвратТоваровПоставщику") Тогда
		
		ТекстЗапроса =
		"ВЫБРАТЬ
		|	ЕСТЬNULL(ХозрасчетныйОборотыДтКт.СуммаОборот, 0) КАК СуммаВзаиморасчетов
		|ИЗ
		|	РегистрБухгалтерии.Хозрасчетный.ОборотыДтКт(, , Регистратор, , , , , ) КАК ХозрасчетныйОборотыДтКт
		|ГДЕ
		|	ХозрасчетныйОборотыДтКт.Регистратор = &Ссылка
		|	И ХозрасчетныйОборотыДтКт.Регистратор.СчетУчетаРасчетовПоПретензиям = ХозрасчетныйОборотыДтКт.СчетДт";	
		
	ИначеЕсли  ДокументСсылка.Метаданные().Имя = "РеализацияТоваровУслуг"
		ИЛИ (НЕ ДокументСсылка.Метаданные().Реквизиты.Найти("ОтражатьВБухгалтерскомУчете") = Неопределено
				И ДокументСсылка.ОтражатьВБухгалтерскомУчете = Ложь) Тогда
				
		ЗапросКурсАванса = Новый Запрос;
		ЗапросКурсАванса.УстановитьПараметр("ДокументСсылка", ДокументСсылка);
		ЗапросКурсАванса.Текст =
		"ВЫБРАТЬ
		|	Док.СуммаВзаиморасчетов,
		|	Док.СуммаРегл
		|ИЗ 
		|	Документ.РеализацияТоваровУслуг.ДокументыРасчетовСКонтрагентом КАК Док
		|ГДЕ Док.Ссылка = &ДокументСсылка
		|ИТОГИ СУММА(СуммаВзаиморасчетов), СУММА(СуммаРегл) ПО ОБЩИЕ";
		
		Выборка = ЗапросКурсАванса.Выполнить().Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
		Если Выборка.Следующий() Тогда
			ВыборкаСуммаВзаиморасчетов = Выборка.СуммаВзаиморасчетов;
			ВыборкаСуммаРегл           = Выборка.СуммаРегл;
		Иначе
			ВыборкаСуммаВзаиморасчетов = 0;
			ВыборкаСуммаРегл           = 0;
		КонецЕсли;
		
		СуммаВзаиморасчетов = МодульВалютногоУчета.ПересчитатьИзВалютыВВалюту(УчетНДС.ПолучитьСуммуДокументаСНДС(ДокументСсылка),
																			  ДокументСсылка.ВалютаДокумента, ДокументСсылка.ДоговорКонтрагента.ВалютаВзаиморасчетов,
																			  ЗаполнениеДокументов.КурсДокумента(ДокументСсылка, ПолучитьВалютуРегламентированногоУчета()), ДокументСсылка.КурсВзаиморасчетов,
																			  ЗаполнениеДокументов.КратностьДокумента(ДокументСсылка, ПолучитьВалютуРегламентированногоУчета()), ДокументСсылка.КратностьВзаиморасчетов);
		НеоплаченнаяСумма = СуммаВзаиморасчетов - ВыборкаСуммаВзаиморасчетов;
		
		НеоплаченнаяСуммаРегл = МодульВалютногоУчета.ПересчитатьИзВалютыВВалюту(НеоплаченнаяСумма,
																				ДокументСсылка.ДоговорКонтрагента.ВалютаВзаиморасчетов, ПолучитьВалютуРегламентированногоУчета(),
																				ДокументСсылка.КурсВзаиморасчетов, 1,
																				ДокументСсылка.КратностьВзаиморасчетов, 1);
															
		Возврат ВыборкаСуммаРегл + НеоплаченнаяСуммаРегл;
		
	ИначеЕсли НЕ ДокументСсылка.Метаданные().Реквизиты.Найти("СчетУчетаРасчетовСКонтрагентом") = Неопределено Тогда
		
		ТекстЗапроса = 
		"ВЫБРАТЬ
		|	СУММА(ЕСТЬNULL(РеализацияТоваровУслугДокументыРасчетовСКонтрагентом.СуммаВзаиморасчетов, 0)) КАК СуммаВзаиморасчетов,
		|	СУММА(ЕСТЬNULL(РеализацияТоваровУслугДокументыРасчетовСКонтрагентом.СуммаРегл, 0)) КАК СуммаРегл
		|ПОМЕСТИТЬ Предоплата
		|ИЗ
		|	Документ.РеализацияТоваровУслуг.ДокументыРасчетовСКонтрагентом КАК РеализацияТоваровУслугДокументыРасчетовСКонтрагентом
		|ГДЕ
		|	РеализацияТоваровУслугДокументыРасчетовСКонтрагентом.Ссылка = &Ссылка
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВЫРАЗИТЬ(ВЫБОР
		|			КОГДА РеализацияТоваровУслуг.ВалютаДокумента = &ВалютаРегл
		|				ТОГДА РеализацияТоваровУслуг.СуммаДокумента
		|			КОГДА НЕ РеализацияТоваровУслуг.КратностьВзаиморасчетов = 0
		|				ТОГДА (РеализацияТоваровУслуг.СуммаДокумента - ЕСТЬNULL(Предоплата.СуммаВзаиморасчетов, 0)) * РеализацияТоваровУслуг.КурсВзаиморасчетов / РеализацияТоваровУслуг.КратностьВзаиморасчетов + ЕСТЬNULL(Предоплата.СуммаРегл, 0)
		|		КОНЕЦ КАК ЧИСЛО(15, 2)) КАК СуммаВзаиморасчетов
		|ИЗ
		|	Документ.РеализацияТоваровУслуг КАК РеализацияТоваровУслуг,
		|	Предоплата КАК Предоплата
		|ГДЕ
		|	РеализацияТоваровУслуг.Ссылка = &Ссылка";
		
	Иначе
		Возврат 0;	
	КонецЕсли;
				
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Ссылка", ДокументСсылка);
	Запрос.УстановитьПараметр("ВалютаРегл", ПолучитьВалютуРегламентированногоУчета());
	Запрос.Текст = ТекстЗапроса;
	
	Результат = Запрос.Выполнить();
	Если Результат.Пустой() Тогда
		Возврат 0;
	КонецЕсли;
	
	Выборка = Результат.Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат Выборка.СуммаВзаиморасчетов;
	Иначе
		Возврат 0; 
	КонецЕсли;
	
КонецФункции

Процедура ПерезаполнитьСуммыПоУЕвРублях(ТабличнаяЧасть, ТекущееОснование, ДанныеШапки) Экспорт 
	
	Если ДанныеШапки.Свойство("СчетФактура") Тогда
		
		ДанныеШапки.Вставить("ТабличнаяЧасть", ТабличнаяЧасть);
		ПересчетВалюты_УПД(ДанныеШапки.СчетФактура, ТекущееОснование, ДанныеШапки);
		
	ИначеЕсли ТипЗнч(ТекущееОснование) = Тип("ДокументСсылка.АктОбОказанииПроизводственныхУслуг") Тогда
		
		ПересчетВалюты_Акт(ТабличнаяЧасть, ТекущееОснование, ДанныеШапки, "АктОбОказанииПроизводственныхУслуг");
		
	ИначеЕсли ТипЗнч(ТекущееОснование) = Тип("ДокументСсылка.ВозвратТоваровПоставщику") Тогда 
		
		ПересчетВалюты_ВозвратТоваровПоставщику(ТабличнаяЧасть, ТекущееОснование, ДанныеШапки);
		
	ИначеЕсли ТипЗнч(ТекущееОснование) = Тип("ДокументСсылка.РеализацияТоваровУслуг") Тогда 
		
		ПересчетВалюты_РеализацияТоваровУслуг(ТабличнаяЧасть, ТекущееОснование, ДанныеШапки);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ПересчетВалюты_Акт(ТабличнаяЧасть, ТекущееОснование, ДанныеШапки, ИмяДокумента)
	
	//1. Рассчитываем по курсу документа
	Курс		= ТекущееОснование.КурсВзаиморасчетов;
	Кратность	= ТекущееОснование.КратностьВзаиморасчетов;
	
	ПересчетВалюты_ПоКурсуДокумента(ТабличнаяЧасть, Курс, Кратность);	
	
	//2. Считаем сумму в рублях с учетом аванса и распределяем
	СтруктураДляПересчета = ПересчетВалюты_СтруктураДляПересчетаСУчетомАванса(ТекущееОснование, "АктОбОказанииПроизводственныхУслуг");
	СуммаРегл = СтруктураДляПересчета.СуммаРегл;
	
	ПересчетВалюты_РаспределениеСуммы(ТабличнаяЧасть, ДанныеШапки, СуммаРегл);
	
	ДанныеШапки.ВалютаДокумента = ПолучитьВалютуРегламентированногоУчета();
	
КонецПроцедуры	

Процедура ПересчетВалюты_ВозвратТоваровПоставщику(ТабличнаяЧасть, ТекущееОснование, ДанныеШапки)
	
	Курс		= ТекущееОснование.КурсВзаиморасчетов;
	Кратность	= ТекущееОснование.КратностьВзаиморасчетов;
	
	ПересчетВалюты_ПоКурсуДокумента(ТабличнаяЧасть, Курс, Кратность);	
	
	ДанныеШапки.ВалютаДокумента = ПолучитьВалютуРегламентированногоУчета();
	
КонецПроцедуры

Процедура ПересчетВалюты_РеализацияТоваровУслуг(ТабличнаяЧасть, ТекущееОснование, ДанныеШапки)
	
	//1. Рассчитываем по курсу документа
	Курс		= ТекущееОснование.КурсВзаиморасчетов;
	Кратность	= ТекущееОснование.КратностьВзаиморасчетов;
	
	ПересчетВалюты_ПоКурсуДокумента(ТабличнаяЧасть, Курс, Кратность);	
	
	//2. Считаем сумму в рублях с учетом аванса и распределяем
	СтруктураДляПересчета = ПересчетВалюты_СтруктураДляПересчетаСУчетомАванса(ТекущееОснование, "РеализацияТоваровУслуг");
	СуммаРегл = СтруктураДляПересчета.СуммаРегл;
	
	ПересчетВалюты_РаспределениеСуммы(ТабличнаяЧасть, ДанныеШапки, СуммаРегл);
	
	ДанныеШапки.ВалютаДокумента = ПолучитьВалютуРегламентированногоУчета();
	
КонецПроцедуры

Процедура ПересчетВалюты_УПД(СчетФактура, ТекущееОснование, ДанныеШапки)
	
	ТабличнаяЧасть		= ДанныеШапки.ТабличнаяЧасть;
	СуммаВзаиморасчетов = УчетНДСФормированиеДвижений.ПолучитьСуммуВзаиморасчетов(СчетФактура, ТекущееОснование, ДанныеШапки);
	
	ПересчетВалюты_РаспределениеСуммы(ТабличнаяЧасть, ДанныеШапки, СуммаВзаиморасчетов);
	
КонецПроцедуры

Функция ПересчетВалюты_СтруктураДляПересчетаСУчетомАванса(Ссылка, ИмяДокумента)
	
	ЗапросКурсАванса = Новый Запрос;
	ЗапросКурсАванса.УстановитьПараметр("ТекущийДокумент", Ссылка);
	ЗапросКурсАванса.Текст =
	"ВЫБРАТЬ
	|	Док.СуммаВзаиморасчетов КАК СуммаВзаиморасчетов,
	|	Док.СуммаРегл КАК СуммаРегл
	|ИЗ
	|	Документ." + ИмяДокумента + ".ДокументыРасчетовСКонтрагентом КАК Док
	|ГДЕ
	|	Док.Ссылка = &ТекущийДокумент
	|ИТОГИ
	|	СУММА(СуммаВзаиморасчетов),
	|	СУММА(СуммаРегл)
	|ПО
	|	ОБЩИЕ";
	
	Выборка = ЗапросКурсАванса.Выполнить().Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	Если Выборка.Следующий() Тогда
		КурсОплаты                 = ?(Выборка.СуммаВзаиморасчетов = 0, 0, Выборка.СуммаРегл / Выборка.СуммаВзаиморасчетов);
		ВыборкаСуммаВзаиморасчетов = Выборка.СуммаВзаиморасчетов;
		ВыборкаСуммаРегл           = Выборка.СуммаРегл;
	Иначе
		КурсОплаты                 = 0;
		ВыборкаСуммаВзаиморасчетов = 0;
		ВыборкаСуммаРегл           = 0;
	КонецЕсли;
	
	СуммаВзаиморасчетов = МодульВалютногоУчета.ПересчитатьИзВалютыВВалюту(УчетНДС.ПолучитьСуммуДокументаСНДС(Ссылка),
																			Ссылка.ВалютаДокумента, Ссылка.ДоговорКонтрагента.ВалютаВзаиморасчетов,
																			ЗаполнениеДокументов.КурсДокумента(Ссылка, ПолучитьВалютуРегламентированногоУчета()), Ссылка.КурсВзаиморасчетов,
																			ЗаполнениеДокументов.КратностьДокумента(Ссылка, ПолучитьВалютуРегламентированногоУчета()), Ссылка.КратностьВзаиморасчетов);
	НеоплаченнаяСумма = СуммаВзаиморасчетов - ВыборкаСуммаВзаиморасчетов;
	Если НеоплаченнаяСумма > 0 Тогда
		КурсДляПечати = (НеоплаченнаяСумма * Ссылка.КурсВзаиморасчетов + ВыборкаСуммаРегл)/(НеоплаченнаяСумма + ВыборкаСуммаВзаиморасчетов);
	Иначе
		КурсДляПечати = КурсОплаты;
	КонецЕсли;
	
	НеоплаченнаяСуммаРегл = МодульВалютногоУчета.ПересчитатьИзВалютыВВалюту(НеоплаченнаяСумма,
																			Ссылка.ДоговорКонтрагента.ВалютаВзаиморасчетов, ПолучитьВалютуРегламентированногоУчета(),
																			Ссылка.КурсВзаиморасчетов, 1,
																			Ссылка.КратностьВзаиморасчетов, 1);
	
	СуммаРегл = ВыборкаСуммаРегл + НеоплаченнаяСуммаРегл;
	
	СтруктураДляПересчета = Новый Структура;
	
	Если КурсДляПечати = 0 Тогда
		СтруктураДляПересчета.Вставить("Курс",		Ссылка.КурсВзаиморасчетов);
		СтруктураДляПересчета.Вставить("Кратность",	?(Ссылка.КратностьВзаиморасчетов = 0, 1, Ссылка.КратностьВзаиморасчетов));
		СтруктураДляПересчета.Вставить("СуммаРегл",	СуммаРегл);
	Иначе
		СтруктураДляПересчета.Вставить("Курс",		КурсДляПечати);
		СтруктураДляПересчета.Вставить("Кратность",	1);
		СтруктураДляПересчета.Вставить("СуммаРегл",	СуммаРегл);
	КонецЕсли;
	
	Возврат СтруктураДляПересчета;
	
КонецФункции	

Процедура ПересчетВалюты_ПоКурсуДокумента(ТабличнаяЧасть, Курс, Кратность)
	
	Для Каждого ТекСтрокаТЧ Из ТабличнаяЧасть Цикл
		
		Сумма		= ТекСтрокаТЧ.Сумма * Курс / Кратность;
		СуммаНДС	= ТекСтрокаТЧ.СуммаНДС * Курс / Кратность;
		
		ТекСтрокаТЧ.Сумма		= Окр(Сумма, 2);
		ТекСтрокаТЧ.СуммаНДС	= Окр(СуммаНДС, 2);
		
	КонецЦикла;
	
КонецПроцедуры	

Процедура ПересчетВалюты_РаспределениеСуммы(ТабличнаяЧасть, ДанныеШапки, СуммаРегл)
	
	РасчетСуммыНДСПоСтавке	= УчетНДС.РасчетНДСвРубляхПоСтавкеДокумента(ДанныеШапки.Дата);
	
	Если СуммаРегл <> 0 Тогда
		
		ТабличнаяЧасть.Колонки.Добавить("СуммаБезНДС");
		ТабличнаяЧасть.Колонки.Добавить("СуммаРублевая");
		
		//1. Распределяем сумму регл пропорционально Сумме с НДС
		МассивРаспределения = Новый Массив;
		
		Для Каждого СтрокаТовар Из ТабличнаяЧасть Цикл
			
			СуммаСНДС	= СтрокаТовар.Сумма + ?(ДанныеШапки.СуммаВключаетНДС, 0, СтрокаТовар.СуммаНДС);
			СуммаБезНДС	= СуммаСНДС - СтрокаТовар.СуммаНДС;
			
			СтрокаТовар.СуммаБезНДС = СуммаБезНДС;
			
			МассивРаспределения.Добавить(СуммаСНДС);
			
		КонецЦикла;
		
		УчетНДС.РаспределитьСуммуПоСтолбцу(МассивРаспределения, СуммаРегл, ТабличнаяЧасть, "СуммаРублевая");
		
		//2. Пересчитываем СуммаНДС и Сумму
		Для Каждого СтрокаТовар Из ТабличнаяЧасть Цикл
			
			Если РасчетСуммыНДСПоСтавке Тогда
				
				ЗначениеСтавкиНДС = УчетНДС.ПолучитьСтавкуНДС(СтрокаТовар.СтавкаНДС);
				
				СтрокаТовар.СуммаНДС	= ?(ЗначениеСтавкиНДС = 0, 0, Окр(СтрокаТовар.СуммаРублевая * ЗначениеСтавкиНДС / (100 + ЗначениеСтавкиНДС), 2));
				СтрокаТовар.Сумма		= СтрокаТовар.СуммаРублевая - СтрокаТовар.СуммаНДС;
				
			Иначе
				
				МассивРаспределения.Очистить();
				МассивРаспределения.Добавить(СтрокаТовар.СуммаБезНДС);
				МассивРаспределения.Добавить(СтрокаТовар.СуммаНДС);
				
				МассивРаспределенныхСумм = ОбщегоНазначения.РаспределитьПропорционально(СтрокаТовар.СуммаРублевая, МассивРаспределения);
				
				Если МассивРаспределенныхСумм <> Неопределено Тогда
					СтрокаТовар.Сумма		= МассивРаспределенныхСумм[0];
					СтрокаТовар.СуммаНДС	= МассивРаспределенныхСумм[1];
				КонецЕсли;
				
			КонецЕсли;
			
			Если ДанныеШапки.СуммаВключаетНДС Тогда
				СтрокаТовар.Сумма = СтрокаТовар.Сумма + СтрокаТовар.СуммаНДС;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ИдентификаторСвойстваИдентификаторДокументаВДиадок(DocumentType) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ИдентификаторСвойстваИдентификаторДокументаВДиадок(DocumentType);
	
КонецФункции

Функция ИдентификаторСвойстваИдентификаторЯщикДокументаВДиадок(DocumentType) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ИдентификаторСвойстваИдентификаторЯщикДокументаВДиадок(DocumentType);
	
КонецФункции 

Функция ДокументБезНДС(СсылкаНаОбъект) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ДокументБезНДС(СсылкаНаОбъект);
	
КонецФункции

Функция ПустаяСсылкаНаНомерГТД() Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПустаяСсылкаНаНомерГТД(); 
	
КонецФункции

Функция НДСПоДокументуИсчисляетсяНалоговымАгентом(ДокументСсылка) Экспорт

	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").НДСПоДокументуИсчисляетсяНалоговымАгентом(ДокументСсылка);

КонецФункции // НДСПоДокументуИсчисляетсяНалоговымАгентом()

//} ТЕЛО МОДУЛЯ

//{ ПОЛУЧЕНИЕ ДАННЫХ

//{ Получить...

Функция ПолучитьНаименованиеКолонкиКодаТовара() Экспорт
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьНаименованиеКолонкиКодаТовара();
КонецФункции

Функция ПолучитьВалютуРегламентированногоУчета()
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьВалютуРегламентированногоУчета();
	
КонецФункции

Функция ДиадокПолучитьПараметрыУчетнойПолитики(ДокСсылка) Экспорт
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ДиадокПолучитьПараметрыУчетнойПолитики(ДокССылка.Дата, Неопределено, ДокССылка.Организация);
КонецФункции

Функция ПолучитьДатуНомерДокументовОплаты(СчетФактура) Экспорт
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьДатуНомерДокументовОплаты(СчетФактура);
КонецФункции

Функция ПолучитьНомерГТД(НомерГТД) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьНомерГТД(НомерГТД);
	
КонецФункции

Функция ПолучитьСсылкуНомерГТД(НомерГТД, СоздатьНовый = Ложь) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьСсылкуНомерГТД(НомерГТД, СоздатьНовый);
	
КонецФункции

Функция ПолучитьСсылкуСерииНоменклатуры(Номенклатура, НомерГТД, СтранаПроисхождения = "", СоздатьНовый = Ложь) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьСсылкуСерииНоменклатуры(Номенклатура, НомерГТД, СтранаПроисхождения, СоздатьНовый);
	
КонецФункции

Функция ПолучитьДатуНомерДоговора(ДокументСсылка) Экспорт
	Договор = ДокументСсылка.ДоговорКонтрагента;
	Возврат Новый Структура("ДоговорНомер, ДоговорДата", Договор.Номер, Договор.Дата);
КонецФункции

Функция ПолучитьФИОРуководителяДляСчета(ДанныеШапки, ПараметрыОтправкиНаСогласование) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьФИОРуководителяДляСчета(ДанныеШапки, ПараметрыОтправкиНаСогласование);
	
КонецФункции

Функция ПолучитьГрузоотправителяГрузополучателя1С(Параметры) Экспорт
	
	Возврат Неопределено;	
	
КонецФункции

Функция ДокументыОснованияСчетаФактуры(СчетФактура)
	
	Результат = ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ДокументыОснованияСчетаФактуры(СчетФактура);
	
	Возврат Результат;
	
КонецФункции

//} Получить...

//{ ТЧПолучитьТекстЗапроса...

Функция ТЧПолучитьТекстЗапроса(Документ, СчетФактура, ЭтоКСФ)
	
	ТекстЗапроса=	"";
	
	Если ТипЗнч(Документ) = Тип("ДокументСсылка.РеализацияТоваровУслуг") Тогда
		
		Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаРеализацияТоваровУслуг(НЕ СчетФактура = Неопределено);
		
	ИначеЕсли ЭтоСчетФактуранаАванс(СчетФактура)  Тогда
		
		Возврат	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаСчетФактураВыданный();
		
	ИначеЕсли ЭтоКСФ = Ложь И Метаданные.Документы.Найти("КорректировкаРеализации") <> Неопределено И ТипЗнч(Документ) = Тип("ДокументСсылка.КорректировкаРеализации") Тогда
		
		Возврат	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаКорректировкаРеализации();
		
	ИначеЕсли ЭтоКСФ = Истина И Метаданные.Документы.Найти("КорректировкаРеализации") <> Неопределено И ТипЗнч(Документ) = Тип("ДокументСсылка.КорректировкаРеализации") Тогда
		
		Если Строка(Документ.ВидОперации) = "Исправление в первичных документах" Тогда
			Возврат	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаИКСФ();
		ИначеЕсли Строка(Документ.ВидОперации) = "Корректировка по согласованию сторон" Тогда
			Возврат	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаКСФ();
		КонецЕсли;
		
	ИначеЕсли ТипЗнч(Документ) = Тип("ДокументСсылка.АктОбОказанииПроизводственныхУслуг") Тогда
		
		Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаАктОбОказанииПроизводственныхУслуг();
		
	ИначеЕсли ТипЗнч(Документ) = Тип("ДокументСсылка.ОтчетКомиссионераОПродажах") Тогда
		
		Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаОтчетКомиссионераОПродажах();
		
	ИначеЕсли ТипЗнч(Документ)= Тип("ДокументСсылка.ОтчетКомитентуОПродажах") Тогда 
		
		Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаОтчетКомитентуОПродажах();
		
	ИначеЕсли ТипЗнч(Документ) = Тип("ДокументСсылка.ВозвратТоваровПоставщику") Тогда
		
		Возврат	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаВозвратТоваровПоставщику();
		
	ИначеЕсли ТипЗнч(Документ) = Тип("ДокументСсылка.ОтражениеРеализацииТоваровИУслугНДС") Тогда
		
		Возврат	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаОтражениеРеализацииТоваровИУслугНДС();
		
	ИначеЕсли ТипЗнч(Документ) = Тип("ДокументСсылка.РеализацияУслугПопереработке") Тогда 
		
		Возврат	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаРеализацияУслугПопереработке();
		
	ИначеЕсли ТипЗнч(Документ) = Тип("ДокументСсылка.ПередачаОС") Тогда 
		
		Возврат	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаПередачаОС();
		
	ИначеЕсли ТипЗнч(Документ) = тип("ДокументСсылка.СчетНаоплатуПокупателю") Тогда
		
		Возврат 	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаСчетНаоплатуПокупателю();
		
	ИначеЕсли ТипЗнч(Документ) = тип("ДокументСсылка.ЗаказПокупателя") Тогда
		
		Возврат 	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТЧПолучитьТекстЗапросаЗаказПокупателя();
		
	КонецЕсли;
	
	Возврат ТекстЗапроса;
	
	ВызватьИсключение "Не удалось сформировать текст запроса к табличной части документа " + Документ;
	
КонецФункции

Функция ПолучитьРезультатЗапросаТЧ(ДокументСсылка, СчетФактура, ЭтоКСФ) Экспорт
	
	ЗапросТЧ = Новый Запрос;
	ЗапросТЧ.Текст = ТЧПолучитьТекстЗапроса(ДокументСсылка, СчетФактура, ЭтоКСФ);
	ЗапросТЧ.УстановитьПараметр("СчетФактура", СчетФактура);
	ЗапросТЧ.УстановитьПараметр("Ссылка", ДокументСсылка);
	
	Возврат ЗапросТЧ.Выполнить();
	
КонецФункции

Функция ПолучитьКодНоменклатурыДляТорг12(ТекущийКод, НоменклатураСсылка) Экспорт 
	КолонкаКодаТовара=	ПолучитьНаименованиеКолонкиКодаТовара();
	Если ЗначениеЗаполнено(НоменклатураСсылка) И КолонкаКодаТовара <> "Код" Тогда
		Возврат НоменклатураСсылка[КолонкаКодаТовара];
	Иначе 
		Возврат ТекущийКод;
	КонецЕсли;
КонецФункции

Функция ТребуетсяПерезаполнитьСуммыПоУЕвРублях(ДокументРеализации, СчетФактура = Неопределено, ФормированиеСФ = Ложь, ЭтоАкт = Ложь) Экспорт 
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТребуетсяПерезаполнитьСуммыПоУЕвРублях(ДокументРеализации, СчетФактура, ФормированиеСФ, ЭтоАкт);
КонецФункции	

//} ТЧПолучитьТекстЗапроса...

//{ ПолучитьДанныеШапки...

Функция ШапкаПолучитьТекстЗапроса(ДокументСсылка, СчетФактура)
	
	Если ЭтоСчетФактуранаАванс(СчетФактура) = Истина Тогда
		Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ШапкаПолучитьТекстЗапросаСФ();
	Иначе 
		Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ШапкаПолучитьТекстЗапросаОсновной();
	КонецЕсли;
КонецФункции

Функция ШапкаПолучитьРезультатЗапроса(ДокументСсылка, СчетФактура = Неопределено) 
	
	Запрос= Новый Запрос;
	Запрос.Текст=	ШапкаПолучитьТекстЗапроса(ДокументСсылка, СчетФактура);
	Запрос.УстановитьПараметр("СчетФактура", СчетФактура);
	Запрос.УстановитьПараметр("ДокументСсылка", ДокументСсылка);
	
	РезультатЗапроса=	Запрос.Выполнить();
	
	Возврат РезультатЗапроса;
	
КонецФункции

Функция ПолучитьВалютуДокумента(ВалютаДокумента, СчетФактура)
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьВалютуДокумента(ВалютаДокумента, СчетФактура);
	
КонецФункции 

Функция ПолучитьСтруктуруШапки(ДокументСсылка, СчетФактура = Неопределено) Экспорт
	
	РезультатЗапроса =	ШапкаПолучитьРезультатЗапроса(ДокументСсылка, СчетФактура);
	Если РезультатЗапроса.Пустой() Тогда
		ВызватьИсключение "Ошибка получения данных шапки, ДокументСсылка - " + ДокументСсылка + ", СчетФактура - " + СчетФактура;
	КонецЕсли;
	
	ТаблицаДанныхШапки = РезультатЗапроса.Выгрузить();
	ДанныеШапки = ТаблицаДанныхШапки[0];
	
	СтруктураШапки = Новый Структура();
	
	Для Каждого Колонка Из РезультатЗапроса.Колонки Цикл
		СтруктураШапки.Вставить(Колонка.Имя, ДанныеШапки[Колонка.Имя]);
	КонецЦикла;
	
	СтруктураШапки.Вставить("ДопИнфо", 			Новый Массив);
	
	Если СчетФактура = Неопределено Тогда
		
		СтруктураШапки.Вставить("Дата", ДокументСсылка.Дата);
		СтруктураШапки.Вставить("Номер", ДокументСсылка.Номер);
		
	Иначе
		
		СтруктураШапки.Вставить("Дата", СчетФактура.Дата);
		СтруктураШапки.Вставить("Номер", СчетФактура.Номер);
		СтруктураШапки.ВалютаДокумента  =  ПолучитьВалютуДокумента(СтруктураШапки.ВалютаДокумента, СчетФактура);
		
		Если СчетФактура.Метаданные().Реквизиты.Найти("ИдентификаторГосконтракта") <> Неопределено Тогда
			СтруктураШапки.Вставить("ИдентификаторГосконтракта", СчетФактура.ИдентификаторГосконтракта);
		КонецЕсли;
		
	КонецЕсли;
	
	Если СтруктураШапки.Свойство("АдресДоставки")
		И ЗначениеЗаполнено(СтруктураШапки.АдресДоставки) Тогда
		
		СтруктураШапки.АдресДоставки = УправлениеКонтактнойИнформацией.ПолучитьПредставлениеАдресаПоСтрока(СтруктураШапки.АдресДоставки);
		
	КонецЕсли;
	
	Возврат СтруктураШапки;
	
КонецФункции

//} ПолучитьДанныеШапки...

//{ ПолучитьДанныеСФ...

Функция ПолучитьНомераДляКСФ(СчетФактура) Экспорт
	Возврат получитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьНомераДляКСФ(СчетФактура);
КонецФункции

Функция ПолучитьНомераДляСФ(СчетФактура)  Экспорт 
	Возврат получитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьНомераДляСФ(СчетФактура);
КонецФункции

Функция ПолучитьКПППродавца(ДокументСсылка) Экспорт
	
	Возврат Неопределено;
	
КонецФункции

Функция ПолучитьКПППокупателя(СчетФактура) Экспорт 
	Возврат получитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьКПППокупателя(СчетФактура);
КонецФункции

//} ПолучитьДанныеСФ...

//} ПОЛУЧЕНИЕ ДАННЫХ

//{ ФОРМИРОВАНИЕ СПИСКА ВЫГРУЗКИ

Функция ОчиститьГрузополучателя(СтрокаТаблицы) Экспорт
	
	Возврат ЗначениеЗаполнено(СтрокаТаблицы.Грузополучатель) И СтрокаТаблицы.Грузополучатель.ИНН <> СтрокаТаблицы.Покупатель.ИНН;
	
КонецФункции

Функция ПроверитьТипДокумента(ДокСсылка, НаименованиеТипа)
	Возврат (Метаданные.Документы.Найти(НаименованиеТипа) <> Неопределено)
		И (ТипЗнч(ДокСсылка) = Тип("ДокументСсылка." + НаименованиеТипа))
КонецФункции

Функция ПолучитьВидОперацииРТУ(Док) 
	Если  ПроверитьТипДокумента(Док, "РеализацияТоваровУслуг") Тогда 
		Возврат Док.ВидОперации
	ИначеЕсли ПроверитьТипДокумента(Док, "РеализацияОтгруженныхТоваров") Тогда 
		Возврат Док.ДокументОснование.ВидОперации
	ИначеЕсли 	ПроверитьТипДокумента(Док, "КорректировкаРеализации") Тогда 
		Возврат ПолучитьВидОперацииРТУ(Док.ДокументРеализации)
	Иначе 
		Возврат Неопределено;
	КонецЕсли;	
КонецФункции 

Функция ПолучитьТипДокумента(Док, ТаблицаНастроекПечати, ЭтоСчет, ФункцияУПД) Экспорт
	
	Результат = "";
	
	Если ЗначениеЗаполнено(ФункцияУПД) Тогда
		
		Результат = ОсновнойМодуль.ТипДокументаУПД(ФункцияУПД);
		Если ЗначениеЗаполнено(Результат) Тогда
			Возврат Результат;
		Иначе
			
			Результат = ОсновнойМодуль.ТипДокументаУКД(ФункцияУПД);
			Если ЗначениеЗаполнено(Результат) Тогда
				Возврат Результат;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	ТипДокумента = ТипЗнч(Док);
	Если ЭтоСчет Или ТипДокумента = Тип("ДокументСсылка.СчетНаОплатуПокупателю") Тогда
		
		Результат = "Счет на оплату";
		
	ИначеЕсли ТипДокумента = Тип("ДокументСсылка.СчетФактураВыданный") Тогда
		
		ЭтоКСФ = Строка(Док.ВидСчетаФактуры) = "Корректировочный";
		ЭтоИсправление = Док.Метаданные().Реквизиты.Найти("Исправление") <> Неопределено И Док.Исправление;
		
		Если ЭтоКСФ Тогда
			Результат = ?(ЭтоИсправление, "Исправление КСФ", "Корректировочный СФ");
		Иначе
			Результат = ?(ЭтоИсправление, "Исправление СФ", "Счет-фактура");
		КонецЕсли;
		
	ИначеЕсли ТипДокумента = Тип("ДокументСсылка.АктСверкиВзаиморасчетов") Тогда
		
		Результат = "Акт сверки";
		
	Иначе
		
		ПараметрыОтбора = Новый Структура("Организация", Док.Организация);
		СтрокиНастроек = ТаблицаНастроекПечати.НайтиСтроки(ПараметрыОтбора);
		ЕстьНастройкиПечати = СтрокиНастроек.Количество() > 0;
		Если ЕстьНастройкиПечати Тогда
			НастройкиПечати = СтрокиНастроек[0];
		КонецЕсли;
		
		Если ТипДокумента = Тип("ДокументСсылка.ВозвратТоваровПоставщику") Тогда
			
			Если ЕстьНастройкиПечати И ЗначениеЗаполнено(НастройкиПечати.ПечатнаяФормаДляТоваров) Тогда
				Результат = НастройкиПечати.ПечатнаяФормаДляТоваров;
			Иначе
				Результат = "Торг-12";
			КонецЕсли;
			
		ИначеЕсли ТипДокумента = Тип("ДокументСсылка.РеализацияТоваровУслуг")
			Или ТипДокумента = Тип("ДокументСсылка.РеализацияОтгруженныхТоваров")
			Или ПроверитьТипДокумента(Док, "КорректировкаРеализации") Тогда
			
			РНК = ?(ТипДокумента = Тип("ДокументСсылка.РеализацияОтгруженныхТоваров"), Док.ДокументОтгрузки, Док);
			СтрокиНастроек = ТаблицаНастроекПечати.НайтиСтроки(Новый Структура("Организация", РНК.Организация));
			Если СтрокиНастроек.Количество() = 0 Тогда
				ФормаДляУслуг = "Акт";
				ФормаДляТоваров = "Торг-12";
			Иначе 
				
				НастройкиПечати = СтрокиНастроек[0];
				ФормаДляТоваров = ?(ЗначениеЗаполнено(НастройкиПечати.ПечатнаяФормаДляТоваров), НастройкиПечати.ПечатнаяФормаДляТоваров, "Торг-12");
				ФормаДляУслуг = ?(ЗначениеЗаполнено(НастройкиПечати.ПечатнаяФормаДляУслуг), НастройкиПечати.ПечатнаяФормаДляУслуг, "Акт");
				
				//хитрая ситуация "в зависимости от вида операции"
				Если ПечатнаяФормаВЗависимостиОтВидаОперации(ФормаДляУслуг) Тогда 
					ВидОперации = ПолучитьВидОперацииРТУ(Док);
					ФормаДляУслуг = ?(ВидОперации = Перечисления.ВидыОперацийРеализацияТоваров.АктВыполненныхРабот , "Акт", "Торг-12");
				КонецЕсли;
				
			КонецЕсли;
			
			ТолькоУслуги = РНК.Товары.Количество() = 0 И РНК.Услуги.Количество() > 0;
			Результат = ?(ТолькоУслуги, ФормаДляУслуг, ФормаДляТоваров);
			
		ИначеЕсли ТипДокумента = Тип("ДокументСсылка.АктОбОказанииПроизводственныхУслуг")
			Или ТипДокумента = Тип("ДокументСсылка.РеализацияУслугПоПереработке")
			Или ТипДокумента = Тип("ДокументСсылка.ОтчетКомитентуОПродажах") Тогда 
			
			Если ЕстьНастройкиПечати
				И ЗначениеЗаполнено(НастройкиПечати.ПечатнаяФормаДляУслуг)
				И Не ПечатнаяФормаВЗависимостиОтВидаОперации(НастройкиПечати.ПечатнаяФормаДляУслуг) Тогда
				
				Результат = НастройкиПечати.ПечатнаяФормаДляУслуг;
				
			Иначе
				Результат = "Акт";
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПечатнаяФормаВЗависимостиОтВидаОперации(ПечатнаяФорма)
	
	Результат = ПечатнаяФорма = "В зависимости от вида операции";
	Возврат Результат;
	
КонецФункции

Функция ПолучитьПараметрыСФ(Док) Экспорт
	
	Результат = Неопределено;
	
	УчетНДС.ПолучитьПараметрыСчетаФактуры(Док, ПолучитьВалютуРегламентированногоУчета(), Результат);
	
	Если Не ЗначениеЗаполнено(Результат.СуммаНДСДокумента) Тогда
		Результат.СуммаНДСДокумента = Док.СуммаНДСДокумента;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

//{ Подготовка временных таблиц для списка отправки документов...

Функция ПодготовитьВТ_СпискаОтправкиДокументов(ТипыДокументов, Запрос) Экспорт
	
	МассивВременныхТаблиц= Новый Массив;
	
	Если ТипыДокументов = 0 ИЛИ ТипыДокументов = 1 ИЛИ ТипыДокументов = 2 ИЛИ ТипыДокументов = 5 ИЛИ ТипыДокументов = 6 Тогда
		ПодготовитьВТ_СЧФ_Основная(Запрос, МассивВременныхТаблиц);
		ПодготовитьВТ_УПД_СЧФДОП(Запрос, МассивВременныхТаблиц, ТипыДокументов);
		ПодготовитьВТ_УКД_КСЧФДИС(Запрос, МассивВременныхТаблиц, ТипыДокументов);
	КонецЕсли;
	
	Если ТипыДокументов = 0 ИЛИ ТипыДокументов = 1 Тогда
		ПодготовитьВТ_Накладные(Запрос, МассивВременныхТаблиц, ТипыДокументов);
	КонецЕсли;
	
	Если ТипыДокументов = 0 ИЛИ ТипыДокументов = 2 Тогда
		ПодготовитьВТ_СчетаФактуры(Запрос, МассивВременныхТаблиц, ТипыДокументов);
	КонецЕсли;
	
	Если ТипыДокументов = 0 ИЛИ ТипыДокументов = 3 Тогда
		ПодготовитьВТ_СчетаНаОплату(Запрос, МассивВременныхТаблиц);
	КонецЕсли;
	
	Если ТипыДокументов = 0 ИЛИ ТипыДокументов = 4 Тогда
		ПодготовитьВТ_АктыСверки(Запрос, МассивВременныхТаблиц);
	КонецЕсли;
	
	Возврат МассивВременныхТаблиц;
	
КонецФункции


Процедура ПодготовитьВТ_СЧФ_Основная(Запрос, МассивВременныхТаблиц)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_СЧФ_Основная(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

Процедура ПодготовитьВТ_УПД_СЧФДОП(Запрос, МассивВременныхТаблиц, ТипыДокументов)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_УПД_СЧФДОП(Запрос, МассивВременныхТаблиц, ТипыДокументов);
	
КонецПроцедуры

Процедура ПодготовитьВТ_УКД_КСЧФДИС(Запрос, МассивВременныхТаблиц, ТипыДокументов)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_УКД_КСЧФДИС(Запрос, МассивВременныхТаблиц, ТипыДокументов);
	
КонецПроцедуры

Процедура ПодготовитьВТ_СчетаФактуры(Запрос, МассивВременныхТаблиц, ТипыДокументов)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_СчетаФактуры(Запрос, МассивВременныхТаблиц, ТипыДокументов);
	
КонецПроцедуры

Процедура ПодготовитьВТ_АктыСверки(Запрос, МассивВременныхТаблиц) Экспорт
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_АктыСверки(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры


Процедура ПодготовитьВТ_Накладные(Запрос, МассивВременныхТаблиц, ТипыДокументов)
	
	ПодготовитьВТ_Накладные_РеализацияТоваровУслуг(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_Накладные_ВозвратТоваровПоставщику(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_Накладные_РеализацияОтгруженныхТоваров(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_Накладные_АктОбОказанииПроизводственныхУслуг(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_Накладные_РеализацияУслугПоПереработке(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_Накладные_ОтчетКомитентуОПродажах(Запрос, МассивВременныхТаблиц);
	
	Если Метаданные.Документы.Найти("КорректировкаРеализации") <> Неопределено Тогда
		ПодготовитьВТ_Накладные_КорректировкаРеализации(Запрос, МассивВременныхТаблиц);
	КонецЕсли;
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_РеализацияТоваровУслуг(Запрос, МассивВременныхТаблиц)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_Накладные_РеализацияТоваровУслуг(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_КорректировкаРеализации(Запрос, МассивВременныхТаблиц)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_Накладные_КорректировкаРеализации(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_ВозвратТоваровПоставщику(Запрос, МассивВременныхТаблиц)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_Накладные_ВозвратТоваровПоставщику(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_РеализацияОтгруженныхТоваров(Запрос, МассивВременныхТаблиц) Экспорт
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_Накладные_РеализацияОтгруженныхТоваров(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_АктОбОказанииПроизводственныхУслуг(Запрос, МассивВременныхТаблиц)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_Накладные_АктОбОказанииПроизводственныхУслуг(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_РеализацияУслугПоПереработке(Запрос, МассивВременныхТаблиц)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_Накладные_РеализацияУслугПоПереработке(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_ОтчетКомитентуОПродажах(Запрос, МассивВременныхТаблиц)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_Накладные_ОтчетКомитентуОПродажах(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры


Процедура ПодготовитьВТ_СчетаНаОплату(Запрос, МассивВременныхТаблиц)
	
	ПодготовитьВТ_СчетаНаОплату_СчетНаОплатуПокупателю(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_СчетаНаОплату_РеализацияТоваровУслуг(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_СчетаНаОплату_ЗаказПокупателя(Запрос, МассивВременныхТаблиц);
	
	Если Метаданные.Документы.Найти("КорректировкаРеализации") <> Неопределено Тогда
		ПодготовитьВТ_СчетаНаОплату_КорректировкаРеализации(Запрос, МассивВременныхТаблиц);
	КонецЕсли;
	
КонецПроцедуры

Процедура ПодготовитьВТ_СчетаНаОплату_СчетНаОплатуПокупателю(Запрос, МассивВременныхТаблиц)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_СчетаНаОплату_СчетНаОплатуПокупателю(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

Процедура ПодготовитьВТ_СчетаНаОплату_РеализацияТоваровУслуг(Запрос, МассивВременныхТаблиц)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_СчетаНаОплату_РеализацияТоваровУслуг(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

Процедура ПодготовитьВТ_СчетаНаОплату_КорректировкаРеализации(Запрос, МассивВременныхТаблиц)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_СчетаНаОплату_КорректировкаРеализации(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

Процедура ПодготовитьВТ_СчетаНаОплату_ЗаказПокупателя(Запрос, МассивВременныхТаблиц)
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПодготовитьВТ_СчетаНаОплату_ЗаказПокупателя(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

//} Подготовка временных таблиц для списка отправки документов...

//} ФОРМИРОВАНИЕ СПИСКА ВЫГРУЗКИ

//////////////////////////////////////////////////////
//функции генерации структуры для ФУФ счета-фактуры
///////////////////////////////////////////////////////

Функция ПолучитьДатуНомерСвидетельстваОГосРегистрацииИП(Организация) Экспорт 
	Если ЗначениеЗаполнено(Организация.СвидетельствоСерияНомер) И ЗначениеЗаполнено(Организация.СвидетельствоДатавыдачи) Тогда 
		Возврат  Организация.СвидетельствоСерияНомер + " от " + Формат(Организация.СвидетельствоДатавыдачи, "ДФ=dd.MM.yyyy")
	Иначе 
		Возврат "";
	КонецЕсли;
	
КонецФункции

//Функция по ИСФ/КСФ возвращает сф-основание
Функция ПолучитьСФОснованиеДляИСФДиадок(ИСФ) Экспорт  
   ДокОснование = ИСФ.ДокументыОснования[0].ДокументОснование;
	Если ДокОснование.Видоперации = перечисления.ВидыОперацийИсправленияПоступленияРеализации.СогласованноеИзменение Тогда 
		Докоснование = ДокОснование.ДокументРеализации
	Иначе 	
		Пока (ТипЗнч(Докоснование) = Тип("ДокументСсылка.КорректировкаРеализации")) 
			И (ДокОснование.ВидОперации = перечисления.ВидыОперацийИсправленияПоступленияРеализации.ИсправлениеОшибки) Цикл
			ДокОснование = ДокОснование.ДокументРеализации
		КонецЦикла;
	КонецЕсли;	
	
	//ищем СФ
	запрос = Новый запрос;
	Запрос.Текст = "ВЫБРАТЬ
	               |	СчетФактураВыданныйДокументыОснования.ДокументОснование,
	               |	СчетФактураВыданныйДокументыОснования.Ссылка
	               |ИЗ
	               |	Документ.СчетФактураВыданный.ДокументыОснования КАК СчетФактураВыданныйДокументыОснования
	               |ГДЕ
	               |	СчетФактураВыданныйДокументыОснования.Ссылка.ПометкаУдаления = Ложь
	               |	И СчетФактураВыданныйДокументыОснования.ДокументОснование = &ДокументОснование";
	Запрос.УстановитьПараметр("Документоснование", ДокОснование);
	выборка = запрос.Выполнить().Выбрать();
	Если выборка.Следующий() Тогда 
		Возврат  выборка.ссылка;
	КонецЕсли;	
	
	Возврат Неопределено;
КонецФункции	

Функция СФОтправлена(ДокСсылка)
	ЗапросСФ = Новый Запрос;
	ЗапросСФ.Текст = "
	|ВЫБРАТЬ
	|	Док.Ссылка ДокСсылка
	|ИЗ
	|	Документ.СчетФактураВыданный КАК Док
	|ГДЕ
	|	Док.ДокументОснование = &ДокОснование";
	ЗапросСФ.УстановитьПараметр("ДокОснование", ДокСсылка);
	ВыборкаСФ = ЗапросСФ.Выполнить().Выбрать();
	Пока ВыборкаСФ.Следующий() Цикл
		DocumentId = ПолучитьМодульПрог("Модуль_Данные1С_СвязиОбъектов").Документ_2_DocumentID(ВыборкаСФ.ДокСсылка);
		Если ЗначениеЗаполнено(DocumentId) Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Ложь;
КонецФункции

Функция ПолучитьПоследнееИсправление(КоррСтарт, ПоследнееОтправленное) Экспорт
	текКор = КоррСтарт;
	
	Пока Истина Цикл
		// получаем следующего кандидата, проверям его
		Запрос = Новый Запрос;
		Запрос.Текст = "
		|ВЫБРАТЬ
		|	Док.Ссылка ДокСсылка
		|ИЗ
		|	Документ.КорректировкаРеализации КАК Док
		|ГДЕ
		|	Док.ДокументРеализации = &ДокСсылка
		|	И Док.ВидОперации = &ВидОперации";
		Запрос.УстановитьПараметр("ДокСсылка", текКор);
		Запрос.УстановитьПараметр("ВидОперации", Перечисления.ВидыОперацийИсправленияПоступленияРеализации.ИсправлениеОшибки);
		Выборка = Запрос.Выполнить().Выбрать();
		Если Выборка.Следующий() Тогда
			// Проверяем кандидата
			Если ПоследнееОтправленное И НЕ СФОтправлена(Выборка.ДокСсылка) Тогда
				Прервать;
			КонецЕсли;
		Иначе
			Прервать;
		КонецЕсли;
		
		текКор = Выборка.ДокСсылка;
	КонецЦикла;
	
	Возврат текКор;
КонецФункции

Функция ПолучитьДочернееИсправление(ДокСсылка) Экспорт
	Запрос = Новый Запрос;
	Запрос.Текст = "
	|ВЫБРАТЬ
	|	Док.Ссылка ДокССылка
	|ИЗ
	|	Документ.КорректировкаРеализации КАК Док
	|ГДЕ
	|	Док.ДокументРеализации = &ДокСсылка
	|	И Док.ВидОперации = &ВидОперации";
	Запрос.УстановитьПараметр("ДокСсылка", ДокСсылка);
	Запрос.УстановитьПараметр("ВидОперации", Перечисления.ВидыОперацийИсправленияПоступленияРеализации.ИсправлениеОшибки);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		Возврат Выборка.ДокССылка;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
КонецФункции

Функция ПолучитьНеотправленнуюКорректировку(ДокСсылка) Экспорт
	Запрос = Новый Запрос;
	Запрос.Текст = "
	|ВЫБРАТЬ
	|	Док.Ссылка ДокССылка
	|ИЗ
	|	Документ.КорректировкаРеализации КАК Док
	|ГДЕ
	|	Док.ДокументРеализации = &ДокСсылка
	|	И Док.ВидОперации = &ВидОперации";
	Запрос.УстановитьПараметр("ДокСсылка", ДокСсылка);
	Запрос.УстановитьПараметр("ВидОперации", Перечисления.ВидыОперацийИсправленияПоступленияРеализации.СогласованноеИзменение);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Если НЕ СФОтправлена(Выборка.ДокССылка) Тогда
			Возврат Выборка.ДокССылка;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Неопределено;
КонецФункции

Функция ПолучитьКорректировкуОбъект(ТипНовогоДокумента, КорректировкаРеализацииОснование) Экспорт
	КорректировкаОбъект = Документы.КорректировкаРеализации.СоздатьДокумент();
	Если ТипНовогоДокумента = "InvoiceRevision" ИЛИ ТипНовогоДокумента = "InvoiceCorrectionRevision" Тогда
		КорректировкаОбъект.ВидОперации = Перечисления.ВидыОперацийИсправленияПоступленияРеализации.ИсправлениеОшибки;
	Иначе
		КорректировкаОбъект.ВидОперации = Перечисления.ВидыОперацийИсправленияПоступленияРеализации.СогласованноеИзменение;
	КонецЕсли;
	
	КорректировкаОбъект.Заполнить(КорректировкаРеализацииОснование);
	
	Возврат КорректировкаОбъект;
КонецФункции

Функция СчетФактураПолученныйПоДокументуОснование(ДокументОснование) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").СчетФактураПолученныйПоДокументуОснование(ДокументОснование);
	
КонецФункции

Функция СчетФактураВыданныйПоДокументуОснование(ДокументОснование) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").СчетФактураВыданныйПоДокументуОснование(ДокументОснование);	
	
КонецФункции


/////////////////////////////////////////////////////////////////////////////
// функции генерации сотруктуры для генерации торг-12 или акта выполненных работ
//////////////////////////////////////////////////////////////////////////////

Функция ПолучитьПредставлениеАдресаДляДиадок(записьрегистра) Экспорт 
	Возврат УправлениеКонтактнойИнформацией.ПолучитьПредставлениеАдреса(записьрегистра);
КонецФункции	

Функция ПолучитьВидЮрАдресаКонтрагента() Экспорт 
	Возврат Справочники.ВидыКонтактнойИнформации.ЮрАдресКонтрагента;
КонецФункции

Процедура ЗаполнитьСтруктуруАдреса(AddressInfo, ЮрФизЛицо, ИспользоватьЮрАдрес = Ложь, АдресСтрокой = "") Экспорт
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЗаполнитьСтруктуруАдреса(AddressInfo, ЮрФизЛицо, ИспользоватьЮрАдрес, АдресСтрокой);
	
КонецПроцедуры

Процедура ЗаполнитьСтруктуруГрузоПолучателяОтправителя(OrganizationInfo, ЮрФизЛицо, ДатаДокумента, АдресСтрокой = "") Экспорт 
	СведенияОбОрганизации =  ПолучитьСведенияОбОрганизации(ЮрФизЛицо, ДатаДокумента);
	Если стрДлина(СведенияОбОрганизации.ИНН)<>12 Тогда 
		НаименованиеОрганизации = СведенияОбОрганизации.ПолноеНаименование;
		ЭтоИП = Ложь;
	Иначе 
		Если типЗнч(юрФизЛицо) = тип("СправочникСсылка.Организации") Тогда 
			//для нашей организации - данные берем из нормализованного представления физлица
			НаименованиеОрганизации  = юрФизЛицо.ИндивидуальныйПредприниматель.Наименование;
		Иначе 
			НаименованиеОрганизации  = СведенияОбОрганизации.ПолноеНаименование 
		КонецЕсли;	  
		ЭтоИП = Истина;
	КонецЕсли;
	
	OrganizationInfo.Name = НаименованиеОрганизации;
	OrganizationInfo.IsSoleProprietor  =  ЭтоИП;
	OrganizationInfo.Ссылка = ЮрФизЛицо;
	OrganizationInfo.SameAsSellerOrBuyer = Ложь;
	
	ЗаполнитьСтруктуруАдреса(OrganizationInfo.Address, ЮрФизЛицо, Ложь, АдресСтрокой);
	
КонецПроцедуры	

Функция ПолучитьГоловноеЮрлицо( ЮрФизЛицо)
	
	ГоловноеЮрЛицо = "";
	
	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Контрагенты") Тогда
		ГоловноеЮрЛицо = ЮрФизЛицо.ГоловнойКонтрагент;	
	КонецЕсли;

	Если ЗначениеЗаполнено(ГоловноеЮрЛицо) И 
		(ЮрФизЛицо.Метаданные().Реквизиты.Найти("ОбособленноеПодразделение")
		<> Неопределено) И (ЮрФизЛицо.ОбособленноеПодразделение = Истина) Тогда
		
		Возврат ГоловноеЮрЛицо;
	Иначе 
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Процедура ЗаполнитьСтруктурируЮрФизЛица(OrganizationInfo, ЮрФизЛицо, ДатаДокумента, БанковскийСчетОрганизации = Неопределено,  ЮрАдрес, АдресДляСФ = Ложь, АдресСтрокой = "")  Экспорт
	
	ГоловноеЮрЛицо = "";
	
	Если АдресДляСФ = Истина Тогда
				
		ГоловноеЮрЛицо = ПолучитьГоловноеЮрлицо( ЮрФизЛицо);
		Если ЗначениеЗаполнено(ГоловноеЮрЛицо) Тогда
			СведенияОПодразделении 	= ПолучитьСведенияОбОрганизации(ЮрФизЛицо, ДатаДокумента,  БанковскийСчетОрганизации);
			СведенияОбОрганизации	= ПолучитьСведенияОбОрганизации(ГоловноеЮрЛицо, ДатаДокумента,  БанковскийСчетОрганизации);
			СведенияОбОрганизации.КПП = СведенияОПодразделении.КПП;
			ОрганизацияДляДанных = ГоловноеЮрЛицо;
		Иначе
			СведенияОбОрганизации = ПолучитьСведенияОбОрганизации(ЮрФизЛицо, ДатаДокумента,  БанковскийСчетОрганизации);
			ОрганизацияДляДанных = ЮрФизЛицо;
		КонецЕсли;	
	Иначе
		СведенияОбОрганизации = ПолучитьСведенияОбОрганизации(ЮрФизЛицо, ДатаДокумента,  БанковскийСчетОрганизации);
		ОрганизацияДляДанных = ЮрФизЛицо;
	КонецЕсли;
	
	Если стрДлина(СведенияОбОрганизации.ИНН)<>12 Тогда 
		НаименованиеОрганизации = СведенияОбОрганизации.ПолноеНаименование;
		ЭтоИП = Ложь;
	Иначе 
		Если типЗнч(юрФизЛицо) = тип("СправочникСсылка.Организации") Тогда 
		  //для нашей организации - данные берем из нормализованного представления физлица
		  НаименованиеОрганизации  = юрФизЛицо.ИндивидуальныйПредприниматель.Наименование;
		Иначе 
		  НаименованиеОрганизации  = СведенияОбОрганизации.ПолноеНаименование 
		КонецЕсли;	  
		ЭтоИП = Истина;
	КонецЕсли;
	
	OrganizationInfo.Name = НаименованиеОрганизации;
	OrganizationInfo.Inn  = СведенияОбОрганизации.ИНН;
	Если ЗначениеЗаполнено(ЮрФизЛицо.ЮрФизЛицо) Тогда
		OrganizationInfo.Kpp  = СведенияОбОрганизации.КПП;
	Иначе
		OrganizationInfo.Kpp  = ЮрФизЛицо.КПП;
	КонецЕсли;
	
	ЗаполнитьСтруктуруАдреса(OrganizationInfo.Address, ОрганизацияДляДанных, ЮрАдрес, АдресСтрокой);	
	OrganizationInfo.вставить("Ссылка", ОрганизацияДляДанных);
	
	OrganizationInfo.IsSoleProprietor  =  ЭтоИП;
	
	Если АдресДляСФ = Ложь Тогда

		OrganizationInfo.Okpo  = СведенияОбОрганизации.КодПоОкпо;
		
		Если найти(СведенияОбОрганизации.Телефоны, "факс:")>0 Тогда 
			телефон = СокрЛП(Лев(СведенияОбОрганизации.Телефоны, найти(СведенияОбОрганизации.Телефоны, "факс:") - 1));
			Если прав(телефон, 1) = ","  Тогда
				телефон = Лев(телефон, стрДлина(телефон)-1);
			КонецЕсли;
			факс = СокрЛП(прав(СведенияОбОрганизации.Телефоны, СтрДлина(СведенияОбОрганизации.Телефоны) - найти(СведенияОбОрганизации.Телефоны, "факс:") - 5));
		Иначе 
			Телефон = СведенияОбОрганизации.Телефоны;
			Факс = "";
		КонецЕсли;	
		OrganizationInfo.Phone = ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").СократитьПредставлениеТелефона(Телефон);
		OrganizationInfo.Fax   = ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").СократитьПредставлениеТелефона(Факс) ;
		OrganizationInfo.BankAccountNumber = ?(ЗначениеЗаполнено(СведенияОбОрганизации.НомерСчета),  СведенияОбОрганизации.НомерСчета, "") ;
		OrganizationInfo.BankName  =   ?(ЗначениеЗаполнено(СведенияОбОрганизации.Банк), СведенияОбОрганизации.Банк.наименование, "");
		OrganizationInfo.BankId =  ?(ЗначениеЗаполнено(СведенияОбОрганизации.БИК),  СокрЛП(СведенияОбОрганизации.БИК), "");
	КонецЕсли;
//	OrganizationInfo.Department  =  "";
КонецПроцедуры	

Процедура Заполнить_ExtendedOrganizationInfo(ExtendedOrganizationInfo, ЮрФизЛицо, ДатаДокумента, ИспользоватьЮрАдрес, БанковскийСчет= Неопределено, АдресСтрокой = "") Экспорт
	
	СведенияОбОрганизации= ПолучитьСведенияОбОрганизации(ЮрФизЛицо, ДатаДокумента, БанковскийСчет);
	СведенияОбОрганизации.Вставить("Ссылка", ЮрФизЛицо);
	
	Если ИспользоватьЮрАдрес Тогда
		
		ГоловноеЮрЛицо= ПолучитьГоловноеЮрлицо(ЮрФизЛицо);
		
		Если ЗначениеЗаполнено(ГоловноеЮрЛицо) Тогда
			
			СведенияОПодразделении= ПолучитьСведенияОбОрганизации(ЮрФизЛицо, ДатаДокумента,  БанковскийСчет);
			СведенияОбОрганизации=  ПолучитьСведенияОбОрганизации(ГоловноеЮрЛицо, ДатаДокумента, БанковскийСчет);
			
			СведенияОбОрганизации.Вставить("Ссылка", ГоловноеЮрЛицо);
			СведенияОбОрганизации.Вставить("КПП"   , СведенияОПодразделении.КПП);
			
		КонецЕсли;
		
	КонецЕсли;

	ЭтоОрганизация= ТипЗнч(СведенияОбОрганизации.Ссылка) = Тип("СправочникСсылка.Организации");
	ЭтоИП= СтрДлина(СведенияОбОрганизации.ИНН) = 12;
	
	Если ЭтоИП И ЭтоОрганизация Тогда
		// Для нашей организации, полное наименование берем из нормализованного представления физлица
		СведенияОбОрганизации.ПолноеНаименование= СведенияОбОрганизации.Ссылка.ИндивидуальныйПредприниматель.Наименование;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(СведенияОбОрганизации.КПП) И НЕ ЗначениеЗаполнено(СведенияОбОрганизации.Ссылка.ЮрФизЛицо) Тогда
		СведенияОбОрганизации.КПП= СведенияОбОрганизации.Ссылка.КПП;
	КонецЕсли;
	
	
	ExtendedOrganizationInfo.Name= 					СведенияОбОрганизации.ПолноеНаименование;
	ExtendedOrganizationInfo.Inn=  					СведенияОбОрганизации.ИНН;
	ExtendedOrganizationInfo.Kpp=  					СведенияОбОрганизации.КПП;
	ExtendedOrganizationInfo.Okpo= 					СведенияОбОрганизации.КодПоОкпо;
	ExtendedOrganizationInfo.CorrespondentAccount = СведенияОбОрганизации.КоррСчет;
	
	ExtendedOrganizationInfo.BankAccountNumber= ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").Значение1C_В_СтрокаCOM(СведенияОбОрганизации.НомерСчета);
	ExtendedOrganizationInfo.BankName=   		ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").Значение1C_В_СтрокаCOM(СведенияОбОрганизации.Банк);
	ExtendedOrganizationInfo.BankId=  			ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").Значение1C_В_СтрокаCOM(СведенияОбОрганизации.БИК);
	ExtendedOrganizationInfo.Phone = 			ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПредставлениеТелефона(СведенияОбОрганизации.Телефоны, "Телефон", 255);
	
	Если ЭтоОрганизация Тогда
		ExtendedOrganizationInfo.Okdp= СведенияОбОрганизации.Ссылка.КодОКВЭД;
	КонецЕсли;
	
	Если ЭтоИП Тогда
		ExtendedOrganizationInfo.Type= "IndividualEntity";
	ИначеЕсли ЭтоОрганизация Тогда
		
		Если СведенияОбОрганизации.Ссылка.ИностраннаяОрганизация
			И ПустаяСтрока(СведенияОбОрганизации.ИНН) Тогда
			// В организации может быть указано, что организация является отделением иностранной организации,
			//   но если у нее указан ИНН, значит, она встала на учет в России.
			// Будем считать ее иностранной только в том случае, если не указан ИНН.
			ExtendedOrganizationInfo.Type= "ForeignEntity";
		Иначе
			ExtendedOrganizationInfo.Type= "LegalEntity";
		КонецЕсли;
		
	Иначе
		
		Если СведенияОбОрганизации.Ссылка.НеЯвляетсяРезидентом
			И ПустаяСтрока(СведенияОбОрганизации.ИНН) Тогда
			// Логика такая же, как в организации выше.
			ExtendedOrganizationInfo.Type= "ForeignEntity";
		Иначе
			ExtendedOrganizationInfo.Type= "LegalEntity";
		КонецЕсли;
		
	КонецЕсли;
	
	ExtendedOrganizationInfo.Email = ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").АдресЭлектроннойПочты(СведенияОбОрганизации.Ссылка, Справочники.ВидыКонтактнойИнформации[?(ЭтоОрганизация, "EmailОрганизации", "АдресЭлектроннойПочтыКонтрагентаДляОбменаДокументами")]);
	
	ЗаполнитьСтруктуруАдреса(ExtendedOrganizationInfo.Address, СведенияОбОрганизации.Ссылка, ИспользоватьЮрАдрес, АдресСтрокой);	
	
	Если ExtendedOrganizationInfo.Type = "IndividualEntity"
		И ДатаДокумента >= ДатаРедакции425() Тогда
		
		Если ЕстьРеквизитМетаданных(ЮрФизЛицо.Метаданные(), "ОГРН") Тогда
			ОГРНОрганизации = ЗначенияРеквизитовОбъектаЛокальная(ЮрФизЛицо, "ОГРН").ОГРН;
		Иначе
			ОГРНОрганизации = "";
		КонецЕсли;
		
		ДатаРегистрации = "";
		
		ExtendedOrganizationInfo.IndividualEntityRegistrationCertificate = СтрокаНомерСВ_2026(
			ОГРНОрганизации,
			ДатаРегистрации
		);
		
	КонецЕсли;
	
	ExtendedOrganizationInfo.Вставить("Ссылка", СведенияОбОрганизации.Ссылка);
	
КонецПроцедуры	

Функция ДатаРедакции425()
	
	Возврат '20260101';
	
КонецФункции

Функция СтрокаНомерСВ_2026(ОГРН, ДатаРегистрации)
	
	Результат = "%1;%2";
	Результат = СтрЗаменить(Результат, "%1", ОГРН);
	Результат = СтрЗаменить(Результат, "%2", ДатаРегистрации);
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьСтруктуруОснования(ДокСсылка, ЭтоАкт = Ложь, ЭтоУПД = Ложь)  Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьСтруктуруОснования(ДокСсылка, ЭтоАкт, ЭтоУПД);
	
КонецФункции

Функция ПолучитьСтруктуруОснованияУПД(ДокументСсылка, ДанныеШапки)  Экспорт
	
	ЭтоАкт	= Ложь;
	ЭтоУПД	= Истина;
	
	Результат = ПолучитьСтруктуруОснования(ДокументСсылка, ЭтоАкт, ЭтоУПД);
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьБанковскийСчетПоДокументу(ДокТоварный) Экспорт 
	БанковскийСчетОрганизации =
		?(ДокТоварный.Метаданные().Реквизиты.Найти("БанковскийСчетОрганизации")
			= Неопределено, Неопределено, ДокТоварный.БанковскийСчетОрганизации);
	Если (ДокТоварный.Метаданные().Реквизиты.найти("СтруктурнаяЕдиница") <> Неопределено)
		И (ТипЗнч(ДокТоварный.СтруктурнаяЕдиница) = Тип("СправочникСсылка.БанковскиеСчета")) Тогда 
		БанковскийСчетОрганизации = ДокТоварный.СтруктурнаяЕдиница;
	ИначеЕсли (ДокТоварный.Метаданные().Реквизиты.найти("БанковскийСчет") <> Неопределено)
		И (ТипЗнч(ДокТоварный.Банковскийсчет) = Тип("СправочникСсылка.БанковскиеСчета")) Тогда 
		БанковскийСчетОрганизации = ДокТоварный.Банковскийсчет;
	КонецЕсли;	 
	Если (ЗначениеЗаполнено(БанковскийСчетОрганизации) = Ложь)
		И ЗначениеЗаполнено(ДокТоварный.Организация.ОсновнойБанковскийСчет) Тогда
		БанковскийСчетОрганизации = ДокТоварный.Организация.ОсновнойБанковскийСчет
	КонецЕсли;	
	Возврат БанковскийСчетОрганизации;
КонецФункции

Функция ПолучитьСведенияОбОрганизации(Организация, Дата, Банковскийсчет = Неопределено) 
	Возврат УправлениеКонтактнойИнформацией.СведенияОЮрФизЛице(Организация, Дата, , Банковскийсчет);
КонецФункции	

Функция ПолучитьОписаниеОрганизацииДляСчетаНаОплату(Контрагент, датаОперации, БанковскийСчет = Неопределено) Экспорт 
	
	СведенияОбОрганизации = ПолучитьСведенияОбОрганизации(Контрагент, датаОперации, БанковскийСчет);
	Возврат ФормированиеПечатныхФорм.ОписаниеОрганизации(СведенияОбОрганизации, "ПолноеНаименование,ИНН,КПП,ЮридическийАдрес,Телефоны,НомерСчета,Банк,БИК,КоррСчет");
	
КонецФункции	

Функция ПолучитьРеквизитыОрганизацииДляСчетаНаОплату(контрагент, ДатаОперации)  Экспорт 
	СведенияОбОрганизации 	  = ПолучитьСведенияОбОрганизации(Контрагент, датаОперации);
	Возврат Новый Структура("Наименование, ИНН, КПП", СведенияОбОрганизации.ПолноеНаименование, СведенияОбОрганизации.ИНН, СведенияОбОрганизации.КПП);
КонецФункции

Функция ПолучитьСтруктуруБанковскогоСчета(Организация, БанковскийСчетОрганизации) Экспорт
	
	Если ЗначениеЗаполнено(БанковскийСчетОрганизации) Тогда
		БанкССылка = БанковскийСчетОрганизации;
		Если БанкСсылка <> Неопределено Тогда 
			мБанк = ?(НЕ ЗначениеЗаполнено(БанкСсылка.БанкДляРасчетов), БанкСсылка.Банк, БанкСсылка.БанкДляРасчетов);
			мГородБанка = мБанк.Город;
			мКоррСчет   = мБанк.КоррСчет;
			мБИК        = мБанк.Код;
			мНомерСчета = ?(БанкСсылка.БанкДляРасчетов.Пустая(), БанкСсылка.НомерСчета, БанкСсылка.Банк.КоррСчет);
			
			Если ПустаяСтрока(БанкСсылка.ТекстКорреспондента) Тогда
				мТекстПолучателя = Организация.НаименованиеПолное;
				Если ЗначениеЗаполнено(БанкСсылка.БанкДляРасчетов) Тогда
					мТекстПолучателя = мТекстПолучателя + " р/с " + БанкСсылка.НомерСчета + " в " + БанкСсылка.Банк + " " + БанкСсылка.Банк.Город;
				КонецЕсли;	
			Иначе
				мТекстПолучателя = БанкСсылка.ТекстКорреспондента;
			КонецЕсли;		
			
		КонецЕсли;
		Возврат Новый Структура ("Банк,БИК,КоррСчет,ГородБанка,НомерМсета,ТекстПолучателя,НомерСчета", мБанк, мБИК, мКоррСчет, мГородБанка, мНомерСчета, мТекстПолучателя, мНомерСчета);
	Иначе
		Возврат Неопределено
	КонецЕсли;
	
КонецФункции

Функция ПолучитьПодразделениеИзДокумента(ДокСсылка) Экспорт 
	
	ПодразделениеСсылка =
		?(ДокСсылка.Метаданные().Реквизиты.Найти("Подразделение") = Неопределено, Неопределено, ДокСсылка.Подразделение);
	
	Если (Докссылка.метаданные().реквизиты.найти("ОтражатьВБухгалтерскомУчете") <> Неопределено)
		И (ДокСсылка.ОтражатьВБухгалтерскомУчете) И ЗначениеЗаполнено(ПодразделениеСсылка) Тогда
		наб = регистрысведений.СоответствиеПодразделенийИПодразделенийОрганизаций.СоздатьНаборЗаписей();
		наб.Отбор.подразделение.установить(ПодразделениеСсылка);
		наб.Отбор.организация.установить(ДокСсылка.Организация);
		наб.Прочитать();
		Если наб.Количество() > 0 Тогда 
			ПодразделениеСсылка = наб[0].ПодразделениеОрганизации;
		Иначе 
			ПодразделениеСсылка = "";
		КонецЕсли;	
	КонецЕсли;	
	
	Возврат ?(ЗначениеЗаполнено(ПодразделениеСсылка), ПодразделениеСсылка.наименование, "");
КонецФункции

Функция ПолучитьПредставлениеХарактеристикиИСерии(Выборка) Экспорт

	Возврат получитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьПредставлениеХарактеристикиИСерии(Выборка);

КонецФункции

Функция Документ_Организация(Документ) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").Документ_Организация(Документ);
	
КонецФункции

Функция Организация_КПП(Организация) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").Организация_КПП(Организация);
	
КонецФункции

Функция ПолучитьДатуНомерВходящейНакладной(ДокСсылка) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьДатуНомерВходящейНакладной(ДокСсылка)
	
КонецФункции

Функция ПолучитьДатуНомерВходящегоСчетаФактуры(ДокСсылка) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьДатуНомерВходящегоСчетаФактуры(ДокСсылка)
	
КонецФункции

Процедура УстановитьДатуНомерВходящейНакладной(ДокСсылка, ДатаВходящегоДокумента, НомерВходящегоДокумента, СписокОтличающихсяРеквизитов) Экспорт 
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").УстановитьДатуНомерВходящейНакладной(ДокСсылка, ДатаВходящегоДокумента, НомерВходящегоДокумента, СписокОтличающихсяРеквизитов);
	
КонецПроцедуры

Процедура УстановитьДатуНомерВходящегоСчетаФактуры(ДокСсылка, ДатаВходящегоДокумента, НомерВходящегоДокумента, СписокОтличающихсяРеквизитов) Экспорт 
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").УстановитьДатуНомерВходящегоСчетаФактуры(ДокСсылка, ДатаВходящегоДокумента, НомерВходящегоДокумента, СписокОтличающихсяРеквизитов);
	
КонецПроцедуры

//Позволяет переопределить имена реквизитов документа. Вызывается из метода "ИменаРеквизитов_НомерИДатаВходящегоДокумента"
//в "Модуль_ИнтеграцияУниверсальнаяЛогика"
//
//Параметры:
//	Результат 	- Структура			- коллекция переопределяемых реквизитов 
//	ДокСсылка	- ДокументСсылка	- документ, имена реквизитов которого проверяем
Процедура ПереопределитьИменаРеквизитов_НомерИДатаВходящегоДокумента(Результат, ДокСсылка) Экспорт
	
	//По умолчанию используем результат из "Модуль_ИнтеграцияУниверсальнаяЛогика" 
	
КонецПроцедуры //ПереопределитьИменаРеквизитов_НомерИДатаВходящегоДокумента()

//Позволяет переопределить имена реквизитов документа. Вызывается из метода "ИменаРеквизитов_КодСпособаПолучения"
//в "Модуль_ИнтеграцияУниверсальнаяЛогика"
//
//Параметры:
//	Результат 	- Строка			- имя переопределяемого реквизита 
//	ДокСсылка	- ДокументСсылка	- документ, имена реквизитов которого проверяем
Процедура ПереопределитьИменаРеквизитов_КодСпособаПолучения(Результат, ДокСсылка) Экспорт
	
	//По умолчанию используем результат из "Модуль_ИнтеграцияУниверсальнаяЛогика" 
	
КонецПроцедуры //ПереопределитьИменаРеквизитов_КодСпособаПолучения()

//Возвращает именя реквизита "Склад" у документа поступления
//так как оно заданно в текущей конфигурации
//
//Возвращаемое значение:
//	Строка
Функция ИменаРеквизитов_Склад() Экспорт

	Результат = ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ИменаРеквизитов_Склад();
	
	Возврат Результат;
	
КонецФункции //ИменаРеквизитов_Склад()

//////////////////////////////////////////////////////////
// функции генерации структуры акта сверки
////////////////////////////////////////////////////////////

Функция ПолучитьПредставителяОрганизацииДляАктаСверки(ДокСсылка)
	ПредставительОрганизации = Новый Структура;
	ДанныеПредставителя = ОбщегоНазначения.ДанныеФизЛица(ДокСсылка.Организация, ДокСсылка.ПредставительОрганизации, ДокСсылка.Дата);
	
	ПредставительОрганизации.Вставить("ФИО", сокрЛП(Строка(ДанныеПредставителя.Фамилия) + " " + ДанныеПредставителя.Имя + " " + ДанныеПредставителя.Отчество));
	ПредставительОрганизации.Вставить("Должность", Строка(ДанныеПредставителя.Должность));
	
	Возврат ПредставительОрганизации;
КонецФункции

Функция  ПолучитьПредставителяКонтрагентаДляАктаСверки(ДокСсылка)
	ПредставительКонтрагента = Новый Структура;
	ПредставительКонтрагента.Вставить("ФИО", ДокСсылка.ПредставительКонтрагента.наименование);
	ПредставительКонтрагента.Вставить("Должность", ДокСсылка.ПредставительКонтрагента.Должность);
	
	Возврат ПредставительКонтрагента;
КонецФункции

Функция ИнициализироватьТЧДляСверки()
	ТЗ = Новый ТаблицаЗначений;
	ТЗ.Колонки.Добавить("Дата");
	ТЗ.Колонки.Добавить("ПредставлениеДокумента");
	ТЗ.Колонки.Добавить("Дебет");
	ТЗ.Колонки.Добавить("Кредит");
	
	Возврат ТЗ;
КонецФункции		

Функция ПолучитьСтруктуруДанныхТекущегоДоговораАктаСверки(МассивДанныхПоДоговорам, ДоговорСсылка)
	
	Для Каждого элемент Из МассивДанныхПоДоговорам Цикл
		Если Элемент.ДоговорСсылка = ДоговорСсылка Тогда
			Возврат Элемент;
		КонецЕсли;
	КонецЦикла;
	
	СтруктураДанных = Новый Структура();
	СтруктураДанных.Вставить("ДанныеОрганизации", ИнициализироватьТЧДляСверки());
	СтруктураДанных.Вставить("ДанныеКонтрагента", ИнициализироватьТЧДляСверки());
	СтруктураДанных.Вставить("ДоговорСсылка"    , ДоговорСсылка);
	
	МассивДанныхПоДоговорам.Добавить(СтруктураДанных);
	
	Возврат СтруктураДанных;
	
КонецФункции

Функция ПолучитьДанныеТЧАктаСверки(ДокСсылка)
	запрос = Новый Запрос;
	запрос.Текст = "ВЫБРАТЬ
	|	АктСверкиВзаиморасчетовПоДаннымОрганизации.НомерСтроки,
	|	АктСверкиВзаиморасчетовПоДаннымОрганизации.Дата,
	|	АктСверкиВзаиморасчетовПоДаннымОрганизации.Представление,
	|	АктСверкиВзаиморасчетовПоДаннымОрганизации.Дебет,
	|	АктСверкиВзаиморасчетовПоДаннымОрганизации.Кредит,
	|	АктСверкиВзаиморасчетовПоДаннымОрганизации.ДоговорКонтрагента КАК Договор,
	|	Истина КАК ЭтоДанныеОрганизации
	|ИЗ
	|	Документ.АктСверкиВзаиморасчетов.ПоДаннымОрганизации КАК АктСверкиВзаиморасчетовПоДаннымОрганизации
	|ГДЕ
	|	АктСверкиВзаиморасчетовПоДаннымОрганизации.Ссылка = &Ссылка
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	АктСверкиВзаиморасчетовПоДаннымКонтрагента.НомерСтроки,
	|	АктСверкиВзаиморасчетовПоДаннымКонтрагента.Дата,
	|	АктСверкиВзаиморасчетовПоДаннымКонтрагента.Представление,
	|	АктСверкиВзаиморасчетовПоДаннымКонтрагента.Дебет,
	|	АктСверкиВзаиморасчетовПоДаннымКонтрагента.Кредит,
	|	АктСверкиВзаиморасчетовПоДаннымКонтрагента.ДоговорКонтрагента,
	|	Ложь
	|ИЗ
	|	Документ.АктСверкиВзаиморасчетов.ПоДаннымКонтрагента КАК АктСверкиВзаиморасчетовПоДаннымКонтрагента
	|ГДЕ
	|	АктСверкиВзаиморасчетовПоДаннымКонтрагента.Ссылка = &Ссылка"; 
	Запрос.УстановитьПараметр("Ссылка", ДокСсылка);
	Выборка = запрос.Выполнить().Выбрать();
	МассивДанныхПоДоговорам = Новый Массив;
	
	Пока выборка.Следующий() Цикл 
		СтруктураДанныхПоДоговору = ПолучитьСтруктуруДанныхТекущегоДоговораАктаСверки(МассивДанныхПоДоговорам,  Неопределено );
		ТаблицаДанных = ?(выборка.ЭтоДанныеОрганизации, СтруктураДанныхПоДоговору.ДанныеОрганизации,  СтруктураДанныхПоДоговору.ДанныеКонтрагента);
		стр = ТаблицаДанных.Добавить();
		Стр.Дата = выборка.Дата;
		Стр.представлениеДокумента = выборка.Представление;
		стр.Дебет = Выборка.Дебет;
		стр.Кредит = выборка.кредит;
	КонецЦикла;	 
	
	Возврат МассивДанныхПоДоговорам;
КонецФункции	

Функция ПолучитьНаименованиеОрганизацииДляАктаСверки(ОрганизацияКонтрагент)
	Возврат ?(ЗначениеЗаполнено(ОрганизацияКонтрагент.НаименованиеПолное), ОрганизацияКонтрагент.НаименованиеПолное, ОрганизацияКонтрагент.Наименование);
КонецФункции	 

Функция ПолучитьДанныеДляФормированияАктаСверки(ДокСсылка) Экспорт 
	
	ДанныеДляАктаСверки = Новый Структура();
	
	ДанныеДляАктаСверки.вставить("Дата", ДокСсылка.Дата);
	ДанныеДляАктаСверки.Вставить("Номер", ДокСсылка.Номер);
	ДанныеДляАктаСверки.Вставить("ДоговорКонтрагента", ДокСсылка.ДоговорКонтрагента);
	
	ДанныеДляАктаСверки.вставить("ДатаНачалаСверки", ДокСсылка.ДатаНачала);
	ДанныеДляАктаСверки.Вставить("ДатаокончанияСверки", ДокСсылка.Датаокончания);
	
	ДанныеДляАктаСверки.Вставить("ПредставлениеОрганизации", ПолучитьНаименованиеОрганизацииДляАктаСверки(ДокСсылка.Организация));
	ДанныеДляАктаСверки.Вставить("ПредставлениеКонтрагента", ПолучитьНаименованиеОрганизацииДляАктаСверки(ДокСсылка.Контрагент));
	
	ДанныеДляАктаСверки.Вставить("ПредставительОрганизации", ПолучитьПредставителяОрганизацииДляАктаСверки(ДокСсылка));
	ДанныеДляАктаСверки.Вставить("ПредставительКонтрагента", ПолучитьПредставителяКонтрагентаДляАктаСверки(ДокСсылка));
	
	ДанныеДляАктаСверки.вставить("ВалютаДокумента", ДокСсылка.ВалютаДокумента);
	ДанныеДляАктаСверки.вставить("МассивДанныхПоДоговорам", ПолучитьДанныеТЧАктаСверки(ДокСсылка));
	
	ДанныеДляАктаСверки.Вставить("СальдоНачальное",  ДокСсылка.ОстатокНаНачало);
	ДанныеДляАктаСверки.Вставить("СверкаСогласована", ДокСсылка.СверкаСогласована);
	ДанныеДляАктаСверки.Вставить("Расхождение", ДокСсылка.Расхождение);
	
	Возврат ДанныеДляАктаСверки;
	
КонецФункции	

//////////////////////////////////////////////////////////
// функции для работы с формами 1С
//////////////////////////////////////////////////////////

Функция ПолучитьФормуВыбораДоговораКонтрагента(Элемент, Организация, ДатаДокумента, Контрагент, ВидыДоговоров, ДоговорКонтрагента) Экспорт
	
	ФрмВыбора = Справочники.ДоговорыКонтрагентов.ПолучитьФормуВыбора(, Элемент);
	ФрмВыбора.ПараметрОтборПоВладельцу = Контрагент;
	ФрмВыбора.ЭлементыФормы.СправочникСписок.НастройкаОтбора.Владелец.Доступность = Ложь;
	ФрмВыбора.СправочникСписок.Отбор.ВидДоговора.ВидСравнения                    = ВидСравнения.ВСписке;
	ФрмВыбора.СправочникСписок.Отбор.ВидДоговора.Значение                            = ВидыДоговоров;
	ФрмВыбора.СправочникСписок.Отбор.ВидДоговора.Использование                       = Истина;
	ФрмВыбора.ЭлементыФормы.СправочникСписок.НастройкаОтбора.ВидДоговора.Доступность = Ложь;
	
	ФрмВыбора.СправочникСписок.Отбор.Организация.Значение                            = Организация;
	ФрмВыбора.СправочникСписок.Отбор.Организация.Использование                       = Истина;
	ФрмВыбора.СправочникСписок.Отбор.Организация.ВидСравнения                        = ВидСравнения.Равно;
	ФрмВыбора.ЭлементыФормы.СправочникСписок.НастройкаОтбора.Организация.Доступность = Ложь;
	
	ФрмВыбора.ПараметрВыборГруппИЭлементов = ИспользованиеГруппИЭлементов.Элементы;
	
	Возврат ФрмВыбора;	
КонецФункции

/////////////////////////////////////////////////////////////
// функции создания новых доков на основании данных ДД
/////////////////////////////////////////////////////////////

Функция ПолучитьПодразделениеПоПодразделениюОрганизации(ПодразделениеОрганизации)
	
	Попытка
		Запрос = Новый Запрос;
		Запрос.Текст = "ВЫБРАТЬ
			|	СоответствиеПодразделенийИПодразделенийОрганизаций.Подразделение
			|ИЗ
			|	РегистрСведений.СоответствиеПодразделенийИПодразделенийОрганизаций КАК СоответствиеПодразделенийИПодразделенийОрганизаций
			|ГДЕ
			|	СоответствиеПодразделенийИПодразделенийОрганизаций.ПодразделениеОрганизации = &ПодразделениеОрганизации";
		
		Запрос.УстановитьПараметр("ПодразделениеОрганизации", ПодразделениеОрганизации);
		
		Результат = Запрос.Выполнить().Выбрать();
		Если Результат.Следующий() Тогда 
			Возврат Результат.Подразделение;
		Иначе 
			Возврат Неопределено;
		КонецЕсли;	
	
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции	

Функция СоздатьДокументПоступлениеТоваровУслуг(параметры, стВероятныеПоля, ЭДОбъект, ТабличнаяЧасть = Неопределено, СуммаВключаетНДС = Истина, ВидОперации, ЗаполнениеГТД = Ложь) Экспорт
	
	Если видОперации = "ВозвратОтПокупателя" Тогда 
		ДокОбъект = Документы.ВозвратТоваровОтПокупателя.СоздатьДокумент();
		ДокОбъект.ВидОперации = перечисления.ВидыОперацийВозвратТоваровОтПокупателя.ПродажаКомиссия;
	ИначеЕсли  видОперации = "ПоступлениеДопРасходов" Тогда
		ДокОбъект = Документы.ПоступлениеДопРасходов.СоздатьДокумент();
		ДокОбъект.ВидОперации = перечисления.ВидыОперацийПоступлениеДопРасходов.УслугаСтороннейОрганизации;
	Иначе 	
		ДокОбъект = Документы.ПоступлениеТоваровУслуг.СоздатьДокумент();
		Если видоперации =  "ПоступлениеОборудования" Тогда 
			ДокОбъект.ВидОперации =   Перечисления.ВидыОперацийПоступлениеТоваровУслуг.Оборудование
		ИначеЕсли   видОперации = "ПоступлениеНМА" Тогда
			ДокОбъект = Документы.ПоступлениеНМА.СоздатьДокумент();
    	ИначеЕсли видОперации = "ПоступлениеОбъектовСтроительства"	Тогда 
			ДокОбъект.ВидОперации = перечисления.ВидыОперацийПоступлениеТоваровУслуг.ОбъектыСтроительства 
		Иначе 	
			ДокОбъект.ВидОперации = Перечисления.ВидыОперацийПоступлениеТоваровУслуг.ПокупкаКомиссия;
		КонецЕсли;	
	КонецЕсли;	
	
	ДокОбъект.ДоговорКонтрагента = параметры.ДоговорКонтрагента;
	ДокОбъект.ВалютаДокумента = ДокОбъект.ДоговорКонтрагента.ВалютаВзаиморасчетов;
	ДокОбъект.КратностьВзаиморасчетов= 1;
	
	Если ВидОперации <> "ВозвратОтПокупателя" Тогда
		ДокОбъект.ДатаВходящегоДокумента = параметры.ДатаВходящегоДокумента;
		ДокОбъект.НомерВходящегоДокумента = параметры.НомерВходящегоДокумента;
	КонецЕсли;
	
	Если (НЕ ДокОбъект.Метаданные().Реквизиты.Найти("ОтражатьВБухгалтерскомУчете") = Неопределено)
		И Параметры.Свойство("ОтражатьВБухУчете") = Истина Тогда
		
		ДокОбъект.ОтражатьВБухгалтерскомУчете = Параметры.ОтражатьВБухУчете;	
	КонецЕсли;
	
	Если (НЕ ДокОбъект.Метаданные().Реквизиты.Найти("ОтражатьВНалоговомУчете") = Неопределено) 
		И Параметры.Свойство("ОтражатьВНалУчете") = Истина Тогда
		
		ДокОбъект.ОтражатьВНалоговомУчете = Параметры.ОтражатьВНалУчете;	
	КонецЕсли;
	
	Если (НЕ ДокОбъект.Метаданные().Реквизиты.Найти("ОтражатьВУправленческомУчете") = Неопределено) 
		И Параметры.Свойство("ОтражатьВУпрУчете") = Истина Тогда
		
		ДокОбъект.ОтражатьВУправленческомУчете = Параметры.ОтражатьВУпрУчете;	
	КонецЕсли;
	
	Если НЕ ДокОбъект.Метаданные().Реквизиты.Найти("РегистрироватьЦеныПоставщика") = Неопределено Тогда
		ДокОбъект.РегистрироватьЦеныПоставщика = ПолучитьМодульПрог("Модуль_1САдаптерНастройкиТекущегоПользователя_Типовые"). ПолучитьНастройку("Регистрировать цены поставщика", Ложь);
	КонецЕсли;
	
	//заполним склад и подразделение
	Если ДокОбъект.Метаданные().Реквизиты.Найти("СкладОрдер")<>Неопределено Тогда
		ДокОбъект.ВидПоступления = перечисления.ВидыПоступленияТоваров.НаСклад;
		ДокОбъект.СкладОрдер = параметры.Склад;
	КонецЕсли;	
	
	Если ДокОбъект.Метаданные().Реквизиты.Найти("ПодразделениеОрганизации")<>Неопределено Тогда 
		ДокОбъект.ПодразделениеОрганизации = параметры.Подразделение;
		
		Если ЗначениеЗаполнено(ДокОбъект.ПодразделениеОрганизации)
			И (ДокОбъект.Метаданные().Реквизиты.Найти("Подразделение") <> Неопределено) Тогда
			ДокОбъект.Подразделение = ПолучитьПодразделениеПоПодразделениюОрганизации(ДокОбъект.ПодразделениеОрганизации);
		КонецЕсли;	
	КонецЕсли;
	
	// Заполнение возможных полей
	Если стВероятныеПоля.Организация = Неопределено Тогда
		Сообщить("Организация в системе Диадок: """ + ЭДОбъект.Organization.Name + """, ИНН/КПП: " + ЭДОбъект.Organization.Inn + "/" + ЭДОбъект.Organization.Kpp + " не ассоциирована с организацией в 1С");
	Иначе
		ДокОбъект.Организация = стВероятныеПоля.Организация;
	КонецЕсли;
	Если стВероятныеПоля.Контрагент = Неопределено Тогда
		Сообщить("Контрагент в системе Диадок: """ + ЭДОбъект.counterAgent.Name + """, ИНН/КПП: " + ЭДОбъект.counterAgent.Inn + "/" + ЭДОбъект.counterAgent.Kpp + " не ассоциирован с контрагентом в 1С");
	Иначе
		ДокОбъект.Контрагент = стВероятныеПоля.Контрагент;
		
		//заполняем договор
		мСтруктураПараметровДляПолученияДоговора = ЗаполнениеДокументов.ПолучитьСтруктуруПараметровДляПолученияДоговораПокупки();
		ЗаполнениеДокументов.ПриИзмененииЗначенияКонтрагента(ДокОбъект, мСтруктураПараметровДляПолученияДоговора);
	КонецЕсли;
	
	Если ВидОперации = "ПоступлениеДопРасходов" Тогда
		ЗаполнениеПоступленияДопРасходов(ДокОбъект, ЭДОбъект, СуммаВключаетНДС);
	Иначе
		Если ТабличнаяЧасть <> Неопределено Тогда
			ЗаполнениетабличнойЧасти(ДокОбъект, ТабличнаяЧасть, ВидОперации);
		КонецЕсли;
	КонецЕсли;
	
	ДокОбъект.СуммаВключаетНДС = СуммаВключаетНДС;
	ДокОбъект.УчитыватьНДС = Истина;
	
	Возврат ДокОбъект;
	
КонецФункции

Функция СоздатьДокументВозвратТоваровОтПокупателя(параметры, стВероятныеПоля, ЭДОбъект) Экспорт
	ДокОбъект = Документы.ВозвратТоваровОтПокупателя.СоздатьДокумент();
	ДокОбъект.ВидОперации = Перечисления.ВидыОперацийВозвратТоваровОтПокупателя.ПродажаКомиссия;
	
	ДокОбъект.КратностьВзаиморасчетов = 1;
	
	// Заполнение возможных полей
	Если стВероятныеПоля.Организация = Неопределено Тогда
		Сообщить("Организация в системе Диадок: """ + ЭДОбъект.Organization.Name + """, ИНН/КПП: " + ЭДОбъект.Organization.Inn + "/" + ЭДОбъект.Organization.Kpp + " не ассоциирована с организацией в 1С");
	Иначе
		ДокОбъект.Организация = стВероятныеПоля.Организация;
	КонецЕсли;
	Если стВероятныеПоля.Контрагент = Неопределено Тогда
		Сообщить("Контрагент в системе Диадок: """ + ЭДОбъект.counterAgent.Name + """, ИНН/КПП: " + ЭДОбъект.counterAgent.Inn + "/" + ЭДОбъект.counterAgent.Kpp + " не ассоциирован с контрагентом в 1С");
	Иначе
		ДокОбъект.Контрагент = стВероятныеПоля.Контрагент;
	КонецЕсли;
	
	мСписокВидовДоговоров = Новый СписокЗначений;
	мСписокВидовДоговоров.Добавить(Перечисления.ВидыДоговоровКонтрагентов.СПокупателем);
	Если ДокОбъект.ВидОперации <> Перечисления.ВидыОперацийВозвратТоваровОтПокупателя.Оборудование Тогда
		мСписокВидовДоговоров.Добавить(Перечисления.ВидыДоговоровКонтрагентов.СКомиссионером);
	КонецЕсли;
	
	СтруктураПараметровДляПолученияДоговора=	Новый Структура();
	СтруктураПараметровДляПолученияДоговора.Вставить("СписокДопустимыхВидовДоговоров", мСписокВидовДоговоров);
	
	ЗаполнениеДокументов.ПриИзмененииЗначенияКонтрагента(ДокОбъект, СтруктураПараметровДляПолученияДоговора);
	
	Возврат ДокОбъект;
КонецФункции

Процедура ЗаполнитьРеквизиты_СчетФактураПолученный(ДокументОбъект, Document, ПараметрыЗаполнения) Экспорт
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЗаполнитьРеквизиты_СчетФактураПолученный(ДокументОбъект, Document, ПараметрыЗаполнения);
	
КонецПроцедуры

Функция СоздатьДокумент_СчетФактураПолученный(Document, ДокументОснование, ПараметрыЗаполнения) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").СоздатьДокумент_СчетФактураПолученный(Document, ДокументОснование, ПараметрыЗаполнения);
	
КонецФункции

Функция СоздатьДокумент_СчетФактураВыданный(Document, ДокументОснование, ПараметрыЗаполнения) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").СоздатьДокумент_СчетФактураВыданный(Document, ДокументОснование, ПараметрыЗаполнения);
	
КонецФункции

Процедура УстановитьДоговорВСчетеФактуреДляДиадокПР(СчетФактура) Экспорт
	мСписокВидовДоговоров = Новый СписокЗначений;
	Если ТипЗнч(СчетФактура) = тип("ДокументОбъект.СчетФактураПолученный") Тогда
		мСписокВидовДоговоров.Добавить(Перечисления.ВидыДоговоровКонтрагентов.СПоставщиком);
		мСписокВидовДоговоров.Добавить(Перечисления.ВидыДоговоровКонтрагентов.СКомитентом);
		СчетФактура.ВидСчетаФактуры = перечисления.ВидСчетаФактурыПолученного.НаПоступление;
	Иначе
		мСписокВидовДоговоров.Добавить(Перечисления.ВидыДоговоровКонтрагентов.СПокупателем);
		мСписокВидовДоговоров.Добавить(Перечисления.ВидыДоговоровКонтрагентов.СКомиссионером);
		СчетФактура.ВидСчетаФактуры = перечисления.ВидСчетаФактурыВыставленного.НаРеализацию;
	КонецЕсли;
	СтруктураПараметровДляПолученияДоговора = Новый Структура;
	СтруктураПараметровДляПолученияДоговора.Вставить("СписокДопустимыхВидовДоговоров", мСписокВидовДоговоров);
	
	ЗаполнениеДокументов.ПриИзмененииЗначенияКонтрагента(СчетФактура, СтруктураПараметровДляПолученияДоговора);
	
КонецПроцедуры

Функция ПолучитьСписокВидовДоговоров(ВидОперации = Неопределено) Экспорт
	СписокВидовДоговоров = Новый СписокЗначений;
	СписокВидовДоговоров.Добавить(Перечисления.ВидыДоговоровКонтрагентов.СПоставщиком);
	СписокВидовДоговоров.Добавить(Перечисления.ВидыДоговоровКонтрагентов.СКомитентом);
	СписокВидовДоговоров.Добавить(Перечисления.ВидыДоговоровКонтрагентов.СКомиссионером);
	
	Если ВидОперации = "ВозвратОтПокупателя" Тогда
		СписокВидовДоговоров.Добавить(Перечисления.ВидыДоговоровКонтрагентов.СПокупателем);
	КонецЕсли;
	
	Возврат СписокВидовДоговоров;
КонецФункции

Функция ДиадокПолучитьОсновнойДоговорКонтрагента(Контрагент, Организация, СписокВидовДоговоров) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ДиадокПолучитьОсновнойДоговорКонтрагента(Контрагент, Организация, СписокВидовДоговоров);
	
КонецФункции

/////////////////////////////////////////////////////////////
// Связи объектов
/////////////////////////////////////////////////////////////

Функция ПолучитьСтавкуНДСДиадок(TaxRate) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьСтавкуНДСДиадок(TaxRate);
	
КонецФункции

Функция ПолучитьЕдиницуИзмерения(Код, Наименование, Номенклатура) Экспорт
	
	// Перебираем все единицы измерения номенклатуры, и ищем по совпадению кода или наименования, либо по наличию коэффициента пересчета.
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	Т.Ссылка,
	|	Классификатор.Наименование,
	|	Классификатор.Код
	|ИЗ
	|	Справочник.ЕдиницыИзмерения КАК Т
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.КлассификаторЕдиницИзмерения КАК Классификатор
	|		ПО Т.ЕдиницаПоКлассификатору = Классификатор.Ссылка
	|ГДЕ
	|	Т.Владелец = &Номенклатура");
	
	Запрос.УстановитьПараметр("Номенклатура", Номенклатура);
	
	РезультатЗапроса= Запрос.Выполнить();
	Запрос= Неопределено;
	
	Если НЕ РезультатЗапроса.Пустой() Тогда
		
		Выборка= РезультатЗапроса.Выбрать();
		РезультатЗапроса= Неопределено;
		
		Пока Выборка.Следующий() Цикл
			Если СокрЛП(Выборка.Код) = Код ИЛИ СокрЛП(Выборка.Наименование) = Наименование Тогда 
				Возврат Выборка.Ссылка;
			КонецЕсли;	
		КонецЦикла;
		
		Выборка.Сбросить();
		Пока Выборка.Следующий() Цикл
			КоэффициентПересчета= ОсновнойМодуль.ПолучитьКоэффициентПересчета(Код, Выборка.Код);
			Если ЗначениеЗаполнено(КоэффициентПересчета) Тогда 
				Возврат Выборка.Ссылка;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Справочники.ЕдиницыИзмерения.ПустаяСсылка();
	
КонецФункции

Функция ПолучитьКоэффициентПересчета(КодЕИ, НаименованиеЕИ, НоваяЕдиницаИзмерения) Экспорт
	
	Если (НоваяЕдиницаИзмерения.ЕдиницаПоКлассификатору.код = КодЕИ) 
		Или (НоваяЕдиницаИзмерения.ЕдиницаПоКлассификатору.наименование = НаименованиеЕИ) Тогда 
		Возврат  1;
	КонецЕсли;	
	
	Коэф = ОсновнойМодуль.ПолучитьКоэффициентПересчета(КодЕИ, НоваяЕдиницаИзмерения.ЕдиницаПоКлассификатору.код);
	Возврат ?(коэф = Неопределено, 1, коэф);
	
КонецФункции

Процедура ЗаполнениетабличнойЧасти(ДокОбъект, ТабличнаяЧасть, ВидОперации)
	
	Для каждого стр Из ТабличнаяЧасть Цикл
		
		ИменаТабличныхЧастей = ИменаТабличныхЧастейПоступлениеТоваровУслуг();
		
		ИмяТЧ = "";
		Если видОперации = "ВозвратОтПокупателя" Или стр.Тип = "Товар" Тогда
			
			стрОбъект = ДокОбъект.Товары.Добавить();
			ИмяТЧ = ИменаТабличныхЧастей.Товары;
			Если видОперации <> "ВозвратОтПокупателя" Тогда
				стрОбъект.ЕдиницаИзмерения = стр.ЕдиницаИзмерения;
				стрОбъект.Коэффициент = стр.ЕдиницаИзмерения.Коэффициент;
			КонецЕсли;
			
		ИначеЕсли стр.Тип = "Оборудование" Тогда
			
			стрОбъект = ДокОбъект.Оборудование.Добавить();
			ИмяТЧ = ИменаТабличныхЧастей.Оборудование;
			стрОбъект.ЕдиницаИзмерения = стр.ЕдиницаИзмерения;
			стрОбъект.Коэффициент = стр.ЕдиницаИзмерения.Коэффициент;
			
		ИначеЕсли стр.Тип = "Объект стр" Тогда 	
			
			стрОбъект = ДокОбъект.ОбъектыСтроительства.Добавить();
			ИмяТЧ = ИменаТабличныхЧастей.ОбъектыСтроительства;
			
		ИначеЕсли стр.Тип = "НМА" Тогда	
			
			стрОбъект = ДокОбъект.НематериальныеАктивы.Добавить();
			ИмяТЧ = ИменаТабличныхЧастей.НематериальныеАктивы;

		Иначе
			
			стрОбъект = ДокОбъект.Услуги.Добавить();
			ИмяТЧ = ИменаТабличныхЧастей.Услуги;

			стрОбъект.Содержание = стр.НоменклатураИзЭД;
			стрОбъект.Номенклатура = стр.Номенклатура;
			
			Если НЕ ЗначениеЗаполнено(стрОбъект.СтатьяЗатрат) Тогда
				стрОбъект.СтатьяЗатрат = стрОбъект.Номенклатура.СтатьяЗатрат;
			КонецЕсли;
			Если НЕ ЗначениеЗаполнено(стрОбъект.НоменклатурнаяГруппа) Тогда
				стрОбъект.НоменклатурнаяГруппа = стрОбъект.Номенклатура.НоменклатурнаяГруппаЗатрат;
			КонецЕсли;
			
			Если ДокОбъект.Метаданные().Реквизиты.найти("ПодразделениеОрганизации") <> Неопределено Тогда 
				стрОбъект.ПодразделениеОрганизации = ДокОбъект.ПодразделениеОрганизации;
			КонецЕсли;
			
			Если ДокОбъект.Метаданные().Реквизиты.найти("Подразделение") <> Неопределено Тогда 
				стрОбъект.Подразделение = ДокОбъект.Подразделение;
			КонецЕсли;
					
		КонецЕсли;
		
		Если стр.Тип = "Объект стр" Тогда 
			стрОбъект.ОбъектСтроительства = стр.Номенклатура;
		ИначеЕсли стр.Тип = "НМА" Тогда 
			стрОбъект.НематериальныйАктив = стр.Номенклатура;				
		Иначе 	
			стрОбъект.Номенклатура = стр.Номенклатура;
			стрОбъект.Количество = стр.Количество;
			стрОбъект.Цена = стр.Цена;
			Если стр.Тип = "Товар" И ТабличнаяЧасть.Колонки.Найти("Серия") <> Неопределено Тогда
				стрОбъект.СерияНоменклатуры = Стр.Серия;
			КонецЕсли;
		КонецЕсли;	
		стрОбъект.Сумма = стр.Сумма;
		стрОбъект.СуммаНДС = стр.СуммаНДС;
		стрОбъект.СтавкаНДС = стр.СтавкаНДС;
		
		Если стр.Тип <> "Объект стр" Тогда
			СчетаУчетаВДокументах.ЗаполнитьСчетаУчетаТабличнойЧасти(ИмяТЧ, стрОбъект, ДокОбъект, Истина, Истина);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция ИменаТабличныхЧастейПоступлениеТоваровУслуг()

	Результат = Новый Структура;
	Результат.Вставить("Товары"					 , "Товары");
	Результат.Вставить("Оборудование"			 , "Оборудование");
	Результат.Вставить("ОбъектыСтроительства"	 , "ОбъектыСтроительства");
	Результат.Вставить("НематериальныеАктивы"	 , "НематериальныеАктивы");
	Результат.Вставить("Услуги"					 , "Услуги");
	
	Возврат Результат;
	
КонецФункции

// Заполняет статью затрат и номенклатурную группу табличной части "Услуги"
//
// Параметры:
//  СтрокаТЧ - СтрокаТабличнойЧасти - строка табличной части "Услуги".
//  Номенклатура - СправочникСсылка.Номенклатура - номенклатура.
//
Процедура ЗаполнитьСтатьюЗатратВПоступленииТоваровУслуг(СтрокаТЧ, Номенклатура)
	
	Если Не ЗначениеЗаполнено(Номенклатура) Тогда
		Возврат;
	КонецЕсли;
	
	ТребуемыеРеквизиты = Новый Структура;
	ТребуемыеРеквизиты.Вставить("СтатьяЗатрат");
	ТребуемыеРеквизиты.Вставить("НоменклатурнаяГруппаЗатрат");
	
	РеквизитыНоменклатуры = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Номенклатура, ТребуемыеРеквизиты);
	СтрокаТЧ.СтатьяЗатрат = РеквизитыНоменклатуры.СтатьяЗатрат;
	СтрокаТЧ.НоменклатурнаяГруппа = РеквизитыНоменклатуры.НоменклатурнаяГруппаЗатрат;
	
КонецПроцедуры

Процедура ЗаполнениеПоступленияДопРасходов(ДокОбъект, ЭДОбъект, СуммаВключаетНДС)
	
	ДокОбъект.Сумма = ?(СуммаВключаетНДС, ЭДОбъект.total, ЭДОбъект.total - ЭДОбъект.vat);
	ДокОбъект.СуммаНДС = ЭДОбъект.vat;
	
КонецПроцедуры

Функция ПолучитьПоследнююКорректировкуПоступления(ПоследняяКорректировка) Экспорт
	
	Пока Истина Цикл
		
		Запрос = Новый Запрос;
		Запрос.Текст = "
		|ВЫБРАТЬ
		|	Док.Ссылка ДокКорректировка
		|ИЗ
		|	Документ.КорректировкаПоступления КАК Док
		|ГДЕ
		|	Док.ДокументПоступления = &ДокСсылка
		|	И Док.ВидОперации = &ВидОперации";
		Запрос.УстановитьПараметр("ДокСсылка", ПоследняяКорректировка);
		Запрос.УстановитьПараметр("ВидОперации", Перечисления.ВидыОперацийИсправленияПоступленияРеализации.ИсправлениеОшибки);
		
		Выборка = Запрос.Выполнить().Выбрать();
		Если Выборка.Следующий() Тогда
			ПоследняяКорректировка = Выборка.ДокКорректировка;
		Иначе
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат ПоследняяКорректировка;
	
КонецФункции

Функция получитьДокументОснованиеДляКорректировкиПоступления(Document) Экспорт 
	Если Document.type =  "InvoiceRevision" Тогда 
		запрос = Новый запрос;
		запрос.Текст = "ВЫБРАТЬ
		|	СчетФактураПолученныйДокументыОснования.ДокументОснование
		|ИЗ
		|	Документ.СчетФактураПолученный.ДокументыОснования КАК СчетФактураПолученныйДокументыОснования
		|ГДЕ
		|	СчетФактураПолученныйДокументыОснования.Ссылка.ДатаВходящегоДокумента = &ДатаВходящегоДокумента
		|	И СчетФактураПолученныйДокументыОснования.Ссылка.НомерВходящегоДокумента = &НомерВходящегоДокумента
		|	И СчетФактураПолученныйДокументыОснования.Ссылка.Исправление = Ложь";
		Запрос.УстановитьПараметр("НомерВходящегоДокумента", Document.OriginalDocumentNumber);		
		Запрос.УстановитьПараметр("ДатаВходящегоДокумента", НачалоДня(Document.OriginalDocumentdate));		
		результат = запрос.Выполнить().Выбрать();
		Если результат.следующий() Тогда 
			Док =  результат.ДокументОснование;
			Возврат 	ПолучитьПоследнююКорректировкуПоступления(Док); 
		Иначе 
			Возврат Неопределено 
		КонецЕсли;	
	ИначеЕсли Document.type = "InvoiceCorrection"  Тогда 
		запрос = Новый Запрос;
		Если ЗначениеЗаполнено(Document.OriginalInvoicerevisionNumber) = Ложь Тогда 
			Запрос.Текст =
			"ВЫБРАТЬ
			|	СчетФактураПолученныйДокументыОснования.ДокументОснование
			|ИЗ
			|	Документ.СчетФактураПолученный.ДокументыОснования КАК СчетФактураПолученныйДокументыОснования
			|ГДЕ
			|	СчетФактураПолученныйДокументыОснования.Ссылка.ДатаВходящегоДокумента = &ДатаВходящегоДокумента
			|	И СчетФактураПолученныйДокументыОснования.Ссылка.НомерВходящегоДокумента = &НомерВходящегоДокумента
			|	И (СчетФактураПолученныйДокументыОснования.Ссылка.Исправление = Ложь)";
		Иначе 
			Запрос.Текст =
			"ВЫБРАТЬ
			|	СчетФактураПолученныйДокументыОснования.ДокументОснование,
			|	СчетФактураПолученныйДокументыОснования.Ссылка.ДатаИсправления,
			|	СчетФактураПолученныйДокументыОснования.Ссылка.Исправление,
			|	СчетФактураПолученныйДокументыОснования.Ссылка.НомерИсправления
			|ИЗ
			|	Документ.СчетФактураПолученный.ДокументыОснования КАК СчетФактураПолученныйДокументыОснования
			|ГДЕ
			|	СчетФактураПолученныйДокументыОснования.Ссылка.ДатаВходящегоДокумента = &ДатаВходящегоДокумента
			|	И СчетФактураПолученныйДокументыОснования.Ссылка.НомерВходящегоДокумента = &НомерВходящегоДокумента
			|	И СчетФактураПолученныйДокументыОснования.Ссылка.Исправление
			|	И СчетФактураПолученныйДокументыОснования.Ссылка.ДатаИсправления = &ДатаИсправления
			|	И СчетФактураПолученныйДокументыОснования.Ссылка.НомерИсправления = &НомерИсправления";
			Запрос.УстановитьПараметр("НомерИсправления", число(Document.OriginalInvoicerevisionNumber));		
			Запрос.УстановитьПараметр("ДатаИсправления", НачалоДня(Document.OriginalInvoicerevisionDate));		
		КонецЕсли;				
		Запрос.УстановитьПараметр("НомерВходящегоДокумента", Document.OriginalDocumentNumber);		
		Запрос.УстановитьПараметр("ДатаВходящегоДокумента", НачалоДня(Document.OriginalDocumentdate));
		результат = запрос.Выполнить().Выбрать();
		Если результат.следующий() Тогда 
			Возврат результат.ДокументОснование;
		Иначе 
			Возврат Неопределено
		КонецЕсли;
	ИначеЕсли Document.type = "InvoiceCorrectionRevision"  Тогда 
		Запрос = Новый Запрос;
		Запрос.Текст = "ВЫБРАТЬ
		|	СчетФактураПолученныйДокументыОснования.ДокументОснование
		|ИЗ
		|	Документ.СчетФактураПолученный.ДокументыОснования КАК СчетФактураПолученныйДокументыОснования
		|ГДЕ
		|	СчетФактураПолученныйДокументыОснования.Ссылка.ДатаВходящегоДокумента = &ДатаВходящегоДокумента
		|	И СчетФактураПолученныйДокументыОснования.Ссылка.НомерВходящегоДокумента = &НомерВходящегоДокумента";
		Запрос.УстановитьПараметр("ДатаВходящегоДокумента", НачалоДня(Document.OriginalInvoiceCorrectionDate));
		Запрос.УстановитьПараметр("НомерВходящегоДокумента", Document.OriginalInvoiceCorrectionNumber);
		результат = запрос.Выполнить().Выбрать();
		Если результат.следующий() Тогда 
			Возврат результат.ДокументОснование;
		Иначе 
			Возврат Неопределено
		КонецЕсли;
	Иначе 
		Возврат Неопределено
	КонецЕсли;	
	
КонецФункции	

Функция ПолучитьСуммуПрописьюДиадок(Total, ВалютаДокумента) Экспорт 
	Возврат ОбщегоНазначения.СформироватьСуммуПрописью(Total, ВалютаДокумента);
КонецФункции	

Функция ДобавитьРазделитель(Стр, Префикс = "") 
	Возврат ?(ЗначениеЗаполнено(стр), Префикс + сокрЛП(Стр) + ", ", "");
КонецФункции

Функция ОтрезатьПравуюЗапятую(стр)
	результат = сокрЛП(стр);
	Пока Прав(результат, 1) = "," Цикл
		Результат = Лев(результат, СтрДлина(результат) - 1);
	КонецЦикла;
	
	Возврат результат;
КонецФункции

Функция ПолучитьПредставлениеКонтрагентаДляАкта(Контрагент, ДатаОперации) Экспорт
	СведенияОбОрганизации = ПолучитьСведенияОбОрганизации(Контрагент, ДатаОперации);
	Возврат ОтрезатьПравуюЗапятую(ДобавитьРазделитель(СведенияОбОрганизации.ПолноеНаименование) + ДобавитьРазделитель(СведенияОбОрганизации.ИНН, "ИНН: ") + ДобавитьРазделитель(СведенияОбОрганизации.КПП, "КПП: "));
КонецФункции

Функция ЕстьВозможностьЗаполненияГТД(Видоперации, ЭДОбъект) Экспорт
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЕстьВозможностьЗаполненияГТД(Видоперации, ЭДОбъект);	
КонецФункции

Функция ЕстьВозможностьЗаполненияСерий(Видоперации, ЭДОбъект) Экспорт
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЕстьВозможностьЗаполненияСерий(Видоперации, ЭДОбъект);	
КонецФункции

//{ Прослеживаемость

// Возвращает признак применения Федерального Закона 
// который вводит понятие прослеживаемости для определенных групп товаров
// Параметры
// Дата - тип дата, в данном параметре передается
//        дата на которую необходимо определить признак применения ФЗ
// Возвращаемое значение:
//  Булево - признак применения
//           Истина - ФЗ применяется
//           Ложь   - ФЗ не применяется
Функция ИспользуетсяУчетПрослеживаемыхТоваров(Дата) Экспорт
		
	Результат = Ложь;
	
	Возврат Результат;
	
КонецФункции 

// Заполняет колонки "ДанныеПрослеживаемости" и "ПрослеживаемыйТовар" в строках таблицы товаров
// если колонок нет, то они добавляются
// Параметры:
// ТаблицаТоваров - ТаблицаЗначений - в данном параметре передается
// СсылкаНаОбъект - ДокументСсылка.РеализацияТоваровУслуг,
//					 ДокументСсылка.КорректировкаРеализации - владелец таблицы товаров
//
Процедура ЗаполнитьСведенияПрослеживаемостиДляОтправки(ТаблицаТоваров, СсылкаНаОбъект) Экспорт
	
КонецПроцедуры

//} Прослеживаемость


//{ СвязиМеждуДокументами

Функция НайтиПодходящийСчетФактуруИзРНК(МассивСсылокРНК) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").НайтиПодходящийСчетФактуруИзРНК(МассивСсылокРНК);
	
КонецФункции

Функция ПолучитьМассивСсылокРНКПоСчетуФактуреВыданному(СсылкаНаОбъект) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьМассивСсылокРНКПоСчетуФактуреВыданному(СсылкаНаОбъект);
	
КонецФункции

Функция ПолучитьСсылкуСчетаФактурыВыданногоПоДокументуОснованию(СсылкаНаОбъект) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьСсылкуСчетаФактурыВыданногоПоДокументуОснованию(СсылкаНаОбъект);
	
КонецФункции

Функция ПолучитьМассивСсылокРТУПоСчетуНаОплату(СсылкаНаОбъект) Экспорт
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	АктОбОказанииПроизводственныхУслуг.Ссылка
	|ИЗ
	|	Документ.АктОбОказанииПроизводственныхУслуг КАК АктОбОказанииПроизводственныхУслуг
	|ГДЕ
	|	НЕ АктОбОказанииПроизводственныхУслуг.ПометкаУдаления
	|	И АктОбОказанииПроизводственныхУслуг.Сделка = &СсылкаНаОбъект
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	РеализацияТоваровУслуг.Ссылка
	|ИЗ
	|	Документ.РеализацияТоваровУслуг КАК РеализацияТоваровУслуг
	|ГДЕ
	|	НЕ РеализацияТоваровУслуг.ПометкаУдаления
	|	И РеализацияТоваровУслуг.Сделка = &СсылкаНаОбъект");
	
	Запрос.УстановитьПараметр("СсылкаНаОбъект", СсылкаНаОбъект);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Результат.Добавить(Выборка.Ссылка);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьСчетНаОплатуНаОснованииРТУ(СсылкаНаОбъект) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьСчетНаОплатуНаОснованииРТУ(СсылкаНаОбъект);
	
КонецФункции

Функция ПолучитьИсправляемыйДокументРеализации(ДокРеализации) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьИсправляемыйДокументРеализации(ДокРеализации);
	
КонецФункции

Функция СФПоМассивуДокументовОснованийИдентичны(МассивСсылок) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").СФПоМассивуДокументовОснованийИдентичны(МассивСсылок);
	
КонецФункции

//} СвязиМеждуДокументами

//{ Приглашение Контрагентов

Функция ЕстьНовыеКонтрагентыДляПриглашения(МассивОрганизаций1С, ПериодАнализаДокументов) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЕстьНовыеКонтрагентыДляПриглашения(МассивОрганизаций1С, ПериодАнализаДокументов);
	
КонецФункции

Функция ТаблицаКонтрагентовДляПриглашения(BoxID, ПериодАнализаДокументов) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТаблицаКонтрагентовДляПриглашения(BoxID, ПериодАнализаДокументов);
	
КонецФункции

//} Приглашение Контрагентов

//{ Внешние обработки

Функция ВнешняяОбработкаПоGUID(GUID) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ВнешняяОбработкаПоGUID(GUID);
	
КонецФункции

//} Внешние обработки

//{ ИдентификацияДокументов

Функция ЭтоЗаказПокупателя(ДокументСсылка) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЭтоЗаказПокупателя(ДокументСсылка);
	
КонецФункции	

Функция ЭтоКорректировкаРеализации(ДокументСсылка) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЭтоКорректировкаРеализации(ДокументСсылка);
	
КонецФункции

Функция ЭтоКСФ(ДокументСсылка) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЭтоКСФ(ДокументСсылка);
	
КонецФункции

Функция ЭтоРТУ_ДД(ДокументСсылка) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЭтоРТУ_ДД(ДокументСсылка);
	
КонецФункции 

Функция ЭтоСФ(ДокументСсылка) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЭтоСФ(ДокументСсылка);
	
КонецФункции	

Функция ЭтоСФПолученный(ДокументСсылка) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЭтоСФПолученный(ДокументСсылка);
	
КонецФункции	

Функция ЭтоСчетНаОплату(ДокументСсылка) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЭтоСчетНаОплату(ДокументСсылка);
	
КонецФункции	

//} ИдентификацияДокументов

//{ ОтправкаДокументов

Функция ПолучитьТаблицуЗначенийДокументовДляОтправкиДиадок(ДатаНачала, ДатаОкончания, ТипыДокументов, Организация, Контрагент) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьТаблицуЗначенийДокументовДляОтправкиДиадок(ДатаНачала, ДатаОкончания, ТипыДокументов, Организация, Контрагент);
	
КонецФункции	

Процедура УстановитьПризнакВыставленияСФ(ДокументСсылка, ТекстКомментарияДиадок) Экспорт 
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").УстановитьПризнакВыставленияСФ(ДокументСсылка, ТекстКомментарияДиадок);
	
КонецПроцедуры

//} ОтправкаДокументов

//{ ПолучениеДанныхДляФУФ

Функция ГрузополучательПоДокументу(ДокументСсылка) Экспорт 
	
	Результат = ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ГрузополучательПоДокументу(ДокументСсылка);
	
	Возврат Результат;
	
КонецФункции

Функция КонтрагентПоДокументу(ДокументСсылка) Экспорт
	
	Результат = ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").КонтрагентПоДокументу(ДокументСсылка);
					
	Возврат Результат;
	
КонецФункции

Функция ПолучитьДоговорПоДокументу(ДокументСсылка) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьДоговорПоДокументу(ДокументСсылка);
	
КонецФункции	

Функция ПолучитьСуммуДокумента(ДокументСсылка) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьСуммуДокумента(ДокументСсылка);
	
КонецФункции	

Функция ПолучитьСуммуНДС(ДокументСсылка) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьСуммуНДС(ДокументСсылка);
	
КонецФункции	

//} ПолучениеДанныхДляФУФ

//{ СвязиМеждуДокументами

Функция ПервыйДокументСсылаетсяНаВторой(ПервыйДокумент, ВторойДокумент) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПервыйДокументСсылаетсяНаВторой(ПервыйДокумент, ВторойДокумент);
	
КонецФункции

Функция ПолучитьДокОснованиеКорректировкиРеализациии(ДокументСсылка) Экспорт 
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьДокОснованиеКорректировкиРеализациии(ДокументСсылка);
	
КонецФункции

Функция ПолучитьСсылкиДляСозданияКорректировкиРеализации(КорректировкаРеализацииОснование, ТипНовогоДокумента) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ПолучитьСсылкиДляСозданияКорректировкиРеализации(КорректировкаРеализацииОснование, ТипНовогоДокумента);
	
КонецФункции

//} СвязиМеждуДокументами

Функция ТекстЗапросаПодборНакладных(Direction) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТекстЗапросаПодборНакладных(Direction);
	
КонецФункции

Функция ТекстЗапросаПодборСчетовФактур(Direction) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ТекстЗапросаПодборСчетовФактур(Direction);
	
КонецФункции

Процедура УстановитьДатуНомерВходящегоДокумента(докСсылка, ДатаВходящегоДокумента, НомерВходящегоДокумента, СписокОтличающихсяРеквизитов) Экспорт 
	
	ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").УстановитьДатуНомерВходящегоДокумента(докСсылка, ДатаВходящегоДокумента, НомерВходящегоДокумента, СписокОтличающихсяРеквизитов);
	
КонецПроцедуры	

//{Внешние печатные формы

Функция ВПФ_Печать(СсылкаНаОбъект, ИдентификаторВПФ) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ВПФ_Печать(СсылкаНаОбъект, ИдентификаторВПФ);
	
КонецФункции

Функция ВПФ_РезультатФункции(ИдентификаторВПФ, ИмяФункции, ПараметрыФункции) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ВПФ_РезультатФункции(ИдентификаторВПФ, ИмяФункции, ПараметрыФункции);
	
КонецФункции

Функция ВПФ_Идентификатор(СсылкаНаОбъект, ВПФ) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ВПФ_Идентификатор(СсылкаНаОбъект, ВПФ);
	
КонецФункции

Функция ВПФ_ДополнительныеПараметры(ИдентификаторВПФ) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ВПФ_ДополнительныеПараметры(ИдентификаторВПФ);
	
КонецФункции

Функция ВПФ_СсылкаНомерСтроки(ИдентификаторВПФ) Экспорт
	
	Возврат ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ВПФ_СсылкаНомерСтроки(ИдентификаторВПФ);
	
КонецФункции

//}Внешние печатные формы

//{Обертки

Функция ЕстьРеквизитМетаданных(ОбъектМетаданных, ИмяРеквизита, ПроверятьСтандартныеРеквизиты = Ложь)
	
	Результат = ОсновнойМодуль.ЕстьРеквизитМетаданных(ОбъектМетаданных, ИмяРеквизита, ПроверятьСтандартныеРеквизиты);
	
	Возврат Результат;
	
КонецФункции

Функция ЗначенияРеквизитовОбъектаЛокальная(Ссылка, Реквизиты)
	
	Результат = ПолучитьМодульПрог("Модуль_ИнтеграцияУниверсальнаяЛогика").ЗначенияРеквизитовОбъектаЛокальная(Ссылка, Реквизиты);
	
	Возврат Результат;
	
КонецФункции

//}Обертки

// Устаревшие 

//Устарел в релизе 5.30.01. Необходимо использовать метод "ГрузополучательПоДокументу"
Функция ПолучитьГрузополучателяПоДокументу(ДокументСсылка) Экспорт 
	
	Результат = ГрузополучательПоДокументу(ДокументСсылка);
	
	Возврат Результат; 
					
КонецФункции
