&НаСервере
Перем _ОбработкаОбъект;

&НаСервере
Перем _МодульЯдроНаСервере;

//{		Сервисные методы

&НаКлиенте
Функция ОсновнаяФорма() Экспорт
	
	Результат = ВладелецФормы.ОсновнаяФорма();
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция Модуль_ЯдроНаКлиенте()
	
	Результат = ОсновнаяФорма().Модуль_ЯдроНаКлиенте();
	
	Возврат Результат;
	
КонецФункции

&НаСервере
Функция Модуль_ЯдроНаСервере()
	
	Если _МодульЯдроНаСервере = Неопределено Тогда
		Модуль = ОбработкаОбъект();
		_МодульЯдроНаСервере = Модуль.Модуль_Ядро();
	КонецЕсли;
	
	Возврат _МодульЯдроНаСервере;
	
КонецФункции

&НаСервере
Функция ОбработкаОбъект()
	
	Если _ОбработкаОбъект = Неопределено Тогда
		_ОбработкаОбъект = РеквизитФормыВЗначение("Объект");
	КонецЕсли;
	
	Возврат _ОбработкаОбъект;
	
КонецФункции

//}		Сервисные методы


//{		ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	СценарийДляОтзыва = Параметры.Сценарий;
	
	НастроитьФормуПоСценариюДляОтзыва();
	ИнициализироватьИдентификаторТрассировки();
	
КонецПроцедуры

//}		ОбработчикиСобытийФормы


//{		ОбработчикиКомандФормы

&НаКлиенте
Процедура ОставитьОтзыв(Команда)
	
	Если ПроверятьЗаполнение И Не ЗначениеЗаполнено(Отзыв) Тогда
		Модуль_ЯдроНаКлиенте()._СтдОбщегоНазначения().СообщитьПользователюЛок(
			"Поле с комментарием - обязательно для заполнения");
		Возврат;
	КонецЕсли;
		
	Метрики_ЗаписатьНажатие_ОставитьОтзыв();
	
	Закрыть(Отзыв);
	
КонецПроцедуры

&НаКлиенте
Процедура Отменить(Команда)
	
	Метрики_ЗаписатьНажатие_Отменить();
	
	Закрыть(Неопределено);
	
КонецПроцедуры

//}		ОбработчикиКомандФормы


//{		СлужебныеПроцедурыИФункции

&НаСервере
Процедура НастроитьФормуСценарийРаботаСКонтрагентами()
	
	ЭтаФорма.Заголовок = Нстр("ru = 'Отзыв о работе с контрагентами'");
	Элементы.ДекорацияСопроводительныйТекст.Заголовок =
		Нстр("ru = 'Мы не хотели вас расстраивать. Поделитесь трудностями, с которыми вы столкнулись. Это поможет сделать наш модуль лучше.'");
	
	Метрика_НаименованиеФормы = "ОтзывОРаботеСКонтрагентами";
	Метрика_Категория = "ТрудностиСКонтрагентами";
	Метрика_Представление = "ОтзывОРаботеСКонтрагентами";
	
КонецПроцедуры

&НаСервере
Процедура НастроитьФормуСценарийОплатаСервиса()
	
	ЭтаФорма.Заголовок = Нстр("ru = 'Отзыв о работе с подписками'");
	Элементы.ДекорацияСопроводительныйТекст.Заголовок =
		Нстр("ru = 'Мы не хотели вас расстраивать. Поделитесь трудностями, с которыми вы столкнулись. Это поможет сделать наш модуль лучше.'");
	
	// BSLLS:UnusedLocalVariable-off
	Метрика_НаименованиеФормы = "ОтзывОРаботеСОнлайнами";
	Метрика_Категория = "ТрудностиСПодписками";
	Метрика_Представление = "ОтзывПоПредложениямОбОплате";
	// BSLLS:UnusedLocalVariable-on
	
КонецПроцедуры

&НаСервере
Процедура НастроитьФормуСценарийПоискДокументовНаОтправку()
	
	ЭтаФорма.Заголовок = НСтр("ru = 'Отзыв об инструменте поиска документов'");
	Элементы.ДекорацияСопроводительныйТекст.Заголовок =
		НСтр("ru = 'Мы не хотели вас расстраивать. Поделитесь трудностями, с которыми вы столкнулись. Это поможет сделать наш модуль лучше.'");
	
	Метрика_НаименованиеФормы = "ОтзывПоПоискуДокументов";
	Метрика_Категория = "ТрудностиПриПоискеДокументов";
	Метрика_Представление = "ОтзывПоПоискуДокументов";
	
КонецПроцедуры

&НаСервере
Процедура НастроитьФормуСценарийСобытияЛентыКонтрагентов()
	
	ЭтаФорма.Заголовок = НСтр("ru = 'Отзыв о работе с событиями'");
	Элементы.ДекорацияСопроводительныйТекст.Заголовок =
		НСтр("ru = 'Мы не хотели вас расстраивать."
			+ " Поделитесь трудностями, с которыми вы столкнулись."
			+ " Это поможет сделать наш модуль лучше.'");
	// BSLLS:UnusedLocalVariable-off
	Метрика_НаименованиеФормы = "ТрудностиССобытиямиКА";
	Метрика_Категория = "ТрудностиСЛентойКонтрагентов";
	Метрика_Представление = "ТрудностиССобытиямиКА";
	// BSLLS:UnusedLocalVariable-on
КонецПроцедуры

&НаСервере
Процедура НастроитьФормуСценарийАвторизация()
	
	ЭтаФорма.Заголовок = НСтр("ru = 'Отзыв о работе с авторизацией'");
	Элементы.ДекорацияСопроводительныйТекст.Заголовок =
		НСтр("ru = 'Мы не хотели вас расстраивать."
			+ " Поделитесь трудностями, с которыми вы столкнулись."
			+ " Это поможет сделать наш модуль лучше.'");
	// BSLLS:UnusedLocalVariable-off
	Метрика_НаименованиеФормы = "ОсновнаяФорма";
	Метрика_Категория = "ТрудностиСАвторизацией";
	Метрика_Представление = "";
	// BSLLS:UnusedLocalVariable-on
КонецПроцедуры

&НаСервере
Процедура НастроитьФормуСценарийАвтоОтправкиПриглашений()
	
	ЭтаФорма.Заголовок = НСтр("ru = 'Отзыв об инструменте отправки приглашений'");
	// BSLLS:UnusedLocalVariable-off
	Метрика_НаименованиеФормы = "Настройки";
	Метрика_Категория = "ОтзывОбАвтоОтправкиПриглашений";
	Метрика_Представление = "";
	
	ЭтаФорма.Ширина = 100;
	ПроверятьЗаполнение = Истина;
	// BSLLS:UnusedLocalVariable-on
	
КонецПроцедуры

&НаСервере
Процедура НастроитьФормуПоСценариюДляОтзыва()
	
	Модуль_Ядро = Модуль_ЯдроНаСервере();
	
	СценарииМодуля = Модуль_Ядро.Перечисление_СценарииМодуляДляОтзывов();
	
	Если СценарийДляОтзыва = СценарииМодуля.РаботаСКонтрагентами Тогда
		
		НастроитьФормуСценарийРаботаСКонтрагентами();
		
	ИначеЕсли СценарийДляОтзыва = СценарииМодуля.ОплатаСервиса Тогда
		
		НастроитьФормуСценарийОплатаСервиса();
		
	ИначеЕсли СценарийДляОтзыва = СценарииМодуля.ПоискДокументовНаОтправку Тогда
		
		НастроитьФормуСценарийПоискДокументовНаОтправку();
		
	ИначеЕсли СценарийДляОтзыва = СценарииМодуля.СобытияЛентыКонтрагентов Тогда
		
		НастроитьФормуСценарийСобытияЛентыКонтрагентов();
		
	ИначеЕсли СценарийДляОтзыва = СценарииМодуля.Авторизация Тогда
		
		НастроитьФормуСценарийАвторизация();
		
	ИначеЕсли СценарийДляОтзыва = СценарииМодуля.ОтзывОбАвтоотправкиПриглашений Тогда
		
		НастроитьФормуСценарийАвтоОтправкиПриглашений();
		
	Иначе
		
		Ошибка = СтрЗаменить(
				"Неожиданное значение параметра ""Сценарий"" (%1)",
				"%1",
				СценарииМодуля
			);
		
		ВызватьИсключение Ошибка;
		
	КонецЕсли;
	
КонецПроцедуры

//}		СлужебныеПроцедурыИФункции


//{		Метрики

&НаКлиенте
Процедура Метрики_ЗаписатьНажатие_ОставитьОтзыв()
	
	Ядро = Модуль_ЯдроНаКлиенте();
	
	Действие = "ОтправитьОтзыв";
	Метка = "Нажатие кнопки";
	Переменные = Неопределено;
	ДобавитьСистемнуюИнформацию = Истина;
	
	Ядро.Метрика_ЗаписатьПоведение(
		Метрика_Категория,
		Действие,
		Метрика_Представление,
		Метка,
		Переменные,
		ДобавитьСистемнуюИнформацию,
		ИдентификаторТрассировки);
	
КонецПроцедуры

&НаКлиенте
Процедура Метрики_ЗаписатьНажатие_Отменить()
	
	Ядро = Модуль_ЯдроНаКлиенте();
	
	Действие = "Отменить";
	Метка = "Нажатие кнопки";
	Переменные = Неопределено;
	ДобавитьСистемнуюИнформацию = Истина;
	
	Ядро.Метрика_ЗаписатьПоведение(
		Метрика_Категория,
		Действие,
		Метрика_Представление,
		Метка,
		Переменные,
		ДобавитьСистемнуюИнформацию,
		ИдентификаторТрассировки);
	
КонецПроцедуры

&НаСервере
Процедура ИнициализироватьИдентификаторТрассировки()
	
	СтрокаИдентификатора = Строка(Новый УникальныйИдентификатор());
	ИдентификаторТрассировки = Метрика_НаименованиеФормы + "_" + СтрокаИдентификатора;
	
КонецПроцедуры

//}		Метрики