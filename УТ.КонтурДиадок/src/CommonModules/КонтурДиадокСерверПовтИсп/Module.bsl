#Область ПрограммныйИнтерфейс

// Получает экземпляр обработки КонтурДиадокЯдро
// 
// Возвращаемое значение:
//  ОбработкаОбъект - экземпляр обработки КонтурДиадокЯдро
//
Функция МодульЯдро() Экспорт
	
	ИдФормы = Новый УникальныйИдентификатор;
	ДопПараметры = Новый Структура;
	
	ОсновнойМодуль = Обработки.КонтурЭДО.Создать();
	ОсновнойМодуль.ИнициализироватьОбщийКонтекст(ИдФормы, ДопПараметры);
	
	Результат = ОсновнойМодуль.Модуль_Ядро();
	
	Возврат Результат;
	
КонецФункции

// Получает экземпляр обработки КонтурДиадокХранениеДанных
// 
// Возвращаемое значение:
//  ОбработкаОбъект - экземпляр обработки КонтурДиадокХранениеДанных
//
Функция МодульХранениеДанных() Экспорт
	
	Результат = Обработки.КонтурДиадокХранениеДанных.Создать();
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти