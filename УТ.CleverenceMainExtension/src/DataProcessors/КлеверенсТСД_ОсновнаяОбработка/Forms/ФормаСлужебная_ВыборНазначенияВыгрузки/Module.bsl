&НаКлиенте 
#Если НЕ ТолстыйКлиентОбычноеПриложение Тогда
	Перем _ЛокКонтекст Экспорт;
#КонецЕсли

&НаКлиенте
Перем ПользователиMSАдрес Экспорт, УстройстваMSАдрес Экспорт;
	
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	#Если НЕ ТолстыйКлиентОбычноеПриложение Тогда
		БЛ = НСтр("be = 'па ўнутранай сетцы'; en = 'over internal network'; es = 'a través de la red interna'; ka = 'შიდა ქსელში'; kk = 'Ішкі желі арқылы'; ro = 'prin rețeaua internă'; ru = 'по внутренней сети'; tr = 'İç ağ üzerinden'; uk = 'по внутрішній мережі'; uz = 'ички тармоқ бўйича'");
		БИ = НСтр("be = 'праз Інтэрнэт'; en = 'via Internet'; es = 'a través de Internet'; ka = 'ინტერნეტით'; kk = 'Интернет арқылы'; ro = 'prin Internet'; ru = 'через Интернет'; tr = 'İnternet üzerinden'; uk = 'через Інтернет'; uz = 'Internet orqali'");
		ЭтаФорма.Элементы.ВыгрузкаНаУстройство.ФорматРедактирования = "БЛ='" + БЛ + "'; БИ='" + БИ + "'";
	#КонецЕсли
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии()	
	
	ЗагрузитьДеревоПользователейНаСервере(ЭтаФорма.ПользователиMSАдрес, ЭтаФорма.УстройстваMSАдрес);
	
	УстановитьДоступностьТаблиц();	
	
	// Восстановление изначальных размеров формы, если она была изменена пользователем (программное нажатие Alt+Shift+R)
	Попытка
		WSHShell=Новый COMObject("WScript.Shell");;
		WSHShell.SendKeys("%+(R)"); 	
	Исключение	
	КонецПопытки;	
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура Выгрузить(Команда)

	Если ВыгрузкаНаУстройство Тогда    		
		
		Если Не ПинКодВведен Тогда
			
			ФормированиеТаблицыУстройствНазначенияВыгрузки(Истина);
			
		Иначе
			
			ЗначениеПинКода = СтрЗаменить(ПинКод, " ", "");	
			
			Если ПустаяСтрока(ЗначениеПинКода) Тогда 
				ТекстПредупреждения = НСтр("be = 'Запоўніце пін-код або выберыце прылад з спісу.'; en = 'Enter a PIN code or select a device from the list.'; es = 'Ingrese el PIN o seleccione un dispositivo de la lista.'; ka = 'შეიყვანეთ PIN-კოდი ან აირჩიეთ მოწყობილობა სიაშიდან.'; kk = 'Пин-кодты толтырыңыз немесе тізімнен құрылғыны таңдаңыз.'; ro = 'Introduceți codul PIN sau selectați dispozitivul din listă.'; ru = 'Заполните пин-код или выберите устройство из списка.'; tr = 'Pin kodunu doldurun veya listeden bir cihaz seçin.'; uk = 'Заповніть пін-код або виберіть пристрій зі списку.'; uz = ""PIN-kodni to'ldiring yoki ro'yxatdan o'tkazilgan qurilmadan tanlang.""");
				ПоказатьПредупреждение(Новый ОписаниеОповещения("ПринятьПинКодЗавершение", ЭтаФорма), ТекстПредупреждения); 
				Возврат;
			ИначеЕсли СтрДлина(ЗначениеПинКода) < 4 Тогда
				ТекстПредупреждения = НСтр("be = 'Пін-код уведзены не поўнасцю!'; en = 'PIN code entered incompletely!'; es = '¡El PIN no ha sido ingresado completamente!'; ka = 'პინ-კოდი შეყვანილი არ არის სრული!'; kk = 'Пин-код толық енгізілмеді!'; ro = 'Codul PIN nu a fost introdus complet!'; ru = 'Пин-код введен не полностью!'; tr = 'Pin kodu tam olarak girilmedi!'; uk = 'Пін-код введений не повністю!'; uz = 'Пин-код тўлиқ киритилмади!'");
				ПоказатьПредупреждение(Новый ОписаниеОповещения("ПринятьПинКодЗавершение", ЭтаФорма), ТекстПредупреждения); 
				Возврат;
			Иначе
				СтрЦифр = "0123456789";		
				Для Инд = 1 По СтрДлина(ЗначениеПинКода) Цикл
					ПроверяемыйСимвол = Сред(ЗначениеПинКода, Инд, 1);
					Если _ЛокКонтекст.ЛокЯдро_СтрНайти(СтрЦифр, ПроверяемыйСимвол) = 0 Тогда
						ТекстПредупреждения = НСтр("be = 'Памылка: пін-код павінен складацца толькі з лічбаў!'; en = 'Error: PIN code must consist only of digits!'; es = '¡Error: el PIN debe consistir solo en números!'; ka = 'შეცდომა: პინ-კოდი უნდა შედგებოდეს მხოლოდ ციფრებისგან!'; kk = 'Қате: пин-код тек сандардан тұруы керек!'; ro = 'Eroare: codul PIN trebuie să conțină doar cifre!'; ru = 'Ошибка: пин-код должен состоять только из цифр!'; tr = 'Hata: Pin kodu sadece rakamlardan oluşmalıdır!'; uk = 'Помилка: пін-код повинен складатися лише з цифр!'; uz = 'Жеткизувчи пин-код фақат сонлардан иборат бўлиши керак!'");
						ПоказатьПредупреждение(Новый ОписаниеОповещения("ПринятьПинКодЗавершение", ЭтаФорма), ТекстПредупреждения); 
						Возврат;					
					КонецЕсли;				
				КонецЦикла;   		
			КонецЕсли;
			
			Если ЗначениеПинКода = Неопределено Тогда
				ЗначениеПинКода = Ложь;
			КонецЕсли;
			
			НазначенныеУстройства = _ЛокКонтекст.ЛокЯдро_ПолучениеУстройстваПоПинкодуДляВыгрузкиДокумента(ЗначениеПинКода, ТипБазы);
			
			Если НазначенныеУстройства <> Неопределено 
				И ТипЗнч(НазначенныеУстройства) = Тип("Массив")
				И НазначенныеУстройства.Количество() > 0 Тогда
				
				НазначенныеУстройстваАдрес = ПоместитьВоВременноеХранилище(НазначенныеУстройства);	
				
				#Если ТолстыйКлиентОбычноеПриложение Тогда
					Если НЕ ОписаниеОповещенияОЗакрытии = Неопределено Тогда	
						ВыполнитьОбработкуОповещения(ОписаниеОповещенияОЗакрытии, НазначенныеУстройстваАдрес);					
					КонецЕсли;
				#КонецЕсли
				Закрыть(НазначенныеУстройстваАдрес);	
												
			Иначе
				
				Если НазначенныеУстройства = Неопределено Тогда
					СообщениеПользователю = НСтр("be = 'Уведзены некарэктны пін-код'; en = 'Incorrect PIN code entered!'; es = '¡Se ha ingresado un código PIN incorrecto!'; ka = 'არასწორი პინ-კოდი შეყვანილია!'; kk = 'Введен некорректный пин-код - Пин-код дұрыс емес енгізілді'; ro = 'Cod PIN incorect introdus!'; ru = 'Введен некорректный пин-код!'; tr = 'Geçersiz PIN kodu girildi!'; uk = 'Введений некоректний пін-код!'; uz = ""Noto'g'ri PIN-kod kiritildi!""");
				Иначе
					СообщениеПользователю = НСтр("be = 'Прылада па пін-кодзе не знойдзена!'; en = 'Device with pin code not found!'; es = '¡Dispositivo no encontrado por código PIN!'; ka = 'პინ-კოდით მოწყობილობა ვერ მოიძებნა!'; kk = 'Пин-код бойынша құрылғы табылмады!'; ro = 'Dispozitivul cu cod PIN nu a fost găsit!'; ru = 'Устройство по пин-коду не найдено!'; tr = 'Pin kodu cihazı bulunamadı!'; uk = 'Пристрій за пін-кодом не знайдено!'; uz = ""Pin-kod bo'yicha qurilma topilmadi!""");
				КонецЕсли;
				
				ПоказатьПредупреждение(Новый ОписаниеОповещения("ПринятьПинКодЗавершение", ЭтаФорма), СообщениеПользователю); 
				Возврат;
				
			КонецЕсли;
			
		КонецЕсли;

	Иначе
		
		МассивПользователейMS = Новый Массив;
		
		#Если ТолстыйКлиентОбычноеПриложение Тогда
			СформироватьМассивПользователейMS(ПользователиMS.Строки, МассивПользователейMS);			
			Если НЕ ОписаниеОповещенияОЗакрытии = Неопределено Тогда	
				ВыполнитьОбработкуОповещения(ОписаниеОповещенияОЗакрытии, МассивПользователейMS);
			КонецЕсли;
		#Иначе
			СформироватьМассивПользователейMS(ПользователиMS.ПолучитьЭлементы(), МассивПользователейMS);			
		#КонецЕсли
		
		Закрыть(МассивПользователейMS);                                                                 
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузкаНаУстройствоПриИзменении(Элемент)

	УстановитьДоступностьТаблиц();
	
КонецПроцедуры

&НаКлиенте
Процедура ВыделитьВсе(Команда)
	
	СтрокиИсключения = Новый Массив;
	СтрокиИсключения.Добавить("Выгружать всем");
	СтрокиИсключения.Добавить("Спрашивать при выгрузке");	
	
	ИзменитьФлажки(Истина, Неопределено, СтрокиИсключения);	
	РазвернутьДерево();
	
КонецПроцедуры

&НаКлиенте
Процедура ОчиститьВыделение(Команда)
	
	ИзменитьФлажки(Ложь);	
	РазвернутьДерево();
		
КонецПроцедуры 

&НаКлиенте
Процедура ПинКодИзменениеТекстаРедактирования(Элемент, Текст, СтандартнаяОбработка)
	
	ОбработкаВводаТекстаПинКода(Текст);
	
КонецПроцедуры

&НаКлиенте
Процедура ПинКодОкончаниеВводаТекста(Элемент, Текст, Значение, СтандартнаяОбработка)
	
	ОбработкаВводаТекстаПинКода(Текст);
	
КонецПроцедуры    

&НаКлиенте
Процедура ОбработкаВводаТекстаПинКода(Текст)

	Если СтрДлина(СокрЛП(Текст)) = 4 Тогда                       				
		#Если Не ТолстыйКлиентОбычноеПриложение Тогда		
			ТекущийЭлемент 							= Элементы.ФормаЗагрузить;					
		#КонецЕсли
		Элементы.ТаблицаУстройстваMS.Доступность 	= Ложь;		
		ПинКодВведен 								= Истина;
	Иначе
		Элементы.ТаблицаУстройстваMS.Доступность 	= Истина;		
		ПинКодВведен 								= Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ТаблицаУстройстваMSВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
    ФормированиеТаблицыУстройствНазначенияВыгрузки(Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ПользователиMSВыгружатьПриИзменении(Элемент)
	
	ТекДанные = Элементы.ПользователиMS.ТекущиеДанные;		
	
	Если ТекДанные = Неопределено Тогда
		Возврат;
	КонецЕсли; 	
	
	Если ТекДанные.Выгружать Тогда		
		
		Если Найти(ВРег(СокрЛП("Выгружать всем")), ВРег(СокрЛП(ТекДанные.ИдГруппыПользователя))) > 0 Тогда			
			СтрокиИсключения = Новый Массив;
			СтрокиИсключения.Добавить(ТекДанные.ИдГруппыПользователя);		
			СтрокиДляИзменения = Неопределено;
		Иначе
			СтрокиДляИзменения = Новый Массив;
			СтрокиДляИзменения.Добавить("Выгружать всем");			
			СтрокиИсключения = Неопределено;
		КонецЕсли;			
		
		ИзменитьФлажки(Ложь, СтрокиДляИзменения, СтрокиИсключения);			
		РазвернутьДерево();		  
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти 

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ЗагрузитьДеревоПользователейНаСервере(ПользователиMSАдрес, УстройстваMSАдрес)
	
	ДеревоПользователейMS 	= ПолучитьИзВременногоХранилища(ПользователиMSАдрес);	
	ДеревоУстройствMS 		= ПолучитьИзВременногоХранилища(УстройстваMSАдрес);	
	
	#Если ТолстыйКлиентОбычноеПриложение Тогда
		УстройстваMS 	= ДеревоУстройствMS;
		ПользователиMS 	= ДеревоПользователейMS;		
	#Иначе  
		ЗначениеВРеквизитФормы(ДеревоПользователейMS, 	"ПользователиMS");
		ЗначениеВРеквизитФормы(ДеревоУстройствMS, 		"УстройстваMS");		
	#КонецЕсли
    
КонецПроцедуры

&НаКлиенте
Процедура ОтметитьКВыгрузкеСтрокуДереваПользователейПоЗначению(СписокЭлементов, МассивЗначений, Счетчик)
	
	Для Каждого СтрокаПользователь Из СписокЭлементов Цикл  
		Для Каждого Значение Из МассивЗначений Цикл
			Если СтрокаПользователь.ИдГруппыПользователя = Значение Тогда
				СтрокаПользователь.Выгружать = Истина;     
				Счетчик = Счетчик - 1;
				Продолжить;  			
			КонецЕсли;
		КонецЦикла;                   
		Если Счетчик = 0 Тогда
			Прервать;
		КонецЕсли;
		#Если ТолстыйКлиентОбычноеПриложение Тогда
			ОтметитьКВыгрузкеСтрокуДереваПользователейПоЗначению(СтрокаПользователь.Строки, МассивЗначений, Счетчик);
		#Иначе
			ОтметитьКВыгрузкеСтрокуДереваПользователейПоЗначению(СтрокаПользователь.ПолучитьЭлементы(), МассивЗначений, Счетчик);
		#КонецЕсли		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьДоступностьТаблиц()
	
	ВыборНазначенияВыгрузкиНаСервер = (НазначитьПользователю = "Спрашивать при выгрузке" 
		Или НазначитьПользователю = "Конкретному устройству" Или ПустаяСтрока(НазначитьПользователю));	
		
	Элементы.ГруппаПользователиMS.Видимость 			= Не ВыгрузкаНаУстройство;
	#Если ТолстыйКлиентОбычноеПриложение Тогда
		Элементы.НадписьПользователям.Видимость			= Не ВыгрузкаНаУстройство;
		Элементы.ПользователиMSКоманднаяПанель.Видимость= Не ВыгрузкаНаУстройство;              
		Элементы.ПользователиMS.Видимость	 			= Не ВыгрузкаНаУстройство;	
		
		Элементы.НадписьНаУстройство.Видимость 			= ВыгрузкаНаУстройство;
		Элементы.НадписьПинКод.Видимость 				= ВыгрузкаНаУстройство;
		Элементы.ПинКод.Видимость 						= ВыгрузкаНаУстройство;
		Элементы.ТаблицаУстройстваMS.Видимость 			= ВыгрузкаНаУстройство;
	#КонецЕсли
	Элементы.ТаблицаУстройстваMS.Доступность 			= СтрДлина(СокрЛП(ПинКод)) <> 4;	
	Элементы.ПользователиMS.ТолькоПросмотр 				= Не ВыборНазначенияВыгрузкиНаСервер;	
	Элементы.ПользователиMSКоманднаяПанель.Доступность	= ВыборНазначенияВыгрузкиНаСервер;	
	
	Элементы.УстройстваMS.Видимость 					= ВыгрузкаНаУстройство;
	
	Если Не ВыборНазначенияВыгрузкиНаСервер Тогда   
		
		НазначенныеПользователи = _ЛокКонтекст.ЛокЯдро_СтрРазделить(НазначитьПользователю, "|", Ложь);
		Счетчик 				= НазначенныеПользователи.Количество();
	    
		#Если ТолстыйКлиентОбычноеПриложение Тогда
			ОтметитьКВыгрузкеСтрокуДереваПользователейПоЗначению(ПользователиMS.Строки, НазначенныеПользователи, Счетчик);		
		#Иначе
			ОтметитьКВыгрузкеСтрокуДереваПользователейПоЗначению(ПользователиMS.ПолучитьЭлементы(), НазначенныеПользователи, Счетчик);		
		#КонецЕсли
	КонецЕсли;		
	
	#Если ТолстыйКлиентОбычноеПриложение Тогда
		Элементы.ПользователиMS.РежимВыделения 		= ?(ОдиночныйРежимВыделения, РежимВыделенияТабличногоПоля.Одиночный, РежимВыделенияТабличногоПоля.Множественный);
		Элементы.ТаблицаУстройстваMS.РежимВыделения = РежимВыделенияТабличногоПоля.Одиночный;
	#Иначе
		Элементы.ПользователиMS.РежимВыделения 		= ?(ОдиночныйРежимВыделения, РежимВыделенияТаблицы.Одиночный, РежимВыделенияТаблицы.Множественный);
		Элементы.ТаблицаУстройстваMS.РежимВыделения = РежимВыделенияТаблицы.Одиночный;
	#КонецЕсли
	
	РазвернутьДерево();
	
	Если НесколькоДокументов Тогда                                                                       
		СтрокаПользователям = НСтр("be = 'Выберыце карыстальніка альбо групу карыстальнікаў, каму будуць выгружаны дакументы.'; en = 'Select a user or group of users to whom the documents will be exported.'; es = 'Elija un usuario o grupo de usuarios a quienes se exportarán los documentos.'; ka = 'აირჩიეთ მომხმარებელი ან ჯგუფი მომხმარებებს, ვისთვის გამჭვიტანება დოკუმენტები.'; kk = 'Документтерді шығаруға берілетін пайдаланушылар немесе топты таңдаңыз.'; ro = 'Alegeți utilizatorul sau grupul de utilizatori cărora li se vor descărca documentele.'; ru = 'Выберите пользователя или группу пользователей, кому будут выгружены документы.'; tr = 'Belgeleri dışa aktarılacak kullanıcı veya kullanıcı grubunu seçin.'; uk = 'Оберіть користувача або групу користувачів, кому будуть вивантажено документи.'; uz = 'Hujjatlarni yuklab olish uchun foydalanuvchi yoki foydalanuvchilar guruhi tanlang.'") 
			+ " "
			+ НСтр("be = 'Дакументы будуць выгружаны ў базу Cleverence.'; en = 'Documents will be uploaded to Cleverence database.'; es = 'Los documentos serán descargados en la base de datos de Cleverence.'; ka = 'დოკუმენტები გამოტანდება Cleverence-ს ბაზაში.'; kk = 'Құжаттар Cleverence базасына жүктеледі.'; ro = 'Documentele vor fi descărcate în baza de date Cleverence.'; ru = 'Документы будут выгружены в базу Клеверенс.'; tr = 'Belgeler Cleverence veri tabanına yüklenecek.'; uk = 'Документи будуть вивантажені в базу Cleverence.'; uz = 'Ҳужжатлар Cleverence базасига юкланади.'") 
			+ Символы.ПС 
			+ НСтр("be = 'Увага: налады выбару карыстальнікаў і груп карыстальнікаў зададзены бізнес-працэсам, для змены - увайдзіце ў налады бізнес-працэсу: ""%1"".'; en = 'Attention: user and user group selection settings are set by the business process, to make changes - go to the business process settings: ""%1"".'; es = 'Atención: la configuración de selección de usuarios y grupos de usuarios está definida por el proceso de negocio, para modificarla, vaya a la configuración del proceso de negocio: ""%1"".'; ka = 'ყურადღება: მომხმარებლების და ჯგუფების არჩევის პარამეტრები დაყენებულია ბიზნეს პროცესით, შეცვლისთვის - შემოდით ბიზნეს პროცესის პარამეტრებში: ""%1"".'; kk = 'Түсінік: пайдаланушылардың және пайдаланушы топтарының таңдау параметрлері бизнес процесімен белгіленген, өзгерту үшін - бизнес процесі параметрлеріне кіріңіз: ""%1"".'; ro = 'Atentie: setarile de selectare a utilizatorilor si grupurilor de utilizatori sunt stabilite de procesul de afaceri, pentru a le modifica - accesati setarile procesului de afaceri: ""%1"".'; ru = 'Внимание: настройки выбора пользователей и групп пользователей заданы бизнес-процессом, для изменения - зайдите в настройки бизнес процесса: ""%1"".'; tr = 'Dikkat: Kullanıcı ve kullanıcı gruplarının seçim ayarları iş süreci tarafından belirlenmiştir, değiştirmek için iş süreci ayarlarına gidin: ""%1"".'; uk = 'Увага: налаштування вибору користувачів та груп користувачів задані бізнес-процесом, для зміни - зайдіть у налаштування бізнес процесу: ""%1"".'; uz = 'Diqqat: foydalanuvchi va guruhlar tanlash sozlamalari biznes jarayoni tomonidan belgilangan, o''zgartirish uchun - biznes jarayoni sozlamalariga kirib ko''ring: ""%1"".'");
		СтрокаУстройству = НСтр("be = 'Увядзіце ПІН-код прылады або выберыце прыладу з спісу для прызначэння дакументаў.'; en = 'Enter the PIN code of the device or select a device from the list to assign documents.'; es = 'Introduzca el código PIN del dispositivo o seleccione el dispositivo de la lista para asignar documentos.'; ka = 'შეიყვანეთ მოწყობილობის PIN-კოდი ან აირჩიეთ მოწყობილობა სიაში დოკუმენტების დანიშვნისთვის.'; kk = 'Құрылғының ПИН-кодын енгізіңіз немесе белгіленген тізімнен құжаттарды тағайындау үшін құрылғыны таңдаңыз.'; ro = 'Introduceți codul PIN al dispozitivului sau selectați dispozitivul din listă pentru a atribui documente.'; ru = 'Введите ПИН-код устройства или выберите устройство из списка для назначения документов.'; tr = 'Belgelerin atanması için cihazın PIN kodunu girin veya listeden bir cihaz seçin.'; uk = 'Введіть ПІН-код пристрою або виберіть пристрій зі списку для призначення документів.'; uz = 'Ҳужжатларни манзиллаштириш учун устройство ПИН-кодини киритинг ёки рўйхатдан устроймани танланг.'")
			+ " "
			+ НСтр("be = 'Дакументы будуць дасланы праз Інтэрнэт на прыладу.'; en = 'Documents will be sent over the Internet to the device.'; es = 'Los documentos serán enviados a través de Internet al dispositivo.'; ka = 'დოკუმენტები გაიგზავნება ინტერნეტით მოწყობილობაზე.'; kk = 'Құжаттар интернет арқылы құрылға жіберіледі.'; ro = 'Documentele vor fi trimise prin Internet pe dispozitiv.'; ru = 'Документы будут отправлены через Интернет на устройство.'; tr = 'Belgeler İnternet aracılığıyla cihaza gönderilecek.'; uk = 'Документи будуть відправлені через Інтернет на пристрій.'; uz = 'Ҳужжатлар интернет орқали қурилмага юборилади.'");
	Иначе
		СтрокаПользователям = НСтр("be = 'Выберыце карыстальніка альбо групу карыстальнікаў, каму будуць выгружаны дакумент.'; en = 'Select a user or group of users to whom the document will be exported.'; es = 'Elija un usuario o grupo de usuarios a quienes se exportarán los documentos.'; ka = 'აირჩიეთ მომხმარებელი ან ჯგუფი მომხმარებებს, ვისთვის გამოიტანება დოკუმენტი.'; kk = 'Документтерді шығаруға берілетін пайдаланушылар немесе топты таңдаңыз.'; ro = 'Alegeți utilizatorul sau grupul de utilizatori cărora li se vor descărca documentele.'; ru = 'Выберите пользователя или группу пользователей, кому будет выгружены документ.'; tr = 'Belgeyi dışa aktarılacak kullanıcı veya kullanıcı grubunu seçin.'; uk = 'Оберіть користувача або групу користувачів, кому буде вивантажено документ.'; uz = 'Hujjatlarni yuklab olish uchun foydalanuvchi yoki foydalanuvchilar guruhi tanlang.'") 
			+ " "
			+ НСтр("be = 'Дакумент будзе выгружаны ў базу Cleverence.'; en = 'Document will be uploaded to Cleverence database.'; es = 'El documento será descargado en la base de datos de Cleverence.'; ka = 'დოკუმენტი გამოტანდება Cleverence-ს ბაზაში.'; kk = 'Құжат Cleverence базасына жүктеледі.'; ro = 'Documentul va fi descărcat în baza de date Cleverence.'; ru = 'Документ будет выгружен в базу Клеверенс.'; tr = 'Belge Cleverence veri tabanına yüklenecek.'; uk = 'Документ буде вивантажений в базу Cleverence.'; uz = 'Ҳужжат Cleverence базасига юкланади.'") 
			+ Символы.ПС 
			+ НСтр("be = 'Увага: налады выбару карыстальнікаў і груп карыстальнікаў зададзены бізнес-працэсам, для змены - увайдзіце ў налады бізнес-працэсу: ""%1"".'; en = 'Attention: user and user group selection settings are set by the business process, to make changes - go to the business process settings: ""%1"".'; es = 'Atención: la configuración de selección de usuarios y grupos de usuarios está definida por el proceso de negocio, para modificarla, vaya a la configuración del proceso de negocio: ""%1"".'; ka = 'ყურადღება: მომხმარებლების და ჯგუფების არჩევის პარამეტრები დაყენებულია ბიზნეს პროცესით, შეცვლისთვის - შემოდით ბიზნეს პროცესის პარამეტრებში: ""%1"".'; kk = 'Түсінік: пайдаланушылардың және пайдаланушы топтарының таңдау параметрлері бизнес процесімен белгіленген, өзгерту үшін - бизнес процесі параметрлеріне кіріңіз: ""%1"".'; ro = 'Atentie: setarile de selectare a utilizatorilor si grupurilor de utilizatori sunt stabilite de procesul de afaceri, pentru a le modifica - accesati setarile procesului de afaceri: ""%1"".'; ru = 'Внимание: настройки выбора пользователей и групп пользователей заданы бизнес-процессом, для изменения - зайдите в настройки бизнес процесса: ""%1"".'; tr = 'Dikkat: Kullanıcı ve kullanıcı gruplarının seçim ayarları iş süreci tarafından belirlenmiştir, değiştirmek için iş süreci ayarlarına gidin: ""%1"".'; uk = 'Увага: налаштування вибору користувачів та груп користувачів задані бізнес-процесом, для зміни - зайдіть у налаштування бізнес процесу: ""%1"".'; uz = 'Diqqat: foydalanuvchi va guruhlar tanlash sozlamalari biznes jarayoni tomonidan belgilangan, o''zgartirish uchun - biznes jarayoni sozlamalariga kirib ko''ring: ""%1"".'");
		СтрокаУстройству = НСтр("be = 'Увядзіце ПІН-код прылады або выберыце прыладу з спісу для прызначэння дакументаў.'; en = 'Enter the PIN code of the device or select a device from the list to assign documents.'; es = 'Introduzca el código PIN del dispositivo o seleccione el dispositivo de la lista para asignar documentos.'; ka = 'შეიყვანეთ მოწყობილობის PIN-კოდი ან აირჩიეთ მოწყობილობა სიაში დოკუმენტების დანიშვნისთვის.'; kk = 'Құрылғының ПИН-кодын енгізіңіз немесе белгіленген тізімнен құжаттарды тағайындау үшін құрылғыны таңдаңыз.'; ro = 'Introduceți codul PIN al dispozitivului sau selectați dispozitivul din listă pentru a atribui documente.'; ru = 'Введите ПИН-код устройства или выберите устройство из списка для назначения документов.'; tr = 'Belgelerin atanması için cihazın PIN kodunu girin veya listeden bir cihaz seçin.'; uk = 'Введіть ПІН-код пристрою або виберіть пристрій зі списку для призначення документів.'; uz = 'Ҳужжатларни манзиллаштириш учун устройство ПИН-кодини киритинг ёки рўйхатдан устроймани танланг.'")
			+ " "
			+ НСтр("be = 'Дакумент будзе дасланы праз Інтэрнэт на прыладу.'; en = 'The document will be sent over the Internet to the device.'; es = 'El documento será enviado a través de Internet al dispositivo.'; ka = 'დოკუმენტი გაიგზავნება ინტერნეტით მოწყობილობაზე.'; kk = 'Құжат интернет арқылы құрылға жіберіледі.'; ro = 'Documentul va fi trimis prin Internet pe dispozitiv.'; ru = 'Документ будет отправлен через Интернет на устройство.'; tr = 'Belge İnternet aracılığıyla cihaza gönderilecek.'; uk = 'Документ буде відправлений через Інтернет на пристрій.'; uz = 'Ҳужжат интернет орқали қурилмага юборилади.'");				
	КонецЕсли;                                                                                           	
		
	СтрокаПользователям = СтрЗаменить(СтрокаПользователям, "%1", ИмяБизнесПроцесса);
		
	Элементы.НадписьПользователям.Заголовок = СтрокаПользователям;
	Элементы.НадписьНаУстройство.Заголовок	= СтрокаУстройству;
	
КонецПроцедуры

&НаКлиенте
Процедура ИзменитьФлажки(ЗначениеФлажка, СтрокиДляИзменения = Неопределено, СтрокиИсключения = Неопределено)		
	
	#Если ТолстыйКлиентОбычноеПриложение Тогда
		УстановитьЗначениеФлажкаВСтрокахДерева(ПользователиMS.Строки, ЗначениеФлажка, СтрокиДляИзменения, СтрокиИсключения);		
	#Иначе
		УстановитьЗначениеФлажкаВСтрокахДерева(ПользователиMS.ПолучитьЭлементы(), ЗначениеФлажка, СтрокиДляИзменения, СтрокиИсключения);		
	#КонецЕсли				
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьЗначениеФлажкаВСтрокахДерева(СтрокиДерева, ЗначениеФлажка, СтрокиДляИзменения = Неопределено, СтрокиИсключения = Неопределено)
	
	Для каждого Строка Из СтрокиДерева Цикл
		
		Если СтрокиИсключения <> Неопределено И СтрокиИсключения.Найти(Строка.ИдГруппыПользователя) <> Неопределено Тогда
			Продолжить;
		Иначе
			Если СтрокиДляИзменения = Неопределено Тогда
				Строка.Выгружать = ЗначениеФлажка;				
			Иначе
				Если СтрокиДляИзменения.Найти(Строка.ИдГруппыПользователя) <> Неопределено Тогда	
					Строка.Выгружать = ЗначениеФлажка;
				Иначе
					Продолжить;
				КонецЕсли;	
			КонецЕсли;				
		КонецЕсли;	
		#Если ТолстыйКлиентОбычноеПриложение Тогда
			УстановитьЗначениеФлажкаВСтрокахДерева(Строка.Строки, ЗначениеФлажка, СтрокиДляИзменения, СтрокиИсключения);
		#Иначе
			УстановитьЗначениеФлажкаВСтрокахДерева(Строка.ПолучитьЭлементы(), ЗначениеФлажка, СтрокиДляИзменения, СтрокиИсключения);
		#КонецЕсли 	
		
	КонецЦикла;	
	
КонецПроцедуры

&НаКлиенте
Процедура РазвернутьДерево()
	
	Если ВыгрузкаНаУстройство Тогда
		ТабличноеПоле 		= Элементы.ТаблицаУстройстваMS;		
	Иначе                                     
		ТабличноеПоле		= Элементы.ПользователиMS;		
	КонецЕсли;
	
	Строка = ТабличноеПоле.ТекущаяСтрока;
	Если Строка <> Неопределено Тогда
		ТабличноеПоле.Развернуть(Строка, Истина);
	КонецЕсли;
        
КонецПроцедуры

&НаКлиенте
Процедура СформироватьМассивПользователейMS(СтрокиДереваЗначений, МассивПользователейMS, Устройство = Ложь)
	
	Если Устройство Тогда
		
		СтрокаВыбораЗаполнена = Ложь;
		#Если ТолстыйКлиентОбычноеПриложение Тогда
			Если ТипЗнч(СтрокиДереваЗначений) = Тип("СтрокаДереваЗначений") Тогда
				Попытка
					СтрокаВыбораЗаполнена = ЗначениеЗаполнено(СтрокиДереваЗначений["ИдГруппыПользователя"]);
				Исключение                                                                    
					СтрокаВыбораЗаполнена = Ложь;
				КонецПопытки;
			ИначеЕсли ТипЗнч(СтрокиДереваЗначений) = Тип("КоллекцияСтрокДереваЗначений") Тогда
				СтрокаВыбораЗаполнена = СтрокиДереваЗначений.Количество();
			КонецЕсли;
		#Иначе
			Если ТипЗнч(СтрокиДереваЗначений) = Тип("ДанныеФормыЭлементДерева") Тогда
				СтрокаВыбораЗаполнена = СтрокиДереваЗначений.Свойство("ИдГруппыПользователя");
			ИначеЕсли ТипЗнч(СтрокиДереваЗначений) = Тип("ДанныеФормыКоллекцияЭлементовДерева") Тогда
				СтрокаВыбораЗаполнена = СтрокиДереваЗначений.Количество();
			КонецЕсли;
		#КонецЕсли
		
		Если СтрокаВыбораЗаполнена Тогда
			
			СтруктураПользователя = Новый Структура; 
			СтруктураПользователя.Вставить("Ид",  СтрокиДереваЗначений.ИдГруппыПользователя);
			СтруктураПользователя.Вставить("Имя", СтрокиДереваЗначений.ГруппаПользователь);			
			
			МассивПользователейMS.Добавить(СтруктураПользователя);			
			
			#Если ТолстыйКлиентОбычноеПриложение Тогда
				СформироватьМассивПользователейMS(СтрокиДереваЗначений.Строки, МассивПользователейMS);
			#Иначе
				СформироватьМассивПользователейMS(СтрокиДереваЗначений.ПолучитьЭлементы(), МассивПользователейMS);
			#КонецЕсли
			
		КонецЕсли;
		
		Возврат;
		
	КонецЕсли;
	
	Для Каждого Строка из СтрокиДереваЗначений Цикл
		
		Если Строка.Выгружать Тогда
			СтруктураПользователя = Новый Структура; 
			СтруктураПользователя.Вставить("Ид", 	Строка.ИдГруппыПользователя);
			СтруктураПользователя.Вставить("Имя", 	Строка.ГруппаПользователь);			
			МассивПользователейMS.Добавить(СтруктураПользователя);			
		КонецЕсли;
		
		#Если ТолстыйКлиентОбычноеПриложение Тогда
			СформироватьМассивПользователейMS(Строка.Строки, МассивПользователейMS);
		#Иначе
		 	СформироватьМассивПользователейMS(Строка.ПолучитьЭлементы(), МассивПользователейMS);
		#КонецЕсли
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ФормированиеТаблицыУстройствНазначенияВыгрузки(Устройство = Ложь)
	
	МассивПользователейMS = Новый Массив;
	
	ТекДанные = Элементы.ТаблицаУстройстваMS.ТекущиеДанные;
	Если ТекДанные <> Неопределено Тогда
		
		#Если ТолстыйКлиентОбычноеПриложение Тогда
			СформироватьМассивПользователейMS(ТекДанные, МассивПользователейMS, Устройство);			
			Если НЕ ОписаниеОповещенияОЗакрытии = Неопределено Тогда	
				ВыполнитьОбработкуОповещения(ОписаниеОповещенияОЗакрытии, МассивПользователейMS);
			КонецЕсли;
		#Иначе
			СформироватьМассивПользователейMS(ТекДанные, МассивПользователейMS, Устройство);
		#КонецЕсли	
		
	КонецЕсли; 
	
	МассивУстройствMSАдрес = ПоместитьВоВременноеХранилище(МассивПользователейMS);	
	Закрыть(МассивУстройствMSАдрес);	

КонецПроцедуры

#КонецОбласти

#Область ОписанияОповещений

&НаКлиенте
Процедура ПринятьПинКодЗавершение(ДополнительныеПараметры) Экспорт
	
	// Очищаем пин-код после некорректного ввода
	ПинКод 										= "";	
	Элементы.ТаблицаУстройстваMS.Доступность 	= Истина;		
	ПинКодВведен 								= Ложь;
	#Если Не ТолстыйКлиентОбычноеПриложение Тогда	
		Элементы.ПинКод.ОбновитьТекстРедактирования();
	#КонецЕсли
	
КонецПроцедуры

#КонецОбласти

#Если ТолстыйКлиентОбычноеПриложение Тогда

	Элементы = ЭлементыФормы;
	
#КонецЕсли