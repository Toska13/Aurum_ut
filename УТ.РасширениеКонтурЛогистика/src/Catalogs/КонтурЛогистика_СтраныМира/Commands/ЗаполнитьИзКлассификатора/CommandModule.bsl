
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	ПерезаполнитьЗначениямиПоУмолчаниюНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПерезаполнитьЗначениямиПоУмолчаниюНаСервере()
	
	ОбновитьНаименования = Ложь;
	Справочники.КонтурЛогистика_СтраныМира.ЗаполнитьЗначениямиПоУмолчанию(ОбновитьНаименования);
	
КонецПроцедуры
